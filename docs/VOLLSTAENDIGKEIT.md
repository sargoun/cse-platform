# Vollständigkeitsregister — was die Plattform *nicht* kann

**Aufgenommen am 21.09.2026.** Grundlage: ein achtfacher Suchlauf über den
gesamten Baum (Arbeiterportal · Zeitkette · Erreichbarkeit · Dienste ohne
Knopf · Routen ohne Aufrufer · Platzhalter · Zustände ohne Erzeuger ·
Verwaltungslücken), jeder Befund anschließend gegengeprüft: *Gibt es eine
dokumentierte Begründung für das Fehlen?* Sechs Befunde wurden so **widerlegt**
und stehen deshalb nicht in der Liste — sie sind bewusste, begründete
Auslassungen (§ 9).

**136 bestätigte Befunde** aus dem Suchlauf: 75 *blockiert* · 59 *behindert* · 2 *Schönheit*. Dazu **fünf nachgetragene** (§ 8a), die beim Bauen auffielen.

Ein Befund ist keine Meinung. Jeder trägt Datei und Zeile, und jeder wurde mit
einer Gegenprobe belegt — meist ein `grep`, der zeigt, dass der gesuchte
Schreibweg **nirgends** existiert.

## Was die drei Schweregrade bedeuten

| Grad | Bedeutung |
|---|---|
| **blockiert** | Ein Vorgang, den der Betrieb braucht, ist **gar nicht** ausführbar. Kein Umweg. |
| **behindert** | Es geht, aber nur über einen Umweg, den niemand von selbst findet (Adresszeile, Seed, `psql`). |
| **Schönheit** | Gebaut, korrekt, ungenutzt. Kein Betriebsschaden. |

## Wie dieses Blatt geführt wird

Jede Zeile hat eine Nummer `V-NNN`. Die Nummer wird **nie wiederverwendet**.
Ist ein Befund behoben, wird die Zeile **nicht gelöscht**, sondern auf
`erledigt` gesetzt, mit dem Commit daneben. Ein Register, aus dem man Zeilen
entfernt, ist nach drei Wochen eine Liste der Dinge, an die sich jemand
erinnert hat.

---

## 1. Was sich nicht anlegen lässt

Der schwerste Block. Diese Zeilen entstehen **ausschließlich im Seed** — d. h.
die Plattform kann heute nur das verwalten, was die Demo-Daten ihr
mitgegeben haben. Ein echter Betrieb kann nicht anfangen.

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-001 | **Objekt** (Gebäude/Gelände) | `src/app/portal/[mandant]/objekte/neu/page.tsx:21` ist ein ausdrücklicher Platzhalter; `insert into objekt` nur in `src/server/db/seed/operations.ts:459` | blockiert | **erledigt** — D-619, `services/objekt/anlegen.ts`, `api/objekt`, Knopf auf der Liste |
| V-002 | **Revier** (Reinigungsfläche) | `src/app/portal/[mandant]/reinigung/reviere/neu/page.tsx:25` Platzhalter; `src/server/services/reinigung/revier.ts:42` nennt „anlegen" und „archivieren" als Lösung — beide gibt es nicht | blockiert | **erledigt** — `legeRevierAn` / `aendereRevier` / `archiviereRevier`, `api/reinigung/reviere`, zweisprachige Seite und Knopf auf der Liste; 18 Prüfungen in `tests/isolation/revier-anlegen.test.ts` |
| V-003 | **Bauprojekt** | `src/server/db/seed/bau.ts:564`; `src/app/api/auftrag/route.ts:126` kennt keinen Anlegezweig — zwanzig gebaute Projektseiten sind für neue Vorhaben unerreichbar | blockiert | **erledigt** — `services/bau/projekt.ts`, `api/bau/projekte`, zweisprachige Seite und Knopf; Nummer und Kunde kommen aus dem AUFTRAG (O-351), die Vertragsgrundlage hat bewusst keinen Vorgabewert; 15 Prüfungen in `tests/isolation/projekt-anlegen.test.ts` |
| V-004 | **Veranstaltung** (Security) | `src/server/db/seed/security.ts:356` — der dritte Einsatz-Ursprung ist tot | blockiert | **erledigt** — `services/security/veranstaltung-anlegen.ts`, `api/security/veranstaltungen`, zweisprachige Seite und Knopf; Ort als Objekt ODER Text, Zeiten als Berliner Wanduhrzeit über `at time zone` (Invariante 2); 18 Prüfungen in `tests/isolation/veranstaltung-anlegen.test.ts` |
| V-005 | **Angebot für Security und Bau** | `src/app/api/angebot/route.ts:39` — der einzige Entstehungsweg ist die Reinigungs-Kalkulation aus dem Raumbuch | blockiert | **erledigt** — `services/angebot/von-hand.ts`, `api/angebot/von-hand` (eigenes Recht `angebot.schreiben`, nicht `angebot.versenden`), zweisprachige Maske auf `/angebote/neu` statt des Platzhalters. Steuersatz, Kennzeichen und Befreiungsgrund kommen am Stichtag aus `steuersatz_gruppe`, die Einheit aus `masseinheit`, die Zeilensumme aus der Datenbank; alles wird geprüft, bevor irgendetwas geschrieben wird (eine Angebotsposition lässt sich nicht löschen). 28 Prüfungen in `tests/isolation/angebot-von-hand.test.ts` |
| V-006 | **Lieferant** | `src/app/portal/[mandant]/finanzen/eingangsrechnungen/neu/page.tsx:271` hat ein Pflichtfeld „Lieferant", dessen Tabelle niemand befüllen kann | blockiert | **erledigt** — `/portal/[mandant]/finanzen/lieferanten` mit Liste, Blatt und Formular, dazu `POST /api/finanzen/lieferanten` (anlegen · ändern · sperren · entsperren · archivieren). **Weder Migration noch Recht nötig:** `lieferant` trug seit `0123` Policy (`t_mandant`, `eingang.schreiben`), Grant, Nummernindex, IBAN-Prüfung, §48-Datum und einen Auslöser gegen Rechnungsbetrug — und keinen einzigen Erzeuger. Die Nummer kommt **nicht** aus dem Nummernkreis (K-12, wie `kunde` und `lead`): die lückenlose Kette gehört Rechnungen, ein Stammsatz ist kein Beleg, und den Rechnungszähler dafür zu benutzen hiesse, ihn von aussen auslösbar zu machen — eine eigene Prüfung hält fest, dass er dabei stillsteht. Die IBAN wird auf **Prüfziffer** geprüft (`ibanGeprueft`, Modulo 97), nicht nur auf Gestalt: ein Zahlendreher ergibt sonst eine gültig aussehende IBAN, die an den Falschen geht. Die Archivierung verweigert sich bei offenen Eingangsrechnungen — eine offene Zahlung zeigte sonst auf einen Empfänger, den niemand mehr auswählen kann. **Vom Spaltenentzug gefunden:** `iban = coalesce($n, iban)` schien harmlos und scheiterte mit „permission denied" — auch die rechte Seite eines `set` LIEST die Spalte, und `cse_app` hält auf ihr kein `select`. Die Zuweisung wird jetzt weggelassen statt umschrieben; das Formular kann die IBAN ohnehin nicht vorbefüllen, und blindes Überschreiben wäre der stille Weg, einen Lieferanten unbezahlbar zu machen. 17 Prüfungen in `tests/isolation/lieferant-anlegen.test.ts` |
| V-007 | **Bankkonto** | `src/server/services/finanz/zahlung/index.ts:103` — „Eingegangen auf" kennt nur Seed-Konten | blockiert | **erledigt** — `/portal/[mandant]/finanzen/bankkonten` mit Liste und Formular; `legeBankkontoAn` gibt es seit `0121` mit Prüfzifferprüfung und eigener Policy und wurde von nichts ausser Tests gerufen. Die IBAN wird in jede festgeschriebene Rechnung eingefroren (BT-85) und ist danach unveränderlich — ein Zahlendreher stünde auf jeder von ihnen, weshalb Modulo 97 vor dem Speichern läuft und nicht als CHECK |
| V-008 | **Stundenkonto** | `src/server/services/zeit/stundenkonto.ts:313` — `eroeffneKonto` hat weder Route noch Knopf; der angekündigte Rollover-Lauf ist nicht registriert | blockiert | **erledigt** — der Lauf `konten_rollover` (`jobs/kontenRollover.ts`, täglich 00:30) öffnet je aktiver Anstellung das Konto des laufenden BERLINER Monats und setzt den Vortrag aus einem gesperrten Vormonat; `schliesseMonatAb` trägt den Saldo beim Sperren selbst weiter (`uebertrageSaldo`, mit Jahreswechsel). Ein gesperrter Folgemonat bleibt unberührt. Keine Sollzeit erfunden (O-18). 12 Prüfungen in `tests/isolation/konten-rollover.test.ts` |
| V-009 | **Urlaubskonto und Urlaubsanspruch** | `src/server/services/zeit/urlaubskonto.ts:236` — beide Schreibfunktionen ohne Aufrufer | blockiert | **erledigt mit V-117 und V-118** — dieselben zwei Funktionen, dort einzeln nachgetragen: `eroeffneUrlaubskonto` ruft seither der Nachtlauf `urlaubskonten_jahr` (V-117), `setzeAnspruch` das Formular unter `/personal/urlaubskonten` über `POST /api/personal/urlaubsanspruch` (V-118). Der Eintrag bleibt stehen und wird nicht gestrichen: ein Register, aus dem Zeilen verschwinden, lässt sich nicht mehr gegen sich selbst prüfen |
| V-010 | **Qualifikationsnachweis** (Sachkunde § 34a, Führungszeugnis, Erste Hilfe) | `drizzle/0030_nachweis_qualifikation.sql:590`, `src/server/db/seed/qualifikation.ts:268` — das Register kann warnen, nie aufnehmen. `0030` baut es vollständig: drei Warnstufen, Quittungsbuch, Dokumentpflicht als Auslöser, nächtlicher Statuslauf, Einsatztor. Die Plattform konnte vor einer ablaufenden Sachkunde warnen und eine Schicht ohne sie sperren — **und die Zeile, vor der sie warnt, entstand nirgends ausser im Seed**. Für ein Sicherheitsunternehmen ist das kein fehlendes Formular, sondern ein Register ohne Eingang | blockiert | **erledigt** — `services/nachweis/aufnahme.ts` (aufnehmen, bestätigen, widerrufen), `POST /api/personal/nachweise`, `/personal/nachweise/erfassen` und die Prüfung auf dem Einzelblatt. **Der Nachweis hängt am MENSCHEN** (Invariante 9, D-09): `person_id`, nie `anstellung_id` — wer in zwei Gesellschaften arbeitet, hat seine Sachkunde einmal; `erfasst_von_mandant_id` trägt, WER aufgenommen hat, nicht wer sehen darf, und die Policy setzt ihn aus der Sitzung. **Die Frist kommt aus der Qualifikation, nicht aus dem Formular:** bleibt „gültig bis" leer und trägt die Qualifikation eine Standardgültigkeit, wird sie gerechnet — in SQL und in KALENDERmonaten, weil `date + interval` das kann und `Date` in einer UTC-Sitzung den Tag verschiebt. Was nicht abläuft, bekommt kein Datum: leer heißt dort unbefristet und nicht vergessen, und ein erfundenes Ende machte aus einer dauerhaften Befähigung eine, die eines Tages lautlos sperrt. **Der Zeuge kommt aus der Sitzung** — `geprueft_von` setzt die Datenbank, nicht das Formular (dieselbe Regel wie beim Modellregister, V-120). Widerrufen wird mit Grund, gelöscht wird nichts (Invariante 8), und ein widerrufener Nachweis lässt sich nicht mehr bestätigen. Die Dokumentpflicht (DOC-01) wird vor dem Auslöser gefragt, damit am Formular ein Satz mit dem NAMEN der Qualifikation steht und kein Auslösertext. 15 Prüfungen in `tests/isolation/nachweis-aufnehmen.test.ts` |
| V-011 | **Betriebsausgabe** (FIN-14/FIN-17) | `drizzle/0180_ausgabe.sql:646`, `src/server/services/finanz/ausgabe.ts:33` — reine Leseansicht: weder erfassen noch freigeben, buchen, ablehnen. `0180` baut vier Zustände, den Übergangsauslöser, die Belegpflicht ab `freigegeben`, die Unveränderlichkeit ab `gebucht` und die Steueraufteilung je Satzgruppe — und **zwischen Tabelle und Oberfläche stand kein Dienst**. Eine Tankquittung, eine Parkgebühr, Material aus dem Baumarkt: im Betrieb der häufigste Beleg überhaupt, und er liess sich nicht erfassen | blockiert | **erledigt** — `services/finanz/ausgabe-schreiben.ts` (erfassen, freigeben, ablehnen, buchen), `POST /api/finanzen/ausgaben`, `/finanzen/ausgaben/erfassen` und die Entscheidung auf dem Einzelblatt. **Zwei Rechte und nicht eines:** erfassen ist Belegarbeit (`eingang.schreiben`), freigeben/ablehnen/buchen sind Entscheidungen über Geld (`eingang.freigeben`) — wer eine Quittung eintippt, gibt sie nicht schon deshalb frei, und genau das ist das Vieraugenprinzip. **Das Formular verlangt NETTO je Satz**, und das ist eine Zumutung mit Grund: aus dem Bruttobetrag liesse sich der Satz nicht zurückrechnen, ohne einen Mischsatz zu erfinden (Invariante 1); auf dem Bon steht beides. Die Steuer rechnet `anteilInBasisPunkten`, eine geprüfte Funktion (Invariante 6), und Satz wie Kategorie werden auf der Zeile EINGEFROREN — eine spätere Satzpflege ändert keinen erfassten Beleg. Gebucht entstehen Zustand und Buchungssatz in DERSELBEN Transaktion (`bucheAusgabe` in `buchhaltung/buchungssatz.ts`); ein Nachlauf hinterliesse gebuchte Ausgaben, die in keiner Buchführung stehen. Fehlt eine Kontenzuordnung, entsteht die Zeile trotzdem — ohne Konto, mit Hinweis, in der Arbeitsliste (V-126, D-425). 14 Prüfungen in `tests/isolation/ausgabe-schreiben.test.ts` |
| V-012 | **Einzelner Raum** | `src/server/services/raumbuch/raum.ts:277` — `speichereRaum` ändert nur Vorhandenes; der einzige Weg ist ein CSV-Import | behindert | **erledigt** — `legeRaumAn` im selben Dienst, `aktion=anlegen` auf `api/raum` (dasselbe Recht `objekt.schreiben`, keine zweite Adresse), zweisprachige Maske `/objekte/[id]/raumbuch/neu` und ein Knopf im Raumbuch. **Die Regeln kommen aus derselben Quelle wie beim Ändern** — Nummer ODER Bezeichnung, Fläche > 0, deutsche Zahl, und die Eindeutigkeitsverstösse über `alsRaumFehler` als Satz statt als Fremdschlüsselcode; ein zweiter Regelsatz für dasselbe Formular gewänne beim ersten Unterschied, ohne dass es jemand merkt. **`quell_schluessel` bleibt leer:** er ist der stabile Schlüssel des IMPORTEURS, und einen zu erfinden hiesse, dass der nächste Import diesen Raum für seinen hält und überschreibt. **Ein archiviertes Objekt bekommt keine neuen Räume** — die RLS verhinderte das nicht, der Fremdschlüssel besteht weiter, und der Raum wäre angelegt und unauffindbar. Der Dateiimport bleibt der Weg für ein ganzes Gebäude und steht als Hinweis auf der Seite. 19 Prüfungen in `tests/isolation/raum-anlegen.test.ts` |
| V-013 | **Einzelne Schicht** | `src/server/services/dienstplan/generator.ts:230` — ein Einsatz entsteht nur aus Serie oder Veranstaltung. `einsatz.quelle` kennt seit `0028` den Wert `manuell`, und `kern.einsatz_quell_schluessel_setzen` vergibt einer schlüssellosen Zeile eigens `manuell:<id>` — **die Datenbank war vorbereitet, angelegt hat so eine Zeile nie jemand**. Eine Grundreinigung am Samstag, eine zusätzliche Wache für eine Nacht, ein Einsatz nach einem Wasserschaden: im Betrieb die häufigste Planung überhaupt, und der einzige Weg dorthin war eine Serie, die ab Montag weitergeneriert | blockiert | **erledigt** — `services/dienstplan/einzelschicht.ts` (anlegen und absagen), `POST /api/dienstplan/einsatz`, `/dienstplan/einsatz/neu`, der Absageblock auf dem Schichtblatt und der Eingang aus der Tagesansicht (mit dem angezeigten Tag im Verweis). **Die Instants entstehen IN DER ANWEISUNG** (Invariante 2): `app.loese_ortszeit` rechnet Wanduhr → Zeitpunkt, und das Ende bekommt seinen EIGENEN Aufruf — es ist ein eigener Wanduhr-Anker und nicht der Anfang plus eine Dauer, sonst wäre die Schicht in der Umstellungsnacht eine Stunde zu lang. Dieselbe Rechnung wie im Generator, nicht eine zweite. **`kunde_id` und `quell_schluessel` schreibt die Datenbank**, nicht der Aufrufer: auf der ersten stehen die Kundendecke und `t_kunde`. Abgesagt wird mit Grund, nie durch Löschen (Invariante 8) — und nicht mehr, sobald Zeit erfasst ist, weil die Stunde sonst im Lohnlauf niemandem mehr zuzuordnen wäre. Die vier von CLAUDE.md vor jeder Planungsoberfläche verlangten Prüfungen stehen namentlich in `tests/isolation/einzelschicht.test.ts` §2: 22:00–06:00 (8 h), DST-Vorstellung (dieselbe Schicht 7 h), DST-Rückstellung (9 h) und zehn Schichten zum selben Zeitpunkt auf EINEM Objekt (zehn Zeilen, zehn Schlüssel — es gibt absichtlich keine Ausschlussbedingung, Überschneidung ist ein Befund). 20 Prüfungen |
| V-014 | **Telefonanmeldung einer Arbeiterin** (`mitarbeiter_zugang`) | `drizzle/0113_mitarbeiter_zugang.sql:193` — weder anlegen noch sperren noch entsperren. Fünf Migrationen bauen um EINE Zeile herum: `0113` legt sie an, `0114` meldet damit an, `0130` bremst das Raten, `0143` stellt den Code durch die Einsatzleitung aus, `0144` zeigt den Stand — **und diese Zeile entstand nirgends ausser im Seed**. Wer nach dem Seed eingestellt wird, also jeder echte Mensch, hatte keine Anmeldenummer und keinen Weg zu einer; die Seite sagte ihm „die Super-Administration trägt die Nummer an der Person ein", und die konnte es nicht (`person.telefon` ist die Personalakte, angemeldet wird mit `mitarbeiter_zugang.telefon_e164`) | blockiert | **erledigt** — `services/personal/zugang.ts` (einrichten, Nummer umschreiben, sperren, entsperren), `POST /api/personal/zugang`, die vier Formulare auf `/personal/personen/[id]/zugang`, `drizzle/0384`. **Drei Befunde kamen beim Bauen dazu, alle drei in der Migration selbst:** (1) `mitarbeiter_zugang` trug **keinen einzigen Auslöser** — `0113` begründet das fehlende Spaltenrecht auf `erstellt_von`/`geaendert_von` mit „die schreibt der Audit-Trigger", den es nie gab. Damit war AUT-08 auf dem sicherheitsrelevantesten Schreibweg der Plattform unerfüllt: wer die Nummer eines Zugangs umschreibt, empfängt ab dem nächsten Code dessen Anmeldungen, und es stand nirgends. `0384` registriert die Tabelle in `AUDITIERT` und `GEAENDERT_AM` und erteilt die zwei Spaltenrechte; der Dienst setzt den Handelnden aus der Sitzung wie überall sonst. (2) `t_zugang_anlegen` prüfte **nicht**, ob der Mensch in dieser Gesellschaft beschäftigt ist — anders als `t_zugang_lesen` und die beiden Definer-Funktionen daneben; ein so angelegter Zugang war danach unsichtbar und blockierte über `unique (person_id)` die Gesellschaft, die den Menschen wirklich beschäftigt. (3) Mit `AUDITIERT` kommt die Löschsperre (`archiv`): eine gelöschte Zeile nähme den Anker jeder Anmeldung mit — `0113` sagt es selbst („Austritt ist KEIN Löschgrund"), die Löschentscheidung führte sie bis dahin als `sperre: keine`. **Das Sperren braucht ausdrücklich kein zweites Augenpaar** — es ist der Notknopf, und `app.zugang_code_einloesen` findet einen gesperrten Zugang gar nicht mehr, ein schon ausgestellter Code ist mit der Sperre wertlos. Ob das UMSCHREIBEN vier Augen braucht, bleibt offen (O-86) und wird nicht erfunden; die Seite sagt es hin. Die Seite fiel dabei aus der Ausnahmeliste der Übersetzungswache (de/en), und `app.zugang_stand` nennt jetzt auch den Sperrgrund. 24 Prüfungen in `tests/isolation/zugang-verwalten.test.ts`, sieben davon gegen die ROLLE |
| V-015 | **Agentenbudget** | `src/app/portal/[mandant]/agenten/budget/page.tsx:97` rein lesend — ohne Budget läuft kein Agent. `0128` baut Preisliste, Reservierung, Verbrauch in Mikrocent, Hartstopp und Stoppmeldung; die Seite zeigt alles. **Und `budget_cent` blieb NULL**, worauf `app.agent_budget_pruefen` mit `budget_fehlt` antwortet: es lief kein einziger Agent | blockiert | **erledigt** — `services/agent/budget-pflege.ts`, `POST /api/agenten/budget`, das Formular auf der Budgetseite, `drizzle/0385`. **Ein zweiter Befund kam beim Bauen dazu, und er ist der schwerere:** `0128` legt die fünf Agententabellen in EINER Schleife an und gibt allen dieselbe Schreibbedingung `agent.aufgabe_starten` — ein Recht, das auch eine `leitung` hält. **Wer einen Agenten starten durfte, durfte seine eigene Obergrenze heraufsetzen**; AGT-05 verlangt eine Obergrenze, und eine, die der Begrenzte selbst verstellt, ist keine. `agent.budget_verwalten` stand seit `0008` im Katalog, gebunden an `super_admin`, bindbar für `admin` — und war an keine Policy und an keinen Code gebunden. `0385` zieht die Schreibpolicy auf `agent_budget` darauf nach; gelesen wird weiter unter `agent.lesen`, denn eine Grenze, die man nicht kennt, ist als Grenze wertlos. **Geld ist ganzzahliger Cent** (Invariante 1): die Eingabe läuft durch `parseGeld`, dieselbe geprüfte Funktion wie bei jeder Rechnung — `1.234` ist eintausendzweihundertvierunddreissig Euro und wird nicht als „eins Komma zwei drei vier" geraten. Null ist erlaubt (eine Entscheidung), negativ nicht (ein Versehen), und eine gesetzte Grenze wird nicht wieder zu NULL: „niemand hat entschieden" ist kein Wert, den man zurücksetzt. **Die HÖHE erfindet die Plattform nicht** — kein Vorgabewert, kein Vorschlag (O-26), und die Warnschwelle bleibt leer (O-195); ein Vorschlag in einer Finanzmaske sieht aus wie eine Abstimmung. Ein neues Budget hebt einen gesetzten Stopp auf, sonst bliebe eine Gesellschaft gestoppt, deren Budget gerade erhöht wurde. 17 Prüfungen in `tests/isolation/agent-budget-pflege.test.ts` — eine davon fand, dass ein von der Policy abgewiesener INSERT WIRFT und nicht null Zeilen gibt: der Dienst hätte einen 500er geliefert, wo „dir fehlt dieses Recht" die Wahrheit ist |
| V-016 | **Radar-Suchprofil** | `src/server/services/radar/profil.ts:488` — nur ändern, nicht anlegen | behindert | **erledigt** — `legeProfilAn` im selben Dienst, `was=anlegen` auf `api/radar/profil` (dasselbe Tor `radar.profil_schreiben`, keine zweite Adresse) und ein Formular auf `/radar/profile`. **Das Formular fragt EIN Feld:** was das Profil suchen soll — CPV, Region, Stichwörter, Wertgrenzen — steht auf dem Profilblatt, und dort steht neben jedem gesperrten Feld sein Grund (O-15, O-47, O-98, O-191, O-721); dieselben Felder ein zweites Mal wären ein zweiter Ort, an dem diese Sätze zu pflegen sind. **Das neue Profil ist ABGESCHALTET, und das ist die eigentliche Zusage:** die Spalte steht auf `default true`, und ein leeres aktives Profil bekäme in der nächsten Nacht für JEDE Bekanntmachung in Euro das volle Wert- und Fristkriterium — `bewerteWert` sagt „Der Auftragswert liegt im Rahmen des Profils", wenn das Profil keine Grenzen führt. Die Rangfolge am Morgen wäre eine, der jemand glaubt und die nichts aussagt. Eingeschaltet wird auf dem Profilblatt, über den Weg, den es schon gab. **Ein von der Policy abgewiesener INSERT WIRFT** (anders als ein UPDATE, das null Zeilen gibt) — der Dienst fängt genau diese Meldung und macht einen Satz daraus; alles andere wird nicht als Rechtefrage verkleidet. Keine Eindeutigkeit über den Namen erfunden: `radar_profil` führt keine, und eine hier wäre eine Regel, die niemand aufgestellt hat. 8 Prüfungen in `tests/isolation/radar-profil-schreiben.test.ts` §9 |

---

## 2. Was sich nicht mehr ändern lässt

Angelegt und danach in Stein. Kein Tippfehler ist korrigierbar.

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-017 | **Kundenstammdaten** | `src/server/services/crm/anlegen.ts:77` — kein schreibender Änderungsdienst | blockiert | **erledigt** — `services/crm/aendern.ts`, `api/crm/kunde` (`aktion=kunde_aendern`) |
| V-018 | **Kunde archivieren** | `drizzle/0020_crm_identitaet.sql:176` — Spalte da, nichts schreibt sie | behindert | **erledigt** — `archiviereKunde`, weist bei laufenden Aufträgen ab |
| V-019 | **Ansprechpartner korrigieren / als ausgeschieden markieren** | `drizzle/0020_crm_identitaet.sql:261` — nur die Rechtsgrundlage ist änderbar | behindert | **erledigt** — `aendereKontakt` + `scheideKontaktAus` |
| V-020 | **Objekt bearbeiten oder archivieren** | `src/app/portal/[mandant]/objekte/[id]/page.tsx:148` zeigt „Archiviert" an — nichts kann den Zustand erzeugen | blockiert | **erledigt** — D-619/D-620, `objekte/[id]/bearbeiten` |
| V-021 | **Planungsserie ändern, beenden, archivieren** | `src/server/services/dienstplan/serie.ts:226` — `serie.ts` legte Serien an, `leseSerie` zeigte sie, `legeAusnahmeAn` setzte Ausnahmen für einzelne Tage, **und keine Zeile änderte je eine bestehende Serie**. `planungsserie.archiviert_am` stand seit `0028` da, und `ps_carrier_uk` ist eigens partiell darauf — geschrieben hat es nie jemand. Ein Turnus, dessen Beginn sich um eine halbe Stunde verschiebt, war nicht zu korrigieren; ein Vertrag, der zum Quartalsende ausläuft, nicht zu beenden | blockiert | **erledigt** — `services/dienstplan/serie-pflege.ts`, `POST /api/dienstplan/serien/[id]` und vier Formulare auf dem Serienblatt. **Drei Handlungen, die ausdrücklich nicht dasselbe sind:** die REGEL liegt auf dem Träger (`turnus`/`posten`) — `planungsserie` ist das Ausführungsprotokoll des Generators und trägt weder RRULE noch Uhrzeit; der LAUF (Horizont, Feiertage, Bundesland) gehört der Serie; BEENDEN ist ein Datum, ARCHIVIEREN ein Zustand. **Der teuerste Befund steckte in `app.planungsbedarf`:** es filtert `gueltig_bis >= p_von`, ein beendeter oder archivierter Träger fällt also aus der Abfrage — der Generator sieht ihn nicht mehr und `storniereVerwaiste` läuft für ihn nie. Die Schichten der nächsten acht Wochen wären für eine Serie stehen geblieben, die es nicht mehr gibt; das Absagen steht darum im Dienst und nicht im Generator. Der Träger überlebt das Archivieren mit Absicht (ein Posten ist Objektschutz und kein Zeitplan), und weil `ps_carrier_uk` partiell ist, lässt sich für ihn später eine neue Serie anlegen. Nie angefasst wird eine Schicht mit erfasster Zeit oder eine, die schon begonnen hat. Jede der drei Handlungen schreibt eine Auditzeile. 22 Prüfungen in `tests/isolation/serie-pflege.test.ts` — eine davon fand einen Laufzeitfehler, den kein Typcheck sehen kann: ein Parameter ohne ausgeschriebenen Typ, den Postgres erst beim Ausführen mit „could not determine data type" abweist |
| V-022 | **Verwaltungskonto entziehen oder deaktivieren** | `src/app/portal/[mandant]/einstellungen/benutzer/[id]/page.tsx:32` — einladen ja, zurücknehmen nie | blockiert | **erledigt** — `app.konto_deaktivieren` und `app.konto_reaktivieren` (`drizzle/0379`), auf dem Benutzerblatt als „Zugang entziehen" und „Zugang wiedergeben". **Kein Löschen** (Invariante 8): `audit_log` benennt dieses Konto als Handelnden, dauerhaft. Der Rückweg geht auf `aktiv` und nicht auf `eingeladen` — die Anmeldung besteht, entzogen war nur der Zugang. Das eigene Konto ist ausgenommen: wer sich selbst entzieht, sperrt sich in derselben Sekunde aus |
| V-023 | **Rollen und Rechte pflegen** | `src/app/portal/[mandant]/einstellungen/rollen/[rolle]/page.tsx:135` zeigt „Abweichung dieser Gesellschaft" — keine Oberfläche erzeugt sie | behindert | **erledigt** — `services/system/rollenrecht.ts`, `POST /api/einstellungen/rollenrecht` und die Matrix als EIN Formular auf dem Rollenblatt. **Alles war gebaut ausser dem Weg:** `0008` legt `mandant_id` an die Bindungszeile, macht `gewaehrt` zum BOOLEAN statt blosser Existenz (damit sich ENTZIEHEN von „nie etwas eingestellt" unterscheiden lässt), löst in `app.hat_recht` „mandantenspezifisch vor Plattformvorgabe" auf und schreibt drei Policies plus das restriktive `aal2`-Gate; die Seitenkarte nennt die Matrix wörtlich „editable per mandant". Das Beispiel aus dem Tabellenkopf von `0008` — „`finanzen.lesen` der `leitung` in `bau` zu entziehen darf in `reinigung` nichts ändern" — war nicht durchführbar. **Die ganze Matrix in EINEM Absenden**, nicht ein Formular je Zeile: sie wird als Ganzes gelesen und als Ganzes entschieden, und zwei Anfragen daraus zu machen hiesse, dass zwischen ihnen ein Zustand steht, den niemand gewollt hat. **Vier Wände, jede gegen einen benannten Fall:** (1) nur die vier gesellschaftsgebundenen Systemrollen — `super_admin` ist die Rolle der PLATTFORM, und `app.hat_recht` wertet für sie eine mandantengebundene Zeile mit aus; eine Gesellschaft könnte der Plattformverwaltung sonst in ihrem Bereich Rechte entziehen. (2) Nur Zellen, die die Matrix aus 03-AUTH §12 kennt (gebunden oder bindbar) — der Katalog sagt selbst, `bindbar` seien „Rollen, für die eine Bindung IN DER UI angelegt werden darf"; eine leere Zelle hier zu füllen hiesse, die Matrix an einem zweiten Ort umzuschreiben. (3) Kein `nur_global`-Recht: `app.hat_recht` verlässt die Auflösung dafür VOR der Mitgliedschaftsrolle, die Abweichung bewirkte nichts — und das Rollenblatt zeigte „gilt: ja" über ein Recht, das die Plattform verweigert. (4) **Keine Selbstaussperrung, und zwar nicht nachgedacht, sondern nachgefragt:** nach allen Schreibvorgängen wird `app.hat_recht('system.rolle_verwalten', …)` in derselben Transaktion noch einmal gestellt; steht dort `false`, rollt alles zurück. Der Trigger `kern.rolle_berechtigung_pruefen` prüft VOR der Zeile und sieht diesen Fall nicht — er greift erst bei der NÄCHSTEN Anweisung. **Nebenbefund, mitgenommen:** derselbe Trigger trägt SEC-A3 („der Vergebende hält es selbst nicht") und die Aussperrsperre des `super_admin` — beide warfen `insufficient_privilege` bis in die Route, also einen 500er dort, wo „du kannst nicht vergeben, was du selbst nicht hast" die Wahrheit ist; der Dienst übersetzt die drei Meldungen und baut die Regeln ausdrücklich NICHT nach. **Zurück zur Plattformvorgabe gibt es nicht** — `0008` erteilt `cse_app` kein `delete` —, und die Seite sagt es hin statt es zu verschweigen. Offen bleibt O-904 (eine Rolle, die es nur in EINER Gesellschaft gibt, steht in keiner Spalte der Matrix); sie wird abgewiesen, nicht geraten. 24 Prüfungen in `tests/isolation/rollenrecht.test.ts` |
| V-024 | **Abrechnungskonfiguration beenden** | `src/app/api/abrechnung/route.ts:161` — `aktion=beenden` existiert in der Route, in keinem Formular | behindert | **erledigt** — ein Formular je LAUFENDER Konfiguration auf `/auftraege/[id]/abrechnung`, mit `min` auf dem Gültigkeitsbeginn und dem Satz, dass der Tag mitzählt. Eine Abrechnungsart, die einmal galt, galt sonst für immer: die Reihe konnte nur wachsen. **Nebenbefund, mitgenommen:** die Route antwortete auf jede Abweisung mit JSON, obwohl sie ausschliesslich `formData` liest — `{"fehler":"keine_abrechnungsart"}` als weisse Seite für einen Menschen, der auf einen Knopf gedrückt hat. Sie kehrt jetzt mit `?fehler=` auf die Seite zurück, die eine Satztabelle dafür führt (D-562); das gilt auch für das bestehende Anlegen-Formular |
| V-025 | **Abwesenheit durch die Verwaltung erfassen** („die Krankmeldung am Telefon um 05:40") | `src/server/services/abwesenheit/index.ts:220` — die Dienstfunktion hängt an der Mitarbeiterroute | blockiert | **erledigt** — `/portal/[mandant]/personal/abwesenheiten/erfassen` und `POST /api/personal/abwesenheit`. **Weder Recht noch Policy fehlten:** `t_mandant_schreiben` auf `abwesenheit` (`0073`) verlangt `zeit.abwesenheit_melden`, und `admin` wie `leitung` halten es seit je — gefehlt hat der Weg. Der Satz „die Krankmeldung am Telefon um 05:40" stand wörtlich im Kopf der Dienstfunktion, die genau einen Aufrufer hatte: die Route der Arbeiterin. Wer um 05:40 anruft, hat kein Telefon in der Hand, mit dem er sich meldet. Die Zeile entsteht als `erfasst` (zur Kenntnis genommen, nicht genehmigt) und trägt in `erstellt_von` den Menschen aus dem Büro. Die Anstellung prüft die Route NICHT: `ab_anstellung_fk (mandant_id, anstellung_id)` lässt keine fremde Gesellschaft zu. **Nebenbefund, im selben Zug behoben:** alle sieben Abwesenheitsarten tragen `bezahlt = null` (O-139), und `pruefeArt` weist sie deshalb ab — ohne Vorwarnung erst beim Absenden. Die Liste zeigt sie jetzt nicht wählbar mit dem Grund daneben, statt die Aufnehmende das Formular umsonst ausfüllen zu lassen. Offen bleibt O-893 (Urlaubsantrag im Namen einer Arbeiterin). 5 Prüfungen in `tests/isolation/abwesenheit-aufnehmen.test.ts` |
| V-026 | **Dokument nach Ablauf der Aufbewahrungsfrist löschen** | `src/server/services/dokument/loeschung.ts:54` — das Recht `dokument.archivieren` prüft keine Seite | behindert | **erledigt** — `POST /api/dokumente/loeschen` und ein Formular auf dem Dokumentblatt. **Der Weg war gebaut, ein MENSCH kam nicht daran:** `services/dokument/loeschung.ts` ist der eine Weg, auf dem ein Dokument samt Datei verschwindet — geprüft, mit zwei Auslösern dahinter — und sein einziger Aufrufer war der Nachtlauf `dokument_aufbewahrung` (V-116). Die Datei, die vor zwei Minuten beim falschen Kunden landete, blieb liegen. `dokument.archivieren` stand im Katalog, an `super_admin`, `admin` und `leitung` gebunden, und war an keine Seite und keine Route gebunden. **`dokument.archivieren` und nicht `dokument.schreiben`:** wer ablegen darf, räumt damit nicht auf — das eine legt hinzu, das andere nimmt fort, und in einem Archiv ist das nicht dieselbe Handlung. **Was bleiben MUSS, entscheidet die Datenbank**, und die Seite baut es nicht nach: `kern.dokument_loeschsperre` (0009) für die Aufbewahrungspflicht, `fin.dokument_haengt_an_buchung` (0132) für den Beleg an einer Buchung — beide stehen VOR der Zeile und gelten für jeden Weg; das Blatt NENNT den Grund, bevor jemand drückt, und sagt dazu, dass die Datenbank ohne `buchhaltung.lesen` mehr weiß als es. **Die Frist ist kein Riegel**, und das ist eine Beobachtung, keine neue Regel: nur `loeschsperre` sperrt, `aufbewahrung_bis` steuert den Nachtlauf — ein versehentlich abgelegtes Angebots-PDF sechs Jahre stehen lassen zu müssen wäre keine Aufbewahrung, sondern ein fehlender Weg; läuft die Frist noch, sagt das Formular es und verlangt denselben Grund. **Ohne verbundenen Speicher wird gar nicht erst gelöscht:** eine Zeile ohne ihre Datei wäre ein Dokument, das als gelöscht gilt und im Bucket liegt. **Nebenbefund, mitgenommen:** eine von der Policy abgewiesene Löschung WIRFT, wenn sie am `with check` fällt (Nur-Lese-Bindung), während ein fehlendes Recht am `using` null Zeilen gibt — der Dienst fing nur `23001` und ließ die RLS-Meldung roh bis zum Aufrufer durch, also einen 500er dort, wo „hier wird nicht geschrieben" die Wahrheit ist. 10 Prüfungen in `tests/isolation/dokument-loeschen.test.ts` |
| V-027 | **DATEV-Stapel als übergeben vermerken oder verwerfen** | `drizzle/0133_datev_export.sql:39` — Trigger, Constraint und Anzeige sind gebaut | behindert | **erledigt** — `services/buchhaltung/datev/stapel.ts`, `POST /api/buchhaltung/datev/[id]/stand` (`buchhaltung.exportieren`, dasselbe Recht wie das Erzeugen) und zwei Formulare auf der Stapelseite. **Es wird nichts gesendet** — es gibt keinen DATEV-Endpunkt und keine Zugangsdaten (O-05); was entsteht, ist der Vermerk eines Menschen über etwas, das er selbst getan hat, mit seinem Namen und der Serveruhr daneben. **Beide Wege führen nur aus `erzeugt` heraus:** einen übergebenen Stapel später zu verwerfen, weil das Büro ihn zurückweist, schriebe um, was geschehen IST — der ehrliche Weg ist ein neuer Stapel für denselben Zeitraum, und `datev_export` führt genau dafür keine Eindeutigkeit über den Zeitraum. Der Verwerfungsgrund ist Pflicht und fällt im DIENST statt im CHECK, weil „violates check constraint" kein Satz ist, den ein Mensch lesen soll. 13 Prüfungen in `tests/isolation/datev-stapel.test.ts` |

---

## 3. Gebaut, aber ohne Weg hin

Diese Seiten und Masken **existieren und funktionieren**. Sie stehen nur in
keiner Leiste und auf keiner anderen Seite. Wer die Adresse nicht auswendig
kennt, findet sie nie. Das ist die teuerste Sorte Lücke: bezahlt, gebaut,
unbenutzt.

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-028 | **Ausschreibungsradar — acht Seiten** | `src/server/registry/navigation.ts:488` | blockiert | **erledigt** — Sidebar-Punkt `radar`, Gruppe „aussen" |
| V-029 | **Berichtsmodul — Index plus REP-01…REP-06** | `src/app/portal/[mandant]/berichte/page.tsx:37` | blockiert | **erledigt** — Sidebar-Punkt `berichte` |
| V-030 | **Datenschutz-Posteingang** | `src/server/registry/navigation.ts:72` — in keiner Navigation und keiner Sprungkarte | blockiert | **erledigt** — Sidebar-Punkt `datenschutz`, am Recht des Posteingangs |
| V-031 | **Eine Betroffenenanfrage liess sich nur durch das öffentliche Formular erzeugen** — und die Befundzeile dieses Eintrags war falsch | Ursprünglich notiert als „`datenschutz/page.tsx:116` — kein Auslöser" für Art. 15/16/17. **Nachgemessen stimmt das nicht:** die drei Unterseiten gibt es (`datenschutz/[id]/auskunft`, `…/berichtigung`, `…/loeschung`), sie tragen fertige Formulare gegen `/api/datenschutz/berichtigung` und `…/loeschung`, und `Vorgangskopf.tsx:201` verweist auf alle drei, gefiltert nach Recht. Der echte Befund liegt eine Stufe davor: `betroffenenanfrage` hatte **genau einen Erzeuger**, `/api/datenschutz/anfrage`, und dessen INSERT-Policy verlangt `formular.schreiben` — das Recht des Eingangsprinzipals. Wer im Büro `datenschutz.auskunft_erstellen` hält, konnte lesen, entscheiden, verlängern, zuordnen und **keine Zeile anlegen**. Art. 12 Abs. 1 DSGVO kennt diese Beschränkung nicht: der Antrag geht „schriftlich oder in anderer Form", der mündliche ausdrücklich eingeschlossen. Ein Brief löste dieselbe Monatsfrist aus wie das Formular und hatte in der Plattform, die diese Frist überwacht, keinen Platz | blockiert | **erledigt** — `/portal/[mandant]/datenschutz/aufnehmen` mit `nimmAnfrageAuf`, Policy `t_betroffenenanfrage_aufnahme` und den Spalten `eingangsweg`/`erfasst_von` (`drizzle/0378`). **Der Eingangszeitpunkt ist eine Angabe, nicht die Uhr des Erfassers:** der Brief vom Ersten, am Zwanzigsten erfasst, hat noch zehn Tage — nicht noch einen Monat. Nach vorn geht es nicht, denn nur diese Richtung verschaffte eine Frist, die niemand gewährt hat; `kern.betroffenenanfrage_frist()` weist sie ab. Die beiden Wege trennt nicht die Policy, sondern der CHECK: `admin` und `leitung` halten `formular.schreiben` ohnehin, und erlaubende Policies werden verodert — was unbedingt gilt, ist die PAARUNG von Weg und Aufnehmendem. Damit ist an jeder Zeile ablesbar, ob ihre Frist gemessen oder protokolliert wurde. 12 Prüfungen in `tests/isolation/anfrage-aufnehmen.test.ts`; offen bleibt O-892 (Anfrage ohne E-Mail-Adresse) |
| V-032 | **Gemeldete Barrieren (BFSG-Meldeweg)** | `src/app/portal/[mandant]/datenschutz/barrieren/page.tsx:15` — das öffentliche Formular schreibt hinein, niemand liest | blockiert | **erledigt** — Verweis auf dem Datenschutz-Posteingang, an `referenz.schreiben` (dem Tor der Liste). Beides ist derselbe Schreibtisch — und ein Meldeweg, an dessen anderem Ende niemand sitzt, ist nach BFSG keiner |
| V-033 | **Ausgabe der Check-in-Marken** | `src/app/portal/[mandant]/zeiten/checkin-links/page.tsx:259` | blockiert | **erledigt** — Sprungpunkt auf `/zeiten`, an `zeit.checkin_verwalten` (dem Recht des Ziels, AUT-06) |
| V-034 | **Nacherfassung** | `src/app/portal/[mandant]/zeiten/page.tsx:167` — und die Wächtermeldung zeigt auf `zeit/nacherfassung` statt `zeiten/nacherfassung` (`src/server/services/waechter/benachrichtigung.ts:50`) | blockiert | **erledigt** — Sprungpunkt an `zeit.nacherfassung_pruefen`; beide toten Meldungsziele berichtigt (siehe V-112) |
| V-035 | **Maske „Neuer Kunde"** | `src/app/portal/[mandant]/crm/kunden/page.tsx:114` — fertig, kein Knopf | blockiert | **erledigt** — Knopf und Leerzustand-Verweis, zweisprachig |
| V-036 | **Maske „Neuer Lead"** | `src/app/portal/[mandant]/crm/leads/page.tsx:127` — fertig, kein Knopf | blockiert | **erledigt** — Knopf und Leerzustand-Verweis, zweisprachig |
| V-037 | **Antwort an eine Bewerberin (Zusage/Absage, § 22 AGG)** | `src/app/portal/[mandant]/recruiting/bewerbungen/[id]/antwort/page.tsx:61` — kein eingehender Verweis | blockiert | **erledigt** — Verweis „Antwort schreiben“ auf dem Bewerbungsblatt. Das Register verlangt für die Seite BEIDE Rechte (`recruiting.bewerbung_bewerten` UND `recruiting.entscheiden`), der Verweis prüft deshalb beide |
| V-038 | **Eigene Kontoseite** | `src/app/portal/konto/[[...rest]]/page.tsx:139` — nur über die Arbeiter-Leiste am Telefon; die Kontowurzel führt drei **gebaute** Seiten unter „Noch nicht gebaut" | blockiert | **erledigt** — Die Kontowurzel führt jetzt ihre vier Seiten auf, statt zu behaupten, es gebe sie nicht. Drei davon waren seit ihren Phasen fertig — der Absatz „Noch nicht gebaut“ war der einzige Text an dieser Stelle und führte aktiv in die Irre |
| V-039 | **`/portal/konto/sicherheit`** (Kennwort, zweiter Faktor, aktive Sitzungen) | `src/server/registry/routen.generiert.ts:451` — existiert nicht, und kein Verweis führt auf die Ersatzseiten | blockiert | **erledigt** — `/portal/konto/sicherheit` gebaut: Verweise auf Kennwortwechsel, zweite Stufe und Wiederherstellungscodes (alle drei Seiten gab es, von nirgendwo erreichbar) — und die Liste der EIGENEN Anmeldungen, jede einzeln beendbar. `benutzer_sitzung` hatte dafür seit je zwei Policies für `cse_app`, die niemand benutzte. Die laufende Anmeldung trägt „diese hier“ und keinen Knopf: sie zu beenden liesse das Sitzungsplätzchen auf etwas zeigen, das es nicht mehr gibt. Vier Sprachen. 7 Prüfungen in `tests/isolation/eigene-sitzungen.test.ts` |
| V-040 | **Kalender und Kalender-Feed** | `src/server/registry/navigation.ts:505` — verweisen nur aufeinander | blockiert | **erledigt** — Sidebar-Punkt `kalender`, Gruppe „heute" |
| V-041 | **Buchhaltungs-Index** | `src/app/portal/[mandant]/buchhaltung/page.tsx:106` — geschlossene Insel | blockiert | **erledigt** — Sidebar-Punkt `buchhaltung` (Gruppe „Geld“, an `buchhaltung.lesen`). Er öffnet zwölf Unterseiten, von denen nur drei in der Leiste standen |
| V-042 | **Finanzen-Index** | `src/server/registry/tableiste.ts:81` — nur in der Telefon-Leiste, und dort nur für Rollen, die weder `admin` noch `leitung` sind | blockiert | **erledigt** — Sidebar-Punkt `finanzen` (Gruppe „Geld“, an `finanzen.lesen`). Er öffnet elf Unterseiten, von denen fünf in keiner Leiste standen |
| V-043 | **Navigationsbaum des Kundenportals** | `src/server/registry/navigation.ts:560` — vollständig gebaut, von keiner Komponente gerendert: sechs fertige Kundenbereiche in keiner Leiste | behindert | **erledigt** — Alle drei Stellen lesen jetzt den dritten Baum: Schiene (`PortalRahmen`), `Mehr`-Blatt (`TabLeiste`) und Rechtekarte (`zugang.ts`, samt `zusatzRecht` für die zwei Routen, die ZWEI Leserechte verlangen). Sechs fertige Kundenbereiche sind damit erreichbar. 7 Prüfungen in `tests/kern/kundenbaum.test.ts` |
| V-044 | **Neun von zehn Reitern der Objektübersicht** | `src/app/portal/[mandant]/objekte/[id]/page.tsx:183` — sechs der neun fehlenden Module sind längst gebaut. `04-SEITENKARTE.md` §5.4 nennt zehn Reiter („each tab rendered only where its module is enabled and its `lesen` right held"); der Kommentar in der Seite sagte „die übrigen hängen an Tabellen aus Phase 5 und 6" — richtig, als er geschrieben wurde, und seither falsch. `revier`, `posten`, `dienstanweisung`, `schluessel`, `auftrag`, `einsatz`, `dokument` und `qualitaetspruefung` tragen alle eine `objekt_id`, und vom Objektblatt aus führte kein Weg dorthin: wer wissen wollte, welche Schlüssel zu diesem Haus gehören, musste die Schlüsselliste öffnen und dort filtern — also wissen, dass es sie gibt | behindert | **erledigt** — alle zehn Reiter stehen, jeder mit Zahl. **Ein Reiter ist ein Recht, kein Vorschlag:** er erscheint nur mit gebuchtem Modul UND gehaltenem Leserecht — nicht ausgegraut, nicht mit „kein Zugriff", weil ein Reiter, der die Existenz dessen verrät, was er nicht zeigen darf, derselbe Verstoß gegen AUT-06 ist wie ein Verweis auf 404. Ein Reiter aus der Adresszeile, den die Sitzung nicht sehen darf, fällt auf die Übersicht zurück statt auf eine Fehlerseite. **Die Zahlen kommen unter der Policy** — ein Zähler ausserhalb behauptete eine Menge, deren Zeilen die Liste darunter nicht zeigt, und dann sucht der Mensch den Fehler bei sich; neun Unterabfragen in EINER Abfrage, nicht neun Rundreisen. `raumbuch` führt auf seine eigene Seite (dort steht die Kalkulation, nicht eine Liste), die übrigen wechseln den Inhalt über `?reiter=` — für sie führt die Seitenkarte keine eigene Route, und acht zu erfinden hieße acht Adressen zu bauen, die im Manifest nicht stehen. `einsaetze` zeigt dreißig Tage rückwärts und alles Kommende: die Frage an dieser Stelle ist „wer ist hier gerade", nicht „wer war hier je". 5 Prüfungen in `tests/kern/objekt-reiter.test.ts` (jedes Reiterrecht steht im Katalog, hat ein Modul und trägt in beiden Sprachen ein Wort) und 14 in `tests/isolation/objekt-umfeld.test.ts` — die Isolationsprüfung, weil neun Unterabfragen über neun Tabellen mit je eigener Policy und eigenen Spaltenrechten genau die Sorte sind, die erst an der Datenbank scheitert, und zwar auf der Seite eines Menschen |
| V-045 | **Detailseite einer Veranstaltung** | `src/app/portal/[mandant]/security/veranstaltungen/page.tsx:94` — die Liste springt daran vorbei | behindert | **erledigt** — der Titel führt jetzt auf das Blatt (`security.lesen`, dasselbe Recht wie die Liste); die Besetzung bekam einen eigenen Verweis an ihrem eigenen Recht |
| V-046 | **Wissensquellen der Agenten** | `src/app/portal/[mandant]/agenten/wissen/page.tsx:54` | behindert | **erledigt** — Verweis „Woher das Wissen kommt“ auf der Agentenseite, an `agent.werkzeug_verbinden`. Dieselbe Nachbarschaft wie die Richtlinien: wer wissen will, was ein Agent antwortet, fragt als nächstes, woher er es hat |
| V-047 | **Knopf „Neues Angebot"** | `src/app/portal/[mandant]/angebote/page.tsx:104` — der einzige Weg führt über das Raumbuch eines Objekts | behindert | **erledigt** — Knopf „Neues Angebot“ auf der Liste, an `angebot.schreiben`. Der einzige Weg führte über das Raumbuch eines Objekts — richtig für die Reinigung, wo die Kalkulation aus Flächen entsteht, unbrauchbar für Sicherheit und Bau, wo es kein Raumbuch gibt |
| V-048 | **Die beiden Schaltstellen der Zeiterfassung** | `src/app/portal/[mandant]/zeiten/page.tsx:149` | behindert | **erledigt** — zusammen mit V-033 und V-034: beide Sprungpunkte stehen in der Leiste über der Wochenliste |

---

## 4. Arbeiterportal — was die Kraft am Telefon nicht kann

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-049 | **Eine Schicht zusagen oder absagen.** Der Zustand `zugesagt` wird im ganzen Baum **von niemandem** geschrieben | `src/app/portal/mein/schichten/[zuordnungId]/page.tsx:100`; Gegenprobe: der einzige `UPDATE` auf `einsatz_zuordnung.status` ist `dienstplan/einteilung.ts:757` (`abgesagt`, durch das Büro) | blockiert | **erledigt** — D-622, `drizzle/0374`, `api/mein/schicht`, Baustein `Zusagefeld` in vier Sprachen |
| V-050 | **Folge von V-049:** die Besetzungswarnung zählt Zusagen (`waechter/benachrichtigung.ts:62`) und meldet darum für **jede** Schicht null; der Pillenzweig „Bereit" (`bausteine.tsx:155`) ist unerreichbar | siehe V-049 | blockiert | **erledigt** — geprueft an der ZAHL, nicht am Spaltenwert (`tests/isolation/schicht-zusage.test.ts` §5) |
| V-051 | **Die Entscheidung über den eigenen Einwand erreicht die Mitarbeiterin nirgends** — weder Begründung noch Meldung | `src/app/portal/mein/zeiten/[id]/einwand/page.tsx:181` | blockiert | **erledigt** — zwei Hälften: die Seite zeigt Zustand (vier Sprachen), Entscheidungsdatum und Begründung; und `entscheideEinwand` stellt eine Benachrichtigung zu (`drizzle/0377`, Art `zeit.einwand_entschieden`). `in_pruefung` und `zurueckgezogen` schweigen. 9 Prüfungen in `tests/isolation/einwand-entscheidung-meldung.test.ts` |
| V-052 | **Den eigenen Einwand zurückziehen.** Der Dienst erlaubt es ausdrücklich, es gibt keinen Weg dorthin | `src/server/services/zeit/einwand.ts:41`, `src/app/api/zeit/einwand/entscheidung/route.ts:39` | behindert | **erledigt — und der Befund war schärfer als notiert.** Der Zustand `zurueckgezogen` steht seit `0052` im Aufzählungstyp, `ZULAESSIG` der Route lässt ihn seit je zu, der Auslöser ebenfalls — und KEIN Formular schickte ihn. Beide Einwandseiten bieten ihn jetzt an (und die Liste zusätzlich `in_pruefung`, das nur das Einzelblatt hatte). Der häufigste Fall dahinter ist banal: die Arbeiterin meldet sich und sagt, sie habe den Plan falsch gelesen — das als „abgelehnt" zu buchen wäre eine Entscheidung GEGEN sie und stünde für immer in ihrer Akte. **Der Widerspruch, den die Zeile nicht kannte:** `services/zeit/einwand.ts` schrieb, „das zieht die betroffene Person selbst zurueck", und `0052` gibt ihr ausdrücklich NUR `t_selbst_einreichen` (INSERT), mit Begründung. Der Kommentar ist richtiggestellt, die Frage als **O-901** aufgenommen — eine Policy, die das ändert, ist eine Migration und keine Oberfläche. **Mitgenommen:** die Route antwortete auf jede Abweisung mit JSON, obwohl sie nur `formData` liest — `{"fehler":"begruendung_zu_kurz"}` als weisse Seite, mit dem getippten Text verloren; sie kehrt jetzt mit `?fehler=` zurück, und beide Seiten führen die Satztabelle (D-562). Sperrklinke: `tests/kern/zeit-sackgassen.test.ts` §5 |
| V-053 | **Einen anderen Monat ansehen.** „Meine Zeiten" liest `?monat=`, kein Bedienelement erzeugt den Parameter | `src/app/portal/mein/zeiten/page.tsx:47` | blockiert | **erledigt** — ein `Monatswechsler` in `mein/bausteine.tsx` auf allen DREI Lesern (Zeiten, Stundenkonto, Monatsnachweis); beschriftet in vier Sprachen statt nackter Pfeile, 44 px, nicht in die leere Zukunft, gewählte Beschäftigung wandert mit. Sperrklinke: `tests/kern/monatswechsel.test.ts` |
| V-054 | **Ein anderes Jahr ansehen · Urlaub beantragen** | `src/app/portal/mein/urlaub/page.tsx:46` — liest `?jahr=` ohne Bedienelement, kein Weg zum Antrag | behindert | **erledigt** — ein `Jahreswechsler` in `mein/bausteine.tsx`, gebaut wie der `Monatswechsler` aus V-053: beschriftet statt nackter Pfeile, in vier Sprachen, 44 px. **Ein Jahr voraus, nicht mehr** — anders als beim Monat, weil ein Anspruch fürs kommende Jahr im Herbst geplant wird; zwei Jahre voraus wären zwei leere Bildschirme. Dazu der Knopf zum Urlaubsantrag: die Seite zeigte den Anspruch und sagte nicht, wie man ihn nimmt — der Antrag liegt zwei Ebenen weiter unter „Anträge", also dort, wo niemand sucht, der auf sein Urlaubskonto schaut. Sperrklinke: `tests/kern/monatswechsel.test.ts` §4 |
| V-055 | **Monatsnachweis:** keine Portalhülle, kein Monatswechsel, keine Wahl der Beschäftigung, „Drucken" als Wort ohne Knopf | `src/app/portal/mein/monatsnachweis/page.tsx:134` | behindert | **erledigt** — Monatswechsel kam mit V-053; hier kommen der DRUCKKNOPF und die WAHL dazu. „Monatsnachweis · Drucken" war eine Anleitung ohne Bedienelement, und auf einem Telefon gibt es kein Datei-Menü; der Knopf ist die einzige Client-Insel dieser Seite (`window.print()` gibt es nur im Browser) und trägt `cse-nicht-drucken` — ein Bedienelement im Ausdruck wäre ein Knopf auf einem Beweisstück. Die Wahl der Beschäftigung steht erst ab ZWEI (D-09): zwei Arbeitsverhältnisse sind zwei Aufzeichnungen gegen zwei Arbeitgeber, und wer für die zweite einen Nachweis brauchte, musste `?anstellung=` mit einer UUID tippen, die nirgends stand. **Die Portalhülle bleibt bewusst weg:** das Blatt ist ein Ausdruck nach § 17 MiLoG mit eigenen A4-Rändern, und eine Navigation darum wäre auf dem Papier eine zweite Aussage. Sperrklinke: §5 derselben Datei |
| V-056 | **Eine gemeldete Abwesenheit korrigieren oder zurücknehmen** — die Zeilen sind nicht einmal anklickbar | `src/app/portal/mein/antraege/page.tsx:142` | blockiert | **erledigt** — `drizzle/0386` gibt `abwesenheit` die Policy `t_selbst_zurueckziehen` (nur die EIGENE Zeile, nur `erfasst` und `beantragt`, nur nach `storniert`), dazu `POST /api/mein/abwesenheit/[id]/zurueckziehen`, das Einzelblatt `/portal/mein/abwesenheit/[id]` und anklickbare Zeilen auf `/portal/mein/antraege`. **Korrigieren gibt es bewusst nicht:** eine geänderte Krankmeldung wäre eine zweite Aussage über denselben Zeitraum — zurücknehmen und neu melden lässt beide Schritte stehen. Ob eine Rücknahme nach dem Lohnexport noch zulässig ist, bleibt **O-895**. 15 Prüfungen in `tests/isolation/abwesenheit-ruecknahme.test.ts` |
| V-057 | **Halbe Abwesenheitstage und der Vermerk „AU-Bescheinigung liegt vor"** — Regel, Spalten und Lohnexport sind gebaut, die Route liest vier Felder, kein Formular sendet sie | `src/app/portal/mein/abwesenheit/neu/page.tsx:97`, `src/app/api/mein/abwesenheit/route.ts:111` | blockiert | **erledigt** — vier Felder im Meldeformular, in allen VIER Sprachen (de/en/ar/tr, SPEC §10). **Die Wirkung steckt in der Zahl:** `rechneTage` zieht je halbem Randtag 0,5 ab, und an derselben Zahl hängt die Sollzeitgutschrift im Stundenkonto (EMP-04) — wer einen halben Tag krank war, meldete einen ganzen, und das Konto war um einen halben Tag falsch. Ein einziger, beidseitig halb gemeldeter Tag zählt 0,5 und nicht 0: `rechneTage` zieht den zweiten Abschlag nur bei `bis <> von`. Der AU-Vermerk und seine Frist stehen jetzt in der Zeile; ohne sie stand in der Akte bei jeder Krankmeldung „keine Bescheinigung", auch wenn sie auf dem Tisch der Personalstelle lag. 15 Prüfungen in `tests/isolation/abwesenheit-ruecknahme.test.ts` (§1 und §2) |
| V-058 | **Zwei Leistungsnachweise auf einem Objekt und Tag:** das Unterschriftsblatt verschwindet dauerhaft, das Anlegeformular kommt zurück | `src/app/portal/mein/schichten/[zuordnungId]/leistungsnachweis/page.tsx:101` | blockiert | **erledigt** — bei mehreren offenen Nachweisen steht jetzt eine WAHL, und das Anlegeformular bleibt weg. Der Befund war schlimmer als „das Blatt verschwindet": der Fall fiel in den `vorschau === null`-Zweig, und der zeigt das ANLEGEFORMULAR — **der angebotene Ausweg aus „zu viele" war ein weiterer**. Jeder Klick ein dritter vorgelegter Nachweis auf derselben Schicht. Der Kommentar im Code sagte „dann steht nur die Liste da"; darunter stand das Formular. Die Frage „welcher wird jetzt unterschrieben" kann der Bildschirm wirklich nicht beantworten — **der Mensch davor kann es, er hat die Schicht gearbeitet.** Also fragt die Seite ihn. Die Wahl kommt über `?nachweis=` und wird gegen die offene Liste geprüft, nie geglaubt: eine fremde Kennung fällt aus der Wahl, ohne 403 und ohne Fehler (AUT-06). In allen vier Sprachen (de/en/ar/tr) |
| V-059 | **Eine vierte Positionszeile im Leistungsnachweis** — es gibt genau drei und keinen Knopf „Zeile hinzufügen" | `…/leistungsnachweis/page.tsx:130` | behindert | **erledigt** — die Zahl der Zeilen kommt aus `?zeilen=` (Vorgabe drei, höchstens zwanzig) mit einem Verweis „Mehr Zeilen" in vier Sprachen, und die Route begrenzt sie ein ZWEITES Mal: ein Formular ist das, was ankommt. Die vierte Zeile ist kein Sonderfall — bei einer Grundreinigung mit Glas, Sanitär, Boden und Sonderfläche ist sie der Regelfall. Der Verweis sagt dazu, dass Getipptes beim Nachladen verloren geht; ein Verweis, der still löscht, ist eine Falle, und auf einem Diensttelefon tippt man langsam. Sperrklinke: `tests/kern/geraetezeit-und-zeilen.test.ts` §2 |
| V-060 | **Die Gerätezeit wird nie gefüllt** — das Wachbuch schickt ein leeres Feld, die Kenntnisnahme gar keines; die Abweichung steht immer auf „—" | `src/app/portal/mein/schichten/[zuordnungId]/wachbuch/page.tsx:266` | behindert | **erledigt — und der Befund war grösser als notiert.** Nicht nur das Wachbuch: **vier** Formulare trugen ein verstecktes `geraete_zeit` und füllten es nie (Stempeluhr, Wachbuch, Qualitätsprüfung, Unterschrift unter dem Leistungsnachweis), und **zwei weitere** hatten gar kein Feld, obwohl ihre Route den Wert entgegennimmt (Kenntnisnahme der Dienstanweisung, Schlüsselquittung). Sechs Routen nahmen also etwas entgegen, das nie ankam; jede Anzeige der Abweichung stand auf „—", nicht weil die Uhren stimmten, sondern weil nie eine zweite Uhr gefragt wurde. Ein Telefon, das eine Stunde nachgeht, erzeugt damit eine Schicht, die um eine Stunde verschoben behauptet wird — und sauber aussieht. `portal/mein/Geraetezeit.tsx` ist eine Client-Insel mit genau einem Zweck und setzt den Wert beim ABSENDEN: beim Laden gesetzt wäre es die Zeit, zu der die Seite geöffnet wurde, und bei einem in Ruhe getippten Wachbucheintrag sind das zwanzig Minuten. **Ohne Javascript bleibt das Feld leer, und die Erfassung gilt trotzdem** — sie darf nicht daran hängen, dass ein Skript geladen hat. Sperrklinke: §1 derselben Datei, und sie verbietet künftig jedes tote `geraete_zeit`-Feld |
| V-061 | **Die Nachweisseite nennt die Sperre und keinen nächsten Schritt** — kein Einreichen, kein Ansprechpartner, kein Link | `src/app/portal/mein/nachweise/page.tsx:119` | behindert | **erledigt** — neben „Ohne diesen Nachweis darf Sie niemand einteilen." steht jetzt, was zu tun ist, WELCHE Gesellschaft es einträgt (beide bei zwei Beschäftigungen — keine ist „die") und ein Weg zu den Nachrichten. **Kein „Nachweis hochladen":** es gibt keine Ablage für Dateien (O-12/O-13, kein Medienspeicher verbunden), und ein Feld, das nichts speichert, wäre schlimmer als keines — der Mensch hielte die Sache für erledigt. Der Satz sagt das ausdrücklich, statt es zu verschweigen. Vier Sprachen. Sperrklinke: §7 derselben Datei |
| V-062 | **Rohe Datenbankwerte auf einem Bildschirm, der vier Sprachen können muss** — obwohl die Übersetzung daneben schon liegt | `src/app/portal/mein/antraege/[id]/page.tsx:81` | behindert | **erledigt** — `antragsart.bezeichnung_i18n` liegt seit `0074` da und wurde nicht gelesen: „Urlaubsantrag" stand als Überschrift auf einem Bildschirm, den jemand auf Arabisch eingestellt hat, weil er kein Deutsch liest. Die Zeilenabfrage gibt die Karte jetzt GANZ zurück, und `artInSprache` wählt erst bei der Anzeige — weil dieselbe Zeile auch im Genehmigungsposteingang der Verwaltung steht, und die beiden Leser nicht dieselbe Sprache sprechen. Rückfall auf Deutsch, wenn niemand übersetzt hat: eine Lücke wäre schlimmer, der Mensch könnte nicht einmal danach fragen. Sperrklinke: §6 derselben Datei |
| V-063 | **Bautagebuch:** Tagesfotos werden aufgelistet, können vom Arbeiterportal aber nie entstehen; die eigene Mannstundenzeile lässt sich nicht stornieren | `src/app/portal/mein/schichten/[zuordnungId]/bautagebuch/page.tsx:414` | behindert | **erledigt** — zwei Routen (`…/bautagebuch/korrektur`, `…/bautagebuch/foto`), zwei Formulare im Arbeiterportal, in allen vier Sprachen. **Beide Wege waren in der Datenbank seit `0303` fertig:** `t_selbst_m1_storno` erlaubt der Kraft genau den einen Übergang auf ihrer EIGENEN, lebenden Zeile — storniert MIT Ersatz —, und `t_selbst_schichtmedien` nennt `bezug_tabelle = 'bautagebuch'` ausdrücklich. Es fehlten nur Route und Formular. Die Kraft auf der Baustelle, die das Telefon in der Hand hat, war die einzige, die kein Foto anhängen konnte. **Die Korrektur ist EINE Handlung, nicht zwei:** `korrigiereZeile` schreibt Storno und Ersatz in einer Transaktion; zwei Knöpfe liessen einen Zustand zu, in dem storniert ist und nichts an die Stelle getreten — die Tagessumme wäre still zu klein, und der Abgleich gegen die Zeiterfassung meldete einen Befund, der keiner ist. **Der Knopf steht nur an der eigenen Zeile:** `leseMannstunden` liefert dafür `eigene` (mit `coalesce(…, false)`, weil eine Verwaltungssitzung ohne Person `NULL` ergäbe); an einer fremden liefe er in die Policy und endete in einer Meldung, die niemand versteht. **Tag und Mandant kommen aus der SCHICHT, nie aus der Anfrage** (K-02) — sonst liesse sich ein Foto an einen fremden Bautag hängen. **Das Foto folgt demselben Muster wie die Schichtfotos:** erst die Zeile, dann der Speicher, noch in der Transaktion; wirft der Speicher, nimmt die Transaktion die Zeile mit. Nicht verbunden heisst „NICHTS gespeichert", und das steht so da. Der Korrekturknopf ist sekundär, nicht rot: ein roter Knopf unter jeder Zeile hätte den eigentlichen der Seite zu einem von vielen gemacht (DESIGN §5). 3 Prüfungen in `tests/isolation/mitarbeiterportal-schicht.test.ts` unter genau der Sitzung, die die Routen bauen — die eigene Zeile, die fremde (abgewiesen, und die Ersatzzeile ist danach nicht da), das Foto |

---

## 5. Zeitwirtschaft — die Verwaltungsseite

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-064 | **Ein laufender Zeiteintrag lässt sich von der Verwaltung weder schließen noch stornieren noch korrigieren** | `src/server/services/zeit/korrektur.ts:160` | blockiert | **erledigt** — `services/zeit/laufender-eintrag.ts` (`schliessen` / `stornieren`), `POST /api/zeit/laufend`, Formular an jeder Karte auf `/zeiten/live` hinter `zeit.korrigieren`. Die gesetzte Zeit bleibt als GESETZT erkennbar (`nacherfasst`, `quelle_ende`, `behauptet_ende`), Begründung Pflicht. 11 Prüfungen in `tests/isolation/laufender-eintrag.test.ts` |
| V-065 | **Die Ausgleichsbuchung hat keinen einzigen Aufrufer** — ohne sie entsteht in einem gesperrten Monat **keine** Korrektur | `src/app/api/zeit/korrektur/route.ts:143`, `src/server/services/zeit/stundenkonto.ts:503` | blockiert | **erledigt** — `korrigiereZeiteintrag` bucht sie selbst, wenn der Eintrag gesperrt ist: Differenz aus `dauer_netto_minuten`, Ziel ist der erste OFFENE Monat, der gesperrte bleibt gesperrt. Offen bleibt der Fall ohne Minutendifferenz (O-891). 4 Prüfungen in `tests/isolation/korrektur-ausgleich.test.ts` |
| V-066 | **„Nacherfassen" ohne Offline-Anspruch ist unmöglich**, obwohl zwei Stellen der Plattform genau das verlangen | `src/server/services/zeit/offline.ts:291` | blockiert | **erledigt** — `services/zeit/nacherfassung.ts` — eine Arbeitszeit eintragen, zu der es KEIN Gerätereignis gibt. `POST /api/zeit/nacherfassung`, Formular auf `/zeiten/nacherfassung`. Vier Spalten halten fest, dass ein MENSCH die Zeit behauptet hat (`nacherfasst`, `quelle_*`, `behauptet_*`); EMP-07 gilt auch hier: niemand erfasst die eigene Zeit nach. 9 Prüfungen in `tests/isolation/nacherfassung-frei.test.ts` |
| V-067 | **Ein anerkannter Einwand der Art „Eintrag fehlt" führt ins Leere** — kein Verweis, keine Handlung | `src/app/portal/[mandant]/zeiten/einwaende/[id]/page.tsx:439` | blockiert | **erledigt** — Auf dem Einwandblatt steht jetzt bei `eintrag_fehlt` der Verweis „Zeit nacherfassen“ — mit der Person vorbelegt, die ZEITEN leer (§1.8: eine vorbelegte Behauptung wäre von einer Entscheidung nicht mehr zu unterscheiden). Der neue Eintrag wird in die Entscheidungsbegründung des Einwands geschrieben, damit die Meldung ihre Antwort trägt |
| V-068 | **Es gibt keinen Knopf, der eine Ausstempel-Marke ausgibt** — die Oberfläche kennt nur `zweck="checkin"` | `src/app/portal/[mandant]/zeiten/checkin-links/page.tsx:263` | blockiert | **erledigt** — zweiter Knopf mit `zweck="checkout"`, sichtbar erst nach dem EINLÖSEN der Einstempel-Marke: `app.checkin_verbrauchen` weist eine Ausstempel-Marke ohne offenen Eintrag ab, und ein Knopf, der einen Fehler erzeugt, ist schlechter als keiner |
| V-069 | **Die Meldung „Morgen unbesetzt" zeigt auf `/portal/<slug>/dienstplan` — diese Route gibt es nicht** | `src/server/services/waechter/benachrichtigung.ts:67` | blockiert | **erledigt** — Ziel ist jetzt `/dienstplan/tag`: von den vier Ansichten ist die Tagesansicht die Disposition, und dort wird nachbesetzt. Die Wochenansicht verlangte einen zweiten Klick an genau der Stelle, an der jemand um sechs Uhr schnell sein muss |
| V-070 | **Der Monatsabschluss nennt die Zahl der nicht freigegebenen Einträge und keinen Weg zu ihnen** | `src/app/portal/[mandant]/personal/stundenkonten/abschluss/page.tsx:201` | behindert | **erledigt** — die Zahl ist ein Verweis geworden: `/zeiten` versteht jetzt ein MONATSFENSTER (`?monat=`) neben der Woche, kennt das Merkmal `nicht_freigegeben` als Gegenprobe zu `freigegeben`, und die Kontozeile trägt die `person_id`. Ein Verweis auf die erste WOCHE des Monats hätte einen Teil der Einträge gezeigt und behauptet, es seien alle. Die Person und nicht die Beschäftigung ist auch fachlich richtig: wer zwei hat (D-09), soll beide Reihen sehen — das Arbeitszeitgesetz rechnet über den Menschen. Der Verweis hängt an `zeit.lesen`, dem Recht SEINES Ziels, nicht an `zeit.konto_lesen` dieser Seite (AUT-06). Sperrklinken: `tests/kern/zeit-sackgassen.test.ts` §1–§3 und `tests/isolation/stundenkonten-seite.test.ts` |
| V-071 | **Das Zeiteintragsblatt lädt Freigabe-, Abrechnungs- und Sperrzustand und zeigt keinen davon** | `src/app/portal/[mandant]/zeiten/[id]/page.tsx:221` | behindert | **erledigt** — die drei stehen jetzt auf dem Blatt, und zwar als ZEITPUNKT in Berliner Ortszeit statt als „ja": es sind genau die drei Fragen, die man an einem Eintrag stellt (korrigierbar? schon im Lohn? Monat zu?), und im Streit lautet die Frage wann. Darunter steht als Satz, was die Sperre bedeutet — eine Zeitangabe allein sagt nicht, dass sie eine Tür schliesst. Sperrklinke: `tests/kern/zeit-sackgassen.test.ts` §4 |
| V-072 | **Die Kachel „Aktuell im Einsatz" (DSH-05) fehlt**; ihr fertiger Dienst hat keinen Aufrufer | `src/server/services/zeit/live.ts:33` | Schönheit | **erledigt** — die Kachel `aktuell_im_einsatz` steht im Kennzahlenregister (`zeit.lesen`, Ziel `/zeiten/live`), und Zahl wie Zeilen lesen DIESELBE Sicht `zeiteintrag_offen`: eine Zahl aus einer anderen Bedingung als die Zeilen, die sie zählt, driftet — und eine Kachel, die etwas anderes sagt als die Liste, ist schlimmer als keine (DSH-04). `info` und nicht `warning`: dass jemand im Einsatz ist, ist der Normalfall; eine vergessene Abmeldung fällt in der Liste auf, wo die Dauer steht. **Und der Dienst ist ENTFALLEN:** `services/zeit/live.ts` bildete dieselbe Sicht ein drittes Mal ab, war für genau diese Kachel gebaut und von ihr nie aufrufbar — das Register nimmt SQL und keine Funktion. Was bleibt, ist eine Frage mit einer Antwort statt dreier Wege zu ihr. Sperrklinke: `tests/kern/zeit-sackgassen.test.ts` §6 |
| V-073 | **Der nächtliche Abgleich des Stundenkontos gegen sein Journal ist gebaut und läuft nie** | `src/server/services/zeit/stundenkonto.ts:686` | Schönheit | **erledigt** — `jobs/stundenkontoAbgleich.ts`, `je_mandant`, `versuche: 0`, 03:40 Uhr: nach den schreibenden Nachtläufen und lange vor dem Arbeitstag, damit der Stand geprüft wird, mit dem die Personalstelle morgens anfängt. **Er meldet und korrigiert nicht** — ein Lauf, der die Zahl still geradezieht, macht aus dem Befund eine Statistik, die immer sauber ist, und aus dem Guthaben eines Menschen eine Zahl, die sich nachts von selbst ändert. **`mandantId` als FILTER, obwohl der Binder den Mandanten setzt:** unter `cse_job` greift `t_job … using (true)` (0060), die RLS grenzt hier NICHT ein — ohne den Filter meldete jeder Durchgang die Abweichungen aller vier Gesellschaften, jede unter dem falschen Namen. Die Meldung trägt Monat, Anstellung und BEIDE Summen, damit morgens niemand erst eine Abfrage schreiben muss. 4 Prüfungen in `tests/kern/stundenkonto-abgleich-job.test.ts` |

---

## 6. Zustände, die niemand erzeugt

Ein Zustandswert, den kein Code schreibt, ist ein Versprechen an die
Oberfläche, das nie eingelöst wird. Filter darauf liefern immer leer; Pillen
dafür erscheinen nie.

| Nr | Zustand | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-074 | `benutzer.status = 'gesperrt'` ist eine **Einbahnstraße** — die Brute-Force-Sperre setzt sie, nichts setzt sie zurück | `drizzle/0007_benutzer_auth.sql:414` | blockiert | **erledigt** — `app.konto_entsperren` (`drizzle/0379`). **Und die Entsperrung musste auch WIRKEN:** die Sperrdauer (30 Min) ist länger als das Zählfenster (15 Min), also fand der nächste Fehlversuch die zehn alten Zeilen noch im Fenster und sperrte sofort wieder — ein Knopf, der manchmal still nichts tut. `benutzer.entsperrt_am` setzt jetzt den Beginn des Fensters. Die Versuchszeilen bleiben stehen (Invariante 8): sie sind die Spur, an der ein Angriff abgelesen wird. Die IP-Schwelle bleibt am vollen Fenster — eine Entsperrung gilt einem KONTO, nicht einer Adresse, hinter der gerade fünfzig andere durchprobiert werden |
| V-075 | `benutzer.status = 'deaktiviert'` hat keinen Erzeuger; der Zugangsentzug für interne Konten hat eine Policy ohne Aufrufer | `drizzle/0102_benutzer_mandant_entzug.sql:41` | blockiert | **erledigt** — mit V-022 in derselben Änderung. Der Entzug beendet die offenen Anmeldungen mit: `app.sitzung_aufloesen` verlangt ohnehin `status = 'aktiv'`, aber eine Liste, die „läuft bis …" zeigt, behauptete einen Zugang, den es nicht mehr gibt |
| V-076 | Das Recht `system.sitzung_widerrufen` ist vergeben — **kein Code prüft es, kein Bildschirm bietet den Widerruf an** | `src/server/auth/katalog.generiert.ts:226` | blockiert | **erledigt** — `app.sitzungen_widerrufen` (`drizzle/0379`), auf dem Benutzerblatt als „Alle Anmeldungen beenden". Das Blatt sagte wörtlich, der Widerruf „kommt mit" diesem Recht; er kam nie. **Am KLEINEREN Recht, und mit Absicht:** ein Widerruf nimmt ein Plätzchen aus dem Verkehr, keinen Menschen aus dem Betrieb — wer widerrufen ist, meldet sich sofort wieder an. Deshalb ist er bis `leitung` bindbar, während der Entzug `system.benutzer_verwalten` verlangt. Die eigenen Anmeldungen gehen diesen Weg nicht: sie stehen unter `/portal/konto/sicherheit` (V-039) |
| V-077 | `lead.status` lässt sich nirgends setzen: die Route nimmt `status` entgegen, kein Formular sendet es | `src/app/api/crm/lead/route.ts:56` | blockiert | **erledigt** — das Standformular auf dem Leadblatt (`/crm/leads/[id]`). `setzeLeadStatus` stand seit `crm/anlegen.ts` da, `POST /api/crm/lead` nahm `status` entgegen — und kein Formular schickte ihn. Eine Anfrage, die niemand weiterstellt, bleibt für immer „neu", und die Auswertung nach gewonnen/verloren zählt eine leere Menge. Das Formular bietet genau die sechs Werte von `lead_status` (0017) an und trägt das Grundfeld daneben: bei „verloren" und „kein Bedarf" ist es Pflicht — `lead_verlust_begruendet` hält es ohnehin, aber mit dem Namen der Bedingung statt mit einem Satz. Nur unter `crm.schreiben`; wer es nicht hält, sieht statt des Formulars, welches Recht fehlt (AUT-06) |
| V-078 | `nachgetragen` (Wachbuch, Schlüsselquittung) wird an sechs Stellen **angezeigt**, von keinem Formular gesetzt, von keiner SQL-Zeile geschrieben | `src/app/api/sicherheit/wachbuch/route.ts:104` | blockiert | **erledigt — und es waren VIER Tabellen, nicht zwei.** `wachbuch_eintrag` und `schluessel_ereignis` (`0070`), `qualitaetspruefung` (`0068`) und `leistungsnachweis_signatur` (`0066`) führen die Spalte; sechs Seiten zeigen den Vermerk. Bei zweien nahm der Dienst ihn seit je entgegen und kein Formular schickte ihn, bei den anderen kannte ihn nicht einmal die Route. Was auf dem Bildschirm stand, war also nie die Aussage eines Menschen, sondern der Vorgabewert der Spalte — und in genau der Richtung, die niemandem auffällt: alles sah aus, als wäre es im Augenblick des Geschehens erfasst worden. **Es wird angekreuzt, nicht abgeleitet:** seit die Gerätezeit wirklich ankommt (V-060) läge „Abweichung gross ⇒ nachgetragen" nahe, und das ist genau der Fehler, den `0070` wörtlich benennt — eine falsch gehende Uhr und ein später getippter Eintrag sind zwei verschiedene Tatsachen. Vier Formulare, vier Sprachen für den Satz daneben; 14 Prüfungen in `tests/kern/nachgetragen.test.ts`, eine davon verbietet die Ableitung im ganzen `src/app` |
| V-079 | Eine **Wiedervorlage** lässt sich niemandem zuweisen — die Route liest `zustaendigBenutzerId` und `leadId`, kein Formular sendet beides | `src/app/api/crm/wiedervorlage/route.ts:67` | blockiert | **erledigt — und der Befund war enger als notiert.** Gemessen (neue Prüfung in `tests/isolation/crm-wiedervorlage.test.ts`): `legeWiedervorlageAn` setzt `coalesce($8, app.aktueller_benutzer())`, eine Wiedervorlage fällt also an den, der sie anlegt, und liegt **nie bei niemandem**. Echt war die Lücke trotzdem, nur schmaler: sie liess sich **niemand ANDEREM** zuweisen, weil kein Formular `zustaendigBenutzerId` schickte, und **zu einem Lead gar nicht anlegen**, weil das einzige Anlegeformular auf dem Kontaktblatt stand. Gebaut: das Zuständigenfeld auf dem Kontaktblatt und ein vollständiges Wiedervorlageformular auf dem Leadblatt mit `leadId`. **Die Namensliste steht nur unter `system.benutzer_lesen`** — eine Auswahlliste, die Namen nennt, IST die Auskunft, und AUT-06 gilt auch für ein `<select>`; ohne das Recht bleibt „mir selbst zuweisen", und dafür braucht es keinen fremden Namen. 3 neue Prüfungen: der Zuständige kommt in der Aktivität UND in der gespiegelten Aufgabe an (dort heisst die Spalte `zugewiesen_an`), und der Leadbezug steht in der Zeile |
| V-080 | Die Akquise-Handlung „ansehen" hat keinen Auslöser; `geprueft` wird angezeigt, von nichts erzeugt | `src/app/api/akquise/ziel/route.ts:68` | behindert | **erledigt** — ein Formular auf dem Akquiseblatt, und der Blick gibt den Stempel endlich heraus. **Alles war gebaut ausser dem Knopf:** `markiereGeprueft` stempelt `angesehen_am` und `angesehen_von` seit je und hebt `neu` auf `geprueft`, die Route nimmt `handlung=ansehen` entgegen — und **kein Formular schickte sie**; das Blatt beschriftete `geprueft` als „In Arbeit", und der Zustand entstand nie. Selbst wenn: `Ziel` gab die beiden Spalten nicht heraus, die Seite konnte also gar nicht zeigen, WER hingesehen hat — beides ist jetzt da. **Warum ein KNOPF und nicht der Seitenaufruf:** der naheliegende Weg wäre, beim Öffnen zu stempeln („angesehen heisst angesehen"). Ein Seitenaufruf ist aber ein GET, und ein GET, der schreibt, wird von jedem Vorschau-Abruf, jedem Linkprüfer und jedem zweiten Reiter ausgelöst — der Stempel hiesse dann nicht „ein Mensch hat entschieden", sondern „irgendetwas hat diese Adresse geholt". Der Zustand bedeutet damit, was er soll: jemand hat sich die Firma angesehen und sie bewusst weder übernommen noch verworfen. **Ein übernommenes Ziel fällt nicht zurück** (`case when status = 'neu'`), sein Zeitstempel wird aber erneuert — wann zuletzt jemand hinsah, ist auch dann eine Auskunft. 2 Prüfungen mehr in `tests/isolation/akquise.test.ts` §6 |
| V-081 | `auftrag.status`: **`aktiv`, `pausiert`, `storniert`** erzeugt nichts | `drizzle/0025_auftrag.sql:35` | behindert | **erledigt** — `services/auftrag/status.ts`, `POST /api/auftrag/status`, `drizzle/0389` und ein Formular auf dem Auftragsblatt. **Geschrieben wurde genau EIN Zustand:** `abgeschlossen`, vom Abschlussdienst. Jeder Auftrag stand von seiner Anlage bis zu seinem Ende auf `angelegt`, während das Blatt vier weitere Etiketten kannte, die nie jemand sah — und im Betrieb fehlt damit genau das, was eine Auftragsliste täglich braucht: läuft dieser Vertrag, ruht er, ist er storniert worden. **Die Tabelle der Wege steht DREIMAL, und das ist Absicht:** in der Oberfläche (damit kein Knopf etwas anbietet, das abgewiesen wird), im Dienst (damit ein Mensch einen Satz bekommt) und im Auslöser `kern.auftrag_status_pruefen` (`0389`, damit sie auch für einen Aufrufer gilt, der den Dienst nie gesehen hat) — die Seite LIEST die Liste des Dienstes, statt sie abzuschreiben. **`storniert` ist einwegig:** das Storno ist die Aussage, dass dieser Vertrag nicht zustande kommt, sie steht in der Kundenakte und nimmt den Auftrag von der FIN-18-Prüfliste (`0183`); wer sich geirrt hat, legt einen neuen an — dieselbe Antwort, die `0296` für den Abschluss gibt. **Ruhen und Stornieren tragen einen Grund** (`status_grund`, mit CHECK), der Normalweg `angelegt → aktiv` nicht: ein Pflichtfeld davor wäre eine Hürde vor dem Regelfall. Der Grund gehört dem AKTUELLEN Zustand — wer wieder aufnimmt, räumt ihn; die Spur bleibt im Protokoll. **`abgeschlossen` ist hier gar nicht wählbar:** der Abschluss stellt nach D-366 die FIN-18-Warnung scharf und trägt sein eigenes Recht — ihn mitanzubieten hiesse, ihn an `auftrag.schreiben` zu binden, genau die Vermischung, die `0296` aufgelöst hat. `status_geaendert_am` setzt der Auslöser aus der Serveruhr (Invariante 5). 17 Prüfungen in `tests/isolation/auftragsstatus.test.ts`, eine davon zählt jeden Weg der Tabelle an der Datenbank nach |
| V-082 | `einsatz.status`: **`laufend`, `abgeschlossen`** werden nie gesetzt, obwohl das Arbeiterportal sie anzeigt | `drizzle/0028_dienstplan.sql:425` | behindert | **erledigt** — `drizzle/0390`: `kern.einsatz_status_ableiten` am Auslöser von `zeiteintrag`, plus der stündliche Lauf `einsatz_abschluss`. **Was die beiden Werte bedeuten, sagt das Datenmodell — nicht diese Runde:** `04-PLANUNG-ZEIT.md` führt `einsatz_ohne_zeit_idx … where status = 'geplant'` und nennt seinen Zweck „the hourly 'shift ended, no zeiteintrag' watchdog"; eine Schicht verlässt `geplant` also, sobald Zeit zu ihr erfasst ist. **ABGELEITET, nicht fortgeschrieben** — derselbe Abschnitt warnt an genau dieser Tabelle „two sources of truth would drift within one sprint", und deshalb rechnet der Auslöser den Zustand jedes Mal neu aus den Zeiteinträgen, statt ihn hochzuzählen: ein storniertes Stempelpaar nimmt ihn ZURÜCK auf `geplant`, ein Eintrag, der die Schicht wechselt, zieht BEIDE nach. Ein Zähler wäre nach dem ersten Storno falsch — und die stündliche Wache fände die Schicht nie wieder. **`storniert` rührt er nie an:** eine abgesagte Schicht mit erfasster Zeit ist ein Befund für die Disposition und keiner, den ein Auslöser stillschweigend wegräumt. **Der Auslöser ist `security definer` mit einem Schreibrecht auf EINE Spalte** (`grant update (status)`, dieselbe Form wie `0374` für `besetzt_anzahl`): eine Arbeiterin, die stempelt, hält `dienstplan.schreiben` nicht — und ein blankes `grant update on einsatz` öffnete jeder künftigen Definer-Funktion still den ganzen Dienstplan. **Der stündliche Lauf rechnet nichts Eigenes**, er ruft dieselbe Ableitung: er schliesst das eine Fenster, das kein Auslöser sehen kann — wer um 11:00 aus einer Schicht bis 14:00 aussteigt, lässt eine zurück, an der danach niemand mehr stempelt. 11 Prüfungen in `tests/isolation/einsatzstatus.test.ts` |
| V-083 | `zeiteintrag.status = 'offen_nacherfassung'` hat keinen Erzeuger, steht aber als Filter und als Pille in **vier** Oberflächen | `drizzle/0034_zeiteintrag.sql:78` | behindert | **erledigt — und zwar OHNE den Erzeuger**, das ist der Punkt. Ihn zu bauen wäre die naheliegende Antwort und die falsche: er wäre der ehrliche Zustand für „von der Planung gesetzt, aber noch nicht bestätigt", nur beschreibt **kein Dokument, wer ihn wieder wegnimmt und was bis dahin gilt** — zählt die Stunde ins Stundenkonto, steht sie im Monatsnachweis, darf sie abgerechnet werden? Ein Eintrag in einem Zustand, aus dem kein Weg herausführt, ist schlimmer als keiner; die Frage steht als **O-890** beim Auftraggeber, und `schliesseLaufendenEintrag` schliesst bis dahin nach `abgeschlossen` mit vollständiger Spur. **Der wirkliche Fehler war ein anderer:** die Zeitenliste bot das Merkmal als lebendige Auswahl an, und wer es wählte, bekam eine leere Liste ohne Grund — sie sagt von sich aus nicht, ob niemand gearbeitet hat oder ob dieser Zustand gar nicht entstehen kann. Jetzt steht der Satz daneben, mit der Nummer der offenen Frage und mit dem Weg, den ein Mensch stattdessen sucht (Merkmal **Nacherfasst**). **Das Merkmal bleibt in der Auswahl:** der Satz der Merkmale ist der Satz der Zustände, und eines herauszunehmen hiesse, die Auswahl bei der Antwort wieder zu ändern und bis dahin zu verschweigen, dass es den Zustand gibt. Die drei Pillen auf den Vorgangsblättern bleiben ebenso — sie kosten nichts und halten, sobald O-890 beantwortet ist. 3 Prüfungen in `tests/kern/offen-nacherfassung.test.ts`, eine davon fällt um, sobald irgendwo jemand den Wert SCHREIBT: dann ist O-890 beantwortet und der erklärende Satz gehört weg |
| V-084 | `mahnung.status = 'erledigt'` hat keinen Erzeuger, obwohl der Übergang ausdrücklich erlaubt ist | `drizzle/0130_pruefbefunde_finanzen.sql:475` | behindert | **erledigt** — `erledige` im Mahnungsdienst, `aktion=erledigen` auf der Route und ein Knopf auf dem Mahnungsblatt, sichtbar nur bei `versendet`. **Unter dem SCHWÄCHEREN Recht** (`mahnung.schreiben`, vor der zweiten `authorize`): es lässt nichts aus dem Haus, im Gegenteil — es schliesst einen Vorgang, der längst heraus ist; wer Mahnläufe führt, soll auch abhaken können. **Ob ein bezahlter offener Posten seine Mahnung von selbst schliesst, bleibt offen (neu: O-902)** — Teilzahlung, Storno und die Mahngebühren stehen davor, und ein Automatismus, der bei Teilzahlung falsch schliesst, ist schlimmer als keiner: eine geschlossene Mahnung wird nicht weiterverfolgt. Sperrklinke: `tests/kern/kleine-formulare.test.ts` §1 |
| V-085 | `angebot.status = 'abgelaufen'` hat keinen Erzeuger — der Lauf, für den `gueltig_bis` und ein eigener Teilindex gebaut wurden, fehlt | `drizzle/0024_angebot.sql:119` | behindert | **erledigt** — `jobs/statuslaeufe.ts`, `angebot_ablauf`, 02:10 Uhr, `uebergreifend`. **`0387` war nötig, und das ist der eigentliche Befund:** `cse_job` hatte auf `angebot` ÜBERHAUPT kein Recht — kein SELECT, kein UPDATE, keine Policy. Ein Lauf ohne diese Zeilen läse null Angebote und meldete „nichts abgelaufen", also die schlechteste Art von Fehler, weil sie wie ein ruhiger Betrieb aussieht. Das Recht ist auf ZWEI SPALTEN begrenzt (`status`, `geaendert_am`): eine Gültigkeitsfrist zu verschieben ist eine kaufmännische Entscheidung und kein Nachtlauf. Der heutige Tag zählt MIT — „gültig bis 23.09." heisst, dass am 23.09. noch angenommen werden kann —, und der Stichtag kommt aus der Datenbank, weil `current_date` im UTC-Prozess zwischen 00:00 und 02:00 noch den Vortag zeigt und genau dann Nachtläufe laufen. 7 Prüfungen in `tests/isolation/statuslaeufe.test.ts`, drei davon auf das, was `cse_job` NICHT darf |
| V-086 | `formular_eingang.status`: erzeugt wird ausschließlich `verarbeitet` — **`neu`, `spam`, `verworfen` nie** | `src/server/services/lead/annahme.ts:210` | behindert | **erledigt als Befund — und ausdrücklich NICHT gebaut.** Der naheliegende Griff wäre, die drei Werte zu setzen, und bei einem davon wäre er eine echte Verschlechterung. Dreimal derselbe Grund: (1) **`neu`** — Eingang und Lead entstehen in EINER Transaktion mit aufgeschobenen Fremdschlüsseln (`0017`); es gibt kein Fenster, in dem ein Eingang ohne Lead steht, der Wert ist der Spaltenvorgabewert, den dieselbe Anweisung überschreibt. (2) **`spam`** — der Honigtopf prüft in `api/anfrage` VOR jeder Datenbankberührung, ausdrücklich, damit ein Bot nicht einmal eine Abfrage kostet; eine Aufzeichnung machte daraus einen **Verstärker**: ein Ansturm, der heute nichts kostet, schriebe dann je Treffer eine Zeile — und fürs Ratenlimit gilt das doppelt, sein Zweck IST das Begrenzen von Schreibvorgängen. (3) **`verworfen`** — es gibt keine Liste von Einsendungen, die Arbeitsliste ist der Lead. **Der Preis wird nicht verschwiegen:** ein falsch positiver Honigtopftreffer (Passwortverwalter, Autofill in einem versteckten Feld) verschwindet SPURLOS, und der Absender bekommt dieselbe Dankseite wie bei Erfolg — die Anfrage ist verloren, und niemand erfährt es. Ob das so bleibt, ist eine Abwägung zwischen einer verlorenen Anfrage und einer Angriffsfläche und steht als **O-905** beim Auftraggeber. 5 Prüfungen in `tests/kern/formular-eingang-zustaende.test.ts`; eine davon misst die REIHENFOLGE im Quelltext der Route — wer den Honigtopf eines Tages hinter eine Abfrage schiebt, bekommt ein rotes Zeichen und nicht erst beim nächsten Ansturm eine Rechnung |
| V-087 | `kunde.status = 'gesperrt'` ist die **UWG-Werbesperre** und lässt sich nirgends setzen; `inaktiv` ebenfalls nicht | `drizzle/0020_crm_identitaet.sql:173` | behindert | **erledigt** — `setzeKundeStatus`, eigener Weg statt Auswahlfeld |
| V-088 | `betroffenenanfrage.status = 'identitaet_offen'` hat keinen Erzeuger, obwohl zwei Oberflächen ihn beschriften | `drizzle/0176_betroffenenanfrage.sql:49` | behindert | **erledigt** — `fordereIdentitaetsnachweis` und `identitaetGeklaert` in `services/datenschutz/anfrage.ts`, zwei Knöpfe auf der Vorgangsakte. **Der Zustand kommt nie allein:** `0388` legt `identitaet_angefordert_am` und `identitaet_grund` an und hält sie mit zwei CHECKs zusammen — kein Zeitpunkt ohne Grund, und kein `identitaet_offen` ohne Zeitpunkt. Art. 12 Abs. 6 erlaubt die Nachfrage nur bei *begründeten* Zweifeln, und wer eine Ausweiskopie verlangt, verarbeitet dafür mehr Daten, als das Auskunftsersuchen selbst enthält; der Grund ist der Beleg dafür und **bleibt auch nach dem Klären stehen**. **Die Monatsfrist läuft weiter** (O-903) — ob eine Rückfrage sie hemmt, sagt die Verordnung nicht, und eine angehaltene Frist wäre eine Rechtsauffassung, die sich als Spaltenwert tarnt. Geprüft in `tests/isolation/datenschutz-identitaet.test.ts` |
| V-089 | Der in der Migration benannte nächtliche Statuslauf **„gültig → abgelaufen"** für Nachweise existiert nicht | `drizzle/0030_nachweis_qualifikation.sql:595` | behindert | **erledigt** — `nachweis_ablauf` in derselben Datei, 02:10 Uhr, nach den Ablaufwarnungen (02:05) und vor dem Dienstplangenerator (02:15): erst warnen, dann den Zustand nachziehen, dann planen. Hier war alles vorbereitet — `0030` schreibt die Policy `n_job_status` samt `grant update (status, geaendert_am) … to cse_job` wörtlich hin und nennt den Lauf beim Namen; **gebaut war er nie.** Ein widerrufener Nachweis bleibt widerrufen: der eine wurde ENTZOGEN, der andere ist verfallen, und wer sie zusammenwirft, verliert die Auskunft, die im Streit zählt. Geprüft in §2 derselben Datei |

---

## 7. Felder, die kein Formular sendet

Die Route liest sie. Die Spalte ist da. Nur schickt sie niemand.

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-090 | **Der § 48-EStG-Einbehalt kann nie gebucht werden** — `bucheBauabzug` hat weder Route noch Knopf | `src/server/services/finanz/zahlung/index.ts:435` | blockiert | **erledigt** — eigener Abschnitt auf der Zahlungsseite, der GENAU die Rechnungen führt, die einen Einbehalt ausweisen und noch keinen gebucht haben. Ohne ihn blieben 15 % der Rechnungssumme dauerhaft offen: in jeder Altersliste und in jedem Mahnlauf, obwohl der Kunde sie ans Finanzamt abgeführt hat und nicht mehr schuldet. Gebucht wird erst hier und nicht beim Festschreiben — ob der Kunde tatsächlich einbehält, zeigt sich am Zahlungseingang |
| V-091 | **Posten gegen Posten ausgleichen** (§ 7.4) ist gebaut, aber nicht auslösbar | `src/server/services/finanz/zahlung/index.ts:530` | behindert | **erledigt** — Formular auf der Zahlungsseite, sichtbar nur, wenn es beides gibt: eine offene Forderung und ein Guthaben. Beide Posten bleiben stehen; was entsteht, ist die Verbindung zwischen ihnen — ohne dass eine Zahlung erfunden wird, die nie geflossen ist |
| V-092 | **Der nach § 7 Abs. 3 Nr. 4 UWG vorgeschriebene Widerspruchslink wird von keinem Weg ausgegeben** | `src/server/services/datenschutz/werbewiderspruch.ts:142` | behindert | **erledigt mit V-115** — `sendeNachAussen` hängt bei `zweck = 'werbung'` den Pflichthinweis samt Link an und vermerkt den Token. **Der ganze Widerspruchsweg stand — und der Schlüssel dazu entstand nirgends:** die öffentliche Seite, die Einlösung, die Drossel, das Protokoll, das UWG-Tor in der Datenbank, alles gebaut und geprüft; `gibTokenAus` hatte im ganzen Baum **keinen Aufrufer**. Solange kein Versender verbunden ist (O-36), fällt das nicht auf — in der Sekunde, in der einer verbunden wird, ginge eine Werbemail ohne den vorgeschriebenen Hinweis hinaus, und das ist keine fehlende Bequemlichkeit, sondern eine Abmahnung. **Der Hinweis hängt am SENDEWEG und nicht am Aufrufer:** es gibt genau einen Weg nach draussen (Invariante 7), und ein Aufrufer, der ihn vergisst, wäre genau der Fehler. Der Satz trägt beide Bestandteile des Absatzes wörtlich — jederzeitiger Widerspruch UND keine Kosten über den Basistarif —, denn das Gesetz verlangt ihn „bei jeder Verwendung", nicht einmal im Impressum. **Die Reihenfolge ist zwingend und geprüft:** Versender-Wand zuerst (sonst würde ein Token für nichts verbrannt), dann Klartext, dann Text, dann Nachricht, dann Vermerk — `werbewiderspruch_token.nachricht_id` trägt einen Fremdschlüssel, und eine ausgehende Nachricht wird nicht nachträglich umgeschrieben. **Ohne `CSE_KANONISCHE_BASIS` wirft es:** `/werbewiderspruch/…` in einer E-Mail ist kein Link, sondern Text, und eine Werbemail mit unklickbarem Hinweis erfüllt den Absatz nicht. **`kunde_id` bleibt leer** — der Widerspruch bindet den Menschen, der die Nachricht bekam; ihn auf die Firma auszudehnen schaltete Kolleginnen stumm, die nie widersprochen haben. 10 Prüfungen in `tests/kern/uwg-pflichthinweis.test.ts` und 7 in `tests/isolation/uwg-widerspruchslink.test.ts` — letztere führen die Kette bis zum ECHTEN Tor `app.darf_kontaktiert_werden` |
| V-093 | **Das einseitige Aufmass ist eine Sackgasse** — die Erhebungsart ist wählbar, die dafür verlangte Ankündigung nirgends eintragbar | `src/app/portal/[mandant]/bau/projekte/[id]/aufmass/neu/page.tsx:218` | blockiert | **erledigt** — `gegenzeichne` nimmt jetzt eine ROLLE und die Ankündigung entgegen, die Route reicht beide durch, und das Freigabeblatt trägt den Block „Einseitige Feststellung" für ein Blatt mit `erhebungsart = einseitig`. **Die Sackgasse war vollständig gebaut und trotzdem geschlossen:** `kern.aufmass_status_setzen` (0072) hebt ein Blatt auf `einseitig_festgestellt`, sobald der AUFTRAGNEHMER unterschreibt UND `ankuendigung_am` steht — `gegenzeichne` schrieb `rolle = auftraggeber` fest verdrahtet in den INSERT, und `ankuendigung_am` hatte überhaupt keinen Schreiber. `pruefeVorlage` meldete korrekt „Ein einseitiges Aufmaß setzt die angekündigte Feststellung voraus", und es gab keinen Weg, den Satz abzustellen. Die Ankündigung wird deshalb VOR `pruefeVorlage` geschrieben und nur, wenn noch keine dasteht: eine einmal erklärte Aufforderung wird nicht umdatiert. `anstellung_id` ist bei `auftragnehmer` Pflicht (`as_auftragnehmer_hat_anstellung`) und fällt sonst auf `aufgenommen_von_anstellung_id` zurück — wer gemessen hat, ist der naheliegende Feststellende. **Keine Frist wird geprüft und keine erfunden** (O-156); der Block sagt das hin. Drei neue Prüfungen gegen den DIENST, nicht gegen rohes SQL |
| V-094 | **Die Gegenzeichnung des Auftragnehmers am Leistungsnachweis** ist gebaut, aber beide Unterschriftswege setzen die Rolle fest auf `auftraggeber` | `src/server/services/reinigung/leistungsnachweis.ts:125` | blockiert | **erledigt** — das Unterschriftsblatt wählt die Rolle und die Beschäftigung. `signiere` kennt beide Rollen seit je, `leistungsnachweis_signatur` sieht sie mit `lns_auftragnehmer_hat_anstellung` eigens vor, die Kundenansicht zeigt sie an — und das einzige Formular schickte `<input type="hidden" name="rolle" value="auftraggeber" />`. Die Gegenzeichnung der eigenen Objektleitung konnte nie entstehen. Vorgabe bleibt der Auftraggeber: das ist der Bildschirm, den der Kunde vor Ort sieht. Die Beschäftigung ist bei `auftragnehmer` Pflicht — der Auftragnehmer sind wir, und wir unterschreiben mit einer Beschäftigung; der Auftraggeber hat keine bei uns (D-09). 2 neue Prüfungen, davon eine, die die Datenbankbedingung beim Namen nennt |
| V-095 | **Eine falsche Mannstunden- oder Positionszeile im Bautagebuch** ist über die Oberfläche nicht korrigierbar | `src/app/api/bau/bautagebuch/route.ts:188` | behindert | **erledigt** — je Zeile ein „Korrigieren" an Mannstunden und Positionen, solange der Tag offen ist. **Alles war gebaut ausser dem Formular:** `korrigiereMannstunden` und `korrigierePosition` im Dienst, `korrektur_mannstunden` und `korrektur_position` in der Route, `storniert_am`/`storno_grund`/`ersetzt_durch_id` seit `0082` in der Tabelle — und der Satz über der Tabelle erklärte die Regel sogar („eine falsche Zeile wird nicht geändert, sondern storniert und durch eine neue ersetzt"). Es gab keinen Knopf dafür. Eine falsche Zeile stand damit dauerhaft im Bautagebuch, also in dem Dokument, das im Streit über den Bauablauf zählt. Das Formular ist vorbelegt mit der alten Zeile — wer sich nur in den Minuten vertippt hat, ändert genau diese eine Zahl — und verlangt den Grund, weil eine Korrektur ohne Grund im Streitfall die halbe Auskunft ist (LEG-01). Sekundärer Knopf, nicht roter: die Wache `roter-knopf-in-schleife` hat den ersten Entwurf mit `variante="primary"` in der Zeilenschleife zu Recht abgewiesen (DESIGN §5). Der Dienstweg ist geprüft in `tests/isolation/bau-bautagebuch.test.ts` §1 („die Korrektur ist eine NEUE Zeile, und die alte bleibt lesbar stehen") |
| V-096 | **Eine Aufgabe lässt sich mit keinem Vorgang verknüpfen** — die Route nimmt fünf Bezugsfelder, das Formular sendet keines | `src/app/portal/[mandant]/aufgaben/page.tsx:246`, `src/app/api/aufgaben/route.ts:136` | behindert | **erledigt** — ein Auswahlfeld auf der Aufgabenseite, `bezugskandidaten` im Dienst, `gewaehlterBezug` in der Route. **Alles war da ausser dem Feld:** `aufgabe` trägt fünf Bezugsfelder, die Route nimmt alle fünf entgegen, `loeseBezugAuf` löst sechs Arten auf, die Detailseite zeigt den Verweis und die Liste lässt sich danach filtern — das Anlegeformular schickte keines davon. Jede von Hand angelegte Aufgabe stand frei in der Luft: „Rechnung prüfen" — welche? **EIN Feld, nicht zwei** (`<typ>:<kennung>`): zwei wären zwei Zustände, die auseinanderlaufen können, und genau das weist `aufgabe_bezug_paarweise` in der Datenbank ab — ein Formular, das einen verbotenen Zustand erzeugen kann, schiebt die Fehlermeldung nur nach hinten. **Angeboten wird genau das, was zurückführt:** `bezug_typ` hat sechsundzwanzig Werte, `AUFLOESER` kennt die sechs mit einer Detailseite; einen siebten anzubieten hiesse, eine Aufgabe an etwas zu hängen, dem man nicht folgen kann. **Für die drei Arten mit echtem Fremdschlüssel wird BEIDES gesetzt** — `auftrag_id` trägt die Liste (über Joins), das polymorphe Paar die Detailseite; nur eines zu setzen liesse die jeweils andere Ansicht still leer. **Die RLS entscheidet, was wählbar ist**, nicht eine zweite Rechteprüfung im Dienst: wer `crm.lesen` nicht hält, bekommt keine Kunden zur Auswahl. Je Art 50 Zeilen — eine Auswahl ohne Deckel ist nach zwei Jahren keine. 8 Prüfungen in `tests/kern/aufgabe-bezug.test.ts` und 8 in `tests/isolation/aufgabe-bezug.test.ts` |
| V-097 | **Der Hauptkontakt eines Kunden lässt sich nicht bestimmen**, obwohl die Seite das Etikett anzeigt | `src/app/portal/[mandant]/crm/kunden/[id]/page.tsx:300`, `src/app/api/crm/kunde/route.ts:69` | behindert | **erledigt** — `setzeHauptkontakt` in `services/crm/aendern.ts`, `aktion=hauptkontakt` auf `api/crm/kunde`, ein Knopf auf dem Kontaktblatt. **Nicht in `aendereKontakt`**, und der Kopf jener Funktion behält recht: der Hauptkontakt ist eine Eigenschaft des KUNDEN, nicht des Kontakts — es kann nur einer sein, und ein Formular je Kontakt kann das nicht sicherstellen. **Zuerst löschen, dann setzen:** `ansprechpartner_hauptkontakt_uk` ist nicht aufgeschoben, und in der anderen Reihenfolge fiele die Eindeutigkeit mit einer Meldung, an der Stelle, an der niemand danach sucht. Geprüft wird VOR dem Löschen, sonst stünde der Kunde nach einem Tippfehler ganz ohne Hauptkontakt da. 6 Prüfungen in `tests/isolation/hauptkontakt.test.ts` |
| V-098 | **Eine Mahnstufe lässt sich nur ohne Folgeaktion bestätigen** — das Formular hat kein Feld dafür | `src/app/portal/[mandant]/einstellungen/mahnwesen/page.tsx:171` | behindert | **erledigt** — das Formular schickt `folgeaktion`, mit allen vier Werten des Aufzählungstyps und der Vorgabe „keine". `bestaetigeStufe` nahm sie seit je entgegen; die Liste zeigte die Spalte an, und was dort stand, war nicht die Entscheidung eines Menschen, sondern der Vorgabewert der Spalte. **Kein anderer Vorgabewert:** welche Aktion zu welcher Stufe gehört, ist eine Vereinbarung — und die drei übrigen sind ernst (ein Lieferstopp trifft den laufenden Auftrag, ein Mahnbescheid ist ein gerichtliches Verfahren). Die Seite sagt ausdrücklich, dass die Plattform sie NICHT auslöst, sondern festhält, was vereinbart ist. Sperrklinke: §3 derselben Datei |
| V-099 | **Brief-, Rechnungs- und Angebotsfußzeile sind pflegbar, erreichen aber kein einziges Dokument** (K-12) | `src/app/portal/[mandant]/einstellungen/identitaet/page.tsx:244` | behindert | **erledigt** — alle drei erreichen jetzt ihr Dokument, und jede auf dem Weg, den ihre Bindung verlangt. **Der Befund war wörtlich wahr:** `brief_fuss`, `rechnung_fuss` und `angebot_fuss` kamen im ganzen Baum nur in der Anzeige derselben Einstellungsseite vor — wer dort etwas eintrug, sah es genau dort wieder und nirgends sonst. `0200` sagt es an der Spalte selbst: „bei der Festschreibung in den kanonischen Payload zu KOPIEREN. Die Kopie ist noch nicht gebaut." **Angebot** (`angebote/[id]/pdf`): aus den lebenden Stammdaten, wie alles andere auf diesem Blatt — ein Angebot ist kein festgeschriebenes Dokument. Sie steht VOR den Pflichtangaben (§ 35a GmbHG), damit ein freier Text nicht zwischen ihnen wie eine Auswahl aussieht. **Mahnbrief** (`mahnungstext`): am Ende, durch eine Trennlinie abgesetzt — darüber steht, was diesen Vorgang betrifft, darunter, was unter jedem Brief steht. **Und sie geht in die NUTZLAST der Freigabe**, nicht nur in den Brief: der Hash über die Nutzlast ist das, was eine Freigabe an einen Inhalt bindet; läge sie draussen, ginge Text hinaus, den niemand gesehen hat, und die Freigabe bliebe trotzdem gültig (Invariante 7). Der Preis ist gewollt — wer die Fußzeile ändert, während eine Mahnung auf Freigabe wartet, muss sie neu freigeben lassen; der Brief ist danach ein anderer. **Rechnung: die Gestalt steigt auf `cse.rechnung.v3`** (K-12). Läse das PDF die Fußzeile aus der lebenden `mandant_identitaet`, änderte jede spätere Pflege rückwirkend jedes alte Dokument — und der Hash bliebe derselbe, weil er über die Nutzlast geht und nicht über die Anzeige. Die Kette sähe heil aus und beschriebe ein anderes Blatt. **v2 bleibt vollwertig lesbar, und das ist der Unterschied zu v1:** v1 trug die Anschrift als EINE Zeile, EN 16931 verlangt vier Felder, und aus einer Zeile liessen sie sich nur raten — deshalb Storno und Neuausstellung. v3 fügt ein Feld HINZU; eine v2-Zeile hat es nicht, weil sie entstand, bevor die Fußzeile überhaupt ein Dokument erreichte. **Keine bestehende Kette wird entwertet.** Die Toleranz ist eine eigene Funktion (`neuerFeldwert`) und kein `?? null` im allgemeinen Feldzugriff — sonst läse sich eine beschädigte Zeile als eine mit Nullwerten; eine Prüfung hält beides auseinander. **Nicht angefasst: die Kundenansicht auf dem Bildschirm.** Sie liest lebende Stammdaten, nicht den Snapshot; die Fußzeile dort einzublenden zeigte dem Kunden eine andere als auf seinem PDF. Das zu ändern hiesse, die ganze Ansicht auf den Snapshot umzustellen — eine eigene Runde, und sie hat nichts mit dieser Fußzeile zu tun. 9 Prüfungen in `tests/kern/rechnung-fusszeile.test.ts` |
| V-100 | **Logo, Avatar und Titelbild einer Gesellschaft lassen sich auf keinem Weg setzen** — weder Behälter noch Feld | `src/app/portal/[mandant]/einstellungen/identitaet/page.tsx:154` | behindert | **erledigt** — Einstellungen › Identität hat einen Abschnitt „Logo, Avatar und Titelbild“ mit Vorschau, Hochladen/Ersetzen und „Zuordnung entfernen“, gesperrt und benannt, wenn kein Speicher verbunden ist; die Formulare nur mit `system.identitaet_verwalten` (AUT-06). `POST /api/einstellungen/identitaet/bild` → `services/mandant/markenbild.ts`: Logos als SVG/PNG/JPEG, Avatar und Titelbild als PNG/JPEG, erkannt am Inhalt; SVG gegen Skript, Ereignisattribute, eingebettetes HTML und Verweise nach draussen geprüft (`storage/svg.ts`); Raster ohne Metadaten; der Inhalt als Name im PRIVATEN Behälter `marke`; Alternativtext Pflicht; kein stiller Erfolg ohne Recht. `0393` gibt das Spaltenrecht und hält mit `mi_bildpfad_eigen` fest, dass ein Pfad nur in den eigenen Ordner zeigt. `GET /api/marke/…` liefert nur für eine veröffentlichte Identität oder eine Sitzung in dieser Gesellschaft. **Die Bilder erreichen ihre Stellen** (DESIGN §1 „Uploaded brand images“): Avatar statt Platzhalterzeichen auf Karte, Fuss, Gesellschaftswahl, Impressum und Kopfbild; Logo für dunkle Flächen im Kopfbild und im Kopf der Profilunterseiten; Titelbild auf Karte und Kopfbild; Drucklogo auf dem gedruckten Angebot (DESIGN §11 — dort stand bis hierher nur der Name). Dabei nebenbei richtiggestellt: zwei veraltete K-12-Sätze (Seite und Dienstkopf) und der Spaltenkommentar von `rechnung_fuss`, die nach V-099 noch „nicht gebaut“ sagten. Belegt durch `tests/kern/markenbild.test.ts` (24) und `tests/isolation/markenbild.test.ts` (14), `mandant-identitaet.test.ts` um den fremden Pfad erweitert (D-628) |
| V-101 | **`sendeNachAussen` ist gebaut und getestet, aber keine Route ruft es** — auch der zugesagte Endpunkt `POST /api/crm/nachrichten` fehlt | `src/server/services/kern/nachricht.ts:532` | behindert | **erledigt** — `POST /api/crm/nachrichten` (Recht `crm.kommunikation_versenden`, erstmals von einer Route benutzt) ruft `services/crm/nachricht-an-kontakt.ts`, und der ruft `sendeNachAussen`. Das Kontaktblatt hat statt des Satzes „Es gibt hier keinen Sendeknopf“ einen Abschnitt „Nachricht senden“ mit Kanal, Zweck, Betreff und Text. **Die Reihenfolge ist entschieden (D-627):** UWG-Tor zuerst — ein Kontakt ohne Rechtsgrundlage bekommt ein „nein“, auch wenn kein Versender da ist; dann der Versender — nicht verbunden (O-36) heisst: NICHTS wird geschrieben, keine Freigabe, keine Nachricht, und der Knopf ist gar nicht erst scharf; dann die benannte Freigabe des Verfassers mit dem Abdruck genau dieses Textes, `gate()`, Versand — in der Transaktion der Anfrage. Belegt durch `tests/kern/nachricht-an-kontakt.test.ts` (der Abdruck ändert sich mit jedem Wort, Kanal, Zweck, Betreff und Empfänger; die Listen sind die des Datenbanktyps) und `tests/isolation/nachricht-an-kontakt.test.ts` (unbekannter Kontakt → 404 wie ein fremder, UWG antwortet vor „nicht verbunden“, ohne Versender bleiben `freigabe`, `nachricht` und `werbewiderspruch_token` unverändert, leerer Text und unbekannter Kanal/Zweck werden abgewiesen); das Recht an der Route halten `tests/kern/routen.test.ts` und `tests/kern/portal-shell.test.ts` fest |
| V-102 | **Die Systemmeldungen im Posteingang sind hart deutsch** — auch die Ablaufwarnung, die eine Sperre ankündigt | `src/server/services/nachweis/benachrichtigung.ts:53` | behindert | **erledigt** — `src/lib/i18n/benachrichtigung.ts` trägt die drei Meldungen in de/en/ar/tr, `BenachrichtigungsKontext` bekam ein `sprache`, und die drei Erzeuger reichen `person.sprache` herein. **Übersetzt werden genau drei Arten, und die Auswahl hat einen Grund, keine Grenze:** es sind die, deren Ziel in `/portal/mein` liegt — die, die einen ARBEITER erreichen. Das Verwaltungsportal bleibt deutsch (CLAUDE.md), und §5 des Prüfstücks fährt jede ANDERE Art durch alle vier Sprachen und verlangt denselben Text. **Die Sprache der Empfängerin, nicht die der Gesellschaft** (O-889, weiter offen): eine Sperrankündigung nach § 34a GewO, die ihr Adressat nicht lesen kann, hat ihren Zweck verfehlt — jemand erscheint zur Schicht und wird weggeschickt. **Eingesetzter Text bleibt, wie er kommt** — Qualifikationsbezeichnung, Begründung der Planung, Firmenname; sie zu übersetzen hiesse, sie zu erfinden. Übersetzt wird, was die Plattform selbst formuliert: das arabische „bis" zwischen zwei Kalendertagen fehlte zunächst und stand als deutsches Wort mitten im arabischen Satz. **Alle drei neuen Verbünde sind LINKS** — die Sprache ist ein Zusatz, kein Filter; ein innerer Verbund liesse die Meldung ausfallen, sobald eine Policy die Person ausblendet. **Und ein Spaltenrecht fehlte:** `0149` gibt `cse_job` auf `person` nur `select (id, vorname, nachname)`; `0391` nimmt `sprache` dazu, sonst bräche der Nachtlauf mit `permission denied for column sprache`, sobald `nachweis_warnungen` die Rolle bindet (D-378). Die Vorschau auf `/portal/konto/benachrichtigungen` zeigt jetzt die Sprache, in der zugestellt wird. 19 Prüfungen in `tests/kern/benachrichtigung-sprachen.test.ts`, 8 in `tests/isolation/benachrichtigung-sprache.test.ts`, 3 in `tests/isolation/einwand-entscheidung-meldung.test.ts` §4 |

---

## 8. Reihenfolge der Reparatur

Nicht nach Modul, sondern nach der Frage: **Kann ein Betrieb am Montag
anfangen?**

**R1 — Ohne diese kann niemand anfangen.** Ein Betrieb legt zuerst Orte und
Kunden an, dann plant er. **Geschlossen.**
V-001 Objekt · V-002 Revier · V-003 Bauprojekt · V-004 Veranstaltung ·
V-035/V-036 die Knöpfe zu den fertigen Kunden- und Lead-Masken ·
V-017 Kunde ändern

**R2 — Die Zeitkette schließen.** Das ist die Kette, an der Lohn und § ArbZG
hängen; der Mandant hat sie ausdrücklich benannt. **Geschlossen.**
V-049 Schicht zusagen ✓ · V-051 Entscheidung erreicht die Kraft ✓ · V-053 Monat
wechseln ✓ · V-064 laufenden Eintrag schließen ✓ · V-065 Ausgleichsbuchung ✓ ·
V-008 Stundenkonto eröffnen ✓ · V-068 Ausstempel-Marke ✓

**R3 — Sichtbarkeit.** Gebaut und unerreichbar ist teurer als nicht gebaut,
weil niemand danach sucht.
V-028 Radar ✓ · V-029 Berichte ✓ · V-030 Datenschutz-Posteingang ✓ · V-033
Check-in-Marken ✓ · V-034 Nacherfassung ✓ · V-038/V-039 Konto ✓ ·
V-041/V-042 Buchhaltung und Finanzen ✓ · V-043 Kundenportal-Leiste ✓ ·
V-044 Objektreiter

**R4 — Personal und Recht.**
V-010 Qualifikationsnachweis ✓ · V-014 Telefonanmeldung ✓

**R5 — Finanzen.**
V-011 Ausgabe · V-027 DATEV · V-084 Mahnung erledigt

**R6 — Der Rest**, in Registerreihenfolge.

---

## 8a. Nachgetragen: gefunden, während anderes gebaut wurde

Diese Befunde standen in keinem Suchlauf. Sie sind beim Bauen aufgefallen und
stehen hier, weil ein Befund ohne Nummer ein Befund ist, den niemand wiederfindet.

| Nr | Was | Beleg | Grad | Stand |
|---|---|---|---|---|
| V-103 | **Der Stichtag war der UTC-Tag, nicht der Berliner.** `benutzer_mandant.gueltig_ab` wird mit `app.berlin_heute()` gestempelt, `app.ist_mitglied` prüfte gegen `CURRENT_DATE` in einer UTC-Sitzung. Zwischen 00:00 und 02:00 Berliner Zeit ist damit **jede neu angelegte Mitgliedschaft für bis zu zwei Stunden unwirksam** — wer in diesem Fenster eine Leitung einlädt, legt ein Konto an, das sich anmelden kann und nichts sieht. Dieselbe Vorgabe stand in `app.katalog_positionen` und `app.leistungswerte_lesen`, wo ein **Preis** daran hängt. | Aufgefallen, weil der Seed um 23:23 UTC mit „Der Verantwortliche gehoert nicht zu dieser Gesellschaft" abbrach — derselbe Seed war Stunden zuvor durchgelaufen | blockiert | **erledigt** — `drizzle/0375`, plus eine Sperrklinke: `tests/isolation/stichtag-berlin.test.ts` §3 verlangt, dass **keine** Funktion in `app`/`kern` mehr auf `CURRENT_DATE` vorgibt |
| V-104 | **Neun rote Knöpfe auf einem Bildschirm.** `/dev/anmelden` gab jeder Kontozeile einen `primary`-Knopf. DESIGN §5: „one primary button per view" — Rot ist knapp, und ein Bildschirm mit neun roten Knöpfen hat **gar keine** Hauptaktion. | `src/app/dev/anmelden/page.tsx` | Schönheit | **erledigt** — die ganze Zeile ist der Knopf (DESIGN §5 „Chooser rows") |
| V-105 | **Das Wechselblatt belegte 9 % des Bildschirms.** Eine Überschrift, ein Satz, ein Knopf — der Rest schwarz. Und es liess den Leser rekonstruieren, was er verlässt: der Bereichswechsel ist die Handlung, die eine Rechnung in die falsche GmbH legt. | `src/components/portal/Wechselblatt.tsx` | behindert | **erledigt** — zeigt jetzt **beide** Seiten mit ihrem Zeichen und ihrer Identitätsfarbe, mit einem Pfeil dazwischen |
| V-106 | **Sieben gleich aussehende graue Wörter in der Kopfzeile.** „Bereich wechseln · Konto · Website · Deutsch English · Abmelden" — alles gleich gewichtet heisst nichts gewichtet; nichts sagte, dass zwei davon eine Wahl sind. | `src/components/portal/PortalRahmen.tsx` | behindert | **erledigt** — drei Gruppen mit Haarlinie (wohin · wie · raus), Sprache als Schiene, Abmelden in `danger-soft` |
| V-107 | **Der Windows-Start brach zweimal an PowerShell ab**, nicht an der Datenbank: erst am Init-Server des Postgres-Abbilds, dann daran, dass PowerShell 5.1 jede stderr-Zeile eines nativen Befehls zu einem terminierenden Fehler macht. | `scripts/windows-start.ps1` | blockiert | **erledigt** — Warten über TCP, Umleitung **im** Behälter, `Continue` für die Dauer der Schleife |

| V-108 | **Von 311 Verwaltungsseiten trugen ZWEI einen Rückweg.** Der Mandant hat es selbst gefunden: er klickte in der Beschäftigungsliste auf eine Person und stand auf einem Blatt ohne Ausgang; er öffnete das Stundenkonto und fand keinen Weg zurück. | `find "src/app/portal/[mandant]" -name page.tsx \| wc -l` → 311; mit Rückweg → 2 | blockiert | **erledigt** — der Rückweg wird jetzt aus der ADRESSE abgeleitet (`server/registry/rueckweg.ts`), vom Tor gegen die Rechte seines Ziels geprüft (AUT-06) und von der Hülle gezeichnet. **256 Seiten** haben ihn damit, 54 sind Modulwurzeln und sollen keinen haben. 11 Prüfungen in `tests/kern/rueckweg.test.ts` |
| V-109 | **Die Bedienfelder zeigten die Bordmittel des Browsers.** `<select>` mit Systempfeil (84 Dateien), `<input type="date">` mit einem Kalendersymbol, das auf dunklem Grund **schwarz auf schwarz** steht und damit unsichtbar ist (55 Dateien), Häkchen im Systemblau. DESIGN §5 „Forms" beschrieb das Feld seit jeher — nirgends stand es als Regel. | `grep -rl 'type="date"' src/app` → 55 · `grep -rl '<select' src/app` → 84 | behindert | **erledigt** — Grundregel in `globals.css`, `:where()` mit Spezifität null: greift überall, überschreibt nichts |

| V-112 | **Kein Test sah, ob ein Meldungsziel überhaupt existiert.** V-034 und V-069 sind zwei Ausprägungen DESSELBEN Fehlers: ein Zielauflöser schreibt eine Adresse hin, und niemand prüft sie. NOT-03 verlangt „a link to the thing“; geprüft wurde bis hierher nur, dass ein Ziel **nicht leer** ist — beide toten Verweise liefen deshalb jede Nacht ins Leere. | `tests/kern/benachrichtigung.test.ts` hatte sechs Prüfblöcke, keinen über Erreichbarkeit | behindert | **erledigt** — §7 prüft JEDES registrierte Ziel Segment für Segment gegen das Routenregister. Die beiden Befunde selbst stehen unter ihren eigenen Nummern (V-034, V-069) |

| V-113 | **`eroeffneKonto` verlor die Sollzeit still.** `on conflict do nothing` hiess: legt der Nachtlauf das Monatskonto mit `soll_minuten = 0` an (die Sollzeit ist offen, O-18), läuft jeder spätere Aufruf MIT einer Sollzeit ins Leere. Der Monat behielt 0, der Saldo war um die ganze Sollzeit falsch, und nichts wurde rot. Der Befund entstand erst dadurch, dass es den Rollover-Lauf überhaupt gibt (V-008). | Gefunden von `tests/isolation/stundenkonto.test.ts` §3, die den Vortrag über zwölf Monate auf die Minute nachrechnet: erwartet 3, erhalten 483 | blockiert | **erledigt** — `on conflict … do update`, aber nur für Felder, die der Aufrufer AUSDRÜCKLICH mitgibt, und nur solange der Monat offen ist |

| V-115 | **Der Token des Werbewiderspruchs wird von nichts ausgegeben.** `gibTokenAus` (`services/datenschutz/werbewiderspruch.ts:142`) erzeugt den Schlüssel, mit dem ein Empfänger seinen Widerspruch nach § 7 UWG / Art. 21 DSGVO belegen kann — und hat im ganzen Baum keinen Aufrufer. Der Widerspruchsweg existiert, der Schlüssel dazu entsteht nirgends. | Gefunden von `tests/kern/dienst-verdrahtung.test.ts` | blockiert | **erledigt mit V-092** — derselbe Befund aus der anderen Richtung, und jetzt hat der Token seinen Erzeuger: `sendeNachAussen` gibt ihn bei `zweck = 'werbung'` aus und schreibt den Pflichthinweis samt Link in den Text der Nachricht. Gespeichert wird der SHA-256, nie der Token (K-08). Die Kette ist bis zum ECHTEN Tor `app.darf_kontaktiert_werden` nachgezogen: nach der Einlösung geht an diesen Menschen keine Werbung mehr hinaus, und die FIRMA bleibt erreichbar — der Widerspruch bindet den, der die Nachricht bekam. Siehe die ausführliche Begründung bei V-092. 7 Prüfungen in `tests/isolation/uwg-widerspruchslink.test.ts` |

| V-116 | **Die GoBD-Löschung eines Dokuments hat ausser dem Test keinen Aufrufer.** `loescheDokument` (`services/dokument/loeschung.ts`) prüft Aufbewahrungsklasse, Löschsperre und Frist — und wird weder von einer Oberfläche noch von einem Aufbewahrungslauf gerufen. `/datenschutz/loeschkonzept` nennt je Klasse eine Frist und ihre Grundlage; wer das liest, liest eine **Zusage**. Eine Frist, die nur abläuft, ohne dass etwas geschieht, ist nach Art. 5 Abs. 1 lit. e DSGVO genau der Zustand, den man nicht haben darf — und vor einer Aufsicht ist die Zusage schlimmer als das Schweigen. | Gefunden von `tests/kern/dienst-verdrahtung.test.ts` | behindert | **erledigt** — Nachtlauf `dokument_aufbewahrung` (05:10, nach Kettenprüfer, Postenabgleich, Belegarchiv und Bewerberlöschung: erst steht fest, dass die Belege unversehrt sind, dann wird abgelegt, erst danach gelöscht). **Er erfindet keine Frist** — und genau deshalb darf er gebaut werden: `kern.setze_aufbewahrung` (0009) setzt `loeschsperre = true`, solange eine Kategorie keine oder nur eine Platzhalter-Regel hat (O-25, O-46), und solche Zeilen fallen schon aus dem `using` der Policy heraus — der Lauf SIEHT sie nicht. `04-SEITENKARTE.md` §5.16 sagt es wörtlich: „the purge job considers no row". `drizzle/0382` gibt `cse_job` genau drei Spalten (`geloescht_am`, `geloescht_von`, `loeschgrund`); ein `grant update` ohne Spaltenliste machte den Nachtlauf zu dem einen Weg, auf dem sich der Speicherort einer archivierten Rechnung nachts ändern liesse. **Und der erste Entwurf war falsch:** er suchte mit `alsJobRolle` quer über alle Gesellschaften, wie `bewerber_loeschung` es tut — `j_dokument_lesen` (0139) hängt aber an `app.aktiver_mandant()` und trägt **kein** `using (true)`. Der Lauf hätte jede Nacht null Zeilen gefunden, ohne ein rotes Zeichen (V-119 eine Tabelle weiter, diesmal VOR dem ersten Lauf bemerkt). Er ist deshalb `je_mandant` und sucht innerhalb der gebundenen Sitzung: was er findet, ist genau das, was er anfassen darf. Zurückgehalten wird getrennt gezählt — eine Zeile, die eine Buchung hält, SOLL bleiben, und sie in denselben Topf wie einen abgerissenen Speicher zu werfen machte aus einer richtigen Entscheidung ein rotes Zeichen, dem niemand mehr glaubt. 13 Prüfungen in `tests/isolation/aufbewahrung-lauf.test.ts` (davon 7 gegen die ROLLE, ohne eine Zeile Anwendungscode dazwischen), plus der Fall im Löschkonzept selbst |

| V-121 | **Die Seitenleiste zeigte auf 111 Seiten den falschen Punkt — oder gar keinen** | Gefunden vom Mandanten mit Bild: ein Klick auf **CRM** liess die Zeile grau, während **Objekte** sich hell färbte. Die Ursache stand in der Seite selbst: `crm/page.tsx` gab `aktiverTab="dashboard"` mit, und `Sidebar.tsx` vergleicht `schluessel === aktiv` — es leuchtete „Übersicht", während der Mensch auf CRM stand. Nachgemessen: **110 Seiten** mit dem falschen Punkt (87-mal `"mehr"`, obwohl ihr Bereich eine eigene Zeile hat; 19-mal `"dashboard"`) und **eine** mit einem Schlüssel, den kein Baum kennt. Jede für sich ein Tippfehler, zusammen eine Leiste, der man nicht glaubt | blockiert | **erledigt** — alle 110 richtiggestellt nach der Regel **längster passender Pfad gewinnt** (`bank` trägt `pfad: 'buchhaltung/bank'`, also gehört `buchhaltung/bank/import` zu `bank` und nicht zu `buchhaltung`; ein Vergleich nur des ersten Segments hätte 29 Seiten falsch gemeldet). `/benachrichtigungen` bleibt bewusst ohne Punkt: der Posteingang wird über die **Glocke** geöffnet und steht in keinem Baum — ihn auf „Mehr" zu setzen wäre dieselbe Lüge in die andere Richtung. **Und zehn Seiten unter `personal/` trugen das Prop gar nicht** — dieselbe Wirkung, nur unauffälliger: kein Tippfehler zu sehen, nur eine Leiste, die schweigt. Sie fielen durch das erste Raster, weil der Punkt `personal` als Pfad seine LANDESEITE trägt (`personal/anstellungen`) und `personal/personen` damit von keinem Präfix gedeckt war. Die Deckungsregel hat deshalb einen zweiten Teil: trägt ein Punkt einen mehrgliedrigen Pfad und beansprucht kein anderer Punkt das erste Glied allein, deckt er den ganzen Bereich. Sperrklinke: `tests/kern/aktiver-tab.test.ts` (5 Prüfungen, Ausnahmeliste mit Grund je Zeile) |

| V-122 | **Eine Kachel, die ein Weg ist, sah aus wie eine, die keiner ist** | Gefunden vom Mandanten mit zwei Bildern: „Neue Anfragen" öffnet eine Seite, „Vorgänge" tut nichts — und die beiden Kacheln sind **Pixel für Pixel gleich**. Dieselbe Fläche, derselbe Rand, dasselbe Symbol. `DESIGN.md` §Cards kannte nur eine **Hover**-Regel, und die antwortet zu spät: erst, wenn der Zeiger schon auf der Kachel liegt — auf einem Telefon nie | behindert | **erledigt** — DESIGN §Cards um den **Ruhezustand** erweitert (erst die Regel, dann der Code, wie CLAUDE.md es verlangt): eine interaktive Kachel trägt `pfeil-rechts` in der Ecke, `icon-sm`, `--text-subtle`. Eine **Form** und keine Farbe (§9), aus dem geschlossenen Satz, ohne neues Zeichen. Ihr Fehlen ist die andere Hälfte des Signals und nur lesbar, solange das Vorhandensein verlässlich ist — dafür `tests/kern/kachel-weg.test.ts` (3 Prüfungen) in **beide** Richtungen: keine Kachel in einem Verweis ohne `interaktiv`, und keine mit `interaktiv` ausserhalb eines Verweises. Die Sperrklinke zählt `<Link>` **und** `<a>`, weil die Kachelraster wegen `typedRoutes` gewöhnliche Anker benutzen — eine, die nur `<Link>` kennt, übersähe genau die Bildschirme mit den meisten Kacheln |

| V-127 | **Die Wache gegen den roten Knopf je Zeile hatte selbst einen Fehler** | Zweimal beim Schreiben neuer Seiten gemeldet, zweimal von Hand umgangen (V-120, V-011): `roter-knopf-in-schleife` fährt die Klammern einer `.map()`-Spanne mit und kannte Zeichenketten — aber **keine Kommentare**. Ein gewöhnliches `"` in einem deutschen Satz INNERHALB eines Kommentars („82,50") eröffnete damit eine Zeichenkette, die nie wieder zuging; von da an zählte die Klammerbilanz irreführend weiter, und ein Knopf hundertsechzig Zeilen später lag scheinbar in einer Schleife, die längst geschlossen war | behindert | **erledigt** — die Spanne fährt Kommentare jetzt MIT, statt sie vorher zu entfernen: `ohneKommentare` ersetzt einen Blockkommentar durch EIN Leerzeichen, und dann stünde im Befund eine fremde Zeilennummer — eine Wache, der man die Stelle nicht glaubt, wird umgangen (derselbe Grund, aus dem dort überhaupt eine Klammerbilanz steht und kein `grep`). **Der eigentliche Befund ist der Umgang damit:** eine Wache stirbt nicht durch Abschalten, sondern durch Gewöhnung an ihren Fehlalarm — ich hatte sie beide Male umgangen statt sie zu reparieren. `tests/kern/wachen.test.ts` §(3i) hält jetzt **beide Richtungen**: ein echter roter Knopf in einer `.map()` fällt, ein gerades Anführungszeichen und ein Apostroph im Kommentar lösen nichts aus |

| V-126 | **Eine Kontenzuordnung für eine Aufwandskategorie liess sich eintragen — und wurde nie gefunden** | Beim Bau des Ausgaben-Buchungssatzes (V-011) gefunden, und **beim ersten Anlauf falsch beschrieben**: ich las in `0126` den CHECK `km_typ_hat_eltern` („verbietet jede Zeile vom Typ `aufwand_kategorie`") und trug das als Befund ein, ohne nach einem späteren `drop constraint` zu suchen. `0180:469` hebt die Sperre auf und setzt `konto_mapping.ausgabe_kategorie_id` samt Fremdschlüssel. **Der wirkliche Befund lag eine Ebene tiefer:** die Stufenleiter in `app.konto_aufloesen` (0126:456–487) hatte **keinen Zweig für `aufwand_kategorie`** — jede solche Zeile bekam `stufe = null` und fiel aus `where ka.stufe is not null` heraus. Man konnte die Zuordnung also eintragen (`/buchhaltung/konten` führt sie als „Aufwandskonto (Kategorie)"), und der Auflöser fand sie nie. Betroffen war nicht nur die Ausgabe: `bucheEingangsrechnung` kontiert seit `0131` über denselben Typ | behindert | **erledigt** — `drizzle/0383`: `drop function` + neue Fassung mit `p_ausgabe_kategorie` (ein Parameter mit Vorgabewert erzeugt eine zweite Signatur, keine ersetzte — beide lebten sonst nebeneinander und ein Aufruf mit zehn Argumenten wäre mehrdeutig). **Vier Stufen, und die Reihenfolge ist die Aussage:** diese Kategorie UND dieser Satz → diese Kategorie → nur dieser Satz → das Sammelkonto. Dieselbe Bauart wie bei `erloes_leistung`, aus demselben Grund: die speziellere Zeile gewinnt, und wo keine steht, fällt es auf die allgemeinere zurück, statt gar nichts zu finden. **Die Stufen 3 und 4 sind kein erfundener Kontenrahmen** — sie greifen nur auf Zeilen, die jemand eingetragen hat; ohne Eintrag bleibt es bei „Kontenzuordnung fehlt", und die Zeile steht mit Hinweis in der Arbeitsliste (D-425). Welches SKR-Konto je Kategorie gilt, bleibt **O-05** und wird hier nicht erfunden. Gegenprobe in `tests/isolation/ausgabe-schreiben.test.ts` §4: ohne Zuordnung steht die Zeile ohne Konto, mit Zuordnung steht `4530` darauf — vor 0383 blieb sie unsichtbar |

| V-128 | **Der Knopf „Buchen" führte in eine rohe Ausnahme** | Gefunden von `tests/isolation/ausgabe-schreiben.test.ts` §4 beim ersten Lauf: das Testkonto hielt `eingang.*` und sonst nichts, und das Buchen scheiterte mit „Die Periode zum 2026-03-12 liess sich weder anlegen noch lesen — fehlt buchhaltung.schreiben?". Buchen legt den Monat über einen Definer an (`app.buchen_erlaubt` verlangt `finanzen.schreiben` ODER `buchhaltung.schreiben`) und **liest ihn als `cse_app` zurück** — und `t_mandant` auf `periode` verlangt dafür `buchhaltung.lesen` (0127). Der Mensch kam bis zum Klick durch und bekam einen 500 mit einer Meldung aus der Tiefe: ein 500 sagt „mein Fehler", wo „dir fehlt ein Recht" die Wahrheit ist | behindert | **erledigt** für die Ausgabe — `POST /api/finanzen/ausgaben` verlangt für `buchen` ein ZWEITES Recht (`buchhaltung.schreiben`), und der Knopf steht nur, wenn es gehalten wird; sonst steht der Satz, welches Recht fehlt (AUT-06). **Dieselbe Lage hat `POST /api/finanzen/eingangsrechnungen` mit `aktion=buchen`** — dort deckt der äussere `authorize` nur `eingang.schreiben`. Sie fällt nur deshalb seltener auf, weil die Periode im Betrieb meist schon offen ist; ist sie es nicht, ist es derselbe 500. Eigener Eintrag, eigene Prüfung — nicht in diese Änderung hineingezogen |
| V-129 | **`einsatz.anforderung_snapshot` und `einsatz.anforderung_erfuellt` haben keinen Schreiber — nirgends.** Eine Suche über das ganze Projekt findet den Spaltennamen genau EINMAL: in `drizzle/0028_dienstplan.sql:414`, wo er angelegt wird. Kein Dienst, kein Auslöser, kein Job füllt ihn. Der Kommentar daneben trägt die Begründung, für die er gebaut wurde: „SEC-04 wird an dem gemessen, was DAMALS verlangt war — ein später geänderter Anforderungskatalog darf eine vergangene Schicht nicht rückwirkend rechtswidrig machen." **Dieses Versprechen hält die Plattform nicht.** Das Tor läuft über `kern.stempel_einsatz_qualifikation` → `app.einsatz_qualifikation_erfuellt`, das `einsatzanforderung` LIVE liest; der Beweis wird je ZUORDNUNG gestempelt (`einsatz_zuordnung.qualifikation_snapshot`), nicht je Schicht. Die Wirkung ist heute gering — es gibt keine Oberfläche, die `einsatzanforderung` ändert (V-Nummer offen), also ändert sich der Katalog nicht. Sobald es eine gibt, verschiebt jede Änderung die Bewertung vergangener Schichten. Zwei ehrliche Auswege: den Schnappschuss beim Materialisieren füllen, oder die beiden Spalten streichen und den Kommentar richtigstellen. **Eine Wahl zwischen beiden ist eine Entscheidung über Rückwirkung und wird nicht nebenbei getroffen** | Gefunden beim Bauen von V-013 | behindert | **erledigt** — eingelöst, nicht gestrichen (D-624). `0394`: ein BEFORE-Auslöser schreibt die Anforderungsmenge in JEDE neue Schicht (auch ein rohes INSERT); bis zum Beginn zieht sie bei jeder Besetzungsänderung dem Katalog nach, ab dem Beginn ist sie eingefroren. `anforderung_erfuellt` wird gegen den Schnappschuss bewertet (`jeder`, `mindestens_einer` mit `mindestanzahl`, Registerpflicht) — `NULL` unbesetzt, meldet und sperrt nicht; gesperrt wird weiter je Zuordnung. Das Schichtblatt zeigt „Qualifikationsmischung“ und „Anforderungen dieser Schicht“. Belegt durch `tests/isolation/anforderung-schnappschuss.test.ts` (rohes INSERT, leere Menge, archivierte und künftige Anforderung, `jeder`, `mindestens_einer`, entfernte Zuordnung, Nachziehen bis zum Beginn, Einfrieren danach) und einen Seedlauf: 86 von 86 Schichten mit Schnappschuss |
| V-130 | **Ein Angebotsentwurf lässt sich weder berichtigen noch verwerfen** | Gefunden beim Bauen von V-005: `angebotsposition` trägt `revoke delete` (`0024:529`) und hatte keine Archivspalte; das einzige `update angebotsposition` im ganzen Projekt setzte den Einzelpreis nach einer bestätigten Kalkulation. Der Kopf stand genauso da — `archiviert_am` schrieb niemand, und `zurueckgezogen` stand seit `0024:27` im Aufzählungstyp, ohne dass ein Dienst ihn je setzte. **Ein Tippfehler in einem Entwurf stand damit dauerhaft in der Angebotsliste** | behindert | **erledigt** — `services/angebot/entwurf.ts` (berichtigen · entfernen · zurückziehen), `POST /api/angebot/entwurf`, eine Bearbeiten-Spalte am Entwurf und `drizzle/0392`. **Die offene Frage O-900 ist beantwortet (D-626), und die Antwort stand schon in der Tabelle:** `angebot_nummer_bei_versand` verlangt `(angebotsnummer is null) = (versendet_am is null)` — Nummer und Versand sind dasselbe Ereignis, und Invariante 4 zieht für die Rechnung genau dieselbe Linie. Ohne Nummer frei änderbar, mit Nummer unveränderlich. Die Härte auf einen ungesehenen Entwurf auszudehnen war keine Entscheidung, sondern eine Lücke. **Gelöscht wird nichts** (Invariante 8): eine entfernte Position behält ihre Zeile mit `entfernt_am`/`entfernt_von`, ein zurückgezogener Entwurf verlässt die Arbeitsliste und nicht die Datenbank. **Drei Riegel kamen beim Bauen dazu, jeder gegen einen Weg, der beim Kunden endet:** die letzte Leistungsposition bleibt stehen (ein Angebot ohne Leistung wäre ein Blatt mit Briefkopf und Bindefrist und nichts darin); ein zurückgezogener Entwurf geht nicht mehr hinaus (`status` und `versendet_am` sind getrennte Spalten, und `angebot_rueckzug_ehrlich` deckt nur die eine Richtung); und der Rückzug wird nicht wiederbelebt (`angebot_05_rueckzug`) — er ist eine Entscheidung, kein Zwischenstand. **Und zwei Summen, die sonst still falsch geworden wären:** `kern.aktualisiere_angebot_summen` und `kern.angebot_versand_festschreiben` zählen nur noch lebende Positionen — ohne das erste zeigte der Bildschirm eine Summe, die sich aus den sichtbaren Zeilen nicht nachrechnen lässt; ohne das zweite wiese `angebot_steuer` dem Kunden Umsatzsteuer auf eine Leistung aus, die im Angebot gar nicht mehr steht, eingefroren an der Stelle, an der niemand mehr nachrechnet. 17 Prüfungen in `tests/isolation/angebotsentwurf.test.ts` |
| V-131 | **Ohne Supabase-Zugang liess sich auf einem Vorführrechner keine einzige Datei ablegen** — Belege, E-Rechnungen, Baustellenfotos, Vergabeunterlagen, DATEV-Stapel, Unterlagen im Mitarbeiterportal: überall „nicht verbunden“. Und die Wahl des Speichers stand an 37 Stellen als `new SupabaseSpeicher()` | Gefunden beim Bauen von V-100 (Logo, Avatar, Titelbild) und vom Mandanten beim Einrichten auf einem neuen Windows-Rechner | behindert | **erledigt** — `waehleSpeicher()` (`server/storage/waehle.ts`) entscheidet an einer Stelle: Supabase, wenn verbunden; sonst der Vorführordner `OrdnerSpeicher`, wenn `CSE_SPEICHER_ORDNER` gesetzt ist, `devFlaechenAn()` gilt und nicht auf Vercel; sonst Supabase, nicht verbunden. Die Datei liegt wirklich im Ordner und kommt nur über `/api/speicher/…` mit Ablauf und HMAC heraus — kein Vortäuschen, kein öffentlicher Pfad (D-623). Einstellungen › Integrationen nennt ihn „Entwicklung“. Das Windows-Startskript setzt den Ordner auf `.speicher` und wechselt selbst in die Projektwurzel. **Der Seed nutzt ihn:** die acht Belegschaftsunterlagen bekommen ein als DEMODATEN beschriftetes Blatt, zwei Behinderungsanzeigen gehen über den echten `dokumentiereVersand` mit archiviertem Schreiben, und die E-Rechnung liegt im Freigabe-Posteingang. **Dabei gefunden und behoben:** „Dauerfrost unter −5 °C“ (echtes Minuszeichen U+2212, wie Word es setzt) brach die Anzeige ab — der PDF-Schreiber bildet jetzt gleichbedeutende Zeichen (Minus, Bindestriche, schmale Leerzeichen) ab und zählt nur noch, was eine andere Bedeutung hätte (`tests/kern/pdf-winansi.test.ts`). Belegt durch `tests/kern/vorfuehrspeicher.test.ts` (Ablage und Abruf über zwei Instanzen, Ausbruchsversuche aus dem Ordner, Ablauf auf die Sekunde, fremde Signaturen, die drei Schranken, die Wahl, die Auslieferung mit 200/403/404) und einen Seedlauf gegen eine frische Datenbank, zweimal hintereinander |
| V-132 | **Die Rechnungs-PDF trägt kein Logo** — DESIGN §11 verlangt „each entity prints its own logo“, und das Angebotsblatt tut es seit V-100; die ZUGFeRD/PDF-A-3-Rechnung druckt den Namen. Ein Logo dort ist ein K-12-Fall: es müsste wie die Fusszeile in den kanonischen Payload (dann `cse.rechnung.v4`), damit eine spätere Änderung keine festgeschriebene Rechnung verändert, und die Bildeinbettung in PDF/A-3 braucht die veraPDF-Prüfung der CI | Gefunden beim Bauen von V-100 | Schönheit | **erledigt** (D-625) — `cse.rechnung.v4` hält Schlüssel, SHA-256 und Typ des Drucklogos (sonst des hellen) fest; v2/v3 lesen sich weiter, mit `logo: null`, und der Leser weist eine v4-Zeile ohne das Feld ebenso ab wie eine v3-Zeile mit ihm. Das Blatt entsteht nur mit genau den Bytes, deren Prüfsumme in der Kette steht — sonst 409 mit Satz statt eines stillen anderen Dokuments. Oben links in `marke-xl`, wie das Angebot. SVG erreicht das PDF/A nicht; die Identitätsseite sagt es, wenn deshalb kein Logo auf die Rechnung käme. CMYK-JPEG wird beim Hochladen eines Papierlogos abgewiesen. **veraPDF**: ohne Logo, mit PNG samt Alphakanal und mit JPEG je PDF/A-3B, 0 verletzte Regeln. Prüfungen: `tests/kern/rechnung-logo.test.ts`, `tests/compliance/zugferd/verapdf.test.ts` |
| V-133 | **Der gebaute Auftritt, durchgeklickt: vier Seiten brachen, die keine Prüfung aufrief** — `/personal/nachweise/erfassen` (500, Spalte `ausgeschieden_am` gibt es nicht), `/objekte/[id]/bearbeiten` (500, entzogene Spalten `zutritt_hinweis`/`bemerkung` direkt gelesen), `/leistungen/unterhaltsreinigung` auf Deutsch und Englisch (500, die JSON-LD-Prüfung verlangte `provider`, den der Baustein absichtlich weglässt); dazu bei 1280px ein zweizeiliger roter Knopf und ein gekürzter Auftrittsname in der öffentlichen Kopfzeile, und die SVG-Richtlinie der Markenbilder kam nicht auf dem Draht an | Gefunden beim Durchlauf durch den Produktionsbau mit Chromium: rund 1800 Portalseitenaufrufe — 400, 350 und 300 als Leitung der drei Gesellschaften, 450 als Super-Administration, 200 als Beschäftigte (Anmeldung mit Telefon und Code), 100 in der Gruppensicht, 29 als Kunde; jede Adresse der Sitemap bei 1366 und 390px | behindert | **erledigt** — alle vier Seiten repariert; Kopfzeile ohne doppelten Aussenabstand (D-417 hält wieder); die SVG-Richtlinie als zweite Regel in `next.config.ts`. **Die Klasse ist geschlossen, nicht nur der Fall:** `tests/isolation/sql-schema.test.ts` parst jede SQL-Anweisung im Quelltext mit `PREPARE` gegen das Schema (rund 1975) und jede lesende aus Seiten, Diensten, öffentlichem Leser und Agenten mit `EXPLAIN (GENERIC_PLAN)` unter `cse_app` gegen die Spaltenrechte (rund 1100); beide Gegenproben melden den alten Fehler mit Datei und Zeile. Ein zweimal gemeldeter React-Hydrationsfehler (#418) liess sich in je eigenem Tab in 20 Ladevorgängen nicht wiederholen — er kam aus dem schnellen Seitenwechsel des Durchlaufs, nicht aus einer Seite |
| V-134 | **Das Rechnungsblatt druckte weniger, als die Rechnung sagt** — das PDF der ZUGFeRD-Rechnung hatte genau eine Seite (was darüber hinausging, lief unter den Fuss und vom Blatt), kürzte die Bezeichnung auf 60 Zeichen, zeigte nur Leistungszeilen, schrieb die Menge `30.870` (auf Deutsch dreissigtausend) und Daten als `2026-09-11`. Es fehlten Angaben, die in der Nutzlast stehen und auf die Rechnung gehören: der Hinweis auf die Steuerschuldnerschaft des Leistungsempfängers (§ 14a Abs. 5 UStG), die abgesetzten Abschläge mit Steuer und der Zahlbetrag (§ 14 Abs. 5 UStG), der Einbehalt nach § 48 EStG und der Überweisungsbetrag, der Befreiungsgrund einer steuerfreien Zeile, das Entgelt je Steuersatz, die USt-IdNr. des Empfängers, Leistungsort, Kopf- und Schlusstext, Zuschläge und Nachlässe. Wer nur das Blatt las, hätte bei einer Schlussrechnung den Bruttobetrag überwiesen. Ein ermässigter Satz von 2,5 % hätte als „3 %“ dagestanden (`toFixed(0)`) | Gefunden beim Bauen von V-132, beim Lesen von `zeichnePositionen` | blockiert | **erledigt** (D-629) — `zugferd/blatt-inhalt.ts` legt fest, WAS draufsteht, `zugferd/blatt.ts`, WIE es gesetzt wird. **Das Blatt rechnet nicht:** jeder Betrag ist ein Feld der Nutzlast, formatiert mit `formatiereGeld`/`formatiereMenge`, jeder Satz mit `prozentText` wie im Kundenportal; jeder Hinweistext kommt aus ihr. Mehrseitig mit Tabellenkopf und Fuss auf jeder Seite und „Seite x von y“; Zeilen brechen um, nichts wird gekürzt; der Zahlungsblock bleibt zusammen. Alle Masse und Farben aus DESIGN §11 (vorher ein eigenes Grau und 7-pt-Fusszeilen). Zeichen ausserhalb der eingebetteten Schrift werden `?` statt `.notdef` (ISO 19005-3, 6.2.11.8); die XML trägt den Namen unverändert. **veraPDF:** einseitig, mit Logo (PNG/JPEG), sechzig Positionen auf vier Seiten und ein arabisch-chinesischer Empfängername — je PDF/A-3B, 0 verletzte Regeln. Prüfungen: `tests/kern/rechnungsblatt.test.ts`, `tests/compliance/zugferd/verapdf.test.ts` |
| V-135 | **Der Schichtgenerator legte Schichten in die Vergangenheit** — sein eigener Kopf sagt „Die Vergangenheit wird nicht angefasst“, aber der Wächter `beginn_zeitpunkt > now()` stand nur am UPDATE-Zweig des Upserts (`dienstplan/generator.ts`), nicht am INSERT. Das Fenster beginnt beim Berliner HEUTE: wer abends den Beginn eines Turnus änderte oder tagsüber eine Serie anlegte, bekam eine frische Schicht „heute 06:00“ — nie besetzbar, als unbesetzt im Plan, neben der Schicht, die gerade lief | Gefunden, weil `tests/isolation/serie-pflege.test.ts` um 22:05 Berliner Zeit rot wurde und „Flake“ keine Ursache ist: die Prüfung verlangte, dass JEDE lebende Schicht nach der Änderung um 06:00 beginnt — sie hätte zwischen 22:00 und 06:00 immer versagt, und dahinter stand der eigentliche Fehler | falsch | **erledigt** — der Insert trägt jetzt `where $22 or anfang.zeitpunkt > now()`; was schon begonnen hat, entsteht nicht neu und steht als `vergangen_oder_gearbeitet` im Bericht der Serie. Nur der Seed legt Vergangenes an und sagt es ausdrücklich (`Lauflage.vergangenheitAnlegen`) — an seinen drei Wochen hängen Zeiteinträge und Nachweise der Vorführung. Die beiden Prüfungen, die bisher nur wegen des Fehlers grün waren, prüfen jetzt die Aussage des Dienstes zu jeder Uhrzeit; `serie-pflege.test.ts` §7 prüft den Wächter mit einem Lauf ab GESTERN, also unabhängig von der Uhrzeit |
| V-136 | **Der zweite Faktor für Admins ließ sich per Adresszeile überspringen** — `admin` und `super_admin` tragen seit 0007 `erfordert_2fa`, und die Anmeldung leitet sie nach dem Kennwort auf den Faktor-Schritt. Durchgesetzt wurde die Pflicht danach nur für die wenigen RECHTE mit `erfordert_2fa` (eines) und drei Routen mit eigenem `aal2`-Wächter: `app.hat_recht_fuer` wertete weder die globale Rolle noch die Mitgliedschaftsrolle gegen ihre eigene Pflicht aus. Wer nach dem Kennwort statt des Faktor-Schritts `/portal/reinigung` aufrief, arbeitete mit `aal1` und allen Rechten der Rolle — ein abgegriffenes Admin-Kennwort öffnete Finanzen, Personal und CRM | Gefunden vom Prüf-Workflow über die Spezifikation (AUT-02), im Code gegengeprüft (`zugang.ts:137`, `0169:134`, `0007:137`) | falsch | **erledigt** (D-630) — `drizzle/0395`: eine Rolle mit `erfordert_2fa` gewährt ohne `aal2` nichts, im globalen wie im Mitgliedschaftszweig; das gilt für jede Policy, jede API und jeden Dienst. `app.faktor_pflicht()` sagt der Portal-Pforte, dass eine `aal1`-Sitzung auf den Faktor-Schritt gehört — mit Rückweg, statt 404 auf jeder Seite. `leitung`, `mitarbeiter`, `kunde` sind nicht berührt (K-15). **Was es kostete, sichtbar zu machen:** 984 Isolationstests in 93 Dateien stützten sich darauf, dass ein Admin bei `aal1` alles darf — der Harness stand auf `aal1` als Vorgabe. Er stellt jetzt eine vollständige Anmeldung dar (`aal2`), wie Seed und Entwicklungszugang; wer den halben Zustand prüft, sagt es ausdrücklich. Prüfungen: `tests/isolation/auth.test.ts` (1b) |
| V-137 | **Eine Web-Anfrage kam beim Menschen nicht an** — die Einsendung (Flächen, Frequenz, Kräfte, Gewerk, Name, E-Mail, Telefon) lag nur in `formular_eingang.daten`, das keine Portalseite las; das LV hatte keinen Bezug zum Eingang; weder die neue Anfrage noch die SLA-Überschreitung wurde gemeldet; die erste Reaktion ließ sich nicht erfassen (jede Aktivität `intern`, und ohne Ansprechpartner ließ das UWG-Tor nichts Ausgehendes zu), also eskalierte jede Web-Anfrage stündlich ohne Ende, auch nach „gewonnen“, mit UUID und UTC-Zeit im Verlauf; UTM kam nie an, und der Referrer war immer die eigene Formularseite; Priorität und Besitzer ließen sich nicht setzen; Fehler beim Setzen des Stands verschwanden (`?meldung=` gegen `?fehler=`) | Gefunden vom Prüf-Workflow (spec-01, spec-02), im Code gegengeprüft (`annahme.ts:253`, `api/lead/route.ts:84`, `eskalation.ts:52`, `sla.ts:97`, `api/anfrage/route.ts:166`) | blockiert | **erledigt** (D-631) — `drizzle/0396`: der Anfragende wird Ansprechpartner mit Grundlage `anfrage` (gleiche E-Mail → derselbe Kontakt), der Besitzer bekommt `crm.neuer_lead`; das Leadblatt zeigt Einsendung, Kontakt, LV, Herkunft, Besitzer und Priorität, erfasst ausgehende Anrufe/E-Mails durch das unveränderte UWG-Tor und hält damit die Uhr an; eskaliert werden nur offene Leads, gemeldet an den Besitzer; die Herkunft liest die Formularseite aus ihrem eigenen Aufruf, ohne Cookie (D-61). Prüfungen: `tests/isolation/lead.test.ts` (6), `tests/kern/lead-einsendung.test.ts`, `tests/kern/formular-herkunft.test.ts`. **Offen:** Herkunft bis zum Auftrag — hängt an der Kette Lead → Angebot → Auftrag |
| V-138 | **Die Kette Lead → Angebot → Auftrag brach am ersten Glied, und der Herkunftsbericht zählte deshalb für jeden Kanal null Aufträge** — `angebot.lead_id` schrieb kein Weg (weder `legeAngebotAn` noch die Maske von Hand, das Raumbuch oder der Seed), `auftrag.lead_id` erbte davon NULL, und `/api/auftrag` kannte das Feld nicht; `lead.kunde_id` und `lead.ansprechpartner_id` ließen sich nach der Anlage nicht setzen und hatten, wie `empfehlung_von_kunde_id` und `ausschreibung_id`, seit 0017 keinen Fremdschlüssel; das Leadblatt zeigte nichts von dem, was aus der Anfrage wurde, und hatte keinen Knopf „Angebot erstellen"; das Kundenblatt hatte weder Angebote noch Rechnungen, Dokumente oder Kommunikation (04-SEITENKARTE §5.2), und seine Auftragszeilen führten nirgends hin; REP-03 zeigte „0 Aufträge, 0,00 €" wie eine Aussage über die Kanäle | Gefunden vom Prüf-Workflow (spec-01 REQ-07, spec-02 CRM-05, spec-08 REP-03), im Code gegengeprüft (`angebot/index.ts:84`, `angebot/index.ts:525`, `api/auftrag/route.ts:126`, `bericht/kennzahlen.ts:219`, `bericht/gruppe.ts:336`, `crm/kunden/[id]/page.tsx:413`) | blockiert | **erledigt** (D-632) — `drizzle/0400`: die vier Fremdschlüssel des Leads; ein Angebot oder Auftrag mit Lead gehört demselben Kunden (`kern.lead_bezug_stimmt`), und ein Lead mit Vorgang wechselt seinen Kunden nicht mehr (`kern.lead_kunde_bleibt`); ein versendetes Angebot stellt den offenen Lead auf „Angebot abgegeben", ein Auftrag auf „gewonnen" — als Definer-Auslöser, damit auch eine Rolle ohne `crm.schreiben` versenden kann, mit Systemzeile im Verlauf. `services/crm/lead-kette.ts`: „Als Kunde übernehmen" (Grundlage `keine`, der Anfragende wandert mit) und „Kunden zuordnen"; `legeAngebotAn`, die Maske von Hand, das Raumbuch (`?lead=`) und `/api/auftrag` tragen die Anfrage, `wandleInAuftrag` reicht sie weiter. Leadblatt: Herkunft, Kunde, „Angebot erstellen", „Angebot aus dem Raumbuch", „Auftrag direkt anlegen", Angebote, Aufträge, Rechnungen; Kundenblatt: Anfragen, Angebote, Rechnungen, Dokumente, Verlauf, Auftragszeilen verlinkt; Angebots- und Auftragsblatt nennen die Anfrage — jeweils nur mit dem Recht der Zielseite. Raumbuch und `/api/auftrag` schicken Abweisungen als Seite statt JSON zurück (D-599). Der Seed geht Empfehlung → Angebot → Versand → Auftrag über die echten Dienste. Prüfungen: `tests/isolation/crm-kette.test.ts` §1–§3, `tests/kern/crm-kette.test.ts`, `tests/kern/verweis-rechte.test.ts`, `tests/isolation/sql-schema.test.ts` |
| V-139 | **Zwei der vier Leadquellen hatten keinen Erzeuger** — CRM-07 nennt Website-Formular, Vergaberadar, manuelle Erfassung und Empfehlung; `lead_herkunft_stimmig` (0017) verlangte für `vergabe_radar` eine `ausschreibung_id` und für `empfehlung` einen empfehlenden Kunden, und keine Zeile Code schrieb eines davon: ein Radartreffer ließ sich nicht übernehmen, „Neuer Lead" kannte keine Empfehlung, und die Auswertung beschriftete „Vergaberadar" und „Empfehlung", die nie entstehen konnten; übernommene Akquiseziele standen im Bericht unter „Manuell erfasst", in der Gruppenliste als roher Schlüssel | Gefunden vom Prüf-Workflow (spec-02 CRM-07), im Code gegengeprüft (`crm/anlegen.ts:241`, `0017_lead.sql:99`, `crm/leads/neu/page.tsx:94`, `bericht/kennzahlen.ts:202`) | blockiert | **erledigt** (D-633) — `legeLeadAn` nimmt `quelle = 'empfehlung'` mit einem Kunden dieser Gesellschaft (nie sich selbst) und prüft auch `kundeId` gegen die Gesellschaft; „Neuer Lead" fragt „Empfohlen von". `services/crm/lead-radar.ts`: „Als Lead übernehmen" auf der Bekanntmachung — Betreff, Beschreibung, Auftraggeber aus der Quelle, der Wert nur in Euro (O-47), einmal je Gesellschaft (`lead_ausschreibung_uk`, 0400), der Radarvorgang bleibt unberührt. Der Bericht nennt „Akquise". Seed: ein Radar-Lead im Bau, eine Empfehlung in der Reinigung. Prüfungen: `tests/isolation/crm-kette.test.ts` §4, `tests/kern/crm-kette.test.ts` |
| V-140 | **Die gesellschaftsübergreifende Kundenhistorie griff nur für Seed-Kunden** — die Gruppenliste erkennt denselben Kunden allein an `kunde.firma_id`; `app.firma_aufloesen` (0020, „die EINZIGE Stelle, an der eine firma entsteht") hatte keinen Aufrufer, `legeKundeAn` und `aendereKunde` setzten `firma_id` nie, auch nicht bei USt-IdNr., und „auch in" blieb für jeden im Portal angelegten Kunden leer | Gefunden vom Prüf-Workflow (spec-02 CRM-06), im Code gegengeprüft (`0020_crm_identitaet.sql:742`, `crm/anlegen.ts:98`, `gruppe/kunden/page.tsx:51`) | falsch | **erledigt** (D-634) — `legeKundeAn` löst Firma und Behörde MIT USt-IdNr. über `app.firma_aufloesen` auf, `aendereKunde` trägt die Firma nach, sobald die Nummer kommt, und tauscht keine vorhandene; ohne Nummer und für Privatpersonen entsteht keine Firma (ein Name ist keine Identität). `drizzle/0401` holt den Bestand mit derselben Regel nach, ohne Privatkunden und Anonymisierte. Prüfungen: `tests/isolation/crm-kette.test.ts` §5 (dieselbe USt-IdNr. in Reinigung und Security → „auch in" nennt die andere; die Abfrage der Gruppenliste wörtlich) |
| V-141 | **Eine Anfrage ohne Webformular bekam nie einen Ansprechpartner** — V-138 machte `lead.kunde_id` setzbar, `lead.ansprechpartner_id` nicht: nur die Annahme eines Webformulars legte einen Kontakt an, das Leadblatt hatte keine Kontaktwahl und riet, „den Kontakt am Kunden anzulegen" (der Lead zeigte danach trotzdem auf niemanden). Jeder ausgehende Anruf und jede ausgehende E-Mail eines Leads von Hand, aus einer Empfehlung, dem Vergaberadar oder der Akquise brach mit `kein_kontakt` ab, auch nach „Als Kunde übernehmen". Dazu schrieb `/api/lead` jede ausgehende Aktivität als `vertraglich` — mit einem setzbaren Kontakt wäre das ein Weg am Werbetor vorbei gewesen | Gefunden von der Nachprüfung zu V-138 (Prüfbefund spec-02 CRM-05, Teilaspekt), im Code gegengeprüft (`crm/lead-kette.ts:143`, `crm/leads/[id]/page.tsx:343`, `api/lead/route.ts:107`) | blockiert | **erledigt** (D-635) — `services/crm/lead-kontakt.ts`: einen erreichbaren Kontakt des Kunden wählen oder einen neuen anlegen (beim Kunden, sonst ohne Kunden — er wandert mit), Grundlage `keine`, dieselbe E-Mail-Adresse → derselbe Kontakt, Systemzeile im Verlauf; die Aktivität hält der Dienst fest, ausgehend mit dem Zweck ihrer Herkunft hinter `LEAD_ZWECK_REGEL` (Webformular vertraglich, Akquise Werbung, von Hand/Empfehlung/Vergaberadar bis O-907 Werbung); `nimmKontaktMit` stellt nicht mehr auf einen Zwilling um, wenn dem Anfragenden widersprochen wurde oder der Zwilling ausgeschieden ist (O-908). Leadblatt: Kontakt wählen, Kontakt anlegen, Kontaktblatt, Hinweis zum Zweck vor dem Anruf. Seed: die Empfehlung bekommt einen Kontakt ihres Kunden. Prüfungen: `tests/isolation/crm-kette.test.ts` §7 (5: Werbetor für den Lead von Hand, vertraglich für den Web-Lead mit demselben Kontaktstand, Wahl nur beim eigenen Kunden, bekannte Adresse, Widerspruch neben Zwilling), `tests/kern/crm-kette.test.ts` (Zweckregel je Quelle, Sätze für jeden Grund) |
| V-142 | **Die Kette hielt nur, solange niemand gleichzeitig klickte, archivierte oder sich vertat** — `kern.lead_bezug_stimmt` las den Lead ohne Sperre (ein gleichzeitiges Umhängen liess ein Angebot für Kunde A an einer Anfrage von Kunde B zurück); dieselbe neue USt-IdNr. in zwei Gesellschaften im selben Augenblick endete in 23505/500; `firmaFuer` gab das Land nicht weiter (neue Firmen immer `DE`, anders als 0401); `lead_ausschreibung_uk` zählte archivierte Leads und sperrte eine Bekanntmachung für immer; einen falsch zugeordneten Kunden erlaubten Dienst und Datenbank zu berichtigen, das Leadblatt bot es nicht an; Rechnungen auf dem Leadblatt hingen still an `auftrag.lesen` („noch keine Rechnung"); „Neues Angebot"/„Neuer Auftrag" nannten für einen archivierten Kunden oder einen jenseits der 500 Namen den falschen Grund; einige Lesezugriffe nur über RLS; falsche Kopfkommentare in 0400/0401 | Gefunden von der Nachprüfung zu V-138 bis V-140, im Code gegengeprüft (`0400:107`, `0400:63`, `crm/anlegen.ts:80`, `0020:110`, `lead-kette.ts:246`, `lead-kette.ts:410`, `leads/[id]/page.tsx:426`, `angebote/neu/page.tsx:117`) | falsch | **erledigt** (D-636) — `drizzle/0402`: `kern.lead_bezug_stimmt` liest unter `FOR SHARE`, `lead_ausschreibung_uk` nur für laufende Leads; `firmaFuer` serialisiert dieselbe Nummer über eine Transaktionssperre und findet so die gleichzeitig angelegte Firma statt 23505, und gibt das Land des Kunden weiter; „Kunden berichtigen" auf dem Leadblatt, solange nichts an der Anfrage hängt (auch kein zurückgezogener Entwurf), mit Systemzeile; Rechnungen ohne `auftrag.lesen` nennen das Recht; die Masken lesen den Kunden aus der Anfrage und sagen „archiviert"; Mandantenfilter in `leseLeadKette`, `ordneLeadKundeZu`, `aendereKunde`. Prüfungen: `tests/isolation/crm-kette.test.ts` §8 (6: Umhängen gegen Angebot mit offener Transaktion, gleichzeitige USt-IdNr., Land, archivierter Radar-Lead, Berichtigung mit zurückgezogenem Entwurf, 23514 → Satz für eine Rolle ohne `angebot.lesen`) |
| V-143 | **Die Formularwege der Kette hielten ihr Versprechen nur zur Hälfte, und das Wichtigste daran war ungeprüft** — `/api/auftrag` antwortete auf „nicht angelegt" mit JSON, der Raumbuchweg in `/api/angebot` auf ein unvollständiges Formular und ein Raumbuch ohne kalkulierbare Fläche ebenso (D-632 Punkt 8 sagte das Gegenteil); kam die Maske zurück, dann leer bis auf `?lead=`; beide Wege standen ganz in ihren Routen, also war weder geprüft, dass eine abgewiesene Anfrage keine Auftragsnummer verbraucht, noch dass Auftrag und Raumbuch-Angebot die Anfrage tragen — der Test „der Weg von /api/auftrag" fügte per SQL ein | Gefunden von der Nachprüfung zu V-138, im Code gegengeprüft (`api/auftrag/route.ts:105/175/189`, `api/angebot/route.ts:236/241`, `tests/isolation/crm-kette.test.ts:348`) | falsch | **erledigt** (D-637) — `services/auftrag/direkt.ts` (Anfrage vor der Nummer) und `services/angebot/aus-raumbuch.ts` tragen die beiden Wege, die Routen rufen sie; jede Abweisung der Eingabe oder des Vorgangs kommt als Seite mit Satz zurück (`nicht_angelegt`, `angebot_unvollstaendig`, `nichts_zu_kalkulieren`), die Antworten des Tors bleiben JSON wie überall (D-562); die Maske „Neuer Auftrag" bekommt ihre Eingaben über `lib/formular/maske.ts` zurück. Prüfungen: `tests/isolation/crm-kette.test.ts` §9 (3: Auftragskreis unberührt bei fremder Anfrage, Auftrag trägt die Anfrage und stellt sie auf gewonnen; Raumbuch-Angebot mit Anfrage, fremde Anfrage ohne Rest; leeres Raumbuch/unvollständig), `tests/kern/crm-kette.test.ts` (Eingaben in der Adresse, Gründe beider Routen mit Satz) |
| V-144 | **Die kleinen Schieflagen der Kette** — „Sichtbar für, wer dieses Recht hält:" war kein deutscher Satz; `kein_schreibrecht` nannte den rohen Schlüssel `crm.schreiben`; `lib/vorgang-pille.ts` sollte die Pillenabbildung je Seite ablösen und liess sechs Kopien stehen; ein Unit-Test prüfte im `try/catch` nichts, wenn keine Ausnahme flog | Gefunden von der Nachprüfung zu V-138 bis V-140, im Code gegengeprüft (`crm-kette.ts:162/259/420`, `angebote/page.tsx:26`, `auftraege/page.tsx:20`, `finanzen/rechnungen/page.tsx:36`, `crm/leads/page.tsx:32`, `crm/leads/[id]/page.tsx:51`, `crm/kunden/[id]/page.tsx:80`, `tests/kern/crm-kette.test.ts:55`) | Schönheit | **erledigt** (D-638) — der Satz heisst „Das sieht, wer dieses Recht hält:", `kein_schreibrecht` nennt den Namen aus `rechtname.ts` (wie `<Recht>`), alle sechs Seiten lesen die Pillen aus `lib/vorgang-pille.ts`, der Test verlangt die Ausnahme. Bewusst unverändert und begründet: die deutschen Systemzeilen im Verlauf (Aufzeichnung, bestehendes Muster), die Pipeline-Regel aus D-632 Punkt 4 (delegierte Ablaufregel). Prüfungen: `tests/kern/crm-kette.test.ts` (kein roher Rechteschlüssel in den Sätzen der Kette, keine eigene Pillenabbildung mehr, Ausnahme mit Grund) |
| V-146 | **Die Wiedervorlage-Erinnerung wurde gespeichert und nie zugestellt** — Lead- und Kontaktblatt boten das Feld „Erinnerung“, `lead_aktivitaet.erinnerung_am` wurde geschrieben und beim Verschieben mitgeführt, aber kein Lauf, keine Benachrichtigungsart und kein Kalenderalarm las den Wert. Wer eine Erinnerung eintrug, bekam keine | Gefunden vom Prüf-Workflow (spec-02, CRM-04), im Code gegengeprüft (`wiedervorlage.ts:251-262`, `:415-418`; `grep erinnerung_am src` → nur dort; keine Art, kein Job) | falsch | **erledigt** (D-640) — `drizzle/0405`: `erinnert_am` als Gedächtnis, Spalten- und Updaterecht für `cse_job` mit mandantengebundenen Policies, Teilindex. Neuer Lauf `wiedervorlage_erinnerung` (je Mandant, alle 15 Minuten, als `cse_job`) nimmt fällige Erinnerungen per `update … returning` und stellt `crm.wiedervorlage_erinnerung` an den Zuständigen (sonst den Ersteller) zu, Ziel `/crm/wiedervorlagen`, nie sammelbar; ohne aktives Konto wird der Anspruch zurückgegeben. `verschiebe` setzt die Erinnerung zurück. Art im Benachrichtigungs-Bootstrap (Einstellungsseite), Lauf im Job-Bootstrap und in `docs/JOB-AUSLOESER.sql`. Prüfungen: `tests/isolation/wiedervorlage-erinnerung.test.ts` (10), `tests/kern/wiedervorlage-erinnerung.test.ts` (7), `tests/kern/jobs-bootstrap.test.ts` |
| V-147 | **Der Kommunikationsverlauf war zerstückelt** — das Kundenblatt zeigte gar keinen Verlauf (Wiedervorlagen am Kunden, Kundenportal-Nachrichten erschienen nirgends, obwohl `lead_aktivitaet_kunde_idx` eigens dafür angelegt ist); das Kontaktblatt las nur `lead_aktivitaet` und zeigte rohe Werte, versendete Nachrichten (`nachricht`) fehlten genau dort, wo sie belegt werden sollen; eine Notiz ließ sich nur an einen Lead hängen | Gefunden vom Prüf-Workflow (spec-02, CRM-03, Seitenkarte „Kommunikation“), im Code gegengeprüft (`kunden/[id]/page.tsx` ohne Verlaufsabfrage, `kontakte/[id]/page.tsx:212-223`, `kern/nachricht.ts:596-623`) | behindert | **erledigt** (D-641) — `services/crm/verlauf.ts` liest Aktivitäten und Nachrichten in EINER Liste (Kunde: am Kunden, an seinen Leads und Kontakten; Kontakt: seine Aktivitäten und jede Nachricht an ihn), mit Richtung, Kanal, Zweck, der Rechtsgrundlage im Moment des Sendens und dem Zustellstand; ein Bauteil für beide Blätter, de/en, keine rohen Werte; das Blatt sagt, wenn `nachricht.lesen` oder `system.benutzer_lesen` fehlt. Neue Route `POST /api/crm/notiz`: Notiz, Anruf, E-Mail, Termin am Kunden oder Kontakt, ausgehend durch das unveränderte UWG-Tor. Prüfungen: `tests/isolation/crm-verlauf.test.ts` (8), `tests/isolation/crm-seitenabfragen.test.ts` (Kontaktverlauf jetzt über den Dienst), `tests/kern/crm-verlauf.test.ts` (9) |
| V-148 | **Abgewiesene Formulare kamen ohne Satz zurück (CRM, Recruiting)** — `/crm/kunden/[id]` nahm keine Suchparameter an, `POST /api/crm/kunde` schickte `?meldung=`: ein Ansprechpartner mit „Bestandskunde“ ohne Quelle oder „Einwilligung“ ohne Kanal verschwand ohne Hinweis. Bewertung, Entscheidung, neue Stelle und Veröffentlichung schicken `zurueck` auf sich selbst, `fuehreRecruitingAus` hängt `?fehler=` an — keine der vier las ihn; ein Kriterium ohne Begründung: Eingaben weg, kein Satz. (Das Leadblatt las `?meldung=` schon seit V-137 — dieser Teil war widerlegt.) | Gefunden vom Prüf-Workflow (quer-formulare, D-562), im Code gegengeprüft (`kunden/[id]/page.tsx:81-83`, `api/crm/kunde/route.ts:200-206`, `api/recruiting/gemeinsam.ts:140/170`, vier Seiten nur mit `params`) | behindert | **erledigt** (D-642) — Kundenblatt liest `?meldung=` und neu `?grund=` (Route schickt beides, nicht `?fehler=`), zeigt den übersetzten Satz über dem Kopf und öffnet das Kontaktformular wieder. Die vier Recruiting-Seiten lesen `?fehler=` über `recruiting/rueckmeldung.tsx` mit je eigener Satztabelle de/en, nie den Schlüssel; lange Formulare sagen, dass die Eingaben neu einzutragen sind; die Veröffentlichung liest auch `?gesendet=1`. Prüfungen: `tests/kern/formular-rueckwege.test.ts` (14: jeder Grund jeder Route hat einen Satz in beiden Sprachen) |
| V-149 | **Übersichtszahlen führten nicht zu ihren Datensätzen** — „Offene Wiedervorlagen“ zählte alle Zuständigen und öffnete die Liste in der Vorgabe „nur meine“ (4 auf der Kachel, 1 in der Liste); „Aktivität (7 Tage)“ zählte `lead_aktivitaet` und führte auf die Kundenliste, auf der keine Aktivität steht; „Angebote offen“ in der Gruppenübersicht war eine nackte Zahl, `/portal/gruppe/angebote` gab es nicht | Gefunden vom Prüf-Workflow (spec-02, DSH-04), im Code gegengeprüft (`kacheln.ts:150-181`, `crm/wiedervorlagen/page.tsx:67`, `gruppe/page.tsx:84-87`, keine Gruppenroute für Angebote im Manifest) | falsch | **erledigt** (D-643) — Kachelziel `crm/wiedervorlagen?wer=alle`; neue Seite `/crm/aktivitaet` (`leseAktivitaeten`: dieselbe Tabelle, dieselbe Frist, keine Nachrichten) als Ziel von „Aktivität (7 Tage)“; neue lesende Seite `/gruppe/angebote` mit Bereichs- und Standfilter, die Zelle verweist mit `?status=offen`; die Mengen stehen einmal in `bericht/mengen.ts`. Prüfungen: `tests/kern/kennzahlen-ziele.test.ts` (jedes Ziel im Manifest, Filter und Werteliste), `tests/isolation/kennzahlen-listen.test.ts` (1): Kachelzahl = Zeilen von `leseAktivitaeten`, ohne die achttägige und ohne fremde Gesellschaft |
| V-150 | **Die Übersicht zeigte die meisten DSH-01-Kennzahlen nicht, obwohl ihre Module gebaut waren** — 13 Kacheln, keine für Aufträge, Bauprojekte, Angebote, Forderungen oder Aufgaben („Modul noch nicht gemergt“); der Gruppenübersicht fehlten aktive Projekte, „aktuell im Einsatz“, anstehende Aufgaben und letzte Aktivität | Gefunden vom Prüf-Workflow (spec-02, DSH-01, DSH-03), im Code gegengeprüft (`kacheln.ts:1-12` und `:47-382`, `registriereKachel(` ohne weiteren Aufrufer, `gruppe/uebersicht.ts:97-130`) | behindert | **erledigt** (D-644) bis auf die Gruppenspalte „letzte Aktivität“ (O-910) — fünf Kacheln (`auftraege_aktiv`, `projekte_in_arbeit`, `angebote_offen`, `forderungen_offen`, `aufgaben_offen`), jede auf ihre Liste MIT Filter; Auftrags-, Angebots- und Projektliste lesen `?status=`, zeigen „Gefiltert: … · Alle anzeigen“ und holen den Kunden per LEFT JOIN; Gruppenübersicht mit „Bauprojekte in Arbeit“, „Offene Aufgaben“ (neue Seite `/gruppe/aufgaben`) und „Im Einsatz“ (Abschnitt auf `/gruppe/auslastung`); Kachelnamen de/en. Umsatz, Aufwand, Ergebnis bleiben auf `/finanzen` bzw. `/gruppe/finanzen` (D-479). Prüfungen: `tests/kern/kennzahlen-ziele.test.ts`, `tests/kern/kennzahlen.test.ts` (18 Kacheln), `tests/isolation/kennzahlen.test.ts` (jede der 18 Kacheln: Zahl = Zeilen, alle vier Bereiche), `tests/isolation/kennzahlen-listen.test.ts` (2)(3): Projektliste ohne `crm.lesen` = Kachelzahl, Gruppenzahl = Liste für Projekte, Einsatz, Aufgaben, `null` ohne Recht |
| V-151 | **Eine Kachel führte auf einen 404 — „Bauprojekte in Arbeit“ in Gesellschaften ohne Bau** — die mit V-150 gekommene Kachel (`bau.lesen`) erschien bei Leitung und Administration in JEDER Gesellschaft (beide halten `bau.lesen` global, 0008) und führte in Reinigung, Security und Operations auf `/bau/projekte?status=in_arbeit`, das die Pforte dort mit 404 beantwortet (Bau nicht gebucht, D-377). Das Dashboard filterte nur nach dem Recht der Kachel, `Kachel.modul` las niemand, kein Test fragte, ob sich das Ziel öffnet. Dazu: „Offene Forderungen“ zeigte ohne `zahlung.lesen` „0“ (RLS auf `offener_posten`, 0121), „Offene Konflikte“ führte ohne `dienstplan.lesen` auf eine Seite, die es verlangt | Gefunden von der Nachprüfung der Gruppe crm-pflege (V-150), im Code gegengeprüft (`kacheln.ts:452-468`, `dashboard.ts:55`, `registry/kennzahlen.ts:46`, `[mandant]/page.tsx:97`, `zugang.ts:450/476`, `seed/index.ts` Buchungen) | falsch | **erledigt** (D-645) — `kachelErreichbar` (`services/bericht/dashboard.ts`) verlangt Recht und `zusatzRechte` der Kachel, die Buchung ihres Moduls, eine Zielroute, die die Pforte nicht sperrt, und jedes Leserecht dieser Route; die Modulsperre steht EINMAL in `routeGesperrt` (`registry/routen.ts`), die Pforte und die Übersicht rufen beide diese Funktion; `dashboard()` verlangt die Buchung, `bereichsDashboard` liest Buchung und Rechte in derselben Transaktion; „Offene Forderungen“ trägt `zusatzRechte: ['zahlung.lesen']`. Prüfungen: `tests/kern/kennzahlen-erreichbar.test.ts` (jede Kachel × fünf Buchungen × drei Rechtelagen: sichtbar ⇒ Route da, nicht gesperrt, jedes Leserecht gehalten, kein `aal2`; der Befund selbst; `dashboard()` fragt keine Bauzählung in der Reinigung), `tests/isolation/kennzahlen-listen.test.ts` (4): dieselbe Leitung — in der Reinigung ohne, im Bau mit der Kachel; Forderungen nur mit `zahlung.lesen`; Konflikte nur mit `dienstplan.lesen` |
| V-152 | **Übersichtszahlen führten weiter auf ungefilterte Listen, und die Gruppenübersicht sprach zwei Sprachen** — die Summenkachel „Aufträge aktiv“ auf `/portal/gruppe` führte auf `/gruppe/auftraege` ohne `?status=aktiv` (alle nicht archivierten gegen nur aktive); „Neue Anfragen“ (Gruppe) sowie „Neue Anfragen“ und „Frist überschritten“ (Bereich) führten auf die ganze Leadliste; „Forderungen offen“ zählte mit `gruppe.zahlung.lesen` und verwies auf eine Seite mit `gruppe.buchhaltung.lesen` (404); nur drei Spaltenköpfe der Übersicht waren übersetzt; die Listen hinter „Aktive Aufträge“, „Offene Angebote“, „Angebote offen“ waren nur am Quelltext geprüft | Gefunden von der Nachprüfung der Gruppe crm-pflege (V-149, V-150), im Code gegengeprüft (`gruppe/page.tsx:60-62`, `gruppe/auftraege/page.tsx:44-49`, `gruppe/leads/page.tsx:53`, `kacheln.ts` neue_leads/leads_ueber_sla, `routen.generiert.ts` offene-posten) | falsch | **erledigt** (D-646) — die Ziele der Übersicht stehen im Dienst (`UEBERSICHT_ZIELE`, `SUMMEN_ZIELE`, `uebersichtZiel`), jedes mit dem Filter seiner Menge; eine Zelle ist eine Zahl nur mit dem Recht der Zahl UND der Liste (`UEBERSICHT_ZIELRECHTE`), eine Summe ohne lesbaren Bereich ein Strich ohne Verweis; Leadlisten im Bereich und in der Gruppe filtern `?status=` und `?frist=ueberschritten` mit EINEM Fristprädikat (`fristUeberschrittenSql`); die Listen sind ein Dienst (`services/bericht/listen.ts`, im Bereich mit `mandant_id` in der Abfrage); die Übersicht ist ganz de/en (Zeile in der Übersetzungs-Ausnahmeliste gestrichen). Prüfungen: `tests/kern/kennzahlen-ziele.test.ts` (jedes Ziel am Manifest, Recht der Route = Recht der Zelle, Filter, keine deutsche Spalte, Leadwerte gegen 0017, Fristprädikat geteilt), `tests/isolation/kennzahlen-listen.test.ts` (5)(6): Aufträge/Angebote/Leads im Bereich ohne `crm.lesen` Zahl = Liste, Summe „Aufträge aktiv“ = Gruppenliste, „Angebote offen“ und „Neue Anfragen“ = Gruppenlisten, Forderungen nur mit `gruppe.buchhaltung.lesen`; `tests/e2e/gruppe.spec.ts` erwartet die gefilterten Verweise |
| V-153 | **Die CRM-Pflege hatte sieben Kanten** — 0405 füllte `erinnert_am` für den Altbestand nicht vor (der erste Lauf hätte alle vergangenen Erinnerungen auf einmal zugestellt, auch per E-Mail); der Erinnerungslauf prüfte den Empfänger weder auf Mitgliedschaft noch auf `crm.lesen`, nahm bei einem stillgelegten Konto den Anspruch alle 15 Minuten und gab ihn zurück, und sagte dem Zuständigen „Sie haben um eine Erinnerung gebeten“; `halteFest` und `legeWiedervorlageAn` prüften `kundeId` nicht gegen den Mandanten (kein Fremdschlüssel), eine Kennung ohne UUID-Form endete als 500; der Zweck einer Notiz war `vertraglich` vorgewählt; ohne `crm.schreiben` zeigten Kunden- und Kontaktblatt weder Formular noch Satz; das Kundenblatt setzte `?meldung=` roh in seinen Warnkasten; die Recruiting-Rückmeldung nannte einen vermerkten Versuch „Nicht gespeichert.“; `/api/crm/notiz` hatte keinen Routentest | Gefunden von der Nachprüfung der Gruppe crm-pflege (V-146, V-147, V-148), im Code gegengeprüft (`0405`, `seed/crm.ts:300-321`, `wiedervorlageErinnerung.ts`, `verlauf.ts` `halteFest`/`planeNotiz`, `0017:186-214`, `kunden/[id]/page.tsx`, `recruiting/rueckmeldung.tsx`, `gemeinsam.ts`) | behindert | **erledigt** (D-647) — `drizzle/0406` stempelt den Altbestand, dessen Wiedervorlage schon fällig war (der Seed ebenso); der Lauf beansprucht nur Zeilen mit einem Empfänger aus `kern.traeger_des_rechts(mandant, 'crm.lesen')` (Zuständiger, sonst Anlegender), sonst bleibt die Zeile unberührt und `wartend`; der Text nennt den Grund, der Anlegende landet auf `?wer=alle`; Notiz und Wiedervorlage prüfen Kunde, Lead, Ansprechpartner und UUID-Form mit dem Mandanten der Sitzung, zuständig nur mit `crm.lesen` hier; Zweck ohne Vorauswahl, Pflicht bei ein- und ausgehend (`ohne_zweck`); `NotizKeinRecht` statt Leere; Kundenblatt nur übersetzte Schlüssel oder „Der Vorgang wurde abgewiesen.“; „Nicht veröffentlicht.“ für vermerkte Versuche. Prüfungen: `tests/isolation/wiedervorlage-erinnerung.test.ts` (§3 kein Hin und Her über zwei Läufe per `xmin`, Anlegender statt fremdem Zuständigen, Satz des Zuständigen; §6 die Anweisung aus 0406 an echten Zeilen), `tests/isolation/crm-verlauf.test.ts` und `crm-wiedervorlage.test.ts` (fremder/unbekannter Kunde, keine UUID, Lead, Zuständiger ohne Zugang), `tests/kern/crm-notiz-route.test.ts` (neu: `?notiert=1`, `?notiz=<grund>`, `&` nach bestehender Abfrage, `internesZiel`, 403, 401), `tests/kern/crm-verlauf.test.ts`, `tests/kern/formular-rueckwege.test.ts` (gerenderte Rückmeldungen), `tests/kern/wiedervorlage-erinnerung.test.ts` |
| V-154 | **Öffentliche Referenzen (PRO-05) ließen sich nicht anlegen** — kein Dienst und keine Route schrieb ein `insert into referenz`, nur der Seed; die Kundenfreigabe am Auftrag versprach „die öffentliche Referenz legt danach ein Mensch unter `/website/referenzen` an", und dort gab es nur Bearbeiten, Freigabe und Veröffentlichen einer BESTEHENDEN Zeile, der Leerzustand hatte keinen Knopf. Eine echte Gesellschaft brachte kein Projekt auf Profil, `/projekte` oder Sitemap | Gefunden vom Prüf-Workflow (spec-01), im Code gegengeprüft (`redaktion.ts` ohne Anlegen, `api/website/referenz/route.ts:48` nur `freigabe`/`felder`, `referenzen/page.tsx:86`, `kundenfreigabe.ts:7`) | blockiert | **erledigt** (D-648) — `legeReferenzAn` legt einen ENTWURF ohne Kundenfreigabe an (Mandant aus der Sitzung, Slug über `app.slug_aus_titel` und vorab auf Eindeutigkeit geprüft, Recht `referenz.kundenfreigabe_erfassen` wie beim Ändern, Kennung ohne `returning`); Handlung `anlegen` in `/api/website/referenz` mit Weiterleitung auf das Blatt der neuen Zeile; „Neue Referenz" auf der Liste auch im Leerzustand; `/website/referenzen/neu` (Seitenkarte ergänzt); von der Kundenfreigabe eines Auftrags mit GELTENDER Freigabe „Referenz aus diesem Auftrag anlegen" (Titel und Kunde vorbelegt), danach schlägt das Blatt Datum (Berliner Tag) und Beleg vor, der Haken bleibt leer (O-913). Dazu im selben Weg: `/api/website/referenzen` (Veröffentlichen/Zurückziehen) antwortete auf eine Abweisung mit `{"fehler":"ohne_kundenfreigabe"}` — jetzt mit dem Grund zurück auf Liste bzw. Veröffentlichungsblatt, beide nennen den Satz. Keine Migration. Prüfungen: `tests/isolation/referenz-anlegen.test.ts` (11), `tests/isolation/website-pflege.test.ts` (40, unverändert grün), `tests/kern/referenz-anlegen-weg.test.ts` (9) |
| V-155 | **Über uns, Aktuelles (News) und Karriere waren von keiner Seite aus verlinkt** — gebaut, befüllt, in der Sitemap; Kopfzeile, Telefonmenü und Fuss führten sie nicht, das Recruiting-Portal war öffentlich unauffindbar. SEITENKARTE §11.4 nannte sie in der Kopfzeile, DESIGN §5 hatte die Kopfzeile auf vier Punkte gekürzt, ohne einen anderen Ort zu nennen | Gefunden vom Prüf-Workflow (spec-01), im Code gegengeprüft (`OeffentlicheShell.tsx:38-51`, Menü `:265`, Fuss `:537`) | behindert | **erledigt** (D-649) — Liste `lib/oeffentliche-navigation.ts`, im Telefonmenü nach den vier Punkten und im Fuss als eigenes `nav` „Die Gruppe"; die Kopfzeile bleibt bei vier (D-417). Von englischen Seiten führt `/karriere` (nur deutsch) auf die deutsche Adresse mit `hrefLang="de"` und „(in German)" statt auf ein `/en/`-404 (`verweisIn`). DESIGN §5 und SEITENKARTE §11.4 nennen den Ort. Prüfungen: `tests/kern/oeffentliche-navigation.test.ts` (Verweise gegen Dateibaum und Routenliste, beide Orte in der Hülle, Kopfzeile bei vier) |
| V-156 | **Die englischen Pflichtformulare waren gebaut, aber nicht geroutet** — `/en/datenschutz/anfrage`, `…/danke` und `/en/barrierefreiheit/feedback` fehlten, obwohl Komponenten und API-Rückleitungen Englisch konnten; Sprachwahl und englischer Werbewiderspruch führten auf 404, `sprachpfade.test.ts` prüfte nur das erste Segment. Das BFSG-Meldeformular war von keiner Seite verlinkt (Erklärung nur mit E-Mail), die Betroffenenanfrage nur über den Werbewiderspruch; die Routen gaben den deutschen Dienstsatz auch an die englische Seite | Gefunden vom Prüf-Workflow (spec-01), im Code gegengeprüft (`find src/app/(public)/en`, `sprache.ts:106`, `Werbewiderspruch.tsx:197`, `Erklaerung.tsx:83`) | falsch | **erledigt** (D-650) — drei englische Seiten auf den vorhandenen Komponenten; die Pfadprüfung vergleicht jetzt Pfad für Pfad und verlangt auch umgekehrt zu jeder englischen eine deutsche Seite; Erklärung führt zuerst auf das Meldeformular; unter der Datenschutzerklärung stehen Betroffenenanfrage und Werbewiderspruch (`Betroffenenwege`); Meldungen der Pflichtformulare über den Grund übersetzt (`pflichtwegMeldung`). Prüfungen: `tests/kern/sprachpfade.test.ts`, `tests/kern/oeffentliche-navigation.test.ts` |
| V-157 | **`/api/anfrage`: der Honigtopf gab dem Browser rohes JSON, und die englische Seite zeigte deutsche Sammel- und Ratenlimitmeldungen** — der Zweig stand vor der Seitenweiche (`antwort=seite`), obwohl `annahme.ts` einem falsch positiven Treffer „dieselbe Dankseite wie bei Erfolg" zusagt, und ein Bot erkannte am anderen Antworttyp, dass er erkannt war; `fehler.message` und die Ratenlimitmeldung gingen unübersetzt auf `/en/angebot/<bereich>` | Gefunden vom Prüf-Workflow (spec-01), im Code gegengeprüft (`api/anfrage/route.ts:138`, `:394`, `:413`, `texte.ts:283`) | falsch | **erledigt** (D-651) — mit `antwort=seite` 303 auf die Dankseite der Sprache, ohne Vorgangsnummer (es gibt keinen Vorgang); Programme bekommen weiter JSON; `API_TEXTE` um `pruefen`, `datenschutzBestaetigen`, `zuVieleAnfragen`, gewählt nach den Feldern des Fehlers (`formularSammelmeldung`). Prüfungen: `tests/kern/anfrage-honigtopf.test.ts` (7, ruft die Route wirklich auf; gegen den alten Stand rot), `tests/kern/formular-eingang-zustaende.test.ts` |
| V-158 | **Rohes JSON nach reinen HTML-Formularen: Einteilung absagen/besetzen und die öffentliche Bewerbung** — `api/einsaetze/[id]/{absagen,besetzen}` antworteten auf Fachfehler mit `{"fehler": code}` (Absagegrund „ok" gegen drei Zeichen im Dienst, Doppelklick, veraltete Seite), das Schichtblatt las `?fehler=` nicht; `api/karriere/bewerbung` antwortete auf jeden Fehler mit JSON, und `name@firma` liess der Browser durch, der Dienst nicht | Gefunden vom Prüf-Workflow (quer-formulare), im Code gegengeprüft (`absagen/route.ts:79`, `besetzen/route.ts:141`, `einteilung.ts:730`, `karriere/bewerbung/route.ts:85`, `Formular.tsx:65`) | falsch | **erledigt** (D-652) — die Einteilungsrouten leiten mit dem GRUND auf die Schicht zurück (`grundAufsFormular`, `api/einsaetze/fehler.ts`), das Schichtblatt nennt den Satz aus `SCHICHT_TEXTE` (de/en) — auch für die Schicht-Absage, deren `?fehler=` bisher niemand las; Absagefeld mit `minLength={3}`, Einteilungsformulare nur mit `dienstplan.schreiben`. Die Bewerbung trägt `antwort=seite`, die Route leitet mit Grund auf die Formularseite (geschlossene Stelle: auf die Liste), der Satz steht als `role="alert"`; Eingaben reisen nicht in die Adresse, dafür prüft das E-Mail-Feld mit der Regel des Dienstes (`pattern` aus `BEWERBUNG_EMAIL`). Prüfungen: `tests/kern/einteilung-bewerbung-rueckweg.test.ts` (12, ruft die Bewerbungsroute wirklich auf) |
| V-159 | **Ein Wert aus der Adresse tat mehr, als er sollte: `?fehler=__proto__` legte die öffentlichen Karriereseiten lahm, und ein normalisierter Rückweg führte nach draussen** — `bewerbungsMeldung` schlug den Grund in einem Objektliteral nach; `__proto__` fand `Object.prototype`, `toString` eine Funktion, der Rückfall griff nicht, React warf, und `/karriere`, `/karriere/initiativbewerbung`, `/karriere/<id>/bewerbung` antworteten mit 500 (`toString`: leerer Alarmkasten). Dasselbe Nachschlagen in den Portalseiten aus V-154/V-158. `internesZiel` las den Pfad nach der Ursprungsprüfung ein zweites Mal als Adresse: `/.//boese.example` wurde zu `https://boese.example/` | Gefunden in der Nachprüfung von V-154…V-158, im Code gegengeprüft (`meldung.ts:48-50`, `karriere/page.tsx:44`, `ursprung.ts:187-193`; Probe an einer Kopie: `renderToString` wirft, Location zeigt nach draussen) | falsch | **erledigt** (D-653) — `eigenerEintrag` (`src/lib/nachschlagen.ts`, `Object.hasOwn`) in `karriere/meldung.ts`, `pflichtwegMeldung` und den fünf Portalseiten dieser Gruppe; ein Pfad, der nach dem Normalisieren mit `//` beginnt, ist kein Rückweg, das Ziel entsteht durch Setzen auf dem eigenen Ursprung und wird am Ende noch einmal verglichen — für jeden Aufrufer von `internesZiel`. **Offen gelassen:** dasselbe Nachschlagen in älteren Seiten hinter der Anmeldung ausserhalb dieser Gruppe (D-653, Punkt 2). Prüfungen: `tests/kern/nachschlagen.test.ts` (Helfer, alle Tabellen hinter `?fehler=`, Quelltext der Seiten), `tests/kern/einteilung-bewerbung-rueckweg.test.ts` (Prototyp-Schlüssel mit `renderToStaticMarkup`, vier Umwege über `grundAufsFormular`), `tests/kern/weiterleitung-ziel.test.ts` (acht normalisierte Formen) — gegen den alten Stand rot |
| V-160 | **Vier Sätze der Formulare sagten nicht, was war** — (1) `formularSammelmeldung` las die Ursache auf Englisch aus den Feldern; weil die Formularversion `datenschutz_hinweis` als Pflicht-Checkbox führt, stand bei mehreren leeren Feldern samt Häkchen auf Deutsch „Bitte prüfen Sie die markierten Felder.", auf Englisch „Please confirm … privacy notice"; (2) `besetzen` schickte ohne Beschäftigung den Grund des Absagens (`keine_auswahl`, „Welche Einteilung gemeint war …"); (3) der Honigtopf-Zweig versprach „dieselbe Antwort wie der Erfolg", führt aber ohne `?nr=` und ohne `leadnummer` — ein vergleichendes Programm erkennt ihn; (4) „(in German)" unter der Datenschutzerklärung stand unterstrichen, weil `no-underline` eine geerbte Linie nicht aufhebt | Gefunden in der Nachprüfung von V-156…V-158, im Code gegengeprüft (`texte.ts:496`, `seed/formulare.ts:60`, `besetzen/route.ts:71`, `dienstplan-schicht.ts:135`, `api/anfrage/route.ts:160-164`, `:423`, `Betroffenenwege.tsx:43`) | falsch | **erledigt** (D-651 und D-652, Nachträge) — `FormularFehler` trägt einen `grund`, gesetzt an derselben Stelle wie der deutsche Satz (`lead/annahme.ts`); fehlt NUR die Bestätigung, heisst er in beiden Sprachen „bestätigen", sonst „prüfen", ein Grund ohne eigenen Satz bekommt den allgemeinen; `besetzen` schickt `keine_anstellung` mit eigenem Satz (de/en); D-651, `annahme.ts` und der Zweig in `api/anfrage/route.ts` nennen die verbliebene Unterscheidbarkeit ausdrücklich als bewussten Preis; die Linie liegt auf dem Verweistext, nicht auf dem Verweis. Prüfungen: `tests/kern/anfrage-honigtopf.test.ts` (der ECHTE Dienst mit einer Datenbankattrappe, die bei jeder Abfrage wirft: mehrere Felder, nur das Häkchen, freiwillige Checkbox, Honigtopf), `tests/kern/einteilung-bewerbung-rueckweg.test.ts`, `tests/kern/oeffentliche-navigation.test.ts` |
| V-161 | **Die Referenz war ein frei getippter Eintrag, und ihr Blatt glaubte der Adresse** — V-154 legte eine Referenz FREI an, obwohl SPEC PRO-05 sie als „completed `auftrag` with customer release on file, not a marketing entry typed by hand" beschreibt; der Verweis von der Kundenfreigabe verlangte keinen Abschluss. Das Referenzblatt nahm `?auftrag=<id>` aus der Adresse und schlug Datum und Beleg IRGENDEINES Auftrags vor. `/neu` behauptete ohne `auftrag.lesen` „keine geltende Kundenfreigabe" über einen ungelesenen Auftrag; nach einer Abweisung waren alle Eingaben weg, ein Doppelklick endete in „Nichts wurde angelegt"; Liste und Blatt mischten Deutsch und Englisch; `{ weiter }` war nur am Quelltext geprüft; der Seed beging den Weg nicht; zu O-913 fehlte der Merker, ein alter trug die Nummer der Fotografie | Gefunden in der Nachprüfung von V-154, im Code gegengeprüft (`SPEC.md:77`, `kundenfreigabe/page.tsx:394`, `referenzen/[id]/page.tsx:129-151`, `neu/page.tsx:89-95`, `referenz-anlegen-weg.test.ts:49`, `inhalt/referenz.ts:186`) | falsch | **erledigt** (D-654) — eine Referenz entsteht nur aus einem ABGESCHLOSSENEN Auftrag mit geltender Kundenfreigabe (eine Regel, `referenzHindernis`; der Dienst prüft beim Anlegen erneut; storniert: nie; laufend: offen als **O-914**) und hält ihn fest: `drizzle/0410` (`referenz.auftrag_id`, zusammengesetzter Fremdschlüssel über die Gesellschaft, Auslöser gegen Umhängen, keine Kaskade — Herkunft, keine Kopplung, O-735 nachgeführt). `/neu` zeigt die Wahl des Auftrags und sagt je Fall das Richtige; das Blatt schlägt aus der eigenen Herkunft vor und zeigt sie; derselbe Auftrag unter derselben Adresse führt auf die vorhandene Referenz (`vorhanden=1`); bei Abweisung kommen Titel, Slug, Kunde und Jahr zurück, ein belegter Slug steht vor dem Absenden am Feld; die vier Referenzseiten sind zweisprachig und aus der Ausnahmeliste der Sprachwache gestrichen; `antwortNachHandlung`/`antwortNachAbweisung` sind am Verhalten geprüft; der Demo-Seed legt einen abgeschlossenen Auftrag mit Freigabe (DEMODATEN) und die Referenz daraus an; Merker O-913 an `legeReferenzAn`, O-914 an der Zustandsliste, der falsche O-13-Merker ersetzt. Prüfungen: `tests/isolation/referenz-anlegen.test.ts` (22, u. a. Herkunft je Hindernis, fremde Gesellschaft, ohne `auftrag.lesen`, Doppelklick, Fremdschlüssel und Auslöser), `tests/kern/referenz-anlegen-weg.test.ts` (Weiche und Abweisung am Verhalten, Regel, Seiten), `tests/isolation/website-pflege.test.ts`; Sichtprobe beider Sprachen gegen den Demo-Seed |
| V-170 | **Objektkoordinaten ließen sich auf keinem Weg erfassen** — `objekt.geo_lat`/`geo_lon` gibt es seit 0021 mit CHECKs, aber weder `legeObjektAn` noch `aendereObjekt` schrieb sie, kein Formular fragte danach, der Seed füllte sie nicht. Das Bautagebuch meldete deshalb für jede Baustelle dauerhaft „Keine Koordinaten am Objekt hinterlegt" (BAU-08), ohne dass es einen Ort zum Eintragen gab; OPS-01 verlangt „address and coordinates" | Gefunden vom Prüf-Workflow (spec-03, OPS-01), im Code gegengeprüft (`objekt/anlegen.ts:148`, `:217`, `bau/wetter.ts:236`) | blockiert | **erledigt** (D-664) — Anlegen und Ändern schreiben das Paar; `leseKoordinate`/`koordinatenAus` nehmen Dezimalgrad mit Komma oder Punkt, runden auf sechs Stellen in Mikrograd (`bigint`, nie Float), prüfen den gerundeten Wert gegen ±90°/±180° und verlangen beide oder keinen; ein leeres Paar räumt ab. Objektformular mit Breiten-/Längengrad, Objektblatt zeigt das Paar oder wozu es fehlt, Abweisungen übersetzt statt als deutscher Rohsatz. Seed mit beschrifteten Näherungswerten: das Bautagebuch-Wetter kommt jetzt bis `keine_station`. Automatische Geokodierung bleibt O-122. Prüfungen: `tests/kern/objekt-koordinaten.test.ts`, `tests/isolation/objekt-anlegen.test.ts` §6 |
| V-171 | **Der Raumbuch-Import wies Excel ab, obwohl die Spec „Excel/CSV" verlangt und die ROADMAP den Punkt abgehakt hatte** — die Abweichung stand nur in einem Code-Kommentar, nicht als Entscheidung oder offene Frage; die Abweisung (und jede andere der Route) kam als weisse JSON-Seite mit HTTP 415/413/400, die Umleitung nahm den Bereich aus `?mandant=`, und der empfohlene Umweg „als CSV speichern" scheiterte an der Windows-1252-Kodierung, die ein deutsches Excel dafür wählt | Gefunden vom Prüf-Workflow (spec-03, OPS-04), im Code gegengeprüft (`api/raumbuch-import/route.ts:77`, `:94`, `raumbuch/tabelle.ts:12`, `ROADMAP.md:199`) | behindert | **erledigt** (D-665) — keine neue Abhängigkeit (O-919); die Importseite nennt die Grenze vor dem Hochladen und den Weg über CSV; `istTabellenkalkulation` erkennt `.xlsx`/`.xlsm`/`.ods` (ZIP) und `.xls` (OLE2) am Inhalt; `leseTextDatei` liest UTF-8 streng und sonst Windows-1252; jede Abweisung führt auf die Importseite mit übersetztem Satz (verschobene Zeile mit Nummer), Bereich aus der Sitzung; ROADMAP: CSV erledigt, Excel offen. **Offen:** Excel direkt lesen — O-919. Prüfungen: `tests/kern/raumbuch-import.test.ts` (10), `tests/isolation/raumbuch-import.test.ts`, `tests/e2e/raumbuch-import.spec.ts` (3, angepasst, nicht in dieser Sitzung gelaufen) |
| V-172 | **Auftragsassistent und Kalkulationsbestätigung antworteten auf jede Abweisung mit einer weissen JSON-Seite, und ihre Umleitung nahm den Bereich aus der Adresszeile** — „1.234,5" oder „40 Std" in den Wochenstunden, ein Personalbedarf über 5.000, ein fehlender Nummernkreis oder ein unlesbarer Stundensatz endeten als `{"fehler":…}` ohne Formular; `Number(roh.replace(',', '.'))` machte aus einer deutschen Zahl `NaN`; ein ungültiges Datum, eine Laufzeit vor dem Start oder eine fremde Leitung liefen bis in die Datenbank (500); `?mandant=` bestimmte das Ziel nach dem Schreiben | Gefunden vom Prüf-Workflow (spec-03, OPS-07/OPS-10), im Code gegengeprüft (`api/auftrag/route.ts:91–159`, `api/kalkulation/route.ts:47–95`, beide Seiten ohne `fehler`) | behindert | **erledigt** (D-666) — Abweisungen führen auf die Maske mit übersetztem Satz (Kalkulation zusätzlich mit dem Feld), erst NACH der Rechteprüfung; `pruefeAuftragsangaben` liest deutsche Zahlen mit den Grenzen aus 0025, `pruefeAuftragsbezug` fragt Datum, Laufzeit, Kunde, Objekt und Leitung unter RLS vor der Nummer; der Bereich kommt aus der Sitzung, `?mandant=` fällt aus beiden Formularen. Prüfungen: `tests/kern/auftrag-angaben.test.ts`, `tests/isolation/auftrag-pflege.test.ts` (1)(2) |
| V-173 | **Ein Auftrag war nach der Anlage unveränderlich; von Hand angelegte hatten nie einen Wert, gewandelte nie Personalbedarf, Stunden und Ausstattung** — der Assistent hatte kein Wertfeld (Wertkennzahlen zählten solche Aufträge mit 0, die Abschlagsrechnung hatte keine Grundlage), `wandleInAuftrag` setzte keines der OPS-10-Felder, und das einzige `update auftrag` betraf Zustand, Abschluss und Kundenfreigabe — kein Tippfehler in Leitung, Laufzeit oder Bezeichnung ließ sich korrigieren | Gefunden vom Prüf-Workflow (spec-03, OPS-05/OPS-09/OPS-10), im Code gegengeprüft (`api/auftrag/route.ts:125`, `angebot/index.ts:525`, `annahme/page.tsx:272`, `grep "update auftrag"`) | blockiert | **erledigt** (D-667) — Assistent mit „Auftragswert netto" über `parseGeld` in Cent; Annahme fragt Personalbedarf, Stunden, Ausstattung und reicht sie an `wandleInAuftrag`; neue Pflege `/auftraege/[id]/bearbeiten` → `POST /api/auftrag/aendern` → `aendereAuftrag` (Bezeichnung, Beschreibung, Leitung, Laufzeit, Wert, Personalbedarf, Stunden, Ausstattung) unter `auftrag.schreiben`, gesperrt für abgeschlossen/storniert, der Wert aus einem Angebot bleibt (Nachtrag), jede Änderung mit Vorher/Nachher im Protokoll; Knopf „Bearbeiten" auf dem Auftragsblatt. Prüfungen: `tests/kern/auftrag-angaben.test.ts` (1b), `tests/isolation/auftrag-pflege.test.ts` (3)(4) |
| V-174 | **Die Kalkulation kannte weder Material noch Gerät; Angebote wurden allein aus Lohn und Zuschlägen gerechnet** — OPS-07 verlangt fünf Kostenblöcke, das Schema trug sie seit 0023, aber `kalkuliere` rechnete nur Lohn → Gemeinkosten → Wagnis/Gewinn und kein Weg legte eine Material- oder Gerätezeile an: ein Reinigungsangebot enthielt nie Reinigungsmittel oder Maschinen und war systematisch zu niedrig. Die gewählte Basis `selbstkosten`/`je_kostenart` wurde gespeichert und trotzdem auf den Lohn gerechnet; die Zuschlagszeilen trugen beide den Lohn als Bezug | Gefunden vom Prüf-Workflow (spec-03, OPS-07), im Code gegengeprüft (`kalkulation/index.ts:105`, `bestaetigung.ts:376`, `:404`, `angebot/index.ts:215`) | falsch | **erledigt** (D-668) — `kalkuliere` nimmt Material und Gerät (Summe erfasster Zeilen, nie vorbelegt) und wendet die Basis an (`lohn` / `selbstkosten` = Lohn + Material + Gerät); `je_kostenart` wird mit TODO(client, O-16) abgewiesen; Kostenzeilen über `setzeKostenposition` (Menge als deutsche Zahl über `leseZahl`, „12.50" als `mehrdeutig` abgewiesen, Menge × Einzelpreis per `multipliziereMitMenge`, nie negativ, Grenzen der Spalte, berichtigt statt gelöscht, Protokoll), danach Neuberechnung über dieselbe Funktion — Kalkulationssumme und Angebotsnetto nennen denselben Betrag; gesperrt nach Versand und nach Preisfreigabe, für Kostenzeile UND neue Bestätigung (O-732); Zuschlagszeilen mit eigenem Bezug und freier Nummer; Kalkulationsseite mit den fünf Blöcken und „Material und Gerät", Abweisung mit Satz und Feld; Seed: zwei Demozeilen im Entwurf. **Offen:** Angebote von Hand (Security, Bau) ohne Kalkulation; ein von Hand berichtigter Raumbuch-Entwurf wird bei jeder Neuberechnung aus der Kalkulation neu bepreist (D-626, schon vor V-174). Prüfungen: `tests/kern/kalkulation-material.test.ts`, `tests/isolation/kalkulation-kostenposition.test.ts`, `tests/isolation/angebot-dienst.test.ts` |
| V-175 | **Plattformkatalog und Registrierungsstand des Vergaberadars ließen sich nirgends pflegen; die Warnung „nicht freigeschaltet" konnte deshalb nie auslösen** — `vergabeplattform` und `mandant_plattform_registrierung` hatten seit 0145 Schema, RLS und Schreibrechte, aber keinen Dienst, keine Route und kein Formular; die Plattformseite versprach „die Super-Administration trägt die Plattformen ein", `ausschreibung.vergabeplattform_id` blieb immer leer, Liste, Kennzahl und Detailwarnung griffen nie, und `ausschreibung_vorgang.plattform_pruefung` schrieb nur der Seed | Gefunden vom Prüf-Workflow (spec-03, RAD-09), im Code gegengeprüft (`radar/plattformen/page.tsx:103`, `radar/[id]/page.tsx:225`, `ls api/radar`, `grep plattform_pruefung src`) | behindert | **erledigt** (D-669) — `services/radar/plattform.ts`: Katalog eintragen, ändern, bestätigen, archivieren nur für die Super-Administration (Satz statt Policy-Verletzung), neuer Eintrag bleibt Platzhalter bis zur Bestätigung (O-07), Seed bleibt leer (D-490); nach jeder Katalogänderung ordnet `app.radar_plattform_nachordnen` (0420, Definer mit `d_`-Policies und Spaltenrechten) die schon eingelesenen Bekanntmachungen ohne Plattform über denselben Auslöser nach; Registrierungsstand je Gesellschaft als Upsert unter `radar.plattform_verwalten` mit Protokoll, Serverzeit und den CHECKs aus 0145 als Satz, kein Kennwort (SEC-A5); `POST /api/radar/plattform` mit Bereich aus der Sitzung und `?fehler=`; Plattformprüfung am Vorgang auf „Stand setzen" (ohne Vorgang abgewiesen); Plattformseite zweisprachig, Stände in Worten auch im Detailblatt. **Offen:** welche Plattformen gelten (O-07, Eintrag durch Menschen). Prüfungen: `tests/kern/radar-plattform.test.ts`, `tests/isolation/radar-plattform.test.ts` |
| V-176 | **Auftrags- und Projektblatt zeigten weder Aufgaben noch Dokumente, und ein Dokument ließ sich gar nicht an einen Auftrag hängen** — OPS-11 verlangt Aufgaben, Fristen, Stand und Dokumente an jedem Auftrag und Projekt, die Seitenkarte nennt am Auftragsblatt die Reiter „Dokumente" und „Aufgaben"; beide Blätter hatten keinen Abschnitt dafür, Aufgaben am Auftrag (V-096) erschienen nur in der Gesamtliste, deren Filter nur den Bezugs-TYP kannte, und `dokument` (0009) kannte nur Kunde, Objekt und Formulareingang | Gefunden vom Prüf-Workflow (spec-03, OPS-11), im Code gegengeprüft (`grep -i "aufgabe\|dokument"` über beide Blätter: 0 Treffer, `AufgabeFilter` in `kern/aufgabe.ts`, `drizzle/0009_dokument.sql`, `grep "alter table dokument add" drizzle`) | behindert | **erledigt** (D-670) — 0421: `dokument.auftrag_id` mit zusammengesetztem Fremdschlüssel (nur ein Auftrag derselben Gesellschaft); `legeAb` prüft den Auftrag unter RLS (`BezugUnbekannt`), die Ablage fragt ihn und übernimmt ihn aus `?auftrag=`; `AufgabeFilter.auftragId`/`projektId` im gemeinsamen WHERE-Baustein (`auftrag_id` oder polymorpher Bezug, beim Projekt dazu Bezug `projekt`), Liste und Kopfzahl über dieselbe Menge; `aufgabenAkte` und `fristInWorten` im Dienst (offen aus der Zählung, überfällig nur Offenes, Frist TT.MM.JJJJ Europe/Berlin); `leseDokumenteAmAuftrag`; ein Bauteil `VorgangAkte` auf Auftrags- und Projektblatt (das Projekt über seinen Auftrag, 0071) mit offenen Aufgaben, Frist, Stand und Dokumenten, „Aufgabe anlegen"/„Dokument ablegen" nur mit dem Schreibrecht des Ziels, fehlendes Leserecht als Satz, zweisprachig; `/aufgaben?auftrag=`/`?projekt=` unter RLS aufgelöst (`vorgangImFilter`) mit Vorwahl im Anlegeformular; Seed hängt beide Kundenschreiben an ihren Auftrag. **Nicht Teil:** die übrigen Reiter der Seitenkarte und das nachträgliche Umhängen eines Dokuments. Prüfungen: `tests/kern/aufgabe-akte.test.ts`, `tests/kern/dokument-ablage.test.ts`, `tests/isolation/auftrag-akte.test.ts` |
| V-177 | **Die Auftragspflege wies jede Änderung ab, sobald die bisherige Leitung die Gesellschaft verlassen hatte — auch ohne Leitungswechsel** — `aendereAuftrag` rief `pruefeAuftragsbezug` bei jedem Speichern, und das fragte `app.ist_mitglied` für die gewählte Leitung, auch wenn sie die bisherige war. Die Pflegeseite nimmt die bisherige Leitung eigens in die Auswahl auf, damit Speichern sie nicht still wechselt; ein Tippfehler in der Bezeichnung endete dann in „Die gewählte Leitung arbeitet nicht in dieser Gesellschaft", obwohl der Auslöser `kern.auftrag_verantwortlich_im_mandant` (0025) bei unveränderter Leitung sofort zurückkehrt und die Zeile angenommen hätte. Dazu boten die Auswahllisten (Pflege, Assistent, Annahme) Mitgliedschaften an, deren `gueltig_ab`/`gueltig_bis` sie nicht prüften (der Assistent auch nicht `entzogen_am`) | Gefunden vom unabhängigen Prüfer der Gruppe operations (wf/operations-2), im Code gegengeprüft (`aendern.ts:136`, `angaben.ts:202`, `drizzle/0375` `app.ist_mitglied`, `drizzle/0025` Auslöser, `bearbeiten/page.tsx:98`, `auftraege/neu/page.tsx:97`, `annahme/page.tsx:105`) | blockiert | **erledigt** (D-667, berichtigt) — `pruefeAuftragsbezug` nimmt `bisherigeLeitung` und fragt die Mitgliedschaft nur bei einem Wechsel (`leitungWechselt`, UUID ohne Rücksicht auf Gross-/Kleinschreibung, wie der Auslöser); `aendereAuftrag` reicht die Leitung der gesperrten Zeile mit. Ein WECHSEL zu jemandem ohne gültige Mitgliedschaft bleibt abgewiesen. Eine Stelle für die Auswahl: `waehlbareLeitungen` fragt `app.ist_mitglied`, dieselbe Funktion wie Dienst und Auslöser; die Pflegeseite nennt eine ausgeschiedene bisherige Leitung als „nicht mehr in dieser Gesellschaft". Prüfungen: `tests/kern/auftrag-angaben.test.ts` (1c), `tests/isolation/auftrag-pflege.test.ts` (3a) — Mitgliedschaft ausgelaufen und entzogen, Grossschreibung, Wechsel, Auswahlliste |
| V-238 | **Angebote von Hand hatten keine Kalkulation, und das stand nirgends als offene Frage** — OPS-07 nennt fünf Kostenblöcke; die Reinigung rechnet sie seit V-174 aus dem Raumbuch. Ein Angebot von Hand (Sicherheit, Bau, `angebot/von-hand.ts`) trägt je Position den eingetragenen Einzelpreis, ohne Material, Gerät, Gemeinkosten oder Wagnis/Gewinn. D-668 und V-174 führten das nur als „nicht Teil", ohne O-Frage und ohne `TODO(client)` | Gefunden vom unabhängigen Prüfer der Gruppe operations (wf/operations-2, Befund 25/OPS-07), im Code gegengeprüft (`angebot/von-hand.ts`, `kalkulation/page.tsx:231`, D-668 „Nicht Teil") | falsch | **offen gehalten, ehrlich** (D-671, O-920) — keine erfundene Kalkulation; `HAND_ANGEBOT_KALKULATION` (Platzhalter, `vorhanden: false`, `offeneFrage: 'O-920'`) mit `TODO(client, O-920)` am Dienst; die Maske `/angebote/neu` sagt zweisprachig, dass der Einzelpreis der eingetragene ist und nichts nachgerechnet wird, das Kalkulationsblatt eines Angebots ohne Kalkulation nennt dieselbe Frage; D-668 an Ort und Stelle berichtigt. Prüfungen: `tests/kern/kalkulation-material.test.ts` (6) |
| V-239 | **Der Wert eines Auftrags aus einem Angebot war in Reinigung und Sicherheit auf keinem Weg korrigierbar — und die Seite verwies auf einen Nachtrag, den es dort nicht gibt** — `aendereAuftrag` sperrt den Wert (`wert_aus_angebot`), die Texte sagten „eine Änderung des Vertragswerts ist ein Nachtrag" (D-667 Punkt 5). Nachträge gibt es nur im Bau (0080, § 2 VOB/B, am Projekt). Die Sperre war eine selbst gesetzte Fachregel ohne O-Frage | Gefunden vom unabhängigen Prüfer der Gruppe operations (Befund 23/OPS-05), im Code gegengeprüft (`aendern.ts:144`, `i18n/verwaltung/auftrag.ts:84`, `drizzle/0080` `nachtrag.projekt_id`) | behindert | **offen gehalten, ehrlich** (D-732, O-921) — die Sperre bleibt überall (keine zweite Wahrheit über den angenommenen Betrag); `wertAenderungsweg(mandant.module)` als bezeichneter Platzhalter mit `TODO(client, O-921)`: `nachtrag` bei gebuchtem Gewerk Bau, sonst `offen`; die Pflegeseite sagt unter dem Wert zweisprachig „Nachtrag nach § 2 VOB/B am Projekt" bzw. „noch nicht entschieden (O-921)"; die Abweisung behauptet keinen Nachtrag mehr; D-667 Punkt 5 berichtigt. Prüfungen: `tests/kern/auftrag-angaben.test.ts` (1d), `tests/isolation/auftrag-pflege.test.ts` (4) |
| V-240 | **Die kleinen Punkte der Gruppe operations** — (1) doppelter Import von `nachSprache` auf dem Auftragsblatt; (2) zwei Primary-Knöpfe auf der Kalkulationsseite; (3) `mt-1` statt Token auf Plattformseite (und Kalkulation, Leistungskatalog); (4) RAD-09: Warnungen beachteten `registrierung_erforderlich` nicht, eine abgelaufene Registrierung galt ausser auf der Plattformseite als freigeschaltet; (5) Registrierung in Liste und Detail nicht ausdrücklich an den aktiven Bereich gebunden; (6) 0420 schrieb bei jeder Katalogänderung ALLE Bekanntmachungen ohne Plattform an; (7) `pruefeAuftragsangaben` überging `mehrdeutig` („12.50" still 12,5 Wochenstunden); (8) die Basis „Selbstkosten (Lohn + Material + Gerät)" legte fest, was O-16 offenlässt; (9) deutsche Formate in der englischen Oberfläche (Koordinate, Geld, Ablagetag, Frist) und der deutsche Satz zu `einsaetze_offen` aus `?meldung=`; (10) das Recht als Schlüssel `objekt.schreiben` (und `angebot.schreiben`) im Satz; (11) nach einer Abweisung standen Pflege und Annahme ohne die Eingaben da; (12) die Zeilennummer der Abweisung `feldzahl` verrutschte bei Leerzeilen und mehrzeiligen Feldern; (13) ein unbekannter Aufgabenstand hiess „offen" | Gefunden vom unabhängigen Prüfer der Gruppe operations (wf/operations-2), jeder Punkt im Code gegengeprüft | behindert | **erledigt bis auf einen begründeten Punkt** (D-733; D-668, D-669 an Ort und Stelle berichtigt) — (1)–(3) behoben, ein Primary „Werte bestätigen" über `Button`; (4)(5) EINE Frage `freischaltungSql` für Liste, Kennzahl, Detail (abgelaufen mit Datum), Plattformseite, Mappe und Gruppe, gebunden an `app.aktiver_mandant()` bzw. die Gesellschaft der Mappe; (6) 0422: Hostregel in `app.radar_url_host`/`app.radar_host_passt`, Auslöser (0145) und Nachordnen (0420) rufen sie, angeschrieben wird nur, was passt; (7) `mehrdeutig` mit Feld, zweisprachig; (8) Auswahl „Lohn + Material + Gerät (Einzelkosten)", Satz nennt O-16, `TODO(client, O-16)` an `gemeinkostenBezug`; (9) `formatiereGeldIn`, `formatiereMengeIn`, `tagInSprache`, `fristInWorten(…, sprache)`, `koordinateAlsText(…, sprache)` für die Anzeige, Formulare bleiben deutsch; `einsaetze_offen` als Zahl `?anzahl=` und Satz je Sprache, kein Text aus der Adresse mehr im Warnkasten (V-153); (10) `rechtName`; (11) `maskeFelder` im Übergangsgerüst, Pflege und Annahme belegen aus `vorbelegt` vor; (12) der Leser zählt Dateizeilen; (13) „Stand unbekannt". **Nicht umgesetzt:** `credential_ref` bleibt ohne Feld — D-669 Punkt 6 (kein Tresor angebunden, ein Feld verwiese auf etwas, das es nicht gibt). Prüfungen: `tests/kern/{objekt-koordinaten,radar-plattform,auftrag-angaben,kalkulation-material,raumbuch-import}.test.ts`, `tests/isolation/{radar-plattform,auftrag-akte}.test.ts` |

| V-125 | **Fünf Seiten waren gebaut, bewacht, im Manifest geführt — und von nirgends erreichbar** | Der Mandant bat um eine Durchsicht „jedes Ein- und Ausgangs" und nannte ausdrücklich „صفحات مخفية" — versteckte Seiten. Nachgemessen über alle 400 Portalseiten, jede Meldung von Hand an der Nabe nachgelesen (die Messung allein gab Dutzende Fehlalarme, weil eine Adresse hier auf fünf Arten entsteht): `/einstellungen/benutzer/einladen` — **der erste Schritt nach der Einrichtung**, wer ein Verwaltungskonto anlegen wollte, musste die Adresse kennen; `/auftraege/[id]/abrechnung` — WIE dieser Auftrag abgerechnet wird (FIN-16), eine Frage, die man am Auftrag stellt und sonst nirgends; `/bau/projekte/[id]/lv/import` — der GAEB-Import, und ein Leistungsverzeichnis entsteht im Regelfall nicht von Hand; `/reinigung/sonderleistungen` — Glas, Sonder- und Grundreinigung, also das, was neben dem Turnus separat beauftragt und abgerechnet wird; und `/finanzen/lieferanten` — **ein Befund in eigener Sache**: die Seite entstand zwei Änderungen zuvor (V-011-Umfeld) und bekam keinen Eingang, während § 13b und § 48 an genau diesen Zeilen hängen | blockiert | **erledigt** — jeder Verweis steht an seiner Nabe und unter dem Recht seines ZIELS (AUT-06): `system.verwaltungskonto_erstellen` an der Benutzerliste, `abrechnung.schreiben` am Auftragsblatt, `bau.schreiben` am Leistungsverzeichnis, die Sonderleistungen ohne eigenen Wächter (dasselbe `reinigung.lesen` wie ihr Modulblatt), die Lieferanten als Karte im Finanzblatt, das die Rechte seiner Karten ohnehin aus dem Manifest zieht. Sperrklinke: `tests/kern/seite-ohne-eingang.test.ts` — die **Umkehrung** von `verweis-rechte.test.ts`: die prüft, ob ein Verweis zu seinem Ziel passt, diese, ob es ihn überhaupt gibt. Sie lässt `routen.generiert.ts` ausdrücklich aus der Quelle — das Verzeichnis ALLER Adressen deckte sonst jede Seite zu, auch die versteckte, und die Prüfung meldete auf ewig null. Ihre bekannte Grenze steht im Kopf der Datei: ein einzelnes Glied, das anderswo noch einmal vorkommt, deckt seine Seite zu (`lv/import` gegen `raumbuch/import` und `bank/import`) — sie meldet lieber zu wenig als zu viel, weil eine Sperrklinke mit Fehlalarmen nach dem dritten Mal abgeschaltet wird und dann gar nichts mehr meldet |

| V-124 | **Eine Rechnung ist nicht zehn Jahre unlöschbar, sondern dauerhaft** | Gefunden beim Bau des Aufbewahrungslaufs (V-116): `dokument_aufbewahrung` führt für `rechnung`, `buchhaltung`, `beleg` und `vertrag` eine **entschiedene** Frist (10 Jahre, § 147 AO) — und in derselben Zeile `loeschsperre = true` (0009:275). `kern.setze_aufbewahrung` schreibt die Sperre beim Anlegen fest, `trg_dokument_aufbewahrung` lässt sie nie wieder lösen (D-49), und auch der Eigentümer der Tabelle kommt nicht daran vorbei — nachgemessen, als der Testaufbau genau daran scheiterte. `08-PR-PLAN.md` PR 64 führt dagegen als Zusage „for the full ten years", und Art. 5 Abs. 1 lit. e DSGVO verlangt eine OBERgrenze, nicht nur eine Untergrenze. Eine der beiden Seiten muss nachgeben | behindert | **offen — Rechtsfrage, keine Einstellung (O-894)**. Was stattdessen geschehen ist: die Reichweite wird nicht mehr behauptet, sondern genannt. Der Lauf erreicht heute `angebot` und `kunde` (je sechs Jahre, § 257 HGB) — die beiden einzigen Klassen ohne Sperre —, das Löschkonzept schreibt genau diese zwei hin, und `tests/isolation/aufbewahrung-lauf.test.ts` §2 hält den Zustand als Prüfung fest: wer sie eines Tages ändert, ändert eine Rechtsfrage und soll das merken |

| V-123 | **Das Portal zeigte dem Menschen den Rechteschlüssel, nicht das Recht** | Vom Mandanten in derselben Bitte gefunden wie die rohe JSON auf dem Freigabeblatt: „ان كان ب اي صفحة الكود ظاهر صلحلي ياهن خليه اطار وجملة بدال كود". Nachgemessen: **141 Stellen** in **81 Dateien** schrieben `<code>kalkulation.lesen</code>` mitten in einen deutschen Satz — „Ihnen fehlt `kalkulation.lesen`", „Bestätigen darf, wer `bau.schreiben` hält". Der Adressat erfährt, dass ihm etwas fehlt, aber nicht WAS, und kann deshalb auch nicht danach fragen | behindert | **erledigt** — `<Recht schluessel=…>` schreibt den Satz („Kalkulationen lesen") und behält den Schlüssel in `title` und `data-recht`: die Administration vergibt ihn wortwörtlich, und die Browserläufe dürfen nicht am übersetzten Wort hängen. **Zusammengesetzt statt abgetippt:** 243 Schlüssel aus 112 Hauptwörtern und 41 Tätigkeitswörtern (`lib/i18n/rechtname.ts`) — 486 handgeschriebene Zeilen wären beim 244. Schlüssel unvollständig, und der Katalog wächst mit jedem Modul. Deutsch stellt das Tätigkeitswort hinten, Englisch vorn; eine gemeinsame Reihenfolge gäbe in einer der beiden Sprachen Unsinn. Der Name steht in Anführungszeichen, **weil er ein Name ist**: „Ihnen fehlt Kalkulationen lesen" ist kein deutscher Satz. Ein unbekanntes Teil wird lesbar gemacht statt versteckt (`crm_entgelt` → „Crm Entgelt"), damit es nachgetragen wird, statt unbemerkt roh dazustehen. Sperrklinken: `tests/kern/recht-im-satz.test.ts` (kein Katalogschlüssel wieder roh in einem `code`-Element, auch nicht über eine `RECHT…`-Konstante) und `tests/kern/rechtname.test.ts` (jeder der 243 Schlüssel ergibt in beiden Sprachen einen Satz) |

| V-120 | **Ein Sprachmodell liess sich nur mit handgeschriebenem SQL freigeben** — gefunden vom Mandanten beim Einrichten | `docs/EINRICHTEN-*.md` §9 beschrieb den letzten Schritt der KI-Anbindung als `insert into modell_register …` in einem Datenbankwerkzeug. Genau dort blieb er stehen: „وشو هاد السطر؟ شو هدون؟" — und er hatte recht. `drizzle/0154` gibt `cse_app` auf `modell_register` **nur `select`**: keine Policy, kein Grant, kein Schreibweg. Die Zeile ist aber eine **Rechtsaussage** (ein Mensch bezeugt, dass ein fremder Dienstleister Vertragstext verarbeiten darf) — ohne Formular gibt es keine Pflichtfelder, keinen Nachweis, und `geprueft_von` hätte der Betreiber über sich selbst eintippen müssen | blockiert | **erledigt** — `/portal/[mandant]/einstellungen/modelle` mit Liste und Formular, `POST /api/system/modelle`, `drizzle/0381`. **Der Zeuge kommt nicht aus dem Formular:** `trg_modell_register_zeuge` setzt `app.aktueller_benutzer()` — ein Name, den jemand über sich selbst schreibt, ist keine Bezeugung. Die drei Bestätigungen stehen einzeln und werden einzeln angezeigt: ein blosses „nicht aufrufbar" sagt nicht, WELCHE fehlt, und das ist die nächste Frage des Betreibers. Eine Freigabe ohne die beiden anderen wird abgewiesen, statt wirkungslos gespeichert zu werden (`app.modell_fuer` verlangt die Konjunktion). Eine Freigabe ohne Nachweis ebenso — sie ist das, was eine Aufsichtsbehörde liest. `aal2` in der Policy (K-15), kein DELETE (Invariante 8). Verlinkt von `/einstellungen/integrationen`, wo man danach sucht. 12 Prüfungen in `tests/isolation/modellregister-schreiben.test.ts` |

| V-119 | **Der Nachtlauf, der die Konten öffnet, durfte gar nicht schreiben — ein Befund in eigener Sache.** `kontenRollover` (V-008) wurde gebaut, mit zwölf Prüfungen abgedeckt und ausgeliefert. **Alle zwölf rufen den DIENST `eroeffneKonto` als `cse_app`.** Der Lauf selbst läuft als `cse_job`, und `drizzle/0060` gibt `cse_job` auf `stundenkonto` nur `select` — mit dem Kommentar „Der nächtliche Abgleich liest — und schreibt nichts". Er wäre bei seinem ersten Versuch um 00:30 mit „permission denied for table stundenkonto" abgebrochen, in einem Protokoll, das niemand liest: genau der Fehler, gegen den er gebaut wurde, eine Ebene tiefer. Dazu die zweite Hälfte: `alsJobSitzung` setzt `app.readonly = 'on'`, solange der Aufrufer nicht `{ nurLesen: false }` mitgibt — jeder andere schreibende Lauf im Baum tut das (`akquise`, `belegarchiv`, `bewerberLoeschung`, `mahnlauf`), dieser nicht. | `drizzle/0060_stundenkonto.sql:749`, `src/server/jobs/kontenRollover.ts:99` gegen `src/server/jobs/sitzung.ts:94` | blockiert | **erledigt** — `drizzle/0380`: `j_konto_anlegen` und `j_urlaubskonto_anlegen`, je `for insert to cse_job`, gebunden an `app.aktiver_mandant()` **und** `not app.ist_readonly()`. Die zweite Bedingung ist die Kopplung, die den zweiten Teil des Befundes unmöglich macht: ein Lauf, der den Schreibmodus vergisst, schreibt nicht etwa doch und tut auch nicht still nichts — er bricht sichtbar ab. Beide Läufe geben jetzt `nurLesen: false` mit. Die Sperrklinke ist `tests/isolation/nachtlauf-schreibrecht.test.ts`: sie bindet dieselben GUCs wie `alsJobSitzung`, schaltet auf `cse_job` und schreibt **ohne eine Zeile Anwendungscode dazwischen** — ein fehlender Grant fällt dort auf und nicht um 00:30. Die alten Lesekommentare in `0060`/`0061` („schreibt nichts") sind richtiggestellt; ein Kommentar, der das Gegenteil dessen behauptet, was danebensteht, beruhigt genau an der Stelle, an der man nachsehen müsste. **Nachgemessen, ob es weitere gibt:** von den zehn Läufen, die `alsJobSitzung` rufen, schreiben vier in Tabellen — `akquise_lauf`, `bewerbung`, `bewerbung_loeschlauf` haben Grant und Policy seit je, und alle vier geben `nurLesen: false` mit. Der Fehler war auf die beiden Kontotabellen beschränkt, also auf das, was in dieser und der letzten Sitzung entstand — nicht auf die Bauart der Läufe |

| V-117 | **Das Urlaubskonto lässt sich nicht eröffnen.** `eroeffneUrlaubskonto` (`services/zeit/urlaubskonto.ts`) hat ausser Tests keinen Aufrufer — dieselbe Lücke, die V-008 beim Stundenkonto hatte, eine Tabelle weiter. Ohne Konto rechnet jeder Urlaubsantrag gegen nichts. | Gefunden von `tests/kern/dienst-verdrahtung.test.ts` | blockiert | **erledigt** — Nachtlauf `urlaubskonten_jahr` (täglich 00:45, nicht nur am 1. Januar: die Anstellung, die am 12. Februar beginnt, braucht ihr Konto am 12. Februar). Er setzt **keinen** Anspruch und **keinen** Übertrag: beides ist Vertrags- und Tarifrecht (O-18, § 7 Abs. 3 BUrlG), und eine hergeleitete Zahl würde zur Grundlage eines Restanspruchs, den niemand zugesagt hat |

| V-118 | **Der Urlaubsanspruch lässt sich nirgends setzen.** `setzeAnspruch` (`services/zeit/urlaubskonto.ts`) hat überhaupt keinen Aufrufer, auch keinen Test. Der Anspruch je Jahr ist die Zahl, gegen die jeder Antrag gerechnet wird; ohne sie ist jeder Urlaub rechnerisch unbegründet. | Gefunden von `tests/kern/dienst-verdrahtung.test.ts` | blockiert | **erledigt** — `/portal/[mandant]/personal/urlaubskonten` mit Liste und Formular, `POST /api/personal/urlaubsanspruch` an `zeit.schreiben`. Die Liste unterscheidet **„nicht hinterlegt"** von **„0 Tage"** — das erste ist eine offene Frage, das zweite eine Auskunft, und wer „Resturlaub: 0" liest, plant sein Jahr danach. Halbe Tage gehen durch `mengeAusEingabe` (neu, 7 Prüfungen in `tests/kern/menge-eingabe.test.ts`): Tausendstel als `bigint`, nie eine Gleitkommazahl — ein halber Tag, der als 0,4999 ankommt, ist der Streit, den niemand gewinnt. 6 Prüfungen in `tests/isolation/urlaubsanspruch.test.ts` |

| V-114 | **Das `Mehr`-Blatt gab Kundenkonten den INTERNEN Baum.** Der Kommentar an `KUNDEN_NAVIGATION` sagte beruhigend, die Kundenleiste trage kein `Mehr` — `OHNE_MEHR` führt aber nur `mitarbeiter` und `gruppe`. Das Blatt gab es also, und weil `MehrZelle` genau zwei Bäume kannte, fiel das Kundenportal auf `NAVIGATION`: ein Kundenkonto mit `objekt.lesen` sah dort interne Punkte, die unter `/portal/kunde` auf 404 führen, und jeder davon verriet die Existenz dessen, was er nicht zeigen darf (AUT-06). | `registry/tableiste.ts:172` gegen den Kommentar in `registry/navigation.ts:600` | blockiert | **erledigt** — mit V-043 in derselben Änderung: die Rechtekarte wird für `kunde` aus `KUNDEN_NAVIGATION` gebaut, interne Schlüssel stehen nicht mehr darin, und `=== true` blendet sie aus |

| V-111 | **Der abgeleitete Rückweg stand auf 46 Seiten ZWEIMAL.** Die Reparatur von V-108 zeichnet ihn in der Hülle; 46 Seiten trugen ihren eigenen weiter als `<nav aria-label="Zurück">` im Inhalt. Zwei Pfeile untereinander, die dasselbe sagen, lassen den Leser prüfen, ob sie dasselbe Ziel haben — ein doppelter Ausgang ist schlechter als ein fehlender. | `grep -rl 'aria-label="Zurück"' src/app/portal` → 46, keine davon mit `zurueck=`-Eigenschaft | behindert | **erledigt** — alle 46 in die Eigenschaft `zurueck` gehoben (Ziel und Wortlaut unverändert, Antippfläche jetzt 44 px); Sperrklinke: `tests/kern/rueckweg.test.ts` §6 verlangt, dass `aria-label="Zurück"` **nur** in `components/portal/Zurueck.tsx` vorkommt |

| V-110 | **Wer ausgeschieden ist, blieb erreichbar.** `app.darf_kontaktiert_werden` prüft Widerspruch, Werbewiderspruch, Archivierung, Anonymisierung, den Einwilligungskanal und die ganze Kundenseite — aber **nicht**, ob die Person das Unternehmen verlassen hat. Eine Einwilligung gehört der Person, nicht dem Stuhl; und vertragliche Post an eine ausgeschiedene Person liest ihr Nachfolger (Art. 5 Abs. 1 lit. d DSGVO). | `grep -n ausgeschieden_am` → nur die Spalte in `drizzle/0020`, in keiner Bedingung. Gefunden, weil ein Test es behauptete und nachmass | blockiert | **erledigt** — `drizzle/0376`, beide Zweige |
| V-234 | **`?fehler=__proto__` legte jede Portalseite mit Fehlerrückweg lahm** — 45 Dateien schlugen den Grund aus der Adresse als `TABELLE[fehler] ?? Rückfall` nach; ein Objektliteral erbt von `Object.prototype`, also fanden `__proto__` und `toString` ein Objekt bzw. eine Funktion, der Rückfall griff nicht, und React antwortete mit 500. Darunter das Leadblatt aus V-137 | Gefunden bei der Gegenprüfung von V-159 (derselbe Fehler auf den Karriereseiten), per `grep` über den ganzen Quellbaum | falsch | **erledigt** (D-728) — 51 Stellen auf `eigenerEintrag()` (`src/lib/nachschlagen.ts`) umgestellt; `tests/kern/nachschlagen.test.ts` liest jetzt den ganzen Quellbaum und schlägt bei jedem neuen `[fehler]`/`[grund]`-Nachschlagen fehl |

| V-162 | **37 schreibende Routen beantworteten ein fehlendes Recht mit 500** — `authorize` wirft `NichtGefundenFehler` (Recht fehlt, fremder Mandant), `ZweiterFaktorFehler` oder `NichtAngemeldetFehler`; `server/auth/antwort.ts` übersetzt sie, aber diese Routen fingen im `catch` nur ihre Fachklasse und warfen den Rest weiter: `agenten/budget`, `akquise/ziel`, `angebot/entwurf`, `angebot/von-hand`, `barrierefreiheit/erledigen`, `bau/projekte`, `crm/ansprechpartner/[id]/rechtsgrundlage`, `crm/ansprechpartner/[id]/widerspruch`, `crm/kunde`, `crm/kunde/konditionen`, `crm/kunde/steuer`, `crm/kunde/zugang`, `crm/lead`, `crm/nachrichten`, `crm/wiedervorlage`, `datenschutz/{aufnehmen,bearbeiten,berichtigung,loeschung,widerspruch,zuordnen}`, `dienstplan/einsatz`, `dienstplan/serien/[id]`, `finanzen/ausgaben`, `finanzen/lieferanten`, `konto/verwaltung`, `objekt`, `personal/{nachweise,urlaubsanspruch,zugang}`, `recruiting/antwort`, `reinigung/reviere`, `security/veranstaltungen`, `system/modelle`, `system/verwaltungskonto`, `website/galerie`, `website/seite`. 500 heisst „hier ist etwas“ — genau das Orakel, das AUT-06 verbietet; `routen.test.ts` prüfte nur, ob `authorize` gerufen wird | Gefunden vom Prüf-Workflow (quer-formulare), im Code gegengeprüft (`reinigung/reviere/route.ts:117`, `objekt/route.ts:112`, `crm/kunde/route.ts:200`; Gegenbeleg `website/referenzen/route.ts:60`) | falsch | **erledigt** (D-656) — jede der 37 Routen ruft `autorisierungsAntwort(fehler)` vor ihrem `throw fehler`: 404 für Recht und Mandant, 403 für den fehlenden Faktor, 401 ohne Sitzung, byte-gleich mit allen übrigen Routen; Fachfehler behalten ihren Weg zurück aufs Formular. Sperrklinke: `tests/kern/autorisierung-uebersetzt.test.ts` liest jede `route.ts` und verlangt die Übersetzung, wo `authorize` gerufen wird (selbst oder im Gerüst eine Ebene tief), mit Gegenprobe an erfundenen Quelltexten und am Übersetzer selbst (gleicher Körper für „Recht fehlt“ und „fremder Mandant“). Prüfungen: `tests/kern/autorisierung-uebersetzt.test.ts` |

| V-163 | **Das Prüfprotokoll hatte keine IP und keinen Agenten** — SEC-A9 verlangt je Eintrag Akteur (Mensch/Agent/System), Handlung, vorher/nachher, Zeitpunkt und IP. `app.protokolliere` las `app.ip`, aber keine Sitzungsbindung setzte die GUC: jede `audit_log`-Zeile trug `ip = NULL`, die Spalte `ip` im GoBD-/Revisionsexport war immer leer. `agent_id` schrieb niemand, und kein Kontext setzte `akteur_typ = 'agent'` — was ein Agentenlauf anlegte, stand als `mensch` mit der Kennung dessen, der den Knopf gedrückt hatte. Dazu hätte ein unbrauchbarer Wert in `app.ip` jede protokollierende Transaktion abgebrochen (nackter `::inet`-Cast) | Gefunden vom Prüf-Workflow (spec-08), im Code gegengeprüft (`0004:126`, `kontext/index.ts:55`, `grep app.ip src` leer) | falsch | **erledigt** (D-657) — `aktuelleSitzung` trägt die IP der Anfrage (`anfrageAdresse`, `isIP`-geprüft), `bindeSitzung` setzt `app.ip` und leert `app.agent_id`; `drizzle/0415` liest IP und `agent_id` (nur zu `akteur_typ = agent`) über `pg_input_is_valid` — ein kaputter Wert wird NULL statt eines Abbruchs; `alsAgent` stellt den Orchestratorlauf auf `agent` um und danach zurück; die Protokollseiten nennen bei Agentenzeilen den Agenten. Die öffentliche Formularannahme schreibt bewusst keine rohe IP (LEG-09). Prüfungen: `tests/isolation/pruefprotokoll-ip-agent.test.ts`, `tests/kern/anfrage-adresse.test.ts`. **Offen:** Speicherdauer der IP im Protokoll — O-92 |

| V-164 | **Die Module einer Administration ließen sich nirgends zuweisen** — AUT-01 verlangt „Admin — assigned modules within assigned areas“. `benutzer_mandant.module` wirkt seit 0008 als Schnittmenge in `app.hat_recht_fuer`, aber keine Funktion, Route, Oberfläche und auch der Seed schrieb die Spalte; jede Administration hielt alle Module ihrer Rolle. Den Prüfauslöser aus 03-AUTH §7.1 gab es nicht, und `t_bm_entziehen` (0102) ließ jede Kontoverwaltung die Spalte direkt ändern — auch die eigene. Die Benutzerseiten zeigten die Liste nur, als rohe Schlüssel | Gefunden vom Prüf-Workflow (spec-02), im Code gegengeprüft (`0007:189`, `0395` Schnittmenge, `0372:275` Insert ohne `module`, `grep p_module` leer, `benutzer/[id]/page.tsx:193` nur Anzeige) | blockiert | **erledigt** (D-658) — `drizzle/0416`: `app.mitgliedschaft_module_setzen` (Definer, `cse_definer`; aktiver Mandant, kein Lesemodus, `aal2`, `system.module_zuweisen`, nur Plattformrolle `admin`, nie das eigene Konto, nie mehr als die eigenen Module; Protokoll mit vorher/nachher), Auslöser `kern.bm_module_pruefen` (Katalog, nie leer, sortiert ohne Doppel, kein Direktschreiben aus `cse_app`) und Policy `d_bm_module_setzen`; Route `/api/einstellungen/mitgliedschaft-module` (Formular, Rückmeldung als Satz auf dem Blatt); Formular auf dem Benutzerblatt, Übersicht „Module der Administrationen“ auf `einstellungen/module`, Modulnamen de/en statt Schlüssel auch in der Benutzerliste; Seed-Konto `admin.vertrieb@cse-gruppe.de` mit sieben Modulen. Prüfungen: `tests/isolation/mitgliedschaft-module.test.ts` (Schnittmenge wirkt, Normalform, Protokoll, unbekanntes Modul, leere Liste, Rolle, fremde Gesellschaft, ohne Recht/Faktor/Gruppenansicht, eigenes Konto, Teilmenge, globale Rolle, Direkt-UPDATE gesperrt, echter Seed), `tests/kern/modul-namen.test.ts` |

| V-165 | **Der Bereichswechsel erfüllte TEN-06 und TEN-10 im echten Portal nicht** — `PortalRahmen` trug oben links ein statisches Zeichen und für JEDE Sitzung den Verweis „Bereich wechseln“ (Kopfzeile, Telefonmenü, `Mehr`-Blatt), auch bei einem Bereich (gegen TEN-06, D-43). `/auth/bereich` zeigte keine Zähler, der Gruppeneintrag keine `NUR LESEN`-Pille und fest „vier“ Gesellschaften. Das Klappmenü mit Zählern, Pille und ⌘K (`BereichsUmschalter`) lebte nur in `/dev/portal` mit Festwerten (24/8/12); eine Zählerquelle (`app.mandant_kennzahlen`) gab es nicht, und ROADMAP Phase 1 hakte es trotzdem ab | Gefunden vom Prüf-Workflow (spec-01), im Code gegengeprüft (`PortalRahmen.tsx:312/377` Verweis ohne Bedingung, `grep BereichsUmschalter src` nur `PortalShell`, `grep mandant_kennzahlen src drizzle` leer, `auth/bereich/page.tsx:153/176`) | falsch | **erledigt** (D-659) — `drizzle/0417`: `app.mandant_kennzahlen()` (nur Anzahlen, nur Bereiche aus `switcher_mandanten`, nur mit dem Leserecht des Betrachters je Bereich, live über vorhandene Indizes) und `app.umschalter_bereiche()` (Bereiche mit gebuchten Gewerken), beide `cse_definer` mit eigenen d-Policies in gehobener Form (InitPlan); `umschalterStand` im Tor (gleiche Transaktion) und auf den fünf Kontoseiten, die am Tor vorbeigehen (`leseKonto` → `merkeUmschalter`) → Anfragespeicher → `kopfWechsel` im `PortalRahmen`: bei einem Bereich weder Umschalter noch Verweis (Kopfzeile, Telefonmenü, `Mehr`-Blatt, Satz auf der Kontowurzel), bei mehreren im internen Portal und in der Gruppenansicht der Umschalter oben links an der Stelle des Zeichens (`BereichsWechsel`: POST an `/api/sitzung/mandant`, ⌘K, Unterzeile „Gewerk · Zähler“, `NUR LESEN` am Gruppeneintrag; Name ab `lg`, gekürzt statt geschoben; auf der Übersicht kein zweites Logo mit demselben Namen), de/en; `/auth/bereich` mit Zählern, Pille, wirklicher Anzahl, de/en; ROADMAP-Zeile ergänzt, 01-KERN §6.3 und 05-API-KARTE mit dem gebauten Stand. Prüfungen: `tests/isolation/bereichswechsel.test.ts` (Zähler = direkte Zählung am echten Seed, Rechte je Bereich inkl. Modulliste und `aal1`, Gruppenansicht, Mitarbeiterin mit zwei Bereichen, Gewerke/`NULL`, Eigentümer und Grants, eigene Policies tragen allein mit Gegenprobe), `tests/kern/bereichswechsel.test.ts` (Zählerwahl, Einzahl/Mehrzahl/deutsche Zahlen, `kopfWechsel` für jede Leiste und Bereichszahl, Auslösername, jeder Verweis unter seiner Bedingung, Kontoseiten, POST statt GET), `tests/kern/portal-shell.test.ts` (D-43 unverändert grün); `tests/e2e/bereichswechsel.spec.ts`, `portal-ausgang.spec.ts` und `portal.spec.ts` im Browser grün (28 von 28, gegen einen frisch geseedeten Produktionsbau); die Kopfzeile nachgemessen bei 360, 390, 768, 1024, 1280 und 1440 px — keine Überbreite, der Auslöser auf der Kante des früheren Logos, Name ab `lg`, auf der Übersicht kein zweites Logo |

| V-166 | **Ein Kundenkonto sah auf der Bereichswahl den Auftragsbestand jeder Gesellschaft über alle Kunden** — `/auth/bereich` fragte `umschalterStand` ohne Angabe, „mit Zählern“ war die Vorgabe, und `app.mandant_kennzahlen` (0417) fragte nur `app.hat_recht`. Die Rolle `kunde` hält `auftrag.lesen` und `bau.lesen` plattformweit; auf die eigenen Zeilen beschränkt sie allein die RLS (`p_kunde_decke`, `p_portal_decke`), die die Definer-Zählung nicht sieht. Ein Kunde mit zwei Gesellschaften (0249) las dort „Reinigung · 57 laufende Aufträge“. Der abgebrochene Nachbesserungsstand rechnete das Portal je Bereich zudem mit dem Gültigkeitsfenster und zählte für eine globale interne Rolle mit abgelaufener Kundenzeile den ganzen Bestand | Gefunden vom unabhängigen Prüfer der Gruppe (blockierend; Befund 7 für Kundenkonten unerledigt), im Code gegengeprüft (`auth/bereich/page.tsx:57`, `umschalter.ts`, `0417:124/132`, `0008:381/398`, `0025:208`, `0071:596`); die Fensterlücke des WIP gefunden bei dessen Durchsicht und mit einer Gegenprobe gegen die WIP-Fassung belegt | falsch | **erledigt** (D-660) — `umschalterStand` verlangt `StandFragen { gruppe, zaehler }` ausdrücklich; Tor und Kontoseiten fragen beides nur für interne Leisten, die Bereichswahl die Zähler nur mit `zaehltFuer` (internes Portal oder Gruppenansicht); `drizzle/0418`: `app.mandant_kennzahlen` zählt nur in einer internen Sitzung (`app.portal()`) und je Bereich nur, wo das Portal nach einem Wechsel intern wäre (wie `app.sitzung_aufloesen`, ohne Fenster), mit eigenen Policies `d_umschalter_rolle`/`d_umschalter_benutzer`; 05-API-KARTE, 01-KERN §6.3 und ROADMAP nachgezogen. Prüfungen: `tests/isolation/bereichswechsel.test.ts` §5 (Kundenkonto in zwei Gesellschaften: keine Zahl, kein Gruppeneintrag, auch nicht auf direkte Frage, in Mandanten- und Kundensicht; Gegenprobe: die RLS zeigt ihm 0, die Gesellschaft hat mehr) und §6 (intern + Kunde: nur der interne Bereich; dieselbe Anmeldung als Kundensitzung: gar nichts; globale Rolle + abgelaufene Kundenzeile: dort keine Zahl, `sitzung_aufloesen` ergibt dort `kunde`; Mitarbeiterrolle mit Leserecht: keine Zahl), §4 um `rolle`/`benutzer` erweitert; `tests/kern/bereichswechsel.test.ts` §5 (`zaehltFuer` je Portal und Ansicht, `umschalterStand` fragt `mandant_kennzahlen` nur auf Verlangen). Nachtrag: die Rollenpolicy aus dem abgebrochenen Stand (`d_umschalter_rolle` mit Unterabfrage auf `benutzer_mandant`) brach jedes Anlegen einer Mitgliedschaft als `cse_definer` mit einer Policy-Rekursion ab — gefunden von `verwaltungskonto-einladung.test.ts`, mit der alten Fassung nachgestellt; jetzt ohne Tabellenabfrage, geprüft in §4 (kein `FROM` in den eigenen Policies) und §7 (Kundenmitgliedschaft als `cse_definer`) |

| V-167 | **Drei Protokollzeilen ohne Sitzung trugen keine IP, obwohl die Adresse bekannt war** — `auth.konto_gesperrt` (Bremse, `app.versuch_protokollieren`, `p_ip` liegt dort vor), `auth.kennwort_gesetzt` (`app.kennwort_token_einloesen`, Einladung und Zurücksetzung) und `auth.zweiter_faktor_eingerichtet` (`app.token_faktor_bestaetigen`, Einladung mit Faktorpflicht). `app.protokolliere` liest `app.ip`, und das setzte nur `bindeSitzung`; D-657 Nr. 1 behauptete, nur Läufe ohne Anfrage blieben leer. Dazu stand in `server/auth/adresse.ts`, der Proxy hänge die Geräteadresse VORN an — ein gewöhnlicher Reverse-Proxy hängt hinten an | Gefunden vom unabhängigen Prüfer der Gruppe (Befund 56 teilweise unerledigt; Proxy-Kommentar als kleiner Punkt), im Code gegengeprüft (`0379:139`, `0155:608`, `0155:693`, `grep app.ip src` nur `bindeSitzung`) | falsch | **erledigt** (D-661) — `bindeHerkunft` (`server/kontext`) setzt nur `app.ip`, transaktionslokal; `meldeAnMitKennwort`, `loeseKennwortTokenEin` und `bestaetigeFaktorMitToken` binden die Adresse selbst, `ip` ist Pflichtangabe; `/auth/passwort-neu` und der Token-Weg von `/auth/zwei-faktor/einrichten` reichen `anfrageAdresse` durch; der Kommentar in `adresse.ts` nennt die wirkliche Voraussetzung (Vercel setzt den Kopf; ein eigener Proxy muss ersetzen, nicht anhängen); D-657 Nr. 1 mit Verweis berichtigt. Prüfungen: `tests/isolation/pruefprotokoll-ip-agent.test.ts` §3 (Sperre nach zehn Fehlversuchen, Kennwort per Zurücksetzungstoken, Einladung mit Kennwort und zweitem Faktor — jede Zeile mit der Adresse der Anfrage, ohne Konto als Akteur; ohne Adresse leer und ohne Abbruch), `tests/isolation/anmeldung-kennwort.test.ts` unverändert grün |

| V-168 | **Die Modulzuweisung hielt nur an der Spalte, nicht an der Zeile — und drei Folgen von V-164 standen schief** — `kern.bm_module_pruefen` (0416) sperrte den Anwendungsweg nur beim Setzen einer Liste; mit `system.benutzer_verwalten` ergab entziehen + ohne Liste neu anlegen, eine entzogene Administration wiederbeleben oder eine Leitung umwidmen eine Administration mit allen Modulen, ohne `system.module_zuweisen` und ohne die Decke der eigenen Module. Die Prüfung des INSERT-Zweigs legte für eine fremde Gesellschaft an und erwartete einen Wurf ohne Meldung. Fünf Seed-Stellen wählten ihr Konto mit `order by r.schluessel limit 1`, obwohl die Reinigung seit V-164 zwei Administrationen trägt. Das Benutzerblatt schlug `?module=`/`?konto=` im Objektliteral nach (`?module=__proto__` warf beim Zeichnen) und gab einen fremden `?konto=`-Wert roh aus | Gefunden vom unabhängigen Prüfer der Gruppe (vier kleine Punkte zu Befund 14), im Code gegengeprüft (`0416` Auslöser nur `update of module`, `t_bm_entziehen`/`t_bm_schreiben`, `mitgliedschaft-module.test.ts:300`, `zeit.ts:77`, `reinigung.ts:490/941`, `benutzer/[id]/page.tsx:112`) | falsch | **erledigt** (D-662) — `drizzle/0419`: `kern.bm_administration_pruefen` weist auf dem Anwendungsweg jedes Anlegen, Wiederbeleben und Umwidmen einer lebenden Plattform-Administration ab (Entziehen und Pflege bleiben; Einladung und Modulzuweisung laufen als `cse_definer`, Seed als Eigentümer); Seed ordnet an sieben Stellen `r.schluessel, bm.module is not null, b.email`; Benutzerblatt über `eigenerEintrag`, `?anzahl=` nur als Zahl. Prüfungen: `tests/isolation/mitgliedschaft-module.test.ts` §2 (INSERT mit Liste im aktiven Mandanten gegen die Meldung, Gegenprobe ohne Liste) und §4 (entziehen + neu anlegen zurückgerollt und die Beschränkung steht, Wiederbeleben, Umwidmen, direktes Anlegen auch für die Plattformverwaltung abgewiesen; Entziehen und Pflege gehen; der Eigentümer legt weiter an), `tests/kern/nachschlagen.test.ts` (Benutzerblatt, beide Tabellen ohne Prototypschlüssel), Seed über den Vorlagenbau der Isolationssuite |

| V-169 | **Bildschirmtexte nannten Entwurfskennungen, und der ⌘K-Listener meldete sich bei jedem Rendern neu an** — die Gruppenzeile der Bereichswahl endete auf „(Invariante 10)“ / „(invariant 10)“, die Erklärung der Modulzuweisung auf „(AUT-01)“; `BereichsUmschalter` hängte den globalen Tastenlistener an `[aktiv, zeilen]`, und `zeilen` entsteht je Rendern neu | Gefunden vom unabhängigen Prüfer der Gruppe (zwei kleine Punkte zu Befund 7 und 14), im Code gegengeprüft (`bereichswechsel.ts:61/85`, `module-zuweisung.ts:114/151`, `BereichsUmschalter.tsx` Effekt mit `[aktiv, zeilen]`) | Schönheit | **erledigt** (D-663) — Verweise aus beiden Texttabellen entfernt, die Aussage bleibt; der Listener hängt an `startFokus`, einer Zahl. Bewusst offen (D-663 Nr. 3): ältere Seiten mit derselben Sorte Verweis, die deutsch gebliebenen Köpfe auf Benutzerliste und -blatt (eingefrorene Übersetzungsausnahme; die Abhilfe ist die ganze Umstellung beider Seiten), `/auth/bereich` ohne ar/tr. Prüfungen: `tests/kern/bereichswechsel.test.ts` §6 (jede Zeichenkette beider Tabellen in beiden Sprachen ohne Entwurfskennung, die Gruppenzeile wörtlich, der Listener an `startFokus` und nicht an `[aktiv, zeilen]`) |

| V-235 | **Check-in und Einmalcode schrieben ihre Protokollzeilen ohne IP, obwohl die Adresse vorlag** — `/api/check-in/[token]` (auch `…/offline`, `…/medien`) reicht die Adresse als `p_ip` an `app.checkin_verbrauchen`/`app.offline_ereignis_annehmen`; dort steht sie in `checkin_token` und im `nachher`-JSON, aber `app.protokolliere` liest `app.ip`, und `withCheckin` setzte nur die Rolle. `zeiteintrag.insert`/`.update`, `zeit.eingestempelt`/`.ausgestempelt`, `zeit.offline_empfangen` trugen `ip = NULL`; ebenso `mitarbeiter_zugang.update` aus `app.zugang_code_einloesen` (Einmalcode der Kraft). D-661 Nr. 7 und der Kommentar an `bindeHerkunft` („Drei Wege …“) behaupteten das Gegenteil; kein Test deckte den Check-in ab | Gefunden vom unabhängigen Prüfer der Gruppe (Befund 56 teilweise offen: `check-in/[token]/route.ts:71/113`, `kontext/checkin.ts:32`, `0035:696/746`, `0034:965`, `0090:363`), im Code gegengeprüft; den Einmalcode fand die Nachzählung aller Transaktionen ohne Sitzungsbindung unter `src/app` (`0114:149`, Auslöser `0384:255`) | falsch | **erledigt** (D-729) — `withCheckin(tx, ip, fn)` bindet `app.ip` über `bindeHerkunft` vor dem Rollenwechsel; `loeseCheckinEin`, `nimmClaimAn` und `codeEinloesen` nehmen die Adresse als Pflichtangabe; die drei Check-in-Routen lesen sie mit `anfrageAdresse` (ein ungültiger Kopf erreicht den `inet`-Parameter nicht mehr), die Stempeluhr reicht `sitzung.ip` durch; D-661 Nr. 7 und D-657 Nr. 1 mit Verweis berichtigt, Kommentar an `bindeHerkunft` neu. Prüfungen: `tests/isolation/pruefprotokoll-ip-agent.test.ts` §4 (Ein- und Ausstempeln per Marke, Nachreichung, Stempeluhr aus der Sitzung, Einmalcode — jede Zeile mit der Adresse; ohne die Bindung rot gesehen), `tests/kern/mitarbeiter-anmeldung.test.ts` (die Adresse wird vor der Einlösung gebunden) |

| V-236 | **Die zweite Linie hielt an zwei Spalten, nicht an der Zeile — eine Administration entstand über Konto oder Fenster** — `kern.bm_administration_pruefen` (0419) hing an `update of rolle_id, entzogen_am`, `cse_app` hielt ein UPDATE auf ganz `benutzer_mandant` (0007), `t_bm_entziehen` (0102) schränkt keine Spalte ein. Mit `system.benutzer_verwalten` und aal2: `set benutzer_id = X` an einer lebenden Administration ohne Liste machte X zur unbeschränkten Administration; `set gueltig_bis = null` an einer abgelaufenen ließ sie wieder gelten (`app.hat_recht_fuer` prüft das Fenster, 0395). D-662 Nr. 1 war damit nicht vollständig wahr; die Prüfung erlaubte die `gueltig_bis`-Pflege, ohne die Umwege zu prüfen | Gefunden vom unabhängigen Prüfer der Gruppe (Zweite Linie teilweise offen: `0419:88`, `0416:102`, `0007:617/713`, `0102:40`, `0395:101`, `mitgliedschaft-module.test.ts:449`), im Code gegengeprüft | falsch | **erledigt** (D-730) — `drizzle/0460`: `cse_app` hält nur noch Spaltenrechte auf die Pflegespalten (kein `benutzer_id`, `mandant_id`, `id`, keine Spur); `kern.bm_administration_pruefen` hängt an jedem Update und lässt eine lebende Plattform-Administration nur durch, wenn sie vorher eine war — dasselbe Konto, dieselbe Gesellschaft, ein Fenster im alten (verkürzen ja, verlängern/öffnen/vorziehen nein; verlängern heißt entziehen und neu einladen); D-662 Nr. 1 und 2 mit Verweis berichtigt. Prüfungen: `tests/isolation/mitgliedschaft-module.test.ts` §4 (Kontotausch an Administration und Leitung am Spaltenrecht abgewiesen; mit zurückgegebenem Spaltenrecht hält der Auslöser allein; abgelaufene Administration: `gueltig_bis = null`, spätere Frist, früherer Beginn abgewiesen und ohne Recht; lebende befristete: verlängern und vorziehen abgewiesen, verkürzen geht und das Recht bleibt) |

| V-237 | **Kleine Befunde der Prüfung von V-163 bis V-169** — (a) die Protokollzeile `system.module_zugewiesen` meldete `geaendert_felder = {benutzer_id, module}`, weil das Konto nur in nachher stand (0416); (b) von der Decke der eigenen Module war jedes Konto mit irgendeiner globalen Rolle ausgenommen, ohne Frage, ob das Recht aus ihr kommt (0416, latent); (c) `d_umschalter_mandant`/`_auftrag`/`_projekt`/`_rolle` fragen `app.switcher_mandanten()`, die `mandant` und `benutzer_mandant` liest — kreisfrei nur wegen des Eigentümers `postgres` und der breiten `using (true)`-Policies, ohne dass es irgendwo stand; (d) DESIGN §6 wich ohne Nachtrag ab: Klappmenü ohne Einblendung, Name im Auslöser erst ab `lg`, keine Zeile für Operations („Digital & KI“), „Security“ statt „Sicherheit“; (e) die Prüfung „was ein Agent schreibt“ lief über null Protokollzeilen und war trivial wahr; (f) `const uhr =(await …` in `kennwort-anmeldung.ts` | Gefunden vom unabhängigen Prüfer der Gruppe (kleine Punkte a–f: `0416:222`, `0415:97`), im Code gegengeprüft | Schönheit | **erledigt** (D-731) — (a) `drizzle/0461`: das Konto steht vorher und nachher, `geaendert_felder = {module}`; Prüfung `tests/isolation/mitgliedschaft-module.test.ts` §1. (b) `drizzle/0462`: ausgenommen ist nur, wessen globale Rolle `system.module_zuweisen` in der Gesellschaft selbst gewährt (dieselbe Suche wie Zweig 1 in `app.hat_recht_fuer`); Prüfung §2 mit einer zweiten globalen Rolle ohne das Recht (Decke gilt) und der Gegenprobe mit dem Recht (keine Decke). (c) festgenagelt statt umgebaut: `tests/isolation/bereichswechsel.test.ts` §4 prüft die Liste der fragenden Policies und dass `switcher_mandanten` und `ist_super_admin` an der RLS vorbei lesen; die Gegenprobe zieht die Funktion zurückgerollt zu `cse_definer` um und sieht „stack depth limit exceeded“. (d) Einblenden nach §6 mit den Tokens aus §7 (`.cse-klappmenue`, `--base`, `--ease`; §6 nannte 180 ms, kein Token); die drei übrigen Abweichungen sind gebaute Entscheidungen (D-659 Nr. 3 und 7, V-164) und stehen jetzt begründet in DESIGN §6, 01-KERN §6.2 berichtigt; Prüfung `tests/kern/bereichswechsel.test.ts` §7. (e) der Spion schreibt beim Anlegen der Freigabe eine echte Zeile über `app.protokolliere`; `tests/isolation/pruefprotokoll-ip-agent.test.ts` §2 prüft sie und jede Zeile zwischen Marken vor und nach dem Lauf (nie leer). (f) formatiert |

| V-202 | **Das Angebotsdokument schrieb den Steuersatz anders als das Kundenportal** — „Umsatzsteuer 19 %“ auf dem Dokument (`/angebote/[id]/pdf`), im Detailblatt und in der Preisfreigabe (`(bp / 100).toLocaleString()`), „19,0 %“ im Kundenportal für dasselbe Angebot (`prozentText`). D-629 hatte genau das für die Rechnung behoben: zwei Schreibweisen für einen Beleg | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`pdf/page.tsx:309`, `angebote/[id]/page.tsx:435/530`, `freigabe/page.tsx:223`) | schoenheit | **erledigt** (D-696, D-629) — alle vier Stellen setzen den Satz mit `prozentText` aus `finanz/prozent.ts`; keine Gleitkommateilung mehr. Nachgezogen mit V-204: Rechnungsblatt, Abschlags- und Festschreibungsseite der Ausgangsrechnung schrieben denselben Beleg als „19,00 %“ bzw. „19 %“ — jetzt ebenfalls `prozentText` (auch Bauabzugsteuer und Sicherheitseinbehalt). Prüfungen: `tests/kern/angebot-lebend.test.ts` (V-202: keine `_bp / 100` auf den Angebotsbelegen, „19,0 %“), `tests/kern/rechnung-entwurf.test.ts` (dasselbe für die drei Rechnungsseiten) |

| V-203 | **Entfernte Angebotspositionen standen weiter im Dokument, im Kundenportal und in der Preisfreigabe** — seit 0392 zählen `netto_cent` und `angebot_steuer` nur lebende Zeilen, aber das Dokument druckte die entfernte Zeile mit Preis (Netto darunter ohne sie), das Kundenportal zeigte und zählte sie, die Preisfreigabe summierte sie ins Netto je Satz neben einem Kopfbetrag ohne sie; Versandseite und Versanddienst zählten sie mit; die Kundenpolicy gab jede Zeile frei | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`pdf/page.tsx:160`, `kundenportal/angebot.ts:111/247`, `freigabe/page.tsx:83/114`, `versand/page.tsx:83`, `angebot/index.ts:320/413`, `0024:438`) | falsch | **erledigt** (D-696) — ein Leser `angebot/lebend.ts` (Dokument, Steuervorschau wie beim Versand, Zählung) für alle fünf Stellen, das Kundenportal filtert selbst; `drizzle/0440`: `t_kunde` verlangt `entfernt_am is null`. Prüfungen: `tests/kern/angebot-lebend.test.ts` (jede lesende Abfrage auf `angebotsposition` im Quellbaum kennt `entfernt_am`), `tests/isolation/angebotsentwurf.test.ts` §7 (Freigabe = eingefrorene Steuerzeilen, Dokument ergibt `netto_cent`, Kundenportal Liste und Positionen, Kunden-RLS ohne Dienst) |

| V-204 | **Der Kopf eines Rechnungsentwurfs war nach dem Anlegen nicht änderbar** — Leistungszeitraum, Zahlungsziel, Zahlungsart, Leistungsort und Texte liessen sich auf keinem Weg nachtragen; ein fehlender Zeitraum oder ein fehlendes Zahlungsziel sperrte den Entwurf dauerhaft, die Vorabprüfung verwies auf ein Blatt, das beides nur anzeigte, und einziger Ausweg war Verwerfen und Neuerfassen aller Positionen | Gefunden vom Prüf-Workflow (spec-06), im Code gegengeprüft (`api/rechnungen/route.ts`: nur `aus-zeiten`, `position`, `anlegen`; `rechnung.ts:167`; `ustg14.ts:527/660`; `rechnungen/[id]/page.tsx:475`) | behindert | **erledigt** (D-697) — `aendereEntwurfKopf` (`finanz/entwurf.ts`), Aktion `kopf`, Abschnitt „Kopf des Entwurfs“ (`#kopf`) mit vorbelegter Maske; nur Entwürfe (Sperre, Zustand, Policy); leeres Zahlungsziel wird neu aufgelöst; Steuersatz am neuen Stichtag geprüft, Summen und Steuerfall neu; Abzug einer Schlussrechnung fällt beim Wechsel von Art oder Auftrag; Vorabprüfung verweist auf `#kopf` (`alsVerweis`); Abweisungen kommen als Seite mit Eingaben zurück. Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §2, §4, `tests/kern/rechnung-entwurf.test.ts` |

| V-205 | **Ein Rechnungsentwurf liess sich keinem Auftrag und keiner Rechnungsart zuordnen** — jede Portalrechnung war `standard` ohne Auftrag: Abschlags- und Schlussrechnung unerreichbar, „Abschläge abziehen“ nie sichtbar, Zeitzeile und Vertragsherkunft nie angeboten, FIN-18 griff nie, die Prüfliste „Auftrag ohne Rechnung“ führte jeden abgeschlossenen Auftrag für immer; auch der Seed legte nur `standard` ohne Auftrag an | Gefunden vom Prüf-Workflow (spec-06), im Code gegengeprüft (`neu/page.tsx:121-208`, `api/rechnungen/route.ts:176-188`, `positionsquelle.ts:563-577`, `vorabpruefung.ts:200-208`, `seed/rechnung.ts:171`) | blockiert | **erledigt** (D-698) — Rechnungsart (ohne `storno`), Auftrag (nach Kunden gruppiert, `?auftrag=` vorbelegt) und Vereinnahmung beim Anlegen und im Kopf; `pruefeAuftragZuordnung` (derselbe Kunde, nicht storniert, sichtbar); kein Auftragswechsel unter Belegen (`zuordnung_gebunden`); „Rechnung anlegen“ am Auftragsblatt und an der Abrechnungsseite; Seed im Bau: Abschlag mit Vereinnahmung festgeschrieben, Schlussrechnung mit Abzug als Entwurf. Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §1, §3 (FIN-18, Prüfliste), §4 (Schlussrechnung mit Abzug festgeschrieben), Seed-Lauf gegen eine Wegwerf-Datenbank |

| V-206 | **Die fünf Abrechnungsarten rechneten nie, und Aufmaß, Einzelabruf und Material hatten keinen Weg auf eine Rechnung** — `berechneAbrechnung`/`bestueckeAusAbrechnungsart` hatten ausser den Tests keinen Aufrufer, die Abrechnungsseite verwies auf einen „Abrechnungslauf“, den es nicht gab, und `POST /api/rechnungen` kannte nur `vertrag` und `manuell`, obwohl Ausgabe- und Sonderleistungsseite die Abrechenbarkeit versprachen | Gefunden vom Prüf-Workflow (spec-06), im Code gegengeprüft (`abrechnungsart/index.ts:394/501`, `api/rechnungen/route.ts:139-144`, `ausgaben/[id]/page.tsx:369`, `sonderleistungen/page.tsx:590`, `auftraege/[id]/abrechnung/page.tsx:16`) | blockiert | **erledigt** (D-699, O-931) — Abschnitt „Nach Abrechnungsart übernehmen“ (`#abrechnungsart`): Vorschau mit derselben Funktion und den Befunden vor dem Knopf, Periode = Leistungszeitraum des Kopfes, Leistungszeile, Aufmaßblätter, Fertigstellungsgrad; blockierende Befunde schreiben nichts; je Entwurf einmal je Vereinbarung (Aufmaß: je Blatt); Herkunft ohne Kennung und mit deutschen Tagen; Herkunft „Material“ mit Ausgabe als Beleg (weiterberechenbar, freigegeben, einmal, passender Auftrag und Kunde), Preis von Hand bis O-931. Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §5, §6 |

| V-207 | **Dieselbe Abrechnungsvereinbarung ließ sich auf beliebig vielen Rechnungen abrechnen — und ein verworfener Entwurf gab nie frei** — die Sperre `schon_uebernommen` (V-206) fragte nur den einen Entwurf; Monatspauschale und Festpreis-Los schreiben ihre Herkunft als `manuell` ohne Teilindex, und keine Strategie las, was schon berechnet war: derselbe August auf jeder weiteren Rechnung, der volle Festpreis „nach Abnahme" auf jeder Rechnung ab der Abnahme, „anteilig" Grad × Festpreis ohne Abzug. Beim Gegenprüfen: `verwerfe()` rief `gibQuellenFrei()` nie (Ausgabe, Abruf und — neu — Monat blieben für immer beansprucht), und die Rücknahme eines Abzugs scheiterte an `fin.abschlag_pruefen`, sobald der Abschlag storniert war | Gefunden vom unabhängigen Prüfer der Gruppe rechnung (blockierend: `entwurf.ts:434/510-522`, `abrechnungsart/index.ts:455-462`, `monatspauschale.ts`, `festpreis-los.ts`), im Code gegengeprüft; das Verwerfen beim Test des Entwurfsanspruchs rot gesehen (`rechnung.ts` `verwerfe`, 0107 Z. 199) | blockiert | **erledigt** (D-700, O-932; D-699 Nr. 3 und 5 berichtigt) — `AbrechnungsEingabe.bisher` (Pflicht, geladen in `berechneMitKonfiguration`): jede Zeile derselben Vereinbarung mit wirksamer Herkunft, auch auf Entwürfen; Schlussrechnung ohne die festgeschriebenen Abschläge ihres Auftrags (FIN-08); `sperrtUeberBeleg` für Stunden, Abruf, Aufmaß. Monatspauschale blockiert einen schon beanspruchten Zeitraum (volle Pauschale = ganzer Monat), Festpreis nach Abnahme einmal, anteilig als Gesamtstand minus bisher (`anteiligerRest`, O-932); Sperre von Entwurf (`for update`) und Vereinbarung (Advisory-Lock); Rechnungsblatt zeigt „schon berechnet" mit Beleg; `verwerfe()` gibt Herkunft und Abzüge frei, `drizzle/0442` (enge Policies `t_verwerfen_gibt_frei`, Nachholung), `drizzle/0441` (reine Rücknahme eines Abzugs ungeprüft). Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §7 (August doppelt, Quartal mit August, Storno gibt frei, halbe Monate, Entwurf beansprucht und Verwerfen gibt frei — auch als Rolle nur mit `finanzen.entwurf_verwerfen`, Festpreis nach Abnahme, 30/60/100 % = Festpreis, Schlussrechnung mit zwei Abschlägen), §4 (stornierter Abschlag im Abzug, verworfener Schlussentwurf), `tests/kern/abrechnungsart.test.ts` (V-207) |

| V-208 | **Die Materialauswahl begrenzte, bevor sie prüfte — ältere offene Ausgaben fielen still heraus, und ein verdeckter Auftrag passte auf jeden Kunden** — `weiterberechenbareAusgaben` holte `limit 200` über alle weiterberechenbaren Ausgaben der Gesellschaft (weiterberechnete bleiben für immer darin) und filterte erst danach; ab der zweihundertsten verschwand die Herkunft „Material" für ältere Ausgaben ohne Hinweis. Kunde und Auftrag einer Ausgabe kamen unter RLS: ohne `auftrag.lesen` wurde `kunde_id` zu `null` und passte damit überall. Dazu wählte das Positionsformular ohne Leistungszeilen „Material" und die erste Ausgabe vor, die Begründung war kein Pflichtfeld mehr | Gefunden vom unabhängigen Prüfer der Gruppe rechnung (blockierend: `entwurf.ts:622-641`, `page.tsx:448-451`; klein: `entwurf.ts:587-607`, `page.tsx:1531/1606`), im Code gegengeprüft | blockiert | **erledigt** (D-701; D-699 Nr. 5 berichtigt) — Weiterberechnung, verdeckter Bezug, Auftrag und Kunde im `WHERE` (`PASST_ZUM_BELEG`, eine Fassung für Auswahl und `fuegeMaterialPositionHinzu`), erst danach `AUSGABEN_HOECHSTENS = 500` mit sichtbarem Hinweis, wenn erreicht; `bezug_verdeckt` (Auftrag, Projekt oder Objekt gesetzt, aber nicht lesbar) passt nie, die Auswahl zählt solche Ausgaben, die Übernahme weist mit Satz ab; „Material" nie vorgewählt (Vertragszeile, sonst „von Hand"), die Ausgabe ohne Vorwahl („— Ausgabe wählen —"). Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §6 (250 neuere fremde Ausgaben verdrängen die alte passende nicht; Rolle ohne `auftrag.lesen`: nicht angeboten, gezählt, abgewiesen) |

| V-209 | **Die kleinen Punkte der Gruppe rechnung** — (1) der Kopf ließ sich nach „Nach Abrechnungsart übernehmen" frei verschieben (Beleg September, Zeilen August); (2) `2026-02-30` passierte das Tagesmuster und endete am `::date` in einer 500; (3) ein Fertigstellungsgrad von 0 kam als „es fehlt eine Menge" zurück; (4) `aktion=kopf` ohne Rechnungsart machte still eine Standardrechnung; (5) die Abweisung stand oben, der Browser sprang zum Abschnitt darunter; (6) die Kopfmaske füllte ein bewusst geleertes Feld nach einer Abweisung wieder; (7) deutsche Texte ohne englische Fassung (Auftragsblatt, Abrechnungs- und Sonderleistungsseite, Abrechnungsart), Einheit der Vorschau als Schlüssel; (8) „(Invariante 4)" im Satz, der Einstellungsschlüssel in der Vorabprüfung, kein Satz für `nicht_uebernommen`; (9) „Rechnung anlegen" auf der Abrechnungsseite auch beim stornierten Auftrag; (10) der Leistungsort weder gegen Sichtbarkeit noch gegen den Kunden geprüft, die Auswahl zeigte alle Objekte. Die übrigen vier kleinen Punkte (Vorwahl „Material", verdeckter Bezug, Rücknahme eines Abzugs bei storniertem Abschlag, Sperre der Übernahme) sind mit V-207/V-208 erledigt | Gefunden vom unabhängigen Prüfer der Gruppe rechnung (KLEIN: `entwurf.ts:183-191`, `route.ts:71/189/194-200`, `page.tsx:637`, `auftraege/[id]/page.tsx:383`, `abrechnung/page.tsx:169`, `sonderleistungen/page.tsx`, `rechnung-entwurf.ts` `kein_entwurf`, `ustg14.ts` Zahlungsziel), jeder Punkt im Code gegengeprüft | behindert | **erledigt bis auf eine offene Frage** (D-702, O-933) — `zeitraum_gebunden` (erweitern ja, verschieben/leeren nein); `istKalendertag` in der Route; Grad 0 → `fertigstellung_ungueltig`; `kopf` ohne Art → `unvollstaendig`; Abweisung im Abschnitt ihrer Maske; Kopfmaske nimmt nach einer Abweisung nur die geschickten Werte; Texte in `rechnung-entwurf.ts` (de/en) inkl. Abrechnungsart mit Erläuterung und Einheit als Bezeichnung — die Befunde der Strategien bleiben deutsch und sind so beschriftet; Sätze ohne interne Kennungen; Status geprüft; `objekt_passt_nicht` (Sichtbarkeit) beim Anlegen und im Kopf, Objekte nach Kunden geordnet. **Offen gehalten:** ob der Leistungsort zum Kunden gehören muss (O-933, `OBJEKT_KUNDE_REGEL`). Prüfungen: `tests/isolation/rechnung-entwurf.test.ts` §2 (Objekt), §5 (Zeitraum gebunden, erweitern), `tests/isolation/rechnung.test.ts` (Zahlungsziel ohne Schlüssel), `tests/kern/rechnung-entwurf.test.ts` (Route, jede Abweisung mit Satz inkl. `nicht_uebernommen`, `zeitraum_gebunden`, `objekt_passt_nicht`) |
| V-210 | **Monatsnachweis und Unterschriftsblatt zeigten Kalendertage als `JJJJ-MM-TT`** — das druckbare Blatt des Monatsnachweises (EMP-06, `lang="de"`, das Blatt für den Lohnstreit) schrieb die Spalte „Tag“ als `2026-09-11`; das Unterschriftsblatt des Leistungsnachweises (CLN-04) zeigte den Leistungszeitraum als „2026-09-01 – 2026-09-30“, in der Verwaltung wie im Arbeiterportal; dieselbe Rohform stand beim Plantag im Kopf jeder Schichtseite (Schicht, Wachbuch, Fotos, Bautagebuch, Leistungsnachweis) und beim Zeitraum der Nachweisliste | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`monatsnachweis/page.tsx:337`, `leistungsnachweis.ts:179`, `unterschrift/page.tsx:123`, `mein/schichten/[zuordnungId]/leistungsnachweis/page.tsx:377`) | Schönheit | **erledigt** — nur die Anzeige: Monatsnachweis und Verwaltungs-Unterschriftsblatt über `tagDeutsch()` (beide Blätter sind deutsch), das Arbeiterportal über `tagInSprache(…, basis.sprache)` wie der Rest der Seite (deutsch/arabisch/türkisch `TT.MM.JJJJ`, englisch „30 Sep 2026“); Dienste, Schnappschuss und Hash behalten den ISO-Tag. `tests/kern/nachweis-datum.test.ts` liest die drei Blätter und alle Schichtseiten und schlägt bei einem rohen ISO-Tag in JSX fehl |

| V-211 | **DATEV-EXTF: ein Anführungszeichen an der Längengrenze liess das Feld offen, und die Stapelbezeichnung verlor ihr Enddatum** — `textFeld()` verdoppelte jedes `"` und schnitt DANACH auf die Höchstlänge; traf der Schnitt ein verdoppeltes Paar, endete das Feld auf `""` (ein maskiertes Zeichen statt des Feldendes), und der Rest der Zeile gehörte für DATEV zum Buchungstext (Beispiel: 59 Zeichen `RE … "Am Parks"`); jeder Text mit Anführungszeichen wurde still um deren Zahl gekürzt; die Kopfbezeichnung `Buchungsstapel 2026-08-01 bis 2026-08-31` (40 Zeichen) wurde auf 30 geschnitten | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`extf.ts:175-176`, `export.ts:180`) | falsch | **erledigt** — `textFeld()` kürzt den Inhalt (in Codepunkten) und maskiert danach; neue `stapelBezeichnung()` → `Stapel 01.08.2026-31.08.2026` (28 Zeichen), die Höchstlänge steht als `BEZEICHNUNG_HOECHSTENS` an einer Stelle. `tests/kern/datev-extf.test.ts` liest die Zeile mit einem RFC-4180-Leser: 59 Zeichen mit schliessendem Anführungszeichen ergeben drei Felder, ein Schnitt auf ein `"` lässt das Feld geschlossen, kein Zeichen geht verloren, die Bezeichnung passt ganz und nennt das Enddatum |

| V-212 | **Ein DATEV-Stapel durfte die Grenze des Wirtschaftsjahres überschreiten, obwohl das Belegdatum ohne Jahr geschrieben wird** — `belegdatumText` schreibt `TTMM`, der Kopf trägt nur den aus `von` abgeleiteten WJ-Beginn; weder Formular noch Route, Dienst oder Tabelle (nur `check (bis >= von)`) verhinderten 01.12.2025–31.01.2026, eine Buchung vom 15.01.2026 stand damit als 15.01.2025 im Stapel, und die Zeilen wurden trotzdem als exportiert gestempelt | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`extf.ts:141-146`, `export.ts:89-100/176`, `api/buchhaltung/datev/route.ts:53-58`, `0133:94`) | falsch | **erledigt** (D-704) — `pruefeEinWirtschaftsjahr` wirft VOR Paket und Stempel mit einem Satz, der die Grenze nennt (WJ aus den geprüften Stammdaten, O-05); die Vorschau sperrt den Knopf und nennt das Datum, wenn sie die Stammdaten sieht; Prüfbedingung `datev_export_ein_wirtschaftsjahr` (0445, `not valid`) als zweite Linie; die Route führt einen Browser bei jeder Abweisung auf die Vorschau zurück (D-599) und fängt Auth-Würfe mit `autorisierungsAntwort`. `tests/kern/datev-wirtschaftsjahr.test.ts` (Kalender-WJ, WJ ab 1. Juli, erster Tag), `tests/isolation/datev-export.test.ts` (2a): Dezember–Januar abgewiesen, nichts gestempelt, kein Stapel; abweichendes WJ aus den Stammdaten; die Tabelle weist direkt ab, auch mit WJ-Beginn 30.02. ohne Datumsfehler |

| V-213 | **Das Mahnschreiben war kein Geschäftsbrief** — das als Beleg für den Verzugsbeginn abgelegte PDF (`mahnungstext()`) hatte keinen Absender (Firma, Anschrift, Registergericht, HRB, Geschäftsführung, USt-IdNr.), vom Empfänger nur „Kunde: Name" ohne Postanschrift; Mahndatum, Fälligkeiten und Zahlungsfrist standen als `2026-09-23`, der Zins als „900 Basispunkte"; das abgelegte PDF war vom Mahnungsblatt nicht erreichbar (`dokument_id` ungelesen); die Rückmeldung nach dem Versand zeigte `versendet_am::text` roh in UTC | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`mahnung/index.ts:115/176/368-406/564`, `mahnungen/[id]/page.tsx:116/120/137`, `api/finanzen/mahnungen/route.ts:167`) | falsch | **erledigt** (D-705) — `KOPF_SQL` liest `mandant` und die (abweichende Rechnungs-)Anschrift; `mahnungstext` setzt Absenderzeile, Empfängeranschrift, Tage als `TT.MM.JJJJ`, Satz als Prozent p. a. und nach der Fusszeile die Pflichtangaben wie das Angebotsblatt; Absender und Empfänger stehen in der Nutzlast (Invariante 7); das Mahnungsblatt zeigt das Schreiben, nennt fehlende Briefkopfangaben (Sperre offen: O-934), verlinkt das abgelegte PDF über den signierten Abruf und zeigt Tage, Beträge und Satz in der Sprache der Sitzung (`prozentTextIn` neu); Versandzeit als Berliner Ortszeit. `tests/kern/mahnschreiben.test.ts`, `tests/isolation/mahnung.test.ts` (8): Briefkopf, Rechnungsanschrift, kein ISO-Tag, Anschriftänderung nach Freigabe sperrt den Versand, Versandzeit, `dokumentId` |

| V-214 | **Der Mahntext je Stufe war nirgends pflegbar und erreichte das Schreiben nie** — `mahnstufe.textbaustein` (0125) schrieb kein Weg (`bestaetigeStufe` ohne Feld, Formular und Route ohne Feld, Seed ohne Wert), die Vorlagenseite behauptete die Pflege unter Einstellungen › Mahnwesen und zeigte dauerhaft „nicht hinterlegt", und der Mahnungsdienst las die Spalte nicht | Gefunden vom Prüf-Workflow (quer-dokumente), im Code gegengeprüft (`stufen.ts:111-122/188-191`, `einstellungen/mahnwesen`, `vorlagen/page.tsx:174-213`, `mahnung/index.ts:113-125`) | behindert | **erledigt** (D-705) — `StufeEingabe.textbaustein` (Text setzt, `null` entfernt, weggelassen übernimmt den der laufenden Fassung; höchstens 1000 Zeichen); Feld „Mahntext dieser Stufe" und „ohne Mahntext" im Stufenformular, die Route reicht beides durch und bringt bei einer Abweisung die Eingaben zurück (D-599); die Mahnung liest den Text der Fassung, auf die sie zeigt, `mahnungstext` setzt ihn zwischen Datum und Forderungsliste, die Nutzlast bindet ihn; Spalte „Mahntext" in der Stufenliste; Seed mit beschriftetem PLATZHALTER (O-19). `tests/isolation/mahnung.test.ts` (7), `tests/kern/mahnschreiben.test.ts` |

| V-215 | **Betriebsausgaben fehlten in Aufwand und Ergebnis und im Z3-Paket** — seit V-011 werden Ausgaben gebucht, aber Monatszahlen („Aufwand netto"), Saldo je Gesellschaft und Gruppe, die beim Periodenschluss eingefrorenen Zahlen und die Monatstabelle des Jahrespakets lasen nur `eingangsrechnung`; das Ergebnis war um jede gebuchte Ausgabe zu hoch; Z3 ohne Tabelle `ausgaben`, Journal ohne `ausgabe_id`, Herkunftstext ohne `ausgabe`/`kassenbewegung` | Gefunden vom Prüf-Workflow (spec-06), im Code gegengeprüft (`monatszahlen.ts:102-118`, `gruppe/finanzen.ts:117-122`, `finanzen/page.tsx:117-121`, `periodenschluss.ts:107-118`, `jahrespaket.ts:117/196`, `z3.ts:128-600`) | falsch | **erledigt** (D-706) — `app.ausgaben_aufwand` (0446, Definer, nur Summen, Gruppe einschliesslich Erstattungen) als eine Quelle; Monatszahlen mit Aufwand = Eingang + Ausgaben und getrennten Spalten samt Verweis auf `/finanzen/ausgaben?monat=` (neuer Monatsfilter); Periodenschluss friert damit ein; Gruppe mit Ausgaben je Gesellschaft und Monat, Saldo und Ergebnis aus dem Aufwand; Finanzübersicht mit Kachel; Jahrespaket mit drei neuen Spalten am Ende; Z3 mit `ausgaben`, `ausgabensteuer`, `buchungen.ausgabe_id` und vollständigem Herkunftstext; D-484 ergänzt. Tests: `tests/kern/gruppe-finanzen.test.ts`, `tests/isolation/buchhaltung-posten-monate.test.ts` (5: Zustände, keine Doppelzählung, Gruppe = Summe mit Erstattung, nur aktiver Mandant, Periodenschluss), `tests/isolation/z3-verfahrensdokumentation.test.ts`, `tests/isolation/jahrespaket-lohnexport.test.ts` |

| V-216 | **Zahlungsausgang an Lieferanten war nicht erfassbar: Kreditorposten blieben für immer offen** — das Buchen einer Eingangsrechnung eröffnet einen Kreditorposten (0123), aber weder die Zahlungsroute (nur `verbucheZahlungseingang`), noch der Bankabgleich (`schlageVor` → „kein Treffer", `bestaetigeZuordnung` nur `eingang` mit Join auf `rechnung`), noch die Eingangsrechnungsseite boten einen Weg; Offene Posten, Altersstruktur, Gruppensumme „Kreditoren offen" und Jahrespaket wiesen jede gebuchte Eingangsrechnung dauerhaft als unbezahlt aus | Gefunden vom Prüf-Workflow (spec-06), im Code gegengeprüft (`api/finanzen/zahlungen/route.ts:83-181`, `zahlung/index.ts:367`, `abgleich.ts:97-107`, `import.ts:407-420`, `eingangsrechnungen/[id]/page.tsx`) | blockiert | **erledigt** (D-707) — `verbucheZahlungsausgang` (Zahlung `ausgang`, Anrechnung auf den Kreditorposten, Überzahlung als `kreditor_guthaben` über `fin.op_kreditor_guthaben_eroeffnen`, 0447); Formular „Zahlung an den Lieferanten erfassen" samt Zahlungsliste auf der gebuchten Eingangsrechnung, Route `aktion=ausgang` mit Rückweg als Seite (D-599); Bankabgleich schlägt für Ausgänge die Verbindlichkeiten vor (`schlageVorAusgang`, nie automatisch), die Klärung bucht sie über denselben Dienst; Zahlungsakte nennt Richtung und Lieferantenbeleg. Die Richtungsprüfung besteht seit 0130 und greift auch hier. Tests: `tests/isolation/zahlungsausgang.test.ts` (ganz, teilweise, Überzahlung mit Protokoll, kein Posten, keine Doppelzahlung, Richtung, Storno, Bank-Vorschlag und -Klärung, kein Ausgang auf eine Ausgangsrechnung), `tests/kern/bank-abgleich.test.ts` (7) |

| V-217 | **Nachbesserung der Gruppe buchhaltung (Prüfung von V-210 bis V-216)** — (a) die WJ-Prüfung des DATEV-Stapels (0445) war eine Prüfbedingung mit `not valid`; PostgreSQL prüft sie trotzdem bei jedem `update`, ein vor 0445 über die WJ-Grenze erzeugter Stapel liess sich deshalb weder verwerfen noch als übergeben vermerken (`23514` → 500) und blieb für immer „erzeugt"; (b) das Mahnschreiben las Briefkopf und Anschrift live, die Freigabe band sie, und aus `freigegeben` führt kein Weg zurück — jede Pflege der Kundenanschrift oder der Unternehmensdaten nach der Freigabe liess die Mahnung für immer stehen und sperrte ihre Posten; (c) die kleinen Punkte: Platzhaltertext wurde in die erste echte Stufenfassung übernommen, Auth-Würfe zweier Routen als 500, ISO-Tage und Zinsschlüssel in der Stufenpflege, `2026-02-31` und ein fremdes Bankkonto im Zahlungsausgang als 500, `abgewiesen` behauptete immer „Betrag null", verlinkte Ausgabenliste passte nicht zur Spalte, nachgebaute Warnkästen, ein trivialer Testsatz, ISO-Tage im Kontoauszug, deutsche Beträge in zweisprachiger Zahlungstabelle; (d) das Kontoauszugsblatt samt der neuen Ausgangsmaske war nur deutsch und zeigte bei einer Abweisung den deutschen Satz des Dienstes, so wie er als `?meldung=` in der Adresse stand | Gefunden vom Prüf-Workflow (Prüfung der Gruppe buchhaltung), im Code gegengeprüft (`0445:40-50`, `datev/stapel.ts:112/154`, `api/buchhaltung/datev/[id]/stand/route.ts:47-50`; `mahnung/index.ts` KOPF_SQL/Nutzlast/`verwirf`, `0130:420-426`, `0125:826-832`, `tests/isolation/mahnung.test.ts` (8)) | blockiert | **erledigt** (D-708, D-709, D-710) — (a) 0448 ersetzt die Bedingung durch den Auslöser `datev_export_ein_wirtschaftsjahr` (`before insert`, dieselbe Rechnung, `23514` mit Bedingungsnamen); Zeitraum und WJ-Beginn sind seit 0133 unveränderlich, ein `update`-Auslöser ist nicht nötig. `tests/isolation/datev-stapel.test.ts` §7: ein alter Stapel über die Grenze lässt sich verwerfen und vermerken, ein neuer wird mit `23514`/`datev_export_ein_wirtschaftsjahr` abgewiesen, der Zeitraum bleibt unveränderlich; `datev-export.test.ts` (2a) unverändert grün; (b) 0449: `mahnung.brief` — `gibFrei` friert Kundenname, Stufenbezeichnung, Absender, Empfänger, Mahntext und Fusszeile aus denselben Werten ein, aus denen der Abdruck entsteht; der Dienst liest ab da nur noch diesen Brief (`briefAusSpalte`, streng); Entwurf ohne Brief, Freigabe nur mit Brief, danach unveränderlich (Auslöser `mahnung_4_brief_eingefroren`); das Blatt sagt, ob der Brief eingefroren ist; Auslandsland als Name (`landZeile`). `tests/isolation/mahnung.test.ts` (9): Pflege von Kunde, Mandant und Fusszeile nach der Freigabe — der freigegebene Brief geht hinaus, Abdruck von Versand und Freigabe gleich; ein geänderter Betrag bleibt am Tor; der Brief ist unveränderlich, im Entwurf leer, eine Freigabe ohne Brief abgewiesen; `tests/kern/mahnschreiben.test.ts` (Rundreise mit gleichem Abdruck, beschädigte Spalte wirft, Ländername); (c) `bestaetigeStufe` übernimmt nur aus bestätigter Fassung, Tage im Satz als TT.MM.JJJJ; `autorisierungsAntwort` in beiden Routen; `istGueltigerKalendertag`; `ZahlungFehler` mit `betrag_nicht_positiv` und `bankkonto_fremd`, Sätze de/en; `AusgabeFilter.nurAufwand` (`?aufwand=ja`) aus Monatszahlen und Übersicht; `<Hinweis rolle>` (DESIGN §5) statt nachgebauter Kästen; ein Primary auf dem Mahnungsentwurf; Zinsart in Worten; `bezahlt` genutzt; `formatiereGeldIn` auf der Eingangsrechnung. `tests/isolation/mahnung.test.ts` (7) Platzhaltertext und TT.MM.JJJJ, `zahlungsausgang.test.ts` (2a), `buchhaltung-posten-monate.test.ts` (5) Liste = Spalte, `tests/kern/zahlungsausgang-texte.test.ts`, `kalendertag-stunden.test.ts`, `bank-abgleich.test.ts` (7); (d) das Blatt spricht zwei Sprachen (`KONTOAUSZUG_TEXTE`, Tage und Beträge in der Sprache der Sitzung, von der Ausnahmeliste der Sperrklinke gestrichen), die Klärung schickt Schlüssel statt Satz (`KlaerungGrund` an `ImportFehler`, `zahlung_<grund>`, `posten_waehlen`), das Blatt schlägt über `eigenerEintrag` nach; `tests/kern/kontoauszug-texte.test.ts` |

---

## 9. Was geprüft und **widerlegt** wurde

Sechs Beobachtungen sahen aus wie Lücken und sind begründete Entscheidungen.
Sie stehen hier, damit sie nicht in drei Monaten erneut als Befund auftauchen.

1. **Der Einwand `eintrag_fehlt` hat sehr wohl ein Formular.** Die Behauptung
   war am Code falsch; sie beruhte zusätzlich auf einer Fehldeutung des
   NULL-Zwecks. (Deshalb ist in § 4 nur die *Entscheidung* (V-051) offen,
   nicht das Formular.)
2. **Der fehlende Zustand `zugesagt` ist im Migrationskopf von
   `drizzle/0028_dienstplan.sql` beschrieben** — als Vokabular, das die
   Oberfläche später füllt. Das Fehlen ist dokumentiert. **Es bleibt trotzdem
   V-049**, denn dokumentiert heißt nicht gebaut, und die Besetzungswarnung
   rechnet heute falsch.
3. Ein weiterer Befund war die wörtlich dokumentierte Folge einer offenen
   Kundenfrage (`TODO(client, …)`) — kein Fehler, sondern eine Frage, die
   niemand beantwortet hat.

Die übrigen drei Gegenproben ergaben **„keine Begründung gefunden"** und
bestätigen damit ihren Befund.

---

## 10. Was dieses Blatt **nicht** behauptet

- Es behauptet nicht, vollständig zu sein. Es behauptet, dass **diese 136
  Befunde real sind** — jeder mit Datei, Zeile und Gegenprobe.
- Es behauptet nicht, dass alles Übrige funktioniert. Acht Suchläufe finden
  nicht, wonach sie nicht gesucht haben.
- Es misst **Erreichbarkeit und Existenz**, nicht Richtigkeit. Dass ein Weg
  existiert, heißt nicht, dass er das Richtige rechnet. Dafür sind die Tests da.
