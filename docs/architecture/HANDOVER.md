# Phase 0 — handover / resume point

Written when a session ran out of budget mid-pass. Everything below is on
branch `claude/phase-0-setup-planning-2jufm5`.

**Phase 0 is delivered and reviewable now.** What remains is finishing work on
consistency between documents, not missing deliverables. No application code has
been written — that was deliberate and still waits on the client's review.

---

## What exists

| Deliverable (client's list) | File | State |
|---|---|---|
| 1 · Folder structure | `01-ORDNERSTRUKTUR.md` | done, harmonised |
| 2 · Full DB schema | `02-datenmodell/01…06` | done, harmonised |
| 3 · Auth + permission model | `03-AUTH-BERECHTIGUNGEN.md` | done, harmonised |
| 4 · Page map | `04-SEITENKARTE.md` | done, harmonised |
| 4 · API map | `05-API-KARTE.md` | done, harmonised |
| 5 · Agents + approval gate | `06-AGENTEN-FREIGABEN.md` | done, harmonised |
| 6 · PR plan (96 PRs) | `08-PR-PLAN.md` | done, harmonised |
| — · Integrations (ROADMAP asks for it) | `07-INTEGRATIONEN.md` | done, harmonised |
| — · Binding conventions K-01…K-21 | `00-KONVENTIONEN.md` | done |

`_review/` holds the raw review artefacts: the twelve adversarial critiques
(`critiques/`), the twelve verification reports (`verify/`), the collated
verification (`verification.md`) and the cross-document contradictions
(`cross.md`). Keep them until Phase 0 is signed off — they are the evidence for
why the documents say what they say, and the to-do list for finishing.

Traceability: **232 / 232 SPEC feature IDs** appear in at least one architecture
document and are assigned to a PR.

---

## How it was built

Draft → adversarial review → rewrite → independent verification → cross-document
check → harmonisation. Roughly 80 agent passes.

- **233 blocking findings** raised by the reviewers, all confirmed fixed in the
  written files by a verifier that read the file rather than the author's claim.
  Six author rejections were judged sound.
- **156 second-order defects** and 36 convention departures found by the
  verifiers, then reconciled.
- **89 cross-document contradictions** and 85 dangling references found by the
  cross-check. Harmonisation of these is the pass that did not finish.

---

## Resume here

### 1. ~~Finish the harmonisation pass~~ — DONE (commit `aac8758`)

All thirteen documents now carry the cross-document fixes. `03-AUTH` owns a
complete permission catalogue with all seven K-19 actions; invented right keys
were remapped onto the closed module list rather than added to it; `t_kunde`
predicates use `app.aktuelle_kunden()` per K-20; and the K-21 names are
canonical, with superseded spellings kept only as explicit withdrawal notes so
they cannot drift back.

**The three consistency checks HAVE now run.** 43 of the 65 issues in their
areas are confirmed resolved, including everything load-bearing: the K-11 DST
and month-split values are byte-identical in nine documents, the K-06
`app.arbzg_belastung` signature and its "durations and boundaries and nothing
else" contract are identical in all nine that name it, `job_lauf` is declared
once with no `mandant_id`, all seven K-19 actions are in the enum,
`app.aktueller_kunde()` no longer resolves through `aktiver_mandant()` anywhere,
and **no employee- or customer-facing route runs under group scope in any of the
thirteen documents**.

**22 inconsistencies remain.** They are listed in full at the end of this file.
None is a silent-failure class like the ones already fixed; they are naming and
column-set reconciliations. Work them first on resume — each names the documents
involved, which one owns the fact, and the fix.

### 2. Rebuild the O-number register — do this by hand, do not trust the claims

Documents independently claimed `O-30`…`O-131` and **there are collisions**: the
same number used for different questions in different documents. Roughly 114
distinct open questions exist.

The safe procedure is mechanical, not editorial:

```bash
grep -rhoE 'O-[0-9]+[^\n]{0,200}' docs/architecture --include='*.md' | sort -u
```

Group by number, find where one number carries two different questions,
renumber into one register, then write the register into `docs/DECISIONS.md`
under **Open** and fix every reference. `O-01` and `O-04`…`O-13` are the
client's originals and **must keep their numbers**; `O-14`…`O-29` come from
`08-PR-PLAN.md` and are internally consistent.

### 3. Write the two remaining index documents

- `README.md` — an index of the set, with a one-line purpose per document and
  the reading order for a reviewer.
- `09-ABDECKUNG.md` — the SPEC traceability matrix. The generator already exists
  and works; it expands `DOC-01…DOC-06` range notation, which a naive grep does
  not:

```
/tmp/claude-0/-home-user-cse-platform/.../scratchpad/trace.py
```

If that scratchpad is gone, it is twenty lines: extract the 232 IDs from
`SPEC.md`, expand range notation, grep each ID across the documents, emit a
table of ID → documents → PR.

### 4. Update `docs/DECISIONS.md`

Add the Phase 0 decisions and the consolidated Open register from step 2.
Candidates for **Decided**, all of them load-bearing and already justified in
`00-KONVENTIONEN.md`: the K-06 ArbZG cross-entity function, K-05 column grants
for wage confidentiality, K-18's four read scopes, K-07's `/portal` prefix, and
K-10 (the AI supplies no number and no input that determines one).

Also carry over three non-client items the review surfaced:

- `docs/DESIGN.md` needs an absence colour palette before the Dienstplan renders
  anything beyond the semantic tokens — it currently carries a `TODO(design)`.
- `02-CRM-OPERATIONS.md` must add
  `dokument.sichtbar_fuer_mitarbeiter boolean not null default false`; worker
  document visibility has nothing to key on today.
- `app.mandant_kennzahlen()` is defined in `01-KERN.md` §6.3 as counts-only over
  `app.switcher_mandanten()`, but is silent on whether the counts respect the
  caller's per-module rights. Settle it before the switcher ships (TEN-10).

---

## Do not lose these decisions

The five that cost the most to find, and that a fresh pass would probably get
wrong again:

1. **K-06 — the ArbZG cross-entity window.** Aggregating one person's hours
   across entities (TIM-14, D-09) is impossible under strict tenant RLS, and it
   fails *silently*: the check returns "no conflict" and a 6h + 5h day is
   scheduled as lawful. Exactly one sanctioned crossing exists — a
   `SECURITY DEFINER` function returning only durations and interval boundaries,
   never the other entity's name, `objekt`, `kunde` or wage rate — audit-logged
   per call, with an isolation test asserting nothing identifying leaks.

2. **K-05 — column grants, not masking views.** The first draft's view did
   `select a.*` *and then* added a masked copy of the wage rate beside it. Both
   repairs fail: `security_invoker` needs privileges just revoked, and without it
   the view runs as an owner that ignores the tenant predicate.

3. **K-18 + K-20 — four read scopes, and accessors that resolve in all of them.**
   The employee and customer portals span tenants as a *subject*, not as a
   manager. Routing them through group scope means K-03's group policy demands a
   `gruppe.*` right they never hold, and both portals read zero rows; widening
   that right hands every cleaner a group-level read. K-20 exists because the
   same bug reappeared one layer down in `app.aktueller_kunde()` and
   `app.portal()`.

4. **K-19 — an unregistered right key is permanent silence.** `hat_recht()`
   returns false for unknown keys, so a typo is an always-empty screen with no
   error. One draft's action vocabulary omitted `schreiben`, which would have
   made every write path in the platform unauthorisable.

5. **K-11 — the DST reference cases, verified.** The transition *night* begins
   the evening before the transition day. The drafted PR plan named
   `2026-03-29` and `2026-10-25` and asserted 420 and 540 minutes against
   shifts that are ordinarily 480. Correct: `2026-03-28 22:00 → 03-29 06:00`
   = **420**, `2026-10-24 22:00 → 10-25 06:00` = **540**, and a shift starting
   22:00 *on* a transition day = **480**, asserted as a control so an
   off-by-one fixture cannot pass.

---

## Before any code

The client's standing conditions, unchanged:

- The first code PR is **PR 1** in `08-PR-PLAN.md`: money, time and ArbZG tests
  with no table, no route and no React — the 22:00–06:00 shift, both DST nights,
  ten concurrent shifts, and one person breaching ArbZG across two entities.
- `person` and `anstellung` are separate **from the first migration** (D-09);
  that is PR 3.
- Anything undecided ships behind an interface with `// TODO(client)` and a
  `DECISIONS.md` row. No invented business rules.
- No fake integrations. Unconnected systems display "not connected".

**Phase 0 stops here and waits for the client's review before any code.**

---

## Outstanding cross-document inconsistencies (22)
Found by the final consistency re-check after harmonisation. 43 of the previous
round's 65 issues in these areas are confirmed resolved; these 22 are not.
Each names the documents involved and the fix.

### tabellen (K-21 names) — 5 remaining

1. **`agent_artefakt` is declared once (K-21 owner `02-datenmodell/06-RADAR-KI-INHALT.md` §3.12), but two documents state two irreconcilable column sets for it, and the non-owner's set is written as a binding requirement on the owner (§9.4: "the shape this document's tools, handles and approval chain need", restated as R-01). They disagree on essentially every column that matters: (a) the `art` enum — owner declares `artefakt_art` as `lv_extrakt · textentwurf · email_entwurf · angebot_entwurf · vergabemappe_entwurf · zusammenfassung` (§3.12 and the DDL at line 3476), while `06-AGENTEN-FREIGABEN.md` §9.4 requires `lv_entwurf · text_entwurf · vergabemappe_pruefliste · buchungsvorschlag_entwurf · abrechnungsentwurf · shortlist` — not one value is spelled the same, and §9.4's own prose elsewhere uses those values as live (line 804 `art 'lv_entwurf'`, line 1528 `incl. 'lv_entwurf', 'text_entwurf', 'shortlist'`, line 2874 `art 'shortlist'`); (b) where the draft lives — owner: `inhalt jsonb NOT NULL` plus `format artefakt_format`, column-granted away and read through `app.agent_artefakt_lesen`; §9.4: `inhalt_ref text`, a private-bucket object key served by signed URL. A jsonb column and a storage-object reference are not the same table; (c) `status` (`entwurf · freigegeben · verworfen · ersetzt`), `vorlage`, `sprache`, `verwendete_werte`, `quellen`, `konfidenz_min`, `unsicher` are required by §9.4 and do not exist in the owner's declaration — yet `06-AGENTEN-FREIGABEN.md` line 534 and line 967 both key agent behaviour on the status value ("writing an `agent_artefakt` in status `entwurf` is `aktion = 'draft'`"), which no column supports. (d) The owner's `version`, `bytes`, `bezug_typ`/`bezug_id`, `loeschfrist_am`, `geloescht_am` are absent from §9.4. The K-21 defect this was meant to close — one table, one column set — is therefore only half closed: ownership was fixed, the column set was not reconciled.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/06-RADAR-KI-INHALT.md §3.12 (lines 1935–1985, enum DDL line 3476); /home/user/cse-platform/docs/architecture/06-AGENTEN-FREIGABEN.md §9.4 (lines 1883–1911), plus lines 534, 751, 804, 967, 1528, 2874, 3201 (R-01)
   - Fix: Reconcile onto the owner. Pick one vocabulary for `artefakt_art` and write it in both places — the natural merge is the owner's enum extended with the acts §9.4 actually produces (`buchungsvorschlag_entwurf`, `abrechnungsentwurf`, `shortlist`, `vergabemappe_pruefliste`) rather than two disjoint lists; then update `06-AGENTEN-FREIGABEN.md` lines 804, 1528 and 2874 to the agreed spellings. Decide storage once: either the owner adds `inhalt_ref text` (and §9.4 drops the expectation of an inline jsonb reader) or §9.4 drops `inhalt_ref` and accepts `inhalt jsonb` + `format` read through `app.agent_artefakt_lesen` — the second matches the owner's LEG-09 redaction design (`geloescht_am` nulls `inhalt` while `inhalt_hash` survives), so it is the cheaper merge. Add to §3.12 the columns §9.4 genuinely needs and the rest of the document already assumes — `status artefakt_status`, `vorlage`, `sprache`, `verwendete_werte jsonb`, `quellen jsonb`, `konfidenz_min`, `unsicher` — or delete the claims that depend on them (lines 534 and 967 in particular). Then restate §9.4 as a pure back-reference to §3.12 rather than a second column table.

2. **`05-FINANZEN.md` contradicts its own K-21 ruling on the VAT catalogue. §0.1 (line 36) states: "There is **no `steuersatz` table**, no `steuersatz_id` column, no `prozent_bp` and no `hinweistext`; every foreign key in the platform is `*.steuersatz_gruppe_id`", and §3.2 (line 957) repeats it. But §12.2's agent-tool mapping table (line 3155) still passes `positionen: { betrag_token, steuersatz_id: BezugHandle }[]` for `berechne_preis` art `ust_split` — the exact identifier the document abolished. The row immediately above it (line 3149) lists `any steuersatz_gruppe_id` among what a tool never accepts, so the two rows also use two different spellings for the same reference. The owner of the tool signatures, `06-AGENTEN-FREIGABEN.md` line 863, writes it correctly as `{ art: 'ust_split'; positionen: { betrag_token: string; steuersatz_gruppe_id: BezugHandle }[] }` with an explicit comment that there is no `steuersatz` table — so the finance document's signature no longer matches the signature owner. The §13 CI test as written (line 3509) checks only that no *table* named `steuersatz` exists; it would not catch this column-name survivor.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/05-FINANZEN.md line 3155 (§12.2), contradicting its own lines 36 and 957 and the tool-signature owner /home/user/cse-platform/docs/architecture/06-AGENTEN-FREIGABEN.md lines 863–866
   - Fix: In `05-FINANZEN.md` §12.2, change `steuersatz_id: BezugHandle` to `steuersatz_gruppe_id: BezugHandle`, matching `06-AGENTEN-FREIGABEN.md` §5 verbatim. Extend the §13 test at line 3509 from "no table named `steuersatz`" to "no identifier `steuersatz_id`, `prozent_bp` or `hinweistext` anywhere", since the phantom survived as a column name after the table was removed.

3. **The owner of `mandant_einstellung` enumerates the O-06 monitoring switches as a closed set that omits three of the five keys its sibling declares canonical. `01-KERN.md` §6.30 (lines 3064–3066) says "**Die O-06-Schalter leben hier und nirgendwo sonst (K-21).** `zeit.geolokalisierung`, `zeit.geraetekennung`, `geo.erfassung_erlaubt` und `wachbuch.uebergabe_fenster` sind Zeilen dieser Tabelle", and the §14 seed (lines 3917–3920) seeds exactly those four, adding "No other key is seeded". `04-PLANUNG-ZEIT.md` §1.15 (lines 399–408) says "**All five are `mandant_einstellung` keys**" and names `zeit.geolokalisierung`, `zeit.geraetekennung`, `zeit.abweichungsauswertung`, `zeit.korrekturstatistik`, `zeit.nichterschienen_auswertung`, each with default `false`; `05-API-KARTE.md` line 630 names the same three missing keys as the gates behind which DSH-05 and the APR-08 per-approver breakdown ship off, and `08-PR-PLAN.md` line 373 makes `zeit.abweichungsauswertung` an acceptance criterion. So three keys that two documents treat as shipping-off-by-default gates are absent from the owner's enumeration and from its seed. Because `app.einstellung()` returns NULL for an unseeded key and each caller supplies its own default, this is not an immediate open-by-default hole — but it means the owner's list of what lives in its own table is wrong, which is precisely the K-21 failure mode.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/01-KERN.md §6.30 lines 3064–3066 and §14 lines 3917–3920; /home/user/cse-platform/docs/architecture/02-datenmodell/04-PLANUNG-ZEIT.md §1.15 lines 399–408 and §17.2 line 2057; /home/user/cse-platform/docs/architecture/05-API-KARTE.md line 630; /home/user/cse-platform/docs/architecture/08-PR-PLAN.md line 373
   - Fix: Extend `01-KERN.md` §6.30's enumeration and §14's seed to the full canonical set: add `zeit.abweichungsauswertung`, `zeit.korrekturstatistik` and `zeit.nichterschienen_auswertung` at `false`, alongside the four already there (`zeit.geolokalisierung`, `zeit.geraetekennung`, `geo.erfassung_erlaubt`, `wachbuch.uebergabe_fenster`). Note in §6.30 that `geo.erfassung_erlaubt` is 03-GEWERKE's signature/photo coordinate switch and distinct from `zeit.geolokalisierung` — the two are easy to read as duplicates. Cross-reference `04-PLANUNG-ZEIT.md` §1.15 as the table that states each key's effect while off, so one list exists in one place.

4. **`00-KONVENTIONEN.md` states the reserved `mandant.slug` list twice, with two different lists. K-07 (line 255) says "`mandant.slug` carries a `CHECK` excluding `gruppe`, `mein`, `api`" — three values. K-21 (line 697) says "Reserved `mandant.slug` values, **one list**: `gruppe`, `mein`, `kunde`, `konto`, `api`" — five. Every domain document has harmonised on K-21's five (`01-KERN.md` line 1451's `CHECK (… slug NOT IN ('gruppe','mein','kunde','konto','api'))`, `03-AUTH-BERECHTIGUNGEN.md` lines 197 and 587, `04-SEITENKARTE.md` line 2841, `05-API-KARTE.md` line 388, `05-FINANZEN.md` line 835, `04-PLANUNG-ZEIT.md` line 1816, `01-ORDNERSTRUKTUR.md` line 3457, `06-RADAR-KI-INHALT.md` line 471), so the only document still carrying the three-value list is the authoritative one — and K-21's own "one list" phrasing is what makes the residual K-07 sentence a contradiction rather than an abbreviation. Since the conventions file wins over domain documents by its own opening paragraph, a reader implementing K-07 literally writes a CHECK that admits `kunde` and `konto` as tenant slugs, which then collide with the `/portal/kunde` and `/portal/konto` statics.**
   - Documents: /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md line 255 (K-07) versus line 697 (K-21)
   - Fix: Amend K-07's sentence to name the same five values as K-21, or replace the enumeration in K-07 with a pointer ("the reserved list of K-21"). Do not amend the domain documents — they are already correct.

5. **`04-PLANUNG-ZEIT.md` still accuses two sibling documents of a defect they no longer have. §1.15 (line 399) reads "`05-API-KARTE.md` and `08-PR-PLAN.md` still read `mandant.geo_erfassung_aktiv` and `mandant.ueberwachung_aktiv`", and §2.3 item 4 (line 479) repeats it as an outstanding requirement ("which `05-API-KARTE.md` and `08-PR-PLAN.md` still write — do not exist and must be replaced"). Both documents were fixed in the harmonisation pass and now state the opposite in bold: `05-API-KARTE.md` line 536 ("**There is no `mandant.geo_erfassung_aktiv` column**") and line 630 ("**There is no `mandant.ueberwachung_aktiv` column**"), and `08-PR-PLAN.md` line 373 ("There is no `mandant.geo_erfassung_aktiv` column and no `mandant.ueberwachung_aktiv` column"). The column names themselves are consistent platform-wide — this is a stale open item, not a schema disagreement, but it leaves a live "must be replaced" instruction in a §2.3 requirements register that a PR would try to action against already-correct documents.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/04-PLANUNG-ZEIT.md lines 399 and 479; /home/user/cse-platform/docs/architecture/05-API-KARTE.md lines 536 and 630; /home/user/cse-platform/docs/architecture/08-PR-PLAN.md line 373
   - Fix: Rewrite both sentences in `04-PLANUNG-ZEIT.md` from an open accusation to a settled statement — e.g. "the O-06 switches are `mandant_einstellung` keys, not columns of `mandant`; `05-API-KARTE.md` §C/§D and `08-PR-PLAN.md` PR 4 now state the same" — and strike item 4's "must be replaced by the keys of §1.15/§17.2" from the §2.3 requirements register, since the requirement has been met.

### rechte + scopes (K-19/K-20) — 8 remaining

1. **03-AUTH-BERECHTIGUNGEN.md §7.2 fixes berechtigung_aktion at exactly 27 values (lesen · erstellen · aendern · schreiben · loeschen · exportieren · importieren · zuweisen · freigeben · genehmigen · entscheiden · verwalten · pruefen · melden · planen · veroeffentlichen · versenden · festschreiben · stornieren · korrigieren · quittieren · uebersteuern · verbinden · einreichen · anmelden · widerrufen · ziehen), with the grammar `<modul> '.' [<objekt> '_'] <aktion>`, `UNIQUE (modul, objekt, aktion)` and `aktion` typed as that enum. Its own catalogue in §12 and §14.2 then uses 18 actions that are not enum values, across 23 keys: `abschliessen` (auftrag.abschliessen, zeit.konto_abschliessen), `archivieren` (dokument.archivieren), `bearbeiten` (datenschutz.berichtigung_bearbeiten), `beenden` (personal.anstellung_beenden), `bewerten` (recruiting.bewerbung_bewerten), `erfassen` (angebot.annahme_erfassen, bau.aufmass_erfassen, referenz.kundenfreigabe_erfassen, vergabe.einreichung_erfassen), `erheben` (freigabe.einspruch_erheben), `export` (dokument.buendel_export), `herunterladen` (finanzen.herunterladen), `pflegen` (abrechnung.freistellung_pflegen), `rueckgaengig` (freigabe.rueckgaengig), `senden` (crm.kommunikation_senden, nachricht.senden), `setzen` (agent.autonomie_setzen, crm.rechtsgrundlage_setzen, radar.status_setzen), `starten` (agent.aufgabe_starten), `verwerfen` (finanzen.entwurf_verwerfen), `zuruecksetzen` (system.zwei_faktor_zuruecksetzen), `zusammenfuehren` (personal.zusammenfuehren). A twenty-fourth, `zeit.freigeben_zur_abrechnung`, does not parse under the grammar at all — its last underscore token is `abrechnung`. None of these rows can be inserted, so app.hat_recht() answers false permanently and silently for each — the exact K-19 failure the enum exists to prevent. Consequences include: the customer can never download an invoice PDF or XRechnung (finanzen.herunterladen gates /portal/kunde/rechnungen in 04-SEITENKARTE), no agent run can be started from the portal, no APR-05 objection and no APR-06 undo can be exercised, no Aufmaß can be recorded on site, and no message can be sent. 02-datenmodell/01-KERN.md §4 mirrors the same 27 values verbatim and calls itself a mirror; 06-AGENTEN-FREIGABEN.md §14.3 asserts outright that 'the action segment of every key above is a member of ... the wider berechtigung_aktion vocabulary 03-AUTH §7.2 declares', which is false for four of the seventeen keys in its own table (agent.aufgabe_starten, agent.autonomie_setzen, freigabe.einspruch_erheben, freigabe.rueckgaengig).**
   - Documents: /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§7.2 line 1153, §12.1–§12.5 lines 2253–2492, §14.2 line 2740), /home/user/cse-platform/docs/architecture/02-datenmodell/01-KERN.md (§4 enum table line 1281, §4 rationale line 1310), /home/user/cse-platform/docs/architecture/06-AGENTEN-FREIGABEN.md (§14.3 key table lines 2465–2490), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-19)
   - Fix: 03-AUTH §7.2 owns this vocabulary, so extend it there and let 01-KERN §4 re-mirror it. Add the missing verbs to berechtigung_aktion — abschliessen, archivieren, bearbeiten, beenden, bewerten, erfassen, erheben, herunterladen, pflegen, rueckgaengig, setzen, starten, verwerfen, zuruecksetzen — and restate the vocabulary as an explicit list with its new count, never as a count alone. Where an existing value already covers the act, prefer renaming the key over widening the enum: dokument.buendel_export → dokument.buendel_exportieren, crm.kommunikation_senden → crm.kommunikation_versenden, nachricht.senden → nachricht.versenden, personal.zusammenfuehren → personal.zusammenfuehrung_verwalten. Separately respell zeit.freigeben_zur_abrechnung so it parses (e.g. zeit.abrechnung_freigeben — objekt=abrechnung, aktion=freigeben) while keeping its O-39 not-seeded marker. Then make the K-19 CI test in §7.2a two-sided: it must fail not only on a key with no catalogue row but on a catalogue key whose last underscore-delimited token is not a berechtigung_aktion value — that is the assertion that would have caught all 23. Finally correct 06-AGENTEN-FREIGABEN.md §14.3's conformance sentence, which currently certifies something untrue about its own four keys.

2. **02-datenmodell/04-PLANUNG-ZEIT.md §1.3 still states, in bold: 'The group-read key for ArbZG findings is `gruppe.dienstplan.lesen`, not `gruppe.dienstplan.arbzg_lesen`. The draft minted the second, and it is not expressible: the group grammar is `gruppe.<modul>.<aktion>` with `aktion` an enum of berechtigung_aktion, and `arbzg_lesen` is not one of its values, so the catalogue row cannot be inserted at all.' It then gives arbeitszeit_verstoss the group key gruppe.dienstplan.lesen and omits gruppe.dienstplan.arbzg_lesen from its enumerated fifteen-key list. 03-AUTH §7.2 has since widened the group grammar to `gruppe.<modul>.[<objekt> '_'] <aktion>` precisely to make that key insertable (objekt = dienstplan_arbzg, aktion = lesen), cites '02-datenmodell/04-PLANUNG-ZEIT.md §1.3' by name as the document that needs it, lists it as a catalogue key in §12.1 alongside gruppe.system.audit_lesen, and §21 tells 04-PLANUNG-ZEIT it 'is expressible under §7.2's widened group grammar (objekt = dienstplan_arbzg, aktion = lesen) and may stay'. The catalogue owner and the domain document now assert opposite things about both the grammar and the key. Under K-19's CI assertion 2 (a catalogue key no code uses fails the build) gruppe.dienstplan.arbzg_lesen fails today, because the only document that would have written it disowns it — and 03-AUTH §12.1's stated justification for widening the grammar cites a user that no longer exists.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/04-PLANUNG-ZEIT.md (§1.3 policy table row for arbeitszeit_verstoss, bolded paragraph at line 216, the fifteen-key list, and its verbatim repeat in §2.3 item 5), /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§7.2 group grammar lines 1130–1150, §12.1 gruppe.*.lesen row line 2277, §21 line 3030), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-19)
   - Fix: The catalogue owner has already decided, so make the domain document follow: delete the 'not expressible' paragraph from 04-PLANUNG-ZEIT §1.3, set arbeitszeit_verstoss's group key to gruppe.dienstplan.arbzg_lesen, and add that key to the enumerated list, taking it from fifteen to sixteen — updating both §1.3 and the verbatim repeat in §2.3 item 5, since a seed test compares the two. If instead the three-segment bare-action grammar is to be restored, then 03-AUTH §7.2 must withdraw the widening, §12.1 must drop both gruppe.dienstplan.arbzg_lesen and gruppe.system.audit_lesen, and 02-datenmodell/01-KERN.md §6.11 plus 04-SEITENKARTE.md's /portal/gruppe/protokoll gate must fall back to gruppe.system.lesen — the more expensive path, and one that reopens 04-SEITENKARTE's already-settled item (h).

3. **03-AUTH-BERECHTIGUNGEN.md §1.4 quotes app.portal_fuer with two arguments — `app.portal_fuer(p_benutzer uuid, p_mandant uuid) returns text` — explicitly labelled '02-datenmodell/01-KERN.md §3.2, quoted because this document's ceilings depend on it verbatim', with the comment 'CALLED ONLY IN mandant SCOPE. In gruppe / person / kunde scope the value comes from the table above and is set by withGroupScope / withPersonScope / withKundeScope'. 02-datenmodell/01-KERN.md §3.2 now declares three arguments — `app.portal_fuer(p_benutzer uuid, p_mandant uuid, p_scope text)` — with a four-branch CASE on p_scope, calls it that way from §3.3 (`app.portal_fuer(s.benutzer_id, s.aktiver_mandant_id, s.ansicht::text)`), and states in bold: 'The two-argument resolver the draft carried therefore fell through to mitarbeiter for every non-global principal outside mandant scope ... The scope argument is what removes that, and there is no second resolver.' 03-AUTH §21 had asked 01-KERN for exactly this ('the four-branch, scope-aware derivation of §1.4 — or a second resolver'); 01-KERN chose the scope argument, while 03-AUTH still publishes the two-argument form as the verbatim quote its ceilings depend on, and still describes the multi-tenant values as being set by the wrappers rather than returned by the resolver. Postgres overloads on the argument list — the same hazard 03-AUTH §21 itself raises for app.checkin_verbrauchen ('a grant on a three-argument signature does not reach the five-argument function') — so this is not a typo: implemented from 03-AUTH, portal_fuer has no scope to branch on, p_mandant is NULL in the three multi-tenant scopes by the benutzer_sitzung CHECK, and app.portal falls through to the fail-closed 'mitarbeiter' in group, person and kunde scope. That is the precise K-20 defect both documents were rewritten to remove. 05-API-KARTE.md §B.4 states the four scope values but never the signature, so it does not settle the tie.**
   - Documents: /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§1.4 SQL block line 147 and four-branch table line 138; also line 921 and §8.3 line 1452, which name app.portal_fuer without arity), /home/user/cse-platform/docs/architecture/02-datenmodell/01-KERN.md (§3.2 lines 983–1041, §3.3 line 1146, §17 line 4401), /home/user/cse-platform/docs/architecture/05-API-KARTE.md (§B.4 lines 170–180), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-20)
   - Fix: 01-KERN owns the function, so 03-AUTH must adopt its signature. Replace the SQL block in 03-AUTH §1.4 with `app.portal_fuer(p_benutzer uuid, p_mandant uuid, p_scope text) returns text`, delete the 'CALLED ONLY IN mandant SCOPE' comment, and change the Derived-from column of the four-branch table so all four rows read 'app.portal_fuer(benutzer, aktiver_mandant, scope)' instead of only the mandant row naming the resolver and the other three naming the wrappers. Add the arity to the K-20 CI test alongside the existing checkin_verbrauchen arity check: assert exactly one app.portal_fuer overload exists platform-wide and that it takes three arguments, so a stale two-argument call or grant cannot pass silently.

4. **03-AUTH-BERECHTIGUNGEN.md §12.1 mints a dedicated catalogue key `system.referenzdaten_verwalten` (erfordert_2fa; SA ✔ / AD ○ / LT — ) for the five global finance reference tables — steuersatz_gruppe, masseinheit, kleinbetrag_grenze, bauabzugsteuer_freigrenze, basiszinssatz — and its note says '02-datenmodell/05-FINANZEN.md §3.2, which calls it referenz.verwalten'. §21 repeats the obligation: 'The reference-data right is system.referenzdaten_verwalten, not referenz.verwalten.' 05-FINANZEN has since dropped referenz.verwalten but landed on a third spelling: §3.2 writes `app.hat_recht('system.einstellung_verwalten', app.aktiver_mandant())` into the WITH CHECK of both r_pflege_i and r_pflege_u, states in bold 'The key is system.einstellung_verwalten, not referenz.verwalten (K-19)', and its §15 item 54 declares the CI extractor 'fails on referenz.verwalten (module collision — §3.2 uses system.einstellung_verwalten)'. Three consequences: system.referenzdaten_verwalten is now a catalogue row no document's code uses, which fails K-19's CI assertion 2 (the catalogue must not rot into a wish list); 03-AUTH §12.1's and §7.4's notes still describe 05-FINANZEN as writing referenz.verwalten, which it no longer does; and the two keys are not behaviourally equivalent — system.referenzdaten_verwalten carries erfordert_2fa while system.einstellung_verwalten does not, and system.einstellung_verwalten is the per-tenant mandant_einstellung key bindable to an admin per mandant, so reusing it for platform-global VAT rates and the §288 BGB base rate reintroduces exactly the one-key-two-meanings collision 05-FINANZEN raises against referenz.verwalten.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/05-FINANZEN.md (§3.2 r_pflege_i / r_pflege_u policy block and the bolded paragraph at line 938; §15 item 54 at line 3495), /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§12.1 line 2276, §7.4 rename table line 1327, §21 line 3031), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-19)
   - Fix: Keep 03-AUTH's system.referenzdaten_verwalten — it is the key that carries erfordert_2fa and is not bindable per mandant, which is what a platform-global tax table needs. In 05-FINANZEN §3.2 change both r_pflege_i and r_pflege_u to name system.referenzdaten_verwalten; rewrite the bolded paragraph to read 'the key is system.referenzdaten_verwalten — not referenz.verwalten (module collision with published website content) and not system.einstellung_verwalten, which is the per-tenant mandant_einstellung key while these five tables are not tenant data'; and update §15 item 54's CI sentence to match. In 03-AUTH §12.1 and §7.4 drop the now-stale claim that 05-FINANZEN 'calls it referenz.verwalten'. While editing §3.2, restate why the WITH CHECK's `app.aktiver_mandant()` conjunct is correct against the new key: §7.3's first gate refuses a NULL mandant, which is what confines the write to mandant scope.

5. **02-datenmodell/06-RADAR-KI-INHALT.md §1.3's module/policy table gives the recruiting job-posting tables — stelle, stelle_intern, stelle_anforderung, jobboard_kanal, postfach_kanal, stelle_veroeffentlichung — the read right `recruiting.lesen`, and that key has no row in 03-AUTH's catalogue. §12.2 carries recruiting.stelle_schreiben, recruiting.stelle_veroeffentlichen, recruiting.bewerbung_lesen, recruiting.bewerbung_bewerten, recruiting.entscheiden and recruiting.daten_loeschen — there is no recruiting.lesen and no recruiting.stelle_lesen. 03-AUTH §14.2's remap list covers recruiting.schreiben / .veroeffentlichen → recruiting.stelle_schreiben / .stelle_veroeffentlichen but says nothing about a read key, and 06-RADAR's own §1.3 remap paragraph remaps nine keys without touching recruiting.lesen. 06-RADAR §15 then certifies that 'every right key of §1.3 has a catalogue row, spelled as §1.3 spells it', naming only wissen, wissen.vertraulich_lesen, agent.protokoll_lesen, social.lesen and gruppe.freigabe.lesen as genuine additions — recruiting.lesen is not among them. Under K-19 the t_mandant USING clause on all six tables therefore names a key app.hat_recht() answers false for, so the whole REC-01…REC-06 job-posting surface reads zero rows for every role including super_admin, with no exception and no log line. This is the same class of defect that paragraph was written to close; one key was missed.**
   - Documents: /home/user/cse-platform/docs/architecture/02-datenmodell/06-RADAR-KI-INHALT.md (§1.3 module table, recruiting row at line 260; §1.3 remap paragraph lines 270–285; §15 first obligation row at line 3954), /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§12.2 lines 2310–2315, §14.2 line 2751), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-19)
   - Fix: Add a `recruiting.stelle_lesen` row to 03-AUTH §12.2 with the same defaults as recruiting.bewerbung_lesen (SA ✔ / AD ✔ / LT ✔ / MA — / KD —), and change 06-RADAR §1.3's recruiting row from recruiting.lesen to recruiting.stelle_lesen. Add it to 06-RADAR §15's list of genuine additions the catalogue must make, beside social.lesen, which is the identical case already handled that way. Do not reuse recruiting.bewerbung_lesen for the job-posting tables: applicant data and vacancy data carry different LEG-11 / LEG-12 exposure, and §12.2 already separates them deliberately.

6. **03-AUTH's two statements of app.aktueller_kunde()'s value in kunde scope contradict each other. §8.4's four-scope accessor table gives it 'NULL by design — never referenced by a policy reachable here', and the paragraph beneath builds the safety argument on that: 'app.aktueller_kunde() is the one accessor that is deliberately undefined in a scope that reaches it, so K-20's second clause applies: it is documented NULL.' But §13.1 defines it as `create function app.aktueller_kunde() returns uuid language sql stable as $$ select (app.aktuelle_kunden())[1] $$`, and §13.1's own four-scope table says that in kunde scope it returns 'the first element, arbitrary — never write a policy against it here'. Those are different values: app.aktuelle_kunden() in kunde scope is 'the whole subject of the request: every live binding, across every entity', so [1] is a real uuid, never NULL. 01-ORDNERSTRUKTUR.md §4.4's accessor table agrees with §13.1, giving the kunde-scope cell as '(app.aktuelle_kunden())[1]'. The distinction is not cosmetic: §8.4's claim implies a mis-written t_kunde policy fails closed (zero rows), whereas the actual definition makes it return one arbitrarily chosen customer binding out of several, silently — precisely the CRM-06 failure mode §13.1 describes two paragraphs later. K-20's CI test (accessor-scopes.test.ts) is specified to assert 'the NULL documented here', so it would be written against an expectation the implementation cannot meet.**
   - Documents: /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§8.4 accessor table line 1522 and the paragraph at line 1524; §13.1 function definition line 2599 and scope table line 2605), /home/user/cse-platform/docs/architecture/01-ORDNERSTRUKTUR.md (§4.4 accessor table line 1096), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-20)
   - Fix: §13.1 and 01-ORDNERSTRUKTUR are right — the definition is (app.aktuelle_kunden())[1] with no branch returning NULL in kunde scope. Change 03-AUTH §8.4's cell from 'NULL by design — never referenced by a policy reachable here' to 'the first element, arbitrary — never referenced by a policy reachable here', and rewrite the paragraph beneath: the accessor is not undefined here, it is defined and wrong for the purpose, which is a stronger reason to forbid it in a t_kunde policy, not a weaker one. Restate the CI assertion to match: accessor-scopes.test.ts must assert that no policy or ceiling names app.aktueller_kunde() (the class-map grep both documents already describe), not that it returns NULL under kunde scope.

7. **app.portal in gruppe scope is given three different derivations. 03-AUTH §8.4's accessor table states it flatly as 'bound at entry — intern (§1.4)'. 03-AUTH §1.4's four-branch table states it as 'the most restrictive rolle.portal across the memberships in app.mandant_ids — in practice intern, because group entry requires a membership whose role has portal = intern (§4.5, DESIGN §6 rule 1)'. 02-datenmodell/01-KERN.md §3.2 implements the most-restrictive reading literally: `case when bool_or(r.portal <> 'intern') then 'mitarbeiter' else 'intern' end` over every live membership in sichtbare_mandanten(). These diverge for the case D-09 and EMP-15 make routine — one human who is leitung of one entity and employed by another. She holds an intern membership in A and a mitarbeiter membership in B, so bool_or is true and 01-KERN's resolver returns 'mitarbeiter' in group scope, firing every K-04 p_ma_ceiling inside the group view and making TEN-05 read nothing; 03-AUTH §8.4 says the value is intern and the ceilings do not fire. 01-KERN and 05-API-KARTE §B.4 add a second escape — such a session is 'refused by the wrapper before the transaction opens' — which removes the ceiling problem by denying that Leitung the group view altogether, an outcome TEN-05 does not contemplate. Separately, §1.4's cited authority does not say what it is cited for: §4.5's [mandant]-segment table gates group entry on 'user has ≥ 2 switcher mandanten' and 'no group right', never on a membership whose role has portal = 'intern'.**
   - Documents: /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§1.4 four-branch table line 141 and its §4.5 citation; §4.5 gate table lines 578–590; §8.4 accessor table line 1515), /home/user/cse-platform/docs/architecture/02-datenmodell/01-KERN.md (§3.2 gruppe branch lines 1000–1015 and the test assertion at line 1040), /home/user/cse-platform/docs/architecture/05-API-KARTE.md (§B.4 gruppe row line 177), /home/user/cse-platform/docs/architecture/00-KONVENTIONEN.md (K-04, K-20)
   - Fix: Pick the rule that keeps TEN-05 working and write it in all four places. The workable rule: group scope is entered only by a principal holding at least one membership whose rolle.portal = 'intern' plus at least one gruppe.<modul>.lesen right, and once entered app.portal is bound to 'intern' unconditionally — group scope is read-only by K-03 and K-18, so an intern ceiling there grants no write and widens nothing. Replace 01-KERN §3.2's bool_or most-restrictive branch with that constant (its own test at line 1040 already asserts app.portal_fuer(u, null, 'gruppe') = 'intern'), change 03-AUTH §1.4's gruppe row from 'the most restrictive rolle.portal across the memberships' to the same constant, and add the intern-membership condition to 03-AUTH §4.5's gate table so §1.4's citation of it becomes true. 05-API-KARTE §B.4 already states the constant; align only its 'refused at the helper' sentence with the entry condition as written in §4.5.

8. **04-SEITENKARTE.md's cross-reference block to 03-AUTH-BERECHTIGUNGEN.md still carries item (g) as **open**: 'the catalogue must carry a row for every key this map gates a route on (K-19: app.hat_recht() returns false for an unregistered key, so a missing row is a permanently empty screen, not an error). One is still missing: agent.protokoll_lesen, which §14.2's rights list omits and which this map now requires on M/agenten/[agent]/protokoll and on the step chain of /aufgaben/[id], and which app.agent_nutzlast_lesen re-checks.' 03-AUTH §14.2 now lists agent.protokoll_lesen in its rights sentence, gives it a dedicated justification row in the five-new-keys table ('the per-step protocol — tool, input, output, model, tokens, cost — is not the run header'), assigns it defaults (SA ✔ / AD ○ / LT ○), and §21 instructs 04-SEITENKARTE to use it on exactly those two surfaces. The obligation is discharged; only 04-SEITENKARTE's status marker still says otherwise. Left as is, the one genuinely open item in that block — (k), the second service principal for the REQ-01 form intake — is obscured by a false one, and a reader reconciling the two documents is told the catalogue is incomplete when it is not.**
   - Documents: /home/user/cse-platform/docs/architecture/04-SEITENKARTE.md (cross-reference block, item (g), line 2841), /home/user/cse-platform/docs/architecture/03-AUTH-BERECHTIGUNGEN.md (§14.2 rights sentence and five-new-keys table, lines 2740–2760; §21 line 3033)
   - Fix: Change item (g) in 04-SEITENKARTE's cross-reference block from **open** to **settled** and rewrite it as: 'settled — agent.protokoll_lesen is a catalogue row in §14.2, and this map gates /agenten/[agent]/protokoll and the step chain of /aufgaben/[id] on it while keeping page-level agent.lesen for the run header.' Leave item (k) as the block's one remaining open item, so the block's status line reads honestly.

### geld, zeit, agent-vertraege — 9 remaining

1. **K-12's canonical-payload sketch in the authoritative conventions file was never amended and still sponsors the deleted spelling. K-12 (00-KONVENTIONEN.md lines 399-406) states `je Steuerzeile { satz, netto_cent, steuer_cent, hinweistext }` plus `abrechnungsart · bauabzugsteuer_cent · kleinbetrag · leistungszeitraum`. The owner (02-datenmodell/05-FINANZEN.md §5.3) declares `steuersatz_gruppe`/`satz_bp`/`kategorie` and `befreiungsgrund_code`/`befreiungsgrund_text`, a `bauabzugsteuer{}` object, `ist_kleinbetrag`, `leistung_von`/`leistung_bis`, and `abrechnungsart` per position rather than per document. 05-API-KARTE.md §C.14 line 1004ff and 05-FINANZEN.md line 36 both state flatly that there is no `steuersatz` table, no `steuersatz_id`, no `prozent_bp` and **no `hinweistext`** — a field K-12 still names. Because 00-KONVENTIONEN §0 says the conventions file wins on conflict, the top-of-chain authority currently mandates a payload the owner document forbids, and the payload is the hash input.**
   - Documents: 00-KONVENTIONEN.md (K-12, lines 399-406) vs 02-datenmodell/05-FINANZEN.md §5.3 (lines 1796-1850) and 05-API-KARTE.md §C.14 (lines 981-1010)
   - Fix: Replace K-12's four-line sketch with the owner's spelling, or reduce it to a pointer: `leistender{…} · empfaenger{…} · je Steuerzeile { steuersatz_gruppe, kategorie, satz_bp, netto_cent, steuer_cent, befreiungsgrund_code, befreiungsgrund_text } · bauabzugsteuer{…} · ist_kleinbetrag · leistung_von/leistung_bis — the full shape is declared once by 02-datenmodell/05-FINANZEN.md §5.3 (`cse.rechnung.v1`)`. Delete `hinweistext`, `bauabzugsteuer_cent`, `kleinbetrag` and `leistungszeitraum` from K-12.

2. **05-API-KARTE.md §D.6's `KanonischeRechnung` claims to be the owner's payload "field-for-field" but omits eight field groups, so a payload built from it canonicalises to different bytes and therefore a different hash from one built to 05-FINANZEN §5.3. Missing entirely: the whole `zahlung { bankkonto { iban, bic, kontoinhaber }, zahlungsmittel_code, zahlungsbedingung_text, zahlungsziel_tage, faellig_am, skonto_bp, skonto_tage }` object, `zuschlaege[]`, `abzuege[]`, `vereinnahmung_geplant_am`, `ueberweisungsbetrag_cent`, and on `positionen[]` both `beschreibung` and `preis_basismenge`; `bauabzugsteuer.freistellungsbescheinigung` is truncated to `{ nummer }` against the owner's `{ nummer, finanzamt, gueltig_von, gueltig_bis, umfang }`. This is exactly the field set 05-FINANZEN §5.3 argues at length must be present ("changing the entity's bank account in 2028 would make every re-render of a 2026 invoice show a different IBAN while the hash still verified, and BT-34/BT-49 are mandatory in XRechnung"). Because §5.3 also rules that nulls are written explicitly and never omitted, the omission is not benign — it changes the digest. §C.14's prose sketch (lines 990-1000) has the same holes for `zuschlaege`, `abzuege`, `vereinnahmung_geplant_am`, `ueberweisungsbetrag_cent` and `reverse_charge_grundlage`. Separately, `buildKanonischePayload` is declared with one parameter (`(r: KanonischeRechnung): Buffer`, 05-API-KARTE line 1632) but called with two in the owner's finalisation transaction (`buildKanonischePayload(await tx.ladeRechnungVollstaendig(r.id), kopf)`, 05-FINANZEN §5.6).**
   - Documents: 05-API-KARTE.md §D.6 (lines 1587-1632) and §C.14 (lines 990-1000) vs 02-datenmodell/05-FINANZEN.md §5.3 (lines 1809-1849) and §5.6 (line ~1948)
   - Fix: Extend `KanonischeRechnung` with `vereinnahmung_geplant_am`, `zuschlaege[]`, `abzuege[]`, `ueberweisungsbetrag_cent`, the full `zahlung{}` object and the full `freistellungsbescheinigung{}`, and add `beschreibung` and `preis_basismenge` to `positionen[]` — or delete the type and have §D.6 cite 05-FINANZEN §5.3 as the single declaration, the way §C.14 already says it does. Reconcile `buildKanonischePayload`'s arity with the two-argument call in 05-FINANZEN §5.6.

3. **How money crosses HTTP is stated two incompatible ways inside one document. R-12 (05-API-KARTE.md line 27) and the §B.9 serialisation table (line 305) say money crosses as an **integer number of cents**, validator `z.number().int()` plus `assertSafeCents`, "never a formatted string" — and R-15 pointedly reserves the decimal-string form for quantities (`zMenge` rejects a JSON number). Three later passages in the same document describe R-12 as doing the opposite: line 1002 "the wire rule R-12 (money as a decimal **string** in HTTP bodies)", line 1105 for `GET/PATCH /api/agenten/budget` "the wire field is `budget_cent` as a decimal string", line 1585 "not the decimal strings R-12 puts on the wire". 06-AGENTEN-FREIGABEN.md line 883 takes the string side too ("Money is bigint cents, serialised as a decimal string across the JSON boundary and never touched by JSON number parsing"), which also contradicts `assertSafeCents(c: Cent): number; // for JSON`. This decides the Zod schema at every money boundary in the platform.**
   - Documents: 05-API-KARTE.md R-12 (line 27) and §B.9 (line 305) vs 05-API-KARTE.md lines 1002, 1105, 1585 and 06-AGENTEN-FREIGABEN.md line 883
   - Fix: Pick one and state it once. R-12 plus the §B.9 table plus line 671 (`stundensatz_intern_cent` "crosses HTTP as integer cents") are the majority and the validator-backed reading, so rewrite lines 1002, 1105 and 1585 to say R-12 puts money on the wire as an integer number of cents, and change 06-AGENTEN §5.4's comment to "bigint cents, serialised as a JSON integer via assertSafeCents".

4. **`KanonischeRechnung` types every money field as TypeScript `number` (`einzelpreis_cent`, `netto_cent`, `steuer_cent`, `netto_gesamt_cent`, `steuer_gesamt_cent`, `brutto_cent`, `abzug_brutto_cent`, `zahlbetrag_cent`, `grundlage_cent`, `einbehalt_cent`, `kleinbetrag_grenze_cent`) while the comment four lines above it says "Amounts are JSON INTEGERS OF CENTS (**bigint here**)". The same document declares the branded `export type Cent = bigint & {…}` at line 1329, and 08-PR-PLAN PR 1 acceptance (8) and (10) require a type test proving a `number` cannot reach a money parameter and a lint failure on a fixture typing money as `number`. Either the comment or the types are wrong; as written the block is self-contradicting and is the only place in the thirteen documents where a money field is typed `number`.**
   - Documents: 05-API-KARTE.md §D.6 (lines 1584-1625) vs 05-API-KARTE.md §D.2 (line 1329) and 08-PR-PLAN.md PR 1 acceptance (8)/(10) (line 80)
   - Fix: Type the fields `Cent` (bigint) in the TypeScript face and say the JSON projection goes through `assertSafeCents` at serialisation, or drop the "(bigint here)" parenthetical and state explicitly that `KanonischeRechnung` is the post-`assertSafeCents` JSON shape and is exempt from the money-is-bigint lint. Whichever is chosen, name it, because the lint rule of PR 1(10) will otherwise fire on this type.

5. **The `ust_split` tool argument is spelled two ways, and the finance owner uses the name it elsewhere declares does not exist. 06-AGENTEN-FREIGABEN.md §5.4 tool 4 (line 863) declares `{ art: 'ust_split'; positionen: { betrag_token: string; steuersatz_gruppe_id: BezugHandle }[] }`. 02-datenmodell/05-FINANZEN.md §12.2 (line 3155) writes `positionen: { betrag_token, steuersatz_id: BezugHandle }[]`. 05-FINANZEN's own line 36 states "there is no `steuersatz` table, no `steuersatz_id` column", and K-21 fixes `*.steuersatz_gruppe_id` as the only spelling — so §12.2 contradicts its own document, the tool owner, and the convention at once. This is a K-10 entry-point argument on the one tool branch that computes VAT.**
   - Documents: 02-datenmodell/05-FINANZEN.md §12.2 (line 3155) vs 06-AGENTEN-FREIGABEN.md §5.4 (line 863), 02-datenmodell/05-FINANZEN.md §0 (line 36) and 00-KONVENTIONEN.md K-21
   - Fix: Change `steuersatz_id` to `steuersatz_gruppe_id` in 05-FINANZEN §12.2's `ust_split` row. While there, 02-datenmodell/02-CRM-OPERATIONS.md §7.2 (line 2210) describes `berechne_preis` as accepting "`AngebotHandle`, plus the Kalkulation as a `BezugHandle`" — no branch of the owner's signature takes a second Kalkulation argument; art `kalkulation` takes only `bezug: AuftragHandle | AngebotHandle`. Reword to match.

6. **05-API-KARTE §E's ArbZG test rows still assert the withdrawn breach vocabulary that the same document's §D.4 declares deleted. T-08 expects "one `tageshoechstarbeitszeit` breach at 660 min" and T-08b expects "a `tageshoechstarbeitszeit` breach"; T-09 expects a "`ruhezeit` breach". §D.4 lines 1461-1463 state explicitly: "The draft collapsed the two §3 limits into one 'tageshoechstarbeitszeit' … a value the enum does not have, so the finding could not be written at all", and §D.4's own `ArbzgRegel` union carries the six values `tagesarbeitszeit_ueber_8h · tagesarbeitszeit_ueber_10h · ruhezeit_unter_11h · pause_fehlt_ueber_6h · pause_fehlt_ueber_9h · ausgleichszeitraum_ueberschritten`. The count is wrong too, not just the spelling: 08-PR-PLAN PR 1(5) and 04-PLANUNG-ZEIT §18 both require the 6h+5h case to raise **both** `tagesarbeitszeit_ueber_8h` **and** `tagesarbeitszeit_ueber_10h` at 660 minutes, because collapsing them makes the 10h finding unpersistable — T-08 asserts exactly one. T-08 is the flagship cross-entity ArbZG test the whole of K-06 exists to make possible.**
   - Documents: 05-API-KARTE.md §E rows T-08, T-08b, T-09 (lines 1888-1890) vs 05-API-KARTE.md §D.4 (lines 1461-1468), 02-datenmodell/04-PLANUNG-ZEIT.md §3 and §18 (line 2079ff) and 08-PR-PLAN.md PR 1(5)-(6) (line 80)
   - Fix: Rewrite T-08 to expect two findings, `tagesarbeitszeit_ueber_8h` and `tagesarbeitszeit_ueber_10h`, both with `minuten = 660`; rewrite T-08b's regel to `tagesarbeitszeit_ueber_8h` (the 660 > 480 case it describes); rewrite T-09's regel to `ruhezeit_unter_11h`. No enum or type changes are needed — only the three test rows.

7. **K-06's storage sketch still omits `person_id`, and 01-ORDNERSTRUKTUR records that as an unresolved obligation on 00-KONVENTIONEN in its own words. K-06 (00-KONVENTIONEN.md lines 193-197) lists `zuordnung_quelle_id`, `quelle`, `aktiv`, `beginn_utc`, `ende_utc` and nothing else. The owner, 02-datenmodell/04-PLANUNG-ZEIT.md §5.12, declares `person_id uuid not null FK → person.id — The query key`, with `fenster_person_idx on (person_id, beginn_utc) where aktiv` as "*the* index", and §6.3's function body filters `where f.person_id = p_person`. 01-ORDNERSTRUKTUR.md §6.1 (lines 1798-1811) spells out why the alternative is wrong — resolving person → anstellung → einsatz_zuordnung inside the definer function cannot reach an `ist` window projected from a `zeiteintrag`, so it silently under-counts actual worked time, which is the precise K-06 failure mode — and its §26 obligation table (line 3435) says "K-06's `zeit_intern.arbeitszeit_fenster` sketch gains `person_id` … until it lands the convention's sketch and the owner's DDL disagree in the owner's favour, which is the one direction §0 does not normally permit". It has not landed. 03-AUTH-BERECHTIGUNGEN.md §11 (line 1919) and 05-API-KARTE.md §C.10 (line 797) reproduce the incomplete five-column list.**
   - Documents: 00-KONVENTIONEN.md K-06 (lines 190-197) vs 02-datenmodell/04-PLANUNG-ZEIT.md §5.12 (lines 1139-1155), 01-ORDNERSTRUKTUR.md §6.1 (lines 1798-1811) and §26 (line 3435), 03-AUTH-BERECHTIGUNGEN.md §11 (line 1919), 05-API-KARTE.md §C.10 (line 797)
   - Fix: Add `person_id uuid` ("the query key") to K-06's storage block, and note that `mandant_id` and `anstellung_id` are also stored but never returned. Then delete the obligation row from 01-ORDNERSTRUKTUR §26, and add `person_id` to the column lists in 03-AUTH §11 and 05-API-KARTE §C.10 so all five restatements match the owner.

8. **The K-16(b) micro-cent → cent conversion sites are closed at two by the agent documents and opened to three by finance. 02-datenmodell/06-RADAR-KI-INHALT.md §1.12 states "The single conversion to cents is at the budget boundary, and there are **exactly two sites**" (`agent_aufgabe.kosten_cent` and the REP-01 expense figure), and 06-AGENTEN-FREIGABEN.md §8.2 calls `agent_aufgabe.kosten_cent` "one of the **two** K-16(b) conversion sites". 02-datenmodell/05-FINANZEN.md §0 (lines 538-541) says of `eingangsrechnung_extraktion.kosten_cent`: "**This column is a third conversion site**, beside the two `06-AGENTEN-FREIGABEN.md` §8.2 enumerates". The two formulas also sum different tables: the agent owner writes `div(Σ agent_kosten.kosten_mikrocent + 5000, 10000)`, 05-FINANZEN §12.1 writes `div(Σ agent_schritt.kosten_mikrocent + 5000, 10000)`. The carve-out itself is correctly confined — the eight `*_mikrocent` columns on the five agent-ledger tables agree in 06-RADAR §1.12, 01-ORDNERSTRUKTUR §9.4, 07-INTEGRATIONEN §2 and 05-API-KARTE §D.2, and every other domain document explicitly denies having one — so this is a count and a source-table disagreement, not a containment breach.**
   - Documents: 02-datenmodell/06-RADAR-KI-INHALT.md §1.12 (lines 693-697) and 06-AGENTEN-FREIGABEN.md §8.2 (lines 1760-1762) vs 02-datenmodell/05-FINANZEN.md §0 (lines 538-541) and §12.1 (line 3191)
   - Fix: Change 06-RADAR §1.12 and 06-AGENTEN §8.2 to say three sites and name `eingangsrechnung_extraktion.kosten_cent` as the third, since the finance document already states the rounding rule at that column as K-16(b) requires. Then reconcile the summed table: pick `agent_kosten.kosten_mikrocent` (the ledger row) or `agent_schritt.kosten_mikrocent` (the step) and use it in both formulas.

9. **The K-08 register row for `app.checkin_verbrauchen` — the function that creates the §17 MiLoG time record — still carries the three-argument signature that no other document uses. 00-KONVENTIONEN.md K-08 (line 278) writes `app.checkin_verbrauchen(token_hash, geraet_zeit, ip)`. The owner (02-datenmodell/04-PLANUNG-ZEIT.md §9.1, line 1639) and 01-KERN.md §3.3 (line 1107), 03-AUTH-BERECHTIGUNGEN.md (line 728), 05-API-KARTE.md §B (line 520) and 07-INTEGRATIONEN.md (line 957) all declare `(p_token_hash text, p_geraete_zeit timestamptz, p_ip inet, p_user_agent text, p_geo jsonb default null)`. Four documents state the discrepancy as an obligation on 00-KONVENTIONEN and say why it matters — Postgres grants resolve per exact signature, so a grant written against the three-argument form reaches no function, and 01-KERN §14 test 54 plus 04-PLANUNG-ZEIT §18 test 38 assert that exactly one signature exists in `pg_proc`.**
   - Documents: 00-KONVENTIONEN.md K-08 (line 278) vs 02-datenmodell/04-PLANUNG-ZEIT.md §9.1 (line 1639) and §2.3 item 14 (line 489), 02-datenmodell/01-KERN.md §3.3 (lines 1107, 1116), 03-AUTH-BERECHTIGUNGEN.md (line 728), 05-API-KARTE.md (lines 520, 2061), 07-INTEGRATIONEN.md (lines 957, 968, 2142)
   - Fix: Change K-08's register row to `app.checkin_verbrauchen(p_token_hash text, p_geraete_zeit timestamptz, p_ip inet, p_user_agent text, p_geo jsonb default null)`, then remove the four "K-08 must be amended" notes that currently point at it.
