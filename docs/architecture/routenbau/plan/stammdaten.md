# PLAN stammdaten
## Gemeinsames
Alle fuenf Routen teilen denselben Zustand und denselben Bauweg — keine ist ein Sonderfall.

1. ALLE FUENF FEHLEN GANZ. Es gibt kein Verzeichnis /home/user/cse-platform/src/app/portal/[mandant]/stammdaten/. Die Adressen antworten heute ueber den Catch-all /home/user/cse-platform/src/app/portal/[mandant]/[...rest]/page.tsx -> MandantUnterseite mit 200 und 'noch nicht gebaut'. Es ist also kein Platzhalter je Route, sondern der gemeinsame Auffangboden.

2. KEINE MIGRATION NOETIG — das ist der wichtigste Befund. Alle fuenf Tabellen (abwesenheitsart, antragsart, belagsart, qualifikation, reinigungsklasse) stehen in drizzle/ UND in der laufenden Datenbank, mit forced RLS, mit kern.verhindere_loeschung als DELETE- und TRUNCATE-Trigger, und alle sind geseedet (7 / 3 / 6 / 4 / 4 Zeilen). Deshalb ist keine Route 'gross'.

3. ES FEHLT GENAU EINE SCHICHT: der Pflegedienst. src/server/services/stammdaten/ existiert nicht. Was es gibt, sind Leser fuer FREMDE Zwecke — leseAbwesenheitsarten und leseAntragsarten in src/server/services/mitarbeiter/antraege.ts sind Auswahllisten fuer das Mitarbeiterportal (Teilmenge der Spalten, ohne archivierte Zeilen, ohne die Verwaltungsfelder), ladeKalkulationsgrundlage liest Belagsarten zum Rechnen. Keine einzige Funktion legt an, aendert oder archiviert. Darum ist jede der fuenf Routen 'mittel': Seite + Dienstfunktionen, kein SQL.

4. DAS RECHT STEHT IM KATALOG, ABER AUF DER WARTELISTE. stammdaten.verwalten ist in /home/user/cse-platform/src/server/auth/katalog.generiert.ts:207 gefuehrt, gebunden an super_admin, admin und leitung. Es steht aber auch in /home/user/cse-platform/tests/kern/katalog-unbenutzt.ts:191 — der eingefrorenen Liste der Schluessel, die noch kein Code prueft. Diese Menge darf nur schrumpfen: die erste gebaute Stammdatenseite muss diese Zeile streichen, sonst behauptet die Liste weiter, das Recht sei ungenutzt.

5. GESCHRIEBEN WIRD UEBER API-ROUTEN, NICHT UEBER SERVER ACTIONS. Im ganzen Portalbaum gibt es kein 'use server'; Schreibwege laufen als Route Handler unter src/app/api/ (Muster: /home/user/cse-platform/src/app/api/einstellungen/mahnwesen/route.ts, 115 Zeilen, dazu die Seite mit 259). Die fuenf Seiten brauchen je eine API-Route unter src/app/api/stammdaten/.

6. DIE HUELLE IST UEBERALL DIESELBE. mandantTor(pfad, slug) aus /home/user/cse-platform/src/app/portal/unterseite.tsx -> MandantAntwort bei allem ausser 'ok' (kuerzester Weg, siehe einstellungen/steuer/page.tsx), dann db().begin(SCHNAPPSCHUSS, ...) + withTenant + kontext.abfrage, Darstellung in PortalRahmen mit DataTable aus src/components/ui/. Den Sprachumschalter und die Beschriftungen bringt PortalRahmen selbst mit (internBeschriftungen); der Seitentext bleibt deutsch, weil die Begriffe rechtlich tragen.

7. VIER DER FUENF KATALOGE SIND HEUTE PLATTFORMWEIT UND DAMIT NICHT PFLEGBAR. abwesenheitsart, antragsart und qualifikation sind vollstaendig global geseedet (mandant_id IS NULL); die RLS laesst Mandantenschreiben nur fuer eigene Zeilen zu, antragsart verbietet ist_system zusaetzlich ausdruecklich. Nur reinigungsklasse und belagsart sind mandantengebunden. Eine Seite, die an globalen Zeilen einen Bearbeiten-Knopf zeigt, verspricht etwas, das die Datenbank abweist — die Unterscheidung 'Plattformkatalog' gegen 'eigene Zeile' muss auf dem Bildschirm stehen.

8. JEDER DER FUENF KATALOGE TRAEGT EINE UNBEANTWORTETE KUNDENFRAGE, und die Seeddaten tragen die Marke bereits: O-17 (+ O-349) fuer belagsart, O-55 fuer reinigungsklasse, O-139 (+ O-138) fuer abwesenheitsart, O-142 fuer antragsart, O-341/O-343/O-144/O-107 fuer qualifikation. Die Vorbildseite dafuer ist /home/user/cse-platform/src/app/portal/[mandant]/einstellungen/abrechnungsarten/page.tsx: die offene Frage steht dort VOR der Liste, nicht als Fussnote, und die Statuspille benutzt nur das feste Vokabular aus DESIGN §5.

9. OHNE EINSTIEG WAEREN ALLE FUENF WAISEN. /home/user/cse-platform/src/server/registry/navigation.ts kennt keinen Schluessel 'stammdaten', und die Seitenkarte fuehrt keine Indexroute /portal/[mandant]/stammdaten. Die Seiten muessen als Karten in die KARTEN-Liste von /home/user/cse-platform/src/app/portal/[mandant]/einstellungen/page.tsx (Zeile 30 ff.), genau wie einstellungen/abrechnungsarten — sonst sind sie nur per getippter Adresse erreichbar. Zusaetzlich gehoeren Querverweise dorthin, wo der Wert wirkt (Raumbuch -> Belagsarten/Reinigungsklassen, Nachweisregister -> Qualifikationen, Antragseingang -> Antragsarten), und zwar nur, wenn der Benutzer das Zielrecht haelt (AUT-06, Muster haeltRechte in abrechnungsarten/page.tsx).

10. MODULSPERRE: 'stammdaten' steht weder in QUERSCHNITT noch in GEWERK_FUER_MODUL in /home/user/cse-platform/src/server/registry/modul.ts. modulAktiv gibt deshalb ueber den Rueckfall 'unbekanntes Modul bleibt offen' (Zeile 135) true zurueck — die Seiten oeffnen in allen vier Gesellschaften, was der Seitenkarte §1.7 entspricht, aber aus Versehen richtig ist. 'stammdaten' gehoert in QUERSCHNITT, damit die Aussage absichtlich wird; das ist eine Zeile und sollte im selben Zug fallen.
## Routen

### /portal/[mandant]/stammdaten/abwesenheitsarten
- zustand: fehlt
- dienst_vorhanden: Kein Pflegedienst. Es gibt nur Leser: src/server/services/mitarbeiter/antraege.ts -> leseAbwesenheitsarten(kontext, sprache) (Mitarbeiter-Auswahlliste: id, schluessel, bezeichnung aus bezeichnung_i18n, zaehltAufUrlaubskonto, bezahlt, nachweisPflichtAbTagen; filtert archiviert_am is null) und src/server/services/abwesenheit/index.ts -> interne pruefeArt (liest id, bezeichnung, bezahlt, Zeile 175). Keine Funktion legt an, aendert oder archiviert. src/server/services/stammdaten/ existiert nicht.
- tabellen_vorhanden: True
- recht: stammdaten.verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/personal/abwesenheiten/page.tsx
- aufwand: mittel
- blocker: O-139 ist offen (bezahlt / Nachweis ab Tag N / Lohnartenschluessel je Art): 5 der 7 geseedeten Zeilen tragen bezahlt = NULL, also 'ungeklaert' und nicht 'nein'. Zweiter, harter Punkt: alle 7 Zeilen sind global (mandant_id IS NULL), die RLS-Policy t_katalog_pflege laesst Schreiben nur fuer mandant_id = app.aktiver_mandant() zu — der Mandanten-Admin sieht den Plattformkatalog, darf ihn aber nicht aendern. Eine Seite mit Bearbeiten-Knopf an diesen Zeilen luegt. Dazu der Trigger kern.abwesenheitsart_schutz: schluessel einer BENUTZTEN Art ist fest, bezahlt faellt nicht auf NULL zurueck, ist_gesundheitsbezogen einer benutzten Art ist fest — das Formular muss das vorher wissen, sonst kommt eine 500 statt eines Satzes.

**Plan**

Die Seite zeigt den Abwesenheitskatalog als Tabelle: Bezeichnung + schluessel, bezahlt als DREI Zustaende (ja / nein / ungeklaert mit O-139-Marke, nie stillschweigend 'nein'), zaehlt_auf_urlaubskonto, erzeugt_stundenkonto_bewegung, ist_gesundheitsbezogen als Art-9-Kennzeichen, nachweis_pflicht_ab_tagen, lohnart_schluessel und farbe_token. Plattformzeilen (mandant_id IS NULL) stehen mit dem Vermerk 'Plattformkatalog — hier nicht aenderbar' da, mandanteigene Zeilen tragen Bearbeiten und Archivieren. Neu und Aendern laufen ueber eine API-Route unter src/app/api/stammdaten/abwesenheitsarten (Muster: src/app/api/einstellungen/mahnwesen/route.ts), die Pflege selbst als neue Dienstfunktionen in src/server/services/stammdaten/abwesenheitsart.ts. Geloescht wird nie: archiviert_am setzen (Invariante 8, kern.verhindere_loeschung liegt als Trigger darauf). Die Seite benennt je Zeile, welche Bildschirme der Wert steuert — bezahlt und nachweis_pflicht_ab_tagen steuern die Krankmeldung im Mitarbeiterportal, zaehlt_auf_urlaubskonto das Urlaubskonto.

### /portal/[mandant]/stammdaten/antragsarten
- zustand: fehlt
- dienst_vorhanden: Kein Pflegedienst. Leser: src/server/services/mitarbeiter/antraege.ts -> leseAntragsarten(kontext, sprache) (id, schluessel, uebersetzte bezeichnung, erfordert_zeitraum / _abwesenheitsart / _einsatz / _tauschpartner) und src/server/services/abwesenheit/antrag.ts, das antragsart in listeAntraege/findeAntrag/entscheideAntrag joint (Zeilen 91, 245). Kein Anlegen, kein Aendern, kein Archivieren.
- tabellen_vorhanden: True
- recht: stammdaten.verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/personal/antraege/page.tsx
- aufwand: mittel
- blocker: O-142 ist offen (welche weiteren Antragsarten die Gruppe fuehrt — unbezahlte Freistellung, Freizeitausgleich, Schichtabgabe, Stammdatenaenderung). Dazu ein struktureller Punkt, der die Seite fast leer macht: alle drei geseedeten Zeilen (urlaub, krankmeldung, schichttausch) sind global (mandant_id IS NULL) UND ist_system = true, und die RLS-Policy t_katalog_pflege verbietet im WITH CHECK ausdruecklich jede Zeile mit ist_system. Solange O-142 unbeantwortet ist, gibt es also keine einzige pflegbare Zeile — die Seite ist bis dahin ein Lesekatalog mit einem Anlegen-Formular fuer Arten, nach denen niemand gefragt hat. Das gehoert auf die Seite geschrieben, nicht in einen Kommentar.

**Plan**

Die Seite listet die Antragsarten mit ihren sechs Schaltern: erfordert_zeitraum, erfordert_abwesenheitsart, erfordert_einsatz, erfordert_tauschpartner, erzeugt_abwesenheit, ist_stammdatenaenderung — und sagt je Schalter, welches Feld er im Antragsformular des Mitarbeiterportals ein- oder ausblendet, weil leseAntragsarten genau diese Flags dorthin traegt. Systemzeilen stehen gesperrt da, mit dem Grund (ist_system, §-gebundene Kette Antrag -> Abwesenheit) statt mit einem toten Knopf. Darueber steht die O-142-Marke: dieser Katalog ist nicht vollstaendig, er ist unbeantwortet. Mandanteigene Arten koennen angelegt, umbenannt und archiviert werden (archiviert_am, nie DELETE); die Pflege kommt als src/server/services/stammdaten/antragsart.ts plus API-Route. bezeichnung_i18n wird im Formular fuer de/en/ar/tr gefuehrt, weil die Art im Mitarbeiterportal erscheint.

### /portal/[mandant]/stammdaten/belagsarten
- zustand: fehlt
- dienst_vorhanden: Kein Pflegedienst. Verbraucher: src/server/services/kalkulation/raumbuch.ts -> ladeKalkulationsgrundlage (liest Belagsart mit Leistungswert und traegt ist_platzhalter bis in kalkuliere) und src/server/services/raumbuch/import.ts, das intern die HEUTE gueltige Zeile je code sucht (Zeilen 189-192, nicht exportiert). Keine Funktion legt eine Belagsart an oder datiert sie um.
- tabellen_vorhanden: True
- recht: stammdaten.verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/objekte/[id]/raumbuch/page.tsx
- aufwand: mittel
- blocker: O-17 ist offen (Leistungswerte m²/h je Belagsart — Quelle und wer sie bestaetigt). Alle 6 geseedeten Zeilen tragen ist_platzhalter = true und quelle = 'Branchenuebliche Groessenordnung — nicht bestaetigt (O-17)'. Verwandt und ebenfalls offen: O-349 (rechnet ein Glasreinigungsrevier auf Glasflaeche, mit welchem Leistungswert — den fuehrt heute kein Katalog). Kein Blocker, aber eine Falle: die RLS-Policy t_mandant liest belagsart mit objekt.lesen, waehrend das Routenregister die Seite mit stammdaten.verwalten oeffnet. Beide Rechte haengen im Katalog an denselben Rollen (super_admin, admin, leitung), aber eine Rolle, der objekt.lesen entzogen wurde, saehe eine leere Tabelle statt einer Sperre.

**Plan**

Die Seite zeigt die HEUTE gueltigen Belagsarten (code, Bezeichnung, Leistungswert m²/h, Quelle, gueltig ab) mit der O-17-Marke vorangestellt, wie einstellungen/abrechnungsarten es fuer O-04 tut, und darunter je code die Historie. Der Kern der Seite ist, dass eine Aenderung des Leistungswerts KEIN Update ist: die Exclusion-Constraint belagsart_zeitraum_eindeutig und die Fremdschluessel aus raum, kalkulation_position und raumbuch_import_zeile bedeuten, dass die alte Zeile mit gueltig_bis geschlossen und eine neue ab dem gewaehlten Datum eingefuegt wird — sonst aenderte sich rueckwirkend der Preis bereits gerechneter Kalkulationen. Das Formular verlangt darum ein gueltig_ab und sagt, ab wann der neue Wert rechnet und welche Kalkulationen er NICHT beruehrt. Die Datierung und die Ueberschneidungspruefung gehoeren als getestete Funktion nach src/server/services/stammdaten/belagsart.ts, nicht in die Seite; leistungswert_qm_pro_stunde ist numeric(10,3) und wird nur formatiert, nie in der Komponente gerechnet.

### /portal/[mandant]/stammdaten/qualifikationen
- zustand: fehlt
- dienst_vorhanden: Kein Pflegedienst. Verbraucher gibt es reichlich: src/server/services/nachweis/uebersicht.ts -> nachweislage, src/server/services/nachweis/ablauf.ts (60/30/7-Waechter), src/server/services/nachweis/gueltigkeit.ts, src/server/services/nachweis/tor.ts, src/server/services/dienstplan/einteilung.ts und src/server/services/security/eventbesetzung.ts lesen qualifikation fuer die SEC-04-Sperre. Keine davon legt eine Qualifikation an, aendert oder archiviert sie.
- tabellen_vorhanden: True
- recht: stammdaten.verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/personal/nachweise/page.tsx
- aufwand: mittel
- blocker: Vier offene Fragen haengen an genau den Feldern, die diese Seite pflegt: O-341 (Ablauffrist des Bewacherausweises — standard_gueltigkeit_monate bleibt deshalb absichtlich leer, alle vier Zeilen bis auf unterweisung_dguv1 haben NULL), O-343 (gehoeren die Urkunden in die Plattform — erfordert_dokument), O-144 (welche Kategorien fuer die Nachweisberichte), O-107 (welche Eignungs- und Personennachweise die Gewerke verlangen). Dazu, wie bei den Abwesenheitsarten: alle 4 geseedeten Zeilen sind global (mandant_id IS NULL), und die Policies q_schreiben/q_aendern lassen globale Zeilen nur app.ist_super_admin() zu — ein Mandanten-Admin mit stammdaten.verwalten sieht den gesetzlichen Katalog, darf aber nur mandanteigene Qualifikationen anlegen und aendern. Der Trigger kern.qualifikation_katalog_schutz macht schluessel unveraenderlich.

**Plan**

Die Seite fuehrt den Qualifikationskatalog, auf den sich Posten (einsatzanforderung) und Nachweise berufen: Bezeichnung + schluessel, Kategorie aus dem Enum qualifikation_kategorie (gesetzlich, fachlich, fuehrerschein, gesundheit, intern), Rechtsgrundlage, laeuft_ab mit standard_gueltigkeit_monate, warnung_tage (Vorgabe 60/30/7), blockiert_einsatz und erfordert_dokument. Je Zeile steht ihre Wirkung und ihre Reichweite: wie viele nachweis-Zeilen und welche einsatzanforderung sie referenzieren — denn blockiert_einsatz einzuschalten laesst ab diesem Moment Einteilungen scheitern, und wer das ohne Zahl davor tut, erfaehrt es vom Dienstplan. Leere Felder tragen ihre O-Nummer sichtbar (standard_gueltigkeit_monate -> O-341, erfordert_dokument -> O-343), statt wie gepflegte Angaben auszusehen. Gesetzliche Plattformzeilen sind gesperrt mit Grund; mandanteigene Zeilen sind anlegbar und archivierbar (archiviert_am + archiviert_von, nie DELETE). bezeichnung_i18n ist per Check-Constraint auf de/en/ar/tr begrenzt — das Formular bietet genau diese vier.

### /portal/[mandant]/stammdaten/reinigungsklassen
- zustand: fehlt
- dienst_vorhanden: Kein Pflegedienst. Verbraucher: src/server/services/reinigung/revier.ts -> ladeRaeume / setzeRaeume, src/server/services/raumbuch/import.ts (interne code-Zuordnung, Zeile 195) und src/server/services/kalkulation/raumbuch.ts -> ladeKalkulationsgrundlage. Keine Funktion legt eine Reinigungsklasse an, benennt sie um oder archiviert sie.
- tabellen_vorhanden: True
- recht: stammdaten.verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/objekte/[id]/raumbuch/page.tsx
- aufwand: mittel
- blocker: O-55 ist offen (welche Reinigungsklassen verwendet werden — DIN 77400 oder ein eigenes Schema). Alle 4 geseedeten Zeilen (RK1 Buero und Besprechung, RK2 Verkehrsflaeche, RK3 Sanitaer, RK4 Technik und Lager) tragen ist_platzhalter = true. Kein technischer Blocker: die Tabelle ist mandantengebunden (mandant_id not null), es gibt also anders als bei den drei Personal-Katalogen wirklich eigene, pflegbare Zeilen.

**Plan**

Die Seite fuehrt die Reinigungsklassen dieser Gesellschaft in ihrer Sortierreihenfolge: code, Bezeichnung, Beschreibung, sortierung — mit der O-55-Marke darueber, weil die vier Klassen geraten und nicht bestaetigt sind. Anlegen, umbenennen und umsortieren sind einfache Updates (kein Datumsbereich, kein Leistungswert — anders als bei den Belagsarten), Archivieren setzt archiviert_am; geloescht wird nie (Trigger kern.verhindere_loeschung). Vor dem Archivieren nennt die Seite die Zahl der raum- und raumbuch_import_zeile-Zeilen, die noch daran haengen — der Teilindex reinigungsklasse_code_uk gibt den code erst nach dem Archivieren wieder frei, und eine Klasse stillschweigend wegzunehmen liesse Raeume ohne Einstufung zurueck. Pflege als src/server/services/stammdaten/reinigungsklasse.ts plus API-Route; die Seite bleibt eine Liste ohne Rechnung.

## KRITIK (5 geprueft, 7 Korrekturen)

- **pfad**: /portal/[mandant]/stammdaten/abwesenheitsarten | **behauptet**: Blocker: "die RLS-Policy t_katalog_pflege laesst Schreiben nur fuer mandant_id = app.aktiver_mandant() zu — der Mandanten-Admin sieht den Plattformkatalog, darf ihn aber nicht aendern". Dazu gemeinsames #2/#3: "KEINE MIGRATION NOETIG" / "Seite + Dienstfunktionen, kein SQL". Aufwand mittel, Plan verspricht "Neu und Aendern laufen ueber eine API-Route" und Pflegefunktionen. | **wirklich**: Nicht nur der Mandanten-Admin — GAR NIEMAND kann die 7 globalen Zeilen aendern. t_katalog_pflege ist die einzige Schreibpolicy und hat, anders als qualifikation (q_schreiben/q_aendern tragen ausdruecklich `app.ist_super_admin()`), KEINEN Super-Admin-Zweig; die Tabelle laeuft mit `force row level security`, cse_app haelt nur select/insert/update. Damit ist die Antwort auf O-139 (bezahlt bei 5 von 7 Zeilen NULL) durch keine Seite eintragbar — es braucht eine Policy-Migration oder eine Definer-Funktion, also SQL. Das widerspricht dem als "wichtigster Befund" verkauften "keine Migration noetig". Verschaerfend: die 5 NULL-Arten sind heute nicht nur ungeklaert, sie sind unbenutzbar — pruefeArt wirft ArtUngeklaertFehler. Zweite, unerwaehnte Falle: abwesenheitsart_key ist `unique nulls not distinct (mandant_id, schluessel)`, eine Mandantenzeile 'urlaub' ist also erlaubt, und leseAbwesenheitsarten filtert nur archiviert_am — der Mitarbeiter saehe dann zwei Eintraege "Urlaub". | **beleg**: psql: select count(*), count(mandant_id) from abwesenheitsart → 7 / 0; select policyname, cmd, with_check from pg_policies where tablename='abwesenheitsart' → nur t_katalog_pflege (ALL) mit `mandant_id = app.aktiver_mandant()`, kein ist_super_admin; select relforcerowsecurity from pg_class where relname='abwesenheitsart' → t. drizzle/0073_abwesenheit.sql:486-497 (enable/force + Policies), :587-588 (grant select, insert, update on abwesenheitsart to cse_app), :668 (revoke delete). Gegenprobe qualifikation: pg_policies q_schreiben/q_aendern enthalten `(mandant_id is null) and app.ist_super_admin()`. src/server/services/abwesenheit/index.ts:173-180 (pruefeArt, Zeile 180: `if (art.bezahlt === null) throw new ArtUngeklaertFehler`). psql: select schluessel, bezahlt from abwesenheitsart → bezahlt nur bei krankheit/urlaub gesetzt. src/server/services/mitarbeiter/antraege.ts:170-185 (kein mandant-Filter). pg_constraint: abwesenheitsart_key = UNIQUE NULLS NOT DISTINCT (mandant_id, schluessel). | **aufwand_korrigiert**: Lesekatalog: mittel. Pflege wie geplant: BLOCKIERT — ohne Migration (Super-Admin-Policy/Definer-Funktion) ist keine der 7 vorhandenen Zeilen aenderbar; mit Migration gross. Die Seite muss bis dahin ehrlich als Nur-Lesen plus Anlege-Formular fuer mandanteigene Arten auftreten.

- **pfad**: /portal/[mandant]/stammdaten/reinigungsklassen | **behauptet**: dienst_vorhanden: "Verbraucher: src/server/services/reinigung/revier.ts -> ladeRaeume / setzeRaeume, src/server/services/raumbuch/import.ts (interne code-Zuordnung, Zeile 195) und src/server/services/kalkulation/raumbuch.ts -> ladeKalkulationsgrundlage." Blocker: "Kein technischer Blocker". Plan: die Seite nennt je Zeile ihre Wirkung. | **wirklich**: Zwei der drei genannten Verbraucher lesen reinigungsklasse ueberhaupt nicht. revier.ts enthaelt NULL Treffer fuer "reinigungsklasse" bzw. "klasse" — ladeRaeume liest raum und den BELAGSART-Katalog ueber app.leistungswerte_lesen; kalkulation/raumbuch.ts ebenso (gruppiert raum nach belagsart_id). Einziger Leser in src/ ist raumbuch/import.ts:195. Damit steuert eine Reinigungsklasse heute genau zwei Dinge: die Code-Zuordnung des Imports und raum.reinigungsklasse_id — sie geht in KEINE Sollzeit und in KEINE Kalkulation; die versprochene Wirkungsangabe je Zeile stuetzt sich auf die falschen Dateien. Ausserdem ist "kein technischer Blocker" unvollstaendig: die Policy t_mandant auf reinigungsklasse liest mit `objekt.lesen` — dieselbe Falle, die der Plan bei belagsart als Falle benennt und hier verschweigt, obwohl die Policy wortgleich ist. Und die 4 Zeilen existieren nur fuer den Mandanten reinigung; in security/bau/operations ist die Seite leer. | **beleg**: grep -in "klasse" src/server/services/reinigung/revier.ts → 0 Treffer; revier.ts:121 ladeRaeume, :130 `from raum r`, :145 `from app.leistungswerte_lesen($1::date)`. grep -rn "reinigungsklasse" src/ (ohne seed/schema) → ausschliesslich src/server/services/raumbuch/import.ts (Lesestelle Zeile 195: `select id, code from reinigungsklasse where archiviert_am is null`); kein Treffer in src/server/services/kalkulation/raumbuch.ts. psql: select policyname, qual from pg_policies where tablename='reinigungsklasse' → t_mandant USING `mandant_id = app.aktiver_mandant() and app.hat_recht('objekt.lesen', ...)`, WITH CHECK stammdaten.verwalten. psql: select m.slug, count(*) from reinigungsklasse r join mandant m … → nur slug 'reinigung', 4 Zeilen. | **aufwand_korrigiert**: klein bis mittel (die einfachste der fuenf: keine Datumsbereiche, keine Rechnung) — aber der Blocker-Satz ist zu ergaenzen (objekt.lesen in der Lesepolicy, Katalog nur bei reinigung befuellt) und der Wirkungstext neu zu schreiben.

- **pfad**: /portal/[mandant]/stammdaten/belagsarten | **behauptet**: dienst_vorhanden: "ladeKalkulationsgrundlage (liest Belagsart mit Leistungswert …)". Blocker: "Kein Blocker, aber eine Falle: die RLS-Policy t_mandant liest belagsart mit objekt.lesen … Beide Rechte haengen im Katalog an denselben Rollen (super_admin, admin, leitung), aber eine Rolle, der objekt.lesen entzogen wurde, saehe eine leere Tabelle statt einer Sperre." | **wirklich**: Drei Fehler. (1) Die Rollenmengen sind NICHT dieselben: objekt.lesen ist an super_admin, admin, leitung UND kunde gebunden, stammdaten.verwalten nur an die ersten drei. (2) ladeKalkulationsgrundlage liest die Tabelle belagsart nicht, sondern die SECURITY-DEFINER-Funktion app.leistungswerte_lesen(date) — dieselbe Funktion benutzt revier.ts. Wer objekt.lesen nicht haelt, bekommt dort keine leere Tabelle, sondern `raise … insufficient_privilege`, also einen 500er; "leere Tabelle statt einer Sperre" beschreibt nur den direkten RLS-Weg. (3) Genau diese Funktion kann die geplante Seite nicht wiederverwenden: sie liefert ausschliesslich die zu EINEM Stichtag gueltige Scheibe des aktiven Mandanten — die im Plan zugesagte Historie je code braucht eine neue, eigene Abfrage direkt auf belagsart und damit den objekt.lesen-USING-Zweig. Nebenbefund: alle 6 Zeilen gehoeren dem Mandanten reinigung; in den drei anderen Gesellschaften ist die Seite leer, was gemeinsames #10 ("stammdaten gehoert in QUERSCHNITT") absichtlich so oeffnen wuerde. | **beleg**: src/server/auth/katalog.generiert.ts:162 (objekt.lesen, gebunden: ["super_admin","admin","leitung","kunde"]) gegen :207 (stammdaten.verwalten, gebunden: ["super_admin","admin","leitung"]). src/server/services/kalkulation/raumbuch.ts:71-74 (`from app.leistungswerte_lesen($1::date)`), src/server/services/reinigung/revier.ts:145 dito. psql pg_get_functiondef(app.leistungswerte_lesen): STABLE SECURITY DEFINER, `if not app.hat_recht('objekt.lesen', app.aktiver_mandant()) then raise exception … insufficient_privilege`, Filter `b.gueltig_ab <= p_stichtag and (b.gueltig_bis is null or b.gueltig_bis >= p_stichtag)` und `b.mandant_id = app.aktiver_mandant()`. psql: select m.slug, count(*) from belagsart b join mandant m on m.id=b.mandant_id group by 1 → reinigung: 6. (Richtig geprueft und bestaetigt: EXCLUDE-Constraint belagsart_zeitraum_eindeutig und die drei FK aus raum, kalkulation_position, raumbuch_import_zeile bestehen.) | **aufwand_korrigiert**: mittel bis gross — als einzige der fuenf braucht sie eine eigene As-of-/Historienabfrage plus getestete Umdatierung (Schliessen mit gueltig_bis + Einfuegen, Ueberschneidung gegen belagsart_zeitraum_eindeutig), und die Rechtefrage objekt.lesen gegen stammdaten.verwalten muss vorher entschieden werden.

- **pfad**: /portal/[mandant]/stammdaten/antragsarten | **behauptet**: "Die Seite listet die Antragsarten mit ihren sechs Schaltern: erfordert_zeitraum, erfordert_abwesenheitsart, erfordert_einsatz, erfordert_tauschpartner, erzeugt_abwesenheit, ist_stammdatenaenderung — und sagt je Schalter, welches Feld er im Antragsformular des Mitarbeiterportals ein- oder ausblendet, weil leseAntragsarten genau diese Flags dorthin traegt." | **wirklich**: leseAntragsarten traegt VIER der sechs Flags, nicht sechs. `erzeugt_abwesenheit` und `ist_stammdatenaenderung` stehen nicht in ihrer Select-Liste und erreichen das Formular nie: erzeugt_abwesenheit wirkt erst bei der ENTSCHEIDUNG (entscheideAntrag) und im Trigger kern.antrag_erzeugt_abwesenheit, ist_stammdatenaenderung kommt in KEINER TypeScript-Datei vor — es wird ausschliesslich vom SQL-Trigger kern.antrag_pflichtfelder geprueft, der einen `aenderungswunsch` ohne dieses Flag abweist. Die Begruendung der Seite ("weil leseAntragsarten genau diese Flags dorthin traegt") ist damit fuer zwei Schalter falsch, und genau diese zwei wirken serverseitig/§-gebunden — dort, wo eine falsche Beschriftung teuer ist. | **beleg**: src/server/services/mitarbeiter/antraege.ts:136-158 — Select: `id, schluessel, bezeichnung_i18n…, erfordert_zeitraum, erfordert_abwesenheitsart, erfordert_einsatz, erfordert_tauschpartner`; kein erzeugt_abwesenheit, kein ist_stammdatenaenderung. src/server/services/abwesenheit/antrag.ts:242 und :256 (erzeugt_abwesenheit erst in entscheideAntrag). grep -rn "ist_stammdatenaenderung" src/ → 0 Treffer; nur drizzle/0074_antrag.sql:53 (Spalte) und :255 (`if new.aenderungswunsch is not null and not v.ist_stammdatenaenderung then`). | **aufwand_korrigiert**: mittel bleibt (Seite + Dienst), aber der Planinhalt ist zu korrigieren: vier Schalter steuern das Mitarbeiterformular, zwei wirken erst bei Entscheidung bzw. nur im Datenbank-Trigger — und der O-142-Befund "keine einzige pflegbare Zeile" bleibt, weil das WITH CHECK ist_system-Zeilen ausschliesst (bestaetigt: alle 3 Zeilen ist_system = true, mandant_id NULL).

- **pfad**: /portal/[mandant]/stammdaten/qualifikationen | **behauptet**: dienst_vorhanden: "src/server/services/nachweis/uebersicht.ts -> nachweislage, src/server/services/nachweis/ablauf.ts …, src/server/services/nachweis/gueltigkeit.ts, src/server/services/nachweis/tor.ts, src/server/services/dienstplan/einteilung.ts und src/server/services/security/eventbesetzung.ts lesen qualifikation fuer die SEC-04-Sperre." | **wirklich**: Vier der sechs genannten Stellen lesen die Tabelle qualifikation nicht. Direkte Leser sind nachweis/uebersicht.ts:132 und nachweis/ablauf.ts:101 — sowie zwei, die der Plan nicht kennt: mitarbeiter/nachweise.ts:132 und bericht/kacheln.ts:339/350 (letzteres ist genau der Nachweisbericht, an dem O-144 haengt). gueltigkeit.ts rechnet nur auf uebergebenen Tatsachen, tor.ts fragt die SQL-Funktion app.einsatz_qualifikation_erfuellt, und einteilung.ts bzw. eventbesetzung.ts rufen/fangen nur assertZuordnungZulaessig bzw. QualifikationFehlt. Folge fuer den Plan: die zugesagte Aussage "blockiert_einsatz einzuschalten laesst ab diesem Moment Einteilungen scheitern" muss gegen app.einsatz_qualifikation_erfuellt (Migration 0031) belegt werden, nicht gegen einteilung.ts, und die Reichweitenzahlen gehoeren zusaetzlich zum Berichtsweg. Nebenbefund: qualifikation.nachweis_art_id wird von keiner Codezeile gelesen — ein Formularfeld dafuer pflegte eine tote Spalte. | **beleg**: grep -rn "from qualifikation|join qualifikation" src/server (ohne seed) → genau 5 Treffer: bericht/kacheln.ts:339, :350, nachweis/uebersicht.ts:132, nachweis/ablauf.ts:101, mitarbeiter/nachweise.ts:132. src/server/services/nachweis/tor.ts:94 (`select app.einsatz_qualifikation_erfuellt($1::uuid, $2::uuid)`); src/server/services/dienstplan/einteilung.ts:557 (`assertZuordnungZulaessig`); src/server/services/security/eventbesetzung.ts:29/216 (Import und catch von QualifikationFehlt). grep -rn "nachweis_art_id" src/ (ohne seed) → 0 Treffer. (Richtig geprueft und bestaetigt: 4 Zeilen, alle mandant_id NULL; standard_gueltigkeit_monate nur bei unterweisung_dguv1 = 12; Enum qualifikation_kategorie = gesetzlich/fachlich/fuehrerschein/gesundheit/intern; CHECK qualifikation_sprachen auf de/en/ar/tr; kern.qualifikation_katalog_schutz macht genau schluessel unveraenderlich; q_schreiben/q_aendern erlauben globale Zeilen nur app.ist_super_admin().) | **aufwand_korrigiert**: mittel bleibt — die Belegkette ist zu korrigieren (SQL-Funktion statt Dienstdateien), und nachweis_art_id gehoert nicht ins Formular.

- **pfad**: alle fuenf: /portal/[mandant]/stammdaten/* (gemeinsames #5) | **behauptet**: "GESCHRIEBEN WIRD UEBER API-ROUTEN … Die fuenf Seiten brauchen je eine API-Route unter src/app/api/stammdaten/." Daraus: Aufwand mittel = Seite + Dienstfunktionen + API-Route. | **wirklich**: Der Plan nennt den Pflichtschritt nicht, der jede neue API-Route zum Build-Brecher macht: jede route.ts unter src/app muss in src/server/auth/route-manifest.ts stehen, mit Recht oder mit Grund. tests/kern/routen.test.ts prueft beide Richtungen gegen das Dateisystem ("keine Route fehlt im Manifest" / "keine Manifest-Zeile zeigt auf eine Route, die es nicht gibt") und verlangt zusaetzlich einen echten authorize()-Aufruf in der Datei oder einem direkt importierten Nachbarn. Das Vorbild mahnwesen hat diesen Eintrag; der Plan hat ihn fuer fuenf neue Routen nirgends. Ohne ihn faellt `pnpm test`, bevor irgendeine Seite geprueft wird. Ebenfalls unerwaehnt: abwesenheitsart und antragsart tragen — anders als belagsart, reinigungsklasse und qualifikation — KEINEN setze_geaendert_am-Trigger; die Pflegefunktionen muessen geaendert_am/geaendert_von selbst setzen. | **beleg**: src/server/auth/route-manifest.ts:16-24 (RouteEintrag: pfad/recht/grund), :1136-1138 (`pfad: 'api/einstellungen/mahnwesen', recht: 'mahnung.schreiben'`); 127 Eintraege. tests/kern/routen.test.ts:11 (`import { ROUTEN } from '../../src/server/auth/route-manifest.js'`), :98 ("keine Route fehlt im Manifest"), :104, :120. psql pg_trigger: trg_belagsart_geaendert_am / trg_reinigungsklasse_geaendert_am / trg_qualifikation_geaendert_am vorhanden, fuer abwesenheitsart und antragsart existiert nur *_kein_hard_delete / *_kein_truncate (+ trg_abwesenheitsart_schutz). | **aufwand_korrigiert**: je Route +1 Pflichtschritt (Eintrag in route-manifest.ts mit stammdaten.verwalten); ohne ihn ist keine der fuenf Routen 'fertig'. Aufwandsstufe bleibt mittel, aber die Definition-of-done im Plan ist unvollstaendig.

- **pfad**: alle fuenf: /portal/[mandant]/stammdaten/* (gemeinsames #4) | **behauptet**: "DAS RECHT STEHT IM KATALOG, ABER AUF DER WARTELISTE. … Es steht aber auch in tests/kern/katalog-unbenutzt.ts:191 — der eingefrorenen Liste der Schluessel, die noch kein Code prueft. Diese Menge darf nur schrumpfen: die erste gebaute Stammdatenseite muss diese Zeile streichen, sonst behauptet die Liste weiter, das Recht sei ungenutzt." | **wirklich**: Der Schluessel gilt dem Pruefer heute schon als BENUTZT — 22 Funde, davon 6 im Routenregister und 16 in fuenf Migrationen (die RLS-Policies pruefen app.hat_recht('stammdaten.verwalten')). funde() liest ganz src/**.ts(x) und drizzle/**.sql und schneidet nur katalog.generiert.ts aus, nicht routen.generiert.ts. Das Recht wird sogar bereits durchgesetzt: der Auffangboden laeuft ueber portalZugang -> pruefeZugang gegen dasselbe Manifest, eine Sitzung ohne stammdaten.verwalten bekommt auf diesen fuenf Adressen also 404 und nicht "noch nicht gebaut" — auch das korrigiert gemeinsames #1 ("antworten heute … mit 200"). Und das Streichen ist von keinem Test erzwungen: katalog.test.ts prueft nur unbenutzt ⊆ NOCH_UNBENUTZT (eine Obermenge ist erlaubt) und dass jeder Wartelisten-Schluessel noch im Katalog existiert. Die Zeile ist veraltete Hygiene, kein Bau-Gate — als "muss" im Plan setzt sie eine falsche Prioritaet. | **beleg**: tsx-Lauf von funde() aus scripts/katalog/benutzung.ts: 22 Treffer fuer stammdaten.verwalten (6x src/server/registry/routen.generiert.ts, dazu drizzle/0021_objekt_raumbuch.sql, 0030_nachweis_qualifikation.sql, 0073_abwesenheit.sql, 0074_antrag.sql u.a.). scripts/katalog/benutzung.ts, funde(): scannt join(WURZEL,'src') + join(WURZEL,'drizzle'), filter nur `!d.endsWith('katalog.generiert.ts')`. tests/kern/katalog.test.ts:47-66 (unbenutzt ⊆ eingefroren) und :68-71. src/app/portal/unterseite.tsx:175-201 (MandantUnterseite -> portalZugang(pfad)), src/app/portal/zugang.ts:204-207 (pruefeZugang gegen das Manifest). | **aufwand_korrigiert**: unveraendert mittel; der Punkt entfaellt als Pflichtschritt (optionale Hygiene: Zeile 191 streichen).
