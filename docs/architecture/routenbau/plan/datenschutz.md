# PLAN datenschutz
## Gemeinsames
Alle fuenf Routen fallen heute in die Catch-all-Huelle /home/user/cse-platform/src/app/portal/[mandant]/[...rest]/page.tsx -> MandantUnterseite -> NochNichtGebaut; keine hat ein eigenes page.tsx. Die Bewachung steht fuer alle fuenf im Register (/home/user/cse-platform/src/server/registry/routen.generiert.ts, Zeilen 371-378), und alle vier Rechte — datenschutz.auskunft_erstellen, datenschutz.berichtigung_bearbeiten, datenschutz.loeschung_pruefen, crm.rechtsgrundlage_lesen — stehen im Katalog (/home/user/cse-platform/src/server/auth/katalog.generiert.ts, Zeilen 55-60) und in der lebenden Tabelle berechtigung.

DREI Dinge teilen sie sich, und sie gehoeren in EINEN Zug erledigt:

1. EINE Huelle fuer die vier [id]-Seiten. Gebaut ist der Posteingang /home/user/cse-platform/src/app/portal/[mandant]/datenschutz/page.tsx — er ist das Vorbild fuer alles: mandantTor(pfad, mandant) + MandantAntwort, haeltRechte(), db().begin(SCHNAPPSCHUSS, …) + withTenant, PortalRahmen, DataTable, Hinweis, StatusPill, Fristanzeige in Europe/Berlin, Schreibweg als method="post" gegen /api/datenschutz/*. Die vier [id]-Seiten sind derselbe Kopf (Vorgang, Frist, Zuordnung) mit vier verschiedenen Koerpern; der Kopf gehoert einmal gebaut, nicht viermal.

2. EIN Dienst und EIN API-Zweig. anfrage.ts traegt bereits lade/liste/entscheide/verlaengere und /api/datenschutz/bearbeiten den Abschluss. Es fehlen drei Dienstdateien im selben Ordner — auskunft.ts (Art. 15), berichtigung.ts (Art. 16), loeschentscheidung.ts (Art. 17) — plus eine Zuordnungsfunktion in anfrage.ts, und daneben ein API-Zweig nach dem Muster von /api/datenschutz/verarbeitungsverzeichnis (md/json, protokollierter Download, SHA-256).

3. EINE Migration, die drei Loecher zugleich stopft: (a) die RLS von /home/user/cse-platform/drizzle/0176_betroffenenanfrage.sql kennt NUR datenschutz.auskunft_erstellen — wer als Torrecht nur berichtigung_bearbeiten oder loeschung_pruefen haelt, kommt durch das Tor und liest null Zeilen; (b) es fehlt der strukturierte Entscheidungsnachweis (Auskunftsartefakt, Loeschentscheidung je Feld und Tabelle) — heute gibt es dafuer eine einzige Textspalte; (c) es fehlt der Leseweg auf ansprechpartner.werbewiderspruch_am (K-05-Spaltenblock: cse_app hat dort kein SELECT), ohne den /widersprueche nur Firmen und keine Kontakte zeigen kann.

Und eines vorweg, sonst ist nichts vorfuehrbar: der Seed legt NULL betroffenenanfrage-Zeilen an und NULL Widersprueche (0 von 6 ansprechpartner, 0 von 7 kunde). Ohne Seedzeilen gibt es keine [id], die man aufrufen kann, und die vier Detailseiten sind weder klickbar noch e2e-pruefbar. Offene Geschaeftsregeln, die NICHT stillschweigend geschlossen werden duerfen: O-71 und O-113 (Umfang von Auskunft und Loeschung gegen GoBD/§147 AO) sowie O-65, O-34, O-95 (was Werbung ist und was vertraglich noetig).
## Routen

### /portal/[mandant]/datenschutz/[id]
- zustand: platzhalter
- dienst_vorhanden: /home/user/cse-platform/src/server/services/datenschutz/anfrage.ts — lade(kontext, id) liefert die Zeile samt tageBisFrist, entscheide(kontext, id, 'beantwortet'|'abgelehnt', text) und verlaengere(kontext, id, grund) sind da; der Schreibweg steht als POST /api/datenschutz/bearbeiten (/home/user/cse-platform/src/app/api/datenschutz/bearbeiten/route.ts). Es FEHLT eine Funktion, die die Zuordnung setzt (person_id / ansprechpartner_id / bewerbung_id) — keine Schreibfunktion im Dienst beruehrt diese drei Spalten.
- tabellen_vorhanden: True
- recht: datenschutz.auskunft_erstellen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/datenschutz/page.tsx (gleiche Domaene, gleiches Recht, gleicher Dienst, mandantTor + PortalRahmen + DataTable + Formular gegen /api/datenschutz/bearbeiten); fuer die Detailform zusaetzlich /home/user/cse-platform/src/app/portal/[mandant]/crm/kunden/[id]/page.tsx
- aufwand: mittel
- blocker: Kein harter Blocker, aber zwei echte Huerden: (1) der Seed legt KEINE betroffenenanfrage an (select count(*) = 0 in cse_test), es gibt also keine [id], die man aufrufen koennte — der Seed muss mitwachsen, sonst ist die Seite nicht vorfuehrbar und die Definition of Done nicht erfuellt; (2) die Zuordnungsspalten ansprechpartner_id und bewerbung_id in drizzle/0176_betroffenenanfrage.sql tragen KEINE Fremdschluessel (nur person_id referenziert person(id)) — wer sie setzt, muss Existenz und Mandant selbst pruefen.

**Plan**

Die Seite zeigt eine einzelne Betroffenenanfrage vollstaendig: Art (ART_TEXT), Status, was die Person ueber sich gesagt hat (name, email, rolle_angabe, nachricht — und daneben der Satz, dass das die SELBSTauskunft ist, nicht der Abgleich), Eingang, wirksame Frist mit Tagen in Berliner Zeit, eine etwaige Verlaengerung mit ihrem Grund, sowie die Entscheidung mit Zeitpunkt und benanntem Menschen, sobald es eine gibt. Darunter der Identitaetsabgleich: die von einem Menschen gesetzte Zuordnung zu person / ansprechpartner / bewerbung, mit Suchfeld und Link in den jeweiligen Datensatz — dafuer kommt eine neue Dienstfunktion ordneZu() in anfrage.ts und ein POST /api/datenschutz/zuordnen dazu, beide gegen datenschutz.auskunft_erstellen. Am Fuss die drei Weiterleitungen nach Art der Anfrage (/auskunft, /berichtigung, /loeschung) und dieselben drei Knoepfe wie in der Liste (Beantwortet, Abgelehnt, Frist verlaengern), die gegen das bestehende /api/datenschutz/bearbeiten posten. Nichts auf dieser Seite loescht oder aendert fremde Daten — sie ist die Akte des Vorgangs.

### /portal/[mandant]/datenschutz/[id]/auskunft
- zustand: platzhalter
- dienst_vorhanden: keiner fuer Art. 15. Nur anfrage.ts (lade) fuer die Huelle. Bausteine da: /home/user/cse-platform/src/server/services/archiv/zip.ts (schreibeZip) fuer das Buendel, /home/user/cse-platform/src/server/services/dokument/pdf.ts, und /home/user/cse-platform/src/server/services/datenschutz/verzeichnis.ts als Vorbild fuer 'Dokument aus lebender Konfiguration erzeugen + alsMarkdown + SHA-256'.
- tabellen_vorhanden: False
- recht: datenschutz.auskunft_erstellen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/datenschutz/verarbeitungsverzeichnis/page.tsx samt /home/user/cse-platform/src/app/api/datenschutz/verarbeitungsverzeichnis/route.ts (Dienst erzeugt ein Dokument, Seite rendert Abschnitte als DataTable, API liefert md/json und protokolliert JEDEN Download)
- aufwand: gross
- fehlende_tabellen:
  - datenschutz_auskunft (das ausgehaendigte Artefakt: Anfrage, Umfang, Zeitpunkt, erzeugender Benutzer, SHA-256 — heute haelt betroffenenanfrage nur EINE Textspalte 'entscheidung')
- blocker: Drei. (1) O-113 ist offen: wie ein Art.-15-Begehren auf Agentenprotokolle, Wissens-Chunks und Freigabe-Snapshots wirkt, wenn GoBD/§147 AO Aufbewahrung verlangt — ohne Antwort darf der Umfang nicht erfunden werden, er gehoert hinter eine Schnittstelle mit TODO(client, O-113). (2) K-05-Spaltenrechte: cse_app hat KEIN SELECT auf ansprechpartner.rechtsgrundlage, .werbewiderspruch_am und .widerspruch_am (nachgemessen in information_schema.column_privileges); der einzige Leseweg ist app.rechtsgrundlage_lesen(uuid) — ein Kontakt pro Aufruf, mit crm.lesen und einer audit_log-Zeile je Aufruf. Eine Auskunft ueber einen Kundenkontakt muss diesen Weg gehen oder eine Migration mitbringen. (3) Kein Artefaktnachweis: der Auskunftsumfang wird ueber ~24 Tabellen mit person_id und 23 mit anstellung_id gezogen; was ausgehaendigt wurde, muss belegbar bleiben (SEC-A9), und dafuer fehlt die Tabelle. Zusatz: 0 Seedzeilen.

**Plan**

Die Seite stellt die Art.-15-Auskunft fuer die zugeordnete Person, Bewerberin oder Kundenkontakt zusammen und zeigt sie VOR dem Versand vollstaendig auf dem Bildschirm — Abschnitt je Datenklasse (Stammdaten, Anstellungen, Zeiteintraege, Abwesenheiten, Nachweise, Dokumente, Benachrichtigungen, Bewerbung), je Abschnitt Zweck, Quelle und Aufbewahrungsfrist aus verzeichnis.ts/loeschkonzept.ts. Ein neuer Dienst services/datenschutz/auskunft.ts sammelt das mandantengebunden ueber den Kontext, rechnet NICHTS und ruft kein Modell — er liest und faltet. Was das System nicht sicher beantworten kann (Agentenprotokolle, Wissens-Chunks, Freigabe-Snapshots) steht als eigener Abschnitt mit dem Satz, dass der Umfang dort offen ist (TODO(client, O-113)), statt stillschweigend zu fehlen. Der Export geht ueber GET /api/datenschutz/auskunft?anfrage=…&format=md|json|zip, wird protokolliert, traegt seinen SHA-256 und verlaesst das System erst, wenn ein Mensch auf der Seite freigegeben hat (Invariante 7); die Aushaendigung selbst bleibt der Mail-/Downloadweg des Menschen, die Seite verschickt nichts von allein.

### /portal/[mandant]/datenschutz/[id]/berichtigung
- zustand: platzhalter
- dienst_vorhanden: keiner fuer Art. 16. anfrage.ts liefert lade() und entscheide() fuer den Abschluss des Vorgangs; die eigentliche Berichtigung hat keine Funktion.
- tabellen_vorhanden: True
- recht: datenschutz.berichtigung_bearbeiten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/datenschutz/page.tsx (Formular gegen /api/datenschutz/bearbeiten, Rechteabfrage ueber haeltRechte, Hinweis-Kasten mit dem Rechtssatz)
- aufwand: mittel
- blocker: Ein konkreter Rechte-Bruch: das Tor der Route verlangt laut Register datenschutz.berichtigung_bearbeiten, die RLS-Policies t_betroffenenanfrage_lesen und t_betroffenenanfrage_bearbeiten in drizzle/0176_betroffenenanfrage.sql verlangen aber ausschliesslich datenschutz.auskunft_erstellen. Wer nur das Berichtigungsrecht gebunden bekommt (im Katalog bindbar an admin, denkbar an leitung), kommt durch das Tor und sieht null Zeilen — eine leere Seite statt 404. Das ist vor dem Bau zu entscheiden: entweder die Policy um das Recht erweitern (Migration) oder das Tor der Route in der Seitenkarte auf auskunft_erstellen + berichtigung_bearbeiten setzen. Zweitens: 0 Seedzeilen. Kein offener O-Punkt.

**Plan**

Die Seite nimmt die Behauptung auf, etwas Gespeichertes sei falsch, und fuehrt sie zu einer nachweisbaren Entscheidung — sie aendert fremde Tabellen NICHT selbst. Oben der Vorgang und die zugeordnete Person; darunter je strittigem Feld drei Angaben: was gespeichert ist, was die Person als richtig angibt, und woher der gespeicherte Wert stammt (Arbeitsvertrag, Selbstauskunft, Formular). Die Korrektur geschieht im zustaendigen Editor — die Seite verlinkt dorthin (Personalakte, CRM-Kontakt, Bewerbung), weil dort die Schreibrechte und die Pruefungen sitzen; hier wird festgehalten, WAS berichtigt wurde, von welchem Wert auf welchen und von wem. Ein neuer Dienst services/datenschutz/berichtigung.ts schreibt diese Entscheidung ueber entscheide() in betroffenenanfrage.entscheidung und legt die Feldliste strukturiert daneben; die Spur der eigentlichen Aenderung liegt ohnehin im audit_log. Abschluss mit Beantwortet/Abgelehnt und dem Pflichthinweis auf Art. 19 DSGVO: wer den falschen Wert bekommen hat, muss die Berichtigung erfahren.

### /portal/[mandant]/datenschutz/[id]/loeschung
- zustand: platzhalter
- dienst_vorhanden: kein Dienst fuer die Einzelfallentscheidung. Sehr nah dran: /home/user/cse-platform/src/server/services/datenschutz/loeschkonzept.ts — erstelleLoeschkonzept() faltet KEIN_HARD_DELETE (src/server/db/schema/rls.ts) mit den Aufbewahrungsregeln (dokument/aufbewahrung.ts, UNTERGRENZE) und den loeschenden Jobs zu Fristen, Sperren und offenen Punkten. Das ist genau das Material der Entscheidung, aber je MANDANT, nicht je Betroffenem. Ausserdem dokument/loeschung.ts (loescheDokument) fuer den Dokumentzweig.
- tabellen_vorhanden: False
- recht: datenschutz.loeschung_pruefen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/datenschutz/loeschkonzept/page.tsx (rendert genau diese Fristen- und Sperrenmatrix und benennt, was NICHT geloescht wird und warum)
- aufwand: gross
- fehlende_tabellen:
  - loeschentscheidung (je Anfrage eine Zeile je Tabelle/Feld: geschuldet oder ueberlagert, mit Rechtsgrundlage der Ueberlagerung und dem Datum, an dem die Sperre faellt) — betroffenenanfrage hat dafuer nur die eine Textspalte entscheidung
- blocker: Vier. (1) O-71 offen: welche Personendaten bei einem Art.-17-Antrag anonymisiert werden, welcher Beleg-Rumpf aus GoBD-/§7-UWG-Gruenden bleibt und wer den Konflikt entscheidet. (2) O-113 offen: dieselbe Frage fuer Agentenprotokolle, Wissens-Chunks und Freigabe-Snapshots. (3) Fehlende Tabelle fuer den Entscheidungsnachweis je Feld und Tabelle — ohne sie ist die Zusage der Seitenkarte ('produces a decision record') ein Freitext. (4) Derselbe Rechte-Bruch wie bei /berichtigung: das Tor verlangt datenschutz.loeschung_pruefen, die RLS in 0176 laesst nur datenschutz.auskunft_erstellen lesen und schreiben. Zusatz: 0 Seedzeilen.

**Plan**

Die Seite prueft einen Loeschantrag GEGEN die Aufbewahrungspflichten und erzeugt eine Entscheidung, niemals eine Loeschung — das sagt sie auch so, sichtbar, ganz oben. Sie zieht fuer die zugeordnete Person die betroffenen Tabellen zusammen und stellt je Zeile drei Dinge nebeneinander: was dort ueber sie steht, ob Loeschung geschuldet ist, und welche Pflicht sie gegebenenfalls ueberlagert (§147 AO zehn Jahre, §17 MiLoG zwei Jahre, Unveraenderlichkeit des Protokolls, die Hashkette der Rechnungen, §7 UWG fuer den Werbewiderspruch). Das Material dafuer kommt aus loeschkonzept.ts, das diese Sperren samt Begruendung bereits kennt; ein neuer Dienst services/datenschutz/loeschentscheidung.ts bindet es an einen konkreten Betroffenen. Was Recht und Pflicht nicht sauber trennen (O-71, O-113), erscheint als eigene Zeile mit 'noch nicht entschieden' und TODO(client, O-71 / O-113) statt als plausibel gesetztes Ergebnis. Der Vollzug ist Anonymisierung plus Tombstone durch die bestehenden Nachtlaeufe — die Seite markiert, WAS freigegeben wurde, und ein Mensch unterschreibt; die Stunden selbst bleiben, weil das Gesetz sie verlangt.

### /portal/[mandant]/datenschutz/widersprueche
- zustand: platzhalter
- dienst_vorhanden: keiner. In services/crm/ liegt nur anlegen.ts (legeKundeAn, legeKontaktAn, legeLeadAn, setzeLeadStatus) — nichts zu Widerspruch. In der Datenbank gibt es app.rechtsgrundlage_lesen(uuid) (ein Kontakt pro Aufruf, prueft crm.lesen, schreibt eine audit_log-Zeile) und app.rechtsgrundlage_von(uuid, uuid); eine mengenwertige Leseform existiert nicht.
- tabellen_vorhanden: False
- recht: crm.rechtsgrundlage_lesen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/crm/kunden/page.tsx (liest kunde.werbewiderspruch_am und kunde.widerspruch_am direkt im withTenant-Kontext und zeigt sie als Spalte in einer DataTable)
- aufwand: gross
- fehlende_tabellen:
  - werbewiderspruch (das Protokoll selbst: Kontakt, Kanal, Zeitpunkt, ausloesende Nachricht, Quelle Token oder Formular — docs/architecture/02-datenmodell/02-CRM-OPERATIONS.md verlangt es ausdruecklich)
  - der Widerspruchstoken je ausgehender Werbenachricht (gehasht, widerrufbar) — ohne ihn gibt es /werbewiderspruch/[token] nicht und damit keinen Eintrag
- blocker: Drei, und der erste ist hart. (1) Spaltenrechte: cse_app hat SELECT auf kunde.werbewiderspruch_am und kunde.widerspruch_am, aber NICHT auf ansprechpartner.werbewiderspruch_am / .widerspruch_am / .rechtsgrundlage (K-05-Block in drizzle/0020_crm_identitaet.sql, nachgemessen in information_schema.column_privileges). Der Hauptfall — der Widerspruch eines KONTAKTS — ist aus der Anwendung heraus nicht listbar; noetig ist entweder eine neue SECURITY-DEFINER-Funktion app.werbewiderspruch_liste() mit Rechtepruefung und EINER Protokollzeile je Abruf oder ein Spaltengrant. Beides ist eine Migration. (2) Es gibt nichts zu protokollieren: die oeffentlichen Routen /werbewiderspruch und /werbewiderspruch/[token] sind nicht gebaut (stehen nur im Register, Phase 4), und die Protokolltabelle fehlt — die Seite saehe heute zwei Zeitstempelspalten und kennte weder Kanal noch ausloesende Nachricht. (3) O-65, O-34 und O-95 sind offen: welche Kommunikation vertraglich noetig statt Werbung ist, der genaue §-7-Abs.-3-Nr.-4-UWG-Wortlaut, und woher die bestandskunde-Adressen stammen. Zusatz: 0 Zeilen mit werbewiderspruch_am in ansprechpartner (6 Zeilen) und kunde (7 Zeilen) im Seed.

**Plan**

Die Seite ist das Nachweisblatt zu §7 UWG und Art. 21 DSGVO: wer hat wann widersprochen, auf welchem Weg, und was sendet seitdem nicht mehr. Sie fuehrt zwei getrennte Listen, weil die Seitenkarte die beiden Widersprueche ausdruecklich trennt — Werbewiderspruch (werbewiderspruch_am, blockt nur zweck='werbung', vertragliche Post laeuft weiter) und Verarbeitungswiderspruch nach Art. 21 (widerspruch_am, zwingt ueber kern.erzwinge_widerspruch() rechtsgrundlage='keine') — und ein Blatt, das sie vermischt, laedt dazu ein, einem Kunden versehentlich die Rechnungen abzustellen. Je Zeile: Kontakt oder Firma, Zeitpunkt in Berliner Zeit, Quelle (Token aus einer Nachricht, tokenloses Formular, manuell erfasst) und die Wirkung in einem Satz. Beide Spalten sind schreibgeschuetzt und nicht raeumbar (der Ausloeser in 0020 wirft, wenn jemand sie auf NULL setzt) — die Seite hat deshalb keinen Zuruecknehmen-Knopf und sagt, warum. Solange die Protokolltabelle und die oeffentlichen Widerspruchsrouten fehlen, zeigt sie die vorhandenen Zeitstempel und benennt die Luecke offen, statt Kanal und Ausloeser zu erfinden.

## KRITIK (5 geprueft, 5 Korrekturen)

- **pfad**: /portal/[mandant]/datenschutz/[id] | **behauptet**: "blocker: Kein harter Blocker, aber zwei echte Huerden" (Seed, fehlende FKs), Aufwand mittel, und im Plan ausdruecklich: "Nichts auf dieser Seite loescht oder aendert fremde Daten — sie ist die Akte des Vorgangs." Am Fuss "die drei Weiterleitungen nach Art der Anfrage". | **wirklich**: Es gibt einen harten Blocker, und er liegt genau in dem Satz, der ihn ausschliesst. Die Seitenkarte weist DIESER Seite die Art.-21-Entscheidung zu: der Verarbeitungswiderspruch wird ueber /datenschutz/anfrage erhoben und "decided at M/datenschutz/[id]", und er setzt ansprechpartner.widerspruch_am / kunde.widerspruch_am — woraufhin kern.erzwinge_widerspruch() rechtsgrundlage auf 'keine' zwingt, rechtsgrundlage_quelle, _erfasst_am und einwilligung_kanaele nullt und jede Ruecknahme wirft. Die Seite MUSS also fremde Daten aendern, einmalig und unwiderruflich. Dafuer gibt es (1) keine Dienstfunktion und keine API-Route — kein einziges .ts unter src/server/services/ oder src/app/api/ nennt widerspruch_am; (2) keinen Leseweg fuer den Kontaktfall: cse_app hat auf ansprechpartner.widerspruch_am nur INSERT/UPDATE, kein SELECT (K-05), die Seite kann den Zustand also nicht anzeigen, den sie setzt. Ausserdem deckt "die drei Weiterleitungen" nur 3 der 6 Enumwerte ab: einschraenkung (Art. 18), uebertragbarkeit (Art. 20) und widerspruch (Art. 21) haben kein Ziel — und Art. 21 ist der, den die Seitenkarte hierher legt. Drittens ist die FK-Aussage irrefuehrend: "nur person_id referenziert person(id)" liest sich, als sei person_id abgesichert. person hat gar kein mandant_id, und t_person_lesen bindet ueber anstellung an app.sichtbare_mandanten(), nicht an app.aktiver_mandant() — die Mandanten- und Existenzpruefung fehlt bei ALLEN DREI Spalten, und eine Person ohne lebende anstellung (Ausgeschiedene, Bewerberin) ist fuer das Suchfeld ueberhaupt nicht lesbar. | **beleg**: docs/architecture/04-SEITENKARTE.md:568 ("to processing — Art. 21, raised through /datenschutz/anfrage and decided at M/datenschutz/[id] (§5.25) | widerspruch_am | the trigger forces rechtsgrundlage = 'keine'"); drizzle/0020_crm_identitaet.sql:620-660 (kern.erzwinge_widerspruch + trg_ansprechpartner_widerspruch); grep -rn widerspruch_am --include=*.ts src/server/services src/app/api -> 0 Treffer; psql "select column_name,privilege_type from information_schema.column_privileges where grantee='cse_app' and table_name='ansprechpartner' and column_name='widerspruch_am'" -> nur INSERT, UPDATE; psql "select count(*) from information_schema.columns where table_name='person' and column_name='mandant_id'" -> 0; pg_get_expr(polqual) von t_person_lesen -> exists(select 1 from anstellung a where a.person_id=person.id and a.geloescht_am is null and a.mandant_id = any(app.sichtbare_mandanten())) or id = app.aktuelle_person(); drizzle/0176_betroffenenanfrage.sql (enum betroffenenanfrage_art hat 6 Werte); src/server/registry/routen.generiert.ts:371-374 (nur 3 Unterrouten) | **aufwand_korrigiert**: gross (nicht mittel): Art.-21-Schreibweg inkl. Migration fuer den Leseweg auf ansprechpartner.widerspruch_am, Zuordnungsfunktion mit Mandanten- und Existenzpruefung fuer alle drei Spalten, Ziel fuer die drei unversorgten Anfragearten, plus Seed

- **pfad**: /portal/[mandant]/datenschutz/[id]/auskunft | **behauptet**: "blocker: Drei." — O-113, K-05-Spaltenrechte bei ansprechpartner, fehlende Artefakttabelle. Der neue Dienst "sammelt das mandantengebunden ueber den Kontext", Abschnitt je Datenklasse, darunter "Benachrichtigungen". | **wirklich**: Es sind mindestens fuenf, und die zwei fehlenden sind die gefaehrlichen, weil sie still wirken. (4) Der Abschnitt "Benachrichtigungen" ist aus der Anwendung heraus fuer eine FREMDE Person gar nicht lesbar: benachrichtigung hat fuer cse_app genau eine Lesepolicy, t_benachrichtigung_eigene mit empfaenger_id = app.aktueller_benutzer(); die zweite Lesepolicy t_benachrichtigung_job_lesen gehoert cse_job. Kein Recht oeffnet das — es braucht eine SECURITY-DEFINER-Funktion oder eine Migration. (5) Alle uebrigen Abschnitte haengen an Rechten, die das Tor dieser Route nicht verlangt: zeiteintrag an zeit.lesen, abwesenheit an zeit.abwesenheit_lesen, dokument an dokument.lesen, leistungsnachweis an nachweis.lesen, bewerbung an recruiting.bewerbung_lesen, ansprechpartner an crm.lesen. datenschutz.auskunft_erstellen ist im Katalog einzeln bindbar (an admin), also kann ein Traeger eine Art.-15-Auskunft erzeugen, die schweigend halb leer ist — genau der Fehler, den der Plan bei /berichtigung selbst als "konkreten Rechte-Bruch" brandmarkt, hier aber nicht sieht. Ein unvollstaendiger Art.-15-Export ohne Kennzeichnung ist schlimmer als eine leere Seite: er sieht aus wie eine Antwort. Der Dienst muss ueber Definer-Funktionen lesen oder die Rechte pruefen und verweigern, statt zu falten, was er zufaellig sieht. | **beleg**: psql "select p.polname,p.polcmd,pg_get_expr(p.polqual,p.polrelid) from pg_policy p where p.polrelid='benachrichtigung'::regclass" -> t_benachrichtigung_eigene (r, cse_app, empfaenger_id = app.aktueller_benutzer()), t_benachrichtigung_job_lesen (r, cse_job, true), sonst nur INSERT/UPDATE-Policies; pg_policy fuer zeiteintrag/abwesenheit/dokument/leistungsnachweis -> t_mandant verlangt app.hat_recht('zeit.lesen'|'zeit.abwesenheit_lesen'|'dokument.lesen'|'nachweis.lesen'); bewerbung: t_bewerbung_lesen verlangt recruiting.bewerbung_lesen; ansprechpartner: t_mandant verlangt crm.lesen; src/server/auth/katalog.generiert.ts:58 (datenschutz.auskunft_erstellen, gebunden super_admin, bindbar admin). Die drei genannten Blocker stimmen: docs/DECISIONS.md:3084 (O-113 offen), information_schema.column_privileges (kein SELECT auf ansprechpartner.rechtsgrundlage/.werbewiderspruch_am/.widerspruch_am), keine Tabelle datenschutz_auskunft in cse_test; 24 Tabellen mit person_id, 23 mit anstellung_id bestaetigt. | **aufwand_korrigiert**: gross bleibt richtig, aber die Blockerliste ist unvollstaendig (3 -> 5): ohne Definer-Leseweg fuer benachrichtigung und ohne Rechtepruefung je Abschnitt liefert die Seite eine still unvollstaendige Auskunft

- **pfad**: /portal/[mandant]/datenschutz/[id]/berichtigung | **behauptet**: "tabellen_vorhanden": true, "fehlende_tabellen": [], Aufwand mittel — und im Plan gleichzeitig: der neue Dienst "schreibt diese Entscheidung ueber entscheide() in betroffenenanfrage.entscheidung und legt die Feldliste strukturiert daneben". | **wirklich**: Der Plan widerspricht sich selbst: es gibt kein "daneben". betroffenenanfrage traegt genau eine Textspalte entscheidung, keine jsonb- oder Detailspalte, und es existiert keine Tabelle fuer "Feld X, gespeicherter Wert, behaupteter Wert, Quelle, wer berichtigt hat" — dieselbe Luecke, die der Plan bei /loeschung als eigenen Blocker ("fehlende Tabelle fuer den Entscheidungsnachweis je Feld und Tabelle") auffuehrt. Entweder faellt der strukturierte Nachweis weg (dann ist die Zusage der Seitenkarte wieder Freitext und die Art.-19-Pflicht — wer den falschen Wert erhalten hat, muss die Berichtigung erfahren — nicht belegbar), oder es braucht dieselbe Migration wie bei /auskunft und /loeschung. "fehlende_tabellen: []" ist damit falsch. | **beleg**: psql \d betroffenenanfrage -> Spalten id, mandant_id, art, status, name, email, nachricht, rolle_angabe, eingegangen_am, frist_am, verlaengert_bis, verlaengert_grund, person_id, ansprechpartner_id, bewerbung_id, beantwortet_am, beantwortet_von, entscheidung (text), erstellt_am, geaendert_am — keine strukturierte Nachweisspalte; keine Tabelle mit 'berichtigung' im Schema public; src/server/services/datenschutz/anfrage.ts entscheide() schreibt nur status/entscheidung/beantwortet_am/beantwortet_von. Der genannte Rechte-Bruch stimmt: routen.generiert.ts:373 verlangt datenschutz.berichtigung_bearbeiten, pg_policy t_betroffenenanfrage_lesen/_bearbeiten verlangen app.hat_recht('datenschutz.auskunft_erstellen', app.aktiver_mandant()); src/app/api/datenschutz/bearbeiten/route.ts:56 authorisiert ebenfalls nur datenschutz.auskunft_erstellen. | **aufwand_korrigiert**: gross (nicht mittel) — sie haengt an derselben EINEN Migration wie /auskunft und /loeschung (RLS-Recht + strukturierter Entscheidungsnachweis); nur wenn der Feldnachweis gestrichen wird, bleibt es mittel

- **pfad**: /portal/[mandant]/datenschutz/[id]/loeschung | **behauptet**: "blocker: Vier" (O-71, O-113, fehlende Tabelle loeschentscheidung, Rechte-Bruch) und im Plan: "Der Vollzug ist Anonymisierung plus Tombstone durch die bestehenden Nachtlaeufe — die Seite markiert, WAS freigegeben wurde, und ein Mensch unterschreibt." | **wirklich**: Die vier Blocker stimmen alle — aber der Vollzugsweg, auf den die Seite ihre Freigabe abgibt, existiert nicht. Es gibt genau EINEN loeschenden Lauf im System, bewerber_loeschung, und der betrifft ausschliesslich abgelaufene Bewerbungen. Eine Funktion app.person_anonymisieren gibt es in der lebenden Datenbank nicht (keine Funktion mit 'anonymisier' im Namen), und kein Codepfad schreibt anonymisiert_am — die Treffer in src/ sind ausschliesslich Kommentare in rls.ts und eine WHERE-Bedingung im Rechnungs-Seed. Fuer eine Beschaeftigte, einen Kundenkontakt oder eine Firma gibt es also weder Anonymisierung noch Tombstone. Die Seite erzeugte damit eine unterschriebene Freigabe fuer eine Ausfuehrung, die niemand ausfuehrt — exakt das, wogegen loeschkonzept.ts seine eigene Registerabfrage begruendet ("statt eine Loeschung zu behaupten, die niemand ausfuehrt"). Das ist ein fuenfter, harter Blocker, keine Randnotiz. | **beleg**: src/server/services/datenschutz/loeschkonzept.ts: LOESCHENDE_LAEUFE enthaelt genau einen Schluessel, 'bewerber_loeschung'; ls src/server/jobs/ -> nur bewerberLoeschung.ts ist loeschend; psql "select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace where proname ilike '%anonymisier%'" -> 0 Zeilen; grep -rn anonymisiert_am --include=*.ts src/ -> nur src/server/db/schema/rls.ts:160,170,1511 (Kommentare) und src/server/db/seed/rechnung.ts:158; docs/architecture/02-datenmodell/02-CRM-OPERATIONS.md:2326 beschreibt app.person_anonymisieren als Soll, nicht als Ist. | **aufwand_korrigiert**: gross bleibt, aber die Blockerliste ist unvollstaendig (4 -> 5): ohne Vollzugsweg (Anonymisierungsprozedur + Lauf) ist die Freigabe der Seite wirkungslos und darf nicht als Vollzug formuliert werden

- **pfad**: /portal/[mandant]/datenschutz/widersprueche | **behauptet**: Fehlende Tabelle "werbewiderspruch (das Protokoll selbst: Kontakt, Kanal, Zeitpunkt, ausloesende Nachricht, Quelle Token oder Formular — docs/architecture/02-datenmodell/02-CRM-OPERATIONS.md verlangt es ausdruecklich)". | **wirklich**: Die Tabelle fehlt tatsaechlich — die Quellenangabe ist aber falsch, und das aendert die Reihenfolge der Arbeit. In 02-CRM-OPERATIONS.md kommt das Wort werbewiderspruch AUSSCHLIESSLICH als Spalte werbewiderspruch_am vor (ansprechpartner, kunde); ein Protokoll oder eine Tabelle dieses Namens steht dort nicht, und §0.1 listet fuer crm.ts firma, kunde, kunde_zugang, kunde_bauleistender_status, freistellungsbescheinigung, ansprechpartner, formular_definition, formular_zustaendigkeit, formular_eingang, lead, lead_aktivitaet — kein werbewiderspruch. Verlangt wird es in 04-SEITENKARTE.md:2870, und zwar als offene Forderung AN 02-CRM-OPERATIONS.md. Dieses Dokument ist laut seiner eigenen Praeambel der normative Vertrag, gegen den die Migrationen gebaut werden, und eine abweichende Umsetzung ist "wrong until this document is amended first". Die Tabellenform (welcher Kanalbegriff, welcher Bezug auf welche Nachrichtentabelle, wie der Token gehasht und widerrufbar ist) ist damit nicht spezifiziert, sondern offen — sie darf nicht aus einer Wunschzeile der Seitenkarte erfunden werden (Arbeitsregel "Never invent a business rule"), das Datenmodelldokument ist zuerst zu ergaenzen. Alles Uebrige der Route habe ich bestaetigt: kein SELECT fuer cse_app auf ansprechpartner.werbewiderspruch_am/.widerspruch_am/.rechtsgrundlage (kunde hat es), /werbewiderspruch und /werbewiderspruch/[token] nur im Register (Phase 4, kein page.tsx), O-65/O-34/O-95 offen, 0 von 6 ansprechpartner und 0 von 7 kunde mit Widerspruch im Seed, app.rechtsgrundlage_lesen(uuid) prueft crm.lesen und protokolliert je Aufruf, kern.erzwinge_widerspruch() wirft bei Ruecknahme BEIDER Spalten. | **beleg**: grep -n werbewiderspruch docs/architecture/02-datenmodell/02-CRM-OPERATIONS.md | grep -v _am -> 0 Treffer; 02-CRM-OPERATIONS.md Zeile 17 (Tabellenliste crm.ts) und :871/:963 (werbewiderspruch_am als Spalte); docs/architecture/04-SEITENKARTE.md:2870 ("/werbewiderspruch/[token] needs a hashed, revocable objection token per outbound message and a werbewiderspruch log row" — Zeile in der Tabelle der offenen Forderungen an 02-CRM-OPERATIONS.md); 02-CRM-OPERATIONS.md:3 ("an implementation that deviates is wrong until this document is amended first"); psql information_schema.column_privileges (cse_app auf ansprechpartner.werbewiderspruch_am nur INSERT/UPDATE); drizzle/0020_crm_identitaet.sql:645-655; src/server/registry/routen.generiert.ts:48-49; psql: select count(*) from ansprechpartner where werbewiderspruch_am is not null -> 0 (von 6), kunde -> 0 (von 7). | **aufwand_korrigiert**: gross bleibt — aber der erste Schritt ist nicht die Migration, sondern die Ergaenzung von docs/architecture/02-datenmodell/02-CRM-OPERATIONS.md um Protokolltabelle und Token; die Tabellenform ist offen, nicht spezifiziert
