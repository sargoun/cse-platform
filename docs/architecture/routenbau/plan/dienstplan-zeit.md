# PLAN dienstplan-zeit
## Gemeinsames
Alle sechs Routen existieren heute nur als Auffangseite: /home/user/cse-platform/src/app/portal/[mandant]/[...rest]/page.tsx faengt sie ab und rendert MandantUnterseite/NochNichtGebaut. Keine hat eine eigene page.tsx — es gibt also keinen Platzhalter zu ersetzen, sondern sechs Dateien anzulegen.

EINE HUELLE, woertlich gleich in allen sechs (Vorbild: konflikte/page.tsx): `export const dynamic = 'force-dynamic'` → portalZugang(pfad) → null ergibt <AnmeldungNoetig/> → slugTor(zugang, mandant), bei art==='wechsel' <Wechselblatt/> → sitzung.aktiverMandantId === null ergibt notFound() (Invariante 10: kein Schreibweg ohne genau einen aktiven Mandanten) → Lesen in db().begin(SCHNAPPSCHUSS, tx => withTenant(tx, sitzung, ...)) → <PortalRahmen bereich={mandant} aktiverTab="dienstplan"|"zeiten" leiste/sichtbareTabs/navigationsRechte aus zugang>. Vier der sechs sind Detailrouten unter einer schon gebauten Liste und brauchen zusaetzlich kennungOder404(id) aus /home/user/cse-platform/src/app/portal/kennung.ts sowie AUT-06: eine fremde Zeile ist 404, nie 403. Fuer Knoepfe, deren Ziel die Sitzung nicht oeffnen darf, gibt es haeltRechte() aus /home/user/cse-platform/src/app/portal/rechte.ts — ein Menuepunkt, der auf 404 fuehrt, ist schlechter als keiner.

EIN MUSTER FUER JEDEN SCHREIBWEG: Formular-POST vom eigenen Ursprung (istGleicherUrsprung), authorize({recht, schreibend:true}, rechtepruefer(...)), Pflichtbegruendung >= 10 Zeichen, Redirect 303 ueber internesZiel(zurueck). Drei der vier schreibenden Routen koennen die BESTEHENDE API weiterverwenden (/api/konflikt, /api/zeit/einwand/entscheidung); neu zu bauen sind nur /api/konflikt/uebersteuern, /api/dienstplan/serien/[id]/ausnahmen und der Veroeffentlichungsweg.

DREI FEHLENDE EINZEL-LESEFUNKTIONEN, dieselbe Luecke dreimal: die Dienste haben je eine Listenfunktion, aber kein leseX(kontext, id) — leseEinwand (zeit/einwand.ts), leseSerie (dienstplan/serie.ts) und ein Konflikt-Einzelsatz (heute nur als SQL in konflikte/page.tsx). Sie gehoeren in die Dienste, nicht in die Seiten; Route Handler und Seiten bleiben duenn (CLAUDE.md: autorisieren → Dienst rufen → zurueckgeben).

EINE MIGRATION, und nur eine: /dienstplan/veroeffentlichung. Alle Tabellen der anderen fuenf Routen sind da, migriert und geseedet. Fuer die Veroeffentlichung gibt es mit Absicht KEINEN Zustand (einsatz_status kennt kein 'veroeffentlicht', drizzle/0028_dienstplan.sql + 04-PLANUNG-ZEIT.md §224, K-17), keinen Vorgangstraeger und keine registrierte Benachrichtigungsart fuer NOT-01 'schedule change'. Das ist die einzige Route der Domaene, die eine offene Geschaeftsregel beruehrt, fuer die es noch KEINE O-Nummer gibt — hoechste vergebene ist O-596, es braucht eine neue (~O-597) mit TODO(client, O-597) und einer Zeile unter 'Open' in docs/DECISIONS.md.

ZWEI STILLE FALLEN, die ein Bauer sonst erst spaet merkt: (1) arbeitszeit_verstoss hat fuer cse_app keine UPDATE-Policy — jedes direkte update trifft 0 Zeilen ohne Fehler; der einzige Weg ist app.arbzg_befund_quittieren, und diese Definer-Funktion prueft dienstplan.arbzg_lesen statt dienstplan.arbzg_uebersteuern, das staerkere Recht muss die Route selbst durchsetzen. (2) ladeAusnahmen() in dienstplan/generator.ts liest nur turnus_ausnahme und kehrt bei Postenserien sofort mit [] zurueck — eine Einzeltermin-Ausnahme auf einer Sicherheitsserie wuerde gespeichert und nie angewendet.

QUERSCHNITT ZU DEN INVARIANTEN: alle Zeitanzeigen kommen als `at time zone 'Europe/Berlin'` aus der Abfrage oder ueber berlinAnzeige/stundenAusMinuten — nirgends aus dem Klienten; Quittierungs- und Entscheidungszeitpunkte setzt ausschliesslich die Serveruhr (now() im Definer bzw. im Ausloeser); nichts loescht hart (alle vier Tabellen tragen kern.verhindere_loeschung); und kein Modell rechnet: Grenzwerte, Dauern und Besetzungsluecken kommen aus getesteten Funktionen bzw. aus Spalten, nie aus einer Formulierung.
## Routen

### /portal/[mandant]/dienstplan/konflikte/[id]/quittung
- zustand: fehlt
- dienst_vorhanden: Kein eigener Dienst unter src/server/services/dienstplan/. Der Schreibweg existiert aber vollstaendig: POST /home/user/cse-platform/src/app/api/konflikt/route.ts setzt status='quittiert' + quittiert_am/quittiert_von + quittierung_begruendung, prueft authorize({recht:'dienstplan.konflikt_quittieren', schreibend:true}), weist blockierende Konflikte mit 409 ab und verlangt >= 10 Zeichen Begruendung. Datenbankseitig re-prueft der Ausloeser kern.planungs_konflikt_quittierung() dasselbe Recht und setzt die Serverzeit. Befunde erzeugt src/server/services/arbzg/detektor.ts (erkenneKonflikte, arbzgFingerabdruck, ueberschneidungsFingerabdruck).
- tabellen_vorhanden: True
- recht: dienstplan.konflikt_quittieren
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/konflikte/page.tsx
- aufwand: klein
- blocker: keiner. Zwei Hinweise: (1) Die Eingangsliste erledigt die Quittierung heute schon inline — die Detailseite darf keinen zweiten, abweichenden Schreibweg bauen, sondern muss dasselbe /api/konflikt benutzen. (2) O-166 (zeit-konflikt-blockiert) ist offen: welche Konflikte blockieren statt nur zu warnen, ist Platzhalter (planungs_konflikt.blockiert, gespeist aus app.einstellung('zeit.konflikt_blockiert')). Die Seite darf daraus nichts ableiten, sondern nur den gespeicherten Wert zeigen.

**Plan**

Einzelansicht eines Konflikts samt Quittungsbeleg. Oben die Karte aus dem Eingang in voll: Art (arbzg / ueberschneidung / qualifikation_entfallen / aufzeichnungsfrist), Schwere, Person, Zeitfenster in Europe/Berlin, Objekt, bei ArbZG Regel/ist_minuten/grenzwert_minuten aus arbeitszeit_verstoss, die Gegenschichten aus details, und bei betrifft_fremden_mandant nur DASS — nie Mandant, Objekt oder Kunde (K-06). Darunter zwei Zustaende und kein dritter: ist der Konflikt offen und nicht blockiert, steht das Formular mit Pflichtbegruendung (min. 10 Zeichen) gegen POST /api/konflikt; ist er blockiert, steht statt des Formulars der Satz, dass § 34a GewO keinen Uebergehen-Knopf kennt. Ist er bereits quittiert, zeigt die Seite die Quittung selbst: wer, wann (Serveruhr), mit welcher Begruendung — das ist der Beleg, der im Streit zaehlt. Fremde Kennung ist 404 und nie 403 (kennungOder404 + AUT-06), Verweis zurueck auf /dienstplan/konflikte und auf /dienstplan/einsatz/[einsatz_id].

### /portal/[mandant]/dienstplan/konflikte/[id]/uebersteuern
- zustand: fehlt
- dienst_vorhanden: Kein TypeScript-Dienst. In der Datenbank existiert app.arbzg_befund_quittieren(p_id uuid, p_begruendung text) als SECURITY DEFINER: setzt arbeitszeit_verstoss.status='quittiert' mit Begruendung, quittiert_von, quittiert_am (Serveruhr) und protokolliert app.protokolliere('arbzg.befund_quittiert', ...). Diese Funktion wird im ganzen src/-Baum NIRGENDS aufgerufen. Lesend vorhanden: src/server/services/arbzg/pruefung.ts (leseBelastung, pruefeEinsatz, schreibeBefund) und src/server/services/zeit/arbzg.ts (ArbzgBefund).
- tabellen_vorhanden: True
- recht: dienstplan.arbzg_uebersteuern
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/konflikte/page.tsx (Huelle, Tor, Karte) — fuer die Uebersteuerungsspur zusaetzlich protokolliereFin18Uebergehung in /home/user/cse-platform/src/server/services/finanz/positionsquelle.ts
- aufwand: mittel
- blocker: Drei, alle real: (1) arbeitszeit_verstoss hat FUER cse_app KEINE UPDATE-Policy — nur t_mandant FOR SELECT und verstoss_definer_update fuer cse_definer. Ein direktes update aus der App trifft still 0 Zeilen, ohne Fehler. Der Schreibweg MUSS ueber app.arbzg_befund_quittieren laufen. (2) Genau diese Definer-Funktion prueft app.hat_recht('dienstplan.arbzg_lesen') — also das SCHWAECHERE Recht, nicht dienstplan.arbzg_uebersteuern, auf das die Seitenkarte die Route torkelt. Die Route muss das staerkere Recht in authorize() selbst durchsetzen; die Datenbank ist dann die schwaechere zweite Linie. Das gehoert als Zeile in DECISIONS.md und mittelfristig als Migration, die den Definer nachzieht. (3) O-166 (zeit-konflikt-blockiert): ein Befund mit blockiert=true ist durch KEIN Recht uebersteuerbar; welche Befunde blockieren, ist unbeantwortet. Die Seite muss blockierende Befunde ausdruecklich abweisen, nicht bloss das Formular verstecken.

**Plan**

Die Seite fuer den einen Fall, den TIM-06 meint: eine ArbZG-WARNUNG bewusst stehen lassen und dafuer geradestehen. Sie laedt den planungs_konflikt und den daran haengenden arbeitszeit_verstoss und zeigt, was uebersteuert wird: Regel (§ 3 / § 4 / § 5 ArbZG), Istwert gegen Grenzwert mit richtiger Richtung (Ruhezeit ist eine MINDEST-, keine Hoechstgrenze), das Fenster in Europe/Berlin und ob der Befund gesellschaftsuebergreifend ist — bei fremd nur Art, Fenster und Dauer, nie die andere Gesellschaft (K-06). Das Formular verlangt eine geschriebene Begruendung und schickt sie an eine neue Route POST /api/konflikt/uebersteuern, die zuerst authorize(dienstplan.arbzg_uebersteuern, schreibend) prueft, blockierende Befunde mit 409 abweist und dann app.arbzg_befund_quittieren aufruft; die Spur entsteht dabei im audit_log neben dem Befund. Ist bereits uebersteuert, zeigt die Seite Urheber, Serverzeitpunkt und Begruendung statt des Formulars. Ohne das Recht: 404, nie 403.

### /portal/[mandant]/dienstplan/offene-schichten
- zustand: fehlt
- dienst_vorhanden: Kein Dienst in src/server/services/dienstplan/. Zwei verwandte Stellen sind da: unterbesetzung(kontext, fenster) in /home/user/cse-platform/src/server/services/security/posten.ts liest die Sicht posten_unterbesetzung — aber NUR postenbasierte (Sicherheits-)Schichten; und die Kachel 'schichten_unbesetzt' in /home/user/cse-platform/src/server/services/bericht/kacheln.ts traegt die gewerkeuebergreifende Abfrage ueber einsatz (besetzt_anzahl < soll_besetzung, status in ('geplant','laufend'), storniert_am is null) samt fertigem Zeilen-select. Die gehoert in eine eigene Dienstfunktion, statt aus der Kachel abgeschrieben zu werden.
- tabellen_vorhanden: True
- recht: dienstplan.lesen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/page.tsx (DataTable, StatusPill, Filter ueber searchParams)
- aufwand: mittel
- blocker: Kein technischer. Zwei offene Regeln begrenzen, was die Seite BEHAUPTEN darf: (1) O-210 — bei einer Veranstaltung ist unklar, ob die vereinbarte Staerke zugleich Mindestbesetzung ist; posten traegt min_besetzung und soll_besetzung getrennt, die Eventschicht bekommt den Spaltenvorgabewert 1. Die Seite darf Dringlichkeit deshalb nur aus min_besetzung ableiten und muss die Luecke gegen soll daneben als blosse Differenz zeigen. (2) Die Seitenkarte sagt 'feeds REC-01', aber /portal/[mandant]/recruiting/bedarf fragt heute keine unbesetzten Schichten ab; wie aus offenen Schichten ein Personalbedarf wird, ist nirgends entschieden. Diese Seite liefert die Zahlen, leitet aber keinen Bedarf ab. Datenlage im Seed traegt die Seite: 115 unterbesetzte von 169 Einsaetzen.

**Plan**

Eine Liste aller Schichten, denen Leute fehlen — gewerkeuebergreifend, nicht nur Sicherheit. Je Zeile: Datum und Fenster in Europe/Berlin (eine Schicht 22:00-06:00 steht am Abend, an dem sie beginnt), Objekt, Kunde, Quelle (Turnus / Posten / Veranstaltung / manuell), soll_besetzung gegen besetzt_anzahl und die Differenz, dazu die Unterschreitung von min_besetzung als eigene, staerkere Markierung. Filter ueber searchParams nach Zeitraum (Vorgabe: naechste 14 Tage), Objekt und 'nur unter Mindestbesetzung'. Jede Zeile verweist auf /dienstplan/einsatz/[id], wo besetzt wird; die Seite selbst schreibt nichts. Die Abfrage zieht ueber einsatz_unterbesetzt_idx und wandert als leseOffeneSchichten() in einen neuen Dienst src/server/services/dienstplan/besetzungsluecke.ts, damit die Kachel und diese Seite eine Aussage teilen statt zwei.

### /portal/[mandant]/dienstplan/serien/[id]
- zustand: fehlt
- dienst_vorhanden: /home/user/cse-platform/src/server/services/dienstplan/serie.ts: wochenRegel, legeTurnusSerieAn, legePlanungsserieAn, SerienErgebnis, SerieEingabeFehlt. /home/user/cse-platform/src/server/services/dienstplan/generator.ts: berlinHeute, materialisiereSerie, ladeSerien, ladeAusnahmen, generiereEinsaetze, SerienBericht. Es fehlt eine Einzel-Lesefunktion (leseSerie) und jede Funktion, die eine Einzeltermin-Ausnahme schreibt. Schreibweg heute nur POST /home/user/cse-platform/src/app/api/dienstplan/serien/route.ts (Anlegen).
- tabellen_vorhanden: True
- recht: dienstplan.lesen (lesend) / dienstplan.schreiben (Ausnahme anlegen)
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/page.tsx
- aufwand: mittel
- blocker: Ein echter: ladeAusnahmen() in generator.ts beginnt mit `if (serie.turnusId === null) return []` und liest ausschliesslich turnus_ausnahme. Postenbasierte Serien (Sicherheit) werten posten_ausnahme NIE aus — eine Einzeltermin-Ausnahme, die diese Seite fuer eine Sicherheitsserie anlegt, wuerde gespeichert und dann stillschweigend ignoriert. Genau die Sorte Fehler, die CLAUDE.md als 'silent, expensive, late-discovered' beschreibt. Entweder der Generator wird um posten_ausnahme erweitert (dann gross statt mittel), oder die Seite bietet Ausnahmen nur fuer Turnusserien an und sagt bei einer Postenserie unmissverstaendlich, dass es noch nicht traegt. Nebenbefund: posten_ausnahme hat 0 Seed-Zeilen, turnus_ausnahme genau 1 — der Weg ist heute durch nichts geuebt.

**Plan**

Das Blatt einer Serie: woher diese Schichten kommen. Kopf mit Bezeichnung, Objekt/Revier bzw. Posten, Quelle, RRULE in Klartext (leseRegel aus lib/datum/rrule), Beginn als Wanduhr Europe/Berlin plus Dauer, Feiertagsregel und Bundesland, Geltungszeitraum, horizont_tage, generiert_bis, letzte_generierung_am und letzte_meldung — generiert_bis steht gross, weil eine stehengebliebene Serie sonst aussieht wie eine Serie ohne Termine. Darunter die materialisierten Einsaetze im Horizont mit Datum, Fenster, Besetzung und Zeitanomalie (dst_luecke / dst_doppelt eigens markiert), jeder verlinkt auf /dienstplan/einsatz/[id]. Dritter Abschnitt: die Einzeltermin-Ausnahmen (TIM-02) — bestehende Zeilen aus turnus_ausnahme mit Datum, Art (ausfall / verschiebung), Ersatzbeginn, Dauer und Pflichtgrund, dazu ein Formular zum Anlegen gegen eine neue Route POST /api/dienstplan/serien/[id]/ausnahmen mit authorize(dienstplan.schreiben). Bei einer Postenserie steht an dieser Stelle der ehrliche Hinweis, dass der Generator posten_ausnahme noch nicht liest.

### /portal/[mandant]/dienstplan/veroeffentlichung
- zustand: fehlt
- dienst_vorhanden: keiner. Weder unter src/server/services/dienstplan/ noch sonstwo gibt es eine Veroeffentlichungsfunktion; dienstplan.veroeffentlichen kommt in src/ nur im Rechtekatalog (src/server/auth/katalog.generiert.ts), im Routenregister und als Kommentar in src/server/services/security/posten.ts vor. Die Benachrichtigungsmaschinerie existiert (src/server/benachrichtigung/registry.ts mit registriereArt/sicherRegistriert, src/server/benachrichtigung/ablage.ts mit stelleZu), aber es ist KEINE dienstplan.*-Art registriert — NOT-01 nennt 'schedule change' ausdruecklich, und die Art dafuer gibt es nicht.
- tabellen_vorhanden: False
- recht: dienstplan.veroeffentlichen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/woche/page.tsx (Zeitraumwahl, Plandarstellung) — fuer den Freigabe-/Veroeffentlichungsvorgang mit Protokoll: /home/user/cse-platform/src/app/portal/[mandant]/social/posts/[id]/page.tsx
- aufwand: gross
- fehlende_tabellen:
  - ein Traeger des Veroeffentlichungsvorgangs (z. B. dienstplan_veroeffentlichung: Mandant, Zeitraum von/bis, veroeffentlicht_am, veroeffentlicht_von, Umfang) — es gibt heute nichts davon
  - kein Zustand auf einsatz: einsatz_status ist ('geplant','laufend','abgeschlossen','storniert'), ein Wert veroeffentlicht fehlt mit Absicht (drizzle/0028_dienstplan.sql, K-17)
  - keine registrierte Benachrichtigungsart dienstplan.* fuer NOT-01
- blocker: Die Geschaeftsregel selbst fehlt, und zwar ABSICHTLICH. drizzle/0028_dienstplan.sql schreibt woertlich: veroeffentlicht ist kein einsatz_status-Wert, weil dienstplan.veroeffentlichen ein Recht auf die HANDLUNG ist und einen Veroeffentlichungsablauf zu modellieren, den die SPEC nicht beschreibt, eine erfundene Geschaeftsregel waere (K-17); docs/architecture/02-datenmodell/04-PLANUNG-ZEIT.md §224 wiederholt es. Unbeantwortet ist damit: (a) Welcher Zeitraum wird veroeffentlicht — Woche, Monat, freies Fenster? (b) Wer wird benachrichtigt — jede eingeteilte Person, oder auch Personen mit gestrichener Schicht? (c) Was bedeutet eine AENDERUNG nach der Veroeffentlichung — neue Meldung, Sperre, gar nichts? (d) Ist eine veroeffentlichte Schicht gegen stille Aenderung geschuetzt? Es gibt dafuer bis heute KEINE O-Nummer in docs/DECISIONS.md; die hoechste vergebene ist O-596, es braucht also eine neue (naechste freie ~O-597) mit TODO(client, O-597) im Code und einer Zeile unter 'Open'. Zusaetzlich fehlt die Benachrichtigungsart fuer NOT-01.

**Plan**

Bis die Frage beantwortet ist, baut die Seite nur, was ohne erfundene Regel traegt. Ein Zeitraumwaehler (Vorgabe: kommende Woche, Berliner Wochengrenzen) und darunter die Vorschau dessen, WAS hinausginge: Zahl der Schichten im Fenster, Zahl der unbesetzten, Zahl der offenen Konflikte und der blockierenden darunter, und je betroffener Person eine Zeile mit ihren Schichten in Europe/Berlin — also genau die Nachricht, die sie bekaeme. Blockierende Konflikte im Fenster stehen als Warnung obenan; ob sie die Veroeffentlichung sperren, ist Teil der offenen Frage und wird NICHT entschieden. Der Knopf ist auf dienstplan.veroeffentlichen getort und laeuft gegen eine neue Route, die den Vorgang in audit_log protokolliert (app.protokolliere('dienstplan.veroeffentlicht', ...)) und je Person eine benachrichtigung-Zeile ueber eine neu zu registrierende Art dienstplan.plan_veroeffentlicht mit Ziel /portal/mein/schichten schreibt. Kein Zustand auf einsatz, keine neue Spalte, kein Einfrieren — dazu steht sichtbar auf der Seite und als TODO(client, O-597) im Code, welche vier Fragen offen sind.

### /portal/[mandant]/zeiten/einwaende/[id]
- zustand: fehlt
- dienst_vorhanden: /home/user/cse-platform/src/server/services/zeit/einwand.ts ist weitgehend vollstaendig: EinwandZeile, EinwandArt, EinwandStatus, ENTSCHIEDEN, mandantDerAnstellung, reicheEinwandEin, entscheideEinwand, listeOffeneEinwaende, listeEigeneEinwaende, EinwandNichtGefundenFehler, EinwandBereitsEntschiedenFehler. Der Schreibweg steht fertig als POST /home/user/cse-platform/src/app/api/zeit/einwand/entscheidung/route.ts (authorize zeit.einwand_entscheiden, Pflichtbegruendung >= 10 Zeichen ausser bei in_pruefung). Es fehlt allein eine Einzel-Lesefunktion leseEinwand(kontext, id).
- tabellen_vorhanden: True
- recht: zeit.einwand_entscheiden
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/zeiten/einwaende/page.tsx (Huelle, Karte, Entscheidungsformular) — fuer die Eintragsseite daneben /home/user/cse-platform/src/app/portal/[mandant]/zeiten/[id]/page.tsx
- aufwand: klein
- blocker: keiner. Ein Hinweis zur Definition-of-done: der Seed traegt genau EINEN zeit_einwand, und der ist offen. Der entschiedene Zustand der Detailseite (Entscheidung, Urheber, Zeitpunkt, Begruendung, verknuepfte Korrektur) wird damit von nichts geuebt — es gehoert ein zweiter, entschiedener Einwand samt zeiteintrag_korrektur in den Seed, sonst ist die Haelfte der Seite ungetestet.

**Plan**

Der einzelne Einwand in voll, als Gegenstueck zur Eingangsliste. Links die Meldung des Menschen: Person, Art (eintrag_fehlt / zeit_falsch / pause_falsch / zuordnung_falsch / sonstiges), betrifft_datum als Berliner Kalendertag, die Begruendung im Wortlaut und die behaupteten Werte (behauptet_beginn/ende/pause_minuten) ausdruecklich als Angabe der Person gekennzeichnet, nie vorbelegt (Invariante 5). Rechts der Zeiteintrag, um den es geht — Serverzeit, geraete_zeit, zeitabweichung_sek, Dauer — mit Verweis auf /zeiten/[id], oder der Hinweis, dass es zu diesem Tag gar keinen Eintrag gibt. Darunter das Entscheidungsformular gegen die bestehende API: in_pruefung ohne Begruendung, anerkannt / teilweise_anerkannt / abgelehnt nur mit. Ist bereits entschieden, steht statt des Formulars die Entscheidung mit Urheber, Zeitpunkt und Begruendung, dazu — und das ist die haeufigste Verwechslung — der Hinweis, ob eine Korrektur gefolgt ist (zeiteintrag_korrektur.zeit_einwand_id) oder eben nicht: eine anerkannte Meldung ohne Korrektur ist genau der Fall, den man sehen will. Fremde Kennung 404, nie 403.

## KRITIK (6 geprueft, 6 Korrekturen)

- **pfad**: /portal/[mandant]/dienstplan/veroeffentlichung | **behauptet**: "Die Benachrichtigungsmaschinerie existiert ..., aber es ist KEINE dienstplan.*-Art registriert"; fehlende Dinge seien nur ein Vorgangstraeger, ein einsatz-Zustand und "keine registrierte Benachrichtigungsart dienstplan.* fuer NOT-01"; der Knopf "schreibt je Person eine benachrichtigung-Zeile"; laut gemeinsames ist die einzige Migration der Domaene die fuer diese Route (Vorgangstraeger). | **wirklich**: Zwei Fehler. (1) Es SIND zwei dienstplan.*-Arten registriert (dienstplan.schicht_ohne_zeiteintrag, dienstplan.morgen_unbesetzt), und das Modul 'dienstplan' steht in MODUL_TITEL — fehlt nur die eine Art fuer die Veroeffentlichung, kein Modulaufbau. (2) Uebersehener HARTER Blocker: cse_app hat auf benachrichtigung ueberhaupt kein INSERT — nicht bloss keine Policy, sondern kein Tabellenrecht. Der geplante Schreibweg (Route schreibt je Person eine Zeile) bricht mit 'permission denied' ab, nicht still. INSERT gibt es nur fuer cse_definer und cse_job; stelleZu wird ausschliesslich aus einem Job und dem Seed gerufen. Es braucht also eine Migration (Grant+Policy oder ein Definer wie app.arbzg_befund_quittieren) selbst fuer die reduzierte Fassung ohne Vorgangstraeger — genau das, was die Planung als "keine neue Spalte, kein Zustand" abtut. Richtig sind: kein 'veroeffentlicht' in einsatz_status, K-17-Kommentar in drizzle/0028_dienstplan.sql:60-64, 04-PLANUNG-ZEIT.md Zeile 224, hoechste O-Nummer O-596 (also ~O-597 noetig). benachrichtigung.art ist blankes text mit Regex-Check — dafuer braucht es keine Migration. | **beleg**: src/server/services/waechter/benachrichtigung.ts:19-20 (ART_SCHICHT_OHNE_ZEIT = `${DIENSTPLAN}.schicht_ohne_zeiteintrag`, ART_MORGEN_UNBESETZT); src/server/benachrichtigung/bootstrap.ts:48 (MODUL_TITEL.dienstplan) und :29-33 (alleArten ruft registriereWaechterArten); psql: select has_table_privilege('cse_app','benachrichtigung','insert') => f; psql: begin; set local role cse_app; insert into benachrichtigung (...) => ERROR: permission denied for table benachrichtigung; pg_policies benachrichtigung: INSERT nur d_benachrichtigung_anlegen {cse_definer} und t_benachrichtigung_job_anlegen {cse_job}; src/server/kontext/index.ts:61 (set local role cse_app); stelleZu-Rufer nur src/server/jobs/nachweisWarnungen.ts:42 und src/server/db/seed/benachrichtigung.ts:113. Gegenprobe: app.protokolliere ist an cse_app granted (proacl cse_app=X), audit_log-INSECT direkt aber nicht (has_table_privilege audit_log insert => f). | **aufwand_korrigiert**: gross — und zwingend MIT Migration (benachrichtigung-Schreibweg fuer cse_app), nicht nur mit O-597; die Behauptung 'KEINE dienstplan.*-Art' ist zu streichen

- **pfad**: /portal/[mandant]/dienstplan/offene-schichten | **behauptet**: Blocker (2): "Die Seitenkarte sagt 'feeds REC-01', aber /portal/[mandant]/recruiting/bedarf fragt heute keine unbesetzten Schichten ab; wie aus offenen Schichten ein Personalbedarf wird, ist nirgends entschieden." Ausserdem: "Datenlage im Seed traegt die Seite: 115 unterbesetzte von 169 Einsaetzen." | **wirklich**: Die REC-01-Behauptung ist falsch: bedarf() fragt genau unbesetzte Schichten ab — einsatz im Fenster app.berlin_heute()+n*7 Tage, wo count(einsatz_zuordnung status='zugesagt') < e.min_besetzung, aggregiert je Objekt; die Seite rendert das unter der Beschriftung "Unbesetzte Schichten je Objekt im gewählten Zeitraum". REC-01 ist also schon gefuettert. Damit wird der eigentliche Punkt der Route umgekehrt: es gibt heute nicht eine, sondern DREI auseinanderlaufende Definitionen von 'unbesetzt' — posten_unterbesetzung (nur Posten), Kachel (besetzt_anzahl < soll_besetzung, 7 Tage), bedarf (zugesagte < min_besetzung, n Wochen). Ein leseOffeneSchichten(), das die Kachel abschreibt, macht daraus vier, solange bedarf nicht mitgezogen wird. Zur Datenlage: die 115 stimmen ueber ALLE Einsaetze (2026-08-24 bis 2029-04-16), im Vorgabefenster der geplanten Seite (naechste 14 Tage) sind es 10; ausserdem ist unter_min == unter_soll == 115, die geplante 'staerkere Markierung' fuer Unterschreitung der Mindestbesetzung ist im Seed also von nichts unterscheidbar und damit ungeuebt (Definition of done: 'seed data exercises it'). | **beleg**: src/server/services/recruiting/dienst.ts:870-907 (bedarf(), 'count(*) ... status = zugesagt ... < e.min_besetzung'); src/app/portal/[mandant]/recruiting/bedarf/page.tsx:85 (DataTable beschriftung) und :4 (import { bedarf }); src/server/services/bericht/kacheln.ts:199-213 (besetzt_anzahl < soll_besetzung, 7 Tage); src/server/services/security/posten.ts:90-107 (nur posten_unterbesetzung); psql: count(*) filter (besetzt_anzahl<soll_besetzung)=115, filter(... and beginn between now() and now()+14 days)=10, filter(besetzt_anzahl<min_besetzung)=115 von 169. | **aufwand_korrigiert**: mittel (unveraendert), aber der Blocker lautet anders: nicht 'REC-01 ist unentschieden', sondern 'drei bestehende Definitionen zusammenfuehren, ohne bedarf() zu widersprechen' — plus ein zweiter Seed-Fall, in dem min < besetzt < soll gilt

- **pfad**: /portal/[mandant]/dienstplan/konflikte/[id]/uebersteuern | **behauptet**: Blocker (1): "arbeitszeit_verstoss hat FUER cse_app KEINE UPDATE-Policy ... Ein direktes update aus der App trifft still 0 Zeilen, ohne Fehler." Ebenso in gemeinsames als eine der "zwei stillen Fallen": "jedes direkte update trifft 0 Zeilen ohne Fehler". | **wirklich**: Die Policy-Lage stimmt (fuer cse_app nur t_mandant/t_gruppe FOR SELECT, UPDATE nur verstoss_definer_update fuer cse_definer und t_job_frist fuer cse_job), die FOLGE aber nicht: cse_app hat auf arbeitszeit_verstoss gar kein Tabellenrecht UPDATE, also bricht ein direktes update LAUT ab — 'ERROR: permission denied for table arbeitszeit_verstoss' — und nicht still mit 0 Zeilen. Das ist keine stille Falle, sondern ein 500er. Wer der Planung glaubt, baut eine Zeilenzahl-Pruefung statt den Definer-Weg und bekommt einen Serverfehler. Zweitens fehlt in den Blockern die LESE-Seite: planungs_konflikt ist fuer cse_app nur mit dienstplan.lesen lesbar und arbeitszeit_verstoss nur mit dienstplan.arbzg_lesen, waehrend das Routentor nur dienstplan.arbzg_uebersteuern verlangt — die Seite braucht drei Rechte, nicht eines. Richtig: app.arbzg_befund_quittieren existiert, prueft dienstplan.arbzg_lesen (das schwaechere Recht), setzt now() und protokolliert, und wird in src/ nirgends gerufen. | **beleg**: psql: select has_table_privilege('cse_app','arbeitszeit_verstoss','update') => f; psql: begin; set local role cse_app; update arbeitszeit_verstoss set status='quittiert' where id='490c3fc2-…' => ERROR: permission denied for table arbeitszeit_verstoss; pg_policies arbeitszeit_verstoss (9 Zeilen, UPDATE nur {cse_definer}/{cse_job}); pg_get_functiondef app.arbzg_befund_quittieren (hat_recht('dienstplan.arbzg_lesen')); pg_policies planungs_konflikt t_mandant qual: hat_recht('dienstplan.lesen', app.aktiver_mandant()); arbeitszeit_verstoss t_mandant qual: hat_recht('dienstplan.arbzg_lesen', …); src/server/registry/routen.generiert.ts:182 (lesen: ["dienstplan.arbzg_uebersteuern"]); grep arbzg_befund_quittieren in src/ => keine Treffer. | **aufwand_korrigiert**: mittel (unveraendert) — Blockertext (1) berichtigen ('bricht mit permission denied ab', nicht 'still 0 Zeilen') und Blocker (4) ergaenzen: zusaetzlich dienstplan.lesen + dienstplan.arbzg_lesen erforderlich

- **pfad**: /portal/[mandant]/dienstplan/serien/[id] | **behauptet**: Dritter Abschnitt: "bestehende Zeilen aus turnus_ausnahme mit Datum, Art (ausfall / verschiebung), Ersatzbeginn, Dauer und Pflichtgrund, dazu ein Formular zum Anlegen"; Kopf mit "Bezeichnung, Objekt/Revier bzw. Posten, Quelle, RRULE …, Beginn …, Feiertagsregel und Bundesland, Geltungszeitraum"; es fehle "eine Einzel-Lesefunktion (leseSerie)". | **wirklich**: Zwei Ungenauigkeiten, die genau den Fehler wiederholen, an dem der Konflikteingang schon einmal hing (dort stand der rohe Enum-Wert, weil die Schluessel nicht die wirklichen waren). (1) turnus_ausnahme_art hat DREI Werte: ausfall, zusatz, verschiebung — und 'zusatz' ist im Generator implementiert (eigener Zweig, der aus der Ausnahme ein zusaetzliches Vorkommnis macht). Eine Liste/ein Formular mit nur zwei Arten laesst eine bestehende zusatz-Zeile unbeschriftet und den einzigen Weg, eine Zusatzschicht in eine Serie zu haengen, unerreichbar. (2) planungsserie traegt KEINE der genannten Kopffelder: keine bezeichnung, kein objekt_id, keine rrule, kein dtstart, keine dauer_minuten, keine feiertagsregel, kein gueltig_ab/gueltig_bis — nur turnus_id/posten_id/veranstaltung_id, quelle, zeitzone, feiertage_ueberspringen, feiertag_bundesland, horizont_tage, generiert_bis, letzte_generierung_am, letzte_meldung. leseSerie ist damit kein Einzelsatz-Read, sondern der coalesce-Join ueber turnus+posten+revier+objekt, wie er in der Listenseite schon steht (Feiertagsregel fuer eine Postenserie wird dort sogar aus ps.feiertage_ueberspringen abgeleitet). Der Hauptblocker der Planung stimmt dagegen: ladeAusnahmen() kehrt bei serie.turnusId === null sofort mit [] zurueck und liest nur turnus_ausnahme; posten_ausnahme wird nirgends in src/ gelesen (nur in rls.ts und einem Kommentar), und cse_app darf hineinschreiben — die Zeile entsteht also und wirkt nie. | **beleg**: psql: select enumlabel from pg_enum … turnus_ausnahme_art => ausfall, zusatz, verschiebung; src/server/services/dienstplan/vorkommnisse.ts:316 (if (a.art !== 'zusatz') continue) und :72 ('Nur bei verschiebung verlangt; bei zusatz zulaessig'); psql: information_schema.columns planungsserie (26 Spalten, keine der genannten); src/app/portal/[mandant]/dienstplan/serien/page.tsx:78-100 (coalesce(t.bezeichnung, p.bezeichnung), coalesce(t.rrule, p.abdeckung_rrule), …); src/server/services/dienstplan/generator.ts:394-402 (ladeAusnahmen: if (serie.turnusId === null) return []; from turnus_ausnahme); grep posten_ausnahme in src/ => nur rls.ts:837/1988, rrule.ts:31, generator.ts:390 (Kommentar); psql: has_table_privilege('cse_app','posten_ausnahme','insert') => t. | **aufwand_korrigiert**: mittel (unveraendert) — aber der Ausnahmeteil deckt DREI Arten (inkl. zusatz), und leseSerie ist ein Traeger-Join, kein Einzelsatz-Read

- **pfad**: /portal/[mandant]/dienstplan/konflikte/[id]/quittung | **behauptet**: "recht: dienstplan.konflikt_quittieren", "blocker: keiner" (nur zwei Hinweise), und die Seite zeigt "bei ArbZG Regel/ist_minuten/grenzwert_minuten aus arbeitszeit_verstoss". | **wirklich**: Der Schreibweg ist wirklich vollstaendig (POST /api/konflikt prueft dienstplan.konflikt_quittieren schreibend, weist blockiert mit 409 ab, verlangt >= 10 Zeichen; der Ausloeser kern.planungs_konflikt_quittierung re-prueft dasselbe Recht und setzt now()), und die Eingangsliste quittiert tatsaechlich inline. Aber 'blocker: keiner' uebersieht eine Rechte-Asymmetrie: dienstplan.konflikt_quittieren allein OEFFNET die Seite, macht sie aber nicht lesbar — planungs_konflikt ist fuer cse_app nur mit dienstplan.lesen lesbar, der ArbZG-Block nur mit dienstplan.arbzg_lesen. Die Schwesterroute /dienstplan/konflikte deklariert beide Rechte, die Detailroute nur eines. Eine Sitzung, die das Tor passiert, aber dienstplan.lesen nicht haelt, bekommt notFound() ohne Grund; haelt sie arbzg_lesen nicht, bleibt der ArbZG-Kasten still leer (left join auf arbeitszeit_verstoss). Im geseedeten Rollenspiegel halten super_admin/admin/leitung alle drei Rechte, der Fall zuendet heute also nicht — die Seite muss die Rechte aber selbst verlangen bzw. den leeren ArbZG-Block benennen, statt ihn zu verschweigen. | **beleg**: pg_policies planungs_konflikt t_mandant qual: (mandant_id = app.aktiver_mandant()) AND hat_recht('dienstplan.lesen', …); arbeitszeit_verstoss t_mandant qual: hat_recht('dienstplan.arbzg_lesen', …); src/server/registry/routen.generiert.ts:180 (Liste: lesen [dienstplan.lesen, dienstplan.arbzg_lesen]) gegen :181 (Detail: lesen [dienstplan.konflikt_quittieren]); src/server/auth/zugang.ts:161-167 (pruefeZugang prueft genau bewachung.lesen); src/app/portal/[mandant]/dienstplan/konflikte/page.tsx:106-135 (left join arbeitszeit_verstoss v) und :264 (form action=/api/konflikt). | **aufwand_korrigiert**: klein (unveraendert) — 'blocker: keiner' wird zu 'Seite muss dienstplan.lesen mitverlangen und den ArbZG-Block ohne dienstplan.arbzg_lesen ausdruecklich als nicht sichtbar ausweisen'

- **pfad**: /portal/[mandant]/zeiten/einwaende/[id] | **behauptet**: "blocker: keiner" (nur ein Seed-Hinweis); rechts der Zeiteintrag mit "Serverzeit, geraete_zeit, zeitabweichung_sek, Dauer"; das Entscheidungsformular laeuft gegen die bestehende API und ist damit fertig. | **wirklich**: Die Dienstlage stimmt (einwand.ts hat alle genannten Ausfuhren, POST /api/zeit/einwand/entscheidung prueft zeit.einwand_entscheiden und >= 10 Zeichen ausser bei in_pruefung; nur leseEinwand fehlt), und die Seed-Kritik ist berechtigt (genau 1 zeit_einwand, offen). Zwei Berichtigungen: (1) Die genannten Spalten heissen nicht so: zeiteintrag traegt geraete_zeit_beginn/geraete_zeit_ende und zeitabweichung_beginn_sek/zeitabweichung_ende_sek sowie dauer_brutto_minuten/dauer_netto_minuten — also je zwei Werte, nicht je einen; ein 'zeitabweichung_sek' gibt es nicht. (2) Ein Blocker ist doch da: der Ausloeser verbietet die Entscheidung ueber den EIGENEN Einwand ('Ueber den eigenen Einwand entscheidet man nicht', EMP-07), entscheideEinwand prueft das vorher NICHT, und die API faengt check_violation nicht ab (sie kennt nur NichtGefunden/BereitsEntschieden/Auth) — der Klick endet in einem ungefangenen 500 statt in einer Meldung. Auf der Detailseite ist das der wahrscheinlichste Weg dorthin, weil dort die betroffene Person ihren eigenen Vorgang offen vor sich hat. Zusaetzlich gilt dieselbe Asymmetrie wie bei den Konfliktrouten: zeit_einwand ist fuer cse_app nur mit zeit.lesen lesbar, das Tor verlangt nur zeit.einwand_entscheiden. | **beleg**: psql: information_schema.columns zeiteintrag => geraete_zeit_beginn, geraete_zeit_ende, zeitabweichung_beginn_sek, zeitabweichung_ende_sek, dauer_brutto_minuten, dauer_netto_minuten; pg_get_functiondef kern.zeit_einwand_status (raise exception 'Ueber den eigenen Einwand entscheidet man nicht' … errcode check_violation); src/server/services/zeit/einwand.ts:189-221 (entscheideEinwand ohne Selbstpruefung); src/app/api/zeit/einwand/entscheidung/route.ts:98-120 (catch-Kette ohne check_violation, schliesst mit throw fehler); pg_policies zeit_einwand t_mandant qual: hat_recht('zeit.lesen', …) gegen routen.generiert.ts:191 (lesen [zeit.einwand_entscheiden]); psql: select count(*) from zeit_einwand => 1, alle offen. | **aufwand_korrigiert**: klein (unveraendert), aber nicht blockerfrei: Selbstentscheidung vorher abfangen (sonst 500), zeit.lesen mitverlangen, Spaltennamen paarweise fuehren
