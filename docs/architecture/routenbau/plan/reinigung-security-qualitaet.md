# PLAN reinigung-security-qualitaet
## Gemeinsames
Alle elf Routen liegen heute im selben Zustand: keine eigene page.tsx, beantwortet vom Sammelweg /home/user/cse-platform/src/app/portal/[mandant]/[...rest]/page.tsx, der ueber MandantUnterseite in /home/user/cse-platform/src/app/portal/unterseite.tsx die Seite NochNichtGebaut zeigt — mit Wache, Rechtepruefung und Modulsperre, aber ohne Inhalt. KEINE der elf braucht eine Migration: turnus, turnus_ausnahme, sonderleistung, qualitaetspruefung, qualitaetspruefung_position, pruefverfahren, bewacher_eintrag und veranstaltung stehen samt RLS und Archivregeln in der laufenden Datenbank. Geteilt sind stattdessen vier Dinge. (1) EINE Huelle: mandantTor/MandantAntwort aus src/app/portal/unterseite.tsx, darin PortalRahmen mit leiste, sichtbareTabs und navigationsRechte aus portalZugang; die Modulsperre (mandant.module gegen den Modulteil des Rechts) sitzt bereits zentral in src/app/portal/zugang.ts und gehoert nicht in die Seiten. (2) EIN Lesepfad: mitLesekontext aus src/app/portal/[mandant]/reinigung/daten.ts beziehungsweise db().begin(SCHNAPPSCHUSS, … withTenant …); jede Zeit kommt als Berliner Ortszeit und jede Zahl als numeric- oder bigint-Text FERTIG aus der Datenbank, Node rechnet nichts um. (3) EINE Schreibstrecke: Formular-POST auf einen Handler unter src/app/api/… nach dem Muster von src/app/api/dienstplan/serien/route.ts — istGleicherUrsprung, aktuelleSitzung, authorize(recht, schreibend) mit rechtepruefer innerhalb von withTenant, danach zurueck auf die Liste; dazu haeltRechte aus src/app/portal/rechte.ts, damit kein Verweis auf eine 404 zeigt (AUT-06). (4) EINE Seed-Luecke, die vier der elf Seiten leer laesst: sonderleistung, qualitaetspruefung(_position), bewacher_eintrag und veranstaltung haben null Zeilen, und fuer veranstaltung gibt es im ganzen Baum keinen Anlegeweg. Das ist gemeinsame Arbeit in src/server/db/seed/security.ts und src/server/db/seed/reinigung.ts und keine Nebensache — ohne sie ist „Seed exerziert es\" fuer diese vier nicht erfuellt. Zwei Nacharbeiten fallen ebenfalls einmal an: die Menuepunkte in src/server/registry/navigation.ts:226 und :238 zeigen ausdruecklich deshalb auf security/posten und reinigung/reviere, weil die Modulwurzeln fehlten — nach dem Bau gehoeren sie auf die Wurzeln; und die inline in src/app/portal/[mandant]/dienstplan/serien/page.tsx stehende Serienabfrage muss in einen Dienst gehoben werden, bevor die Turnusliste sie ein zweites Mal braucht. Offen und nicht zu erfinden bleiben O-29 (Qualitaetspruefung: Ausloeser, Skala, Schwelle, Folge), O-40 (Bewacher-ID-Format und Pflichtfelder) und O-210 (zaehlt soll_besetzung als Mindestbesetzung).
## Routen

### /portal/[mandant]/reinigung
- zustand: platzhalter
- dienst_vorhanden: Teilweise. /home/user/cse-platform/src/app/portal/[mandant]/reinigung/daten.ts (mitLesekontext, ladeReviere, findeRevier, ladeZugeordneteRaeume); /home/user/cse-platform/src/server/services/reinigung/leistungsnachweis.ts (listeNachweise mit Statusfilter, NachweisKopf); /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/daten.ts (ladePlanfenster fuer die heutigen Einsaetze). Eine Uebersichtsfunktion fuer den Modulkopf gibt es nicht; die Turnus-Gesundheit (planungsserie.generiert_bis gegen heute) ist nirgends als Funktion gefasst.
- tabellen_vorhanden: True
- recht: reinigung.lesen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/reinigung/reviere/page.tsx
- aufwand: mittel
- blocker: keiner — alle Tabellen sind migriert und geseedet (7 turnus, 6 revier, 4 leistungsnachweis im Mandanten reinigung). Nachzuarbeiten ist eine Kleinigkeit ausserhalb der Seite: /home/user/cse-platform/src/server/registry/navigation.ts:238 zeigt den Menuepunkt „Reinigung" bewusst auf reinigung/reviere, weil die Modulwurzel fehlt; nach dem Bau gehoert der Pfad auf reinigung.

**Plan**

Modulkopf mit drei KpiStat-Kacheln (heute geplante Reviere/Einsaetze, offene Leistungsnachweise im Status entwurf/vorgelegt, stehengebliebene Serien) und darunter drei kurze Listen. Liste 1: die Einsaetze des Berliner Kalendertages je Revier und Objekt aus ladePlanfenster, mit Besetzungsstand. Liste 2: Leistungsnachweise, die noch nicht signiert sind, verlinkt auf .../reinigung/leistungsnachweise/[id] — der Verweis nur, wenn haeltRechte(sitzung,'nachweis.lesen') gilt (AUT-06, wie in qualitaet/reklamationen/page.tsx). Liste 3: Turnus-Gesundheit — je Serie generiert_bis, gueltig_bis und Zahl der erzeugten Einsaetze; eine Serie, deren generiert_bis in der Vergangenheit liegt, wird als „Generator steht" markiert, nicht als „keine Termine". Die Zahlen kommen fertig aus der Datenbank (Minuten als numeric-Text, Zeiten via at time zone 'Europe/Berlin'), die Seite rechnet nichts.

### /portal/[mandant]/reinigung/sonderleistungen
- zustand: platzhalter
- dienst_vorhanden: keiner. Die Tabelle wird bisher nur lesend in /home/user/cse-platform/src/server/services/finanz/abrechnungsart/einzelabruf.ts benutzt (private Funktion ladeAbrufe, Status 'erbracht', Datierung ausfuehrung_bis→ausfuehrung_von→beauftragt_am). Es gibt keine Liste, kein Anlegen, keine Statuspflege.
- tabellen_vorhanden: True
- recht: reinigung.lesen (lesen) / katalog.schreiben (schreiben) — beide im Katalog (katalog.generiert.ts:196 bzw. :148)
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/qualitaet/reklamationen/page.tsx
- aufwand: mittel
- blocker: Zwei Dinge, beide echt. (1) Die Zeile der Seitenkarte (04-SEITENKARTE.md:1096) beschreibt Katalogeintraege mit eigenen Zeitwerten und vergibt katalog.schreiben — der Datenbestand dahinter sind aber ZWEI Dinge: leistungskatalog_position (Zeitwert, Einheit, Preis, ist_platzhalter) und sonderleistung (der einzelne Abruf: beauftragt_am, Menge, Status, Leistungsnachweis). Welche Haelfte diese Seite pflegt, steht nirgends fest; /portal/[mandant]/leistungskatalog ist selbst noch nicht gebaut (Phase 4, katalog.lesen), es gibt also noch keine Nachbarseite, auf die der Katalogteil verweisen koennte. (2) Seed: sonderleistung hat 0 Zeilen und leistungskatalog_position genau 1 (das Platzhalterelement) — ohne Seed-Erweiterung zeigt die Seite eine leere Tabelle und die Definition-of-done ist nicht erfuellt. Preise gehoeren ausdruecklich NICHT auf die Abrufzeile (0067_sonderleistung.sql: keine Preisspalte, Bepreisung ueber auftrag_leistung) — kein Preisfeld erfinden.

**Plan**

Eine Seite mit zwei getrennten, beschrifteten Abschnitten statt einer vermischten Tabelle. Oben die Katalogzeilen fuer Glas, Sonderreinigung und Warenraeumung aus leistungskatalog_position mit Zeitwert (numeric-Text), Einheit und dem Platzhalterkennzeichen; Bearbeiten nur bei katalog.schreiben und, solange /leistungskatalog fehlt, als Formular auf dieser Seite. Unten die Abrufe aus sonderleistung: Objekt, Kunde, Bezeichnung, beauftragt_am, Ausfuehrungsfenster, Menge/Einheit und Status als StatusPill ueber die sechs Enumwerte, dazu der Verweis auf den Leistungsnachweis, wenn einer haengt. Neuer Dienst /home/user/cse-platform/src/server/services/reinigung/sonderleistung.ts mit listeAbrufe, findeAbruf, erfasseAbruf und setzeStatus — keine Statusautomatik, weil 0067 ausdruecklich keinen gerichteten Pfad festlegt. Storno als eigene Spalte, nie als Loeschung (Invariante 8, loeschsperre steht auf true).

### /portal/[mandant]/reinigung/turnus
- zustand: platzhalter
- dienst_vorhanden: Teilweise. /home/user/cse-platform/src/server/services/dienstplan/serie.ts (wochenRegel, legeTurnusSerieAn, legePlanungsserieAn); /home/user/cse-platform/src/server/services/dienstplan/generator.ts (generiereEinsaetze, liest turnus_ausnahme); /home/user/cse-platform/src/lib/datum/rrule.ts (leseRegel, entfalte, RegelFehler); /home/user/cse-platform/src/server/services/dienstplan/vorkommnisse.ts (planeVorkommnisse). Eine Lese-/Listenfunktion fuer Turnusse gibt es NICHT — die vorhandene Abfrage steht inline in der Seite /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/page.tsx (Zeilen 70–100).
- tabellen_vorhanden: True
- recht: reinigung.lesen / reinigung.schreiben — beide im Katalog
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/page.tsx
- aufwand: mittel
- blocker: Ueberschneidung mit einer gebauten Seite: /portal/[mandant]/dienstplan/serien zeigt Turnus- UND Postenserien in einer Liste und verlangt dienstplan-Rechte; diese Route zeigt nur die Turnusse der Reinigung und verlangt reinigung.lesen. Das ist „zwei Oberflaechen, ein Artefakt" und nur dann unschaedlich, wenn beide DIESELBE Lesefunktion benutzen — die SQL zu kopieren waere die zweite Wahrheit ueber „wie viele Termine hat diese Serie". Die Inline-Abfrage muss also zuerst in einen Dienst gehoben werden.

**Plan**

Die Inline-Abfrage aus dienstplan/serien/page.tsx nach /home/user/cse-platform/src/server/services/reinigung/turnus.ts heben (listeTurnusse, findeTurnus) und die bestehende Seite darauf umstellen, damit es eine Quelle bleibt. Die Liste zeigt je Turnus: Objekt, Revier, Bezeichnung, die Regel in Klartext (aus leseRegel entfaltet, nicht der rohe RRULE-String), Beginn als Berliner Wanduhr HH:MM, Dauer in Minuten (integer, als Sollangabe beschriftet), Feiertagsregel, Geltungszeitraum, generiert_bis und die Zahl lebender Einsaetze. Archivierte Turnusse stehen am Ende und sind als solche markiert, nicht ausgeblendet. Eine Regel, die leseRegel zurueckweist, wird als Fehlerzeile gezeigt statt stillschweigend uebersprungen.

### /portal/[mandant]/reinigung/turnus/[id]
- zustand: platzhalter
- dienst_vorhanden: Teilweise. planeVorkommnisse aus /home/user/cse-platform/src/server/services/dienstplan/vorkommnisse.ts liefert die Vorschau der Termine samt DST-Anomalie und Feiertagsausfall; entfalte/leseRegel aus /home/user/cse-platform/src/lib/datum/rrule.ts. Es gibt keine findeTurnus-Funktion, keine aendereTurnus-Funktion und keinen Dienst fuer turnus_ausnahme — der Seed schreibt die eine vorhandene Ausnahme direkt (src/server/db/seed/dienstplan.ts:170), gelesen wird sie nur vom Generator.
- tabellen_vorhanden: True
- recht: reinigung.lesen / reinigung.schreiben — beide im Katalog
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/reinigung/reviere/[id]/page.tsx
- aufwand: mittel
- blocker: Offen ist die Wirkung einer Aenderung, nicht eine Tabelle. Ein Turnus haengt an einer planungsserie, aus der bereits Einsaetze materialisiert sind; was mit diesen Einsaetzen geschieht, wenn jemand die Regel, die Uhrzeit oder die Dauer aendert (nachziehen, nur zukuenftige, oder Serie beenden und neu anlegen), ist nirgends entschieden. Ohne diese Antwort wird die Detailseite lesend plus Ausnahmen gebaut, und das Feld-Bearbeiten bekommt einen TODO(client)-Vermerk und eine Zeile unter „Offen" in docs/DECISIONS.md; eine still gewaehlte Variante veraendert rueckwirkend Schichten, an denen Check-in-Links, Leistungsnachweise und Rechnungen haengen.

**Plan**

Kopf mit Objekt, Revier, Leistung, Regel in Klartext, Beginn (Berliner Wanduhr), Dauer, Feiertagsregel, Geltung und generiert_bis. Darunter die Vorkommnis-Vorschau der naechsten acht Wochen aus planeVorkommnisse: Plandatum, Ortszeit, der daraus folgende UTC-Instant und die Kennzeichnung dst_luecke / dst_doppelt sowie feiertagsbedingter Ausfall — die Vorschau rechnet ueber die Wanduhr, nie ueber UTC. Dritter Block: die Ausnahmen aus turnus_ausnahme (Datum, Art ausfall/zusatz/verschiebung, Ersatzbeginn, Dauer, Grund, abrechnungsrelevant) mit einem Anlegeformular hinter reinigung.schreiben; abrechnungsrelevant bleibt leer, wenn niemand es setzt, und wird nicht aus der Art abgeleitet. Vierter Block: die bereits erzeugten Einsaetze mit Besetzungsstand, verlinkt in den Dienstplan, sofern das Recht dafuer gehalten wird.

### /portal/[mandant]/reinigung/turnus/neu
- zustand: platzhalter
- dienst_vorhanden: Ja, ueberwiegend. legeTurnusSerieAn und wochenRegel in /home/user/cse-platform/src/server/services/dienstplan/serie.ts, dahinter generiereEinsaetze; die Schreibstrecke existiert als Formular-POST in /home/user/cse-platform/src/app/api/dienstplan/serien/route.ts (art=turnus) und wird heute von /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/neu/page.tsx bedient.
- tabellen_vorhanden: True
- recht: reinigung.lesen / reinigung.schreiben — beide im Katalog
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/neu/page.tsx
- aufwand: mittel
- blocker: Zwei konkrete Luecken. (1) Rechtebruch zwischen Manifest und Schreibstrecke: die Route verlangt laut Seitenkarte reinigung.schreiben, der bestehende API-Handler autorisiert hart auf dienstplan.schreiben (api/dienstplan/serien/route.ts, authorize-Aufruf). Eine Reinigungsleitung ohne Dienstplanrecht bekaeme 403 auf einer Seite, die ihr Recht anzeigt. Der Handler muss beide Wege kennen, oder die Seite braucht einen eigenen Handler, der denselben Dienst ruft. (2) wochenRegel baut ausschliesslich FREQ=WEEKLY;BYDAY=… — die Seitenkarte verlangt einen RRULE-Builder nach RFC 5545, und der Seed enthaelt bereits monatliche Turnusse (FREQ=MONTHLY;BYMONTHDAY=15). Der monatliche Zweig fehlt im Dienst und muss dort entstehen, nicht im Formular.

**Plan**

Formular mit Revier (nur Reviere des aktiven Mandanten), Leistungskatalogposition, Bezeichnung, Frequenz woechentlich/monatlich, Wochentagen bzw. Monatstag, Beginn als Berliner Wanduhr HH:MM, Dauer in Minuten, Geltung ab/bis und Feiertagsregel. Die Regel wird gebaut und vom selben Parser gegengelesen, der sie spaeter entfaltet (leseRegel) — was der Parser abweist, wird nicht gespeichert. Vor dem Absenden eine Terminvorschau der ersten vier Wochen aus planeVorkommnisse, damit sichtbar ist, was entsteht. Nach dem Anlegen laeuft der Generator sofort und die Seite meldet die Zahl der erzeugten Schichten sowie das, was er uebersprungen hat, statt es zu verschweigen.

### /portal/[mandant]/security
- zustand: platzhalter
- dienst_vorhanden: Teilweise. /home/user/cse-platform/src/server/services/security/posten.ts (postenUebersicht, unterbesetzung, assertBesetzungVeroeffentlichbar); /home/user/cse-platform/src/server/services/security/wachbuch.ts (leseBuch mit BuchFilter, leseEintrag); /home/user/cse-platform/src/server/services/security/eventbesetzung.ts (kommendeVeranstaltungen). Die ablaufenden Nachweise liegen NICHT in einem Dienst, sondern seitenlokal in /home/user/cse-platform/src/app/portal/[mandant]/personal/nachweise/daten.ts (leseRegister, lageVon, leseWarnungen); nachweislage in src/server/services/nachweis/uebersicht.ts arbeitet nur je Person.
- tabellen_vorhanden: True
- recht: security.lesen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/security/posten/page.tsx
- aufwand: mittel
- blocker: „Offene Vorfaelle" gibt es als Zustand nicht: wachbuch kennt keine Spalte offen/geschlossen, eine Korrektur ist eine nachfolgende Zeile (ersetzt_durch_id), und ein Vorfall wird nie geschlossen. Eine Statusspalte zu erfinden waere eine Geschaeftsregel; die Kachel zaehlt deshalb Vorfaelle eines benannten Zeitfensters und sagt das im Text. Zweitens haengen die ablaufenden Nachweise an personal.nachweis_lesen (RLS-Policy be_lesen / Nachweispolicies) — eine reine Security-Rolle sieht dort nichts; die Kachel und ihr Verweis werden ueber haeltRechte gekapselt (AUT-06), statt auf eine 404 zu fuehren. Drittens muss leseRegister aus der Seitendatei in einen Dienst gehoben werden, sonst liest eine Seite die daten.ts einer anderen Seite.

**Plan**

Modulkopf mit drei Kacheln und drei Listen. Kachel/Liste 1: heute besetzte und unterbesetzte Posten aus postenUebersicht/unterbesetzung, mit der Fehlzahl je Posten und Zeitfenster. Kachel/Liste 2: §34a-Nachweise, die abgelaufen sind oder im Vorwarnfenster stehen, mit Restfrist und der Lage-Beschriftung aus lageVon — die Schwellen kommen aus qualifikation.warnung_tage, nie aus einer Konstante in der Seite; SEC-04 ist eine harte Sperre, deshalb wird abgelaufen als Sperrgrund benannt und nicht als Warnung. Kachel/Liste 3: Wachbucheintraege der Art vorfall aus den letzten sieben Berliner Tagen, mit Objekt, Nummer, Uhrzeit und dem Hinweis polizei_informiert; die Ueberschrift sagt ausdruecklich „letzte 7 Tage" statt „offen". Dazu ein Verweisblock auf Wachbuch, Posten, Veranstaltungen, Dienstanweisungen, Schluessel und Bewacherregister, jeder Verweis nur mit dem Recht dahinter.

### /portal/[mandant]/security/bewacherregister
- zustand: platzhalter
- dienst_vorhanden: Nur je Person. nachweislage in /home/user/cse-platform/src/server/services/nachweis/uebersicht.ts liefert eine Bewacherlage (vorhanden, bewacherId, status, gueltigBis, gueltigAmStichtag, quelle 'manuell', verbindung 'nicht_verbunden') fuer EINE person_id; genutzt in personal/nachweise/[id]/page.tsx und personal/personen/[id]/page.tsx. Es gibt keine Liste ueber die Personen des Mandanten und KEINEN Schreibweg — nirgends im Baum wird ein bewacher_eintrag angelegt oder geaendert, auch nicht im Seed.
- tabellen_vorhanden: True
- recht: personal.bewacher_verwalten — im Katalog (katalog.generiert.ts:167, gebunden an super_admin/admin/leitung, bindbar leer)
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/personal/nachweise/page.tsx
- aufwand: mittel
- blocker: Drei Punkte. (1) O-40 ist offen: Bewacher-ID-Format samt Pruefziffer, Pflichtfelder, Statusvokabular und Meldeereignisse. Es wird nichts validiert — das Feld nimmt, was die Behoerde ausgestellt hat, und die Seite kennzeichnet den Stand als handerfasst; der TODO(client, O-40) steht bereits in 04-SEITENKARTE.md §5.8 und gehoert in den Dienst. (2) Es gibt keine Schnittstelle zum Bewacherregister — kein simulierter Abgleich, kein Knopf „Status abrufen"; die Seite schreibt „nicht verbunden" hin, wie Bewacherlage.verbindung es bereits fuehrt. (3) bewacher_eintrag traegt KEIN mandant_id (der Eintrag haengt am Menschen, D-09); die Liste muss ueber anstellung auf den aktiven Mandanten eingegrenzt werden und darf dabei kein Entgeltfeld mitnehmen — die Feldliste von uebersicht.ts (BEWACHERLAGE_FELDER) ist dafuer der festgeschriebene Rahmen. Nebenbefund: RLS be_lesen verlangt personal.nachweis_lesen oder die eigene Person, be_aendern verlangt personal.bewacher_verwalten — beide Rechte sind an dieselben drei Rollen gebunden und nicht frei bindbar, die Luecke ist heute theoretisch, aber der Lesepfad ist damit an ein anderes Recht gebunden als die Route. Und: 0 Zeilen im Seed.

**Plan**

Eine Registerliste ueber die Personen mit lebender Anstellung im aktiven Mandanten: Name, Bewacher-ID, Status (beantragt/registriert/abgelehnt/erloschen/gesperrt/unbekannt) als StatusPill, registriert_seit, gueltig_bis mit Restfrist, letzte und naechste Pruefung, Registerauszug als Dokumentverweis. Sortiert nach Ablauf, nicht nach Name — wie das Nachweisregister. Personen ohne Eintrag stehen mit „kein Eintrag erfasst" in derselben Liste, weil genau sie die Luecke sind, die SEC-03 sichtbar machen soll. Neuer Dienst src/server/services/security/bewacherregister.ts (leseRegister, erfasseEintrag, aktualisiereEintrag) plus ein API-Handler nach dem Muster von api/dienstplan/serien/route.ts, autorisiert auf personal.bewacher_verwalten. Kein Feldformat wird geprueft, keine Frist wird abgeleitet, und ueber der Tabelle steht ein Satz, dass der Stand handerfasst ist.

### /portal/[mandant]/security/veranstaltungen/[id]
- zustand: platzhalter
- dienst_vorhanden: Teilweise. /home/user/cse-platform/src/server/services/security/eventbesetzung.ts: kommendeVeranstaltungen (Liste mit Besetzungsstand), erzeugeVeranstaltungsschicht, besetzeVeranstaltung, VeranstaltungNichtGefunden, VeranstaltungOhneObjekt. Eine findeVeranstaltung-Funktion fuer das Einzelblatt fehlt.
- tabellen_vorhanden: True
- recht: security.lesen (das Kind /besetzung verlangt dienstplan.schreiben)
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/security/veranstaltungen/page.tsx
- aufwand: mittel
- blocker: Es gibt keine einzige Veranstaltung und keinen Weg, eine anzulegen: veranstaltung hat 0 Zeilen, der Seed schreibt keine, und im ganzen Baum steht kein insert into veranstaltung. Die Seitenkarte §5.8 fuehrt auch keine Route /veranstaltungen/neu — woher ein Eventauftrag kommt (aus auftrag_leistung, aus dem Vertrieb, handerfasst), ist nicht entschieden. Ohne Antwort ist die Detailseite baubar, aber nicht durch Seeddaten belegbar; mindestens eine Seedzeile in src/server/db/seed/security.ts gehoert dazu, sonst ist die Definition-of-done nicht erfuellt. Zweiter Punkt: O-210 ist offen — ob soll_besetzung zugleich Mindestbesetzung ist; die Seite darf daraus also keine Dringlichkeit und keine Veroeffentlichungssperre ableiten, sie zeigt nur die Zahl.

**Plan**

Kopf mit Bezeichnung, Anlass, Kunde, Ort (Objekt oder veranstaltungsort_text, mit dem bestehenden Warnhinweis, dass ein reiner Texteort keine Schichten traegt), Beginn und Ende als Berliner Ortszeit aus der Datenbank, erwartete Besucher, Sollbesetzung und Leitung. Darunter der Besetzungsstand: die erzeugte Eventschicht mit den eingeteilten Menschen, jeweils mit §34a-Nachweislage, und ein Verweis auf .../besetzung — der Verweis nur, wenn dienstplan.schreiben gehalten wird, sonst steht der Stand als Text (AUT-06, wie in der Liste daneben). Dritter Block: die zugeordnete Dienstanweisung mit Verweis auf ihre Version, sofern dienstanweisung.lesen gehalten wird. Keine Ampel und kein „dringend" aus soll_besetzung, solange O-210 offen ist.

### /portal/[mandant]/qualitaet/pruefungen
- zustand: platzhalter
- dienst_vorhanden: Teilweise. /home/user/cse-platform/src/server/services/reinigung/qualitaet.ts: listePruefverfahren, erfassePruefung, schwellenBewertung/QualitaetsBewertung, summiereText, PRUEFUNG_NUMMER_PRAEFIX, PruefungNichtGefunden. Es fehlen listePruefungen und findePruefung — der Lesepfad auf qualitaetspruefung existiert noch gar nicht.
- tabellen_vorhanden: True
- recht: qualitaet.lesen / qualitaet.schreiben — beide im Katalog (Zeilen 179/180); qualitaet ist ein Querschnittsmodul (registry/modul.ts), also nicht an ein Gewerk gebunden
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/qualitaet/reklamationen/page.tsx
- aufwand: mittel
- blocker: O-29 ist offen (Ausloeser, Skala, Bestehensschwelle, Folge einer nicht bestandenen Pruefung). In der Datenbank steht je Mandant genau ein Pruefverfahren „unbestimmt" mit max_punkte NULL, bestehensschwelle_prozent NULL und ist_platzhalter true; qualitaetspruefung.bestanden bleibt NULL. Die Liste zeigt deshalb KEINE Bestanden-Pille und keinen Erfuellungsgrad als Bewertung. Zweitens: qualitaetspruefung hat 0 Zeilen im Seed — ohne Seed-Erweiterung ist die Seite leer.

**Plan**

Tabelle der Pruefungen: Nummer (QP-JJJJ-NNNN), geprueft_am als Berliner Ortszeit, Bezug (Objekt, Revier oder Projekt — qp_ein_anker verlangt mindestens Objekt oder Projekt), Pruefverfahren mit sichtbarem Platzhalterkennzeichen, Pruefer (Anstellung oder externer Name), „mit Kunde" und die Zahl der Befunde, davon nio. Statt einer Bewertungsspalte steht dort, solange O-29 offen ist, der Erfuellungsgrad nur dann, wenn das Verfahren eine Skala traegt, und sonst ein Strich mit Erklaerzeile ueber der Tabelle. Zwei KpiStat-Kacheln: Pruefungen im laufenden Monat und offene Maengelfristen (qualitaetspruefung_position.frist_am in der Vergangenheit, Ergebnis nio). Neue Dienstfunktionen listePruefungen(filter) und findePruefung in services/reinigung/qualitaet.ts, kein SQL in der Seite.

### /portal/[mandant]/qualitaet/pruefungen/[id]
- zustand: platzhalter
- dienst_vorhanden: Teilweise. Dieselbe Datei /home/user/cse-platform/src/server/services/reinigung/qualitaet.ts; findePruefung und eine Befundliste (qualitaetspruefung_position) fehlen beide. Fuer den Medienverweis gibt es /home/user/cse-platform/src/app/api/medien/[id]/route.ts.
- tabellen_vorhanden: True
- recht: qualitaet.lesen / qualitaet.schreiben
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/qualitaet/reklamationen/[id]/page.tsx
- aufwand: mittel
- blocker: O-29 wie bei der Liste: kein Urteil, keine Folge. Ausserdem ist nicht modelliert, was aus einem Mangel wird — qualitaetspruefung_position traegt mangel_beschreibung und frist_am, aber KEINEN Schluessel auf reklamation oder aufgabe; eine Verknuepfung „Mangel wird Reklamation" waere erfunden und wird nicht gebaut, sondern als offene Zeile notiert. Drittens ist der Korrekturweg offen: die Tabelle hat archiviert_am und loeschsperre, aber kein ersetzt_durch_id wie das Wachbuch — wie eine falsch erfasste Pruefung berichtigt wird, ist nicht entschieden; bis dahin ist das Blatt lesend, Aenderungen sind nicht vorgesehen und Loeschen ohnehin nicht (Invariante 8).

**Plan**

Kopfblatt: Nummer, Bezug (Objekt/Revier/Projekt/Posten/Kunde), Verfahren mit Platzhalterkennzeichen, Pruefzeitpunkt als Berliner Ortszeit, daneben geraete_zeit und zeitabweichung_sek getrennt ausgewiesen (Invariante 5), Kennzeichen nachgetragen, Pruefer, mit_kunde, Bemerkung und der Dokumentverweis. Darunter die Befunde in Reihenfolge: Kriterium, Raum bzw. Revierraum, Ergebnis io/nio/nicht_pruefbar, Punkte als numeric-Text, Mangelbeschreibung, Frist und das Foto ueber den signierten Medienweg. Ueberfaellige Fristen werden farblich UND im Text markiert (DESIGN §9). Statt eines Bestanden-Felds steht ein Satz, warum keines da ist, mit dem Verweis auf O-29.

### /portal/[mandant]/qualitaet/pruefungen/neu
- zustand: platzhalter
- dienst_vorhanden: Ja fuer das Schreiben: erfassePruefung in /home/user/cse-platform/src/server/services/reinigung/qualitaet.ts (vergibt die Nummer, summiert die Punkte in TypeScript, legt die Befunde an, setzt bestanden nur, wenn eine Schwelle existiert). Es fehlt der HTTP-Weg — unter /home/user/cse-platform/src/app/api/qualitaet/ gibt es nur reklamationen/route.ts — und es fehlen die Auswahllisten (Objekte, Reviere, Raeume, Anstellungen).
- tabellen_vorhanden: True
- recht: qualitaet.schreiben
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/qualitaet/reklamationen/neu/page.tsx (Formular) und /home/user/cse-platform/src/app/api/qualitaet/reklamationen/route.ts (Schreibstrecke)
- aufwand: mittel
- blocker: O-29: es gibt keinen Kriterienkatalog. qualitaetspruefung_position.kriterium ist freier Text, und eine Tabelle mit vorgegebenen Pruefkriterien existiert nicht — die Kriterien werden also getippt oder aus einer klar als Platzhalter beschrifteten Liste gewaehlt, und es wird keine Punkteskala vorgegeben. Solange das Verfahren keine max_punkte traegt, weist die Datenbank eine Kopfpunktzahl ohnehin zurueck (Pruefregel qp_punkte_brauchen_skala); die einzelnen Befunde duerfen Punkte tragen, damit nichts verloren geht, sobald die Skala kommt. Ausserdem verlangt qp_ein_anker mindestens ein Objekt oder ein Projekt — das Formular muss den Bezug erzwingen, nicht optional anbieten.

**Plan**

Formular in drei Schritten auf einer Seite: Bezug (Objekt pflichtig, Revier und Kunde optional und aus dem Objekt vorgeschlagen), Verfahren (Auswahl aus listePruefverfahren, mit sichtbarem Hinweis, dass das Verfahren ein Platzhalter ohne Skala und ohne Schwelle ist), Pruefer (eigene Anstellung oder externer Name) und das Kennzeichen „mit Kunde". Darunter eine wachsende Befundliste: Kriterium, optionaler Raum aus dem Revier, Ergebnis io/nio/nicht_pruefbar, optionale Punkte, bei nio Mangelbeschreibung und Frist. Abgesendet wird als Formular-POST an einen neuen Handler /api/qualitaet/pruefungen mit istGleicherUrsprung, aktuelleSitzung und authorize auf qualitaet.schreiben innerhalb von withTenant, der erfassePruefung ruft; der Dienst vergibt Nummer und Zeit, das Formular niemals. Die Geraetezeit wird mitgeschickt und getrennt gespeichert, die Serverzeit bleibt massgeblich.

## KRITIK (11 geprueft, 9 Korrekturen)

- **pfad**: /portal/[mandant]/reinigung | **behauptet**: "blocker: keiner — alle Tabellen sind migriert und geseedet"; Liste 3 zeigt "je Serie generiert_bis, gueltig_bis"; Liste 1 kommt aus ladePlanfenster; Kachel 2 zaehlt Nachweise im Status entwurf/vorgelegt. | **wirklich**: Es gibt einen Blocker, und er ist derselbe, den die Planung beim Security-Modulkopf selbst als Blocker fuehrt: zwei der drei Kacheln lesen Tabellen, deren RLS ein ANDERES Recht verlangt als die Route haelt. Die Route ist auf reinigung.lesen bewacht (src/server/registry/routen.generiert.ts:119), aber einsatz und planungsserie verlangen dienstplan.lesen, leistungsnachweis verlangt nachweis.lesen. RLS filtert still, es gibt keine Fehlermeldung — heutige Reviere/Einsaetze, "stehengebliebene Serien" und "offene Leistungsnachweise" waeren dann alle 0, und die Seite meldet "Generator laeuft", wo niemand nachgesehen hat. Die Planung kapselt nur den VERWEIS mit haeltRechte('nachweis.lesen'), nicht die Daten. Zweitens: planungsserie hat keine Spalte gueltig_bis — die Geltung haengt am turnus (turnus.gueltig_ab/gueltig_bis); die bestehende Serienabfrage holt sie deshalb per coalesce(t.gueltig_bis, p.gueltig_bis). Drittens nimmt listeNachweise nur EINEN Status ({status?: string|null}, leistungsnachweis.ts:221-232), "entwurf/vorgelegt" braucht also zwei Aufrufe oder eine Dienstaenderung. | **beleg**: psql: select policyname,cmd,qual from pg_policies where tablename in ('einsatz','planungsserie','leistungsnachweis') → t_mandant je app.hat_recht('dienstplan.lesen') bzw. ('nachweis.lesen'); psql \d planungsserie → kein gueltig_bis (Spalten: … horizont_tage, generiert_bis, letzte_generierung_am …); /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/daten.ts:113 ("from einsatz e") und :192 (einsatz_zuordnung); /home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/page.tsx:85 (coalesce(t.gueltig_bis, p.gueltig_bis)); /home/user/cse-platform/src/server/services/reinigung/leistungsnachweis.ts:221 | **aufwand_korrigiert**: mittel–gross (Rechteentscheid noetig: entweder dienstplan.lesen/nachweis.lesen ins Routenmanifest, oder jede Kachel einzeln ueber haeltRechte kapseln und den Zustand "nicht geprueft" vom Zustand "nichts offen" unterscheiden — wie ladePlanfenster.abwesenheitGeprueft es vormacht)

- **pfad**: /portal/[mandant]/reinigung/sonderleistungen | **behauptet**: "dienst_vorhanden: keiner. Die Tabelle wird bisher nur lesend in .../finanz/abrechnungsart/einzelabruf.ts benutzt … Es gibt keine Liste, kein Anlegen, keine Statuspflege." Dazu neuer Dienst mit setzeStatus, "keine Statusautomatik". Lesen auf reinigung.lesen, oben die Katalogzeilen aus leistungskatalog_position (Zeitwert, Einheit, Preis, ist_platzhalter). | **wirklich**: Es gibt bereits eine Statuspflege, und sie ist finanzwirksam: bei der Rechnungsuebernahme wird sonderleistung.status von 'erbracht' auf 'abgerechnet' geschrieben. Ein frei bedienbares setzeStatus ohne Sperre kann einen bereits abgerechneten Abruf zurueck auf 'erbracht' stellen — dann liest einzelabruf.ts ihn wieder als abrechenbar, und nur der Sperrindex quelle_sonderleistung_uk (0112) verhindert die zweite Rechnung. setzeStatus muss 'abgerechnet' und 'storniert' als Endzustaende behandeln, und das ist eine getestete Regel, keine Oberflaeche. Zweitens: der Katalogteil ist auf dieser Route nicht lesbar — leistungskatalog_position verlangt in der RLS katalog.lesen, die Route haelt lesend nur reinigung.lesen (Schreiben katalog.schreiben). Der Abrufteil (sonderleistung) laeuft dagegen auf reinigung.lesen, passt also. Drittens tragen die Katalogzeilen nicht die genannten Feldnamen: es gibt kein bezeichnung, sondern kurztext/langtext, zeitwert_minuten (numeric) und standard_einzelpreis_cent. | **beleg**: /home/user/cse-platform/src/server/services/finanz/positionsquelle.ts:461 ("update sonderleistung s set status = 'abgerechnet' …") mit Begruendung :442-448; psql: select policyname,qual from pg_policies where tablename='leistungskatalog_position' → t_mandant: app.hat_recht('katalog.lesen', …); psql \d leistungskatalog_position → kurztext, langtext, einheit, zeitwert_minuten, standard_einzelpreis_cent, ist_platzhalter; psql select count(*) from leistungskatalog_position → 1 (Unterhaltsreinigung, ist_platzhalter=t) | **aufwand_korrigiert**: gross (offene Zuordnung Katalog/Abruf, Seed fuer beide Haelften, zweites Leserecht, plus die Statusregel gegen 'abgerechnet' mit Test)

- **pfad**: /portal/[mandant]/reinigung/turnus | **behauptet**: Blocker ist allein die Ueberschneidung mit dienstplan/serien; eine gemeinsame Lesefunktion listeTurnusse loest es. Die Liste zeigt generiert_bis und "die Zahl lebender Einsaetze". | **wirklich**: Eine gemeinsame Lesefunktion loest die Doppelung, nicht das Recht. Genau die beiden Werte, um die es der Seite geht — generiert_bis (planungsserie) und die Zahl lebender Einsaetze (einsatz) — liegen hinter dienstplan.lesen; turnus selbst liegt korrekt hinter reinigung.lesen. Dieselbe Funktion in beiden Seiten liefert also auf /reinigung/turnus 0 Einsaetze und generiert_bis = null, sobald die Sitzung dienstplan.lesen nicht haelt, und zwar still — "Generator steht" waere dann nicht zu unterscheiden von "kein Recht". Auch der naheliegende Abkuerzungsweg ladeSerien aus generator.ts hilft nicht: er liest app.planungsbedarf_eigen, das laut Kopfkommentar dienstplan.schreiben verlangt. | **beleg**: psql: pg_policies t_mandant auf planungsserie und einsatz → app.hat_recht('dienstplan.lesen', …); auf turnus → app.hat_recht('reinigung.lesen', …); /home/user/cse-platform/src/server/services/dienstplan/generator.ts:318-333 (LaufOptionen.eigen → app.planungsbedarf_eigen, "0143, dienstplan.schreiben"); Routenmanifest src/server/registry/routen.generiert.ts:124 (lesen: reinigung.lesen) | **aufwand_korrigiert**: mittel–gross (Dienst heben PLUS Entscheid, ob die Route dienstplan.lesen mitfuehrt oder die Spalten "generiert_bis"/"Einsaetze" bei fehlendem Recht als "nicht geprueft" statt als 0 erscheinen)

- **pfad**: /portal/[mandant]/reinigung/turnus/[id] | **behauptet**: "planeVorkommnisse … liefert die Vorschau der Termine samt DST-Anomalie und Feiertagsausfall"; die Vorschau zeigt "Plandatum, Ortszeit, der daraus folgende UTC-Instant und die Kennzeichnung dst_luecke / dst_doppelt". | **wirklich**: planeVorkommnisse liefert weder den UTC-Instant noch die DST-Kennzeichnung. Sein Rueckgabetyp GeplanterEinsatz hat genau: quellSchluessel, planDatum, beginnLokal, endeLokal, endetAmFolgetag, sollBesetzung, minBesetzung, herkunft, feiertagDatum — kein anomalie-Feld und kein Zeitpunkt; der Instant wird bewusst weggelassen ("Der Instant interessiert hier nicht — Postgres setzt ihn"). Die Anomalie entsteht eine Ebene tiefer in entfalte/loeseOrtszeitAuf (Vorkommnis.anomalie, Zeitanomalie 'keine'|'dst_luecke'|'dst_doppelt') und wird von planeVorkommnisse verworfen. Wer dst_luecke/dst_doppelt und den Instant zeigen will, ruft entfalte bzw. loeseOrtszeitAuf selbst — oder planeVorkommnisse wird um die Felder erweitert (dann ist es eine Dienstaenderung mit Test, kein Seitenbau). Zweitens braucht planeVorkommnisse die Feiertage als Argument (feiertage: ReadonlyMap<string,string>); der einzige Lader ladeFeiertage ist in generator.ts privat. Eine leere Map uebergeben heisst: der Feiertagsausfall fehlt lautlos — genau die Zusage CLN-03/TIM-02. | **beleg**: /home/user/cse-platform/src/server/services/dienstplan/vorkommnisse.ts:85-98 (GeplanterEinsatz, feiertagDatum:97, kein anomalie), :222-227 (Signatur mit feiertage), :356-360 ("Der Instant interessiert hier nicht", anomalie nur lokal); /home/user/cse-platform/src/lib/datum/rrule.ts:142-151 (Vorkommnis.anomalie), :100-101 (ZEITANOMALIEN); /home/user/cse-platform/src/server/services/dienstplan/generator.ts:181 (async function ladeFeiertage — nicht exportiert) | **aufwand_korrigiert**: gross (Vorschau ueber entfalte/loeseOrtszeitAuf neu zusammensetzen oder planeVorkommnisse erweitern, Feiertagslader exportieren, dazu die vier Pflichtfaelle aus CLAUDE.md als Test — plus der ohnehin offene Entscheid zur Wirkung einer Regelaenderung)

- **pfad**: /portal/[mandant]/reinigung/turnus/neu | **behauptet**: "Vor dem Absenden eine Terminvorschau der ersten vier Wochen aus planeVorkommnisse, damit sichtbar ist, was entsteht." Blocker sind nur der Rechtebruch im API-Handler und der fehlende Monatszweig in wochenRegel. | **wirklich**: Die beiden genannten Blocker stimmen (api/dienstplan/serien/route.ts:52 autorisiert hart auf 'dienstplan.schreiben'; wochenRegel baut ausschliesslich FREQ=WEEKLY;BYDAY=…, waehrend der Seed FREQ=MONTHLY;BYMONTHDAY=15 fuehrt und leseRegel MONTHLY bereits versteht). Die Vorschau ist aber nicht einfach "aus planeVorkommnisse": die Funktion verlangt die Feiertagsmap als drittes Argument, und ihr einziger Lader ist privat (generator.ts:181). Ohne ihn zeigt die Vorschau vor dem Absenden Termine an Feiertagen, die der Generator unmittelbar danach ueberspringt — Vorschau und Ergebnis widersprechen sich auf derselben Seite. Zudem lehnt pruefeTurnusEingabe eine Dauer > 1440 ab, waehrend planeVorkommnisse bei MAX_DAUER_MINUTEN = 24*60-1 liegt: zwei Grenzen fuer dasselbe Feld, die das Formular nicht erfinden darf. | **beleg**: /home/user/cse-platform/src/server/services/dienstplan/vorkommnisse.ts:222-227 und :122 (MAX_DAUER_MINUTEN = 24*60-1); /home/user/cse-platform/src/server/services/dienstplan/generator.ts:181; /home/user/cse-platform/src/server/services/dienstplan/serie.ts:80-94 (wochenRegel), :100-102 (dauer 15…1440); /home/user/cse-platform/src/app/api/dienstplan/serien/route.ts:51-54; psql: select rrule from turnus → FREQ=MONTHLY;BYMONTHDAY=15 (2 Zeilen) | **aufwand_korrigiert**: gross (Monatszweig im Dienst mit Tests, Schreibstrecke-Recht klaeren, Feiertagslader exportieren, Dauergrenze auf einen Wert bringen)

- **pfad**: /portal/[mandant]/security | **behauptet**: "Kachel/Liste 3: Wachbucheintraege der Art vorfall aus den letzten sieben Berliner Tagen" — und als Blocker, dass es fuer "offene Vorfaelle" keinen Zustand gibt. | **wirklich**: Die Art heisst nicht 'vorfall'. Der Aufzaehlungstyp wachbuch_art hat genau fuenf Werte: rundgang | vorkommnis | uebergabe | schluessel | alarm. 'vorfall' existiert nirgends; als Filterwert gaebe es 22P02 (invalid input value for enum wachbuch_art), und BuchFilter.art ist auf WachbuchArt typisiert, faellt also schon im typecheck. Auch die Tabelle heisst wachbuch_eintrag, nicht wachbuch. Der Befund dahinter ist richtig — es gibt keine Spalte offen/geschlossen, Korrektur laeuft ueber ersetzt_durch_id — und das Zeitfenster ist bereits gebaut (BuchFilter.von/bis, Berliner Kalendertage). Zu korrigieren ist nur das Vokabular: die Kachel zaehlt Eintraege der Art 'vorkommnis'. | **beleg**: psql: select typname, string_agg(enumlabel,'|') from pg_type join pg_enum … where typname='wachbuch_art' → rundgang|vorkommnis|uebergabe|schluessel|alarm; /home/user/cse-platform/src/server/services/security/wachbuch.ts:24-36 (WACHBUCH_ARTEN, ART_TEXT: vorkommnis → 'Vorkommnis'), :474-481 (BuchFilter mit art/von/bis); psql: information_schema.tables → wachbuch_eintrag (kein wachbuch) | **aufwand_korrigiert**: mittel (unveraendert; nur der Enumwert und der Tabellenname sind zu berichtigen, bevor jemand die Abfrage schreibt)

- **pfad**: /portal/[mandant]/security/veranstaltungen/[id] | **behauptet**: Recht security.lesen; "Darunter der Besetzungsstand: die erzeugte Eventschicht mit den eingeteilten Menschen, jeweils mit §34a-Nachweislage, und ein Verweis auf .../besetzung — der Verweis nur, wenn dienstplan.schreiben gehalten wird, sonst steht der Stand als Text." | **wirklich**: Der Stand steht auch nicht als Text: einsatz und einsatz_zuordnung verlangen in der RLS dienstplan.lesen, die Nachweislage je Person verlangt personal.nachweis_lesen. Mit security.lesen allein ist die Veranstaltung sichtbar (veranstaltung → security.lesen), der Besetzungsstand aber leer — und leer ist hier gefaehrlich, weil eine unbesetzte Veranstaltung und eine nicht lesbare Veranstaltung gleich aussehen. Die Planung kapselt nur den Verweis (dienstplan.schreiben), nicht die Daten. Die uebrigen Befunde stimmen: veranstaltung hat 0 Zeilen, im ganzen src-Baum gibt es kein insert into veranstaltung (nur tests/isolation/security-posten.test.ts:155), die Seitenkarte §5.8 fuehrt kein /veranstaltungen/neu, O-210 ist offen, und findeVeranstaltung fehlt — ladeVeranstaltung ist privat und liefert nur id, objekt_id, kunde_id, auftrag_leistung_id, soll_besetzung, archiviert, also keines der Kopffelder. | **beleg**: psql: pg_policies t_mandant auf einsatz und einsatz_zuordnung → app.hat_recht('dienstplan.lesen', …), auf veranstaltung → ('security.lesen'), auf nachweis (n_lesen) → ('personal.nachweis_lesen'); /home/user/cse-platform/src/server/services/security/eventbesetzung.ts:62-82 (VeranstaltungZeile/ladeVeranstaltung, privat); psql select count(*) from veranstaltung → 0; docs/architecture/04-SEITENKARTE.md:1137-1138 | **aufwand_korrigiert**: mittel–gross (Besetzungsblock braucht eigene Rechtekapselung mit drei unterscheidbaren Zustaenden, dazu mindestens eine Seedzeile und der offene Entscheid, woher ein Eventauftrag kommt)

- **pfad**: /portal/[mandant]/security/bewacherregister | **behauptet**: Drei Blocker (O-40, keine Registerschnittstelle, kein mandant_id auf bewacher_eintrag), Nebenbefund: der Lesepfad haengt an einem anderen Recht als die Route. | **wirklich**: Die drei Punkte stimmen (bewacher_eintrag hat kein mandant_id; be_lesen verlangt personal.nachweis_lesen, be_aendern personal.bewacher_verwalten; 0 Zeilen; O-40 offen — und anders als die Planung vermuten laesst, gibt es sehr wohl eine INSERT-Policy be_schreiben auf personal.bewacher_verwalten, erfasseEintrag braucht also KEINE Migration). Es fehlt aber der vierte, und er ist der Grund, aus dem modul.ts ueberhaupt existiert: die Modulsperre bindet diese Route nicht an das Security-Modul. Die Route traegt als einziges Recht personal.bewacher_verwalten, und 'personal' steht in QUERSCHNITT — die Sperre prueft ausschliesslich den Modulteil der Routenrechte. /portal/bau/security/bewacherregister und /portal/operations/security/bewacherregister sind damit fuer jede admin/leitung erreichbar, obwohl REALTIME Service und CSE Operations kein Security gebucht haben; genau der Fall, den der Kopfkommentar von modul.ts als Befund des Mandanten beschreibt ("Sidebar-Punkte Reinigung und Security" im Hochbau). Die Liste ist dort zwar leer, weil sie ueber anstellung eingegrenzt wird — aber ein leeres Bewacherregister in einer Baugesellschaft ist eine Aussage, die niemand treffen wollte. | **beleg**: /home/user/cse-platform/src/server/registry/modul.ts:47 ('personal' in QUERSCHNITT); /home/user/cse-platform/src/app/portal/zugang.ts:356-357 (modulGesperrt aus [...bewachung.lesen, ...bewachung.schreiben].some(r => !modulAktiv(buchung, r))); src/server/registry/routen.generiert.ts:149 (lesen: ["personal.bewacher_verwalten"], keine security-Komponente); psql: select name, module from mandant → REALTIME Service {bau}, CSE Operations {}; psql: pg_policies bewacher_eintrag → be_schreiben INSERT with check app.hat_recht('personal.bewacher_verwalten', …) | **aufwand_korrigiert**: mittel–gross (zusaetzlich security.lesen ins Routenmanifest oder eine ausdrueckliche Modulpruefung in der Seite, sonst wandert das Register in Gesellschaften, die keine Bewacher haben)

- **pfad**: /portal/[mandant]/qualitaet/pruefungen/neu | **behauptet**: "dienst_vorhanden: Ja fuer das Schreiben" (nur HTTP-Weg und Auswahllisten fehlen); "Solange das Verfahren keine max_punkte traegt, weist die Datenbank eine Kopfpunktzahl ohnehin zurueck (Pruefregel qp_punkte_brauchen_skala)"; Befunde tragen "optionaler Raum aus dem Revier". | **wirklich**: Zwei Dinge. (1) Die Zusage steht nicht in der Datenbank. qp_punkte_brauchen_skala lautet CHECK (punkte IS NULL OR max_punkte IS NOT NULL) und prueft die EIGENE Spalte qualitaetspruefung.max_punkte — pruefverfahren.max_punkte kommt darin nicht vor. Wer max_punkte mitschickt, darf jede Punktzahl schreiben; die Sperre ist allein erfassePruefung, das verfahren.max_punkte kopiert und die Summe auf null setzt, wenn dort nichts steht. Die Regel als "Datenbank weist es zurueck" zu beschreiben, laedt dazu ein, den Dienst zu umgehen — genau das, was Invariante 6 verhindern soll. (2) Der Dienst ist fuer das geplante Formular nicht fertig: erfassePruefung setzt beim Befund nur raum_id und revier_raum_id, NIE revier_id. Der Fremdschluessel auf den Revierraum ist zusammengesetzt (mandant_id, revier_id, revier_raum_id) und laeuft als MATCH SIMPLE — mit revier_id = null wird er gar nicht geprueft. Eine revier_raum_id aus einem fremden Revier wird also stillschweigend angenommen und steht dann auf dem Pruefprotokoll. Das ist eine Dienstaenderung mit Test, nicht ein Handler. | **beleg**: psql \d qualitaetspruefung → "qp_punkte_brauchen_skala" CHECK (punkte IS NULL OR max_punkte IS NOT NULL); /home/user/cse-platform/src/server/services/reinigung/qualitaet.ts:183-189 (Kommentar "qp_punkte_brauchen_skala weist … zurueck") und :205-206 (summe, verfahren.max_punkte), :227-236 (Positions-INSERT ohne revier_id); psql \d qualitaetspruefung_position → "qpp_revier_raum_fk" FOREIGN KEY (mandant_id, revier_id, revier_raum_id) REFERENCES revier_raum(mandant_id, revier_id, id); Routenmanifest src/server/registry/routen.generiert.ts:114 (lesen: qualitaet.lesen, schreiben: qualitaet.schreiben — nicht nur qualitaet.schreiben) | **aufwand_korrigiert**: gross (erfassePruefung um revier_id/Anker erweitern und testen, Handler und Auswahllisten bauen, Seed anlegen — der Dienst ist entgegen der Annahme mitbetroffen)
