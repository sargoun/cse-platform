# PLAN agenten-freigaben-radar
## Gemeinsames
Alle sechs Routen stehen bereits im Manifest `src/server/registry/routen.generiert.ts` (Zeilen 282, 286, 293, 295, 303, 304) und werden heute vom gemeinsamen Auffangpfad `src/app/portal/[mandant]/[...rest]/page.tsx` bedient: `MandantUnterseite` -> `NochNichtGebaut`, mit laufendem Tor (Sitzung, Recht, Slug). Keine der sechs hat eine eigene `page.tsx` — es fehlt die Seite, nicht die Bewachung, und niemand bekommt heute ein 404. Die gemeinsame Huelle fuer alle sechs ist dieselbe: `mandantTor()` / `MandantAntwort` aus `/home/user/cse-platform/src/app/portal/unterseite.tsx`, `PortalRahmen`, `haeltRechte()` aus `/home/user/cse-platform/src/app/portal/rechte.ts`, `db().begin(SCHNAPPSCHUSS, ...)` + `withTenant`, und als Vorbild fuer eine Formular-Unterseite unter einem `[id]` genau `/home/user/cse-platform/src/app/portal/[mandant]/radar/[id]/mappe/einreichung/page.tsx` (Tor, `kennungOder404`, UUID-Pruefung, `FEHLER_TEXT`-Tabelle fuer die `?fehler=`-Rueckkehr, POST an eine bestehende API-Route, Zeit aus dem Server). Drei der sechs (radar/status, freigaben/einspruch, freigaben/rueckgaengig) sind reine Huellenarbeit: Dienst UND API-Route existieren vollstaendig (`/api/radar/vorgang`, `/api/freigaben/fenster`), und dieselben Formulare stehen heute schon eingebettet auf der jeweiligen Detailseite — die Unterseite ist die fokussierte Fassung davon, kein neuer Weg. Zwei brauchen einen neuen Schreibweg (Radar-Profileditor, Agenten-Richtlinieneditor), und GENAU EINE braucht eine Migration: `agent_richtlinie` traegt die RLS-Policies `t_richtlinie_lesen` = `versand.lesen` und `t_richtlinie_schreiben` = `versand.freigeben`, waehrend Seitenkarte und Manifest die Route auf `agent.richtlinie_verwalten` stellen — zwei verschiedene Schluessel fuer dieselbe Tabelle, und das Ergebnis ist eine leere Seite ohne Erklaerung oder ein stilles UPDATE mit null Zeilen. Alle sechs Rechte stehen im Katalog (`drizzle/0008_berechtigung_matrix.sql`, in der lebenden Datenbank geprueft); alle Tabellen existieren und sind geseedet (radar_profil 3, agent_richtlinie 12, ausschreibung 7, ausschreibung_vorgang 3, freigabe 33, agent_aufgabe 5).
## Routen

### /portal/[mandant]/agenten/[agent]/start
- zustand: platzhalter
- dienst_vorhanden: Vollstaendig. /home/user/cse-platform/src/server/agent/orchestrator.ts -> fuehreLaufAus(), AgentKennung, LaufAuftrag, LaufErgebnis; /home/user/cse-platform/src/server/agent/auftraege.ts -> ENTWURF_AUFTRAEGE (vier Eintraege: ceo_assistent, akquise, backoffice, finanzen) und fuelleTatsachen(); /home/user/cse-platform/src/app/portal/[mandant]/agenten/kennung.ts -> kennungFuer()/slugFuer() (Slug 'ceo-assistent' <-> Enum 'ceo_assistent'). Die API-Route /home/user/cse-platform/src/app/api/agenten/lauf/route.ts ist fertig: Ursprungspruefung, authorize('agent.aufgabe_starten'), Mandant aus der Sitzung statt aus dem Rumpf, Idempotenzschluessel aus dem Formular, drei Ausgaenge (vorgelegt / bestand / gestoert).
- tabellen_vorhanden: True
- recht: agent.aufgabe_starten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/agenten/[agent]/page.tsx
- aufwand: klein
- blocker: keiner

**Plan**

Die Seite zeigt vor dem Knopf, WAS dieser Lauf tun wird: den Titel und die Vorgangsart aus ENTWURF_AUFTRAEGE fuer genau diesen Agenten (also z. B. 'Antwortentwurf auf die juengste Anfrage', Aktion anfrage_antwort_entwurf), die Tatsachen-Schluessel, die fuelleTatsachen() aus dieser Gesellschaft fuellt, und welches Modell formuliert (app.modell_fuer('entwurf_text') samt Anbieter aus modell_register — oder 'hausinterner Demobetrieb'). Darunter steht in einem Satz, dass ein Lauf nichts versendet, sondern eine offene Freigabe im Posteingang anlegt (Invariante 7), und daneben der Budgetstand, damit niemand gegen den harten Stopp aus AGT-05 laeuft. Das Formular ist dasselbe wie heute auf der Agentenseite (Zeilen 331-353): verstecktes agent-Feld, ein einmal beim Zeichnen erzeugter Idempotenzschluessel, POST auf /api/agenten/lauf. Ohne agent.aufgabe_starten wird der Knopf nicht gezeichnet, sondern durch den Satz ersetzt, dass Starten eine eigene Befugnis ist; ohne freigegebenes Modell steht dort, dass die Arbeit von Hand laeuft — kein Knopf, der verspricht, was nicht geht. Zu klaeren beim Bauen: die API-Route leitet heute fest auf /portal/{slug}/agenten/{agent}?lauf=... zurueck, also auf die Detailseite; entweder bleibt das so (dann ist /start ein reines Vorschaltblatt) oder die Route bekommt ein Ziel-Feld — die erste Variante ist die ehrlichere, weil das Ergebnis eines Laufs zur Aktivitaetsliste des Agenten gehoert.

### /portal/[mandant]/agenten/richtlinien/[id]
- zustand: platzhalter
- dienst_vorhanden: Kein Dienst. /home/user/cse-platform/src/server/agent/policy.ts liefert nur den Lesekopf und das Tor: das Interface Richtlinie (mandantId, aktion, autoErlaubt, maxBetragCent, ist_aktiv), die Liste AKTIONEN, gate() und nutzlastHash() — alles rein, nichts, was agent_richtlinie liest oder schreibt. Der einzige Lesezugriff im Haus ist eine eingebettete Abfrage in /home/user/cse-platform/src/app/portal/[mandant]/agenten/[agent]/page.tsx Zeilen 162-165 (select aktion, auto_erlaubt, begruendung from agent_richtlinie), dazu eine weitere in /home/user/cse-platform/src/server/services/bau/nachtrag.ts:422. Es gibt weder eine Datei unter src/server/services/ noch eine API-Route unter src/app/api/agenten/ fuer Richtlinien, und auch die Elternseite /portal/[mandant]/agenten/richtlinien (Manifest Zeile 294) fehlt.
- tabellen_vorhanden: True
- recht: agent.richtlinie_verwalten
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/agenten/[agent]/page.tsx
- aufwand: gross
- blocker: Zwei Blocker. ERSTENS eine Rechte-Diskrepanz, die eine Migration braucht: agent_richtlinie traegt in der lebenden Datenbank die Policies t_richtlinie_lesen (USING app.hat_recht('versand.lesen', mandant_id)) und t_richtlinie_schreiben (USING/WITH CHECK app.hat_recht('versand.freigeben', ...)), waehrend Seitenkarte §5.19 und routen.generiert.ts Zeile 295 die Route auf agent.richtlinie_verwalten stellen. Beide Schluessel stehen im Katalog, aber sie sind nicht dieselben und im Rollen-Seed nicht deckungsgleich (agent.richtlinie_verwalten ist in drizzle/0008 nur fuer super_admin geseedet). Wer agent.richtlinie_verwalten haelt und versand.lesen nicht, kommt durch das Tor und sieht eine leere Seite; wer versand.freigeben nicht haelt, schickt ein UPDATE ab, das null Zeilen trifft und wie ein Erfolg aussieht — genau die stille, spaet entdeckte Sorte Fehler. Der Code weiss das bereits (Kommentar in agenten/[agent]/page.tsx Zeile 137), aber niemand hat es aufgeloest. Entschieden werden muss: entweder die Policy auf agent.richtlinie_verwalten umstellen (Migration) oder die Seitenkarte auf versand.* korrigieren — nicht die Seite beide Rechte pruefen lassen und den Widerspruch verstecken. ZWEITENS: es gibt keinen Schreibweg (kein Dienst, keine API-Route), und agent_richtlinie hat keine geloescht_am-Spalte — Loeschen ist also ohnehin nicht vorgesehen, deaktiviert wird ueber ist_aktiv (Invariante 8).

**Plan**

Die Seite bearbeitet EINE Zeile aus agent_richtlinie: die Aktion (unveraenderlich, sie ist Teil des eindeutigen Schluessels mandant_id+aktion), auto_erlaubt als Schalter, max_betrag_cent als bigint-Cent-Feld mit Euro-Eingabe und begruendung als Pflichttext — wer Automatik einschaltet, schuldet den anderen eine Erklaerung, und in einem halben Jahr ist 'warum darf das von allein hinaus' eine echte Frage. Darueber steht in einem Satz, was diese Zeile bewirkt, gelesen aus gate(): eine Richtlinie entscheidet nur dort, wo keine menschliche Freigabe noetig ist, und sie hebt LEG-08 (fehlende Rechtsgrundlage) unter keinen Umstaenden auf. Die Aktionsauswahl kommt aus der Konstante AKTIONEN in policy.ts und nicht aus freiem Text — die Spalte ist blankes text ohne Enum, ein Tippfehler legte sonst eine Richtlinie an, die nie greift und nie auffaellt. Die Check-Bedingung agent_richtlinie_kein_auto_angebot (aktion <> 'angebot_senden' oder nicht auto_erlaubt) wird als gesperrtes Feld mit Begruendung gezeigt, nicht als Datenbankfehler nach dem Absenden: 'ein Angebot sendet sich unter keiner Einstellung selbst' ist eine Aussage der Seitenkarte, kein Betriebsunfall. Zu bauen sind dafuer: src/server/services/agent/richtlinie.ts (leseRichtlinie, schreibeRichtlinie), eine API-Route src/app/api/agenten/richtlinie/route.ts nach dem Muster von /api/radar/vorgang, die Elternliste /portal/[mandant]/agenten/richtlinien — sonst hat die Detailseite keinen Eingang — und die Migration, die die Rechte-Diskrepanz aufloest.

### /portal/[mandant]/freigaben/[id]/einspruch
- zustand: platzhalter
- dienst_vorhanden: Vollstaendig. /home/user/cse-platform/src/server/services/freigabe/stapel.ts -> erhebeEinspruch(kontext, freigabeId, grund) (ruft app.freigabe_einspruch, drizzle/0152_freigabe_stapel_einspruch_undo.sql:164) und FensterFehler; /home/user/cse-platform/src/server/services/freigabe/laden.ts -> ladeFreigabe(), vermerkeAnsicht(), oeffneFreigabe(), FreigabeAnsicht, FreigabeKopf; /home/user/cse-platform/src/server/services/freigabe/fenster.platzhalter.ts -> EINSPRUCH_MINUTEN = 30 und FENSTER_OFFENE_FRAGE = 'O-108'. Die API-Route /home/user/cse-platform/src/app/api/freigaben/fenster/route.ts ist fertig (POST, was=einspruch, Grund mindestens 5 Zeichen, eigene authorize-Pruefung auf freigabe.einspruch_erheben, Rueckkehr mit ?vermerkt= oder ?fehler=grund|fenster).
- tabellen_vorhanden: True
- recht: freigabe.einspruch_erheben
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/freigaben/[id]/page.tsx
- aufwand: klein
- blocker: keiner

**Plan**

Die Seite zeigt den Kopf der genehmigten, aber noch nicht ausgefuehrten Freigabe — Titel, Vorgangsart, wer wann genehmigt hat — und darunter den laufenden Countdown bis verzoegert_bis in Europe/Berlin, mit dem ausgeschriebenen Satz, dass die Fensterlaenge von dreissig Minuten ein Platzhalter ist (O-108, fenster.platzhalter.ts). Das Formular ist eins zu eins der Block aus Zeilen 514-539 der Detailseite: verstecktes mandant, freigabe, was=einspruch, ein Pflichtgrund (5 bis 500 Zeichen), POST auf /api/freigaben/fenster. Ist das Fenster abgelaufen oder die Freigabe schon ausgefuehrt, steht statt des Knopfes der Grund dafuer — ein Knopf, den die Datenbank abweisen wuerde, haette gar nicht erst dastehen duerfen. Eine Falle, die beim Bauen leicht zuschnappt: diese Seite darf NICHT oeffneFreigabe() aufrufen. Das schreibt nach K-13 eine freigabe_ansicht-Zeile mit geoeffnet_am_server, und die ist die Messgrundlage der Pruefdauer-Verteilung aus APR-08 — ein Einspruchsblatt, das als Pruefvorgang zaehlt, verfaelscht genau den Bericht, der Durchwinken aufdecken soll. Gelesen wird ueber ladeFreigabe() beziehungsweise eine schlanke Kopfabfrage, nicht ueber den Oeffner.

### /portal/[mandant]/freigaben/[id]/rueckgaengig
- zustand: platzhalter
- dienst_vorhanden: Vollstaendig. /home/user/cse-platform/src/server/services/freigabe/stapel.ts -> nimmZurueck(kontext, freigabeId, grund) (ruft app.freigabe_ruecknahme, drizzle/0152:281), armiereRuecknahme() und FensterFehler; /home/user/cse-platform/src/server/services/freigabe/fenster.platzhalter.ts -> RUECKNAHME_MINUTEN = 60 und RUECKNAHME_OFFENE_FRAGE = 'O-368'; /home/user/cse-platform/src/server/services/freigabe/laden.ts -> ladeFreigabe() mit undoBis und ausfuehrungStatus. Dieselbe API-Route /home/user/cse-platform/src/app/api/freigaben/fenster/route.ts mit was=ruecknahme.
- tabellen_vorhanden: True
- recht: freigabe.rueckgaengig
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/freigaben/[id]/page.tsx
- aufwand: klein
- blocker: O-368: app.freigabe_umkehrbar gibt in der lebenden Datenbank fuer JEDE Vorgangsart false zurueck — der Funktionsrumpf ist woertlich 'select false and p_typ is not null'. Damit armiert armiereRuecknahme() nie ein Fenster, undo_bis bleibt immer null, und die Seite kann heute unter keiner Bedingung einen funktionierenden Knopf zeigen. Der Grund steht in DECISIONS.md Zeile 5168: die einzige Handlung, die ueberhaupt ausgefuehrt wird (eingangsrechnung_uebernehmen) legt eine Eingangsrechnung an, und dafuer gibt es keinen gebauten Rueckweg — in Finanzen wird nicht hart geloescht (Invariante 8), korrigiert wird per Storno, und eine Stornofunktion fuer Eingangsrechnungen existiert nicht. Die Route ist also zu bauen und geht dauerhaft in ihrem leeren Zustand in Betrieb, bis jemand eine Rueckholung baut und ihre Vorgangsart in app.freigabe_umkehrbar eintraegt. Das ist kein Grund, sie wegzulassen — aber es ist der Satz, der auf der Seite stehen muss, und niemand darf sie als 'gebaut und benutzbar' abhaken.

**Plan**

Die Seite hat zwei Zustaende, und der haeufigere ist heute der leere. Laeuft ein Fenster (status genehmigt, ausfuehrung_status ausgefuehrt, undo_bis in der Zukunft), zeigt sie Kopf, Countdown bis undo_bis in Europe/Berlin und das Formular aus Zeilen 566-591 der Detailseite: mandant, freigabe, was=ruecknahme, Pflichtgrund, POST auf /api/freigaben/fenster — dazu den Satz, dass die Ausfuehrung zurueckgenommen wird und nicht die Entscheidung, der Schnappschuss bleibt nach APR-07 unveraendert. Laeuft keines, steht an der Stelle, an der der Knopf stuende, der Grund: der Weg ist vollstaendig gebaut — Fenster, Frist, eigenes Recht, Protokoll, Abweisung nach Ablauf —, aber fuer diese Handlung ist er nicht armiert, eine Korrektur ist eine NEUE Freigabe, offene Frage O-368. Genau diese Unterscheidung ist der Zweck der Seite: eine ausgefuehrte Handlung ohne Fenster sah vorher aus wie eine, bei der man das Fenster verpasst hat, und sie ist etwas anderes. Auch hier gilt: kein oeffneFreigabe(), damit die Pruefdauer-Messung aus APR-08 sauber bleibt.

### /portal/[mandant]/radar/[id]/status
- zustand: platzhalter
- dienst_vorhanden: Vollstaendig. /home/user/cse-platform/src/server/services/radar/vorgang.ts -> setzeVorgangsstand(kontext, eingabe), SETZBAR = ['geprueft','in_bearbeitung','verworfen'], SetzbarerStatus, VorgangEingabe, VorgangErgebnis, VorgangFehler (Codes 'grund' und 'mappe_recht'); /home/user/cse-platform/src/app/portal/[mandant]/radar/daten.ts -> leseRadarZeile(), leseRadarListe(), RadarZeile. Die API-Route /home/user/cse-platform/src/app/api/radar/vorgang/route.ts ist fertig: authorize('radar.status_setzen'), UUID-Pruefung, Statuspruefung gegen SETZBAR, fehlender Verwerfungsgrund als Auskunft (?fehler=grund) statt als Serverfehler.
- tabellen_vorhanden: True
- recht: radar.status_setzen
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/radar/[id]/page.tsx
- aufwand: klein
- blocker: keiner

**Plan**

Die Seite ist die ausgeschriebene Fassung der Uebergangssteuerung, die heute als Formularblock in Zeilen 344-390 der Detailseite steckt: sie zeigt den aktuellen Stand der Bekanntmachung, die Abgabefrist mit dem danger-Pill unter fuenf Tagen (RAD-06), und je setzbarem Ziel eine Zeile mit der Bedingung davor. geprueft geht immer; verworfen verlangt einen Grund, und zwar als Pflichtfeld im Formular, weil der Dienst sonst abweist (RAD-07); in_bearbeitung verlangt eine bestehende Vergabemappe, und fehlt sie, steht dort der Verweis auf /radar/[id]/mappe statt eines Knopfes, der in einen Fehler laeuft. Was NICHT auf dieser Seite steht, gehoert ausdruecklich dazu: eingereicht ist hier nicht setzbar — das erfasst /radar/[id]/mappe/einreichung unter dem eigenen Recht vergabe.einreichung_erfassen, und einen Knopf 'jetzt einreichen' gibt es nach D-07 nirgends, weil keine Vergabeplattform dafuer eine Schnittstelle anbietet. Die Statushistorie aus ausschreibung_vorgang wird mit Zeitpunkt, Person und Grund darunter gelistet, damit die Zahlen des Pipeline-Berichts (REP-06: gefunden, geprueft, geboten, gewonnen) an ihrer Quelle nachlesbar sind. Beim Bauen zu entscheiden: die API-Route leitet heute auf /portal/{mandant}/radar/{id} zurueck, nicht auf /status — das bleibt sinnvoll so, weil nach dem Setzen die Detailseite die richtige Antwort ist.

### /portal/[mandant]/radar/profile/[id]
- zustand: platzhalter
- dienst_vorhanden: Nur lesend. /home/user/cse-platform/src/app/portal/[mandant]/radar/daten.ts -> leseProfile(kontext) liefert ALLE Profile eines Mandanten samt CPV, NUTS, Stichwoertern, Wertgrenzen, Empfaengern und Bewertungszahl (ProfilZeile) — einen Lader fuer ein EINZELNES Profil gibt es nicht. Ergaenzend /home/user/cse-platform/src/server/services/radar/gewichte.platzhalter.ts (GEWICHTE_PLATZHALTER, ABZUG_PLATZHALTER, SKALA_MAX_PLATZHALTER) und REGEL_VERSION aus /home/user/cse-platform/src/server/services/radar/bewertung.ts. Es gibt KEINEN Schreibdienst: keine Datei src/server/services/radar/profil.ts, keine API-Route unter src/app/api/radar/ ausser vorgang. Die Listenseite sagt das selbst: 'Bearbeiten kommt mit dem naechsten Schritt; bis dahin ist diese Seite ehrlich lesend statt ein Formular, das nichts speichert.'
- tabellen_vorhanden: True
- recht: radar.profil_schreiben
- recht_im_katalog: True
- vorbildseite: /home/user/cse-platform/src/app/portal/[mandant]/radar/profile/page.tsx
- aufwand: mittel
- blocker: Keine fehlende Tabelle und keine Migration noetig — radar_profil, radar_profil_cpv und radar_profil_empfaenger existieren, und ihre Policy t_profil_schreiben nennt exakt radar.profil_schreiben, also kein Rechte-Bruch wie bei agent_richtlinie. Was fehlt, ist der Schreibweg (Dienst + API-Route). Die Blocker sind offene Geschaeftsregeln, und sie entscheiden, welche Felder der Editor ueberhaupt anbieten darf: O-15 (Gewichtung der Kriterien und die Benachrichtigungsschwelle aus RAD-08 hat niemand entschieden) — das jsonb-Feld gewichtung und benachrichtigung_ab_punkte duerfen also nicht als freie Zahlenfelder erscheinen, die so tun, als waere die Gewichtung bestaetigt; O-98 (die CPV-Listen fuer Reinigung, Sicherheit und Bau sind gegen die amtliche Liste unbestaetigt) — jede von Hand eingetragene CPV-Zeile bleibt ist_platzhalter = true und wird als solche gezeigt; O-191 (schliesst ein Negativ-Stichwort aus oder zieht es nur ab) — negativ_wirkung steht bis zur Antwort fest auf 'abzug', der sicheren Richtung, weil ein Ausschluss still verwirft, was nie ein Mensch gesehen hat; O-47 (keine Umrechnung fremder Waehrungen) — die Wertgrenzen tragen ihre Waehrung mit und werden nie konvertiert.

**Plan**

Die Seite bearbeitet ein einzelnes Suchprofil: Name, NUTS-Praefixe, positive und negative Stichwoerter, Wertgrenzen als bigint-Cent mit Waehrung, frist_min_tage, ist_aktiv — und darunter die CPV-Zeilen aus radar_profil_cpv (Code, Praefixlaenge 2 bis 8, Wirkung positiv/abzug/ausschluss) sowie die Empfaenger aus radar_profil_empfaenger mit ihrer Schwelle. Die gesperrten Felder stehen sichtbar als gesperrt da, mit der offenen Frage daneben statt weggelassen: gewichtung und benachrichtigung_ab_punkte tragen O-15, jede neue CPV-Zeile wird als Platzhalter markiert bis O-98, negativ_wirkung ist bis O-191 nicht umstellbar. Ueber dem Formular steht, was ein Speichern auslost, denn die Datenbank tut das von selbst: der Trigger trg_radar_profil_version und die drei CPV-Trigger zaehlen version hoch, und der naechste Nachtlauf schreibt eine NEUE bewertung-Zeile, waehrend die alte stehen bleibt — so bleibt nachlesbar, mit welcher Suche eine Bekanntmachung damals bewertet wurde. Geloescht wird nicht hart, sondern ueber geloescht_am/geloescht_von beziehungsweise ist_aktiv. Zu bauen sind src/server/services/radar/profil.ts (leseProfil fuer eine ID, schreibeProfil, setzeCpv, setzeEmpfaenger) und eine API-Route src/app/api/radar/profil/route.ts nach dem Muster von /api/radar/vorgang; die Listenseite bekommt von jedem Profil aus einen Verweis hierher.

## KRITIK (6 geprueft, 10 Korrekturen)

- **pfad**: /portal/[mandant]/agenten/[agent]/start | **behauptet**: "Die API-Route /api/agenten/lauf ist fertig" / "blocker: keiner"; der Plan will die heutige Umleitung auf /portal/{slug}/agenten/{agent} ausdruecklich SO lassen ("die erste Variante ist die ehrlichere"). | **wirklich**: Die Umleitung ist kaputt und der Plan schreibt den Fehler fest. Das Formular sendet agent=kopf.kennung, also den ENUM-Wert; die Route baut daraus direkt den Pfad, ohne slugFuer(). Fuer ceo_assistent entsteht /portal/<slug>/agenten/ceo_assistent, die Detailseite loest aber nur Slugs auf (kennungFuer('ceo-assistent')) und ruft bei allem anderen notFound(). Jeder Lauf des CEO-Assistenten endet heute nach dem 303 auf einem 404 — und /start wuerde als 'reines Vorschaltblatt' genau dort hineinlaufen. Die Listenseite macht es richtig (slugFuer(a.kennung)), die API-Route nicht. Ein Blocker von einer Zeile, aber er existiert. | **beleg**: src/app/api/agenten/lauf/route.ts:120 `const seite = `/portal/${ergebnis.slug}/agenten/${agent}`;` (agent = String(daten.get('agent'))); src/app/portal/[mandant]/agenten/[agent]/page.tsx:340 `<input type="hidden" name="agent" value={kopf.kennung} />` und :109 `kennung::text as kennung`; src/app/portal/[mandant]/agenten/kennung.ts:19-31 (SLUG.ceo_assistent = 'ceo-assistent', kennungFuer matcht nur Slugs); page.tsx:85 `if (kennung === undefined) notFound()`; Gegenbeispiel src/app/portal/[mandant]/agenten/page.tsx:230 nutzt slugFuer(). | **aufwand_korrigiert**: klein — aber nicht "blocker: keiner": zuerst slugFuer() in route.ts:120, sonst baut man ein Vorschaltblatt vor einen 404.

- **pfad**: /portal/[mandant]/agenten/[agent]/start | **behauptet**: "Ohne agent.aufgabe_starten wird der Knopf nicht gezeichnet, sondern durch den Satz ersetzt, dass Starten eine eigene Befugnis ist" — und der Budgetstand steht neben dem Knopf. | **wirklich**: Beides geht so nicht. (1) Die Route ist im Manifest selbst auf agent.aufgabe_starten bewacht; pruefeZugang antwortet bei fehlendem Recht mit notFound(). Wer das Recht nicht haelt, sieht die Seite nie — der geplante Ersatzsatz ist toter Code (auf der Detailseite ist er richtig, weil die nur agent.lesen verlangt). (2) agent_budget ist per RLS an agent.lesen gebunden, nicht an agent.aufgabe_starten; die Seite muss agent.lesen mitpruefen, sonst zeigt sie bei fehlendem Recht still "kein Budget" statt "darf ich nicht sehen" — genau die Falschaussage, die die Detailseite bei den Richtlinien ausdruecklich vermeidet. | **beleg**: src/server/registry/routen.generiert.ts:293 `bewachung: {"lesen":["agent.aufgabe_starten"]}`; src/app/portal/zugang.ts:206 und :397-404 (`if (entscheidung.art !== 'erlaubt') … notFound()`); psql: `select policyname, qual from pg_policies where tablename='agent_budget'` → t_mandant USING app.hat_recht('agent.lesen', app.aktiver_mandant()); Vorbild fuer die Unterscheidung: agenten/[agent]/page.tsx:137-146. | **aufwand_korrigiert**: klein (unveraendert), aber der geplante Rechte-Zweig entfaellt und agent.lesen muss fuer den Budgetstand dazu

- **pfad**: /portal/[mandant]/agenten/richtlinien/[id] | **behauptet**: Blocker 1: "Wer agent.richtlinie_verwalten haelt und versand.lesen nicht, kommt durch das Tor und sieht eine leere Seite; wer versand.freigeben nicht haelt, schickt ein UPDATE ab, das null Zeilen trifft" — eine Migration sei noetig. | **wirklich**: Die Diskrepanz der Schluessel stimmt, die beiden Schadensfaelle sind heute aber unerreichbar — der Blocker ist ueberzeichnet. In der lebenden Rechtematrix haelt agent.richtlinie_verwalten AUSSCHLIESSLICH super_admin, und super_admin haelt versand.lesen UND versand.freigeben. Es gibt keine Rolle mit richtlinie_verwalten ohne versand.lesen, also keine leere Seite, und keine ohne versand.freigeben, also kein stilles Null-Zeilen-UPDATE. Die Asymmetrie laeuft in die ANDERE Richtung: admin und leitung halten versand.* und kommen wegen des Manifests gar nicht auf die Seite. Die Migration ist eine Konsistenzkorrektur, keine Vorbedingung — die Seite ist ohne sie baubar und funktioniert fuer ihren einzigen heutigen Halter. | **beleg**: psql: `select b.schluessel, string_agg(r.schluessel,',') from rolle_berechtigung rb join berechtigung b on b.id=rb.berechtigung_id join rolle r on r.id=rb.rolle_id where rb.gewaehrt and b.schluessel in ('agent.richtlinie_verwalten','versand.lesen','versand.freigeben') group by 1` → agent.richtlinie_verwalten|super_admin · versand.freigeben|admin,super_admin · versand.lesen|admin,leitung,super_admin; drizzle/0008_berechtigung_matrix.sql:347,793-797; Policies bestaetigt via `\d agent_richtlinie`. | **aufwand_korrigiert**: gross (unveraendert) — aber ein Blocker statt zwei: Dienst + API-Route + Elternliste fehlen wirklich, die Migration ist Aufraeumarbeit und blockiert nichts

- **pfad**: /portal/[mandant]/freigaben/[id]/einspruch | **behauptet**: "blocker: keiner" · "reine Huellenarbeit" · Kerninhalt ist "der laufende Countdown bis verzoegert_bis". | **wirklich**: Die Seite kann mit den vorhandenen Daten NIE einen Countdown zeigen — sie geht genauso leer in Betrieb wie /rueckgaengig, das der Plan deshalb als Blocker fuehrt. Keine einzige der 33 Freigaben hat verzoegerte_freigabe_bis gesetzt (0 von 33), und keine kann es bekommen: das Fenster armiert nur app.freigabe_verzoegern, das status='genehmigt' UND risiko='niedrig' UND stapel_faehig verlangt, dessen einziger Aufrufer der Stapelweg ist — und es gibt 0 offene Zeilen mit risiko='niedrig' and stapel_faehig. Damit ist auch die Definition of done aus CLAUDE.md ("seed data exercises it") nicht erfuellt. Die Spalte heisst ausserdem verzoegerte_freigabe_bis, nicht verzoegert_bis. | **beleg**: psql: `select status, risiko, stapel_faehig, count(*), count(verzoegerte_freigabe_bis) from freigabe group by 1,2,3` → offen|mittel|f|5|0 · offen|hoch|f|8|0 · genehmigt|niedrig|t|1|0 · … (mit_fenster durchgehend 0); `select count(*) from freigabe where status='offen' and risiko='niedrig' and stapel_faehig` → 0; drizzle/0152_freigabe_stapel_einspruch_undo.sql:70-83 (Rechte- und Risikobedingungen) und :102 (einziges UPDATE auf verzoegerte_freigabe_bis); einziger Aufrufer src/server/services/freigabe/stapel.ts:189. | **aufwand_korrigiert**: klein fuer die Huelle, aber "blocker: keiner" ist falsch: entweder eine Seed-Zeile mit laufendem Fenster (risiko niedrig + stapel_faehig + Stapelgenehmigung) oder die Seite wird ehrlich als dauerhaft leer geplant wie /rueckgaengig

- **pfad**: /portal/[mandant]/freigaben/[id]/einspruch | **behauptet**: Recht der Route ist freigabe.einspruch_erheben, "dieselbe API-Route ist fertig … eigene authorize-Pruefung auf freigabe.einspruch_erheben", blocker keiner. | **wirklich**: Dasselbe Rechte-Loch, das der Plan bei agent_richtlinie zum Blocker erklaert, steht hier unerwaehnt — und es ist hier SCHLIMMER, weil der Fehlertext luegt. Zum Lesen der Freigabe braucht die Sitzung freigabe.lesen (RLS t_mandant), die Route ist aber nur auf freigabe.einspruch_erheben bewacht: wer nur das Einspruchsrecht haelt, kommt durch das Tor und bekommt null Zeilen. Und app.freigabe_einspruch verlangt ZUSAETZLICH das erforderliches_recht der Zeile (Vorgabe freigabe.entscheiden) — erhebeEinspruch() verpackt aber JEDEN Fehler in FensterFehler, und die Route macht daraus ?fehler=fenster, also "das Fenster ist abgelaufen". Ein fehlendes Recht wird damit als abgelaufenes Fenster ausgegeben. Dazu ist O-367 offen und nennt genau dieses Recht. | **beleg**: psql: `select policyname, qual from pg_policies where tablename='freigabe'` → t_mandant (cse_app) USING app.hat_recht('freigabe.lesen', …); d_freigabe_lesen gilt nur fuer cse_definer; drizzle/0152:160 `if not app.hat_recht(coalesce(f.erforderliches_recht,'freigabe.entscheiden'), v_mandant)`; psql `select coalesce(erforderliches_recht,'(null)'), count(*) from freigabe group by 1` → recruiting.stelle_veroeffentlichen|5, eingang.freigeben|3, social.freigeben|1; src/server/services/freigabe/stapel.ts:234-242 (catch → FensterFehler); src/app/api/freigaben/fenster/route.ts:74-77 (FensterFehler → ?fehler=fenster); docs/DECISIONS.md:5169 (O-367). | **aufwand_korrigiert**: klein bis mittel — die Seite muss freigabe.lesen und das erforderliches_recht der Zeile selbst pruefen und den Knopf danach zeichnen, sonst ist der Fehlerpfad eine Falschaussage

- **pfad**: /portal/[mandant]/freigaben/[id]/rueckgaengig | **behauptet**: Gemeinsames: Vorbild ist die Einreichungs-Unterseite "mit FEHLER_TEXT-Tabelle fuer die ?fehler=-Rueckkehr", die Unterseiten seien "die fokussierte Fassung" der eingebetteten Formulare (gilt genauso fuer /einspruch). | **wirklich**: Das Vorbild traegt hier nicht: /api/freigaben/fenster leitet in ALLEN drei Ausgaengen fest auf die DETAILseite /portal/{mandant}/freigaben/{id} um, nie auf die Unterseite. Eine FEHLER_TEXT-Tabelle auf /rueckgaengig bzw. /einspruch wird also nie erreicht — nach Absenden landet man auf der Detailseite, die ?fehler=grund und ?fehler=fenster ihrerseits gar nicht auswertet (sie kennt nur fehler==='ausfuehrung' und vermerkt=). Der Unterschied zum Vorbild ist belegt: /api/vergabe/einreichung leitet im Fehlerfall ausdruecklich auf das FORMULAR zurueck und nur im Erfolgsfall auf die Mappenseite. Ohne ein Ziel-/Rueckkehrfeld in der Fenster-Route verschwindet jede Fehlermeldung dieser zwei Seiten spurlos. | **beleg**: src/app/api/freigaben/fenster/route.ts:46 `const seite = `/portal/${mandant}/freigaben/${freigabe}`;` — verwendet in :52 (?fehler=grund), :75 (?fehler=fenster) und :84 (?vermerkt=); src/app/portal/[mandant]/freigaben/[id]/page.tsx:257 wertet nur `fehler === 'ausfuehrung'` aus; Gegenbeispiel src/app/api/vergabe/einreichung/route.ts:51 (`formular`), :73-74 (Fehler → formular) gegen :81-82 (Erfolg → mappenseite). | **aufwand_korrigiert**: klein (unveraendert), aber mit einer Aenderung an /api/freigaben/fenster (gepruefter Rueckkehrpfad) — sonst ist der geplante Fehlerzweig beider Seiten unerreichbar

- **pfad**: /portal/[mandant]/radar/[id]/status | **behauptet**: "in_bearbeitung verlangt eine bestehende Vergabemappe, und fehlt sie, steht dort der Verweis auf /radar/[id]/mappe statt eines Knopfes, der in einen Fehler laeuft." | **wirklich**: Falsch — und so gebaut nimmt die Unterseite eine funktionierende Handlung weg (CLAUDE.md: "Adding a feature never removes an existing one"). setzeVorgangsstand LEGT die Mappe bei in_bearbeitung selbst an, idempotent; eine vorhandene Mappe ist keine Voraussetzung. Die einzige Vorbedingung ist das RECHT vergabe.schreiben, und dafuer gibt der Dienst eigens den Code 'mappe_recht' zurueck, den die Route als ?fehler=mappe_recht spiegelt. Richtig ist also: Knopf zeichnen, wenn vergabe.schreiben gehalten wird (unabhaengig davon, ob eine Mappe existiert), und sonst den Rechtesatz zeigen — nicht einen Verweis auf die Mappenseite, die ohne Vorgang ohnehin nichts anzeigt. | **beleg**: src/server/services/radar/vorgang.ts:104-114 (`app.hat_recht('vergabe.schreiben', …)` → VorgangFehler('mappe_recht'), danach `await legeMappeAn(kontext, zeile.id)`); src/server/services/vergabe/mappe.ts:51-66 (insert … on conflict (mandant_id, ausschreibung_vorgang_id) do update — ausdruecklich idempotent); src/app/api/radar/vorgang/route.ts:78-81 (nur 'grund' und 'mappe_recht' werden zur Auskunft). | **aufwand_korrigiert**: klein (unveraendert) — aber die Bedingung ist das Recht vergabe.schreiben, nicht die Existenz einer Mappe

- **pfad**: /portal/[mandant]/radar/[id]/status | **behauptet**: "Die Statushistorie aus ausschreibung_vorgang wird mit Zeitpunkt, Person und Grund darunter gelistet, damit die Zahlen des Pipeline-Berichts (REP-06) an ihrer Quelle nachlesbar sind." | **wirklich**: Diese Historie gibt es in ausschreibung_vorgang nicht. Die Tabelle fuehrt genau EINE Zeile je (mandant_id, ausschreibung_id) — Unique-Index av_uk, und setzeVorgangsstand schreibt per on conflict … do update in dieselbe Zeile. Erhalten bleibt nur die LETZTE Aenderung (status_geaendert_am, status_geaendert_von, verworfen_grund); jeder Zwischenstand ist ueberschrieben. Die tatsaechliche Historie liegt allein im audit_log, den der Dienst per app.protokolliere('radar.stand_gesetzt', …) fuellt. Wer die geplante Liste will, liest audit_log (plus Join auf benutzer fuer die Person) — eine zweite Quelle, die im Plan nicht vorkommt. | **beleg**: psql `\d ausschreibung_vorgang` → `"av_uk" UNIQUE, btree (mandant_id, ausschreibung_id) WHERE geloescht_am IS NULL`, Spalten status_geaendert_am/status_geaendert_von/verworfen_grund (je einmal); src/server/services/radar/vorgang.ts:76-88 (on conflict (mandant_id, ausschreibung_id) … do update) und :126-130 (app.protokolliere('radar.stand_gesetzt', …)); psql `select tablename, qual from pg_policies where tablename='audit_log'` → t_audit_lesen (nur Mandantensichtbarkeit). | **aufwand_korrigiert**: mittel statt klein — die versprochene Historie ist ein neuer Lesepfad auf audit_log, nicht ein weiterer select auf ausschreibung_vorgang

- **pfad**: /portal/[mandant]/radar/profile/[id] | **behauptet**: "Geloescht wird nicht hart, sondern ueber geloescht_am/geloescht_von beziehungsweise ist_aktiv" — und zu bauen sind unter anderem setzeCpv und setzeEmpfaenger. | **wirklich**: Das gilt nur fuer radar_profil. radar_profil_cpv und radar_profil_empfaenger — genau die beiden Tabellen, die der Editor bearbeiten soll — haben WEDER geloescht_am noch geloescht_von noch ist_aktiv; sie haengen per on delete cascade am Profil und tragen eigens einen DELETE-Trigger fuer den Versionszaehler. setzeCpv/setzeEmpfaenger MUESSEN also hart loeschen. Das ist zulaessig (Invariante 8 nennt Finanzen, Zeiterfassung, Audit — Radar nicht), aber die Regel im Plan ist fuer die beiden Kindtabellen nicht erfuellbar und muss beim Bauen ausdruecklich anders lauten, sonst sucht jemand eine Spalte, die es nicht gibt. | **beleg**: psql `\d radar_profil_cpv` → Spalten id, mandant_id, radar_profil_id, cpv_code, praefix_laenge, gewichtung, wirkung, bezeichnung, ist_platzhalter, erstellt_am (kein geloescht_am/ist_aktiv), `rpc_profil_fk … ON DELETE CASCADE`, Trigger `trg_rpc_version_del AFTER DELETE`; `\d radar_profil_empfaenger` → id, mandant_id, radar_profil_id, benutzer_id, ab_punkte, erstellt_am (ebenso keine); zum Vergleich `\d radar_profil` → geloescht_am, geloescht_von, ist_aktiv vorhanden. | **aufwand_korrigiert**: mittel (unveraendert fuer diesen Punkt), aber die Loeschregel im Plan ist fuer die Kindtabellen zu streichen

- **pfad**: /portal/[mandant]/radar/profile/[id] | **behauptet**: aufwand "mittel" — waehrend der Richtlinieneditor (eine Zeile, drei Felder) "gross" bekommt. | **wirklich**: Die Einordnung ist gegenlaeufig zur eigenen Massstabsetzung des Plans. Hier sind drei Tabellen zu schreiben (radar_profil, radar_profil_cpv, radar_profil_empfaenger), zwei text[]-Spalten, Wertgrenzen als bigint-Cent mit Waehrung, vier gesperrte Felder mit offenen Fragen (O-15, O-98, O-191, O-47), dazu Dienst (leseProfil, schreibeProfil, setzeCpv, setzeEmpfaenger), API-Route, Verweise von der Liste — und ein Einzelprofil-Lader existiert nicht. Der Richtlinieneditor schreibt dagegen genau eine Zeile mit auto_erlaubt, max_betrag_cent und begruendung. Wenn richtlinien/[id] "gross" ist, ist profile/[id] es mindestens auch. | **beleg**: src/app/portal/[mandant]/radar/daten.ts:192 leseProfile() liefert nur eine Liste (kein Lader fuer eine ID), und weder src/server/services/radar/profil.ts noch src/app/api/radar/profil/ existieren (ls src/server/services/radar/ · ls src/app/api/radar/ → nur vorgang); psql `\d radar_profil` (27 Spalten, darunter nuts_praefixe/positiv_keywords/negativ_keywords als text[], waehrung character(3), gewichtung jsonb); zum Vergleich `\d agent_richtlinie` (10 Spalten, davon drei bearbeitbar); docs/DECISIONS.md:2856 (O-15), :3048 (O-47), :3064 (O-98), :2993 (O-191). | **aufwand_korrigiert**: gross
