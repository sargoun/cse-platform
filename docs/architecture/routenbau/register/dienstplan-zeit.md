# Registereinträge: dienstplan-zeit

**Warteschlange, kein Archiv.** Diese Einträge sind **noch nicht** im Baum. Die
gemeinsamen Dateien pflegt EINE Hand, weil mehrere Agenten gleichzeitig arbeiten
und sich sonst in dieselbe Zeile schreiben. Ist ein Abschnitt eingetragen, wird
er hier gelöscht — solange er hier steht, fehlt er dort.

**Die Einträge des Behebungsschritts gelten.** Er lief zuletzt und hatte den
Auftrag, die vollständige aktuelle Liste zu liefern — auch das, was sich seit
dem Bauschritt geändert hat.

## Stand

- Bau: fertig
- Kritik: 11 Befunde
- Behebung: 13 behoben, 3 widerlegt, 5 offen

## Migrationen (gegen eine eigene Datenbank gefahren: True)

- /home/user/cse-platform/drizzle/0265_dienstplan_veroeffentlichung.sql — Tabelle dienstplan_veroeffentlichung (Mandant, Zeitraum von/bis als Berliner Kalendertage, umfang als CHECK statt Enum, die Zahlen, die BEIM Veroeffentlichen galten, empfaenger_anzahl und ohne_zugang_anzahl), RLS mit FORCE, Lesen mit dienstplan.lesen / Anlegen mit dienstplan.veroeffentlichen, restriktive Portaldecke (nur intern, K-04), kein UPDATE (eine Bekanntgabe ist ein Ereignis), Gruppenansicht nur lesend, Loeschsperre-Block (append)
- /home/user/cse-platform/drizzle/0266_dienstplan_veroeffentlichung_definer.sql — app.dienstplan_veroeffentlichung_anlegen(date,date,text,int,int,int,int,jsonb,text,text,jsonb): der EINZIGE Weg, auf dem eine Anwendungsrolle eine benachrichtigung-Zeile erzeugen kann, weil cse_app darauf kein Tabellenrecht INSERT haelt. Prueft dienstplan.veroeffentlichen, Nur-Lesen und app.portal()='intern' selbst, weist jeden Artschluessel ausser dienstplan.plan_veroeffentlicht ab, loest den Empfaenger deckungsgleich mit benutzer_person_key auf, zaehlt die ohne Zugang, protokolliert dienstplan.veroeffentlicht, Zeit aus now(). owner to cse_definer (K-01), revoke from public, grant execute an cse_app (0092)
- 0267-0274 aus meinem Bereich NICHT verbraucht — kein neuer Enum-Wert, keine neue Spalte, kein Zustand auf einsatz (K-17 ausdruecklich respektiert)
- (Behebung) drizzle/0265_dienstplan_veroeffentlichung.sql — geaendert: der Block zwischen den Sentinels ist jetzt byteweise `erzeuge('0265')` (vorher handgeschrieben UND um eine Leerzeile abweichend). Keine Schemaaenderung.
- (Behebung) drizzle/0266_dienstplan_veroeffentlichung_definer.sql — geaendert: app.dienstplan_veroeffentlichung_anlegen loest den Empfaenger zusaetzlich gegen `anstellung (person_id, mandant_id = v_mandant)` auf, in beiden Schleifen wortgleich; ein Eintrag ohne Anstellung zaehlt als ohne_zugang statt zugestellt zu werden. Kopfkommentar und comment-on-function nachgezogen. Signatur, Owner (cse_definer) und Grants unveraendert.
- (Behebung) Keine neue Migrationsnummer angelegt — 0267 bis 0274 bleiben frei.

## Gebaute Adressen

- `/portal/[mandant]/dienstplan/konflikte/[id]/quittung` — fertig
  - Einzelansicht plus Quittungsbeleg; schreibt ueber das BESTEHENDE POST /api/konflikt, kein zweiter Weg. Drei Zustaende: offen+nicht blockiert = Formular mit Pflichtbegruendung (>=10 Zeichen); blockiert = der Satz, dass § 34a GewO keinen Uebergehen-Knopf kennt; quittiert = der Beleg (wer, wann nach Serveruhr, welche Begruendung). Die Kritik hat 'blocker: keiner' widerlegt: die Route ist nur auf dienstplan.konflikt_quittieren getort, planungs_konflikt ist aber nur mit dienstplan.lesen lesbar, arbeitszeit_verstoss nur mit dienstplan.arbzg_lesen. leseKonflikt() liefert deshalb arbzgSichtbar MIT, und die Seite weist den ArbZG-Block ohne das Recht als 'nicht einsehbar' aus statt als 'kein Befund'. Fremder Mandant nur als DASS (K-06), fremde Kennung 404 (AUT-06).
- `/portal/[mandant]/dienstplan/konflikte/[id]/uebersteuern` — fertig
  - Schreibweg ausschliesslich ueber app.arbzg_befund_quittieren (uebersteuereBefund). Die Kritik korrigiert den Plan und ist nachgemessen: cse_app hat auf arbeitszeit_verstoss gar kein Tabellenrecht UPDATE, ein direktes update bricht LAUT mit 'permission denied' ab (500er), nicht still mit 0 Zeilen. Der Definer prueft nur dienstplan.arbzg_lesen; das staerkere dienstplan.arbzg_uebersteuern setzt die Route mit authorize() selbst durch, die Datenbank ist hier ausdruecklich die schwaechere zweite Linie (siehe D-Zeile in registry_sonstiges). Blockierende Befunde werden mit 409 abgewiesen, nicht bloss das Formular versteckt. Grenzwertrichtung richtig: grenzwertIstMindestwert('ruhezeit_unter_11h') = true, damit '4,07 h statt mindestens 11,00 h' dasteht und nicht 'statt hoechstens'.
- `/portal/[mandant]/dienstplan/offene-schichten` — fertig
  - Gewerkeuebergreifende Liste der Besetzungsluecken, Filter ueber searchParams (Vorgabe: naechste 14 Tage), Objektfilter, 'nur unter Mindestbesetzung'. Der Blocker lautet nach der Kritik anders als im Plan: REC-01 IST gefuettert (recruiting/dienst.ts bedarf() fragt genau unbesetzte Schichten ab) — das Problem sind drei auseinanderlaufende Definitionen von 'unbesetzt' (posten_unterbesetzung, Kachel schichten_unbesetzt, bedarf()). leseOffeneSchichten() fuehrt sie nicht zusammen, sondern macht die Differenz SICHTBAR und nennt sie: Dringlichkeit kommt allein aus min_besetzung, die Luecke gegen soll steht als blosse Differenz daneben. Der Seed uebte den schwaecheren Fall von nichts (unter_min == unter_soll == 115); dafuer gibt es jetzt seedTeilbesetzteSchicht().
- `/portal/[mandant]/dienstplan/serien/[id]` — fertig
  - Serienblatt mit Traeger-Join (leseSerie ist KEIN Einzelsatz-Read: planungsserie traegt keine bezeichnung/rrule/dauer — die kommen per coalesce aus turnus/posten/revier/objekt, wie in der Listenseite), materialisierte Einsaetze im Horizont mit dst_luecke/dst_doppelt, und der Ausnahmeteil mit ALLEN DREI Arten (ausfall, verschiebung, zusatz — 'zusatz' fehlte im Plan und ist der einzige Weg, eine Zusatzschicht in eine Serie zu haengen). Der echte Blocker ist BEHOBEN statt umschrieben: ladeAusnahmen() kehrte bei serie.turnusId === null sofort mit [] zurueck und las nur turnus_ausnahme; posten_ausnahme wurde im ganzen src/-Baum nie gelesen, obwohl cse_app hineinschreiben darf. Jetzt liest der Generator beide Tabellen und nimmt ersatz_besetzung mit. Die Ausnahmetabellen gehoeren den GEWERKEN: turnus_ausnahme verlangt reinigung.schreiben, posten_ausnahme security.schreiben — die Route prueft deshalb dienstplan.schreiben UND das Gewerkerecht, das der Traeger bestimmt.
- `/portal/[mandant]/dienstplan/veroeffentlichung` — teilweise
  - Vollstaendig gebaut, aber vier Geschaeftsregeln bleiben Platzhalter nach Regel 1 (O-710 bis O-713), in der Oberflaeche sichtbar als 'offen (O-NN)'. Zeitraumwaehler, Vorschau (Schichten, unbesetzte, offene und blockierende Konflikte, je Person ihre Schichten in Europe/Berlin — genau die Nachricht, die sie bekaeme), Verlauf der letzten Vorgaenge. KEIN Zustand auf einsatz: 'veroeffentlicht' fehlt in einsatz_status mit Absicht (K-17) und wurde NICHT ergaenzt. Der HARTE Blocker der Kritik ist erledigt: cse_app hat auf benachrichtigung kein INSERT (nicht bloss keine Policy) — der geplante Weg 'Route schreibt je Person eine Zeile' waere mit 'permission denied' gebrochen. Dafuer 0266: app.dienstplan_veroeffentlichung_anlegen, security definer, owner cse_definer, stellt AUSSCHLIESSLICH dienstplan.plan_veroeffentlicht zu und weist jeden anderen Artschluessel ab. Die Kritik hat ausserdem recht, dass zwei dienstplan.*-Arten schon registriert sind — es fehlte nur diese eine, und sie ist in bootstrap.ts eingehaengt. ohne_zugang_anzahl wird gezaehlt und angezeigt: eine Bekanntgabe, die die halbe Kolonne nicht erreicht, ist keine.
- `/portal/[mandant]/zeiten/einwaende/[id]` — fertig
  - Links die Meldung des Menschen (behauptet_* ausdruecklich als Angabe gekennzeichnet, nie vorbelegt, Invariante 5), rechts die Aufzeichnung. Die Spaltennamen stehen PAARWEISE, wie die Kritik verlangt: geraete_zeit_beginn/_ende, zeitabweichung_beginn_sek/_ende_sek, dauer_brutto_minuten/_netto_minuten — ein 'zeitabweichung_sek' gibt es nicht. Selbstentscheidung wird VORHER abgefangen (EinwandEigenerFehler in entscheideEinwand, gleiche Bedingung wie kern.zeit_einwand_status), sonst endete der Klick in einem ungefangenen 500. zeit.lesen wird mitverlangt, sonst notFound() ohne Grund. NEU in dieser Sitzung: die Verknuepfung Korrektur -> Meldung. zeiteintrag_korrektur.zeit_einwand_id existiert seit Anlage der Tabelle mit eigenem Fremdschluessel zk_einwand_fk, wurde von leseEinwand GELESEN und von niemandem GESCHRIEBEN — das Blatt sagte darum IMMER 'anerkannt, aber keine Korrektur'. korrigiereZeiteintrag nimmt jetzt zeitEinwandId, die Route reicht das Formularfeld 'einwand' durch, das Korrekturblatt traegt es als verstecktes Feld und benennt eine nicht einsehbare Kennung statt sie still fallenzulassen, und der Knopf auf dem Einwandblatt bringt ?einwand= mit.

## src/server/registry/dienste.ts

In src/server/registry/dienste.ts, ans Ende von DIENSTE (fuenf Zeilen, die fuenfte ist der Nachtrag ueber die Befunde hinaus):

  /*
   * Dienstplan, Phase „Bekanntgabe" — die Bekanntgabe selbst, der
   * Konflikt-Einzelsatz hinter Quittung und Uebersteuerung, die eine
   * Meldungsart (NOT-01) und die gewerkeuebergreifende Besetzungsluecke.
   */
  {
    modul: 'dienstplan', pfad: 'dienstplan/veroeffentlichung',
    schreibend: true, schreibRecht: 'dienstplan.veroeffentlichen',
  },
  /* Schreibt ueber app.arbzg_befund_quittieren — arbeitszeit_verstoss
     gewaehrt cse_app kein UPDATE; das staerkere Recht setzt die Route durch. */
  {
    modul: 'dienstplan', pfad: 'dienstplan/konflikt',
    schreibend: true, schreibRecht: 'dienstplan.arbzg_uebersteuern',
  },
  /* Registriert nur die Art dienstplan.plan_veroeffentlicht (D-493). */
  { modul: 'dienstplan', pfad: 'dienstplan/benachrichtigung', schreibend: false },
  { modul: 'dienstplan', pfad: 'dienstplan/besetzungsluecke', schreibend: false },
  { modul: 'dienstplan', pfad: 'dienstplan/serienliste', schreibend: false },

## src/server/auth/route-manifest.ts

In src/server/auth/route-manifest.ts, in ROUTEN (drei Eintraege):

  {
    /**
     * Einen ArbZG-Befund uebersteuern (TIM-06) — **nicht** `api/konflikt`.
     * Dort wird der PLANUNGSKONFLIKT quittiert, hier der BEFUND dahinter
     * uebersteuert: zwei Aufzeichnungen, zwei Rechte, zwei Handlungen.
     * `app.arbzg_befund_quittieren` prueft nur `dienstplan.arbzg_lesen`,
     * also das schwaechere Recht; das staerkere setzt diese Route mit
     * `authorize()` selbst durch, die Datenbank ist hier die zweite Linie.
     */
    pfad: 'api/konflikt/uebersteuern',
    recht: 'dienstplan.arbzg_uebersteuern',
  },
  {
    /**
     * Einen Zeitraum bekanntgeben (TIM-01, NOT-01). Geschrieben wird ueber
     * `app.dienstplan_veroeffentlichung_anlegen` (0266) — `cse_app` haelt auf
     * `benachrichtigung` kein Tabellenrecht INSERT. Die Leserechte
     * (`dienstplan.lesen`, `objekt.lesen`) prueft der Dienst zusaetzlich,
     * damit der Beleg nicht aus einer von der RLS leergeraeumten Abfrage
     * entsteht.
     */
    pfad: 'api/dienstplan/veroeffentlichung',
    recht: 'dienstplan.veroeffentlichen',
  },
  {
    /**
     * Eine Einzeltermin-Ausnahme einer Serie (TIM-02): ausfall,
     * verschiebung, zusatz. `dienstplan.schreiben` ist das Torrecht; die
     * Ausnahmetabellen gehoeren aber den GEWERKEN — `turnus_ausnahme`
     * verlangt `reinigung.schreiben` (0029), `posten_ausnahme`
     * `security.schreiben` (0069). Welches gilt, entscheidet der Traeger,
     * und der Handler prueft es nach dem Lesen der Serie zusaetzlich selbst.
     */
    pfad: 'api/dienstplan/serien/[id]/ausnahmen',
    recht: 'dienstplan.schreiben',
  },

## src/server/db/schema/rls.ts

In src/server/db/schema/rls.ts, in KEIN_HARD_DELETE (der Block in drizzle/0265 ist bereits byteweise das, was `erzeuge('0265')` mit GENAU diesem Text produziert — nach dem Eintragen `pnpm db:triggers` laufen lassen, es darf sich nichts aendern):

  {
    tabelle: 'dienstplan_veroeffentlichung',
    art: 'append',
    migration: '0265',
    grund:
      'TIM-01, NOT-01, LEG-03. Sie ist der Beleg, dass ein Zeitraum an einem '
      + 'Zeitpunkt bekanntgegeben wurde — im Streit ueber eine Schicht, von der '
      + 'jemand nichts gewusst haben will, genau die Zeile, die zaehlt. Es gibt '
      + 'nichts, was eine Bekanntgabe beendet: eine Aenderung ist eine zweite '
      + 'Zeile, nie ein Loeschen der ersten.',
  },

Dazu ist `'0265': join(WURZEL, 'drizzle/0265_dienstplan_veroeffentlichung.sql')` in MIGRATIONS_DATEIEN (scripts/generate-triggers.ts) bereits eingetragen — diese Datei steht nicht auf der Sperrliste und die Zeile ist ohne den Registereintrag wirkungslos.

## src/server/registry/navigation.ts

Keine Aenderung. Die vier Routen dieser Domaene haengen unter dem bestehenden Tab „dienstplan" bzw. „zeiten"; ein neuer Menuepunkt entsteht nicht, und 04-SEITENKARTE.md wurde nicht angefasst.

## Sonstiges

D-Zeile fuer docs/DECISIONS.md (Abschnitt der Entscheidungen, naechste freie D-Nummer):

| D-NNN | **Der Uebersteuerungsweg fuer ArbZG-Befunde setzt das staerkere Recht in der ROUTE durch, nicht in der Datenbank.** `arbeitszeit_verstoss` gewaehrt `cse_app` kein Tabellenrecht `UPDATE` — ein direktes `update` bricht LAUT ab (`permission denied`, also ein 500er), nicht still mit null Zeilen. Der einzige Schreibweg ist `app.arbzg_befund_quittieren`, und diese Definer-Funktion prueft `dienstplan.arbzg_lesen`, also das SCHWAECHERE Recht. `dienstplan.arbzg_uebersteuern`, auf das die Seitenkarte `…/uebersteuern` tort, setzt deshalb `POST /api/konflikt/uebersteuern` mit `authorize()` selbst durch; die Datenbank ist hier ausdruecklich die zweite und schwaechere Linie. Verlassen darf sich niemand darauf. Mittelfristig zieht eine Migration den Definer nach — bis dahin gilt: wer einen zweiten Aufrufer fuer `app.arbzg_befund_quittieren` baut, muss das Recht dort erneut pruefen. | TIM-06, AUT-06, `services/dienstplan/konflikt.ts`, `api/konflikt/uebersteuern/route.ts`, 0040 |

Zwei Dinge, die diese Zeile begleiten und schon im Code stehen: (a) `uebersteuereBefund` liest den Erfolg nach, weil `app.arbzg_befund_quittieren` `where hinfaellig_am is null` aktualisiert und danach BEDINGUNGSLOS protokolliert; (b) `app.arbzg_befund_ueberholen` laesst `status='offen'` stehen, der Status allein ist also kein Ausdruck der Uebersteuerbarkeit.

## Zeilen für docs/DECISIONS.md, Abschnitt „Offen"

| O-710 | **Welcher Zeitraum wird veröffentlicht — die Kalenderwoche, der Kalendermonat oder ein frei gewähltes Fenster?** `dienstplan.veroeffentlichen` ist ein Recht auf eine HANDLUNG; `einsatz_status` kennt mit Absicht keinen Wert `veroeffentlicht` (K-17, drizzle/0028, 04-PLANUNG-ZEIT.md §224), und einen Ablauf zu modellieren, den die SPEC nicht beschreibt, wäre eine erfundene Geschäftsregel. **Heute gebaut:** `dienstplan_veroeffentlichung.umfang` trägt die drei Kandidaten `woche`/`monat`/`freier_zeitraum` als Platzhalter (`UMFAENGE`, eine Konstante); die Oberfläche gibt die kommende Woche in Berliner Wochengrenzen vor und nennt das ausdrücklich eine Oberflächenvorgabe. Die Datenbank begrenzt nur auf höchstens ein Jahr (`dv_zeitraum_begrenzt`). | TIM-01, NOT-01, K-17, `services/dienstplan/veroeffentlichung.ts`, `drizzle/0265` |
| O-711 | **Wer wird bei einer Veröffentlichung benachrichtigt — jede im Zeitraum eingeteilte Person, oder auch Personen, deren Schicht gestrichen wurde?** Der zweite Fall tut am meisten weh: wer eine Schicht hatte und sie nicht mehr hat, erfährt heute nichts. **Heute gebaut:** benachrichtigt wird, wer im Fenster eine lebende Einteilung hat (`entfernt_am is null`, `status <> 'abgesagt'`); die Abgrenzung steht wörtlich in `umfang_daten` der Vorgangszeile, und die Vorschau zeigt je Person ihre Schichten in Europe/Berlin — genau die Nachricht, die sie bekäme. Wer keinen aktiven Zugang hat, wird als `ohne_zugang` GEZÄHLT und auf dem Bildschirm als „kein Zugang — nur telefonisch" geführt, solange der Planer noch anrufen kann. Der Definer (0266) prüft zusätzlich eine `anstellung` in der aktiven Gesellschaft — eine Schranke gegen beliebige Konten, keine Antwort auf diese Frage. | TIM-01, NOT-01, D-09, EMP-14, `services/dienstplan/veroeffentlichung.ts`, `drizzle/0266` |
| O-712 | **Was bedeutet eine Änderung des Plans NACH der Veröffentlichung — eine neue Meldung an die Betroffenen, eine Sperre, oder gar nichts? Und sperren blockierende Konflikte im Fenster die Veröffentlichung?** **Heute gebaut:** nichts geschieht von selbst; eine zweite Bekanntgabe ist eine zweite Zeile, nie ein Überschreiben der ersten (`dienstplan_veroeffentlichung` kennt kein UPDATE und keine harte Löschung, Invariante 8). Blockierende Konflikte halten die Bekanntgabe NICHT auf — eine Sperre, die niemand bestellt hat, ließe die Disposition am Einsatztag stehen. Ihre Zahl steht in der Vorschau obenan und wird im Beleg mitgeschrieben: wer trotz drei Sperren veröffentlicht hat, ist damit nachweisbar. | TIM-01, NOT-01, O-166, Invariante 8, `services/dienstplan/veroeffentlichung.ts` |
| O-713 | **Ist eine bekanntgegebene Schicht gegen stille Änderung geschützt, und wenn ja: wer darf sie danach noch ändern und unter welcher Protokollpflicht?** **Heute gebaut: nicht, und zwar nicht aus Versehen.** Es gibt keinen Zustand auf `einsatz`, keine neue Spalte und kein Einfrieren — `einsatz_status` kennt `veroeffentlicht` mit Absicht nicht (K-17). Der Vorgang ist ein Beleg neben dem Plan, kein Schloss darauf. Wird die Frage mit „geschützt" beantwortet, ist das eine Migration (Zustand oder Sperrspalte) plus eine Protokollpflicht, kein Zusatz in einer Route. | TIM-01, K-17, drizzle/0028, `drizzle/0265`, 04-PLANUNG-ZEIT.md §224 |
| O-714 | **Darf eine Ausnahme mit reduzierter Stärke unter die Mindestbesetzung des Postens gehen, oder ist die Mindestbesetzung eine harte Untergrenze, die eine Ausnahme nicht senken kann?** `posten_ausnahme.ersatz_besetzung` ersetzt laut 0069 §6.4 die SOLLbesetzung; was aus der MINDESTbesetzung wird, steht dort nicht — und es ist keine Kleinigkeit: eine Nacht mit einer Wache statt zwei liegt unter dem Minimum eines Postens, der zwei verlangt, und ob das zulässig ist, entscheidet der Vertrag (SEC-01). **Heute gebaut:** `ersatz_besetzung` ersetzt die Sollstärke, und die Mindestbesetzung wird auf `min(min, ersatz)` MITGESENKT (`besetzungMitAusnahme`), damit keine Schicht entsteht, die per Konstruktion unterbesetzt gemeldet wird (`min > soll` wäre genau das) und die reduzierte Nacht in `/dienstplan/offene-schichten` nicht als Notfall erscheint, den niemand beheben kann. Die Gegenrichtung — Minimum stehen lassen und die Nacht dauerhaft rot melden — ist die andere denkbare Antwort, und sie gehört dem Kunden. | SEC-01, TIM-02, O-210, 0069 §6.4, `services/dienstplan/vorkommnisse.ts`, `tests/kern/dienstplan-ausnahme-staerke.test.ts` |

## Befunde des Prüfers (11)

- **blockierend** · `/home/user/cse-platform/src/app/portal/[mandant]/dienstplan/veroeffentlichung/page.tsx` — Die Seite holt `dienstplan.lesen` und `dienstplan.arbzg_lesen` (Zeile 107-109) und BENUTZT beide nie — verwendet wird ausschliesslich `darf['dienstplan.veroeffentlichen']` (Zeile 306). Die Schwesterseiten dieser Domaene (quittung/page.tsx:75, uebersteuern/page.tsx:76) rufen an derselben Stelle `notFound()`. Die Seitenkarte tort diese Route nur auf `dienstplan.veroeffentlichen` (routen.generiert.ts:184), `einsatz` ist fuer `cse_app` aber nur mit `dienstplan.lesen` lesbar. Eine Sitzung mit dem Veroeffentlichungsrecht ohne Leserecht bekommt deshalb eine Vorschau aus lauter Nullen, der Knopf steht trotzdem da, und `app.dienstplan_veroeffentlichung_anlegen` prueft seinerseits nur `dienstplan.veroeffentlichen`. Ergebnis: eine `dienstplan_veroeffentlichung`-Zeile mit schichten=0/empfaenger=0, ein `audit_log`-Eintrag `dienstplan.veroeffentlicht`, KEINE einzige Benachrichtigung — und auf dem Bildschirm der gruene Satz „Veröffentlicht. 0 Person(en)". Die Zeile ist laut 0265 genau der Beleg, „der im Streit zaehlt"; sie behauptet dann eine Bekanntgabe, die niemanden erreicht hat.
  - Behebung: Nach `haeltRechte` dieselbe Zeile wie in quittung/page.tsx einziehen: `if (darf['dienstplan.lesen'] !== true) notFound();`. Zusaetzlich in `veroeffentliche()` (src/server/services/dienstplan/veroeffentlichung.ts:290) den Fall `bild.schichten === 0 && bild.personen.length === 0` nicht stumm schreiben, sondern abweisen bzw. `dienstplan.lesen` im Definer 0266 mitpruefen — der Beleg darf nicht aus einer Abfrage entstehen, die die RLS gerade leergeraeumt hat. `darf['dienstplan.arbzg_lesen']` entweder benutzen (die blockierenden Konflikte kommen aus `planungs_konflikt`, nicht aus `arbeitszeit_verstoss` — dann ist der Schluessel ueberfluessig) oder streichen.
- **wichtig** · `/home/user/cse-platform/src/app/api/konflikt/uebersteuern/route.ts` — Die Route weist einen bereits entschiedenen Befund ueber `k.verstoss.status !== 'offen'` ab (Zeile 110), prueft aber `hinfaellig_am` nicht — weder am Konflikt (`k.hinfaellig` wird gelesen und nicht benutzt) noch am Befund (`leseKonflikt` selektiert `v.hinfaellig_am` gar nicht). Ein ueberholter Befund traegt `hinfaellig_am is not null` UND weiterhin `status='offen'` (so schreibt es `app.arbzg_befund_ueberholen`). `app.arbzg_befund_quittieren` aktualisiert aber nur `where v.id = p_id and v.hinfaellig_am is null` und protokolliert danach BEDINGUNGSLOS `arbzg.befund_quittiert`. Folge: null Zeilen geaendert, ein Pruefprotokolleintrag, der eine Uebersteuerung behauptet, die es nicht gibt, und der Redirect auf `?uebersteuert=1` zeigt den gruenen Satz „Der Befund ist übersteuert", waehrend der ArbZG-Block daneben weiter „offen" sagt. Erreichbar durch das Rennen zwischen gerendertem Formular und Nachtlauf (`raeumeAuf` in arbzg/detektor.ts:347) sowie durch jeden nachgebauten POST. `uebersteuereBefund` (konflikt.ts:272) ignoriert zudem, ob ueberhaupt eine Zeile getroffen wurde.
  - Behebung: `leseKonflikt` um `v.hinfaellig_am` erweitern (Feld `hinfaellig` an `ArbzgBlatt`), in der Route zusaetzlich `if (k.hinfaellig || k.verstoss.hinfaellig) return 409 'nicht_mehr_aktuell'` pruefen, und `uebersteuereBefund` den Erfolg nachlesen lassen (`returning`/erneuter `select status`), damit ein Null-Zeilen-Fall nicht als Erfolg redirected wird.
- **wichtig** · `/home/user/cse-platform/src/server/services/dienstplan/serie.ts` — `legeAusnahmeAn` laesst `dauerMinuten` bis einschliesslich 1440 zu (Zeile 694), obwohl `nominalesEnde` in vorkommnisse.ts bei `> MAX_DAUER_MINUTEN` (= 1439) einen `PlanungsFehler` wirft — genau die Inkonsistenz, die derselbe Commit fuer `pruefeTurnusEingabe` ausdruecklich behoben und in vorkommnisse.ts:132-138 dokumentiert hat („Eine Zahl, zwei Aufrufer"). Das Serienblatt bietet den Wert an (`max={1440}`, page.tsx:500). Bei art `zusatz` oder `verschiebung` laeuft der Generator in derselben Transaktion (`legeAusnahmeAn` ruft `generiereEinsaetze`), `nominalesEnde` wirft, `PlanungsFehler` traegt weder `code` noch `status`, `alsAntwort` gibt `null` zurueck und die Route wirft weiter: HTTP 500, Transaktion zurueckgerollt, kein Hinweis fuer den Planer.
  - Behebung: In serie.ts `MAX_DAUER_MINUTEN` importieren (der Import steht bereits in Zeile 5) und die Schranke auf `dauer > MAX_DAUER_MINUTEN` setzen; `max={1440}` im Formular auf denselben Wert ziehen (am besten den konstanten Wert in die Seite reichen statt ihn zu tippen).
- **wichtig** · `/home/user/cse-platform/src/server/services/dienstplan/serie.ts` — Eine zweite Ausnahme am selben Tag endet als HTTP 500 statt als Meldung. `posten_ausnahme` traegt `posten_ausnahme_uk UNIQUE (posten_id, datum)` — UNBEDINGT, also auch fuer `zusatz`; `turnus_ausnahme_uk` ist partiell auf `ausfall`/`verschiebung`. Ein Unique-Verstoss (23505) kommt als roher Postgres-Fehler zurueck; `alsAntwort` erkennt nur Fehler mit `status`+`code`-Feldern und gibt `null` zurueck, die Route wirft weiter. Ein Doppelklick auf „Ausnahme anlegen" oder die Korrektur eines Tippfehlers am selben Tag produziert damit einen Serverfehler statt „fuer diesen Tag gibt es schon eine Ausnahme". Dasselbe gilt fuer `posten_ausnahme_verschiebung_braucht_beginn` und `..._ersatz_besetzung` (23514), sofern sie doch einmal durchschlagen.
  - Behebung: In `legeAusnahmeAn` den Postgres-Fehler abfangen und in einen `AusnahmeNichtTragfaehig` (der traegt bereits `code`/`status` 409) uebersetzen — mindestens `23505` („Fuer diesen Tag gibt es schon eine Ausnahme dieser Serie") und `23514`. Die Klasse existiert schon in serie.ts:628.
- **wichtig** · `/home/user/cse-platform/src/server/registry/dienste.ts` — Die im Bericht unter `registry_dienste` / `registry_manifest` zitierten Eintraege stehen NICHT im Baum. Drei Kerntests sind dadurch rot: `tests/kern/portal-shell.test.ts` meldet `dienstplan/veroeffentlichung`, `dienstplan/konflikt`, `dienstplan/benachrichtigung`, `dienstplan/besetzungsluecke` als nicht registrierte Dienste, `tests/kern/routen.test.ts` meldet `api/konflikt/uebersteuern`, `api/dienstplan/veroeffentlichung` und `api/dienstplan/serien/[id]/ausnahmen` als nicht im Route-Manifest. Die Routen pruefen ihr Recht selbst, es fehlt also keine Autorisierung — aber die Aufzaehlungspruefung, die genau das beweisen soll, ist ausgeschaltet, und „Definition of done: tests pass" ist nicht erfuellt. (Andere parallele Domaenen haengen an denselben beiden Tests; der Eintrag ist trotzdem nachzuziehen.)
  - Behebung: Die vier Dienstzeilen in `src/server/registry/dienste.ts` und die drei Manifestzeilen in `src/server/auth/route-manifest.ts` wirklich eintragen (der Bericht enthaelt den fertigen Text), danach beide Tests laufen lassen.
- **wichtig** · `/home/user/cse-platform/docs/DECISIONS.md` — O-710 bis O-714 sind im Code als `TODO(client, O-NN)` gesetzt, stehen aber in keiner Zeile von docs/DECISIONS.md. Die Merge-Wache `todo-client-nicht-im-register` schlaegt genau darauf an, `pnpm lint` (das die Wachen mitlaeuft) scheitert. CLAUDE.md verlangt fuer jede offen gelassene Geschaeftsregel Schritt 4 ausdruecklich („Record it in docs/DECISIONS.md under 'Open'"), und die Definition of done nennt „DECISIONS.md updated". Hoechste vergebene Nummer dort ist heute O-596.
  - Behebung: Die fuenf Zeilen aus `decisions_zeilen` unter „Offen" in docs/DECISIONS.md eintragen und die Wache erneut laufen lassen.
- **wichtig** · `/home/user/cse-platform/drizzle/0265_dienstplan_veroeffentlichung.sql` — Am Dateiende steht ein Block mit der Signatur `-- <<< generiert aus src/server/db/schema/rls.ts — nicht von Hand ändern (0265)`, aber `dienstplan_veroeffentlichung` kommt in `src/server/db/schema/rls.ts` nicht vor, und `0265` fehlt in `MIGRATIONS_DATEIEN` von scripts/generate-triggers.ts. Der Block ist also von Hand geschrieben und behauptet eine Herkunft, die er nicht hat; `tests/kern/loeschsperre.test.ts` — der Test, der genau dieses Auseinanderlaufen verhindern soll — laeuft an ihm vorbei und ist gruen. Wird der Registereintrag spaeter nachgetragen, nimmt `MIGRATIONEN` `0265` auf und `MIGRATIONS_DATEIEN['0265']!` ist `undefined`, also scheitert derselbe Test mit einem Pfadfehler statt mit einer Aussage.
  - Behebung: Die Zeile aus `registry_rls` in `KEIN_HARD_DELETE` (src/server/db/schema/rls.ts) eintragen, `'0265': join(WURZEL, 'drizzle/0265_dienstplan_veroeffentlichung.sql')` in `MIGRATIONS_DATEIEN` ergaenzen, dann `pnpm db:triggers` laufen lassen und `tests/kern/loeschsperre.test.ts` pruefen.
- **klein** · `/home/user/cse-platform/drizzle/0266_dienstplan_veroeffentlichung_definer.sql` — Der Definer loest den Empfaenger nur ueber `benutzer.person_id` auf und prueft nicht, ob dieser Mensch im veroeffentlichten Fenster ueberhaupt eingeteilt ist oder zum aktiven Mandanten gehoert — Titel, Text und Ziel kommen ebenfalls unveraendert vom Aufrufer. Damit ist die Funktion fuer jede Stelle in `cse_app` ein Primitiv „stelle beliebigem Konto beliebigen Text unter `dienstplan.plan_veroeffentlicht` zu". Heute ist die Route der einzige Aufrufer und liefert nur Personen aus der RLS-begrenzten Vorschau, und die Lesepolicy von `benachrichtigung` verhindert, dass ein Konto einer fremden Gesellschaft die Zeile sieht — der Schaden ist begrenzt, die Schranke fehlt aber.
  - Behebung: Im Definer den Empfaenger gegen `anstellung a where a.person_id = v_person and a.mandant_id = v_mandant` (oder direkt gegen eine Einteilung im Fenster) pruefen und einen unpassenden Eintrag wie „ohne Zugang" zaehlen statt zuzustellen.
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/zeiten/einwaende/[id]/page.tsx` — In Zeile 384 wird `e.korrektur.grundKategorie` roh ausgegeben, also `vergessen_auszustempeln`, `einwand_mitarbeiter`, `nachtrag_offline` usw. Die Seite fuehrt fuer jeden anderen Enum eine deutsche Karte (`ART_TEXT`, `STATUS_TEXT`, `EINTRAG_STATUS_TEXT`, `KORREKTUR_ART_TEXT`) — fuer `korrektur_grund` fehlt sie. Das ist genau der Fehlertyp, den der Konflikteingang schon einmal hatte und den Bausteine.tsx:43-55 ausdruecklich als Lehre notiert.
  - Behebung: Eine `KORREKTUR_GRUND_TEXT`-Karte mit genau diesen sechs Schluesseln anlegen und `KORREKTUR_GRUND_TEXT[...] ?? e.korrektur.grundKategorie` rendern. Gleiches gilt fuer die rohen Statuswoerter in quittung/page.tsx:212 und uebersteuern/page.tsx:229 (`({k.status})` bzw. `({befund.status})`).
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/dienstplan/serien/[id]/Bausteine.tsx` — `lesbareRegel` existiert jetzt zweimal im Baum, zeichengleich: hier (Zeile 24-43) und in `src/lib/datum/regeltext.ts`, aus dem die Listenseite `dienstplan/serien/page.tsx` sie importiert. Das ist die zweite Auslegung derselben RRULE, vor der der Kopfkommentar dieser Datei selbst warnt („zwei Fassungen derselben Aussage"). Die gemeinsame Datei ist waehrend derselben Welle entstanden; der Bericht fuehrt sie nicht.
  - Behebung: `lesbareRegel` aus Bausteine.tsx streichen und in serien/[id]/page.tsx aus `@/lib/datum/regeltext` importieren.
- **klein** · `/home/user/cse-platform/src/server/services/dienstplan/vorkommnisse.ts` — Die im Bericht vorgeschlagene O-714-Registerzeile beschreibt den Code falsch: sie sagt, `besetzungMitAusnahme` trage `ersatz_besetzung` „unveraendert durch, ohne sie gegen min_besetzung zu pruefen". Tatsaechlich SENKT die Funktion die Mindestbesetzung mit (`minBesetzung: Math.min(traeger.minBesetzung, ersatzBesetzung)`, Zeile 250) — und genau das ist der gewaehlte Platzhalterwert, um den O-714 kreist. Die Oberflaeche (serien/[id]/page.tsx, Absatz am Formularende) sagt es richtig; die Registerzeile, die in DECISIONS.md landen soll, sagt das Gegenteil.
  - Behebung: Den O-714-Text vor dem Eintragen korrigieren: „Heute gebaut: `ersatz_besetzung` ersetzt die Sollstaerke, und die Mindestbesetzung wird auf `min(min, ersatz)` MITGESENKT, damit keine Schicht entsteht, die per Konstruktion unterbesetzt gemeldet wird."

**Urteil:** Substanziell gebaut, nicht leer: alle sechs Seiten tragen echte Abfragen, die Dienste werden gerufen, und die Spaltennamen und Enum-Werte halten gegen echtes Postgres stand (einsatz_status, zuordnung_status, konflikt_status, einwand_status, turnus_ausnahme_art, arbzg_regel, korrektur_art/-grund — alle nachgemessen, keine Abweichung). `pnpm typecheck` ist in dieser Domaene sauber; die fuenf Kern-Testdateien laufen gruen (81 Faelle) und pruefen Verhalten, nicht Kosmetik; 0265/0266 sind angewandt, RLS mit FORCE, Definer `owner to cse_definer` und `revoke from public` nachgesehen; Zeiten durchgaengig `timestamptz` mit `at time zone 'Europe/Berlin'` erst zur Anzeige, Dauern als Differenz von Instants; keine Geldspalte, keine erfundene Farbe (die Wache meldet 0 Tailwind-Verstoesse), keine vorgetaeuschte Integration, keine entfernte Funktionalitaet in den Dateien dieser Domaene. ABER: ein blockierender Befund — die Veroeffentlichungsseite holt `dienstplan.lesen`, wirft es weg und laesst eine Sitzung ohne Leserecht einen Null-Beleg samt Protokolleintrag schreiben, ohne dass eine einzige Meldung hinausgeht. Dazu zwei stille Fehlschlaege (ueberholter ArbZG-Befund wird als „uebersteuert\" quittiert, obwohl der Definer null Zeilen aendert; Ausnahme mit dauer=1440 endet als 500) und vier offene Nacharbeiten: Dienstregister, Route-Manifest, rls.ts und die fuenf O-Zeilen in DECISIONS.md sind im Bericht zitiert, aber nicht im Baum — dadurch sind `tests/kern/routen.test.ts`, `tests/kern/portal-shell.test.ts` und die Wache `todo-client-nicht-im-register` rot, die Definition of done also nicht erfuellt.

## Vom Behebenden WIDERLEGT (Befund war falsch)

- Kein Befund war falsch. Eine Teilaussage in Befund 1 ist zu ENG: der Pruefer nennt nur `dienstplan.lesen`. Nachgemessen in w_dpz laeuft die Empfaengerabfrage von `vorschau()` zusaetzlich ueber `join objekt o` mit `objekt.t_mandant = mandant_id = app.aktiver_mandant() AND app.hat_recht('objekt.lesen', …)` — ohne `objekt.lesen` bleibt die Empfaengerliste leer, ohne dass jemand fehlt, und der Beleg behauptete dasselbe Falsche. Die Behebung deckt beide Rechte ab.
- Teilkorrektur an Befund 1: der Pruefer schlaegt vor, im Definer 0266 `dienstplan.lesen` mitzupruefen. Das ist nicht der richtige Ort — der Definer liest weder `einsatz` noch `objekt`, er bekommt fertige Zahlen. Die Schranke steht deshalb im Dienst (`veroeffentliche()`), wo die Zahlen ENTSTEHEN; sie wirkt damit fuer jeden Aufrufer und nicht nur fuer diesen einen Weg. Der Definer hat stattdessen die Schranke bekommen, die zu ihm gehoert (Befund 8: Empfaenger nur mit Anstellung in der aktiven Gesellschaft).
- Praezisierung an Befund 7: der Pruefer sagt, `tests/kern/loeschsperre.test.ts` laufe am Block vorbei und sei gruen — das stimmt. Er sagt weiter, ein spaeter nachgetragener Registereintrag lasse den Test mit einem PFADFEHLER scheitern. Auch das stimmt, aber es waere nicht das erste Problem gewesen: der handgeschriebene Block wich ohnehin um eine Leerzeile von `erzeuge('0265')` ab, `pnpm db:triggers --check` waere also unabhaengig davon rot geworden. Beides ist behoben.

## Nach der Behebung noch offen

- Die acht Registereintraege oben muessen zentral eingefuegt werden (5 Dienste, 3 Manifestzeilen, 1 RLS-Zeile, 5 DECISIONS-Zeilen, 1 D-Zeile). Bis dahin bleiben tests/kern/portal-shell.test.ts und tests/kern/routen.test.ts rot und `pnpm lint` scheitert an der Wache `todo-client-nicht-im-register` fuer O-710…O-714. Die Routen pruefen ihr Recht selbst — es fehlt keine Autorisierung, nur ihr Nachweis.
- Nach dem Eintragen der RLS-Zeile: `pnpm db:triggers` laufen lassen und `tests/kern/loeschsperre.test.ts` pruefen. Der 0265-Block ist so geschrieben, dass sich dabei nichts aendern darf; tut es das doch, weicht der eingetragene `grund`-Text vom oben angegebenen ab.
- O-710 bis O-714 bleiben inhaltlich offen — das ist Absicht (Regel 1), nicht ein Rest. Die Platzhalter sind in der Oberflaeche als „offen (O-NN)" sichtbar.
- tests/isolation/dienstplan-serie-ausnahme.test.ts sollte bei Gelegenheit um den 23505-Fall gegen echte Policies erweitert werden (zweite Ausnahme am selben Tag auf einer POSTENserie — dort ist das UK unbedingt, auch fuer `zusatz`). Der Kerntest deckt die Uebersetzung ab, aber nicht, dass Postgres genau diesen Code liefert. Nicht gelaufen, weil die Isolationsharness an der geteilten `cse_test` haengt.
- scripts/tmp-probe/kond.ts und rest.ts (untracked, waehrend dieses Laufs von einem parallelen Agenten entstanden) brechen `pnpm typecheck` mit vier TS2345. Nicht von mir, nicht angefasst — bitte zentral loeschen.

## NICHT gebaut, mit Grund

- Kein Einfrieren veroeffentlichter Schichten und kein Zustand am einsatz — das ist kein Rueckstand, sondern O-713 und K-17 (drizzle/0028_dienstplan.sql:60-64). Ein Wert 'veroeffentlicht' in einsatz_status wurde ausdruecklich NICHT ergaenzt.
- Kein Ableiten eines Personalbedarfs aus offenen Schichten. Die Kritik hat den Plan hier widerlegt: REC-01 ist ueber recruiting/dienst.ts bedarf() schon gefuettert. /dienstplan/offene-schichten liefert Zahlen und leitet nichts ab — was die drei bestehenden Definitionen von 'unbesetzt' (posten_unterbesetzung, Kachel schichten_unbesetzt, bedarf()) zusammenfuehrt, ist eine Entscheidung ausserhalb dieser Domaene.
- Keine Pruefung von ersatz_besetzung gegen min_besetzung (O-714). Die Ausnahme wird unveraendert durchgetragen und die offene Frage benannt.
- Kein Nachziehen von app.arbzg_befund_quittieren auf dienstplan.arbzg_uebersteuern. Der Definer bleibt bei dienstplan.arbzg_lesen; das staerkere Recht setzt die Route durch. Eine Migration, die den Definer aendert, haette eine bestehende Funktion aus einem fremden Nummernbereich ersetzt — sie gehoert als D-Zeile ins Register (siehe registry_sonstiges) und in einen eigenen Lauf.

## Notizen des Bauender

FORTSETZUNG NACH DEM LIMIT. Der Bau dieser Domaene lief im Workflow wf_250f2691-bb5 (Agent a1b4e5bed525c5e7f) und wurde vom Nutzungslimit mitten im Satz getroffen: unmittelbar nach dem Schreiben von tests/isolation/dienstplan-serie-ausnahme.test.ts, also VOR der StructuredOutput. Die Arbeit selbst war committet (99c7ad0, a4f9e28); verloren war nur das Ergebnis, also genau die Registereintraege in dieser Antwort. Ich habe den Stand aus dem Journal rekonstruiert, alles nachgeprueft und die drei Luecken geschlossen, die noch offen waren.

WAS ICH NACHGEPRUEFT HABE (nicht behauptet):
1. Migrationen: eigene Datenbank w_dpz frisch angelegt, Fensterschluessel gesetzt, `pnpm db:migrate` bis `Migrationen angewendet.` durchgelaufen. Zweimal, am Anfang und am Ende der Sitzung.
2. `npx tsc --noEmit` ueber den ganzen Baum: Exitcode 0, keine Fehler, auch nicht in fremden Pfaden.
3. `pnpm guards`: keine Wache meldet etwas in meinen Pfaden. Die einzige rote Wache ist `todo-client-nicht-im-register` mit 39 Treffern quer durch ALLE Domaenen (O-600 bis O-714) — sie wird gruen, sobald die O-Zeilen in docs/DECISIONS.md stehen, und die Datei pflegst Du zentral. Meine fuenf (O-710…O-714) sind darunter.
4. Kern-Tests meiner Domaene: 127 Faelle gruen. EINE fremde Datei ist rot: tests/kern/routen.test.ts meldet 36 API-Routen, die im zentralen route-manifest.ts fehlen — quer durch alle Domaenen (bau, crm, datenschutz, einstellungen, personal, stammdaten …). Drei davon sind meine und stehen in `registry_manifest`. Der Test wird mit dem Eintragen gruen; er ist kein Befund an meinem Code.
5. Abfragen von Hand gegen echtes Postgres: die Proof-Skripte der unterbrochenen Sitzung liegen im Kratzverzeichnis und ihre Ausgaben im Journal. Belegt sind dort: cse_app hat auf `benachrichtigung` kein INSERT; der Definer aus 0266 legt Vorgang und Meldung an und weist fremden Artschluessel, Nur-Lesen, fehlendes Recht, verkehrten und zu langen Zeitraum ab; `app.arbzg_befund_quittieren` traegt und setzt die Serveruhr, waehrend ein direktes `update` mit `permission denied for table arbeitszeit_verstoss` abbricht; ohne `dienstplan.arbzg_lesen` bleibt der Konflikt sichtbar und der Befund nicht; `turnus_ausnahme` verlangt `reinigung.schreiben` und traegt danach alle drei Arten; `posten_ausnahme` mit `ersatz_besetzung` wird geschrieben und gelesen.

DREI LUECKEN, DIE ICH IN DIESER SITZUNG GESCHLOSSEN HABE:
(a) DIE VERKNUEPFUNG, DIE ES NIE GAB. `zeiteintrag_korrektur.zeit_einwand_id` existiert seit Anlage der Tabelle mit eigenem Fremdschluessel `zk_einwand_fk` auf `zeit_einwand(mandant_id, id)`. `leseEinwand` LIEST sie und haengt daran den Abschnitt „Ist eine Korrektur gefolgt?" des neuen Einwandblatts. GESCHRIEBEN hat sie im ganzen src/-Baum niemand: `korrigiereZeiteintrag` fuehrte die Spalte nicht in seinem `insert`, und `KorrekturEingabe` hatte kein Feld dafuer. Das Blatt haette also fuer immer „anerkannt, aber keine Korrektur" gesagt — auch fuer die Korrektur, die genau diese Meldung beantwortet und eine Minute spaeter entsteht. Ein Bildschirm, der eine Warnung zeigt, die sich nicht abschalten laesst, lehrt seinen Leser, sie zu uebersehen. Geaendert: optionales `zeitEinwandId` in `KorrekturEingabe` und die Spalte im `insert`; `POST /api/zeit/korrektur` nimmt das Formularfeld `einwand`, prueft seine Form und faengt 23503 (schon vorher) als 404 (AUT-06); das Korrekturblatt liest `?einwand=`, laedt die Meldung mit `leseEinwand`, traegt sie als verstecktes Feld und BENENNT eine nicht einsehbare Kennung, statt sie still fallenzulassen; der Knopf auf dem Einwandblatt bringt `?einwand=` mit. Die ganze Kette lief gegen echtes Postgres durch (eigene geseedete Datenbank, Klon): gemeldet als die betroffene Person ueber `t_selbst_einreichen`, anerkannt von der Planung, korrigiert mit Verweis, `zeit_einwand_id` gesetzt, `leseEinwand` zeigt die Korrektur; eine erfundene Kennung wird von `zk_einwand_fk` mit 23503 abgewiesen.
(b) DER SEED UEBTE DIE HAELFTE DES BLATTS VON NICHTS. Der Seed legte KEINEN einzigen `zeit_einwand` an. Die eine Zeile, die eine Pruefung in einer geseedeten Datenbank fand, stammte aus einem Browserlauf (tests/e2e/mitarbeiter.spec.ts) und verschwindet mit dem naechsten Neuaufbau — die Kritik hat sie fuer Seed-Daten gehalten, sie ist keine. Der Einwandeingang und das Einwandblatt waren damit auf jedem Bildschirm leer, und zwar mit „0" und nicht mit einer Meldung. Neu in src/server/db/seed/zeit.ts: `seedEinwaende()` legt zwei an — einen OFFENEN (Eingang hat etwas zu tun) und einen, der den ganzen Weg geht (gemeldet, anerkannt, korrigiert, Korrektur verknuepft). Jeder Schritt mit der Sitzung, die ihn im Betrieb macht: die Meldung schreibt die PERSON, Entscheidung und Korrektur die PLANUNG (EMP-07, zk_nicht_selbst).
(c) DIE SCHWAECHERE MARKIERUNG WAR VON NICHTS AUSLOESBAR. Ueber alle 169 geseedeten Einsaetze galt `unter_min == unter_soll == 115`: kein einziger Fall, in dem die Mindestbesetzung steht und die Sollbesetzung fehlt. Genau das ist aber die schwaechere der beiden Markierungen auf /dienstplan/offene-schichten. Neu: `seedTeilbesetzteSchicht()` mit soll 3, min 1, eine Person eingeteilt, uebermorgen (also im Vorgabefenster der Seite). Nachgemessen nach dem Lauf: 170 Einsaetze, `nur_unter_soll` = 1 statt 0.
Beide Seed-Funktionen habe ich gegen einen KLON einer geseedeten Datenbank laufen lassen (nie gegen cse_dev oder cse_test) und dabei auch den zweiten Lauf geprueft: idempotent, 0 zusaetzliche Zeilen. `pnpm db:seed` habe ich nicht ausgefuehrt; verwendet habe ich ein kurzes tsx-Skript, das `seedZeit` gegen die eigene Datenbank ruft, und es danach wieder entfernt.

EIN GESTALTUNGSWERT MUSSTE ERSETZT WERDEN: `bg-surface-sunken` gibt es im Thema nicht — die Wache `tailwind-farbe` hat es sofort gemeldet. Genommen ist der naechstliegende vorhandene Wert `bg-surface-2` (FARBEN_BASIS['surface-2'], tailwind.config.ts). Sonst kein erfundener Wert, kein Hexcode, kein eigener Abstand.

WAS ICH NICHT AUSFUEHREN KONNTE: pnpm test:isolation (die globalSetup setzt cse_test zurueck), pnpm db:triggers, pnpm db:seed, pnpm build, pnpm test:e2e. Die vier Isolationsdateien dieser Domaene sind damit GESCHRIEBEN und von Hand gegen echtes Postgres nachvollzogen, aber nicht vom Laeufer ausgefuehrt — sie gehoeren in Schritt 5 der README, nach `pnpm db:triggers` (die Loeschsperre auf dienstplan_veroeffentlichung steht als generierter Block in 0265 UND braucht den rls.ts-Eintrag aus dieser Antwort, sonst zaehlt unveraenderbarkeit.test.ts Datenbank gegen Register in die falsche Richtung).

MEINE GEAENDERTEN DATEIEN IN DIESER SITZUNG (alles andere war schon committet): src/server/services/zeit/korrektur.ts, src/app/api/zeit/korrektur/route.ts, src/app/portal/[mandant]/zeiten/[id]/korrektur/page.tsx, src/app/portal/[mandant]/zeiten/einwaende/[id]/page.tsx, src/server/db/seed/zeit.ts, tests/kern/zeit-korrektur-weg.test.ts, tests/isolation/zeit-auftrag.test.ts. Nicht meine: src/server/services/stammdaten/belagsart.ts und die ungetrackten Dateien der Kundenportal- und Website-Agenten, die gleichzeitig im Baum arbeiten.

## Notizen des Behebender

Migration nach jeder Aenderung gegen eine frische eigene Datenbank geprueft: `drop database if exists w_dpz` / `create database w_dpz` / `alter database … set cse.fenster_schluessel = …` / `DATABASE_URL=…/w_dpz pnpm db:migrate` — laeuft bis 0304 durch (exit 0). In w_dpz nachgesehen: app.dienstplan_veroeffentlichung_anlegen enthaelt die anstellung-Schranke zweimal und gehoert cse_definer (K-01); trg_dienstplan_veroeffentlichung_kein_hard_delete und _kein_truncate stehen. Keine neuen Migrationsnummern angelegt — 0265 und 0266 sind die eigenen und bleiben es.

Gesperrte gemeinsame Dateien NICHT angefasst: registry/*, route-manifest.ts, schema/rls.ts, docs/DECISIONS.md, 04-SEITENKARTE.md, generierte Bloecke, package.json. rls.ts wurde fuer die Byte-Probe des 0265-Blocks kurz gepatcht und aus einer Kopie zurueckgestellt; `git diff` auf rls.ts UND auf src/server/db/triggers/no-hard-delete.sql ist leer. Angefasst wurde scripts/generate-triggers.ts (eine Zeile in MIGRATIONS_DATEIEN) — sie steht nicht auf der Sperrliste und ist die Voraussetzung dafuer, dass der rls.ts-Eintrag spaeter nicht mit einem Pfadfehler statt mit einer Aussage scheitert.

Keine erfundenen Gestaltungswerte: nur vorhandene Tokens (border-warning, text-warning, text-text-muted, bg-surface, rounded-lg, p-s4, p-s5, gap-s3) — border-warning stand schon in auth/mitarbeiter/*.

`pnpm typecheck` ist in meinen Dateien sauber. Die vier Fehler, die tsc jetzt meldet, liegen alle in scripts/tmp-probe/kond.ts und rest.ts — die Dateien sind um 09:04/09:05 waehrend dieses Laufs entstanden und stammen nicht von mir (paralleler Agent, untracked). Bitte zentral aufraeumen.

Nicht gelaufen: tests/isolation/dienstplan-serie-ausnahme.test.ts. Die Isolationsharness haengt an der geteilten `cse_test`, die parallele Agenten mitbenutzen; ein Lauf haette sie migrieren muessen. Der 23505-Pfad ist stattdessen im Kern mit einem Stub-Kontext geprueft — die Uebersetzung ist eine Aussage ueber den DIENST und faellt jetzt auch auf einem Rechner ohne Postgres.

Die beiden Aufzaehlungstests (tests/kern/portal-shell.test.ts, tests/kern/routen.test.ts) bleiben rot, bis die unten stehenden Eintraege zentral landen — die Differenz enthaelt auch die Eintraege anderer Domaenen. `npx tsx scripts/guards/run-all.ts` meldet fuer diese Domaene ausschliesslich `todo-client-nicht-im-register` fuer O-710…O-714; sie verschwinden mit den fuenf DECISIONS-Zeilen.

Ein Fund ueber die Befunde hinaus: `src/server/services/dienstplan/serienliste.ts` fehlt ebenfalls im Dienstregister (die Testdifferenz nennt es). Die Zeile steht unten mit.
