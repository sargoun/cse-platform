# Registereinträge: einstellungen

**Warteschlange, kein Archiv.** Diese Einträge sind **noch nicht** im Baum. Die
gemeinsamen Dateien pflegt EINE Hand, weil mehrere Agenten gleichzeitig arbeiten
und sich sonst in dieselbe Zeile schreiben. Ist ein Abschnitt eingetragen, wird
er hier gelöscht — solange er hier steht, fehlt er dort.

**Die Einträge des Behebungsschritts gelten.** Er lief zuletzt und hatte den
Auftrag, die vollständige aktuelle Liste zu liefern — auch das, was sich seit
dem Bauschritt geändert hat.

## Stand

- Bau: fertig
- Kritik: 18 Befunde
- Behebung: 18 behoben, 0 widerlegt, 5 offen

## Migrationen (gegen eine eigene Datenbank gefahren: True)

- drizzle/0200_mandant_identitaet.sql
- drizzle/0201_arbeitszeitmodell_tarif.sql
- drizzle/0202_migration_lauf.sql
- drizzle/0203_agent_richtlinie_recht.sql
- drizzle/0204_audit_kette.sql
- (Behebung) drizzle/0200_mandant_identitaet.sql (GEAENDERT: UPDATE-Spaltengrant gekuerzt auf die gepflegten Spalten; View-Kommentar sagt jetzt, welche Grenze sie ist und welche nicht)
- (Behebung) drizzle/0201_arbeitszeitmodell_tarif.sql (unveraendert)
- (Behebung) drizzle/0202_migration_lauf.sql (unveraendert)
- (Behebung) drizzle/0203_agent_richtlinie_recht.sql (unveraendert)
- (Behebung) drizzle/0204_audit_kette.sql (GEAENDERT: Spalte kern.audit_kette.start_hash + CHECK, Trigger trg_audit_kette_start_unveraenderlich / kern.audit_kette_start_unveraenderlich, fortschreiben setzt start_hash beim Anlegen, pruefen rechnet gegen start_hash statt gegen den jetzigen Vormonatskopf)
- (Behebung) drizzle/0205_arbeitszeitmodell_uebertrag_art.sql (NEU: check (uebertrag_art in ('offen')) + Spaltenkommentar — Spiegel von UEBERTRAG_ARTEN)
- (Behebung) drizzle/0206_audit_sensitiv_zweiter_faktor.sql (NEU: berechtigung.erfordert_2fa = true fuer system.audit_sensitiv_lesen + Spaltenkommentar; beantwortet O-624)

## Gebaute Adressen

- `/portal/[mandant]/einstellungen/agent-richtlinien` — fertig
  - Alle acht AKTIONEN als vollstaendiger Konfigurationsraum, auch die fuenf ohne Zeile ('nicht hinterlegt' = Freigabe noetig). Rechtekonflikt behoben: 0203 zieht t_richtlinie_lesen/-schreiben auf agent.richtlinie_verwalten ODER versand.* (vorher Tor offen, Datenbank leer). Angebot/Nachtrag/Behinderung sind im Code gesperrt; setzeRichtlinie weist eine gespeicherte Erlaubnis ab, ein kern-Test haelt IM_CODE_GESPERRT gegen gate(). 0203 gibt der Tabelle ihre erste Spur (Audit-, geaendert_am-, Loeschsperr-Trigger, geaendert_von) - sie trug KEINEN Trigger.
- `/portal/[mandant]/einstellungen/protokoll/export` — fertig
  - Zaehlen vor Herunterladen; Manifest (kanonisches JSON, SHA-256 im Kopf und Dateinamen) und ZIP (STORE, Nullzeitstempel, reproduzierbar). Nur ebene=mandant dieser Gesellschaft, Spalte ebene steht trotzdem in jeder Zeile. Vorher/Nachher ueber die neue mengenwertige app.audit_nutzlast_buendel: verlangt system.audit_exportieren UND system.audit_sensitiv_lesen und schreibt EINE Protokollzeile statt einer je Zeile (0139 schreibt eine je Zeile). Fehlt das zweite Recht, ist das Buendel redigiert und das Manifest sagt warum; Kopfzeile x-cse-redigiert. Kette: kern.audit_kette + kern.audit_kettenglied, fortgeschrieben beim Buendeln, nachgerechnet von app.audit_kette_pruefen.
- `/portal/[mandant]/einstellungen/vorlagen` — fertig
  - Vier Abschnitte an ihren echten Quellen, EIN Editor je Zeile: Behinderungsvorlage (pflegbar, Ablösung durch Archivierung), Mahntext (lesend, verlinkt auf /einstellungen/mahnwesen), Benachrichtigungsarten (im Code definiert, Pille 'nicht verbunden' mit O-36/O-501), Fusszeilen (lesend, verlinkt auf /einstellungen/identitaet). Der vierte Blocker der Kritik ist geloest, ohne die RLS zu weiten: die Seite fragt bau.lesen/bau.schreiben/mahnung.lesen ausdruecklich und sagt 'nicht sichtbar' statt 'nicht hinterlegt'; die POST-Route autorisiert system.einstellung_verwalten UND bau.schreiben.
- `/portal/[mandant]/einstellungen/import` — fertig
  - Ehrliche Zustandsseite aus dem PORT (nicht aus einer zweiten Liste) plus echte Lauf-Tabelle. MigrationImportPort mit einer ausgelieferten Umsetzung, die NichtVerbundenFehler wirft - kein Hochladefeld, keine simulierte Uebernahme. Die beiden feststehenden Regeln stehen auf der Seite: Zeiten als historische Zeilen mit quelle=migration ohne Serveruhr-Anspruch (Invariante 5), Rechnungen als Beleg, nie in Nummernkreis und nie in die Hashkette. Registerzeile 'altsystem' in registry/integrationen.ts ergaenzt, tests/kern/integrationen.test.ts entsprechend angepasst (die Liste war exakt festgeschrieben). Keine 'nicht verbunden'-Pille erfunden: StatusPill 'Inaktiv' + STAND_TEXT, genau wie /einstellungen/integrationen.
- `/portal/[mandant]/einstellungen/identitaet` — fertig
  - mandant_identitaet vollstaendig nach 01-KERN §6.2, samt mi_oeffentlich (prinzipalloser Renderpfad), Projektions-View mandant_identitaet_oeffentlich (ohne email_absender/email_signatur/domain), Alt-Text-CHECK, Anlage-Trigger und Nachtragung der vorhandenen Bereiche. Farbe ist ein TOKEN mit CHECK - kein Farbwaehler, das Farbfeld zeigt nur den Wert aus DESIGN §1. Kein Hochladeknopf, weil es keinen Marken-Bucket und keinen POST unter api/medien gibt; Alternativtexte sind trotzdem pflegbar. Kartentext/Profiltext werden hier NICHT gepflegt (unternehmensprofil je Sprache, D-82).
- `/portal/[mandant]/einstellungen/arbeitszeit` — teilweise
  - Seite, Tabellen, Dienst, Rechte, Leerzustand und beide Schreibwege sind vollstaendig. 'teilweise' betrifft genau EINE Regel: die Sollzeitherleitung bleibt nach Regel 1 ein Platzhalter (sollzeitregel='offen', SOLLSTUNDEN_OFFEN antwortet null, Seite zeigt 'offen (O-18)'). regelFuerModell ist die zweite Umsetzung der SollstundenRegel-Schnittstelle an genau einer Stelle; ein benannter, aber nicht gebauter Regelname traegt seinen Namen in die Meldung. Tarif: tv_mindestens_gesetz laesst keinen Wert unter § 4/§ 5 ArbZG zu, der Dienst weist ihn VORHER benannt ab - die ArbZG-Grenzen sind keine Einstellung (Seitenkarte Z. 1927-1932).

## Sonstiges

1) scripts/generate-triggers.ts — die vier Zeilen aus dem Bauschritt bleiben nötig (0200–0203). 0204, 0205 und 0206 brauchen KEINE: 0204 legt seine beiden Tabellen in `kern` an (NUR_UEBER_DEFINER, Sperren von Hand), 0205 setzt eine CHECK-Constraint, 0206 ein UPDATE auf `berechtigung`. `pnpm db:triggers --check` meldet „Trigger sind aktuell."

2) docs/architecture/04-SEITENKARTE.md — wie im Bauschritt: die Zeilen 1903, 1910, 1911, 1912, 1917 und 1918 dürfen ihren „not built"-Vermerk verlieren. Die dort genannte Korrektur (a) ist ERLEDIGT: der falsche Satz stand in src/app/portal/[mandant]/einstellungen/protokoll/page.tsx:14-17 und ist ersetzt. Punkt (b) steht unverändert: /portal/[mandant]/agenten/richtlinien und /[id] gehören zu agenten-freigaben-radar und sollten Weiterleitungen werden — ein anderer Agent legt dort inzwischen selbst Dateien an (src/app/portal/[mandant]/agenten/richtlinien/ ist untracked), ich habe nichts angefasst.

3) docs/DESIGN.md — weiterhin keine Ergänzung nötig, und weiterhin bewusst: keine neue Farbe, kein neuer Abstand, keine erfundene „nicht verbunden"-Pille. Neu verwendet habe ich nur vorhandene Klassen (text-text/-muted/-subtle, s1…s7, whitespace-pre-line, max-w-[48ch] analog zu den vorhandenen max-w-[72ch]). Die Tailwind-Wache meldet zu meinen Pfaden nichts.

4) src/server/registry/integrationen.ts — unverändert gegenüber dem Bauschritt (Zeile `altsystem`, offen: 'O-128') samt der angepassten Erwartung in tests/kern/integrationen.test.ts:38.

5) src/server/services/zeit/arbzg.ts — unverändert (die fünf exportierten Konstanten aus dem Bauschritt).

6) NEU: tests/isolation/harness.ts ist geändert und ist eine gemeinsame Datei — zwei rein additive Stellen. (a) `['kern','audit_kette']` in RESET_WURZELN (sonst ist audit-kette.test.ts reihenfolgeabhängig). (b) `Sitzung.aal?: 'aal1' | 'aal2'` plus `set_config('app.aal', …)`, Vorgabe `aal1` — für jede andere Testdatei ändert sich dadurch nichts (app.aal() fiel vorher schon auf 'aal1' zurück), aber ein Recht mit `berechtigung.erfordert_2fa` ist ohne diese Angabe nicht mehr prüfbar.

7) NEU: src/server/services/finanz/mahnung/stufen.ts ist geändert (fremde Domäne, rein additiv): `textbaustein` in `Roh`, `StufenZeile` und dem `select` von `mahnstufen()`. Kein vorhandener Aufrufer ändert sich; `npx tsc --noEmit` ist sauber.

## Zeilen für docs/DECISIONS.md, Abschnitt „Offen“ — ERLEDIGT (18.09.2026)

Eingetragen heisst gelöscht. Die 6 Zeilen dieser Domäne stehen in
`docs/DECISIONS.md` unter „Open — ask, do not guess“, Unterabschnitt
„Raised while building · die Domänenwelle (Routenbau)“ — O-620, O-621, O-622,
O-623, O-625 und O-626.

Die siebte Zeile war keine: **O-624 ist beantwortet** und steht jetzt als **D-603**
im Abschnitt der getroffenen Entscheidungen. Gegen `w_reg` nachgemessen:
`berechtigung.erfordert_2fa` ist für `system.audit_sensitiv_lesen` `true`, für jedes
andere Recht `false`. Hier ist nichts mehr offen.

## Befunde des Prüfers (18)

- **blockierend** · `/home/user/cse-platform/src/server/services/audit/buendel.ts` — Das Manifest des Beweismittelbuendels bindet die NUTZLAST nicht. `manifestWert` (Z. 302-348) traegt die Zeilen-Metadaten, `nutzlast: { redigiert, grund, anzahl }` und die Kettenzahlen — aber KEINEN SHA-256 von `protokoll.csv` und keinen von `nutzlast.csv`, und ueberhaupt keine Dateiliste. `packeAuditBuendel` (Z. 396-405) legt beide CSV-Dateien unveraendert daneben. Wer die Vorher/Nachher-Werte in `nutzlast.csv` im ZIP austauscht, aendert `x-cse-manifest-sha256` nicht, der Dateiname stimmt weiter, und die Pruefung des Manifests geht durch. Genau dieses Loch schliesst das Vorbild: `services/buchhaltung/pruefbuendel.ts` fuehrt je Datei `sha256` im Manifest (Z. 173, 180) und `packePruefbuendel` rechnet die Bytes vor dem Packen dagegen (Z. 204-205). Fuer ein Beweismittel nach SEC-A9/LEG-01 ist das der Unterschied zwischen einer Signatur und einer Behauptung.
  - Behebung: In `manifestWert` ein `dateien: [{ pfad, sha256, groesseBytes }]` fuer `protokoll.csv` und — wenn nicht redigiert — `nutzlast.csv` aufnehmen (die CSV-Bytes dafuer VOR dem Manifest bilden), und in `packeAuditBuendel` wie `packePruefbuendel` jeden Eintrag gegen seinen Manifesteintrag nachrechnen und bei Abweichung werfen. Danach `tests/kern/audit-buendel.test.ts` um den Fall erweitern: eine veraenderte Nutzlastdatei muss auffallen.
- **wichtig** · `/home/user/cse-platform/drizzle/0204_audit_kette.sql` — `app.audit_kette_pruefen` und `app.audit_kette_fortschreiben` starten mit VERSCHIEDENEN Vorgaengerhashes und melden deshalb einen Bruch, wo keiner ist. `fortschreiben` nimmt fuer eine bestehende Kette `v_prev := v_kette.letzter_hash` (Z. 259), `pruefen` nimmt immer den JETZIGEN `letzter_hash` der Vorgaengerkette (Z. 347-348). Beide stimmen nur ueberein, solange die Glieder einer Monatskette bei `ketten_nr = 1` beginnen und der Kopf des Vormonats sich nie mehr bewegt. Beides ist nicht garantiert: `audit_log.erstellt_am` ist `now()`, also die Startzeit der Transaktion — eine Transaktion, die am 31.08. 23:59:50 beginnt und am 01.09. committet, legt AUGUST-Zeilen ab, nachdem die Septemberkette schon entstanden ist. `fortschreiben` haengt sie korrekt an die Augustkette und aendert deren `letzter_hash`; `pruefen('audit_log_2026_09')` rechnet danach gegen den NEUEN Augusthash und setzt `gebrochen_bei` auf das erste Septemberglied. Ein falscher Manipulationsalarm auf der Beweiskette — und nach O-623/O-357 soll genau diese Funktion ein Nachtlauf werden.
  - Behebung: `pruefen` den Startwert aus dem ERSTEN Glied der Kette nehmen (`select vorheriger_hash from kern.audit_kettenglied where kette_id = v_kette.id order by ketten_nr limit 1`) und nur die Kette in sich nachrechnen — oder die Monatsketten ueber einen unveraenderlichen `start_hash` auf `kern.audit_kette` verbinden, der beim Anlegen gesetzt und danach nie mehr veraendert wird. Zusaetzlich ein Testfall, der eine Zeile mit einem Zeitstempel des Vormonats nachtraegt und danach `pruefen` auf dem Folgemonat `bruch_bei = null` erwarten laesst.
- **wichtig** · `/home/user/cse-platform/tests/isolation/harness.ts` — `kern.audit_kette` wird von `seed()` nicht zurueckgesetzt, `kern.audit_kettenglied` schon — damit ist `tests/isolation/audit-kette.test.ts` reihenfolgeabhaengig. `RESET_WURZELN` (Z. 201-204) laeuft die Fremdschluessel VON den Wurzeln aus abwaerts; `audit_kettenglied` haengt an `audit_log` und wird geleert, `audit_kette` ist Elternteil und wird nie erreicht. Nach dem ersten Test steht der Kettenkopf auf `letzte_nr = N` mit `letzter_hash = H`, im naechsten Test beginnt `fortschreiben` bei `N+1` mit `vorheriger_hash = H`, waehrend `pruefen` mit `NULL` startet — der Test „eine unveraenderte Kette ist geschlossen" muss danach einen Bruch sehen. (Ich konnte es nicht laufen lassen: `pnpm test:isolation` scheitert schon im global-setup, weil der Seed an einem Syntaxfehler in `src/server/services/angebot/index.ts:547` abbricht — fremde Domaene.)
  - Behebung: `['kern','audit_kette']` in `RESET_WURZELN` aufnehmen (oder im Testfile in `beforeEach` unter `session_replication_role = replica` leeren). Dieselbe Behebung deckt den Befund zu 0204 nicht ab — beide sind noetig.
- **wichtig** · `/home/user/cse-platform/src/server/services/zeit/arbeitszeitmodell.ts` — Erfundene Geschaeftsregel ohne Entscheidung: `setzeArbeitszeitmodell` weist JEDE Fassung mit `gueltig_ab` vor heute ab (Z. 278-283, dazu `min={heute}` auf der Seite, Z. 310). Diese Regel hat niemand entschieden, sie steht in keinem `TODO(client, O-NN)` und in keiner Registerzeile, und sie ist nicht Hausstil: das genannte Vorbild `mahnstufe` kennt sie nicht (`bestaetigeStufe` prueft nur `e.gueltigAb > laufend.gueltig_ab`, stufen.ts:155), und die Schwesterfunktion `setzeTarifvereinbarung` in derselben Datei hat sie ebenfalls nicht — Tarife duerfen also rueckwirkend gelten, Modelle nicht. Folge in der Praxis: ein Mandant, der bei der Uebernahme das seit 2020 geltende Modell hinterlegen will (genau der Fall, den `/einstellungen/import` vorbereitet), kann es nicht — und die Sollstundenrechnung fuer die Vergangenheit bleibt ohne Grundlage, obwohl der Kommentar § 17 MiLoG als Grund anfuehrt.
  - Behebung: Entweder die Sperre streichen und die Ablösung wie bei `mahnstufe` allein ueber `azm_kein_ueberlapp` regeln, oder die Regel als offene Frage fuehren: `// TODO(client, O-18): Darf eine Fassung rueckwirkend hinterlegt werden (Uebernahme von Altbestaenden), und ab welchem Datum ist der Monat gesperrt?` plus Zeile unter „Open" in `docs/DECISIONS.md`. In jedem Fall beide Schreibwege derselben Datei gleich behandeln.
- **wichtig** · `/home/user/cse-platform/src/app/api/einstellungen/arbeitszeit/route.ts` — `uebertrag_art` ist ein offener Konfigurationsraum ohne jede Schranke: die Route nimmt `text('uebertragArt') ?? 'offen'` ungeprueft (Z. 88), die Seite bietet dafuer ein FREIES Textfeld (arbeitszeit/page.tsx:296-300), und `arbeitszeitmodell.uebertrag_art` traegt in 0201 keinen CHECK (nur `not null default 'offen'`). Ein Tippfehler („verfalen") wird gespeichert, und die Tabelle zeigt ihn danach als gueltige Uebertragsregel an (page.tsx:167-171 rendert jeden Wert ausser 'offen' als hinterlegte Regel statt als „offen (O-18)"). Das ist das Gegenteil dessen, was CLAUDE.md fuer eine offene Regel verlangt — ein klar benannter Platzhalter hinter einer Schnittstelle, nicht ein Freitextfeld mit Lohnfolge.
  - Behebung: Eine geschlossene Menge einfuehren — `UEBERTRAG_ARTEN = ['offen'] as const` im Dienst (solange O-18 offen ist genau EIN Wert), die Route dagegen pruefen und werfen, die Seite als `<select>` daraus rendern, und in einer Migration `check (uebertrag_art in (…))` nachziehen. Erweitert wird die Liste, wenn O-18 beantwortet ist.
- **wichtig** · `/home/user/cse-platform/src/app/api/einstellungen/arbeitszeit/route.ts` — `ganz()` (Z. 57-62) verschluckt jede unbrauchbare Eingabe als `null` statt sie abzuweisen: `Number.parseInt` auf „dreissig", „30 min" oder „-5" ergibt `NaN` bzw. eine negative Zahl, und die Funktion antwortet in allen Faellen `null`. Fuer `pause6`/`pause9`/`ruhezeit` heisst `null` aber ausdruecklich „der Tarif sagt dazu nichts" (arbeitszeit/page.tsx:340) — der Dienst prueft es dann gegen nichts, speichert die Zeile, und die Route meldet „Die Tarifregel fuer bau gilt ab …". Eine strengere Pausenregel, die der Nutzer eingegeben hat, ist damit lautlos verschwunden, und die Seite zeigt „nicht hinterlegt (O-50)" — dasselbe Bild wie bei „nie eingetragen". Gleiches gilt fuer `uebertragGrenze` und `verfallMonate`.
  - Behebung: `ganz()` zwischen „Feld leer" und „Feld unlesbar" unterscheiden: bei leerem Feld `null`, bei nicht parsebarem oder negativem Wert einen `ArbeitszeitFehler('ungueltig', …)` mit dem Feldnamen werfen (das Formular bekommt ohnehin eine Seite mit dem Satz zurueck).
- **wichtig** · `/home/user/cse-platform/src/app/portal/[mandant]/einstellungen/vorlagen/page.tsx` — Der Abschnitt heisst „Mahntext je Stufe" (Z. 157) und zeigt keinen Mahntext. Die Tabelle fuehrt Stufe, Bezeichnung, Gueltig ab, Zustand und „Gepflegt unter" — `mahnstufe.textbaustein` kommt nicht vor, weil `StufenZeile` die Spalte nicht traegt und `mahnstufen()` sie nicht selektiert (stufen.ts:35-48, 89-97). Damit fehlt genau das, was die Seite verspricht und was der Plan verlangt („je Stufe der textbaustein aus mahnstufe, schreibgeschuetzt"): wer alle Vorlagen an einer Stelle sehen will, sieht hier eine Stufenliste, die er unter /einstellungen/mahnwesen schon hat.
  - Behebung: `textbaustein` in `Roh`/`StufenZeile`/das `select` von `mahnstufen()` aufnehmen (rein additiv) und in der Tabelle als eigene, schreibgeschuetzte Spalte zeigen — oder, falls das nicht gewollt ist, die Ueberschrift auf „Mahnstufen" aendern und den Satz darunter entsprechend.
- **wichtig** · `/home/user/cse-platform/src/app/api/einstellungen/vorlagen/route.ts` — Der zentrale Mechanismus dieser Route ist ungetestet. Die doppelte Autorisierung (`system.einstellung_verwalten` UND `bau.schreiben`, Z. 66-69) und die dazugehoerige Lesepruefung im Dienst (`ladeVorlagenuebersicht` fragt `bau.lesen`/`bau.schreiben`/`mahnung.lesen`, vorlagen.ts:88-99) sind die Behebung des vierten Blockers — also eine Aenderung an Invariante 3, und die verlangt nach CLAUDE.md einen Test. Es gibt keinen: `tests/kern/einstellungen-vorlagen.test.ts` prueft ausschliesslich Platzhalter (`ERLAUBTE_PLATZHALTER`, `platzhalterIn`, `setzeVorlage`), und eine `tests/isolation/einstellungen-vorlagen*.test.ts` existiert nicht. Nicht geprueft ist damit: sieht eine Sitzung mit dem Recht der Seite und ohne `bau.lesen` „nicht sichtbar" statt „nicht hinterlegt", und wird ihre Schreibung mit 404 abgewiesen statt von der RLS still verworfen.
  - Behebung: Eine `tests/isolation/einstellungen-vorlagen.test.ts` nach dem Muster von `agent-richtlinie-recht.test.ts`: (a) Sitzung mit `system.einstellung_verwalten` ohne `bau.lesen` -> `ladeVorlagenuebersicht().rechte.bauLesen === false` und leere Liste; (b) mit `bau.lesen` -> Zeilen; (c) `setzeBehinderungsvorlage` ohne `bau.schreiben` -> abgewiesen; (d) mit beiden Rechten -> alte Fassung archiviert, neue mit `ist_platzhalter = false`.
- **wichtig** · `/home/user/cse-platform/src/app/api/einstellungen/protokoll/export/route.ts` — Die erste Massenausgabe der Vorher/Nachher-Werte laeuft ohne zweiten Faktor. 03-AUTH-BERECHTIGUNGEN Z. 2342 fuehrt `system.audit_sensitiv_lesen` ausdruecklich mit `erfordert_2fa`; in der lebenden Datenbank steht `erfordert_2fa = false` — und zwar fuer JEDES Recht (`select schluessel from berechtigung where erfordert_2fa` liefert 0 Zeilen). `app.hat_recht_fuer` wuerde die Pflicht durchsetzen, sie ist nur nicht gesetzt, und `authorize` erzwingt sie nur bei `erfordert2fa: true` an der Ressource — was diese Route nicht uebergibt (Z. 63-66). Ergebnis: eine `aal1`-Sitzung laedt `nutzlast.csv` mit Loehnen, Geburtsdaten und gesundheitsnahen Abwesenheitsgruenden herunter. O-624 stellt das als offene Frage; die Antwort steht aber schon in der Spezifikation.
  - Behebung: Kurzfristig in der Route den zweiten Faktor fuer den sensiblen Teil verlangen — vor `erstelleAuditBuendel` ein `authorize(sitzung, { recht: 'system.audit_sensitiv_lesen', erfordert2fa: true }, pruefer)` in einem `try`, dessen `ZweiterFaktorFehler` zum redigierten Buendel fuehrt (und dessen `NichtGefundenFehler` wie heute). Dauerhaft `berechtigung.erfordert_2fa = true` fuer `system.audit_sensitiv_lesen` setzen (Katalogzeile/Migration) — dann faellt O-624 weg, statt als Kundenfrage gefuehrt zu werden.
- **wichtig** · `/home/user/cse-platform/src/server/db/seed/index.ts` — Vier der fuenf neuen Tabellen werden vom Seed nicht beruehrt: `arbeitszeitmodell`, `tarifvereinbarung`, `migration_lauf`, `migration_zeile` — und `mandant_identitaet` nur durch den Anlagetrigger, also mit `kurzname` und Token und sonst ausschliesslich NULL. Damit zeigt `/einstellungen/arbeitszeit` in der Demo nur den Leerzustand, `/einstellungen/identitaet` fuenf Mal „nicht hinterlegt", und die Anzeigepfade fuer ausgefuellte Zeilen (Pillen, Ablösung, Fusszeilen, Kettenzahlen) laufen nie. CLAUDE.md fuehrt „seed data exercises it" in der Definition of done; `agent_richtlinie` ist der einzige Fall, den der Seed abdeckt (Z. 1109-1118, drei Aktionen je Bereich, alle fail-closed).
  - Behebung: Im Seed je Bereich (a) eine Identitaetszeile mit Claim, den drei Fusszeilen und E-Mail-Absender aktualisieren — `oeffentlich_sichtbar` nur mit Alt-Texten, damit `mi_alt_text` mitgeprueft wird; (b) ein `arbeitszeitmodell` als PLATZHALTER (`ist_platzhalter = true`, `sollzeitregel = 'offen'`) samt einer abgeloesten Vorfassung, damit die Ablösung sichtbar ist; (c) eine `tarifvereinbarung` fuer genau EIN Gewerk, strenger als das Gesetz und als Platzhalter markiert; (d) `migration_lauf`/`migration_zeile` NICHT seeden — ein Lauf ohne Parser wuerde eine Uebernahme vortaeuschen, und die leere Liste ist dort die richtige Aussage.
- **klein** · `/home/user/cse-platform/drizzle/0200_mandant_identitaet.sql` — Der Spaltengrant widerspricht den Zusagen, die dieselbe Migration und der Dienst geben. `grant update (…)` (Z. 217-225) umfasst `identitaets_token`, `domain`, `kurzbeschreibung`, `beschreibung` und `platzhalter_medien` — also genau die fuenf Felder, zu denen `identitaet.ts` schreibt „niemand kann ihn setzen" (Farbe), „O-08 ist offen" (Domain), „maßgeblich ist unternehmensprofil" (Profiltexte) und „keine Einstellung" (Platzhaltermarke). Heute schreibt sie kein Pfad; der Grant laesst den naechsten Pfad sie schreiben, ohne dass jemand die Entscheidung noch einmal liest.
  - Behebung: Den Grant auf die pflegbaren Spalten kuerzen: `kurzname, logo_alt, avatar_alt, cover_alt, claim, brief_fuss, rechnung_fuss, angebot_fuss, email_absender, email_signatur, oeffentlich_sichtbar, geaendert_am, geaendert_von`. Die uebrigen kommen mit dem Pfad, der sie braucht (Bildupload O-12/O-13, Domain O-08).
- **klein** · `/home/user/cse-platform/drizzle/0200_mandant_identitaet.sql` — Die Projektions-View ist keine Grenze, sondern eine Konvention. `mi_oeffentlich` (Z. 197-200) erlaubt dem prinzipallosen Pfad SELECT auf die TABELLE, und `grant select on mandant_identitaet to cse_app` (Z. 217) gilt fuer alle Spalten — `email_absender`, `email_signatur` und `domain` eingeschlossen. Der Kommentar der View (Z. 231-238) liest sich so, als hielte sie diese drei zurueck; sie tut es nur fuer den, der die View benutzt.
  - Behebung: `grant select on mandant_identitaet to cse_app` durch einen Spaltengrant ohne `email_absender, email_signatur, domain` ersetzen und diese drei nur der internen Leseabfrage geben (eigener Grant fuer eine Definer-Funktion oder eine zweite Rolle) — oder im Isolationstest zusaetzlich festhalten, dass der prinzipallose Pfad die drei Spalten der TABELLE nicht lesen kann.
- **klein** · `/home/user/cse-platform/src/server/services/einstellung/vorlagen.ts` — Der Kopfkommentar verweist auf einen Test, den es nicht gibt: „`tests/kern/vorlagen.test.ts` haelt die Liste gegen `Vorlagenwerte`" (Z. 84). Die Datei heisst `tests/kern/einstellungen-vorlagen.test.ts`. Der Test existiert und prueft genau das — der Verweis fuehrt nur ins Leere, und wer ihn nachschlaegt, haelt die Zusicherung fuer unbelegt.
  - Behebung: Den Dateinamen im Kommentar korrigieren.
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/einstellungen/arbeitszeit/page.tsx` — Die Seite behauptet eine Tatsache ueber den Datenbestand, die sie nicht abfragt: „Der Bestand traegt in `anstellung_kondition.arbeitszeitmodell` ausschliesslich „unbekannt"" (Z. 126-128). Der Satz ist fest verdrahtet und erscheint immer, wenn kein Modell hinterlegt ist. `anstellung_kondition` existiert inzwischen (0190-0194) und enthaelt derzeit null Zeilen — die Aussage ist also schon heute falsch und wird es bei jedem gepflegten Bestand bleiben, ohne dass sich der Satz aendert.
  - Behebung: Entweder die Zahl mitlesen (`select count(*) filter (where arbeitszeitmodell = 'unbekannt'), count(*) from anstellung_kondition`) und den Satz daraus bilden, oder ihn auf die nachpruefbare Aussage kuerzen: „Solange hier kein Modell steht, gibt es in den Anstellungskonditionen nichts zu waehlen."
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/einstellungen/agent-richtlinien/page.tsx` — Zwei kleine Loecher im Editor. (1) Die Spalte „Begruendung" zeigt `z.grund ?? z.begruendung` (Z. 158-163) — `grund` ist der statische Gesetzestext aus `AKTION_GRUND`, also verdeckt er fuer Angebot, Nachtrag und Behinderungsanzeige die vom Menschen gespeicherte `begruendung` dauerhaft. (2) Die drei im Code gesperrten Aktionen sind aus dem Auswahlfeld gefiltert (`waehlbar`, Z. 68 / :185-189); damit gibt es keinen Weg, fuer sie `ist_aktiv` oder eine Begruendung zu setzen, obwohl `setzeRichtlinie` das ausdruecklich erlaubt (es weist nur `autoErlaubt` ab, richtlinie.ts:263-269). Heute ohne Folge, weil der Seed fuer diese drei keine Zeile legt.
  - Behebung: Beide Texte nebeneinander zeigen (statischer Grund als Fussnote, gespeicherte Begruendung in der Spalte), und die gesperrten Aktionen im Auswahlfeld belassen, aber das Kaestchen „Darf ohne menschliche Freigabe hinausgehen" fuer sie deaktivieren — dann deckt sich die Oberflaeche mit dem, was der Dienst zulaesst.
- **klein** · `/home/user/cse-platform/src/server/services/zeit/arbeitszeitmodell.ts` — Der zweite Zweig von `regelFuerModell` ist unerreichbar. Er soll einen „benannten, aber nicht gebauten" Regelnamen tragen (Z. 213-221), aber `sollzeitregel` laesst sich nirgends auf etwas anderes als `'offen'` setzen: der INSERT in `setzeArbeitszeitmodell` schreibt den Wert hart (Z. 305: `'offen'`), die Spalte hat `default 'offen'`, und `geaendert`/UPDATE fasst sie nicht an — der Spaltengrant erlaubt es auch nicht. Der kern-Test deckt den Zweig ab, kein Codepfad kann ihn erzeugen.
  - Behebung: Entweder `sollzeitregel` als Eingabe durchlassen, sobald es einen zweiten Namen gibt (dann gehoert eine geschlossene Menge dazu, wie bei `uebertrag_art`), oder den Zweig mit einem Satz markieren, dass er die Spalte gegen einen kuenftigen Migrationswert absichert — damit niemand ihn fuer eine benutzbare Einstellung haelt.
- **klein** · `/home/user/cse-platform/src/app/api/einstellungen/protokoll/export/route.ts` — Der einzige schreibende der sechs Handler prueft nicht als schreibend. `authorize(sitzung, { recht: 'system.audit_exportieren' }, …)` (Z. 63-66) laesst `schreibend: true` weg, obwohl der Aufruf drei Dinge schreibt (Kettenglieder, die Protokollzeile der Nutzlast, die Protokollzeile des Abrufs). Die uebrigen fuenf Routen dieser Domaene setzen es. Praktisch faengt `withTenant` die Gruppenansicht ohnehin ab; die Invariante-10-Pruefung von `authorize` laeuft hier aber nicht, und der `app.ist_readonly()`-Riegel in `app.audit_kette_fortschreiben` kann von diesem Aufrufer nie greifen, weil `withTenant` `app.readonly` immer auf `off` bindet.
  - Behebung: `schreibend: true` ergaenzen.
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/einstellungen/protokoll/page.tsx` — Der Kopfkommentar der vorhandenen Leseseite sagt jetzt etwas Falsches ueber die neue Exportseite: „sie stehen dem Export mit `system.audit_exportieren` offen" (Z. 14-16). Die gebaute Ausgabe verlangt fuer Vorher/Nachher zusaetzlich `system.audit_sensitiv_lesen` und liefert sonst ein redigiertes Buendel. Der Bericht nennt die Stelle und hat sie bewusst nicht angefasst; solange sie steht, widersprechen sich zwei Dateien derselben Domaene.
  - Behebung: Den Satz ersetzen: „… sie stehen dem Export nur mit `system.audit_sensitiv_lesen` offen; ohne dieses Recht ist das Buendel redigiert (Einstellungen › Protokoll exportieren)."

**Urteil:** Substanziell gebaut, kein Potemkin-Dorf: alle sechs Seiten lesen echte Zeilen ueber ihren Dienst in einer gebundenen `withTenant`-Transaktion, kein Dienst wird umgangen, und die fuenf Migrationen 0200-0204 laufen auf einer frisch angelegten Datenbank zusammen mit allen anderen Domaenen bis „Migrationen angewendet." durch (gepruefte Neuanlage `w_einst_pruef`). Ich habe jede neue Abfrage gegen echtes Postgres gestellt (`explain` auf allen fuenf Leseabfragen: OK) — keine falsche Spalte, kein falscher Enumwert; `ml_status`, `ml_quelle`, `tv_gewerk`, `mi_token` deckten sich mit dem Code. Jede neue Tabelle traegt `mandant_id`, `enable`+`force row level security`, Policies je Kommando, Loeschsperre, `geaendert_am`- und (bis auf die anfuegende `migration_zeile`) Audit-Trigger; die beiden `kern`-Tabellen haben keinen `cse_app`-Grant, und alle fuenf neuen Definer-Funktionen sind mit `revoke execute … from public` abgedichtet. Geld bleibt `bigint`/Cent, alle Zeitstempel sind `timestamptz`, das Tagesfenster des Exports wird in `app.loese_ortszeit` aufgeloest und nicht in Node. Fehlendes Recht fuehrt ueber `authorize` auf 404. Kein erfundener Gestaltungswert (jede Klasse und jedes willkuerliche Tailwind-Mass ist im Baum etabliert, 27-238 Fundstellen), keine vorgetaeuschte Integration (`NichtVerbundenPort` wirft, kein Hochladefeld), und alle vier kern-Testdateien laufen gruen (47 Tests). Die vier Isolationstestdateien existieren und pruefen echtes Verhalten, ich konnte sie aber NICHT laufen lassen: `pnpm test:isolation` scheitert bereits im global-setup, weil der Seed an einem Syntaxfehler in `src/server/services/angebot/index.ts:547` abbricht — fremde Domaene, nicht diese.

Einen blockierenden Befund gibt es trotzdem, und er sitzt genau dort, wo diese Domaene ihren Wert hat: das Manifest des Beweismittelbuendels bindet die Nutzlastdatei nicht durch einen Hash, anders als das Vorbild `pruefbuendel.ts`. Ein manipuliertes `nutzlast.csv` besteht die Pruefung des Manifests. Daneben zwei Befunde an der Hashkette selbst (asymmetrischer Startwert in `pruefen` gegen `fortschreiben`, was einen falschen Bruchalarm erzeugt, sobald eine Zeile mit dem Zeitstempel des Vormonats nachkommt; und `kern.audit_kette` faellt aus dem Reset der Isolationssuite, womit deren eigener Test reihenfolgeabhaengig wird). Eine erfundene Geschaeftsregel habe ich gefunden — „rueckwirkend gibt es keine Fassung" in `setzeArbeitszeitmodell`, ohne `TODO(client)`, ohne Registerzeile, nicht Hausstil (mahnstufe kennt sie nicht) und in derselben Datei inkonsistent (der Tarif darf rueckwirkend). `uebertrag_art` ist ein Freitextfeld ohne CHECK, `ganz()` verschluckt unlesbare und negative Eingaben als „der Tarif sagt dazu nichts", der Abschnitt „Mahntext je Stufe" zeigt keinen Mahntext, und die Behebung des vierten Kritikpunkts (zwei Rechte fuer /vorlagen) hat keinen einzigen Test. Der Bericht selbst ist ungewoehnlich ehrlich — „teilweise" fuer /arbeitszeit ist richtig eingeordnet, O-620 bis O-623 sind echte offene Fragen mit nachrechenbarer Begruendung; nur O-624 ist keine Kundenfrage, sondern eine Implementierungsluecke: 03-AUTH-BERECHTIGUNGEN Z. 2342 antwortet schon, und bis dahin verlaesst ein Loehnexport das Haus ohne zweiten Faktor. Die zentral gepflegten Register (`route-manifest.ts`, `registry/dienste.ts`, `schema/rls.ts`, `scripts/generate-triggers.ts`, DECISIONS O-620..624, 04-SEITENKARTE) sind wie angekuendigt noch leer — ohne sie fallen `routen`-, `rls`- und `db:triggers --check`-Wachen, das ist die naechste Handbewegung und kein Befund.

## Nach der Behebung noch offen

- FREMDE DOMAENE, BLOCKIERT MEINE TESTLAEUFE: `pnpm db:seed` bricht im global-setup der Isolationssuite ab — `new row violates row-level security policy for table "leistungskatalog_position"` (RLS `t_mandant` verlangt `katalog.schreiben`). Das kommt aus der in Arbeit befindlichen Domaene leistungskatalog (drizzle/0298_leistungskatalog_status.sql und src/app/portal/[mandant]/leistungskatalog/ sind untracked). Der vom Pruefer genannte Syntaxfehler in angebot/index.ts:547 ist inzwischen weg. Folge: `tests/isolation/*` konnte ich NICHT ausfuehren. Jede neue und jede geaenderte Zusicherung habe ich stattdessen von Hand gegen eine echte, migrierte und geseedete w_einst gefahren (Details in notizen).
- TYPECHECK-FEHLER AUS FREMDER DOMAENE (nicht von mir, nicht angefasst): src/app/api/personal/personen/[id]/stammdaten/route.ts:46 — `exactOptionalPropertyTypes`, `geburtsdatum: string | null | undefined` gegen `StammdatenEingabe`. Gehoert zur parallel laufenden Domaene personal. Ohne diese eine Stelle ist `npx tsc --noEmit` ueber das ganze Repository sauber.
- REST DES VIEW-BEFUNDS (klein, bewusst so gelassen): `grant select on mandant_identitaet to cse_app` bleibt ein Tabellen-Grant ueber alle Spalten. Der prinzipallose Renderpfad koennte `email_absender`, `email_signatur` und `domain` also weiterhin aus der TABELLE lesen, wenn jemand einen neuen Pfad an der View vorbei baut. Eine echte Spaltengrenze bedeutet: Tabellen-Grant entziehen, Spaltengrant ohne die drei, und den internen Editor (`ladeIdentitaet` UND den Gruppenlesepfad) auf eine Definer-Funktion mit `system.identitaet_verwalten` legen. Das sind zwei Lesepfade in einer Domaene, die ich nicht mit Tests abdecken kann, solange die Isolationssuite blockiert ist — deshalb steht der Stand jetzt ausdruecklich im Migrationskommentar UND in einem Isolationsfall, statt behauptet zu werden. Vorschlag: eigene kleine Migration, wenn der Bildupload (O-12/O-13) ohnehin an die Tabelle geht.
- Das Haekchen „Darf ohne menschliche Freigabe hinausgehen" laesst sich fuer die drei im Code gesperrten Aktionen nicht clientseitig deaktivieren (kein JS in diesem Formular). Der Dienst weist die Kombination benannt ab, und zwei Saetze auf der Seite sagen es vorher — ein echtes `disabled` bekaeme die Seite erst mit einem Client-Baustein.
- `app.audit_kette_fortschreiben` laeuft weiterhin nur beim Bilden eines Buendels — der Nachtlauf ist O-623/O-357 und bleibt offen.

## NICHT gebaut, mit Grund

- Der eigentliche Altsystem-PARSER (MigrationImportPort mit echtem Format, 07-INTEGRATIONEN §25.3, PR 89). Grund: O-128 nennt kein Exportformat. Gebaut ist Port, Zustandsseite, Registerzeile und die Lauftabelle; die eine fehlende Stelle ist migrationPort(), das heute NichtVerbundenPort liefert.
- Bild-Upload fuer Logo, Avatar und Cover (/einstellungen/identitaet). Grund: BUCKETS in server/storage/adapter.ts kennt keinen Marken-Bucket, und unter src/app/api/medien liegt kein POST. Bewusst KEIN Knopf, der nichts tut (O-12, O-13). Die Alternativtexte sind pflegbar, weil sie Text sind und nach PUB-09/LEG-07 Pflicht.
- Das K-12-Kopieren von rechnung_fuss in den kanonischen Rechnungs-Payload. Grund: der Payload kennt kein Mandanten-Fussfeld, und ein neues Feld darin ist ein Eingriff in die Hashketten-Eingabe mit eigenen Tests. Als O-621 zurueckgegeben; beide Seiten sagen es ausdruecklich.
- Die datierte Sollzeitherleitung, Uebertrags- und Verfallsregel (O-18) und die konkreten Tarifwerte (O-50). Grund: Regel 1 — jede gelieferte Zahl waere eine erfundene Geschaeftsregel mit Lohnfolge. Tabellen, Dienst, Seite, Formulare und Rechte stehen vollstaendig; offen ist nur der Wert, und die Seite zeigt ihn als 'offen (O-18)' bzw. 'nicht hinterlegt (O-50)'.
- Die Weiterleitung von /portal/[mandant]/agenten/richtlinien(+/[id]) auf den Editor in den Einstellungen. Grund: /agenten/** gehoert zur Domaene agenten-freigaben-radar; zwei Agenten im selben Arbeitsbaum an derselben Datei waere schlimmer als der Befund. Als Punkt 2 in registry_sonstiges zurueckgegeben.
- Ein Nachtlauf, der app.audit_kette_fortschreiben regelmaessig ruft. Grund: O-357 halt den Kettenpruefer ausdruecklich aus dem Job-Register heraus, solange offen ist, wer die Meldung 'Hashkette gebrochen' bekommt. Die Kette waechst deshalb beim Bilden eines Beweismittels; O-623 fragt nach dem Lauf.

## Notizen des Bauender

ALLE SECHS ROUTEN SIND GEBAUT, keine leere Seite, keine simulierte Anbindung. `npx tsc --noEmit` ist ueber das ganze Repository sauber, `npx eslint` auf allen meinen Dateien ohne Befund, die vier neuen kern-Tests laufen gruen (51 Faelle zusammen mit tests/kern/integrationen.test.ts), und `scripts/guards/run-all.ts` meldet zu meinen Pfaden nichts (die 22 Befunde dort sind todo-client-Nummern anderer Agenten im Bereich O-600..O-652, die noch nicht im Register stehen).

MIGRATIONEN GEPRUEFT: 0200-0204 laufen auf einer frisch angelegten `w_einst` bis `Migrationen angewendet.` durch — mehrfach, zuletzt nach der letzten Aenderung. Jede Abfrage und jede Policy habe ich zusaetzlich VON HAND als `cse_app` mit gesetzten GUCs gegen dieselbe Datenbank ausgefuehrt, weil ich die Isolationssuite nicht starten darf. Von Hand bestaetigt: Anlage-Trigger und Abbruch bei unbekanntem Slug; leitung liest die Identitaet, schreibt sie nicht (0 Zeilen); mandant_id und INSERT fuer cse_app verweigert; prinzipalloser Pfad nur mit oeffentlich_sichtbar, auch ueber die View; mi_alt_text; tv_mindestens_gesetz, tv_pausen_geordnet, azm_kein_ueberlapp und dass die zweite Fassung nach dem Schliessen durchgeht; azm_bestaetigt_kein_platzhalter und azm_bestaetigung_paarweise; Lesen des Modells mit zeit.konto_lesen ohne stammdaten.verwalten, wochenstunden-UPDATE verweigert, Kundenportal leer; alle sieben migration_lauf/-zeile-CHECKs samt mz_lauf_fk; Kette ueber 21 Zeilen, zweiter Lauf 0, Bruch an Position 5 erkannt und am Kopf vermerkt, Kettenkopf fuer cse_app unerreichbar, Loeschsperre auf beiden kern-Tabellen; agent_richtlinie mit module={agent} lesbar UND schreibbar (vor 0203: null Zeilen), Audit- und geaendert_am-Trigger schreiben.

EIN BEFUND, DEN ICH BEIM BAUEN GEFUNDEN UND SOFORT GESCHLOSSEN HABE: `system.audit_exportieren` traegt die Aktion `exportieren`, und `app.hat_recht_fuer` laesst `lesen` und `exportieren` in der Gruppenansicht ausdruecklich durch. `app.audit_kette_fortschreiben` und `app.audit_kette_pruefen` SCHREIBEN aber — ein Super-Admin mit globaler Rolle waere ueber den Rechtezweig durchgekommen und haette in einer nur lesenden Ansicht geschrieben (Invariante 10). Beide Funktionen pruefen jetzt zusaetzlich `app.ist_readonly()`; von Hand bestaetigt: 21 Glieder bei readonly=off, 0 bei readonly=on.

ZUR KRITIK, PUNKT FUER PUNKT: (1) Die Definer-Funktion `app.audit_nutzlast_lesen` existiert wirklich — ich habe sie nicht ersetzt, sondern eine mengenwertige Huelle daneben gestellt, weil die vorhandene zeilenweise arbeitet und JEDEN Aufruf protokolliert; ein Jahresbuendel haette Zehntausende Auditzeilen ueber sein eigenes Lesen erzeugt. Das Tor ist wie von der Kritik verlangt `system.audit_sensitiv_lesen` ZUSAETZLICH zum Exportrecht. (2) Es gab genau EIN Inline-SQL auf agent_richtlinie (nachtrag.ts:421), und ich habe es nicht angefasst: die fail-closed Aufrufer bleiben fail-closed, die Entscheidung ist O-622. (3) Den Pfad api/buchhaltung/camt/import gibt es nicht; ich habe das Vorschau-vor-Uebernahme-Muster des Raumbuchs genommen und die DESIGN-§5-Frage nicht durch eine erfundene Pille geloest, sondern durch die vorhandene Darstellung von /einstellungen/integrationen. (4) Der vierte Blocker auf /vorlagen ist geloest, ohne die Bau- und Mahn-RLS zu weiten. (5) mi_oeffentlich und die Projektions-View sind gebaut; der fehlende Schreibweg auf `mandant` betrifft /einstellungen/mandant, nicht diese Route. (6) Server Actions gibt es nur unter src/app/auth/** und src/app/dev — im Portal habe ich ueber Route-Handler geschrieben. (7) §6.15 ist die richtige Fundstelle fuer anstellung_kondition; die Tabelle existiert seit 0192 (personal war vor mir fertig), ich verweise nur darauf.

ZWEI DINGE, DIE DER PARENT WISSEN SOLLTE: (a) `mandant.farbe_token` bleibt tote Spalte — die Bereichsfarbe kommt jetzt aus `mandant_identitaet.identitaets_token` mit CHECK, und der Seed schreibt farbe_token weiterhin, ohne dass jemand liest. Ein Aufraeumen waere eine eigene kleine Migration, und sie gehoert nicht in meine Nummern. (b) Die Testfixtur `seed()` laeuft unter `session_replication_role = replica`, also feuert `kern.mandant_identitaet_anlegen` dort NICHT und die Identitaetszeilen fehlen nach jedem seed(); dasselbe hatte reinigung.test.ts:1051 fuer die Behinderungsvorlagen. Meine Isolationsdatei legt die Zeilen deshalb selbst an und prueft den Ausloeser ueber seinen Abbruchfall. Der PRODUKTIONSSEED benutzt replica nicht — dort entstehen die vier Zeilen wie vorgesehen.

GESTALTUNG: keine neue Farbe, kein neuer Abstand, keine neue Pille. Nur Klassen, die im Repository schon stehen (text-text/-muted/-subtle, bg-surface/-3, border-line/-strong, text-warning, bg-brand/hover, s1..s7, text-h1/h2/h3/micro), StatusPill aus dem geschlossenen Vokabular und Hinweis mit art hinweis|warnung. Einmal steht ein Hex-Wert als inline-`style`: das Farbfeld auf /einstellungen/identitaet zeigt den Wert aus FARBEN_BEREICH, also DESIGN §1 als Daten — eine Tailwind-Klasse je Bereich waere eine zweite Liste derselben Farbe. Fehlend und gemeldet: DESIGN §4 fuehrt kein Import-Symbol, die Uebernahme-Karte nimmt deshalb `eingang`.

## Notizen des Behebender

ALLE 18 BEFUNDE TREFFEN ZU — ich habe keinen widerlegt. Bei zweien bin ich der zweiten der beiden vom Pruefer angebotenen Behebungen gefolgt, und sage warum:

(1) Arbeitszeitmodell/Rueckwirkung: Der Pruefer liess „Sperre streichen" ODER „als offene Frage fuehren" zu. Ich habe BEIDES gemacht — die Sperre ist weg (sonst blieben Tarife rueckwirkend und Modelle nicht, und die Uebernahme von Altbestaenden waere unmoeglich), und die eigentliche Frage („ab wann ist ein Monat gesperrt?") steht als O-626 mit TODO in derselben Zeile.

(2) mandant_identitaet-Spaltengrant (klein, zweiter Befund): Eine echte Spaltengrenze haette den Tabellen-Grant entzogen und BEIDE internen Lesepfade (`ladeIdentitaet` und den Gruppenlesepfad) auf eine Definer-Funktion legen muessen — zwei Lesepfade, die ich ohne laufende Isolationssuite nicht absichern kann. Ich habe deshalb die zweite Variante genommen: der Isolationstest HAELT den Stand fest (cse_app hat die drei Spaltenrechte; die Zeilengrenze greift auch auf der Tabelle), und der View-Kommentar sagt jetzt ausdruecklich, dass sie eine Konvention mit einer sichtbaren Stelle ist und keine Schranke. Der Rest steht unter „offen".

MIGRATIONEN GEPRUEFT: nach jeder Aenderung `drop database if exists w_einst` / `create database w_einst` / `alter database … set cse.fenster_schluessel = …` / `DATABASE_URL=…/w_einst pnpm db:migrate` — zuletzt nach der letzten Aenderung, bis „Migrationen angewendet." (0200–0206 plus alle fremden bis 0304).

ISOLATIONSSUITE BLOCKIERT, DESHALB VON HAND GEPRUEFT. `pnpm test:isolation` scheitert im global-setup, weil `pnpm db:seed` an `leistungskatalog_position` in die RLS laeuft (fremde, in Arbeit befindliche Domaene). Gegen eine migrierte und geseedete w_einst von Hand bestaetigt:
 - Kette: Vormonatszeile nachgetragen NACH Anlage der Folgekette → Vormonatskopf wandert (b0d7c3… → a3b13b…), `start_hash` der Folgekette bleibt b0d7c3…, `app.audit_kette_pruefen('audit_log_2026_09')` gibt 658 Glieder und bruch_bei = null. Vorher haette dieselbe Lage einen Bruch am ersten Septemberglied gemeldet.
 - `update kern.audit_kette set start_hash = repeat('a',64)` → „Der Startwert einer Auditkette ist fest …".
 - Buendel ueber den ECHTEN Dienst (eigenes tsx-Skript, cse_app mit gesetzten GUCs): aal2 → dateien=[protokoll.csv,nutzlast.csv], beide Hashes stimmen mit den ZIP-Bytes, gekettet 462/462, manifest.json im Archiv; eine ausgetauschte Nutzlast wird beim Packen erkannt („Die Datei nutzlast.csv hat nicht mehr den SHA-256 ihres Manifesteintrags"). aal1 → redigiert, dateien=[protokoll.csv], nutzlast.grund gesetzt, kein nutzlast.csv im ZIP. Das ist 0206 in Aktion.
 - Vorlagen-Rechte: Sitzung mit `admin` + mandantenspezifisch gebundenem `system.einstellung_verwalten` + module={system} → seite=t, bau_lesen=f, bau_schreiben=f, mahnung=f, 0 Vorlagen; module=null → seite=t, bau_lesen=t, 2 Vorlagen; INSERT ohne bau.schreiben → RLS-Abweisung.
 - Harness-Reset: `delete from kern.audit_kettenglied; delete from kern.audit_kette` unter `session_replication_role = replica` geht durch (671 bzw. 1 Zeile), Kopf danach leer.
 - Seed: 8 arbeitszeitmodell-Zeilen, 1 tarifvereinbarung, 4 mandant_identitaet mit Claim, 0 migration_lauf.

WAS GRUEN LAEUFT: `npx tsc --noEmit` ueber das ganze Repository sauber bis auf EINEN Fehler aus der fremden Domaene personal (siehe „offen"). `npx eslint` auf allen 18 von mir angefassten Dateien ohne Befund. kern-Tests: audit-buendel (16, davon 5 neu: Datei-Hashes im Archiv, veraenderte Nutzlast, veraenderte Protokollzeile, Manifesteintrag ohne Datei, Datei ohne Manifesteintrag), arbeitszeit-modell (18, davon 2 neu zu UEBERTRAG_ARTEN), einstellungen-vorlagen, agent-richtlinie — 54 Faelle zusammen, alle gruen. `pnpm db:triggers --check` meldet „Trigger sind aktuell". `scripts/guards/run-all.ts` meldet zu meinen Pfaden genau EINEN Punkt: `todo-client-nicht-im-register` fuer O-626 — das ist die Zeile, die zentral nach docs/DECISIONS.md gehoert (die uebrigen 75 Befunde sind O-Nummern anderer Agenten in derselben Warteschlange). Keine Design-, keine Tailwind-Farb-Wache schlaegt an.

GESTALTUNG: keine neue Farbe, kein neuer Abstand, keine neue Pille. Verwendet habe ich nur vorhandene Tokens (text-text/-muted/-subtle, s1…s7, text-warning, bg-surface/-3, border-line) und, fuer die neue Mahntext-Spalte, `whitespace-pre-line` mit `max-w-[48ch]` — dieselbe Bauform wie die vorhandenen `max-w-[72ch]`. Nichts in docs/DESIGN.md zu ergaenzen.

DREI DINGE, DIE DER PARENT WISSEN SOLLTE: (a) tests/isolation/harness.ts ist eine gemeinsame Datei und hat zwei additive Aenderungen von mir (RESET_WURZELN um kern.audit_kette, Sitzung.aal) — fuer alle anderen Testdateien aendert sich das Verhalten nicht. (b) src/server/services/finanz/mahnung/stufen.ts gehoert der Domaene finanzen; meine Aenderung ist rein additiv (eine Spalte im select und im Zeilentyp). (c) O-624 faellt weg, weil 0206 die Frage aus der Spezifikation beantwortet — die Ersatzzeile fuer den Entscheidungsteil liegt in decisions_zeilen, die D-Nummer vergibt bitte die zentrale Hand.
