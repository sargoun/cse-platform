# Registereinträge: stammdaten

**Warteschlange, kein Archiv.** Diese Einträge sind **noch nicht** im Baum. Die
gemeinsamen Dateien pflegt EINE Hand, weil mehrere Agenten gleichzeitig arbeiten
und sich sonst in dieselbe Zeile schreiben. Ist ein Abschnitt eingetragen, wird
er hier gelöscht — solange er hier steht, fehlt er dort.

**Die Einträge des Behebungsschritts gelten.** Er lief zuletzt und hatte den
Auftrag, die vollständige aktuelle Liste zu liefern — auch das, was sich seit
dem Bauschritt geändert hat.

## Stand

- Bau: fertig
- Kritik: 14 Befunde
- Behebung: 11 behoben, 2 widerlegt, 7 offen

## Migrationen (gegen eine eigene Datenbank gefahren: True)

- /home/user/cse-platform/drizzle/0275_stammdaten_katalogpflege.sql — Plattform-Schreibzweig fuer abwesenheitsart und antragsart (t_plattform_anlegen / t_plattform_aendern, nur app.ist_super_admin(), not app.ist_readonly(), antragsart zusaetzlich not ist_system). Antwort auf den ersten KRITIK-Punkt: t_katalog_pflege war die EINZIGE Schreibpolicy und hatte keinen Super-Admin-Zweig — die sieben Plattformarten waren fuer NIEMANDEN aenderbar, O-139 also durch keine Oberflaeche beantwortbar. Das Muster ist woertlich das von qualifikation (q_schreiben/q_aendern, 0030, 01-KERN §6.16) und keine neue Regel.
- /home/user/cse-platform/drizzle/0276_katalog_schluessel_kollision.sql — kern.katalog_schluessel_frei() (security definer, Eigentuemer cse_definer) + trg_abwesenheitsart_schluessel_frei / trg_antragsart_schluessel_frei, dazu die fehlende cse_definer-Lesepolicy at_definer auf antragsart. Schliesst die von der KRITIK gefundene Luecke: unique nulls not distinct (mandant_id, schluessel) erlaubt 'urlaub' plattformweit UND je Gesellschaft, waehrend leseAbwesenheitsarten/leseAntragsarten nur archiviert_am filtern — im Antragsformular stuenden zwei gleich aussehende Eintraege mit verschiedener Lohnfolge. Archivierte Zeilen geben den Schluessel frei.
- /home/user/cse-platform/drizzle/0277_belagsart_historie_lesen.sql — app.belagsart_historie_lesen() (stable, security definer, Eigentuemer cse_definer, prueft Portal 'intern' UND stammdaten.verwalten) plus grant select on belagsart to cse_definer und policy b_definer. Ohne sie ist die Historie mit Leistungswert nicht lesbar (K-05-Spaltenentzug), und app.leistungswerte_lesen gibt nur die Stichtagsscheibe und prueft objekt.lesen.
- /home/user/cse-platform/drizzle/0278_abwesenheitsart_schutz_definer.sql — kern.abwesenheitsart_schutz() als security definer mit Eigentuemer cse_definer, Rumpf unveraendert aus 0073. Eigener Befund: der Ausloeser prueft 'ist die Art benutzt' mit den Rechten des AUFRUFERS gegen abwesenheit (force RLS, t_mandant verlangt zeit.abwesenheit_lesen). Eine Pflegesitzung mit stammdaten.verwalten, aber ohne Abwesenheitsrecht, bekam dort false — die Sperre gegen das Umbenennen einer benutzten Art verschwand lautlos genau fuer die Sitzung, fuer die es sie gibt.

## Gebaute Adressen

- `/portal/[mandant]/stammdaten/abwesenheitsarten` — fertig
  - Katalog + echter Schreibweg (anlegen/aendern/archivieren). bezahlt hat DREI Zustaende (ja/nein/ungeklaert mit O-139-Marke, nie stillschweigend nein), dazu zaehlt_auf_urlaubskonto, erzeugt_stundenkonto_bewegung, ist_gesundheitsbezogen, nachweis_pflicht_ab_tagen, lohnart_schluessel, farbe_token (nur DESIGN-Tokens) und bezeichnung_i18n fuer de/en/ar/tr. Plattformzeilen (alle 7) stehen mit dem Vermerk 'Plattformkatalog — hier nicht aenderbar' da; mit Super-Admin-Sitzung (aal2) sind sie pflegbar (Migration 0275) — das ist der Weg, auf dem O-139 ueberhaupt beantwortbar wird. Geloescht wird nie: archiviert_am. Der Dienst setzt geaendert_am/geaendert_von selbst, weil diese Tabelle keinen setze_geaendert_am-Ausloeser traegt.
- `/portal/[mandant]/stammdaten/antragsarten` — fertig
  - Liste mit Herkunft, Pflichtfeldern, Folgewirkung und Antragsbestand; Anlege-/Aenderungs-/Archivierungsweg fuer mandanteigene und (Super-Admin) plattformweite Arten. KRITIK umgesetzt: die sechs Schalter tragen je den ORT ihrer Wirkung — vier im Antragsformular (leseAntragsarten), erzeugt_abwesenheit erst bei der Entscheidung, ist_stammdatenaenderung ausschliesslich in kern.antrag_pflichtfelder. Systemzeilen (alle drei vorhandenen) sind fuer JEDEN gesperrt, mit Grund statt totem Knopf; die O-142-Marke steht vor der Liste. Dienstpruefung: erzeugt_abwesenheit verlangt Zeitraum UND Abwesenheitsart, sonst scheitert erst die Genehmigung an NOT NULL in abwesenheit.
- `/portal/[mandant]/stammdaten/belagsarten` — fertig
  - Heute gueltige Werte + Historie je Code + Umdatierung (laufende Fassung zum Vortag schliessen, neue ab gewaehltem Tag) + Richtigstellung ohne neue Fassung. Der Leistungswert ist cse_app spaltenweise entzogen (K-05), deshalb neuer Definer app.belagsart_historie_lesen() mit stammdaten.verwalten (0277) statt app.leistungswerte_lesen (objekt.lesen, nur Stichtagsscheibe). Rechtefalle aus der KRITIK aufgeloest: Lesen laeuft ueber den Definer, SCHREIBEN braucht zusaetzlich objekt.lesen (t_mandant USING) — fehlt es, sagt die Seite das und blendet die Formulare aus, statt beim Speichern abzuweisen. O-17 und O-349 stehen vor der Liste. IN DIESER SITZUNG KORRIGIERT: datiereBelagsartUm normalisiert den Leistungswert jetzt selbst mit alsLeistungswert (siehe notizen).
- `/portal/[mandant]/stammdaten/qualifikationen` — fertig
  - Katalog mit Kategorie (O-144), Rechtsgrundlage (Anzeigetext), laeuft_ab + standard_gueltigkeit_monate (leer = O-341), Warnstufen 60/30/7, blockiert_einsatz, erfordert_dokument (O-343) und Reichweite je Zeile (Nachweise / Posten-Anforderungen, 'n. l.' wo das Recht fehlt — nie 0). KRITIK umgesetzt: die Sperrwirkung wird gegen app.einsatz_qualifikation_erfuellt (0031) belegt, nicht gegen einteilung.ts; nachweis_art_id ist KEIN Formularfeld (von keiner Codezeile gelesen). Plattformzeilen nur Super-Admin (q_schreiben/q_aendern, 0030) — keine Migration noetig.
- `/portal/[mandant]/stammdaten/reinigungsklassen` — fertig
  - Code, Bezeichnung, Beschreibung, Sortierung, Bestaetigung (O-55) — anlegen, aendern, umsortieren, archivieren. Wirkungstext nach KRITIK neu geschrieben: die Klasse steuert NUR raum.reinigungsklasse_id und die Code-Zuordnung des Raumbuch-Imports, sie geht in keine Sollzeit und keine Kalkulation (die rechnen ueber die Belagsart). Vor dem Archivieren nennt die Zeile die Zahl der Raeume und Importzeilen; der Teilindex gibt den Code erst danach frei. Ergaenzt: Lesen braucht objekt.lesen (Policy t_mandant) — die Seite sagt, dass eine leere Liste dann nicht 'keine Klassen' heisst.

## src/server/registry/dienste.ts

In src/server/registry/dienste.ts, Array DIENSTE, sechs Eintraege:

  /**
   * **Die fuenf Stammdatenkataloge (SEITENKARTE §5.13).** `katalog` ist das
   * Gemeinsame der fuenf — Schluesselform, Pflichttexte, i18n auf genau
   * de/en/ar/tr, `pflegbar`/`sperrgrund`, die Uebersetzer fuer 23505/42501 und
   * die Stufenkollision aus 0276. Es rechnet nichts und schreibt nichts,
   * deshalb `schreibend: false`.
   *
   * Die fuenf Fachdienste schreiben und nennen dafuer `stammdaten.verwalten`
   * — dasselbe Recht, das die Routen und die WITH-CHECK-Policies verlangen.
   * Modul ist `stammdaten` und nicht `reinigung`: abwesenheitsart,
   * antragsart und qualifikation haengen am MENSCHEN, belagsart und
   * reinigungsklasse sind in einer Gesellschaft ohne Reinigung einfach leer.
   */
  { modul: 'stammdaten', pfad: 'stammdaten/katalog', schreibend: false },
  {
    modul: 'stammdaten', pfad: 'stammdaten/abwesenheitsart',
    schreibend: true, schreibRecht: 'stammdaten.verwalten',
  },
  {
    modul: 'stammdaten', pfad: 'stammdaten/antragsart',
    schreibend: true, schreibRecht: 'stammdaten.verwalten',
  },
  {
    modul: 'stammdaten', pfad: 'stammdaten/belagsart',
    schreibend: true, schreibRecht: 'stammdaten.verwalten',
  },
  {
    modul: 'stammdaten', pfad: 'stammdaten/qualifikation',
    schreibend: true, schreibRecht: 'stammdaten.verwalten',
  },
  {
    modul: 'stammdaten', pfad: 'stammdaten/reinigungsklasse',
    schreibend: true, schreibRecht: 'stammdaten.verwalten',
  },

## src/server/auth/route-manifest.ts

In src/server/auth/route-manifest.ts, Array ROUTEN, fuenf Eintraege (Recht stimmt wortgleich mit authorize({ recht: 'stammdaten.verwalten', schreibend: true }) in allen fuenf Handlern und mit den WITH-CHECK-Policies der Tabellen ueberein — kein Rechtebruch zu ueberbruecken):

  {
    /**
     * Die fuenf Stammdatenkataloge (SEITENKARTE §5.13, OPS-02, OPS-03,
     * SEC-01, EMP-05, EMP-10).
     *
     * `stammdaten.verwalten` und nicht `objekt.lesen`: hier wird der KATALOG
     * gepflegt, nicht ein Objekt gelesen. Dass belagsart und reinigungsklasse
     * zusaetzlich `objekt.lesen` brauchen, ist die Lesepolicy der Tabellen
     * (0021) und keine zweite Routenfrage — die Seiten sagen es dem Menschen.
     */
    pfad: 'api/stammdaten/abwesenheitsarten',
    recht: 'stammdaten.verwalten',
  },
  { pfad: 'api/stammdaten/antragsarten', recht: 'stammdaten.verwalten' },
  { pfad: 'api/stammdaten/belagsarten', recht: 'stammdaten.verwalten' },
  { pfad: 'api/stammdaten/qualifikationen', recht: 'stammdaten.verwalten' },
  { pfad: 'api/stammdaten/reinigungsklassen', recht: 'stammdaten.verwalten' },

## src/server/db/schema/rls.ts

KEIN Eintrag noetig — geprueft. Alle fuenf Tabellen stehen bereits in src/server/db/schema/rls.ts, weil sie nicht neu sind: belagsart:182, reinigungsklasse:192, qualifikation:390, abwesenheitsart:788, antragsart:806; die AUDITIERT-Liste fuehrt belagsart:1737 (bewusst als einzige der fuenf), die Ausloeserlisten belagsart:1954, reinigungsklasse:1955, qualifikation:1973. Die Kommentare ab 1730 und 1753 begruenden ausdruecklich, warum abwesenheitsart, antragsart, qualifikation und reinigungsklasse KEINEN Audit-Ausloeser tragen — genau deshalb schreiben die vier Dienste ihre Protokollzeile von Hand, und genau das war die Ursache des blockierenden K-19-Befundes.

## src/server/registry/navigation.ts

KEIN Eintrag noetig — und das ist geprueft, nicht angenommen. src/server/registry/navigation.ts kennt keinen Schluessel 'stammdaten' und braucht auch keinen: die fuenf Seiten sind Karten in der KARTEN-Liste von src/app/portal/[mandant]/einstellungen/page.tsx (Zeilen 104-118, bereits eingetragen und keine gesperrte Datei), genau wie einstellungen/abrechnungsarten. Der Tab ist 'mehr'. Zusaetzlich sind sie seit dieser Sitzung aus den drei Nachbarseiten verlinkt, an denen der Wert wirkt (Raumbuch, Nachweisregister, Antragseingang) — rechtegeprueft ueber haeltRechte, AUT-06.

## Sonstiges

1) src/server/registry/modul.ts, Set QUERSCHNITT — eine Zeile, in die bestehende Liste (heute: 'bericht', 'crm', 'objekt', 'raum', 'dienstplan', 'zeit', 'personal', 'angebot', 'auftrag', 'kalkulation', 'katalog', 'finanzen', 'dokument', …):

  // `stammdaten` (SEITENKARTE §5.13): die fuenf Kataloge sind KEIN Gewerk.
  // abwesenheitsart, antragsart und qualifikation haengen am Menschen und
  // gelten in jeder Gesellschaft; belagsart und reinigungsklasse sind in
  // einer Gesellschaft ohne Reinigung einfach leer — leer ist nicht
  // dasselbe wie gesperrt. modulAktiv gibt heute ueber den Rueckfall
  // 'unbekanntes Modul bleibt offen' (Zeile 135) zufaellig dieselbe
  // Antwort; diese Zeile macht sie absichtlich. Ohne sie nimmt der erste
  // GEWERK_FUER_MODUL['stammdaten'] = 'reinigung' die drei
  // Personal-Kataloge fuer security, bau und operations vom Bildschirm.
  'stammdaten',

2) tests/kern/katalog-unbenutzt.ts:191 — optionale Hygiene, von mir bewusst NICHT angefasst. Die KRITIK des Plans weist nach, dass 'stammdaten.verwalten' dem Pruefer laengst als benutzt gilt (22 Funde, davon 16 in Migrationen) und dass katalog.test.ts nur `unbenutzt ⊆ Warteliste` prueft — eine Obermenge ist erlaubt, es ist also kein Bau-Gate.

3) Migration: KEINE neue und KEINE geaenderte. 0275-0279 stehen unveraendert, wie sie der Bauschritt hinterlassen hat. Wie verlangt nach der Arbeit erneut geprueft: `drop database if exists w_stamm` / `create database w_stamm` / `alter database w_stamm set cse.fenster_schluessel = '…'` / `DATABASE_URL=…/w_stamm pnpm db:migrate` -> laeuft bis 0304_leistungsnachweis_auf_der_schicht.sql durch, „Migrationen angewendet."

## Zeilen für docs/DECISIONS.md, Abschnitt „Offen"

| O-690 | **Fuehrt jede Gesellschaft eigene Abwesenheitsarten, oder gilt der Katalog gruppenweit einheitlich — und muss der Lohnartenschluessel je Art in allen drei Rechtseinheiten derselbe sein?** Die Plattform kann beides: `abwesenheitsart` ist zweistufig (K-17), die Pflegeseite legt auf Wunsch eine mandanteigene Art an und `kern.katalog_schluessel_frei` (0276) verhindert, dass derselbe Schluessel auf beiden Stufen aktiv ist. Offen ist die organisatorische Haelfte: eine eigene Art je Gesellschaft heisst je Gesellschaft eine eigene Lohnzuordnung im ACC-12-Export. | EMP-05, ACC-12, `abwesenheitsart`, O-139 |
| O-691 | **Wer pflegt in der Gruppe den PLATTFORM-Katalog (Abwesenheitsarten, Antragsarten, Qualifikationen), und braucht eine Aenderung daran eine zweite Zustimmung?** Plattformweite Katalogzeilen gelten fuer alle vier Gesellschaften; sie sind seit 0275 (abwesenheitsart, antragsart) und 0030 §6.16 (qualifikation) genau dem Super-Admin mit zweitem Faktor zugaenglich, und jede Aenderung steht im Pruefprotokoll. Ein Vier-Augen-Prinzip ist NICHT gebaut — `bezahlt` oder `blockiert_einsatz` wirken mit dem Speichern. | AUT-01, AUT-02, K-17, `abwesenheitsart`, `antragsart`, `qualifikation` |
| O-692 | **Darf eine Belagsart-Fassung zwischen dem Beginn der laufenden Fassung und heute beginnen, wenn fuer diesen Zeitraum schon Kalkulationen gerechnet wurden — und wer gibt das frei?** Heute gilt: eine neue Fassung beginnt STRIKT NACH dem Beginn der laufenden; ein frueherer Beginn wird von `pruefeDatierung` abgewiesen und waere ohnehin unmoeglich, weil die laufende `gueltig_bis is null` traegt und `belagsart_zeitraum_eindeutig` jede Ueberschneidung mit ihrem Bereich `daterange(gueltig_ab, 'infinity')` ausschliesst. Unbewacht ist genau der Bereich DAZWISCHEN: ein Beginn nach dem Start der laufenden Fassung, aber vor `app.berlin_heute()`, wird angenommen und aendert rueckwirkend die Grundlage jeder Kalkulation aus dieser Zeit, ohne dass jemand zustimmt. Die strengere Variante — Beginn nie vor `app.berlin_heute()` — ist eine Zeile im Dienst. | OPS-03, O-17, `belagsart`, `kalkulation_position` |
| O-693 | **Was geschieht mit Raeumen, die auf eine archivierte Reinigungsklasse zeigen — bleibt die Einstufung stehen oder muessen sie vor dem Archivieren umgestuft werden?** Heute bleibt sie stehen: `archiviert_am` gibt den Code frei (Teilindex `reinigungsklasse_code_uk`), `raum.reinigungsklasse_id` wird NICHT geleert, und die Pflegeseite nennt vor dem Archivieren die Zahl der betroffenen Raeume und Importzeilen. Die Alternative — Archivieren nur ohne haengende Raeume — waere eine Sperre, die ein Mensch heute nicht erwartet. | OPS-02, O-55, `reinigungsklasse`, `raum` |
| O-694 | **Gibt es eine Hoechstlaenge fuer Belagsart- und Reinigungsklassen-Codes aus dem Kundenraumbuch — und wenn ja, welche?** Heute gilt: KEINE. `belagsart.code` und `reinigungsklasse.code` sind `text` ohne CHECK und ohne `varchar(n)`; die Dienste pruefen nur auf Vorhandensein und Randleerraum, weil der Code aus dem Raumbuch DES KUNDEN kommt und dort so aussieht, wie er dort aussieht. Eine vorher im Dienst gefuehrte Grenze von 20 Zeichen war erfunden (weder Tabelle noch SPEC/DESIGN/DECISIONS nannten sie) und haette eine echte Kundendatei mit einem laengeren Code abgewiesen; sie ist entfernt. | OPS-02, OPS-03, `belagsart`, `reinigungsklasse`, `raumbuch_import_zeile` |

## Befunde des Prüfers (14)

- **blockierend** · `/home/user/cse-platform/src/server/services/stammdaten/abwesenheitsart.ts` — Die 12 Auditaktionsnamen der vier handgeschriebenen Protokollschreiber gelten dem K-19-Pruefer als RECHTESCHLUESSEL ohne Katalogzeile — tests/kern/katalog.test.ts ist damit ROT, und der Bericht erwaehnt das nicht. Ursache ist die Schreibweise: die uebrigen ~25 Dienste im Haus stellen den Aktionsnamen als Literal IN den SQL-Aufruf (`app.protokolliere('dienstplan.serie_angelegt', ...)`), und genau dieses Muster schneidet scripts/katalog/benutzung.ts Regel (2) heraus. Hier steht der Name stattdessen als TypeScript-Argument einer Hilfsfunktion (`protokolliere(kontext, 'stammdaten.abwesenheitsart_angelegt', ...)`), also AUSSERHALB des `app.protokolliere(...)`-Textes — MODUL_LITERAL greift, weil 'stammdaten' in MODULE steht, und meldet einen nicht existierenden Rechteschluessel.
  - Behebung: Den Aktionsnamen in den SQL-Text ziehen, wie es jeder andere Dienst tut — d. h. statt `protokolliere(kontext, 'stammdaten.x_angelegt', ...)` je Aktion `select app.protokolliere('stammdaten.x_angelegt', 'x', $1, $2::jsonb, $3::jsonb, app.aktiver_mandant())` schreiben (drei kurze Aufrufe statt einer Hilfsfunktion mit Namensparameter). Dann greift der Ausschnitt des Pruefers und katalog.test.ts ist wieder gruen. Den Katalog NICHT um 12 Pseudo-Rechte erweitern.
- **blockierend** · `/home/user/cse-platform/src/server/auth/route-manifest.ts` — Die fuenf neuen Route Handler unter src/app/api/stammdaten/ fehlen im Routenmanifest. tests/kern/routen.test.ts ('keine Route fehlt im Manifest') ist rot und nennt sie namentlich. Der Bericht kennt das (registry_sonstiges #6, registry_manifest) — die Zeilen sind ein VORSCHLAG und nicht eingefuegt; damit ist keine der fuenf Routen im Sinne der Definition-of-done fertig.
  - Behebung: Die fuenf Zeilen aus dem Feld `registry_manifest` des Berichts in ROUTEN eintragen (je `{ pfad: 'api/stammdaten/<x>', recht: 'stammdaten.verwalten' }`). Das Recht stimmt mit `authorize(...)` in den Handlern und mit den Tabellen-Policies ueberein, es gibt also keinen Rechtebruch zu ueberbruecken.
- **blockierend** · `/home/user/cse-platform/src/server/registry/dienste.ts` — Die sechs neuen Dienstmodule unter src/server/services/stammdaten/ fehlen im Diensteregister. tests/kern/portal-shell.test.ts ('jede Dienstquelle ist registriert') ist rot. Ebenfalls nur vorgeschlagen, nicht eingefuegt.
  - Behebung: Die sechs Zeilen aus dem Feld `registry_dienste` in DIENSTE eintragen (katalog mit `schreibend: false`, die fuenf Fachdienste mit `schreibRecht: 'stammdaten.verwalten'`).
- **wichtig** · `/home/user/cse-platform/src/server/services/stammdaten/abwesenheitsart.ts` — Das Pruefprotokoll bekommt VORHER und NACHHER in zwei verschiedenen Schluessel-Vokabularen: `einZeile()` liest die DB-Spalten (snake_case: zaehlt_auf_urlaubskonto, ist_gesundheitsbezogen, lohnart_schluessel, farbe_token), `nachher` ist das Eingabeobjekt (camelCase plus i18n/plattform). `app.protokolliere` rechnet `geaendert_felder` aus den Schluesseln von NACHHER gegen VORHER — jedes nur-camelCase-Feld ist dort immer `distinct` von NULL. Folge: geaendert_felder nennt bei JEDER Aenderung dieselben Felder als geaendert, auch wenn sich nichts geaendert hat, und eine ECHTE Umstellung von zaehlt_auf_urlaubskonto oder ist_gesundheitsbezogen ist im Protokoll nicht als Aenderung erkennbar. Der Kommentar an der Funktion nennt genau diese Zeile 'die Antwort auf: seit wann rechnet das so'. Dieselbe Verwechslung in antragsart.ts:292, qualifikation.ts:309 und reinigungsklasse.ts:197 (dort heisst vorher `ist_platzhalter`, nachher `bestaetigt` — das Umschalten der O-55-Marke steht im Protokoll ueberhaupt nicht).
  - Behebung: Entweder `nachher` vor dem Protokollieren auf die DB-Spaltennamen abbilden (und i18n/plattform weglassen, die keine Spalte sind), oder — einfacher und pruefbar — nach dem UPDATE `returning` auf denselben Spaltensatz wie `einZeile` verwenden und diese Zeile als `nachher` protokollieren. Danach den Isolationstest von `not.toBeNull()` auf den erwarteten Feldsatz verschaerfen (z. B. genau ['bezeichnung'] bei einer reinen Umbenennung).
- **wichtig** · `/home/user/cse-platform/src/app/portal/[mandant]/stammdaten/belagsarten/page.tsx` — Das Formular 'Neue Fassung' in der Zeile (was=datieren, ab Zeile 236) traegt KEIN Feld `beschreibung`. `pruefeBelagsartEingabe` liest `lies('beschreibung')` und bekommt null, `datiereBelagsartUm` schreibt NULL in die neue Fassung. Wer den Leistungswert umdatiert, verliert damit lautlos die Beschreibung der Belagsart — Bezeichnung, Leistungswert und Quelle sind vorbelegt, die Beschreibung fehlt einfach, und auf dem Bildschirm steht kein Wort darueber.
  - Behebung: In der datieren-Form ein Feld `beschreibung` mit `defaultValue={f.beschreibung ?? ''}` ergaenzen (wie in der richtigstellen-Form). Dasselbe Feld fehlt nicht im Anlegeformular am Seitenende, dort ist es vorhanden.
- **wichtig** · `/home/user/cse-platform/src/server/services/stammdaten/belagsart.ts` — Der Docstring von `pruefeDatierung` und die Registerzeile O-692 behaupten das Gegenteil des gebauten Verhaltens: 'eine Fassung darf vor der laufenden beginnen, solange die Schranke belagsart_zeitraum_eindeutig keine Ueberschneidung findet'. Der Code weist genau das ab (`if (laufendeSeit >= gueltigAb) return fehler`), und der Test pinnt die Abweisung. Die Behauptung ist auch technisch unmoeglich: die laufende Fassung hat `gueltig_bis is null`, ihr Bereich ist `daterange(gueltig_ab, 'infinity')` — jedes Datum vor ihrem Beginn ueberschneidet sie. Das WIRKLICHE offene Risiko ist ein anderes und steht so nirgends: ein Beginn NACH dem Start der laufenden Fassung, aber VOR app.berlin_heute(), ist erlaubt und aendert rueckwirkend die Grundlage jeder Kalkulation aus dieser Zeit. Die Seite sagt dem Benutzer dazu 'Ein Beginn in der Vergangenheit ist möglich, solange sich kein Zeitraum überschneidet' — fuer einen vorhandenen Code ist das falsch.
  - Behebung: Docstring, Registerzeile O-692 und den Satz auf der Seite auf das tatsaechliche Verhalten umschreiben: eine neue Fassung beginnt STRIKT NACH dem Beginn der laufenden; rueckwirkend heisst hier 'zwischen dem Beginn der laufenden Fassung und heute', und genau dieser Bereich ist unbewacht. Die strengere Variante (Beginn nie vor app.berlin_heute()) bleibt die offene Frage — dann aber mit richtig beschriebenem Ausgangszustand.
- **wichtig** · `/home/user/cse-platform/src/server/services/stammdaten/belagsart.ts` — Erfundene Eingabegrenze: `code.length > 20` wird mit dem Satz 'Der Code ist höchstens 20 Zeichen lang — er steht in jeder Zeile des Raumbuchs' abgewiesen. In der Datenbank gibt es diese Grenze nicht (`code text not null`, kein CHECK, kein varchar), in SPEC/DESIGN/DECISIONS steht sie nicht, und es gibt kein `TODO(client, O-NN)` und keine Registerzeile dazu. Derselbe Dienst begruendet zwei Zeilen weiter, der Code komme aus dem Raumbuch DES KUNDEN und werde deshalb nicht umgeschrieben — eine Datei mit einem 24-stelligen Belagscode wird damit von einer Zahl abgewiesen, die niemand entschieden hat. Identisch in reinigungsklasse.ts:128.
  - Behebung: Entweder die Grenze entfernen (die Tabelle fuehrt keine) oder sie als ausdruecklichen Platzhalter mit `// TODO(client, O-NN): Wie lang darf ein Belagsart-/Reinigungsklassen-Code aus dem Kundenraumbuch sein?` fuehren, in docs/DECISIONS.md unter 'Open' aufnehmen und die Meldung entsprechend formulieren. Nebenbei dieselbe Pruefung fuer die uebrigen stillen Obergrenzen (nachweisAbTagen \d{1,3}, standardGueltigkeitMonate \d{1,3}, warnungTage \d{1,4}, Leistungswert \d{1,6} bei numeric(10,3) = 7 Vorkommastellen, ganzzahlOderNull \d{1,6}).
- **wichtig** · `/home/user/cse-platform/docs/DECISIONS.md` — Die vier NEUEN offenen Fragen O-690 bis O-693 stehen nur in docs/architecture/routenbau/register/stammdaten.md, nicht in docs/DECISIONS.md unter 'Open'. CLAUDE.md verlangt fuer jeden Platzhalter genau drei Dinge: Interface, sichtbare Marke, `TODO(client)` UND die Registerzeile in DECISIONS.md; die Definition-of-done nennt 'DECISIONS.md updated with anything assumed'. Der Hauswaechter meldet es namentlich.
  - Behebung: Die vier Zeilen aus dem Feld `decisions_zeilen` des Berichts in docs/DECISIONS.md unter 'Open' eintragen — mit der bei O-692 korrigierten Beschreibung des heutigen Verhaltens (siehe Befund zu belagsart.ts).
- **wichtig** · `/home/user/cse-platform/src/app/portal/[mandant]/stammdaten/reinigungsklassen/page.tsx` — Die Seite blendet bei fehlendem `objekt.lesen` nur die LISTE aus, zeigt das Formular 'Klasse anlegen' aber weiter (Abschnitt ab Zeile 260 steht ausserhalb jeder `lesbar`-Bedingung) — und der Hinweis darueber liest sich so, als koenne man trotzdem pflegen ('Gelesen wird der Katalog mit objekt.lesen, gepflegt mit stammdaten.verwalten'). Speichern scheitert aber: `legeReinigungsklasseAn` schreibt `insert ... returning id`, und Postgres wendet auf das RETURNING die SELECT-Policy an — `t_mandant` USING verlangt `objekt.lesen`, also 42501. Den Fehler uebersetzt `alsStammdatenFehler` in den Satz 'Diese Zeile gehört dem Plattformkatalog und wird von der Super-Administration gepflegt' — reinigungsklasse hat `mandant_id not null` und ueberhaupt keine Plattformstufe. Der Mensch bekommt eine Erklaerung, die auf diese Tabelle nicht zutrifft. Die Nachbarseite belagsarten macht es richtig und blendet ihre Formulare aus (schreibbar-Zweig).
  - Behebung: Den Anlege-Abschnitt (und den Hinweistext) wie bei belagsarten in `lesbar ? ... : null` fassen und im Warnzweig sagen, dass mit dieser Rolle nicht gespeichert werden kann. Zusaetzlich `alsStammdatenFehler` einen tabellenpassenden 42501-Satz mitgeben (oder in reinigungsklasse.ts wie in datiereBelagsartUm einen eigenen 42501-Zweig fuehren), damit nie eine Plattformstufe erklaert wird, die es nicht gibt.
- **klein** · `/home/user/cse-platform/src/server/services/stammdaten/abwesenheitsart.ts` — `pruefeStufenkollision` kann in der Plattformrichtung nichts finden: die Abfrage sucht Zeilen mit `mandant_id is not null`, die Lesepolicy `t_katalog` zeigt aber nur `mandant_id is null or mandant_id = any(app.sichtbare_mandanten())`, und in der Mandantensicht ist das genau der aktive Mandant. Legt ein Super-Admin eine Plattformart an, deren Schluessel eine ANDERE Gesellschaft fuehrt, meldet die Vorpruefung nichts; es greift der Definer-Ausloeser kern.katalog_schluessel_frei mit errcode unique_violation, und `alsStammdatenFehler` macht daraus 'Diesen Schlüssel führt dieser Katalog schon' — die Meldung fuer eine Dublette auf DERSELBEN Stufe. Der eigens gebaute Grund 'kollision' mit dem passenden Satz ('mindestens eine Gesellschaft führt eine eigene Art…') ist damit unerreichbar. Gleich in antragsart.ts:210 und qualifikation.ts:234.
  - Behebung: Die Kollisionsmeldung aus dem Ausloeser erkennbar machen — z. B. im Dienst vor `alsStammdatenFehler` auf 23505 mit Text 'andere Katalogstufe' pruefen und daraus 'kollision' bauen, oder kern.katalog_schluessel_frei einen eigenen errcode geben. Die Vorpruefung in der Plattformrichtung ehrlich beschriften: sie sieht nur den aktiven Mandanten.
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/stammdaten/qualifikationen/page.tsx` — In der Spalte 'Sperrt' steht fuer `blockiert_einsatz` die Statuspille `Überfällig` neben dem Wort 'Einteilung'. Gelesen ergibt das 'Überfällig Einteilung' — das feste Vokabular aus DESIGN §5 bedeutet dort 'ueberfaellig' (Frist verstrichen), nicht 'sperrt'. Die Pille traegt ihren Text selbst (DESIGN §9: Farbe ist nie das einzige Signal), also liest der Mensch hier einen Zustand, den die Zeile nicht hat.
  - Behebung: Fuer eine harte Sperre ist im festen Satz kein Wort vorgesehen. Entweder ohne Pille auskommen (Text 'sperrt die Einteilung (SEC-04)' in text-danger, wie es die Seite an anderen Stellen fuer Platzhaltermarken tut) oder DESIGN.md §5 zuerst um ein Pill-Wort fuer 'gesperrt' erweitern und dann benutzen — nie umgekehrt.
- **klein** · `/home/user/cse-platform/src/app/portal/[mandant]/stammdaten/qualifikationen/page.tsx` — Die Spalte 'Urkunde' zeigt fuer `erfordert_dokument = false` den Text 'offen (O-343)'. Die Spalte ist `boolean not null default false` — ein bewusstes 'nein' ist damit von 'noch nicht beantwortet' nicht zu unterscheiden, und die Seite behauptet fuer jedes ausgeschaltete Haekchen, die Frage sei offen. Genau diese Verwechslung vermeidet dieselbe Domaene bei `abwesenheitsart.bezahlt` mit drei Zustaenden und nennt das ausdruecklich ihren Kern.
  - Behebung: Entweder den Text auf 'nicht verlangt' aendern und O-343 nur in der Frage VOR der Liste fuehren (dort steht sie schon), oder — wenn die Unterscheidung wirklich gebraucht wird — die Spalte wie `bezahlt` dreiwertig machen; das waere aber eine Migration und gehoert erst nach der Antwort auf O-343.
- **klein** · `/home/user/cse-platform/src/server/registry/modul.ts` — 'stammdaten' steht weder in QUERSCHNITT noch in GEWERK_FUER_MODUL. `modulAktiv` gibt heute `true` zurueck, aber ueber den Rueckfall 'ein Modul, das in KEINER der beiden Tabellen steht, bleibt offen' — die richtige Antwort aus Versehen. Wer spaeter GEWERK_FUER_MODUL['stammdaten'] setzt (naheliegend: 'reinigung', weil belagsart und reinigungsklasse dort haengen), nimmt die drei Personal-Kataloge fuer security, bau und operations vom Bildschirm. Der Bericht schlaegt die Zeile vor, sie ist nicht eingetragen.
  - Behebung: Die eine Zeile aus dem Feld `registry_sonstiges` #1 in QUERSCHNITT eintragen, mit der dort schon formulierten Begruendung (die fuenf Kataloge sind kein Gewerk; abwesenheitsart/antragsart/qualifikation haengen am Menschen, belagsart/reinigungsklasse sind in einer Gesellschaft ohne Reinigung einfach leer).
- **klein** · `/home/user/cse-platform/docs/architecture/routenbau/plan/stammdaten.md` — Gemeinsames #9 des Plans verlangt Querverweise in BEIDE Richtungen; gebaut ist nur die Richtung von den fuenf Katalogseiten nach draussen. Die Gegenrichtung fehlt vollstaendig: Raumbuch -> Belagsarten/Reinigungsklassen, Nachweisregister -> Qualifikationen, Antragseingang -> Antragsarten. Der Bericht erklaert das mit paralleler Arbeit an fremden Dateien — die Folge bleibt, dass die fuenf Seiten nur ueber die Einstellungskarten gefunden werden, also genau dort nicht, wo der Wert wirkt.
  - Behebung: Wenn die Nachbardomaenen wieder frei sind: je einen rechtegepruefen Verweis (Muster haeltRechte, AUT-06) in objekte/[id]/raumbuch/page.tsx, personal/nachweise/page.tsx und personal/antraege/page.tsx ergaenzen.

**Urteil:** Nicht abnahmefaehig. Die fuenf Seiten sind echt gebaut — sie lesen ueber die Dienste aus der Datenbank, Spaltennamen und Enum-Werte halten gegen w_stamm, Geld kommt nicht vor, alle Zeitstempel sind timestamptz, das Vorgabedatum kommt aus app.berlin_heute() statt aus new Date(), das Recht wird serverseitig in jedem Handler und ueber mandantTor geprueft und fehlendes Recht endet in 404 (authorize -> NichtGefundenFehler), keine vorgetaeuschte Integration, kein Hexwert und kein Einzelwert-Padding, kein Hard Delete, und die vier Migrationen 0275-0278 sind in der Datenbank mit Definer-Eigentum cse_definer, gesetztem search_path und revoke-from-public auf der app-Funktion. tests/kern/stammdaten.test.ts (47) und tests/isolation/stammdaten.test.ts (32, gegen einen Klon von w_stamm nachgefahren) laufen gruen, typecheck und eslint sind sauber, die eine Quelltextaenderung dieser Sitzung (belagsart.ts, alsLeistungswert am Diensteingang) entfernt nichts. Dagegen stehen 14 Befunde, davon 3 blockierend: der Bericht meldet zwei rote Kern-Tests (route-manifest.ts, dienste.ts) selbst, aber einen dritten nicht — tests/kern/katalog.test.ts ist rot, weil die 12 handgeschriebenen Auditaktionsnamen dem K-19-Pruefer als Rechteschluessel ohne Katalogzeile gelten; das ist allein der Schreibweise dieser Domaene geschuldet und in den eigenen Dateien zu beheben. Dazu vier wichtige Sachbefunde, die der Bericht nicht kennt: geaendert_felder im Pruefprotokoll ist in allen vier handgeschriebenen Schreibern strukturell falsch (snake_case gegen camelCase, mit Beweis in psql) und der Isolationstest deckt das mit not.toBeNull() ab; das Umdatieren einer Belagsart verliert lautlos die Beschreibung; Registerzeile O-692 und Docstring beschreiben ein Rueckdatierungsverhalten, das der eigene Test als abgewiesen festhaelt; und die 20-Zeichen-Grenze fuer Belagsart- und Reinigungsklassen-Codes ist eine erfundene Grenze ohne Datenbankschranke, ohne Quelle und ohne TODO(client). Die vier neuen offenen Fragen O-690 bis O-693 stehen nur im Routenbau-Register, nicht in docs/DECISIONS.md — der Hauswaechter meldet es namentlich.

## Vom Behebenden WIDERLEGT (Befund war falsch)

- TEILWEISE WIDERLEGT — Befund 10 (pruefeStufenkollision), Satz 'Gleich in antragsart.ts:210 und qualifikation.ts:234': fuer qualifikation ist die vorgeschlagene Behebung ('die Kollisionsmeldung aus dem Ausloeser erkennbar machen') NICHT ausfuehrbar, weil es dort keinen Ausloeser gibt. kern.katalog_schluessel_frei aus 0276 haengt ausschliesslich an abwesenheitsart und antragsart (trg_abwesenheitsart_schluessel_frei, trg_antragsart_schluessel_frei). BELEG: psql gegen w_stamm — `select tgname from pg_trigger where tgrelid='qualifikation'::regclass and not tgisinternal` liefert nur a_qualifikation_katalog_schutz, trg_qualifikation_kein_hard_delete, trg_qualifikation_kein_truncate, trg_qualifikation_geaendert_am; die einzige Schranke ist qualifikation_schluessel_key = UNIQUE NULLS NOT DISTINCT (mandant_id, schluessel), und die ERLAUBT denselben Schluessel einmal plattformweit und einmal je Gesellschaft. Die Lage ist damit schlechter als der Befund sagt: bei qualifikation ist die blinde Vorpruefung nicht nur ungenau, sie ist die EINZIGE Instanz. Code unveraendert gelassen, weil eine dritte Ausloesertabelle eine Regel waere, die niemand entschieden hat (0276 begruendet sich ausdruecklich mit dem ungefilterten Auswahlleser des Mitarbeiterportals fuer Antraege). Stattdessen steht die Lage jetzt im Docstring von pruefeStufenkollision in qualifikation.ts, ausdruecklich als 'Hinweis, keine Sperre'.
- NICHT WIDERLEGT, aber praezisiert — Befund 2 und 3 (route-manifest.ts, registry/dienste.ts) und Befund 13 (registry/modul.ts) stimmen sachlich vollstaendig; die Dateien stehen aber auf der Sperrliste dieses Auftrags ('die gemeinsamen Dateien registry/*, route-manifest.ts, ... nicht anfassen — Eintraege zurueckgeben'). Sie sind deshalb NICHT eingefuegt, sondern unten vollstaendig und wortgenau zurueckgegeben. Zusatzbefund: beide Tests sind derzeit auch fuer viele FREMDE Domaenen rot (routen.test.ts nennt u. a. api/bau/*, api/crm/*, api/datenschutz/*; portal-shell.test.ts nennt bau/*, crm/*, dokument/*), der Rueckstand ist also geteilt und wird zentral in einem Zug geschlossen.

## Nach der Behebung noch offen

- docs/DECISIONS.md, Abschnitt „Offen": die fuenf Zeilen O-690 bis O-694 (Feld decisions_zeilen). Solange sie fehlen, meldet `npx tsx scripts/guards/run-all.ts` fuer meine Dateien [todo-client-nicht-im-register] an sechs Stellen: abwesenheitsarten/page.tsx:41 (O-690), belagsarten/page.tsx:47 (O-692), qualifikationen/page.tsx:44 (O-341/O-343-Kontext), reinigungsklassen/page.tsx:41 (O-693), belagsart.ts:156 und reinigungsklasse.ts:128 (beide O-694). Sonst meldet der Waechter zu meinen Dateien NICHTS — keine tailwind-farbe, kein page-fremder-export.
- src/server/auth/route-manifest.ts: fuenf Zeilen (Feld registry_manifest). Bis dahin bleibt tests/kern/routen.test.ts rot.
- src/server/registry/dienste.ts: sechs Zeilen (Feld registry_dienste). Bis dahin bleibt tests/kern/portal-shell.test.ts rot.
- src/server/registry/modul.ts: eine Zeile in QUERSCHNITT (Feld registry_sonstiges). Kein Test erzwingt sie — modulAktiv gibt heute ueber den Rueckfall 'unbekanntes Modul bleibt offen' aus Versehen die richtige Antwort. Wer spaeter GEWERK_FUER_MODUL['stammdaten'] setzt, nimmt die drei Personal-Kataloge fuer security, bau und operations vom Bildschirm.
- tests/kern/katalog.test.ts bleibt rot wegen DREI FREMDER Funde, die ich nicht angefasst habe: nummernkreis.letzter_hash (src/app/portal/[mandant]/finanzen/hashkette/page.tsx), radar.stand_gesetzt (src/app/portal/[mandant]/radar/daten.ts), dienstplan.plan_veroeffentlicht (drizzle/0266_dienstplan_veroeffentlichung_definer.sql). Alle drei haben dieselbe Ursache wie mein blockierender Befund — ein Auditaktionsname ausserhalb des app.protokolliere(...)-Textes — und liessen sich mit derselben Handbewegung beheben.
- O-692 bleibt sachlich offen: der Bereich zwischen dem Beginn der laufenden Belagsart-Fassung und app.berlin_heute() ist weiterhin unbewacht. Behoben ist nur, dass Docstring, Seite und Registerzeile den Zustand jetzt richtig beschreiben; die strengere Variante (Beginn nie vor app.berlin_heute()) waere eine Zeile in pruefeDatierung und wartet auf die Kundenantwort.
- O-694 bleibt offen: es gibt heute KEINE Laengengrenze fuer die beiden Code-Spalten, weil die Tabellen keine fuehren. Falls der Kunde eine nennt, gehoert sie als CHECK in eine Migration UND in die beiden Dienste — nicht nur in den Dienst.

## NICHT gebaut, mit Grund

- Indexseite /portal/[mandant]/stammdaten — die Seitenkarte und routen.generiert.ts fuehren KEINE Indexroute (in dieser Sitzung erneut geprueft: nur die fuenf Blattrouten, Zeilen 216–220); eine gebaute Seite dort faellt in portalZugang auf 404. Einstieg sind stattdessen fuenf Karten in src/app/portal/[mandant]/einstellungen/page.tsx.
- Verwendungszaehlung je Abwesenheitsart — abwesenheit.abwesenheitsart_id ist cse_app spaltenweise ENTZOGEN (K-05, 0073); ein group by scheitert mit 42501, und eine Zaehlung je Art waere die Auskunft 'wie viele Krankmeldungen'. Die Seite sagt das und verlaesst sich auf kern.abwesenheitsart_schutz, dessen Meldungen der Dienst in Saetze uebersetzt. Der Plan hatte hier 'Reichweite je Zeile' zugesagt.
- Formularfeld fuer qualifikation.nachweis_art_id — tote Spalte (KRITIK bestaetigt: 0 Codetreffer); ein Feld pflegte einen Wert, auf den sich nichts stuetzt.
- Rueckverweise AUS den Nachbardomaenen (Raumbuch -> Belagsarten/Reinigungsklassen, Nachweisregister -> Qualifikationen, Antragseingang -> Antragsarten). Die fuenf Seiten verweisen rechtegeprueft NACH draussen; die Gegenrichtung liegt in Dateien anderer Domaenen, an denen parallel gearbeitet wird.
- Streichung von 'stammdaten.verwalten' in tests/kern/katalog-unbenutzt.ts:191 — die KRITIK weist nach, dass der Schluessel dem Pruefer schon als benutzt gilt (22 Funde) und kein Test das Streichen erzwingt. Gemeinsame Datei, paralleler Zugriff: bewusst gelassen.
- tests/e2e-Spec fuer die fuenf Seiten — die Browsersuite darf ich nicht starten; alle data-cse-Anker sind gesetzt (arten, antragsarten, belagsarten, klassen, qualifikationen, *-leer, *-offen, *-gesperrt, *-bearbeiten, stammdaten-hinweis).

## Notizen des Bauender

FORTGESETZTE SITZUNG. Der Bau der fuenf Routen lag beim Abbruch (Nutzungslimit) schon im Verlauf (Commit fbe8dc4), zusammen mit einer Registerdatei docs/architecture/routenbau/register/stammdaten.md. Ich habe diese Zusagen NICHT geglaubt, sondern nachgeprueft — und dabei genau einen echten Fehler gefunden und behoben. Meine einzige Quelltextaenderung in dieser Sitzung ist src/server/services/stammdaten/belagsart.ts (16 Zeilen +, 1 -).

DER GEFUNDENE FEHLER (behoben). `datiereBelagsartUm` reichte `e.leistungswert` unveraendert an `$4::numeric` weiter. Normalisiert hat nur der Formularleser `pruefeBelagsartEingabe`; `BelagsartEingabe.leistungswert` ist aber ein blanker `string`, und der Dienst ist exportiert. Ein Aufrufer, der die Eingabe selbst zusammensetzt (Job, Import, Test), schickt `300,5` mit deutschem Komma — Postgres antwortet `invalid input syntax for type numeric: "300,5"`, also eine Meldung ueber einen Datentyp, wo ein Satz ueber ein Formularfeld hingehoert. Schlimmer: sie fiel ERST beim Einfuegen, NACHDEM die laufende Fassung schon zum Vortag geschlossen war. Behoben durch `alsLeistungswert(e.leistungswert)` am Diensteingang — dieselbe getestete Funktion (K-16, Invariante 6), auf `300.500` idempotent (tests/kern/stammdaten.test.ts:135 haelt das fest). Gefunden hat ihn erst der WIRKLICHE Isolationslauf; die Vorgaengersitzung hatte diese Faelle nur von Hand nachgestellt und darum 'gruen' gemeldet.

PRUEFSTAND, alles in dieser Sitzung selbst gefahren:
· Migrationslauf KANONISCH auf Port 55432, genau wie vorgegeben (drop/create w_stamm, fenster_schluessel gesetzt, `pnpm db:migrate`): laeuft bis `Migrationen angewendet.` durch, mit 0275–0278 als letzten vier Dateien. Kein fremder Fehler unterwegs.
· Zusaetzlich derselbe Lauf auf einem eigenen, frisch mit initdb angelegten Cluster — dort habe ich auch per psql bestaetigt, dass jedes Objekt aus 0275–0278 wirklich entsteht: app.belagsart_historie_lesen, kern.abwesenheitsart_schutz und kern.katalog_schluessel_frei als `security definer` mit Eigentuemer cse_definer, die Policies t_plattform_anlegen/t_plattform_aendern (abwesenheitsart, antragsart), at_definer, b_definer und beide Ausloeser.
· tests/kern/stammdaten.test.ts: 47/47 gruen.
· tests/isolation/stammdaten.test.ts: 32/32 gruen gegen echtes Postgres (Weg siehe tests-Feld) — vorher 31/32 mit dem oben beschriebenen Fehler.
· npx tsc --noEmit: 0 Fehler im ganzen Baum, also auch keiner in meinen Pfaden.
· eslint auf alle Stammdaten-Dateien plus die geaenderte einstellungen/page.tsx: sauber.
· npx tsx scripts/guards/run-all.ts: zu meinen Dateien meldet es AUSSCHLIESSLICH die vier erwarteten `todo-client-nicht-im-register` (O-690 bis O-693) — sie verstummen, sobald die vier Zeilen aus decisions_zeilen in docs/DECISIONS.md stehen. Keine `tailwind-farbe`, kein `page-fremder-export`. (Dieselbe Wache meldet aktuell 39 Verstoesse insgesamt, der Rest gehoert anderen Domaenen.)

ZUR INFRASTRUKTUR, damit es der naechste nicht als eigenen Fehler sucht: beim Sitzungsbeginn war auf Port 55432 kein Server erreichbar, und der Cluster unter /var/lib/postgresql/cse-test flappte — er startete, verlor binnen Sekunden seine postmaster.pid und fuhr sich dann selbst mit `data directory lock file is invalid` herunter. Der laufende Postgres steht in einem ANDEREN PID-Namensraum (pgrep sieht ihn nicht, psql erreicht ihn), mehrere Agenten greifen also gleichzeitig auf dieselbe Datenverzeichnis-Sperre zu. Ich habe daraufhin NICHT weiter am gemeinsamen Verzeichnis gedreht (angefasst habe ich dort nur eine einzige verwaiste postmaster.pid), sondern mir mit initdb einen eigenen Cluster auf Port 55442 gebaut, dort geprueft, und den kanonischen Lauf auf 55432 nachgeholt, als der gemeinsame Server wieder stand. Mein eigener Cluster ist danach gestoppt und geloescht; der gemeinsame laeuft.

WAS DIE KRITIK GEAENDERT HAT (sie galt, wo sie dem Plan widersprach):
· „Keine Migration noetig" war der falsche Befund. Ohne 0275 kann NIEMAND die sieben Plattform-Abwesenheitsarten aendern (t_katalog_pflege ist die einzige Schreibpolicy und hat keinen Super-Admin-Zweig, force RLS, cse_app haelt nur select/insert/update) — O-139 waere durch keine Oberflaeche beantwortbar geblieben, und fuenf der sieben Arten bleiben mit bezahlt = NULL unbenutzbar (pruefeArt wirft ArtUngeklaertFehler). Der Isolationslauf belegt beides: derselbe Aufruf scheitert als admin und gelingt als super_admin mit aal2.
· Die von der KRITIK zusaetzlich gefundene Falle (unique nulls not distinct erlaubt 'urlaub' zweimal, der Auswahlleser filtert nur archiviert_am) ist als 0276 geschlossen, in BEIDEN Richtungen und als security definer, damit die Pruefung auch die Zeile einer fremden Gesellschaft sieht.
· belagsart: die Historie ist ueber app.leistungswerte_lesen nicht darstellbar (nur Stichtagsscheibe) und direkt nicht lesbar (K-05-Spaltenentzug) — daher 0277 mit stammdaten.verwalten statt objekt.lesen. Die Rollenmengen sind wirklich verschieden (objekt.lesen haengt auch an `kunde`), objekt.lesen im Definer haette den Katalog also dem Kundenportal geoeffnet, gegen p_intern_decke.
· reinigungsklasse: der Wirkungstext des Plans stuetzte sich auf die falschen Dateien. Einziger Leser in src/ ist raumbuch/import.ts; revier.ts und kalkulation/raumbuch.ts lesen belagsart ueber app.leistungswerte_lesen. Die Seite sagt das jetzt so.
· antragsart: vier Schalter erreichen das Mitarbeiterformular, erzeugt_abwesenheit wirkt bei der Entscheidung, ist_stammdatenaenderung nur im SQL-Ausloeser. Steht als SCHALTER-Liste im Dienst (nicht in der Seite, weil eine page.tsx nur exportieren darf, was Next kennt) und ist im Unit-Test festgeschrieben.
· qualifikation: nachweis_art_id ist kein Formularfeld; die Sperrwirkung wird gegen app.einsatz_qualifikation_erfuellt (0031) belegt.
· Jede der fuenf neuen API-Routen braucht einen Eintrag in route-manifest.ts, sonst faellt tests/kern/routen.test.ts — siehe registry_manifest. Nachgeprueft: jede der fuenf Routen ruft `authorize()` selbst, in derselben gebundenen Transaktion wie den Dienst (db().begin -> withTenant -> authorize -> Dienst), der Handler bleibt duenn.

EIGENER BEFUND UEBER DEN PLAN HINAUS (0278): kern.abwesenheitsart_schutz prueft „ist diese Art benutzt" mit den Rechten des Aufrufers gegen abwesenheit. Unter force RLS bekommt eine Sitzung ohne zeit.abwesenheit_lesen dort `false` — die Sperre gegen das Umbenennen einer benutzten Art und gegen das Kippen ihrer Art-9-Einstufung verschwand also lautlos genau fuer die Pflegesitzung, die nur stammdaten.verwalten haelt. Jetzt security definer mit Eigentuemer cse_definer (Policy ab_definer und Tabellenrecht bestehen seit 0073). Die Funktion gibt nach wie vor nichts zurueck als new oder eine Meldung; der Art-9-Grund bleibt hinter app.abwesenheit_grund_lesen.

WAS AN DIESEN SEITEN EIN PLATZHALTER BLEIBT (Regel 1, alles in der Oberflaeche sichtbar): bezahlt / nachweis_pflicht_ab_tagen / lohnart_schluessel je Abwesenheitsart -> „offen (O-139)"; der Bestand der Antragsarten -> O-142 vor der Liste; Leistungswerte -> „unbestaetigt (O-17)" plus O-349 fuer Glasflaechen; Reinigungsklassen -> „unbestaetigt (O-55)"; standard_gueltigkeit_monate -> „Frist offen (O-341)", erfordert_dokument -> „offen (O-343)", Kategorie -> O-144, Artenliste -> O-107. Vier NEUE Fragen sind O-690 bis O-693; ihre TODO(client, O-NNN)-Zeilen stehen in den Seiten (abwesenheitsarten:41, qualifikationen:44, belagsarten:41, reinigungsklassen:41).

FUER EINE „Entschieden"-SEKTION in docs/DECISIONS.md (nicht Offen), wenn Du sie fuehrst:
· D-…: Plattformweite Personal-Kataloge pflegt der Super-Admin mit zweitem Faktor — dasselbe Muster wie qualifikation (§6.16), nachgezogen fuer abwesenheitsart und antragsart (0275). ist_system-Antragsarten bleiben fuer JEDEN fest.
· D-…: Ein Katalogschluessel gehoert einer Stufe. Plattform und Mandant duerfen denselben aktiven Schluessel nicht fuehren (0276), weil der Auswahlleser des Mitarbeiterportals nicht nach Stufe filtert.
· D-…: Der Leistungswert wird nie ueberschrieben, sondern umdatiert; Richtigstellung (Name/Quelle) und neue Fassung (Wert) sind zwei getrennte Aktionen und zwei getrennte Formulare. Und er wird IM DIENST normalisiert, nicht nur im Formularleser — ein exportierter Dienst, der auf eine schon gepruefte Zeichenkette hofft, verschiebt den Fehler nach Postgres.
· D-…: Eine Zahl, die eine Sitzung nicht lesen DARF, wird als „nicht lesbar" gezeigt und nie als 0 (Reichweite der Qualifikationen, Antragsbestand, Raeume/Importzeilen je Klasse). Eine 0 aus einer leeren RLS-Antwort waere eine Aussage ueber den Bestand statt ueber die Rechte — und genau darauf stuetzte sich dann die Entscheidung ueber blockiert_einsatz oder ein Archivieren.

NICHT AUSGEFUEHRT (untersagt): pnpm test, pnpm test:isolation, pnpm test:e2e, pnpm db:seed, pnpm db:triggers, pnpm build, pnpm db:migrate gegen cse_dev/cse_test, git add/commit. Die Seeddaten der fuenf Kataloge kommen aus Migration (abwesenheitsart, antragsart) bzw. src/server/db/seed/qualifikation.ts und reinigung.ts; die Pflegewege sind stattdessen auf leeren und auf befuellten Katalogen geprueft (harness.seed() im Isolationslauf).

## Notizen des Behebender

PRUEFSTAND NACH DER ARBEIT
- npx tsc --noEmit -p tsconfig.json: SAUBER, keine Ausgabe (zwischendurch standen dort Fehler aus bau/ und crm/, die parallel arbeitende Agenten selbst behoben haben — aus meinen Dateien kam zu keinem Zeitpunkt einer).
- npx eslint ueber alle beruehrten Dateien: sauber.
- tests/kern/stammdaten.test.ts: 55/55 gruen (vorher 47 — acht neue Faelle: lange Codes bei Belagsart und Reinigungsklasse, siebenstellige Ganzzahl, siebenstelliger Leistungswert, alsStufenkollision erkennt/ignoriert, und zwei fuer die wirkliche Rueckwirkungsgrenze aus O-692).
- tests/kern/katalog.test.ts: 11/12; die 12 stammdaten-Funde sind weg, die verbleibenden 3 sind fremd.
- tests/isolation/stammdaten.test.ts: 33/33 gruen, einzeln gegen die frisch migrierte Datenbank w_stamm gelaufen (eigene vitest-Konfiguration im Scratchpad, ohne globalSetup, damit keine fremde Arbeiterdatenbank angefasst wird) — keine volle Suite, kein pnpm test:isolation.
- Migration erneut gegen die eigene Datenbank geprueft (siehe registry_sonstiges #3).
- npx tsx scripts/guards/run-all.ts: zu meinen Dateien ausschliesslich [todo-client-nicht-im-register] fuer O-690..O-694 — erwartet, solange docs/DECISIONS.md gesperrt ist. Keine tailwind-farbe, kein page-fremder-export.

ZWEI ENTSCHEIDUNGEN, DIE ICH ERKLAEREN MUSS
1) Bei Befund 7 habe ich von den zwei angebotenen Wegen den ERSTEN gewaehlt (Grenze entfernen) statt des zweiten (Grenze als Platzhalter fuehren). Grund: eine Grenze als Platzhalter zu fuehren hiesse, weiter eine Zahl abzuweisen, die niemand entschieden hat — nur mit besserem Etikett. Die Tabelle fuehrt keine, also fuehrt der Dienst jetzt auch keine; die offene Frage steht als O-694 mit TODO(client, O-694) an beiden Stellen. Dieselbe Logik bei den uebrigen stillen Obergrenzen: sie sind nicht entfernt, sondern auf die ECHTE Grenze der Spalte gezogen (int4 bzw. numeric(10,3)), und die Meldung nennt sie jetzt — vorher sagte sie „ist keine ganze Zahl" ueber eine ganze Zahl.
2) Bei Befund 9 habe ich fuer den 42501-Fall den zweiten Vorschlag genommen (eigener Zweig in reinigungsklasse.ts) statt alsStammdatenFehler einen tabellenabhaengigen Satz mitzugeben. Grund: alsStammdatenFehler ist von allen fuenf Katalogen geteilt, und sein 42501-Satz ist fuer die vier ZWEISTUFIGEN Tabellen richtig. Ihn parametrisierbar zu machen haette die richtige Erklaerung von der falschen nur durch ein weiteres Argument getrennt; ein eigener Zweig in der einen Datei, die keine Plattformstufe hat, sagt dasselbe an der Stelle, an der es gilt.

WAS AUFGEFALLEN IST, ABER NICHT MEINS WAR
Die drei verbleibenden Funde in katalog.test.ts (nummernkreis.letzter_hash, radar.stand_gesetzt, dienstplan.plan_veroeffentlicht) haben EXAKT dieselbe Ursache wie mein blockierender Befund: ein Auditaktionsname, der ausserhalb des `app.protokolliere(...)`-Textes steht und deshalb von Regel (2) in scripts/katalog/benutzung.ts nicht herausgeschnitten wird. Wer die drei anfasst, braucht dieselbe Handbewegung — Namen als Literal in den SQL-Text ziehen, Katalog nicht anfassen. Bei dienstplan.plan_veroeffentlicht liegt der Name in einer Migration (0266), dort ist es vermutlich ein `execute format(...)` oder ein Ausdruck statt eines Literals.

DESIGN
Kein neuer Gestaltungswert. Verwendet wurden ausschliesslich vorhandene Tokens: text-danger, text-warning, text-text-muted, text-xs, s2-s7, border-line, bg-surface/-2/-3. Die Statuspille wurde ENTFERNT und nicht durch ein neues Pill-Wort ersetzt — DESIGN.md §5 fuehrt kein Wort fuer „gesperrt", und die Reihenfolge ist DESIGN zuerst, nie umgekehrt.
