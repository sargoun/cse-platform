# Registereinträge: kundenportal

**Warteschlange, kein Archiv.** Diese Einträge sind **noch nicht** im Baum. Die
gemeinsamen Dateien pflegt EINE Hand, weil mehrere Agenten gleichzeitig arbeiten
und sich sonst in dieselbe Zeile schreiben. Ist ein Abschnitt eingetragen, wird
er hier gelöscht — solange er hier steht, fehlt er dort.

**Die Einträge des Behebungsschritts gelten.** Er lief zuletzt und hatte den
Auftrag, die vollständige aktuelle Liste zu liefern — auch das, was sich seit
dem Bauschritt geändert hat.

## Stand

- Bau: fertig
- Kritik: 18 Befunde
- Behebung: 14 behoben, 2 widerlegt, 6 offen

## Migrationen (gegen eine eigene Datenbank gefahren: True)

- drizzle/0255_kundenportal_nachricht.sql — nachricht.kunde_id (+ zusammengesetzter FK auf kunde(mandant_id,id)), Teilindex nachricht_kunde_idx, permissive t_kunde auf nachricht, restriktive p_kunde_decke auf nachricht (app.portal() <> 'kunde' or eigener Kunde + nicht geloescht + richtung <> 'intern'), permissive t_kunde auf nachricht_empfaenger (nur ansprechpartner des eigenen Kunden, wortgleich mit dem Kundenzweig von p_beteiligt). Keine neuen Grants, kein Schreibweg.
- drizzle/0256_kundenportal_rechnung_snapshot.sql — p_intern_ceiling auf rechnung_snapshot wird ERSETZT durch p_portal_decke mit Kundenzweig (nur eigene festgeschriebene Belege, Schreibzweig bleibt 'intern'), plus permissive t_kunde. rechnung_hash, rechnungsausgangsbuch und rechnungsposition_quelle bleiben unberuehrt.
- (Behebung) drizzle/0255_kundenportal_nachricht.sql — ERWEITERT: `nachricht_empfaenger` bekommt `kunde_id` (+ zusammengesetzter FK `ne_kunde_fk`), `kopf_richtung` (`nachricht_richtung`, not null) und `kopf_geloescht_am`, je mit `comment on column`; `kern.nachricht_empfaenger_kopf_denormalisieren()` als `before insert or update`-Ausloeser; `kern.nachricht_kopf_fortschreiben()` als `after update on nachricht`, `security definer set search_path = pg_catalog, public` (Begruendung woertlich nach dem Vorbild `kern.ln_kopfstatus_fortschreiben()` in 0066: `cse_app` darf ueber `t_empfaenger_eigene_stempeln` nur die EIGENE Zeile stempeln, ein Ausloeser als Aufrufer liesse die uebrigen stumm auf dem alten Stand); Bestandsbackfill `update nachricht_empfaenger e … from nachricht n`; `alter column kopf_richtung set not null`; Teilindex `ne_kunde_idx`; neue restriktive `p_kunde_decke on nachricht_empfaenger` in der Form `app.portal() <> 'kunde' or (kunde_id = any(app.aktuelle_kunden()) and kopf_geloescht_am is null and kopf_richtung <> 'intern')`. Der bestehende `t_kunde`-Lesepfad bleibt unveraendert; die falsche Zusage der alten Begruendung („Waere sie weiter, sperrte die Decke den Ueberschuss ohnehin") ist durch die Messung ersetzt.
- (Behebung) drizzle/0256_kundenportal_rechnung_snapshot.sql — UNVERAENDERT.

## Gebaute Adressen

- `/portal/kunde` — fertig
  - Uebersicht neu gebaut: kundePortal-Huelle (nach Vorbild meinPortal), sechs rechtegepruefte Sprungkarten mit echten Zahlen aus EINER Abfrage (kundenUebersicht). Die Sprungkarten sind heute der einzige Weg zu Projekte/Reklamationen/Zahlungen — die Kunden-Tableiste fuehrt nur fuenf Ziele und hat kein 'Mehr'. Stichtag aus app.berlin_heute() (K-11), nicht new Date().
- `/portal/kunde/nachrichten` — fertig
  - DataTable mit Betreff, Richtung ('Von Ihnen'/'An Sie' statt der Enumworte aus Haussicht), Zeitpunkt in Europe/Berlin aus der Datenbank, Gesellschaft je Zeile, Anlagenzahl, Gelesen-Zustand als StatusPill (Wartet/Inaktiv). Absender wird NICHT aufgeloest (§8: keine Namen). Kein Verfassen-Knopf, Offen-Hinweis O-74.
- `/portal/kunde/nachrichten/[id]` — fertig
  - Kopf + Volltext (whitespace-pre-line, kein HTML) + der ganze Faden (ladeFaden) darunter. Fremde Nachricht: notFound(), nie 403 — gegen echtes Postgres geprueft (findeKundennachricht auf den internen Vermerk gibt null). Anlagen: Zahl sichtbar, Download nicht, weil dokument keine permissive t_kunde hat — Offen-Hinweis O-671 statt eines Verweises auf 404.
- `/portal/kunde/nachweise` — fertig
  - Leistungsnachweise UND Aufmasse in einer Liste, zusammengefuehrt und sortiert per union all + order by in der Datenbank (nicht in TypeScript). EIGENE schlanke Signaturprojektion statt ladeSignaturen: kein breitengrad/laengengrad/zeitabweichung_sek/geraete_zeit/ip/user_agent/snapshot, Name nur bei rolle='auftraggeber'. Unterschriften aller Zeilen in EINER Abfrage (= any($1::uuid[])). Offen-Hinweis O-89.
- `/portal/kunde/projekte` — fertig
  - Nummer, Bezeichnung, Art, Vertragsgrundlage (VOB/B|BGB), Soll-Ende, Fertig am, Aufmasszahl, Gesellschaft, Status als Pille. Spalte 'Leistungsverzeichnisse' bewusst WEG: leistungsverzeichnis traegt kein t_kunde, die Unterabfrage zaehlt im Kunden-Scope stumm 0 (nachgemessen). Keine Auftragssumme, kein Verantwortlicher. Im Demo 0 Zeilen (Portalkunde ist Reinigungskunde) — Leerzustand sagt das ausdruecklich.
- `/portal/kunde/projekte/[id]` — fertig
  - Kopfkarte + genau eine Unterliste (Aufmasse des Projekts mit Messdatum, Erhebungsart, Zeilenzahl, Gegenzeichnung inkl. Vorbehalt). Bautagebuch wird gar nicht abgefragt (p_intern_einsatz_decke); Nachtrag und Behinderung tragen laut pg_policies BEIDE p_intern_decke — der Plan hielt sie fuer 'nur fachlich offen', die Datenbank hat es schon entschieden. Kein Platzhalterabschnitt, stattdessen O-78 und O-674.
- `/portal/kunde/rechnungen` — fertig
  - Spaltenliste gegen die echte Tabelle korrigiert: leistung_von/leistung_bis (kein Feld 'leistungszeitraum'), Storno ueber rechnung_beziehung (kein storniert_durch/storniert_am). Brutto UND Zahlbetrag, weil beide bei einer Schlussrechnung verschieden sind. Gesellschaftsfilter (O-52), erscheint nur bei >1 Lieferant — kein Mandantenwechsler. Keine Nummernkreis-, Hash- oder Ausgangsbuchspalte.
- `/portal/kunde/rechnungen/[id]` — fertig
  - Beleg vollstaendig: Kopf, Positionen (Menge durch formatiereMenge/mengeAusPostgres), Steuerzeilen JE Steuersatzgruppe, Zuschlaege, Summen, Zahlbetrag, Zahlungsziel, Skonto, Reverse-Charge, Fusstext. Nichts wird in der Komponente gerechnet. 0256 oeffnet rechnung_snapshot eng fuer den Kunden — der im Code vorgemerkte Blocker ist damit weg (im Kunden-Scope jetzt 19 statt 0 Snapshots, nachgemessen). Zwei eigene Kundenausgaberouten mit withKundeScope; authorize bekommt den Bereich der Rechnung, weil app.hat_recht(recht, null) im Kunden-Scope false ist.
- `/portal/kunde/reklamationen` — fertig
  - Statusfilter ueber searchParams (Enumwert als Parameter, nie im SQL-Text), Nummer, Objekt, Eingang, Prioritaet, Frist ('offen (O-14)' wenn keine gesetzt), Gesellschaft, Status als Pille. ursache und verantwortlich_benutzer_id stehen in der Projektion NICHT; 'quelle' ebenfalls weg. Offen-Hinweis O-74.
- `/portal/kunde/reklamationen/[id]` — fertig
  - Nummer als h1, Kopfkarte, Beschreibung im Volltext, 'Was wir getan haben' (massnahme), Verweis auf den bestrittenen Nachweis nur mit nachweis.lesen+bau.lesen (AUT-06). Verlauf/Abstellung wird nicht gezeigt. Offen-Hinweis O-74 mit dem Hinweis, dass eine Bestaetigung rechtlich die Abnahme der Nacharbeit ist.
- `/portal/kunde/zahlungen` — fertig
  - KRITIK umgesetzt: bezahlt_cent und offen_cent kommen DIREKT aus offener_posten, NICHT als Summe ueber zahlung_zuordnung — eine Zuordnung zu einer stornierten Zahlung ist fuer den Kunden sichtbar, der Storno (zahlung.storniert_am) liegt hinter p_intern_ceiling, und die Summe haette Posten 5a841421 als bezahlt gemeldet (im Demo real vorhanden, nachgemessen). Stichtagswahl, drei Summenkacheln (in der Datenbank gebildet), Altersklasse aus klasseFuer, Zustand offen/teilweise/ausgeglichen. Kein 'bezahlt am' (O-672), keine Mahnstufe (O-673). Demo zeigt alle drei Zustaende (18 Posten).

## src/server/registry/dienste.ts

"In `src/server/registry/dienste.ts`, am Ende von `DIENSTE`:\n\n```ts\n  /**\n   * **Das Kundenportal (AUT-01, CRM-06, 04-SEITENKARTE §8).** Acht Dienste,\n   * alle LESEND — und das ist keine Momentaufnahme, sondern der Typ: jeder\n   * bekommt einen `KundenAbfrage` mit genau einer Methode (`abfrage`), und\n   * `kundePortal` reicht ihm einen `LeseKontext` ohne `schreibe`. Ein\n   * Schreibversuch aus einer Kundenseite ist damit ein Compilerfehler und\n   * keine Laufzeitentscheidung; ob ein Kundenzugang im Portal ueberhaupt\n   * schreiben darf, ist O-74.\n   *\n   * Kein `schreibRecht` an einer der acht Zeilen — dann greift die erste\n   * Pruefung („jeder schreibende Dienst nennt sein Schreibrecht\") gar nicht.\n   */\n  { modul: 'kundenportal', pfad: 'kundenportal/basis', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/nachricht', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/nachweis', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/projekt', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/rechnung', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/reklamation', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/uebersicht', schreibend: false },\n  { modul: 'kundenportal', pfad: 'kundenportal/zahlung', schreibend: false },\n```\n\nDanach `npx vitest run tests/kern/portal-shell.test.ts` — die acht Namen verschwinden aus der Fehlerliste."

## src/server/auth/route-manifest.ts

"In `src/server/auth/route-manifest.ts`, bei den uebrigen Finanz-Ausgaberouten (die interne Schwester steht in Zeile 1052):\n\n```ts\n  {\n    /**\n     * Der EIGENE Beleg des Kunden als ZUGFeRD (FIN-11, FIN-12, DOC-03).\n     *\n     * Eine eigene Route neben `api/finanzen/rechnungen/[id]/zugferd.pdf`, weil\n     * die interne mit 401 abbricht, sobald `sitzung.aktiverMandantId` null ist\n     * — und im Kunden-Scope ist sie das IMMER (K-20). Dieselbe Datei aus\n     * derselben Quelle (`rechnung_snapshot`, K-12); was sich unterscheidet,\n     * ist ausschliesslich, WER lesen darf.\n     *\n     * Dasselbe Recht wie intern: `authorize` bekommt den Bereich aus der\n     * angefragten Rechnung (`mandantZurRechnung`), weil `app.hat_recht(recht,\n     * null)` im Kunden-Scope auf alles `false` antwortet. Eine fremde Kennung\n     * liefert `null` und damit 404, noch bevor ein Recht gefragt wird\n     * (AUT-06).\n     */\n    pfad: 'api/kunde/rechnungen/[id]/zugferd.pdf',\n    recht: 'finanzen.herunterladen',\n  },\n  {\n    /**\n     * Derselbe Beleg als XRechnung (UBL). Gleiches Recht, gleiches Tor,\n     * gleiche Quelle — nur das Format wechselt.\n     */\n    pfad: 'api/kunde/rechnungen/[id]/xrechnung.xml',\n    recht: 'finanzen.herunterladen',\n  },\n```\n\nDanach `npx vitest run tests/kern/routen.test.ts` — die beiden Zeilen verschwinden aus der Fehlerliste (die uebrigen gehoeren anderen Stapeln)."

## src/server/db/schema/rls.ts

"Keine Eintraege. `src/server/db/schema/rls.ts` fuehrt Loeschsperren je TABELLE; `nachricht_empfaenger` ist keine neue Tabelle (0011/0231) und bekommt in 0255 nur Spalten, Ausloeser und Policies. `nachricht` steht bereits dort. Nachgesehen, nicht angenommen: `grep -n \"nachricht\" src/server/db/schema/rls.ts`."

## src/server/registry/navigation.ts

"In `src/server/registry/navigation.ts`, neben `GRUPPEN_NAVIGATION`:\n\n```ts\n/**\n * Der Navigationsbaum des KUNDENPORTALS — das, was hinter `Mehr` steht.\n *\n * **Warum es ihn braucht.** Die Kundenleiste fuehrt fuenf Ziele; gebaut sind\n * acht Bildschirme. `projekte`, `zahlungen` und `reklamationen` waren damit\n * gebaut und aus KEINER Leiste erreichbar — die Sprungkarten der Uebersicht\n * reichen einen Schritt weit, und von `/portal/kunde/rechnungen/[id]` kommt\n * man ohne Adresszeile gar nicht zu den Zahlungen.\n *\n * **`recht` ist EIN Schluessel, zwei Routen verlangen zwei.**\n * `/portal/kunde/rechnungen` fuehrt im Manifest\n * `[\"finanzen.lesen\",\"finanzen.herunterladen\"]`, `/portal/kunde/nachweise`\n * fuehrt `[\"nachweis.lesen\",\"bau.lesen\"]`, und `pruefeZugang` verknuepft sie\n * mit UND. Ein Punkt, der nur das erste prueft, fuehrte auf 404 und verriete\n * die Existenz dessen, was er nicht zeigen darf (AUT-06, D-581). Deshalb das\n * optionale `zusatzRecht` unten — und nicht, weil ein zweites Feld huebscher\n * waere.\n */\nexport const KUNDEN_NAVIGATION: readonly NaviEintrag[] = [\n  { schluessel: 'uebersicht', label: 'Übersicht', pfad: '', recht: 'bericht.dashboard_lesen', icon: 'uebersicht' },\n  { schluessel: 'auftraege', label: 'Aufträge', pfad: 'auftraege', recht: 'auftrag.lesen', icon: 'auftrag' },\n  { schluessel: 'projekte', label: 'Bauprojekte', pfad: 'projekte', recht: 'bau.lesen', icon: 'aufmass' },\n  { schluessel: 'rechnungen', label: 'Rechnungen', pfad: 'rechnungen', recht: 'finanzen.lesen', zusatzRecht: 'finanzen.herunterladen', icon: 'rechnung' },\n  { schluessel: 'zahlungen', label: 'Zahlungen', pfad: 'zahlungen', recht: 'zahlung.lesen', icon: 'euro' },\n  { schluessel: 'nachweise', label: 'Nachweise', pfad: 'nachweise', recht: 'nachweis.lesen', zusatzRecht: 'bau.lesen', icon: 'dokument' },\n  { schluessel: 'reklamationen', label: 'Reklamationen', pfad: 'reklamationen', recht: 'qualitaet.lesen', icon: 'qualitaet' },\n  { schluessel: 'nachrichten', label: 'Nachrichten', pfad: 'nachrichten', recht: 'nachricht.lesen', icon: 'mail' },\n];\n```\n\nUnd dazu das eine neue Feld an `NaviEintrag` (additiv, alle bestehenden Eintraege bleiben gueltig):\n\n```ts\n  /**\n   * Ein ZWEITER Rechteschluessel, wenn die Zielroute zwei Leserechte fuehrt.\n   *\n   * `pruefeZugang` verknuepft die Leserechte einer Route mit UND. Ein\n   * Menuepunkt, der nur eines prueft, ist der 404 aus AUT-06 mit Ansage.\n   */\n  readonly zusatzRecht?: string;\n```\n\n`angebote`, `objekte` und `dokumente` stehen bewusst NICHT darin: die Seiten\ndahinter gehoeren anderen Stapeln und sind heute Platzhalter (`[...rest]`).\n`auftraege` steht darin, weil es heute schon in der Tableiste steht und in\nderselben Auffangroute endet — der Punkt wird also nicht schlechter, er wandert\nnur."

## Sonstiges

"DREI Dateien, damit die Kundenleiste ein `Mehr` bekommt und `KUNDEN_NAVIGATION` dahinter erscheint. Alle drei sind ohne einander nicht uebersetzbar, also bitte zusammen einspielen.\n\n**(1) `src/server/registry/tableiste.ts`** — der Block `schluessel: 'kunde'` wird ersetzt (fuenf bleiben fuenf: `Auftraege` weicht in das `Mehr`-Blatt):\n\n```ts\n  {\n    schluessel: 'kunde',\n    familie: 'kunde',\n    /*\n     * MIT `Mehr` — im Gegensatz zu `mitarbeiter` und `gruppe` (OHNE_MEHR).\n     * Das Kundenportal fuehrt acht Bildschirme und die Leiste vier; ohne das\n     * fuenfte Ziel waeren `projekte`, `zahlungen` und `reklamationen` gebaut\n     * und aus keiner Leiste erreichbar. `Auftraege` weicht dafuer in das\n     * Blatt: es ist heute ohnehin die Auffangroute („wird noch gebaut\").\n     */\n    ziele: [\n      { schluessel: 'uebersicht', label: 'Übersicht', pfad: '', recht: 'bericht.dashboard_lesen', icon: 'uebersicht' },\n      { schluessel: 'rechnungen', label: 'Rechnungen', pfad: 'rechnungen', recht: 'finanzen.lesen', icon: 'rechnung' },\n      { schluessel: 'nachweise', label: 'Nachweise', pfad: 'nachweise', recht: 'nachweis.lesen', icon: 'dokument' },\n      { schluessel: 'nachrichten', label: 'Nachrichten', pfad: 'nachrichten', recht: 'nachricht.lesen', icon: 'mail' },\n      MEHR,\n    ],\n  },\n```\n\n**(2) `src/components/portal/TabLeiste.tsx`** — `MehrZelle` kennt heute genau zwei Baeume. Die Zeile\n\n```ts\n  const punkte = (gruppenansicht ? GRUPPEN_NAVIGATION : NAVIGATION)\n    .filter((n) => rechte[n.schluessel] === true);\n```\n\nwird zu\n\n```ts\n  const baum = kundenansicht\n    ? KUNDEN_NAVIGATION\n    : (gruppenansicht ? GRUPPEN_NAVIGATION : NAVIGATION);\n  const punkte = baum.filter((n) => rechte[n.schluessel] === true);\n```\n\nmit `kundenansicht?: boolean` an den Eigenschaften von `MehrZelle` und `TabLeiste`, `KUNDEN_NAVIGATION` im Import, und im `map` ueber `gezeigt`:\n`<MehrZelle … kundenansicht={kundenansicht} … />`.\n\n**(3) `src/components/portal/PortalRahmen.tsx`** — zwei Stellen. Die Schiene bekommt fuer die Kundenleiste denselben Baum wie das Blatt:\n\n```tsx\n        <SeitenNavigation\n          ziele={leiste === 'intern_global' || leiste === 'intern_admin'\n            || leiste === 'intern_leitung'\n            ? NAVIGATION\n            : leiste === 'kunde' ? KUNDEN_NAVIGATION : tabs.ziele}\n```\n\nund die Sichtbarkeitskarte fuer `leiste === 'kunde'` aus `navigationsRechte` statt aus `sichtbareTabs` (die kennt nur die vier Tabs):\n\n```tsx\n          {...(leiste === 'intern_global' || leiste === 'intern_admin'\n            || leiste === 'intern_leitung' || leiste === 'kunde'\n            ? (navigationsRechte === undefined ? {} : { sichtbar: navigationsRechte })\n            : (sichtbareTabs === undefined ? {} : { sichtbar: sichtbareTabs }))}\n```\n\nund an `<TabLeiste … kundenansicht={leiste === 'kunde'} … />`.\n\n**(4) `src/app/portal/zugang.ts`** — sonst ist das Blatt LEER, weil die Rechte nie gefragt werden. Zwei Stellen:\n\n```ts\n    const gefragt = [...new Set([\n      ...ziele.map((z) => z.recht).filter((r): r is string => r !== null),\n      ...NAVIGATION.map((n) => n.recht),\n      ...GRUPPEN_NAVIGATION.map((n) => n.recht),\n      /*\n       * Und die des Kundenbaums. Es sind dieselben Modulrechte wie intern\n       * (`finanzen.lesen`, `nachweis.lesen`, …), aber `zusatzRecht` kommt hier\n       * neu dazu; ohne es bliebe der Punkt „Rechnungen\" sichtbar fuer eine\n       * Sitzung, die dahinter 404 bekaeme.\n       */\n      ...KUNDEN_NAVIGATION.flatMap((n) => n.zusatzRecht === undefined\n        ? [n.recht] : [n.recht, n.zusatzRecht]),\n    ])];\n```\n\n```ts\n    const navigationsRechte: Record<string, boolean> = {};\n    const baum = sitzung.ansicht === 'gruppe'\n      ? GRUPPEN_NAVIGATION\n      : (sitzung.ansicht === 'kunde' ? KUNDEN_NAVIGATION : NAVIGATION);\n    for (const n of baum) {\n      navigationsRechte[n.schluessel] =\n        gehalten.has(n.recht) && frei(n.recht)\n        && (n.zusatzRecht === undefined\n            || (gehalten.has(n.zusatzRecht) && frei(n.zusatzRecht)));\n    }\n```\n\nNach diesen vier Bloecken greift auch die bestehende Probe „jeder Punkt zeigt auf eine Route, die es GIBT\" fuer die neuen Eintraege, und die `aktiverTab`-Schluessel `zahlungen`, `projekte` und `reklamationen` (in diesem Lauf schon gesetzt) heben von allein den richtigen Punkt hervor."

## Zeilen für docs/DECISIONS.md, Abschnitt „Offen"

| O-670 | **Wird eine Portalnachricht dem Kunden sofort sichtbar, sobald sie angelegt ist — oder braucht sie eine ausdrückliche Freigabe?** `0255` öffnet den Kundenlesepfad auf `nachricht` über `t_kunde` und deckelt ihn mit `p_kunde_decke`. **Heute gilt:** sichtbar, sobald `kunde_id` gesetzt ist, `richtung <> 'intern'`, `geloescht_am is null` und eine Empfängerzeile auf einen Ansprechpartner dieses Kunden existiert. `gesendet_am is not null` steht bewusst NICHT in der Bedingung: für `kanal = 'portal'` geht nichts „hinaus" (`kern.nachricht_sendetor()` kehrt für diesen Kanal sofort zurück), und wäre der Zeitpunkt Bedingung, sähe der Kunde je nach Schreibpfad ALLES oder NICHTS. Offen ist die kaufmännische Frage: darf eine Sachbearbeiterin einen Text vorbereiten, den der Kunde noch nicht sieht — und wenn ja, woran erkennt die Datenbank ihn? | `drizzle/0255`, `services/kundenportal/nachricht.ts`, NOT-03, SPEC §22 `nachricht` |
| O-671 | **Dürfen Anhänge einer Kundennachricht im Portal heruntergeladen werden — oder bleiben Anlagen dem Mailweg vorbehalten?** `dokument` trägt die Kundendecke `p_kunde_ceiling` auf `sichtbar_fuer_kunde`, aber KEINE permissive `t_kunde`; im Kunden-Scope liefert die Tabelle deshalb null Zeilen. Die Nachrichtenseite zeigt darum die ANZAHL der Anlagen und keinen Verweis — ein Knopf stünde vor einem 404. Dieselbe Frage stellt der Dokumentweg aus einer anderen Richtung (`services/dokument/kundenfreigabe.ts`, `drizzle/0297`), und sie gehört einmal beantwortet: bekommt `dokument` eine eng gefasste permissive `t_kunde` auf `sichtbar_fuer_kunde` (die Decke dafür steht bereits), oder nicht? | `drizzle/0255`, `drizzle/0297`, `services/kundenportal/nachricht.ts`, DOC-01, DOC-03 |
| O-672 | **Soll dem Kunden das Valutadatum seiner eigenen Zahlung angezeigt werden — oder genügt „ausgeglichen am"?** `zahlung` trägt `p_intern_ceiling` und kein `t_kunde`; im Kunden-Scope liefert die Tabelle null Zeilen, und damit ist auch kein Valutadatum lesbar. `zahlung_zuordnung` führt `betrag_cent` und `art`, aber kein Datum — `erstellt_am` der Zuordnung wäre der BUCHUNGSzeitpunkt und nicht der Zahlungseingang, also eine Zahl, die aussieht wie eine Antwort und eine andere Frage beantwortet. **Heute gezeigt:** `offener_posten.ausgeglichen_am`, der Tag, an dem der Posten geschlossen wurde. Ein „bezahlt am" verlangte einen eng gefassten Kundenlesepfad auf die eigenen Zahlungseingänge. | `services/kundenportal/zahlung.ts`, `app/portal/kunde/zahlungen/page.tsx`, ACC-04, FIN-15 |
| O-673 | **Sieht ein Kunde seinen eigenen Mahnstand im Portal — Stufe, Datum, Gebühr —, oder bleibt das Mahnwesen ein Vorgang per Post und Mail?** `offener_posten.letzte_mahnstufe` ist im Kunden-Scope lesbar, `mahnung` selbst nicht. Eine Stufe ohne das Schreiben dahinter ist eine Drohung ohne Text, deshalb zeigt die Zahlungsseite sie heute NICHT. Die Frage ist kaufmännisch und nicht technisch: das Mahnwesen ist ein Eskalationsweg, und ob er im Selbstbedienungsportal sichtbar wird, entscheidet die Geschäftsführung. | `services/kundenportal/zahlung.ts`, `app/portal/kunde/zahlungen/page.tsx`, ACC-04 |
| O-674 | **Sieht der Auftraggeber seine eigenen Nachträge (VOB/B § 2) und die an ihn gerichteten Behinderungsanzeigen (VOB/B § 6) im Portal?** `nachtrag` und `behinderung` tragen BEIDE die restriktive `p_intern_decke` mit `app.portal() = 'intern'` und kein `t_kunde` (in `pg_policies` nachgesehen, nicht vermutet) — die Datenbank hat die Frage also schon entschieden, genauso wie beim Bautagebuch (`p_intern_einsatz_decke`). Die offene Frage ist deshalb nicht „anschliessen oder nicht", sondern „soll eine Migration die Decke öffnen": ein Nachtrag berührt Geld, eine Behinderungsanzeige ist eine empfangsbedürftige Erklärung — beides braucht eine Entscheidung, bevor eine Decke geöffnet wird. Die Kundenprojektseite zeigt darum heute nur Kopfdaten und die Aufmaße des Projekts. | `services/kundenportal/projekt.ts`, `app/portal/kunde/projekte/[id]/page.tsx`, BAU-02, BAU-03, VOB/B §§ 2, 6 |

## Befunde des Prüfers (18)

- **blockierend** · `/home/user/cse-platform/src/server/services/kundenportal/rechnung.ts` — `Rechnungsposition.nettoCent` ist als `string` typisiert und die Belegseite rechnet `formatiereGeld(cent(BigInt(p.nettoCent)))` ohne Wache — aber `rechnungsposition.netto_cent` ist NULLABLE, und zwar nicht zufällig: der CHECK `rp_ohne_leistung_ohne_betrag` ERZWINGT NULL für jede Position mit `positionsart <> 'leistung'`, also für `textzeile` und `zwischensumme`. Eine Rechnung mit einer Gliederungs- oder Textzeile (Normalfall bei einer Schlussrechnung aus einem Leistungsverzeichnis) lässt `/portal/kunde/rechnungen/[id]` mit `TypeError: Cannot convert null to a BigInt` auf 500 laufen — der Kunde sieht seinen eigenen Beleg gar nicht mehr. `menge` und `einzelpreis_cent` (dieselbe Constraint, dieselbe Nullbarkeit) sind auf der Seite korrekt abgefangen, `netto_cent` nicht. Der interne Weg macht es richtig: `src/server/services/finanz/rechnung.ts:937` typisiert `netto_cent: string | null`, :1056 mappt `p.netto_cent === null ? null : cent(...)`, und die interne Seite rendert :525 `p.netto_cent === null ? '—' : …`. Der Kundendienst hat diese Nullbarkeit verloren. Heute nicht auslösbar, weil der Seed nur `positionsart='leistung'` führt — genau der spät entdeckte Ausfall, den CLAUDE.md meint.
  - Behebung: In `kundenportal/rechnung.ts` `nettoCent: string | null` (und `satzBp` bleibt, der ist not null) und in `[id]/page.tsx` die Netto-Zelle wie die Einzelpreis-Zelle absichern: `p.nettoCent === null ? <span className="text-text-subtle">—</span> : formatiereGeld(cent(BigInt(p.nettoCent)))`. Dazu in `tests/kern/kundenportal.test.ts` eine Quelltextwache oder besser eine Fixtur-Position mit `positionsart='textzeile'` in der Isolationsprobe, damit die Nullbarkeit nicht ein zweites Mal verloren geht.
- **wichtig** · `/home/user/cse-platform/src/app/portal/kunde/page.tsx` — Die sechs Sprungkarten prüfen je GENAU EIN Recht, das Routen-Manifest verlangt für zwei Ziele aber ZWEI — und `pruefeZugang` verknüpft die Leserechte einer Route mit UND. `/portal/kunde/rechnungen` führt `lesen: ["finanzen.lesen","finanzen.herunterladen"]`, die Karte filtert nur auf `finanzen.lesen`; `/portal/kunde/nachweise` führt `lesen: ["nachweis.lesen","bau.lesen"]`, die Karte filtert nur auf `nachweis.lesen`. Eine Anmeldung, die das zweite Recht nicht hält, sieht die Karte und bekommt dahinter 404 — genau das, was der eigene Kopfkommentar der Datei ausschliesst („Ein Verweis, dessen Ziel diese Sitzung nicht oeffnen darf, verraet die Existenz dessen, was er nicht zeigen darf (AUT-06, D-581) — und dahinter stuende ein 404"). Derselbe Fehler in `zahlungen/page.tsx`: der Verweis auf `/portal/kunde/rechnungen/[id]` hängt an `basis.rechte['finanzen.lesen']`, die Route verlangt zusätzlich `finanzen.herunterladen`. Dass es richtig gehen kann, zeigt `reklamationen/[id]/page.tsx:72`, das für denselben Zweck BEIDE Rechte abfragt. Heute hält die Seed-Rolle `kunde` alle sechs, der Befund ist also latent — aber die Rechtevergabe ist das, was sich als Erstes ändert.
  - Behebung: Pro Karte eine Rechte-LISTE statt eines Schlüssels führen und `sichtbar` mit `every` filtern; am robustesten die Liste nicht abschreiben, sondern aus dem Manifest ziehen (`leserechte(findeRoute(z.pfad))` aus `src/server/registry/routen.ts` — die Funktion existiert genau dafür) und alle so gewonnenen Schlüssel an `kundePortal(..., rechte)` übergeben. In `zahlungen/page.tsx` `darfRechnung` auf `finanzen.lesen && finanzen.herunterladen` erweitern und `finanzen.herunterladen` in die Rechteliste des Aufrufs aufnehmen.
- **wichtig** · `/home/user/cse-platform/drizzle/0255_kundenportal_nachricht.sql` — Das neue permissive `t_kunde` auf `nachricht_empfaenger` (Zeile 201) verengt NUR den Empfänger und sagt über die Eltern-Nachricht nichts — und es gibt auf dieser Tabelle keine Kundendecke ausser `p_beteiligt`, deren Kundenzweig WORTGLEICH ist. Die Zusage der Migration („Waere sie weiter, sperrte die Decke den Ueberschuss ohnehin") ist damit nachweislich falsch: gemessen in einer echten Kundensitzung sieht der Kunde 5 Empfängerzeilen, darunter die zum internen Vermerk `richtung='intern'` („INTERN: Kunde zahlt schlecht"), die zur weich gelöschten Nachricht und die zu einer Nachricht mit `kunde_id IS NULL` — während `nachricht` selbst korrekt nur 2 Zeilen zeigt. Herausgegeben werden damit Existenz, `erstellt_am`, `zugestellt_am` und `gelesen_am` interner und zurückgezogener Vorgänge; die drei Bedingungen, die `p_kunde_decke` auf `nachricht` sorgfältig setzt (eigener Kunde, nicht gelöscht, nicht intern), fehlen auf der Kindtabelle vollständig. Die Isolationsprobe deckt das nicht ab: Test (10) prüft nur, dass ein interner MITEMPFÄNGER unsichtbar ist — also die Empfänger-, nicht die Nachrichtendimension. Heute zeigt keine Seite diese Tabelle direkt (der `exists` für „gelesen" ist über `n.id` gebunden), RLS ist hier aber die zweite Verteidigungslinie und hält nicht, was sie behauptet.
  - Behebung: Die Elternfakten auf die Kindtabelle denormalisieren, wie dieses Schema es an derselben Stelle schon tut (`leistungsnachweis_signatur.kopf_status/kopf_storniert_am/kunde_id`, `aufmass_zeile.kopf_status/kopf_storniert_am`): `nachricht_empfaenger` bekommt `kunde_id`, `kopf_richtung` und `kopf_geloescht_am`, per Auslöser aus `nachricht` gefüllt, und `t_kunde` plus eine neue restriktive `p_kunde_decke` prüfen sie (`kunde_id = any(app.aktuelle_kunden()) and kopf_richtung <> 'intern' and kopf_geloescht_am is null`). Ein `exists (select 1 from nachricht …)` ist hier wirklich unmöglich (Ringverweis über `p_beteiligt`) — das ist in der Migration korrekt begründet und bleibt der Grund für die Denormalisierung. Dazu drei Tests: Empfängerzeile zu interner, zu weich gelöschter und zu kundenloser Nachricht je 0 Zeilen.
- **wichtig** · `/home/user/cse-platform/src/server/auth/route-manifest.ts` — Die beiden neuen Kundenausgaberouten fehlen im Route-Manifest, und `tests/kern/routen.test.ts` („keine Route fehlt im Manifest") fällt deswegen und nennt sie beide beim Namen. Der Bericht führt die Manifestzeilen nur als Text mit der Begründung, Registerdateien würden zentral gepflegt — `src/server/auth/route-manifest.ts` ist aber kein erzeugtes Register, sondern handgeschriebener Auth-Code mit Prosa-Begründung je Zeile (der Kopfkommentar sagt: „Eine Route, die absichtlich offen ist, steht mit `oeffentlich: true` drin"). Sicherheitlich ist nichts offen — beide Routen rufen `authorize` mit `recht: 'finanzen.herunterladen'` —, aber die Aufzählungsprüfung, die genau diese Zusage für ALLE Routen hält, ist rot, und „Definition of done: tests pass" ist damit nicht erfüllt.
  - Behebung: Die zwei Einträge aus dem Berichtsabschnitt `registry_manifest` nach `src/server/auth/route-manifest.ts` übernehmen (`pfad: 'api/kunde/rechnungen/[id]/zugferd.pdf'` und `'…/xrechnung.xml'`, je `recht: 'finanzen.herunterladen'`), dann `npx vitest run tests/kern/routen.test.ts` — die beiden Zeilen verschwinden aus der Fehlerliste (die übrigen gehören anderen Stapeln).
- **wichtig** · `/home/user/cse-platform/src/server/registry/dienste.ts` — Keiner der acht neuen `kundenportal/*`-Dienste steht im Dienstregister, und der Gegentest `tests/kern/portal-shell.test.ts` („und das Register kennt jeden Dienst, der existiert") fällt und listet alle acht auf. Der Bericht liefert die Zeilen als Text; die Datei ist aber genau das Register, dessen Vollständigkeit die Gruppenansichts-Zusage von PR 8 trägt („Diese Zusage über eine Liste zu prüfen, die jemand pflegt, wäre eine Erinnerung; über ein Register geprüft ist sie eine Eigenschaft").
  - Behebung: Die acht Zeilen aus dem Berichtsabschnitt `registry_dienste` samt Blockkommentar einfügen, alle mit `schreibend: false` (kein `schreibRecht` — dann greift auch die erste Prüfung „jeder schreibende Dienst nennt sein Schreibrecht" nicht).
- **wichtig** · `/home/user/cse-platform/docs/DECISIONS.md` — Die fünf neuen offenen Fragen O-670 bis O-674 stehen in Migrationskommentaren, in `TODO(client, …)`-Zeilen der Dienste UND als „offen (O-672)" / „offen (O-671)" im sichtbaren Text der Kundenseiten — aber `docs/DECISIONS.md` ist unangetastet, und keine der fünf Nummern existiert dort. CLAUDE.md verlangt für jede offene Geschäftsregel ausdrücklich „Record it in docs/DECISIONS.md under 'Open'" und „Definition of done: … DECISIONS.md updated with anything assumed". Es ist keine Formalie: die bestehende Wache `scripts/guards/run-all.ts` prüft genau diese Kopplung und endet mit Exit 1, sie nennt vier Dienststellen und zwei Migrationsstellen dieses Stapels. Zusätzlich sind die Nummern frei erfunden über die bestehende Reihe hinaus — höchste vorhandene Nummer ist O-596 —, also kann ein Nachbarstapel dieselben 670er noch einmal vergeben.
  - Behebung: Die fünf Zeilen aus dem Berichtsabschnitt `decisions_zeilen` unter „Offen" in `docs/DECISIONS.md` eintragen, vorher aber die Nummern auf die nächsten wirklich freien ziehen (ab O-597) und in allen sechs Quellstellen plus den vier Seitentexten mitziehen. Danach `npx tsx scripts/guards/run-all.ts` — die sechs Treffer dieses Stapels müssen verschwinden.
- **wichtig** · `/home/user/cse-platform/src/server/registry/tableiste.ts` — Die Kunden-Tableiste führt weiterhin fünf Ziele ohne `Mehr` (Übersicht, Aufträge, Rechnungen, Nachweise, Nachrichten) und `KUNDEN_NAVIGATION` existiert nicht — `projekte`, `zahlungen` und `reklamationen` sind damit gebaut und aus KEINER Leiste erreichbar. Der Bauende nennt das selbst und verweist auf die Sprungkarten der Übersicht; die reichen aber nur EINEN Schritt: auf `/portal/kunde/zahlungen` steht in der Leiste kein Weg zurück ausser „Übersicht", und von `/portal/kunde/rechnungen/[id]` kommt man ohne Adresszeile gar nicht zu den Zahlungen. Das ist genau die Lage, die der Plan unter Punkt 5 als gemeinsame Arbeit dieses Stapels benennt („Projekte, Reklamationen und Zahlungen sind damit gebaut, aber unerreichbar"), und der Bericht hat den fertigen Text dafür — er ist nur nicht angewandt.
  - Behebung: Die drei Textblöcke aus `registry_sonstiges`/`registry_navigation` anwenden: `MEHR` in die `kunde`-Leiste (Aufträge weicht, fünf bleiben fünf), `KUNDEN_NAVIGATION` in `src/server/registry/navigation.ts`, und die eine Zeile in `PortalRahmen.tsx` (`leiste === 'kunde' ? KUNDEN_NAVIGATION : …` plus `leiste === 'kunde'` in der Bedingung, die die Schiene rendert). Danach greift auch die bestehende Probe „jeder Punkt zeigt auf eine Route, die es GIBT" für die neuen Einträge.
- **wichtig** · `/home/user/cse-platform/src/app/portal/kunde/rechnungen/[id]/page.tsx` — Der ganze Ausgabeweg — Migration 0256, die beiden neuen API-Routen, der Snapshot-Lesepfad, also das aufwendigste Stück dieses Stapels — ist im Betrieb nirgends erreichbar und von keinem Test durchlaufen. `belegAusgabe` antwortet für JEDE Rechnung des Portalkunden `unvollstaendig`, weil BT-10 (Käuferreferenz/Leitweg-ID) bei 27 von 27 festgeschriebenen Rechnungen fehlt und BT-49 (elektronische Adresse des Empfängers) bei 23 von 27; BT-10 ist unabhängig von `leitwegPflicht` Pflicht, und `ist_pflicht` ist im Seed bei allen `false`. Die Seite rendert damit immer den Erklärsatz und NIE einen der beiden Knöpfe. Die Isolationsprobe prüft nur, dass der Snapshot lesbar ist, nicht dass die Route antwortet; einen Routentest gibt es nicht. CLAUDE.md „Definition of done: … seed data exercises it" ist für diesen Teil also nicht erfüllt, und die Zusage „im Kunden-Scope jetzt 19 statt 0 Snapshots" belegt die Policy, nicht die Auslieferung.
  - Behebung: Für den Portalkunden `kunde.elektronische_adresse` (+ EAS-Schema) und `rechnung.kaeufer_referenz` mindestens auf einer Rechnung seeden — der Bauende führt genau diese Zeilen unter `nicht_gebaut`. Zusätzlich eine Probe, die die Route selbst aufruft (Kundensitzung → 200 mit `application/pdf`, fremde Kennung → 404, fehlende Pflichtangabe → 422); ohne sie ist `authorize` mit `mandantZurRechnung` nur durch eine Quelltextwache gedeckt.
- **klein** · `/home/user/cse-platform/src/app/portal/kunde/zahlungen/page.tsx` — `aktiverTab="uebersicht"` auf einer Seite, die nicht die Übersicht ist. `PortalRahmen` reicht den Wert als `aktiv` an die Leiste weiter, die den Tab damit als aktuelle Seite markiert (`aria-current`) — ein Screenreader liest auf `/portal/kunde/zahlungen` „Übersicht, aktuelle Seite", und visuell steht der falsche Tab hervor. Dasselbe in `projekte/page.tsx`, `projekte/[id]/page.tsx`, `reklamationen/page.tsx` und `reklamationen/[id]/page.tsx`. Ursache ist die zu kurze Leiste (eigener Befund), die Wahl „dann eben Übersicht" macht daraus aber eine falsche Aussage statt einer fehlenden.
  - Behebung: Den eigenen Schlüssel übergeben (`aktiverTab="zahlungen"`, `"projekte"`, `"reklamationen"`). Ein Schlüssel, den die Leiste nicht führt, hebt keinen Tab hervor — das ist der ehrliche Zustand, bis das `Mehr` steht; danach stimmt es von allein.
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/nachweis.ts` — `kundenUnterschriften` sortiert mit `order by lokal` — also nach der ANZEIGEFORM `DD.MM.YYYY HH24:MI` als Text. Das ist genau der Fehler, den dieselbe Datei 150 Zeilen weiter oben ausdrücklich vermeidet und begründet („die Anzeigeform daneben ist Berliner Ortszeit und taugt nicht zum Sortieren („01.08.2026“ vor „28.02.2026“)"). Unterschriften eines Nachweises erscheinen dadurch in falscher Reihenfolge, sobald sie über einen Monats- oder Jahreswechsel liegen: „01.09.2026" sortiert vor „28.08.2026". Sichtbar auf der Nachweisliste und in der Spalte „Gegenzeichnung" der Projektseite, wo die Reihenfolge Auftragnehmer/Auftraggeber die Aussage trägt.
  - Behebung: Eine Sortierspalte mitführen und darauf ordnen: in beiden Zweigen `s.unterzeichnet_am as sort_ts` selektieren und `order by sort_ts` (das Feld danach in `alsZeile`/der Projektion nicht weitergeben oder als unbenutzt belassen). Die Anzeigeform bleibt, was sie ist.
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/zahlung.ts` — `op.ausgeglichen_am` ist eine `date`-Spalte, wird aber mit `at time zone 'Europe/Berlin'` behandelt, als wäre sie ein Zeitpunkt. Postgres castet den `date` dafür über die SITZUNGSZONE nach `timestamptz` und rechnet dann nach Berlin — das Ergebnis hängt also an `TimeZone` der Verbindung. Heute stimmt es zufällig, weil die Sitzung in UTC läuft und Berlin davor liegt; mit einer Zone östlich von Berlin kippt der Tag. Dieselbe Abfrage macht es zwei Zeilen höher bei `faellig_am` richtig (`to_char(op.faellig_am, 'DD.MM.YYYY')`), die Spalte ist also nur an einer Stelle anders behandelt als ihr Typ.
  - Behebung: `to_char(op.ausgeglichen_am, 'DD.MM.YYYY') as ausgeglichen_lokal` — ein Kalendertag braucht keine Zonenumrechnung, genau wie `faellig_am` in derselben Abfrage. (Sonst im Haus korrekt: alle `at time zone`-Stellen dieses Ordners sitzen auf echten `timestamptz`-Spalten — `eingang_am`, `faellig_am` der Reklamation, `festgeschrieben_am`, `unterzeichnet_am`, `gesendet_am`/`erstellt_am`.)
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/zahlung.ts` — Die drei Summenkacheln und die Liste darunter können auseinanderlaufen, gleich dreifach: (1) `kundenZahlungsstand` bricht bei `limit 200` ab, `kundenZahlungsSummen` summiert ohne Grenze über alle Posten — die Kachel nennt dann Geld zu Zeilen, die nicht auf dem Bildschirm stehen; (2) `sum(op.offen_cent)` zählt auch negative Restbeträge aus Überzahlungen mit und senkt damit die Forderung unter die Summe der offenen Zeilen (dass Überzahlungen real möglich sind, sagt die eigene Funktion `zustandVon` mit `<= 0` und der CHECK `op_ausgleich_stimmig`, der `ausgeglichen_am` in diesem Fall NULL erzwingt); (3) der Überfällig-Filter hier (`ausgeglichen_am is null and stichtag - faellig_am >= 0`) ist ein anderer als der in `kundenUebersicht` (dieselbe Bedingung PLUS `offen_cent > 0`) — zwei Bildschirme, zwei Zahlen für „überfällig".
  - Behebung: Denselben Filter in beiden Diensten führen (`offen_cent > 0` auch in `kundenZahlungsSummen`), die Offen-Kachel als `sum(op.offen_cent) filter (where op.offen_cent > 0)` bilden, und die Grenze entweder in beiden anwenden oder auf der Seite sagen, dass die Liste die 200 jüngsten Posten zeigt.
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/nachweis.ts` — `aufmasseZumProjekt` liefert als `bezug` die Bezeichnung des PROJEKTS (`p.bezeichnung`), und die Projektseite zeigt sie in einer Spalte mit dem Kopf „Bezeichnung". Jede Zeile der Aufmaßtabelle wiederholt damit denselben Projektnamen, der eine Karte höher schon als Überschrift steht, während `aufmass.bezeichnung` — `not null`, mit eigener CHECK gegen Leerstring — nie gelesen wird. Der Kunde sieht also für jedes Aufmaß nicht, WAS gemessen wurde.
  - Behebung: In `aufmasseZumProjekt` `a.bezeichnung as bezug` selektieren (die Projektzugehörigkeit steht ohnehin fest, weil die Abfrage auf `a.projekt_id = $1` verengt ist). Im Sammelzweig von `listeKundennachweise` bleibt `p.bezeichnung` richtig — dort ist die Spalte „Objekt / Projekt".
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/nachweis.ts` — Die Spalte heisst „Objekt / Projekt", der Leistungsnachweis-Zweig löst aber nur `objekt` auf und setzt `projekt_id` fest auf `null::uuid`. `leistungsnachweis` darf nach der CHECK `ln_ein_anker` an einem Objekt ODER an einem Projekt hängen — ein am Projekt verankerter Nachweis (der Bau-Fall, den die Seite mit „Leistungsnachweise der Gebäudereinigung und Aufmaße des Baus" ausdrücklich mit abdeckt) zeigt deshalb „—", obwohl die Zugehörigkeit in der Zeile steht. Nebenbei ist `leistungsnachweis.nummer` nullable und ohne Constraint für `vorgelegt`/`signiert`, während die Projektion `nummer: string` behauptet — die Zelle bleibt dann leer statt „ohne Nummer".
  - Behebung: Im Leistungsnachweis-Zweig zusätzlich `left join projekt pr on pr.mandant_id = l.mandant_id and pr.id = l.projekt_id` und `coalesce(o.bezeichnung, pr.bezeichnung) as bezug`, dazu `l.projekt_id` statt `null::uuid`, damit der Verweis auf die Projektseite auch hier greift. `nummer` als `string | null` typisieren und in der Zelle einen Ersatztext setzen.
- **klein** · `/home/user/cse-platform/src/server/services/kundenportal/nachricht.ts` — Der Gelesen-Zustand wird für JEDE Nachricht aus den Empfängerzeilen des Kunden gebildet, also auch für `richtung = 'eingehend'` — eine Nachricht, die der KUNDE geschrieben hat. Die Liste zeigt dann „Von Ihnen" und daneben die Pille `Wartet`, und die Zeile zählt in „N ungelesen" der Kopfzeile sowie in `nachrichtenUngelesen` der Übersichtskarte mit. Im Demobestand sind dadurch 2 von 2 Nachrichten „ungelesen", eine davon die eigene Rückfrage des Kunden — der Kunde soll seine eigene Nachricht lesen.
  - Behebung: Den Zustand nur für die Richtung bilden, in der er etwas bedeutet: `case when n.richtung = 'ausgehend' then exists (…) else true end as gelesen` — und in `uebersicht.ts` den Ungelesen-Zähler auf `n.richtung = 'ausgehend'` einschränken. Damit trägt die eigene Nachricht keine Pille, die eine Handlung verlangt.
- **klein** · `/home/user/cse-platform/src/app/portal/kunde/bausteine.tsx` — `mt-[2px]` ist ein ad hoc gesetzter Abstandswert. DESIGN.md führt die Abstände als `s1`…`s7`, und CLAUDE.md verlangt ausdrücklich „no one-off padding" beziehungsweise „If something is missing, add it to DESIGN.md first, then use it". Die Datei begründet drei Zeilen höher selbst, dass hier nichts erfunden wird („Kein Hex, keine eigene Groesse") — und setzt dann eine eigene Grösse. Im ganzen Baum gibt es dafür genau einen weiteren Fall.
  - Behebung: Die optische Ausrichtung über die Typografie lösen statt über einen Pixelwert — `items-start` plus `leading`-passendes Icon oder `groesse="md"` im `flex`-Container, wie `Hinweis` es an anderen Stellen tut. Wird der Wert wirklich gebraucht, zuerst als Token in DESIGN.md aufnehmen.
- **klein** · `/home/user/cse-platform/src/app/portal/kunde/rechnungen/[id]/page.tsx` — Die fünf Detailseiten geben `kundePortal` das ROUTENMUSTER statt der konkreten Adresse (`'/portal/kunde/rechnungen/[id]'`). Der Mustervergleich in `findeRoute` trägt das noch, aber jede andere Portalseite im Haus übergibt die echte URL, und `portalZugang` legt den Wert per `merkeHuelle` als Rückweg der Sitzung ab — gespeichert wird dann `/portal/kunde/rechnungen/[id]`, eine Adresse, die es nicht gibt. Folgenlos ist das heute nur deshalb, weil der Sprachumschalter ausschliesslich für interne Leisten rendert; sobald das Kundenportal einen bekommt (oder etwas anderes den gemerkten Pfad benutzt), landet der Kunde auf einer Auffangroute.
  - Behebung: Die konkrete Adresse übergeben, also `` kundePortal(`/portal/kunde/rechnungen/${id}`, …) `` (nach `kennungOder404`, damit dort nie ein roher Pfadabschnitt landet) — analog in den vier anderen Detailseiten.
- **klein** · `/home/user/cse-platform/src/app/portal/kunde/rechnungen/[id]/page.tsx` — Die Zeile „Bereits berechnete Abschläge" setzt ein Minuszeichen fest vor den Betrag und prüft nur `abzugBruttoCent !== '0'`. Bei einer Stornorechnung ist `brutto_cent` negativ, und die CHECK `rechnung_abzug_vorzeichen` bindet das Vorzeichen von `abzug_brutto_cent` daran — der Abzug ist dort also selbst negativ, und die Seite zeigt „−-1.190,00 €". Auf einem Beleg ist ein doppeltes Minus keine Kosmetik, sondern eine Zahl, bei der der Leser rätselt, in welche Richtung sie wirkt.
  - Behebung: Das Vorzeichen aus der Zahl lesen statt es vorzuschreiben: den Betrag negiert formatieren (`formatiereGeld(cent(-BigInt(r.abzugBruttoCent)))`) oder das feste `−` weglassen und `formatiereGeld` das Vorzeichen setzen lassen. Die interne Belegansicht zeigt dieselbe Zeile — die Abbildung dort als Vorlage nehmen, damit beide denselben Beleg gleich lesen.

**Urteil:** Der Stapel ist substanziell und gut gebaut: alle 27 behaupteten Dateien existieren, `npx tsc --noEmit` meldet für `portal/kunde`, `kundenportal/*` und `api/kunde` KEINEN Fehler, jeder Spaltenname und JEDER Enumwert hält gegen echtes Postgres (`projekt.art/status/vertragsgrundlage`, `aufmass.status/erhebungsart`, `leistungsnachweis.status`, `reklamation.status/prioritaet`, `rechnungsart`, `rechnung_beziehung.art`, `nachricht_richtung/kanal`), 0255 und 0256 sind in `w_kupo_seed` angewandt, und die zentralen Zusagen habe ich in einer ECHTEN Kundensitzung (app.scope=kunde, benutzer 99ffdb4a…) nachgemessen und bestätigt: `app.hat_recht(x,null)=false` gegen `app.hat_recht(x,mandant)=true`, 19 Snapshots lesbar, `zahlung/op_ausgleich/mahnung/rechnung_hash/rechnungsausgangsbuch/rechnungsposition_quelle/leistungsverzeichnis/bautagebuch/nachtrag/behinderung/dokument` je 0 Zeilen, `insert into rechnung_snapshot` → permission denied, `update nachricht` → 0 Zeilen, nur die 2 eigenen Nachrichten sichtbar. `tests/kern/kundenportal.test.ts` läuft mit 65/65 grün.

Trotzdem 18 Befunde, davon einer blockierend und sieben wichtig. Der schwerste ist ein echter 500 auf der Rechnungsseite des Kunden: `rechnungsposition.netto_cent` ist per CHECK-Constraint für `positionsart <> 'leistung'` ZWINGEND NULL, der Kundendienst typisiert die Spalte aber non-null und die Seite ruft `BigInt()` darauf ohne Wache — genau die Stelle, die der interne Dienst und die interne Seite beide korrekt behandeln. Dazu zwei Verweise, die auf 404 zeigen können (die Sprungkarten prüfen ein Recht, wo das Routen-Manifest zwei verlangt), und eine Isolationslücke in 0255, die ich gemessen habe: `nachricht_empfaenger` gibt dem Kunden 5 Zeilen heraus, darunter die zum internen Vermerk, zur weich gelöschten und zur kundenlosen Nachricht — während `nachricht` korrekt nur 2 zeigt. Die Begründung in der Migration („die Decke sperrt den Ueberschuss ohnehin") ist nachweislich falsch, weil es auf dieser Tabelle keine Kundendecke ausser der wortgleichen `p_beteiligt` gibt; Test (10) prüft nur die Empfänger-, nicht die Nachrichtendimension.

Drei Registerlücken sind harte Torfehler, nicht Kosmetik: `tests/kern/routen.test.ts` und `tests/kern/portal-shell.test.ts` fallen NACHWEISLICH und nennen die zwei neuen API-Routen bzw. alle acht `kundenportal/*`-Dienste; `scripts/guards/run-all.ts` endet mit Exit 1 und nennt O-670…O-674 als „todo-client-nicht-im-register". Der Bauende hat die Zeilen als Text geliefert — `src/server/auth/route-manifest.ts` und `src/server/registry/dienste.ts` sind aber handgepflegte Dateien mit Prosa-Begründungen, keine erzeugten Register, und `docs/DECISIONS.md` ist unangetastet. (Anmerkung zur Beweislage: `tests/isolation/kundenportal.test.ts` konnte ich NICHT laufen lassen — das global-setup bricht beim Seed mit „new row violates row-level security policy for table leistungskatalog_position" ab, also an `drizzle/0298_leistungskatalog_status.sql` eines ANDEREN Stapels. Das ist kein Befund gegen diese Domäne; ich habe die Isolationszusagen deshalb direkt in psql gegengeprüft und dabei die Lücke in Befund 3 gefunden.)

Nicht gefunden habe ich: erfundene Geldbeträge, Fristen oder Steuersätze (jede Zahl kommt als Ganzzahltext aus der Datenbank, `prozentText` rechnet ganzzahlig, nichts wird in einer Komponente addiert), einen Schreibpfad, einen Hex-Code oder eine Farbe ausserhalb des Themas, eine vorgetäuschte Integration, oder eine entfernte Funktion — `git diff` zeigt an `src/app/portal/kunde/page.tsx` als einziger geänderter Datei dieser Domäne nur Ersatz des Platzhalters durch die Sprungkarten.

## Vom Behebenden WIDERLEGT (Befund war falsch)

- TEILWEISE WIDERLEGT — die Forderung aus dem DECISIONS-Befund, die Nummern O-670…O-674 „auf die naechsten wirklich freien" ab O-597 zu ziehen. Das waere der Fehler, den die Nummernvergabe gerade verhindern soll. `docs/architecture/routenbau/README.md:85-89` teilt die Bereiche ausdruecklich zu: „Ebenso zugeteilt sind die Nummern fuer offene Geschaeftsfragen, damit zwei Agenten nicht dieselbe O-NN fuer zwei verschiedene Fragen vergeben: O-600 ff. finanzen · O-610 personal · O-620 einstellungen · O-630 bau · O-640 datenschutz · O-650 aufgaben/nachrichten · O-660 crm · **O-670 kundenportal** · O-680 website · O-690 stammdaten · …". Der Auftrag dieses Laufs nennt denselben Block („Deine neuen O-Nummern: O-670 bis O-679"). Dass O-596 die hoechste Nummer IN docs/DECISIONS.md ist, sagt nichts ueber die Vergabe, sondern nur, dass die Datei zentral gepflegt wird und noch kein Nachbarstapel eingetragen ist: `npx tsx scripts/guards/run-all.ts` meldet heute 84 Verstoesse derselben Art, darunter O-640, O-645, O-650, O-651, O-660, O-661, O-662, O-710…O-713, O-730, O-732, O-734…O-736 und O-740 — jeder laufende Stapel steht an derselben Stelle. Ein Umnummerieren auf O-597 ff. kollidierte mit dem finanzen-Block. Ausserdem benutzt ein bereits gelandeter Nachbarstapel O-671 in DERSELBEN Bedeutung (drizzle/0297_dokument_kundenfreigabe.sql:55/63 und src/app/portal/[mandant]/dokumente/[id]/kundenfreigabe/page.tsx:31/248: „der Kundenzugang zu Dokumenten ist offen (O-671)") — ein Umnummerieren risse diese Verweise entzwei. Die Nummern bleiben, wie sie sind; der zweite Teil des Befundes (die fuenf Zeilen fehlen in docs/DECISIONS.md) stimmt und ist unter `decisions_zeilen` zurueckgegeben, weil die Datei zu den gemeinsamen gehoert.
- PRAEZISIERT, nicht widerlegt — der Ausgabeweg-Befund nennt als Ursache BT-10 (27 von 27) und BT-49 (23 von 27). Beide stimmen; die Behebung, die er vorschlaegt (nur `kunde.elektronische_adresse` + EAS und `rechnung.kaeufer_referenz` seeden), reicht aber NICHT: `rechnung.zahlungsmittel_code` hat keinen Vorgabewert und wird im ganzen Seed nirgends gesetzt, also fehlt BT-81 (BR-DE-1) auf jedem Demobeleg. Nachgestellt: mit nur den beiden genannten Feldern antwortete `belegAusgabe` weiterhin `{ art: 'unvollstaendig', anzahl: 1 }` und `zugferdZurRechnung` warf `XRechnungUnvollstaendigFehler … BT-81`. Erst mit der dritten Angabe steht `{ art: 'moeglich' }` und ein echtes PDF.

## Nach der Behebung noch offen

- Die fuenf DECISIONS-Zeilen O-670…O-674 muessen zentral in docs/DECISIONS.md unter „Offen" eingetragen werden (Text unter `decisions_zeilen`). Erst danach faellt `npx tsx scripts/guards/run-all.ts` fuer diesen Stapel nicht mehr: heute meldet er `[todo-client-nicht-im-register]` fuer src/server/services/kundenportal/nachricht.ts:199, …/projekt.ts:44, …/zahlung.ts:53, …/zahlung.ts:60, drizzle/0255_kundenportal_nachricht.sql:153 und :376.
- Die zwei Route-Manifest-Eintraege (`registry_manifest`) fehlen noch — `npx vitest run tests/kern/routen.test.ts` nennt `api/kunde/rechnungen/[id]/xrechnung.xml` und `api/kunde/rechnungen/[id]/zugferd.pdf` weiterhin beim Namen. Sicherheitlich ist nichts offen (beide Routen rufen `authorize` mit `recht: 'finanzen.herunterladen'` und dem Bereich aus `mandantZurRechnung`), aber die Aufzaehlungspruefung ist rot.
- Die acht Dienstregister-Zeilen (`registry_dienste`) fehlen noch — `npx vitest run tests/kern/portal-shell.test.ts -t "Register kennt jeden Dienst"` listet alle acht auf.
- Die Kunden-Tableiste hat weiterhin kein `Mehr`, und `KUNDEN_NAVIGATION` existiert nicht: `/portal/kunde/projekte`, `/portal/kunde/zahlungen` und `/portal/kunde/reklamationen` sind nur ueber die Sprungkarten der Uebersicht erreichbar, und von einer Detailseite fuehrt kein Weg quer. Die vier Textbloecke dafuer stehen unter `registry_navigation` und `registry_sonstiges` (tableiste.ts, navigation.ts, PortalRahmen.tsx, TabLeiste.tsx, zugang.ts). Bis sie stehen, heben die korrigierten `aktiverTab`-Schluessel (`zahlungen`, `projekte`, `reklamationen`) bewusst keinen Tab hervor — das ist der ehrliche Zustand.
- Die Isolationsproben dieses Stapels (jetzt 27 statt 19) konnten NICHT ausgefuehrt werden, weil `pnpm db:seed` auf dem aktuellen Stand abbricht: `PostgresError: new row violates row-level security policy for table "leistungskatalog_position"` in `legeKatalogHierarchieAn` (src/server/db/seed/vertrieb.ts:552). Das ist NICHT dieser Stapel — die Stelle gehoert vertrieb/stammdaten, `src/server/db/seed/vertrieb.ts` ist von einem Nachbarstapel geaendert, und der Abbruch trat schon vor jeder meiner Aenderungen auf (erster Seedlauf dieses Laufs, vor der ersten Zeile Code). `tests/isolation/global-setup.ts` ruft genau diesen Seed, um die Vorlage zu bauen, also steht die ganze Isolationssuite still. Ersatzweise ist jede Aussage der neuen Proben von Hand gegen echtes Postgres nachgemessen (Kundensitzung in w_kupo_seed, Ausgabeweg ueber den echten Rechnungsdienst in w_kupo) — die Ergebnisse stehen unter `behoben`.
- `npx vitest run tests/kern/api-verdrahtung.test.ts` faellt weiterhin mit `/api/mein/schichten/[zuordnungId]/leistungsnachweis/[id]/unterschrift` — eine Route des Mitarbeiterportal-Stapels, nicht dieses. Die beiden `api/kunde/...`-Routen sind verdrahtet (die Belegseite ruft sie) und stehen nicht mehr in der Liste.

## NICHT gebaut, mit Grund

- /portal/kunde/auftraege und /portal/kunde/auftraege/[id] — nicht in meinem Plan; bleiben von der Auffangroute [...rest] abgedeckt (sie steht bewusst weiter, weil 'Auftraege' in der Tableiste haengt und ohne sie ein sichtbarer Menuepunkt auf 404 fuehren wuerde).
- /portal/kunde/angebote, /objekte, /dokumente und deren Detailseiten — andere Stapel, ebenfalls von [...rest] abgedeckt.
- Seed-Zeilen fuer den Portalkunden (0 Projekte, 0 Aufmasse, 0 Nachrichten, kein kaeufer_eadresse): src/server/db/seed/** NICHT angefasst, weil pnpm db:seed nicht ausgefuehrt werden darf und ungepruefter Seedcode den Seed fuer alle bricht. Die genau benoetigten Zeilen stehen in notizen.
- KUNDEN_NAVIGATION und ein 'Mehr' in der Kunden-Tableiste — Registerdateien werden zentral gepflegt; Text steht in registry_navigation/registry_sonstiges. Erreichbar sind alle zehn Seiten trotzdem, ueber die Sprungkarten der Uebersicht.

## Notizen des Bauender

## Geprueft, nicht behauptet

Eigene Datenbanken auf 127.0.0.1:55432: `w_kupo` (leer, alle 196 Migrationen ab
null — laeuft bis „Migrationen angewendet." durch, zweimal gemacht, zuletzt nach
der letzten Aenderung), `w_kupo_seed` (Klon von `cse_test` + alle Migrationen,
also MIT Demodaten), `w_kupo_iso_w1` (fuer den Isolationslauf). `cse_dev`,
`cse_test`, `pnpm db:triggers`, `pnpm test`, `pnpm test:isolation`,
`pnpm test:e2e`, `pnpm db:seed`, `pnpm build`, `git add/commit` — nicht
angefasst. Der Postgres-Server war beim Start dieser Sitzung aus; er lief nur
mit `dangerouslyDisableSandbox` an, weil die Sandbox `postmaster.pid` nach dem
Aufruf wieder entfernt und der Server sich daraufhin selbst beendet („data
directory lock file is invalid").

Jede Abfrage lief in einer ECHTEN Kundensitzung (`app.scope=kunde`,
`app.portal=kunde`, `benutzer kunde.demo@example.test`), und zwar nicht nur als
SQL von Hand, sondern als Aufruf der Dienstfunktionen selbst (tsx-Skript im
Kratzverzeichnis, danach geloescht). Gemessen: 19 Rechnungen, 18
Debitorenposten (davon einer teilweise und einer voll ausgeglichen), 4
Reklamationen, 4 Leistungsnachweise, 2 Unterschriften, 0 Projekte, 0 Aufmasse,
19 `rechnung_snapshot` (vor 0256: 0).

Die KRITIK hat in allen Punkten recht gehabt, und die Korrekturen sind drin:
4 statt 2 Nachweise/Reklamationen, 18 statt 2 Posten, 3 sichtbare
`zahlung_zuordnung`, `ladeSignaturen` nicht kundensicher, `mandant`-Join fehlt
allen vier Diensten, `leistungszeitraum`/`storniert_durch` gibt es nicht,
`lv_anzahl` faellt im Kunden-Scope stumm auf 0, `nachtrag`/`behinderung` sind
per Policy schon zu, `dokument` fehlt nur die permissive `t_kunde`.

## Zwei Befunde, die im Plan nicht standen

**1. Der Snapshot ist lesbar, aber aus den Demorechnungen entsteht KEINE
E-Rechnung.** 0256 wirkt (19 Snapshots statt 0), und `ublZurRechnung` kommt bis
zur Pflichtfeldpruefung — dort fehlen fuer JEDE der 19 Rechnungen des
Portalkunden zwei Angaben: BT-49 (`kunde.kaeufer_eadresse`) und BT-10
(Kaeuferreferenz/Leitweg-ID, O-22). Ein Download-Knopf haette neunzehnmal eine
JSON-Fehlermeldung mit BT-Nummern geladen. Deshalb: neue Dienstfunktion
`belegAusgabe(kontext, id)`, die mit DERSELBEN Funktion prueft, die das
Dokument prueft (`fehlendePflichtfelder`), und die ZAHL zurueckgibt statt der
Liste; die Detailseite zeigt danach die Knoepfe oder einen Satz. Die
Rechnungsliste fuehrt deshalb bewusst KEINE Download-Spalte — die Pruefung
braucht die Snapshot-Nutzlast, und 200-mal parsen fuer eine Liste ist zu teuer.
Fuer eine vorzeigbare Demo genuegt `update kunde set kaeufer_eadresse =
'...@...', kaeufer_eadresse_schema = 'EM'` plus `kaeufer_referenz` am Kunden.

**2. `nachricht_empfaenger` gibt dem Kunden einen Rest an Metadaten, und das
laesst sich nicht schliessen, ohne die Datenbank zu brechen.** `t_kunde` (0255)
zeigt jede Empfaengerzeile, die auf einen Ansprechpartner des Kunden zeigt —
auch die einer Nachricht, die er nicht lesen darf (interner Vermerk, geloescht,
ohne `kunde_id`). Gemessen: 5 sichtbare Empfaengerzeilen bei 2 lesbaren
Nachrichten. Sichtbar sind dabei nur `nachricht_id`, `empfaenger_typ`,
`empfaenger_id` (seine eigene Kontaktperson), `art`, `gelesen_am` — kein
Betreff, kein Text. Die naheliegende Verengung (`exists` auf `nachricht`) ist
UNMOEGLICH: `p_beteiligt` auf `nachricht` fragt bereits
`nachricht_empfaenger`, und Postgres bricht den Ringverweis zur LAUFZEIT mit
„infinite recursion detected in policy" ab. Sauber schliessen liesse sich das
nur mit einem Definer neben `kern.nachricht_thread_wurzel` — und dessen
Spalten-Grant (`mandant_id, id, thread_id`) muesste um `kunde_id`, `richtung`
und `geloescht_am` erweitert werden, was Absicht von 0231 ist und nicht meine
Entscheidung. Der Dienst liest die Tabelle nie allein, sondern immer als
Unterabfrage der sichtbaren Nachricht; der Isolationstest haelt die Grenze
fest (Test 10: die Zeile eines internen Mitempfaengers bleibt unsichtbar).

## Was ich im Nachrichtendienst gebraucht haette (fuer 0230–0244)

Ein Lesepfad auf `nachricht` existierte unter `services/` bisher gar nicht —
`/portal/mein/nachrichten` liest `benachrichtigung`. Ich habe deshalb den
Kundenlesepfad in `src/server/services/kundenportal/nachricht.ts` gebaut
(`listeKundennachrichten`, `findeKundennachricht`, `ladeFaden`). Wenn die
Domaene `aufgaben-nachrichten-oeffentlich` einen internen Nachrichtendienst
anlegt, waeren zwei Dinge nuetzlich, die ich dort nicht vorwegnehmen wollte:
(a) eine Fadenabfrage, die `thread_id` als Wurzel nimmt und je Faden die
neueste Zeile plus die Zahl der Antworten liefert — die Kundenliste ist heute
flach nach Zeitpunkt sortiert, weil ein Faden-Posteingang die interne
Darstellung ist; (b) eine Stelle, die beim Anlegen einer Kundennachricht
`kunde_id` SETZT. Ohne sie bleibt jeder neue Faden im Kundenportal unsichtbar,
und zwar still — das ist genau der K-18-Fehlermodus, und `0255` kann ihn nur
lesbar machen, nicht befuellen.

## Seed — was fehlt, und warum ich es nicht geschrieben habe

`src/server/db/seed/**` ist unangetastet: `pnpm db:seed` darf ich nicht
ausfuehren, und Seedcode, den ich nicht laufen lassen kann, bricht im
Zweifelsfall den Seed fuer alle. Vier von zehn Seiten sind im Demo deshalb
leer oder halb leer. Konkret fehlt:

1. **Ein Bau-Kunde mit `kunde_zugang`** (oder ein zweiter Zugang fuer
   `kunde.demo@example.test`) samt 1–2 `projekt`-Zeilen. Ohne ihn sind
   `/portal/kunde/projekte` und `/portal/kunde/projekte/[id]` ungepruefte Ware
   — der einzige Portalkunde ist ein Reinigungskunde. Der zweite Zugang fuer
   dasselbe Konto ist zugleich die Demo fuer O-52 (ein Login, zwei
   Gesellschaften) und fuer den Gesellschaftsfilter der Rechnungsliste.
2. **1–2 `aufmass`-Zeilen** (Status `gegengezeichnet` und
   `einseitig_festgestellt`) mit `aufmass_zeile` und je einer
   `aufmass_signatur` — die Aufmasshaelfte der Nachweisliste und der
   Vorbehaltsfall sind sonst nie zu sehen.
3. **3–4 `nachricht`-Zeilen mit `kunde_id`** plus `nachricht_empfaenger` auf
   einen Ansprechpartner des Kunden: eine ausgehende, eine eingehende als
   Antwort im selben Faden (fuer die Fadenanzeige), eine mit
   `richtung = 'intern'` (die der Kunde NICHT sehen darf — das macht die
   Demo zur Probe) und eine mit `nachricht_anhang` (fuer die Anlagenzahl).
4. **`kunde.kaeufer_eadresse` + `kaeufer_referenz`** am Portalkunden, sonst
   bleiben PDF und XRechnung im Demo hinter dem 422-Satz.

## Kleinigkeiten, die in DESIGN.md nichts fehlen liessen

Keinen Wert erfunden und keinen vermisst. Zwei Stellen, an denen ich vom
naechstliegenden Vorbild abgewichen bin und warum: (a) `prozentText` rechnet
in GANZEN Zahlen statt `(bp/100).toLocaleString('de-DE', {…})` — der
naheliegende Weg teilt eine ganze Zahl in eine Gleitkommazahl (K-16) und wird
ausserdem von der Zeitzonenwache `anzeige-berlin` getroffen, weil er Zeichen
fuer Zeichen derselbe Aufruf ist, mit dem jemand ein Datum ohne Zone
formatiert; (b) auf `/portal/kunde/zahlungen` fehlt die Pille `Überfällig`,
obwohl DESIGN §5 sie kennt — ob ein Posten ueberfaellig ist, haengt am
Stichtag und nicht am Posten, und die Altersklasse daneben sagt es genauer.

## Eine Wache, die dieser Stapel mitbringt

`tests/kern/kundenportal.test.ts` liest den QUELLTEXT der Kundendienste und
haelt drei Zusagen fest, die sich am Rueckgabewert nicht pruefen lassen: kein
`from`/`join` auf zwoelf intern-only Tabellen, kein `app.aktiver_mandant()`
(K-20), und achtzehn interne Spalten stehen in keiner Abfrage. Der Grund ist
K-18: RLS wirkt zeilen-, nicht spaltenweise — die Policy laesst die Zeile zu
RECHT durch, und ein Test gegen eine leere Liste sieht keinen Unterschied
zwischen „nicht sichtbar" und „liegt nichts vor". Wer die Dienste erweitert,
faellt an dieser Datei statt an einem Anruf des Kunden.

## Notizen des Behebender

"**Wie geprüft wurde.** Jeder Befund erst nachvollzogen, dann behoben — Belege stehen je Punkt unter `behoben`. Die Datenbankaussagen sind gegen echtes Postgres gemessen: `w_kupo` (frisch angelegt, `cse.fenster_schluessel` gesetzt, `pnpm db:migrate` bis 0304 sauber durch) für Schema, Constraints und Policies; `w_kupo_seed` (migriert + `pnpm db:seed`) für die Kundensitzung. Die Kundensitzung wurde genau so gebunden, wie `withKundeScope` sie bindet (scope=kunde, kein aktiver Mandant, portal=kunde, readonly, `app.mandant_ids` aus `app.sichtbare_mandanten()`).\n\n**Migration.** `drizzle/0255` nach jeder Änderung gegen eine frisch angelegte `w_kupo` geprüft, zuletzt komplett: `drop database … / create database … / alter database … set cse.fenster_schluessel / DATABASE_URL=… pnpm db:migrate` → „Migrationen angewendet.\" Migrationsnummern innerhalb 0255–0264 geblieben (nur 0255 angefasst, 0256 unverändert).\n\n**Was ich NICHT angefasst habe**, wie vorgegeben: `src/server/registry/*` (dienste.ts, tableiste.ts, navigation.ts, routen.generiert.ts), `src/server/auth/route-manifest.ts`, `src/server/db/schema/rls.ts`, `docs/DECISIONS.md`, `04-SEITENKARTE.md`, `package.json`. Die vollständigen Einträge dafür stehen in `registry_manifest`, `registry_dienste`, `registry_navigation`, `registry_sonstiges`, `registry_rls` und `decisions_zeilen` — auch die aus dem Bauschritt, unverändert übernommen, wo sie noch stimmen. `src/components/portal/PortalRahmen.tsx`, `TabLeiste.tsx` und `src/app/portal/zugang.ts` sind KEINE Registerdateien, aber ihre Änderung ist ohne `KUNDEN_NAVIGATION` nicht übersetzbar — sie gehen deshalb als Text mit, in `registry_sonstiges`.\n\n**Gestaltungswerte.** Kein neuer erfunden. Der einzige Ad-hoc-Wert des Stapels (`mt-[2px]`) ist ersatzlos weg: das Icon wird jetzt über `align-middle` ausgerichtet, der Abstand ist `mr-s2` (DESIGN §3). Der neue Hinweissatz auf der Zahlungsseite benutzt nur vorhandene Tokens (`border-line`, `bg-surface-2`, `p-s4`, `text-sm`, `text-text-muted`, `rounded-lg`). `npx vitest run tests/design` → 58 Proben grün.\n\n**Zustand der Prüfungen.** `pnpm typecheck` sauber. `npx eslint` über alle geänderten Dateien sauber. `npx vitest run tests/kern/kundenportal.test.ts` → 69 grün (vorher 62; sieben neue Wachen). `tests/kern/routen.test.ts` und `tests/kern/portal-shell.test.ts` bleiben rot, bis die Registereinträge zentral stehen — das ist genau der zurückgegebene Teil. `tests/kern/api-verdrahtung.test.ts` bleibt an einer fremden Route rot.\n\n**Zwei Dinge, die anderen Stapeln gehören und hier Zeit gekostet haben.**\n(1) `pnpm db:seed` bricht auf dem aktuellen Stand ab: `new row violates row-level security policy for table \"leistungskatalog_position\"`, ausgelöst von `legeKatalogHierarchieAn` in `src/server/db/seed/vertrieb.ts:552` (die Datei ist von einem Nachbarstapel geändert). Der Abbruch trat schon vor meiner ersten Zeile Code auf. Folge für mich: `tests/isolation/global-setup.ts` baut seine Vorlage über genau diesen Seed, also lässt sich die Isolationssuite derzeit gar nicht starten — die 27 Proben in `tests/isolation/kundenportal.test.ts` sind geschrieben und von Hand gegen echtes Postgres nachgestellt, aber nicht vom Läufer ausgeführt. Wer das aufräumt, sollte danach `pnpm test:isolation tests/isolation/kundenportal.test.ts` nachziehen.\n(2) `npx tsx scripts/guards/run-all.ts` meldet 84 Verstöße, ausnahmslos `[todo-client-nicht-im-register]` — jeder laufende Stapel wartet auf denselben zentralen DECISIONS-Eintrag. Sechs davon gehören mir.\n\n**Ein Befund war unvollständig, nicht falsch** (Details unter `widerlegt`): der Ausgabeweg brauchte drei Seed-Angaben, nicht zwei. BT-81 (`rechnung.zahlungsmittel_code`) wird im ganzen Seed nirgends gesetzt und hat keinen Vorgabewert. Erst mit `zahlungsmittelCode: '58'` in `src/server/db/seed/rechnung.ts` steht `belegAusgabe: { art: 'moeglich' }` und es entsteht ein echtes PDF (28.845 Bytes, Signatur `%PDF-`).\n\n**Eine Anmerkung zur Vollständigkeit:** die mir übergebene Befundliste bricht im letzten Eintrag mitten im Satz ab („sobald das Kundenportal einen bekommt (oder etwas anderes den gemerkten P\"). Den erkennbaren Befund — Routenmuster statt konkreter Adresse an `kundePortal` — habe ich behoben und mit einer Wache versehen. Sollten dahinter weitere Einträge gestanden haben, sind sie mir nicht zugegangen."
